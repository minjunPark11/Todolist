# TickTick 역설계 종합 — Component 01~13

작성일: 2026-08-20
근거 문서: `TICKTICK_COMPONENT_01`~`13` (13개, 약 374KB) + `DESIGN_ELEMENT_INVENTORY.md`
측정 대상: TickTick Web (ticktick.com/webapp), 사용자 실제 계정, dark 테마, ko 로케일

---

## 0. 이 문서가 무엇인가

13개 컴포넌트 문서는 각각 **한 컴포넌트를 끝까지** 판 기록이다. 이 문서는 그 13개를 가로질러 **무엇이 규칙이고 무엇이 아닌지**를 정리한다.

- 개별 수치는 여기 반복하지 않는다. 원본 문서를 가리킨다.
- **13개를 다 보고 나서야 알 수 있는 것**만 적는다.
- 각 컴포넌트에서 기존 문서를 정정한 항목(R-1 ~ R-21)을 한자리에 모은다.

---

## 1. 가장 중요한 결론 — 전역 규칙은 생각보다 적다

13개를 관통하는 규칙은 **다섯 개뿐**이다. 나머지는 전부 **영역별로 다르다.**

이것이 이 시리즈의 핵심 발견이다. 초반 5개(01~05)가 전부 사이드바여서 "전역 규칙"처럼 보이던 항목들이, 본문(06)을 한 번 보자마자 셋이 깨졌고(R-9·R-10·R-11), 떠 있는 레이어(11·12)에서 또 둘이 깨졌다(R-19·R-20·R-21).

**[Inference]** TickTick에는 하나의 디자인 시스템이 아니라 **영역별 규칙 세트가 4벌** 있는 것으로 보인다. 색 체계와 아이콘 방식만 공유하고, 상호작용 표현·간격·전환은 영역이 정한다.

**우리 앱에 주는 함의**: "TickTick처럼"을 한 벌의 토큰으로 옮기려 하면 실패한다. **어느 영역의 규칙인지를 함께 옮겨야 한다.**

---

## 2. 확정된 공통 규칙 (13/13 또는 그에 준함)

| # | 규칙 | 근거 |
|---|---|---|
| **G-1** | **전경색은 흰색 하나이고, 위계는 알파로만 만든다** | 13/13 전 컴포넌트 |
| **G-2** | **흐름 안 요소에는 그림자가 없다** | 13/13. 떠 있는 레이어만 예외(§4.4) |
| **G-3** | **아이콘은 SVG 스프라이트 + `currentColor`** | 7/7 (아이콘이 있는 모든 컴포넌트) |
| **G-4** | **상태는 클래스 교체로 표현한다. `data-*`는 스타일 훅이 아니다** | 6/6. C03에서 실험으로 검증(속성만 바꿔도 배경 불변) |
| **G-5** | **색은 CSS 변수 토큰, 치수는 유틸리티 임의값** | 12/13 (C03의 `rounded-default`만 부분 반례) |

### 2.1 토큰 구조 (C01 §5)

색은 `body`에 **307개의 CSS 변수**로 있고, 구조가 2축이다.

```
--color-<name>            : "255,255,255"  ← RGB 트리플릿
--opacity-variant-<name>-<n> : 0.03 …      ← 알파 22단
                ↓
rgba(var(--color-x), var(--opacity-variant-x-3))
```

`:root`에는 18개뿐이고 그것도 라이트 기본값이다. **테마 스위치는 `body.dark`다.**

**[Observed] 치수(radius·spacing·크기)에 관여하는 custom property는 하나도 없다.** 전부 `w-[50px]` 같은 임의값이거나 인라인 style이다.

---

## 3. 실측 스케일 집계

### 3.1 알파 — 11단

| 알파 | 쓰이는 곳 |
|---|---|
| **3%** | 사이드바 행/헤더/폴더 hover(01·03·04) · **퀵애드 입력 배경**(10) |
| **5%** | rail→사이드바 경계(02) · 태스크 행 구분선(06) · 헤더 아이콘 hover 배경(09) · 본문 행 hover(06) · 메뉴 항목 hover(11) |
| **6%** | 사이드바→본문 경계(02) |
| **8%** | selected 배경(01·04·06) |
| **10%** | 사이드바 내부 구분선(02) · detail 경계·헤더선(07) · 모달 보더·secondary 버튼 보더(12) |
| **20%** | 퀵애드 플레이스홀더(10) |
| **30%** | 섹션 헤더 제목(03) · 오버레이 스크롤바(02·07) |
| **40%** | 카운트·more·chevron(01·03·04) · Rail 아이콘 기본(08) · 컬럼 카운트(13) · 우선순위 없음(06) |
| **60%** | Rail 아이콘 hover(08) · 컬럼 `+` 아이콘(13) · 모달 secondary 텍스트(12) |
| **80%** | 메뉴 항목 텍스트(11) · 모달 폼 라벨(12) |
| **100%** | 본문 텍스트 전반 |

**[Observed] 같은 알파가 여러 의미로 쓰인다** — 3%가 사이드바에서는 hover, 퀵애드에서는 휴지 배경이다(R-18). **"알파 = 의미"의 매핑은 없다.**

**[Inference]** 그나마 일관된 것은 **"비활성 보조 요소 = 40%"** 하나다. 4개 컴포넌트에서 반복됐다.

### 3.2 Radius — 5단

| 값 | 쓰이는 곳 |
|---|---|
| **6** | 섹션 헤더 트리거(03) · Rail 아바타(08) |
| **8** | 메뉴 항목 hover(11) · 모달 버튼(12) · 헤더 아이콘 hover 배경(09) |
| **10** | 사이드바 행(01·04) · 태스크 행/카드 배경(06·13) · 퀵애드 입력(10) |
| **12** | 메뉴 패널(11) · 드래그 오버 마스크(13) |
| **16** | 모달 패널(12) |

**[Inference]** "작은 컨트롤 6~8 → 행 10 → 떠 있는 패널 12~16"의 경향은 있으나, 섹션 헤더(6)와 같은 크기의 행(10)이 다른 등 예외가 있어 **규칙으로 확정하지 않는다.**

### 3.3 타이포 — 6단

| size / weight / line-height | 쓰이는 곳 |
|---|---|
| 11 / — | 모달 유효성 메시지(12) |
| **12 / 400** | 카운트·날짜 힌트(01·06) · 컬럼 카운트(13) |
| **12 / 700** | 섹션 헤더 제목(03) |
| **13 / 400** | 메뉴 항목(11) · 모달 폼 라벨(12) |
| **14 / 400 / 20** | 사이드바 행 라벨(01·04) |
| **14 / 400 / 24** | 태스크 행 제목(06) |
| **14 / 400 / 20** | 칸반 카드 제목(13) |
| **14 / 600** | 칸반 컬럼 제목(13) |
| **16 / 700** | 모달 제목(12) |
| **19 / 700 / 27** | 상세 패널 제목(07) |
| **20 / 600 / 28** | 본문 헤더 뷰 이름(09) — **화면에서 가장 큰 글자** |

**[Observed] 같은 태스크의 제목이 리스트에서 14/400, 상세에서 19/700이다.** 훑는 화면과 읽는 화면의 역할 차이를 타이포로 표현한다.

**[Observed] 13px은 떠 있는 레이어 안에서만 쓰인다**(메뉴 · 모달 폼 라벨). 표본 2개라 규칙으로 확정하지 않는다.

### 3.4 치수 격자 — 규칙 없음

시리즈 전체에서 관찰된 치수: 1 · 2 · 3 · 4 · **5** · 6 · 8 · 10 · 11 · 12 · 14 · 16 · 17 · 18 · 20 · 22 · 24 · 26 · 28 · 30 · 32 · 34 · 36 · 38 · 39 · 40 · 41 · 48 · 49 · 50 · 55 · 56 · 62 · 64 · 70 · …

- Component 01에서 "모든 값이 짝수"였는데 **C05의 5px 리사이즈 핸들**, **C06의 17px 체크박스**가 깼다.
- 4의 배수가 아닌 값이 계속 나온다(6 · 10 · 14 · 18 · 26 · 30 · 34 · 38 · 41 · 49 · 55).
- 468.6 같은 **비정수 런타임 계산값**도 있다.

**[Observed] 8pt 그리드는 지지되지 않는다. 2px 격자조차 예외가 있다.** 13개를 다 보고도 **치수 격자는 확정할 수 없다.**

---

## 4. 영역별 규칙 세트 — 이 시리즈의 실질적 산출물

### 4.1 사이드바 (Component 01·03·04)

| 항목 | 값 |
|---|---|
| 행 높이 / 리듬 | **36 / 38** (36 + margin 2) |
| 섹션 헤더 높이 | **30** |
| radius | 행 **10**, 헤더 **6** |
| 바깥 gutter | **10** (LI 좌우 padding, **클릭 불가 완충대**) |
| 안쪽 padding | 행 12/12, 헤더 14/10 |
| **hover** | **전경색 3%** |
| **selected** | **전경색 8%**, 클래스 교체(`hover:` 변형이 제거됨) |
| selected + hover | **변화 없음** |
| 액션 | 평소 숨김 → hover/focus에서 등장, hit area **16×16** |
| **focus ring** | `outline: auto 1px accent`, **offset 0** |
| transition | **없음 (0s)** |
| 들여쓰기 | depth당 **26** (래퍼 `padding-left`), 우측 rail 고정 |

### 4.2 본문 (Component 06·09·10·13)

| 항목 | 값 |
|---|---|
| 행 높이 | **40** (리스트) / 카드 48~70 (칸반) |
| 행 리듬 | **40** (간격 0, 1px 구분선) / 카드 간격 8 |
| radius | **10** (배경 레이어) |
| **가로 인셋** | **20** (헤더·퀵애드·상세 공통) |
| **hover** | **전경색 5%** ← 사이드바와 다름 |
| selected | 전경색 8% (사이드바와 같음) |
| **focus ring** | **없음** ← 사이드바와 다름 |
| **transition** | **0.2s** 선언 있음 (실제 배경 변화는 즉시) |
| 아이콘 hover | **배경 레이어**(아이콘 + 5px씩), radius 8 |
| 구분선 | 1px 5%, **hover/selected에서 감춤** |

### 4.3 Global Rail (Component 08)

| 항목 | 값 |
|---|---|
| 폭 | **50** |
| 슬롯 / 히트 / 아이콘 | **48×50 / 40×40 / 28×28** |
| 세로 스텝 | **50** |
| 배경 | **`rgb(36,36,36)`** — 본문보다 밝다 |
| 항목 배경·radius | **없음 / 0** (어느 상태에서도) |
| **상태** | **아이콘 opacity 0.4 → hover 0.6 → active 1.0** |
| focus ring | `auto 1px accent`, **offset 1** |
| aria | **`aria-current="page"`** — 시리즈에서 유일한 제대로 된 aria |

### 4.4 떠 있는 레이어 (Component 11·12, +09의 좁은 폭 사이드바)

| 항목 | 메뉴 | 모달 | 공통? |
|---|---|---|---|
| 배경 | `rgb(36,36,36)` | `rgb(28,28,28)` | **불투명이라는 점만 공통** |
| 보더 | 1px **실색** `rgb(54,54,54)` | 1px **알파 10%** | **공통 아님** |
| radius | 12 | 16 | 공통 아님 |
| **box-shadow** | `0 16px 40px rgba(0,0,0,0.2)` | **동일** | **공통** |
| 딤 backdrop | 없음 | **투명 cover** | **공통 — 딤 없음** |
| z-index | 1050 | cover 1050 | 공통 |

**[Observed] 떠 있는 레이어의 확실한 공통점은 두 개다 — 불투명 배경, 그리고 `0 16px 40px rgba(0,0,0,0.2)` 그림자.**

### 4.5 스크롤 — 전 영역 공통 (Component 02·07·13)

| 항목 | 값 |
|---|---|
| 네이티브 스크롤바 | `scrollbar-width: none` → **폭 0, 레이아웃에 영향 없음** |
| 오버레이 스크롤바 | **6px, radius 7, 전경색 30%, `right: 2px`** |
| 표시 규칙 | **hover로는 안 뜸.** 실제 스크롤 시 등장 |
| 숨김 | 포인터가 영역을 벗어나고 **약 2초 뒤**, `opacity 0.3s` 페이드 |
| edge fade / shadow | **없음** |

**[Observed] 이것이 시리즈에서 가장 일관된 컴포넌트다.** 사이드바·상세 패널·칸반 컬럼에서 값이 완전히 동일했다.

---

## 5. 반복 패턴 — "선언은 있는데 변화가 없다"

세 컴포넌트에서 같은 현상이 나왔다.

| 컴포넌트 | 관찰 |
|---|---|
| **C10 퀵애드** | 1px **투명** 보더를 예약해 두고 hover·focus 어디서도 색이 들어가지 않음. cursor도 `text`가 아니라 `auto` |
| **C11 메뉴** | `role="menuitem"` + `tabindex="-1"`이 있으나 **Arrow 키 동작 없음**. `deleteItem` 클래스가 있으나 **위험 색 없음** |
| **C12 모달** | 버튼에 `transition: color… 200ms`가 선언돼 있으나 **hover에서 아무 값도 변하지 않음**(취소·추가 둘 다) |

**[Inference]** 마크업·클래스·전환이 먼저 들어가고 대응 구현이 빠진 지점이 반복된다.

**우리 앱에 주는 함의**: **"TickTick에 클래스가 있으니 기능도 있겠지"라고 가정하고 베끼면 안 된다.** 이 시리즈에서 클래스 이름만 보고 판단했다면 세 번 틀렸을 것이다.

---

## 6. 접근성 — 관찰된 그대로

| 항목 | 상태 |
|---|---|
| `aria-current` | **Rail에만 있음**(C08) |
| `role="menuitem"` | 메뉴 항목에 있음. 단 **컨테이너에 `role="menu"` 없음**, Arrow 키 미동작(C11) |
| `aria-expanded` | **어디에도 없음** — 섹션 헤더(C03)·폴더(C04)·사이드바 접기(C05) 전부 |
| `aria-label` / accessible name | **아이콘 버튼 전부 없음** — 사이드바 more, 섹션 `+`, Rail 항목, 헤더 액션, 접기 버튼 |
| tree semantics | **없음** — `role="tree"`/`treeitem`/`aria-level` 0개(C04) |
| heading | **사이드바에 0개**. 섹션 제목이 `<p>`(C03) |
| 키보드로 접기/펼치기 | **불가** — 섹션(C03)·폴더(C04)·사이드바(C05) 전부 |
| focus ring | 사이드바·Rail에는 있고 **본문 태스크 행에는 없음**(C06) |
| 키보드 resize | **불가** — `role="separator"`·`aria-value*` 없음(C05) |

**[Observed] 접근성은 이 제품의 약한 부분이다.** 우리 앱이 그대로 따라갈 이유가 없는 유일한 영역이고, 각 문서의 Appendix A에 개선안을 분리해 적었다.

---

## 7. 정정 이력 R-1 ~ R-21

각 컴포넌트가 **이전 문서의 서술을 뒤집거나 좁힌** 항목이다. 기존 문서는 수정하지 않았고 후보로만 기록했다.

| # | 출처 | 대상 | 내용 | 성격 |
|---|---|---|---|---|
| R-1 | C04 | C01 | 카운트는 숨겨지는 게 아니라 **조건부 렌더**(태스크 0이면 아예 없음) | 보강 |
| **R-2** | C04 | C03 | "슬롯 폭 = padding-left라 안 밀린다"는 인과가 폴더에는 안 맞는다. 진짜 이유는 **`absolute`** | **정정** |
| R-3 | C04 | C03 | 섹션 헤더 chevron은 평소 opacity 0, 폴더 disclosure는 **항상 1** — 일관되지 않음 | 차이 기록 |
| R-4 | C04 | C02 | 패딩 불균일은 **섹션 레벨**의 문제. 행 레벨(pitch 38)은 균일 | 보강 |
| R-5 | C04 | C01 | 선택된 행은 배경만 불변. **group-hover 액션은 여전히 동작** | 보강 |
| **R-6** | C05 | C02 | collapse가 `margin-left: -240`일 거라는 추론은 **틀렸다.** 실제는 `width: 0` + `left: -W` + `visibility: hidden` | **정정** |
| **R-7** | C05 | C02 | `.lists`의 `margin 0.2s` 전환은 **collapse 경로에서 쓰이지 않는다.** 인라인 `transition: none`이 덮는다 | **정정** |
| R-8 | C05 | C02 | 사이드바 폭 **min 213 / max 468.6**, 클램프는 JS, 저장은 localStorage | 보강 |
| **R-9** | C06 | C01·03·04 | "hover = 3%"는 **사이드바 한정**. 본문은 **5%** | **범위 축소** |
| **R-10** | C06 | C01·03·04 | focus ring은 사이드바 계열에만. **본문 태스크 행은 포커스가 보이지 않는다** | **범위 축소 + 결함** |
| **R-11** | C06 | C01~05 | "transition 없음"(5/5)이 깨짐. 태스크 `LI`에 **0.2s** | **약화** |
| **R-12** | C07 | fidelity audit·C02 | detail pane은 **500px 고정이 아니라 `36%` + `min 320`** | **정정** |
| R-13 | C07 | C02 | 경계 알파가 5·6·**10**% 세 값 — 공통 상수 아님 | 범위 축소 |
| **R-14** | C08 | C01·03·04 | "aria 0개"(4/4)가 깨짐. Rail에 **`aria-current="page"`** | **범위 축소** |
| R-15 | C08 | C01·03·04 | focus ring offset이 **0(사이드바) vs 1(Rail)** | 보강 |
| **R-16** | C09 | C02 | 본문 헤더 높이 49는 **칸반 값**. 리스트는 **64** | **범위 축소** |
| **R-17** | C09 | C02 | "데스크톱에서 overlay 아님"이 깨짐. 좁아지면 **backdrop + 그림자 오버레이**로 전환 | **범위 축소** |
| R-18 | C10 | C01·03 | 3%는 "hover의 값"이 아니라 **"가장 약한 채움"**의 값 | 보강 |
| **R-19** | C11 | C01~08 | "그림자 없음"(9/9)이 깨짐. **떠 있는 레이어에는 그림자가 있다** | **범위 확정** |
| **R-20** | C11 | C02·07 | 경계가 알파 하이라인만은 아니다. 메뉴는 **실색 보더** | **범위 축소** |
| **R-21** | C12 | C11 | "떠 있는 레이어 = 실색 보더"는 틀렸다. 모달은 **알파 보더** | **정정** |

**[Observed] 정정 21건 중 6건이 명백한 오류 정정이고, 나머지는 범위 축소다.** 오류 6건 중 4건(R-2·R-6·R-7·R-21)은 **"클래스 이름이나 선언을 보고 추론한 인과"**가 틀린 경우였다.

**[Inference]** 실측이 아닌 **추론을 문서에 남기면 그 추론이 다음 문서의 전제가 되어 오류가 전파된다.** 이 시리즈에서 `[Observed]`와 `[Inference]`를 분리한 규칙이 실제로 값을 했다 — 틀린 것은 전부 `[Inference]` 쪽이었고, 실측치는 하나도 뒤집히지 않았다.

---

## 8. 측정 신뢰도 — 무엇이 확실하고 무엇이 아닌가

### 8.1 확실한 것

- **모든 `[Observed]` 수치**는 `getBoundingClientRect()` / `getComputedStyle()` 반환값이다. 스크린샷 추정치는 하나도 없다.
- 상태 변화는 **실제 마우스 hover / 실제 Tab·Arrow·Escape 키**로 만들었다.
- 토큰 값은 `body`의 computed custom property에서 직접 읽거나 **probe 요소**로 유틸리티 클래스를 해석했다.

### 8.2 방법상의 한계 (전 컴포넌트 공통)

| 한계 | 영향 |
|---|---|
| **원본 CSS 규칙을 읽지 못함** (CORS, fetch 403) | cascade는 probe 실험으로 대체. "어느 규칙에서 왔는가"는 미확정 |
| **뷰포트를 제어하지 못함** | 반응형 breakpoint 전부 미측정(C02·C05·C09·C12) |
| **탭이 백그라운드** (`document.hidden`) | `requestAnimationFrame` 정지 → 프레임 단위 애니메이션 관찰 불가 |
| **`:active`(누름) 측정 불가** | 마우스 다운 유지 상태를 잴 API가 없음 — 전 컴포넌트 미측정 |
| **일부 실제 클릭이 핸들러에 닿지 않음** | 리사이즈 드래그(C05), 메뉴 열기(C11), 퀵애드 포커스(C10) — 합성 이벤트나 `element.click()`로 대체하고 그 사실을 각 문서에 명시 |

### 8.3 측정 조건이 일정하지 않았다

| 컴포넌트 | viewport |
|---|---|
| C01 | 800 × 805 |
| C02 | 1387 × 713 → 763 × 392 (측정 중 변함) |
| C03 · C04 | 763 × 392 |
| C05 ~ C13 | 1387 × 713 |

C02·C05·C09에서 **페이지 줌이 통제 불가로 튀는 현상**이 반복됐고(최악은 145×54), 새로고침으로만 복구할 수 있었다. **CSS px는 줌에 불변이므로 수치 자체는 유효하지만**, 뷰포트 의존 값(detail 폭 36%, 헤더 높이, 모달 폭)은 해당 뷰포트에서의 값으로 읽어야 한다.

### 8.4 세션에 남긴 것

| 항목 | 상태 |
|---|---|
| **테스트 데이터** | `ZZ Folder` + 리스트 3개 — **C04에서 생성, 아직 남아 있음** |
| 사이드바 폭 | 240으로 복원(C05·C09에서 두 번 덮어써졌다가 복구) |
| detail 폭 | 35.976% (원래 36%, 잔차 0.3px) |
| 그 외 | 사용자 태스크·리스트·설정 **변경 없음** |

---

## 9. 우리 앱 적용 — 우선순위

`DESIGN_ELEMENT_INVENTORY.md` §3의 우선순위를 13개 측정 결과로 갱신한다.

### 9.1 지금 바로 할 수 있는 것 (토큰 몇 줄)

| # | 항목 | 우리 현재 | 목표 | 근거 |
|---|---|---|---|---|
| 1 | Rail 폭 + 아이콘 **함께** | 56 / 24 | **50 / 28** | C08 §7 — **따로 하면 나빠진다** |
| 2 | 사이드바 행 radius | 6 | **10** | C01 |
| 3 | 사이드바 행 라벨 | 13px | **14px / lh 20** | C01 |
| 4 | selected 시 weight | 500 | **400 고정** | C01 |
| 5 | selected 배경 | 고정색 `#eceef3` | **전경색 8% 알파** | C01·C04·C06 (3/3 일치) |
| 6 | hover 배경 | 한 값 | **사이드바 3% / 본문 5% 두 값** | C06 R-9 |
| 7 | 떠 있는 레이어 그림자 | `--shadow-lg` | **`0 16px 40px rgba(0,0,0,0.2)`** | C11·C12 (2/2 동일) |

### 9.2 구조 변경 (설계 판단 필요)

| # | 항목 | 내용 | 근거 |
|---|---|---|---|
| 8 | **행 ↔ 카드 통합** | 배경을 별도 레이어로 뽑고 인셋·불투명도·래퍼 패딩만 뷰별로 바꾼다 | **C13 §9.1** — 인벤토리의 "행 구현 5벌" 문제에 직접 쓰이는 처방 |
| 9 | **바깥 10 + 안쪽 12 2단 인셋** | 선택 배경이 사이드바 가장자리에 닿지 않게 | C01 §11.5 |
| 10 | **카운트 ↔ more 슬롯 공유** | `min-width` 24px 슬롯에서 교대 → 라벨이 밀리지 않음 | C01 §2.10.7 |
| 11 | **detail pane 상시 렌더** | 선택 여부로 리스트 컬럼 폭이 흔들리지 않는다 | C02·C07 |
| 12 | **퀵애드를 스크롤 밖 3단 flex로** | sticky 없이 항상 상단 | C10 §1 |

### 9.3 TickTick을 따라가지 **않을** 것

| # | 항목 | 이유 |
|---|---|---|
| 13 | **본문 태스크 행에도 focus ring** | TickTick은 없다(R-10). 명백한 결함 |
| 14 | **모달에 딤 backdrop** | TickTick은 딤이 없고 모달 배경이 화면 배경과 같은 색이라 분리가 보더+그림자에만 의존(C12) |
| 15 | **아이콘 버튼에 accessible name** | 전 영역에서 없음(§6) |
| 16 | **hit area 16×16 → 24 이상** | WCAG 2.2 SC 2.5.8 미달(C01·C03) |
| 17 | **키보드로 접기/펼치기** | 섹션·폴더·사이드바 전부 마우스 전용(C03·C04·C05) |
| 18 | **Rail 밝기 방향** | TickTick은 Rail이 더 밝다. 우리는 V-1에서 "내비가 콘텐츠보다 어둡게"로 이미 정함 — 뒤집을 이유 없음(C08) |
| 19 | **우선순위를 색 단독으로 표현하지 않기** | TickTick은 체크박스 색만 씀 → 색각 이상 사용자가 구분 불가(C06) |

---

## 10. 아직 측정하지 못한 것 (전체)

| 영역 | 미측정 항목 |
|---|---|
| **상호작용** | `:active`(전 컴포넌트) · 드래그 앤 드롭(C04·C13) · 리사이즈 실제 사람 드래그(C05) |
| **입력 상태** | 퀵애드 입력 중/후 — 스마트 파싱, 액션 슬롯 등장, 높이 확장(**C10에서 가장 흥미로운 부분**) |
| **반응형** | breakpoint 전부. 좁은 폭 오버레이는 우발적으로 한 지점만 봄(C09) |
| **테마** | **라이트 테마 전부** |
| **데이터 의존** | 완료 태스크의 리스트 뷰 렌더(C06) · 폴더 count 합계(C04) · 하위작업·첨부·댓글 섹션(C07) · 알림 배지(C08) |
| **화면** | 날짜 피커 · 검색 · 캘린더 뷰 · 매트릭스 · 습관 · 설정 |
| **cascade** | 원본 CSS 규칙(CORS로 전 컴포넌트 불가) |

---

## 11. 문서 지도

| # | 문서 | 다루는 것 |
|---|---|---|
| — | `DESIGN_ELEMENT_INVENTORY.md` | **우리 앱**의 디자인 요소 전수 인벤토리 + 죽은 CSS 실측 |
| 01 | `TICKTICK_COMPONENT_01_SIDEBAR_ROW.md` | 사이드바 행 — 기하·상태·토큰의 기준점 |
| 02 | `…_02_SIDEBAR_SHELL.md` | 사이드바 셸 — 스크롤·경계·표면 위계 |
| 03 | `…_03_SECTION_HEADER.md` | 섹션 헤더 — 위계 표현·collapse |
| 04 | `…_04_FOLDER_TREE.md` | 폴더/트리 — 들여쓰기 26px의 출처, 이동/접기 분리 |
| 05 | `…_05_SIDEBAR_RESIZE_COLLAPSE.md` | 리사이즈/접기 — min 213 / max 468.6, persistence |
| 06 | `…_06_TASK_LIST_ROW.md` | 태스크 행 — **본문 규칙 세트의 시작** |
| 07 | `…_07_TASK_DETAIL_PANEL.md` | 상세 패널 — 36% 폭, 3단 구조 |
| 08 | `…_08_GLOBAL_RAIL.md` | Rail — opacity 3단, 유일한 aria |
| 09 | `…_09_CONTENT_HEADER_BAR.md` | 본문 헤더 — 뷰별 높이, 좁은 폭 오버레이 |
| 10 | `…_10_QUICK_ADD.md` | 퀵애드 — CodeMirror, 무상태 |
| 11 | `…_11_CONTEXT_MENU.md` | 메뉴 — **떠 있는 레이어 규칙의 시작** |
| 12 | `…_12_MODAL_DIALOG.md` | 모달 — 떠 있는 레이어 규칙의 정정 |
| 13 | `…_13_KANBAN_BOARD.md` | 칸반 — **행↔카드 전환 방법** |
| — | **이 문서** | 13개를 가로지른 규칙·정정·적용 우선순위 |

---

## 12. 다음에 할 만한 것

측정 관점에서 값이 큰 순서:

1. **라이트 테마 전수** — 알파 기반 체계가 라이트에서 어떻게 뒤집히는지. 지금 문서 전부가 다크 전용이다.
2. **퀵애드 입력 중 상태**(C10 미측정분) — 테스트 태스크를 만들었다 지우면 볼 수 있다.
3. **날짜 피커** — 남은 주요 프리미티브 중 가장 복잡하다.
4. **`:active` 상태** — 측정 수단을 찾으면 전 컴포넌트에 소급 적용된다.

구현 관점에서는 §9.1의 7개가 토큰 수정만으로 끝나므로 그것부터가 맞다.
