# 막대가 어느 리스트의 일인지 말하고, 누르면 그 자리에서 열린다

> 상태: **구현 완료 — §11의 1·2·3·4** · 2026-09-04
> (I1 = B · I2 = C · I3 = 셋 다 · I4 = 유지 · I5 = A · I6·I7 = 넣음 · I8 = 아니오 · I9 = C, 사용자 승인)
> (사용자가 한 장을 주었다: 타임라인 목업 — 주간 7열, 행마다 색 점과 이름, 막대 안에
> 날짜 범위, 오늘 열에 알약과 세로선, 리스트별 색.)
> 요청: "타임라인 디자인을 이렇게. **좌측 패널은 내 것 그대로**. 특정 task를 누르면
> **팝업으로 정보가 뜨게**."
> 대상: `components/{TimelineView,TaskGanttView}.tsx` · `components/tasks/TasksModule.tsx` ·
> `domain/view/timeline.ts` · `styles/12-timeline.css`
> 선행 문서: `GANTT_TIMELINE_DESIGN.md`(§11.2 막대 안 글자 · D3/D11 가로 스크롤 거부 ·
> D5 추론된 시작 · D7 의존성) · `BOARD_TASK_POPUP_DESIGN.md`(§4.1 팝업의 근거 —
> **이 문서가 그 근거를 타임라인에 그대로 적용한다**) ·
> `CALENDAR_FILL_READABILITY_DESIGN.md`(§I1의 충돌)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 목업 1장 | [관찰] |
| **실측** | `--bar-color`를 세팅하는 곳이 **없다** · 막대 클릭이 앵커 없이 `openTask`를 부른다 · `taskDetailPresentationFor`가 보드에만 팝업을 준다 · 열 라벨이 `MM.DD` · `projectItems`가 반복을 펼치지 않는다 | [실측] |
| **결정** | 사용자 — 목업대로, 좌측 패널은 유지, 클릭은 팝업 | [결정] |
| **추론** | 목업이 안 보여주는 것들(의존성 화살표·Arrange 패널)이 사라진 것인지 그냥 안 찍힌 것인지 | [추론] |

---

## 1. 먼저 사실 하나 — 모든 막대가 같은 색이다 [실측]

```css
/* 12-timeline.css:154 */
background: var(--bar-color, var(--accent));
```

`--bar-color`를 **쓰는 곳은 있는데 세우는 곳이 없다.** `grep '--bar-color' --include=*.tsx`가
빈 결과다. 그래서 모든 막대가 폴백인 액센트 하나로 그려진다 — 실제로 띄워서 확인했다.

**사용자 확정: 막대 색은 리스트의 색을 따른다.** 아래는 그것이 왜 지금 안 되고 있는지와,
어떤 값으로 칠할 것인지(§5, I2)다.

[관찰]의 목업이 타임라인처럼 읽히는 이유의 절반이 색이다. 논문 포프젝트는 보라, FocusFlow
개발은 파랑, 운동은 분홍 — **행을 세지 않고도 무슨 일의 묶음인지 보인다.** 우리 화면은
파란 막대 열두 개다.

그리고 그 색은 이미 있다. `colorForList`(캘린더 작업에서 만든 것)가 리스트의 색을 —
아무도 칠하지 않은 리스트까지 — 결정적으로 답한다. `Item.listId`도 이미 모든 행에 있다.
**세울 값이 없어서가 아니라 세우는 줄이 없어서 안 되고 있었다.**

---

## 2. 팝업은 이미 지어져 있다 [실측]

요청의 나머지 절반은 거의 배선 문제다.

```tsx
// TasksModule.tsx:1590 — 보드만 팝업을 받는다
presentation={taskDetailPresentationFor(mode, state.view === "board" ? "board" : "list")}
// TasksModule.tsx:1409 — 타임라인은 앵커 없이 연다
onOpenItem={(item) => openTask(item.sourceId)}
// TasksModule.tsx:524 — 앵커를 받을 준비는 되어 있다
function openTask(taskId: string, anchor?: Rect) { setTaskAnchor(anchor ?? null); … }
```

`taskDetailPresentationFor`는 `surface !== "list"`이면 `anchored-popover`를 준다. 타임라인은
`"list"`로 넘어가고 있어서 오른쪽 컬럼으로 열린다.

**그리고 `BOARD_TASK_POPUP_DESIGN.md` §4.1의 논거가 타임라인에 그대로 맞는다**: 팝업이
필요한 이유는 "밑에 있는 것이 폭을 내줄 수 있느냐"이고, 보드의 칼럼은 못 내준다. 타임라인의
열도 못 내준다 — D3/D11이 **열 개수를 고정하고 가로 스크롤을 거부**했기 때문에, 오른쪽에
컬럼을 하나 내주면 그만큼 **날짜가 좁아진다.** 실측한 바로 그 문제다(§11.1: 격자 438에서
트랙이 210px).

즉 타임라인은 보드보다 팝업이 **더** 필요한 화면이다.

### 2.1 필요한 것

| 무엇 | 어디 |
|---|---|
| 막대가 자기 사각형을 넘긴다 | `TimelineView`의 막대 `onClick` → `getBoundingClientRect()` |
| 뷰가 그것을 위로 올린다 | `TaskGanttView`의 `onOpenItem(item, rect)` |
| 모듈이 앵커와 함께 연다 | `openTask(item.sourceId, rect)` |
| 표현을 팝업으로 | `state.view === "board"` → `state.view === "list" ? "list" : state.view` 꼴로 |

캘린더가 이미 같은 일을 한다(`CalendarView.tsx:701`) — 그 줄을 그대로 따라 쓰면 된다.

---

## 3. 목업과 우리의 차이 [관찰] vs [실측]

| | 목업 | 지금 |
|---|---|---|
| 막대 색 | **리스트별** | 액센트 하나 (§1) |
| 막대 안 | **날짜 범위** (`8.31 – 9.3`) | 제목 (80px 이상일 때) |
| 막대 모양 | 연한 틴트, 둥근 모서리 | 진한 액센트, 작은 라운드 |
| 행 라벨 | **색 점** + 이름 | 이름만 |
| 열 머리 | `8.30 (일)` — 요일 포함 | `08.30` |
| 오늘 | **알약 + 열 배경 + 세로선** | 글자색 + 셀 배경 |
| 줌 | 일 / 주 / 월 | 일 / 주 / 월 / 반년 / 1년 |
| 반복 일정 | **분리된 막대 셋** (바이올린 연습) | 태스크당 막대 하나 |
| 클릭 | **팝업** | 오른쪽 컬럼 |
| 의존성 화살표 | 안 보임 | 있음 |
| Arrange 패널 | 안 보임 | 있음 (날짜 없는 일이 있을 때) |

---

## 4. 막대 안에 무엇을 쓰나 — 결정 I1

`GANTT_TIMELINE_DESIGN.md` §11.2는 **제목**을 넣기로 했고, 그 근거를 이렇게 적었다:

> "라벨 열이 **언제나 읽히는** 제목이므로, 중복은 반대쪽에서 없앤다."

그 문장은 사실 목업 쪽을 가리킨다. 라벨 열이 제목을 이미 말하고 있으면 막대 안의 제목은
**같은 말의 두 번째 사본**이고, 목업은 그 자리에 아직 화면에 없는 것 — 날짜 — 을 넣는다.

| 안 | 막대 안 | 결과 |
|---|---|---|
| **A** | 제목 (지금) | 한 행에 제목이 두 번. 좁으면 사라져 아무것도 안 남는다 |
| **B** | **날짜 범위** (목업) | 제목 한 번 + 날짜 한 번. 좁으면 날짜가 사라지고 제목은 라벨 열에 남는다 |

> **권장 B.** §11.2의 논거를 끝까지 따르면 나오는 답이다. 덤으로 §11.3이 만든 우회로
> (이름을 잃은 막대의 툴팁에 제목을 앞에 붙인 것)가 필요 없어진다 — 이름은 언제나 왼쪽에
> 있다.
>
> 하루짜리는 목업처럼 날짜 하나만(`9.3`), 여러 날은 범위(`8.31 – 9.3`). 컨테이너 쿼리
> 임계(80px)는 그대로 쓰되 값을 다시 재야 한다 — 날짜 범위는 제목보다 폭이 예측 가능해서
> 임계가 더 정확해질 수 있다.

### 4.1 그 범위의 단위는 줌이 정한다 [결정]

**사용자 결정: 주별 줌은 날짜, 일별 줌은 시간.**

이건 목업을 넘어서는 제안인데, 우리 줌 모델이 이미 그렇게 생겼다 [실측]:

```ts
// domain/view/timeline.ts:44
day:      { unit: "hour",  columns: 24 },   // ← 열 하나가 한 시간
week:     { unit: "day",   columns: 7 },
month:    { unit: "week",  columns: 5 },
halfYear: { unit: "month", columns: 6 },
year:     { unit: "month", columns: 12 },
```

그리고 `day`에 붙은 주석이 이미 같은 말을 한다:

> "One day, hour by hour (§15). **The only zoom where the record's times are
> legible at all** — above it an hour is under a pixel."

일별 줌에서 막대의 **폭이 곧 시간 길이**다. 그 막대에 `9.3 – 9.3`이라고 쓰는 것은 화면
전체가 이미 말하고 있는 것을 반복하는 것이고, 반대로 시간은 그 화면에서만 읽을 수 있는
유일한 정보다. 데이터도 이미 있다 — `Span`이 `startTime`/`endTime`을 들고 다닌다.

| 줌 | 열 단위 | 막대 안 |
|---|---|---|
| **일** | 시간 | `14:00 – 16:00` |
| **주** | 일 | `8.31 – 9.3`, 하루면 `9.3` |
| 월 · 반년 · 1년 | 주 · 월 | 주와 같게 (날짜) |

#### 결정 I9 — 일별 줌에서 시간이 없는 태스크

`Span.startTime`/`endTime`은 `""`일 수 있다. 그런 태스크는 일별 줌에서 24열 전체를
차지하는 막대가 된다.

| 안 | 막대 안 |
|---|---|
| **A** | 날짜 (`9.3`) — 화면 전체가 이미 그 날짜다 |
| **B** | 아무것도 |
| **C** | **`종일`** |

> **권장 C.** A는 동어반복이고, B는 옆 막대들이 시간을 말하는 화면에서 이 막대만 침묵해
> "왜 얘만 없지"가 된다. `종일`은 **막대가 왜 하루를 다 차지하는지**를 설명하는 유일한
> 답이고, 캘린더의 종일 칸이 이미 쓰는 낱말이라 두 화면이 같은 말을 같은 단어로 한다.

---

## 5. 막대 색 — 결정 I2, 그리고 캘린더와의 충돌

리스트 색을 쓰는 것은 **사용자가 확정했다**(§1). 남은 문제는 **어떻게 칠하느냐**다 —
리스트 색을 배경으로 쓰는 방법이 셋이고, 셋 다 "리스트 색대로"라는 문장을 만족한다.

[관찰]의 막대는 **연한 틴트에 진한 글씨**다. 캘린더는 하루 전에 그 반대로 갔다 — 꽉 찬
채움에 흰 글씨, 이유는 회색 블록의 검은 글씨가 안 읽힌다는 지적이었다
(`CALENDAR_FILL_READABILITY_DESIGN.md`). **같은 앱의 두 화면이 같은 리스트 색을 반대
방식으로 칠하게 된다.**

| 안 | 타임라인 막대 | 캘린더와 |
|---|---|---|
| **A** | 목업대로 연한 틴트 + 진한 글씨 | 어긋난다 |
| **B** | 캘린더처럼 진한 채움 + 흰 글씨 | 같다. 목업과 다르다 |
| **C** | 연한 틴트 + 진한 글씨, 단 대비는 계산해서 보장 | 어긋나지만 읽히기는 한다 |

> **권장 C.** 두 화면이 다른 것은 **밀도가 다르기 때문**이라고 본다. 캘린더 블록은
> 격자 위에 겹겹이 쌓이고 시간대가 곧 위치라 서로 구별돼야 하지만, 타임라인은 **한 행에
> 하나**다. 행이 이미 분리돼 있으므로 막대가 소리칠 필요가 없고, 열두 개의 진한 막대는
> 목업의 조용함을 잃는다.
>
> 다만 A를 그대로 하면 어제의 문제가 여기서 재현된다 — 연한 노랑 위 회색 글씨. 그래서
> C다: `darkenForWhiteInk`의 형제로 **`tintForDarkInk(hex)`**를 만들어, 리스트 색을
> 배경으로 옅히되 **그 위 글씨가 4.5:1을 넘는 것을 계산으로 보장**한다. 어제 만든 순수
> 함수와 테스트가 그대로 재사용된다.

---

## 6. 오늘 표시 — 결정 I3

[관찰]은 셋을 겹친다: 열 머리의 **파란 알약**, 열 전체의 **연한 배경 띠**, 격자를 가로지르는
**세로선**.

지금은 글자색과 `--accent-soft` 셀 배경 둘뿐이고, 세로선이 없다.

> **권장: 셋 다.** 세로선이 타임라인에서 하는 일이 크다 — 막대가 오늘을 지났는지가 한 눈에
> 보이고, 그건 이 화면이 답해야 하는 질문이다. 알약은 캘린더가 이미 같은 모양을 쓰고
> 있어서(주간 뷰의 날짜 배지) 두 화면이 같은 말을 같은 그림으로 하게 된다.
>
> 세로선은 `TimelineConnectors`가 이미 쓰는 절대배치 오버레이에 얹는다 — 새 레이어를
> 만들지 않는다.

---

## 7. 목업이 안 보여주는 것들 — 결정 I4

[추론]. 목업에 없는 것이 "빼자"인지 "안 찍힌 것"인지는 그림으로 알 수 없다.

| 무엇 | 지금 | 빼면 |
|---|---|---|
| 의존성 화살표 (D7) | 선행/후행을 잇고 모순이면 다르게 그림 | 이 앱의 타임라인이 캘린더와 달라지는 유일한 축을 잃는다 |
| Arrange tasks 패널 | 날짜 없는 일을 끌어다 놓는 자리 | 날짜 없는 일이 화면에서 사라진다 |
| 반년 / 1년 줌 | 있음 | 긴 프로젝트를 한 화면에 못 본다 |

> **권장: 셋 다 유지.** 목업은 열두 개 태스크가 모두 날짜를 가진 한 주를 그린 그림이라
> 셋 다 나올 이유가 없는 화면이다. Arrange 패널은 날짜 없는 일이 있을 때만 나타나고,
> 화살표는 의존성이 있을 때만, 반년/1년은 고르지 않으면 안 보인다 — **셋 다 조건부라
> 목업과 모순되지 않는다.**

---

## 8. 반복 일정 — 결정 I5

[관찰]의 `바이올린 연습`은 **떨어진 막대 셋**이다. 반복 일정이 회차마다 그려진 것으로
보인다 [추론].

우리 `projectItems`는 반복을 펼치지 않는다 [실측] — 태스크 하나가 막대 하나다. 캘린더는
펼친다(`buildCalendarItems`).

| 안 | 무엇 |
|---|---|
| **A** | 이번 범위에서 제외. 지금처럼 막대 하나 |
| **B** | 창 안의 회차를 펼쳐 막대 여러 개로 |

> **권장 A — 제외.** B는 타임라인의 다른 모든 것과 충돌한다: 막대를 끌면 **어느 회차의**
> 날짜를 쓰는가? 의존성 화살표는 **어느 막대**에 붙는가? 회차 하나를 완료하면? 캘린더는
> 이 질문들이 없어서 펼칠 수 있다(끌기가 그 날짜만 바꾸고, 화살표가 없다).
>
> 반복을 타임라인에서 펼치려면 그 세 질문에 답부터 해야 하고, 그건 이 문서가 아니라
> 자기 문서를 가질 크기다.

---

## 9. 남은 작은 것들 — 결정 I6~I8

| | 무엇 | 권장 |
|---|---|---|
| **I6** | 행 라벨에 리스트 색 점 | **넣는다.** `colorForList`로, Tasks 사이드바가 이미 같은 점을 쓴다 |
| **I7** | 열 머리에 요일 (`8.30 (일)`) | **넣는다.** 주간 줌에서만 — 월/반년/1년 열은 하루가 아니라 요일이 없다 |
| **I8** | 타임라인을 최상위 목적지로 | **아니오** — §9.1 |

### 9.1 목업의 좌측 사이드바는 우리 것이 아니다

[관찰]의 사이드바에는 `타임라인`이 **최상위 항목**으로 있다. 우리는 그것을 없앴다 —
`TaskGanttView`의 머리 주석이 "사이드바가 최상위 Timeline을 그만 제공하면서 전역
`TimelinePage`가 사라졌다"고 기록한다.

사용자의 요청은 "**좌측 패널은 내 것 그대로**"였다. 그러므로 목업의 사이드바는 따르지
않고, 타임라인은 지금처럼 스코프의 뷰(`?view=gantt`)로 남는다. 이 문서는 격자와 막대와
팝업만 건드린다.

---

## 10. 무엇을 테스트하는가

**단위 (vitest)**
- `tintForDarkInk`: 리스트 프리셋 8색 + Inbox 회색이 진한 글씨로 4.5:1 이상 (I2-C)
- 막대 안 문자열: 하루는 `9.3`, 여러 날은 `8.31 – 9.3`, 줌마다 (I1-B)
- 열 라벨: 주간 줌에 요일이 붙고 월간 줌에는 안 붙는다 (I7)

**E2E (playwright)**
- 서로 다른 리스트의 막대가 서로 다른 색이다 (§1의 회귀 — 지금은 전부 같다)
- 막대를 누르면 **팝업**이 열리고, 오른쪽 컬럼은 열리지 않는다 (I 요청의 본체)
- 팝업이 막대 옆에 뜬다 — 앵커가 실제로 쓰였는지
- Escape로 닫히고 초점이 막대로 돌아온다
- 오늘 세로선이 오늘 열 위에 있다
- 날짜 없는 일이 있으면 Arrange 패널이 여전히 나온다 (§7의 회귀)

---

## 11. 구현 순서

1. **팝업** (§2) — 요청의 본체이고, 배선 네 줄이다. 이것만으로 화면이 달라진다.
2. **막대 색** (§1, §5) — `tintForDarkInk` + `--bar-color`를 세운다.
3. **막대 안 날짜** (§4) — §11.2의 문장을 그 결론까지.
4. **행 색 점 · 요일 · 오늘 세로선** (§6, §9) — 그림을 목업에 맞추는 마무리.

1과 2만 해도 "무슨 일인지 색으로 보이고, 누르면 그 자리에서 열린다"는 요청의 두 문장이
끝난다.

### 11.1 하면서 나온 것 [실측]

설계에 없던 두 가지가 구현 중에 드러났고, 둘 다 고쳤다.

- **`Show completed`가 아무것도 안 하고 있었다.** 타임라인이 받던 `rows`는
  `queryScopeTasks(scope, ctx)`이고 그건 완료된 일을 미리 떨어뜨린다. 스위치가 이미
  비어 있는 집합을 지배하고 있었던 것. 타임라인일 때만 `{ finished: true }`로 다시
  묻는다 (`e2e/ganttCompleted.spec.ts`).
- **팝업으로 바꿨더니 빈 Detail 컬럼이 남았다.** 예약 컬럼의 조건(`state.view !==
  "board"`)과 표현의 조건이 따로 쓰여 있어서, 타임라인은 480px를 비워 둔 채로 좁게
  앉아 있다가 막대를 누르면 오히려 **넓어졌다** — 컬럼을 예약하는 이유의 정반대.
  두 조건을 `detailSurface` 한 값으로 합쳤다 (`e2e/ganttBars.spec.ts`가 폭이 안 변하는
  것을 잰다).
- **80px 임계를 실제로 재보니 하나로는 안 됐다.** 12px/600에서 가장 긴 줄
  (`12.31 – 12.31` + 완료 `✓` + 손잡이 12px)이 103px, 그 줄의 앞 절반이 69px.
  임계를 정직하게 104로 올리면 옛 임계보다 더 많은 막대가 글자를 잃는다. 그래서 둘로
  나눴다 — 104 위는 범위 전체, 72~104는 앞 절반(`8.31 –`), 그 아래는 없음.
- **막대에서 제목을 빼면 스크린 리더가 이름을 잃는다.** 보이는 글자는 날짜지만 버튼의
  `aria-label`은 `제목 · 날짜`로 둔다.
- **일별 줌에서 `오늘` 열 표시는 자정을 가리키고 있었다.** 24열이 전부 같은 날짜라
  `columnOf`가 0열을 답한다. 그 줌에서는 열 표시를 하지 않고 세로선이 순간을 맡는다.

---

## 12. 결정 목록

| | 질문 | 안 | 권장 |
|---|---|---|---|
| **I1** | 막대 안에 무엇 | A 제목(지금) / B 날짜 범위(목업) | **B** — 확정 |
| **I9** | 일별 줌에서 시간 없는 태스크 | A 날짜 / B 공백 / C `종일` | **C** |
| **I2** | 막대를 어떻게 칠하나 | A 연한 틴트 / B 캘린더처럼 진하게 / C 연한 틴트 + 대비 보장 | **C** |
| **I3** | 오늘 표시 | 알약 + 배경 + 세로선 셋 다 / 일부 | **셋 다** |
| **I4** | 화살표 · Arrange · 반년·1년 줌 | 유지 / 제거 | **유지** |
| **I5** | 반복 일정을 회차마다 그리나 | A 제외 / B 펼침 | **A** |
| **I6** | 행 라벨의 색 점 | 넣는다 / 안 넣는다 | **넣는다** |
| **I7** | 열 머리의 요일 | 넣는다 / 안 넣는다 | **넣는다 (주간 줌만)** |
| **I8** | 타임라인을 최상위 목적지로 | 예 / 아니오 | **아니오** (요청대로 좌측 패널 유지) |
