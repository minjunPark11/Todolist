# 오늘 — 카드 여섯 장에서 목록 한 장으로

> 상태: **설계 · 1~5단계 구현됨** · 2026-09-01
> (사용자가 참조 앱의 Today와 우리 `/app`을 나란히 놓고 "이렇게 재설계하자"고 했다.
> 두 화면의 차이는 기능이 아니라 **모양과 그룹**이었고, 기능은 넷 다 살린다.)
> 대상: `components/TodayPage.tsx` · `components/today/*` · `styles/04-today.css`

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 사용자가 준 스크린샷 2장 — 참조 앱의 Today, 우리 `/app`의 Today | [관찰] |
| **실측** | `TodayPage.tsx` · `today/FocusQueue.tsx` · `04-today.css` · `railNav.ts` · `viewGroups.ts` · `scopeQuery.ts` | [실측] |
| **인용** | `TICKTICK_STYLE_…_v16_E2E_IMPLEMENTATION_PLAN.md` §7.5 · §7.6 · V29 · V30 | [인용] |
| **추론** | 위 셋으로 메운 것 | [추론] |

참조 앱 스크린샷은 **한 장**이고, 그 한 장은 **할 일이 셋뿐인 날**이다. 정렬 메뉴가
무엇을 여는지, 행에 마우스를 올리면 무엇이 나오는지, 목록이 길 때 무엇이 달라지는지는
**보이지 않았다**. 아래에서 [관찰]로 적는 것은 그 한 장에 실제로 있는 것뿐이다.

---

## 1. 참조 앱의 Today [관찰]

### 1.1 머리

```
☰  Today                                        💡   ↕   ⋯
```

제목 한 줄, 오른쪽 끝에 아이콘 셋. **날짜 부제가 없다.** 검색창이 없다. 큰 추가
버튼도 없다.

셋 중 가운데는 위아래 화살표 — 정렬이다 [추론]. 오른쪽은 `⋯`. 왼쪽 것은 전구처럼
보이는데 **무엇인지 판별할 수 없다.** 이 문서는 그것을 짓지 않는다.

### 1.2 추가는 목록의 첫 줄이다

```
+  Add Task
```

머리 바로 아래, 목록 폭 전체, 흐린 글씨 한 줄. **추가 진입점은 이것 하나다.**

### 1.3 그룹은 날짜에서 나온다

```
⌄ Overdue  2                                          Postpone
  ☐ ADSE Final exam            학교  ▤   May 22
  ☐ LAC Lab 6                  학교      Aug 20

⌄ Today's Habit  1
  ☐ 운동                                   Today
```

- 그룹 머리 = `⌄` 펼침 화살표 · 굵은 이름 · **흐린 개수**
- `Overdue` 머리의 오른쪽 끝에 **`Postpone`** — 그룹 하나에 붙은 행동이다
- 그룹 이름이 `Now / Next / Later`가 아니라 **왜 이것이 오늘 목록에 있는가**다

### 1.4 행

`☐ 제목` … 오른쪽에 `리스트 이름 · 아이콘 · 날짜`.

- **행이 카드가 아니다.** 테두리도 배경도 없고, 구분선 하나가 **체크박스 열 다음부터**
  시작한다
- 열려 있는 행만 배경이 밝다(`ADSE Final exam` — 오른쪽 상세에 열린 그 작업이다)
- 체크박스 **테두리 색이 행마다 다르다** — 주황과 파랑. 우선순위인지 리스트 색인지는
  이 한 장으로 판별 불가
- 기한이 지난 날짜는 빨강, 오늘인 것은 파랑
- 본문이 있는 작업에 `▤` 아이콘 하나
- 목록 아래는 **빈 공간이다.** 채우는 카드가 없다

---

## 2. 우리 `/app` [관찰 + 실측]

```
오늘
2026년 9월 1일 화요일

🔍 [오늘 목록에서 찾기          ]     [ + 추가 ]

┌ + [빠르게 적어두기 — 예: …]  [오늘] [추가] ┐
│ Enter 저장 · Alt+Enter 자세히 입력 · Esc 지우기 │
└──────────────────────────────────────────┘

┌ ☰ 오늘 할 일                            ⋯ ┐
│  ● 오늘 완료  3                            │
│  ┌ ✓ 새로운 이벤트          오전 6:45  ⋯ ┐ │
│  ┌ ✓ Lecture 1   보통  60분 오전 8:00 ⋯ ┐ │
│  ┌ ✓ Lecture 2   보통      오전 9:00 ⋯ ┐ │
└──────────────────────────────────────────┘

┌ 받은함 정리  14              [ 지금 정리 ] ┐
│  [회의] [Leetcode 3,4] [Lecture 6] +11    │
└──────────────────────────────────────────┘
```

세 겹이다: **페이지 안에 카드, 카드 안에 그룹, 그룹 안에 또 카드인 행.** 참조 앱은
한 겹이다.

### 2.1 추가하는 길이 둘이다 [실측]

`tdy-add` 버튼(`TodayPage.tsx:392`)은 `QuickAddTaskModal`을 열고, 그 아래 `InlineCapture`
카드는 자기 입력을 갖는다. 같은 화면에서 같은 일을 하는 두 컨트롤이고, 참조 앱에는
하나다.

### 2.2 그룹이 다른 질문에 답한다 [실측]

`FocusQueue`는 `BUCKETS = ["now", "next", "later"]`로 나눈다(`FocusQueue.tsx:12`).
**사용자가 손으로 넣는 칸**이고, 그것을 채우는 것이 `Plan Today`다.

계획서는 다른 것을 권한다 [인용: §7.5]:

> 기본적으로 과도한 시간대 그룹은 사용하지 않는다. 권장:
> `기한 지남` ← 있을 때만 / `오늘` / `완료됨` ← 선택적, 접힘

그리고 그 그룹의 근거는 이미 질의에 있다 [실측: `scopeQuery.ts:215`] — *"Today는
`dueDate == today`가 아니다: 기한 지남 + 오늘 마감 + 오늘 계획이다."* 셋을 한 목록에
담아 놓고 **왜 담겼는지는 화면에서 말하지 않는다.**

### 2.3 그리고 그 그룹을 만드는 것이 이미 있다 [실측]

`domain/view/viewGroups.ts`가 `overdue · today · tomorrow · later · none · someday ·
completed`로 나누고, 완료는 5개씩 접어 보여 준다(`COMPLETED_PAGE`). 매트릭스가 쓰고
Inbox 보드가 쓴다. **Today가 세 번째가 되는 것이지 새 규칙을 만드는 것이 아니다.**

### 2.4 Today가 둘이다 [실측]

| | 주소 | 어떻게 도달하나 |
|---|---|---|
| 모듈의 Today | `/today` | **레일의 `Tasks`**(`railNav.ts:36` `TASKS_HOME`) |
| 이 문서의 Today | `/app` | 시작 페이지 설정 · `t` 단축키 · 집중 페이지의 "Go to Today" |

`TASK_DETAIL_PANEL_MERGE_DESIGN.md` §6.1이 "둘 중 어느 쪽이 살아남는지는 P0-2의
질문"이라고 적어 두었다. **이 문서는 그 질문을 열지 않는다** — 사용자가 `/app`을
고치기로 정했고, 합치는 결정은 이 재설계가 끝난 뒤에도 그대로 남아 있다.

다만 여기 적어 둔다: 이 문서가 끝나면 두 Today는 **더 닮아 있을 것**이다. 같은
`viewGroups`로 그룹을 만들고 같은 모양의 행을 그리게 되므로, 합치는 일이 그만큼
싸진다.

---

## 3. 결정

### 3.1 겹을 하나로 줄인다

| 지금 | 다음 |
|---|---|
| `오늘 할 일` 카드 안의 목록 | **목록이 곧 페이지다.** 카드 껍질을 벗긴다 |
| 각자 테두리를 가진 행 카드 | 테두리 없는 행 + 체크박스 열 다음부터 시작하는 구분선 |
| 검색창 + 큰 `추가` 버튼이 머리에 | 머리는 제목과 아이콘. 추가는 목록의 첫 줄 |

`받은함 정리`는 **카드로 남는다.** 그것은 오늘의 할 일이 아니라 옆에 있는 다른 일이고,
참조 앱에 대응 개념이 없으니 카드를 벗기라고 말하는 근거도 없다.

### 3.2 머리 — 제목 하나와 아이콘 둘

```
오늘  9월 1일 화                                    ↕   ⋯
```

- **날짜는 남기되 한 줄로 내린다.** 참조 앱에는 없지만, 없는 것이 지우라는 근거는
  아니다 — 잠든 노트북을 열었을 때 "오늘"이 며칠인지 말해 주는 것은 이 화면의 일이고,
  제목 옆이면 줄을 하나도 쓰지 않는다
- **`↕`(정렬·그룹)** — §3.4의 축 선택이 여기 들어간다
- **`⋯`** — `완료 표시`, `목록에서 찾기`
- **전구 아이콘은 짓지 않는다.** 무엇인지 못 봤다(§1.1)

**검색은 `⋯` 뒤로 들어간다.** 사용자가 살리기로 한 기능이고, 참조 앱 머리에는 없으며,
앱의 전역 검색은 레일에 따로 있다. 상시 노출된 입력이 머리 폭의 절반을 쓰는 것이 지금
모양이고, 열었을 때만 목록 위에 한 줄로 나오는 것이 다음 모양이다 [추론].

### 3.3 추가는 한 곳이다

`+ 작업 추가` 한 줄이 목록 맨 위에 선다. 큰 파란 버튼과 `InlineCapture` 카드가 **그 한
줄로 합쳐진다.**

지금 카드가 들고 있는 것을 잃지 않는다 — 줄에 **포커스가 가면** 펼쳐진다:

```
포커스 전:   + 작업 추가
포커스 후:   + [빠르게 적어두기 — 예: 내일 오후 3시 팀 회의]  [오늘]  [추가]
             Enter 저장 · Alt+Enter 자세히 입력 · Esc 지우기
```

`오늘` 토글도 힌트 줄도 살아 있고, 쓰지 않는 동안 두 줄을 돌려받는다. `Alt+Enter`는
그대로 `QuickAddTaskModal`을 연다.

### 3.4 그룹은 날짜가 기본, 버킷은 고를 수 있게

계획서 §7.5의 셋이 기본이다:

```
기한 지남   ← 있을 때만
오늘
완료됨      ← 접힘, 5개씩
```

`viewGroups.ts`의 `groupTasks`가 그대로 만든다 — Today는 `overdue`와 `today`만 담기
때문에(`scopeQuery.ts:215`) `tomorrow` 이후 버킷은 자연히 비고, 빈 그룹은 그리지 않는
규칙이 이미 있다.

**버킷은 사라지지 않는다.** `↕` 메뉴가 축을 고른다:

| 축 | 그룹 |
|---|---|
| **날짜**(기본) | 기한 지남 · 오늘 · 완료됨 |
| **내 계획** | 지금 · 다음 · 나중 · 완료됨 |

`Plan Today` · `모두 나중으로` · `계획 지우기`는 **`내 계획` 축을 골랐을 때만** 나온다.
축이 날짜일 때 계획을 편집하는 버튼은 보이지 않는 것을 바꾸는 버튼이다.

정렬은 계획서 §7.6이다 [인용]: `TaskDailyPlan.sortKey` → 시간 → 우선순위 → 안정 순서.

### 3.5 `기한 지남` 그룹은 자기 행동을 갖는다

[관찰] §1.3의 `Postpone`. 그룹 머리 오른쪽 끝, 그 그룹에만.

우리 말로는 **`오늘로 미루기`**이고, 하는 일은 그 그룹의 모든 작업의 마감을 오늘로
옮기는 것이다. 되돌리기가 붙는다 — 한 번의 클릭이 열 개의 날짜를 바꾸므로, §9.35가
말하는 "상태를 복원하는 undo"가 가장 필요한 자리다.

### 3.6 행 — 배지는 오른쪽으로 모인다

지금 행은 왼쪽부터 `체크박스 · 제목 · 이유 배지 · 예상 · 시각`이다. 다음은 참조 앱의
배치다:

```
☐  제목                          이유   리스트  ▤   날짜
```

이유 배지(`기한 지남` · `진행 중` · `대기` · `차단됨`)는 **살린다** — 참조 앱에는 없지만
이 앱이 오늘 목록에 무엇을 왜 담았는지 말하는 방식이고, 담는 규칙 자체가 우리 것이다.
다만 제목 옆이 아니라 **오른쪽 메타 뭉치의 첫 자리**로 간다. 제목이 먼저 읽혀야 한다.

`기한 지남` 배지는 **그룹이 이미 말하므로 날짜 축에서는 그리지 않는다.** 같은 사실을
두 번 말하는 것이 지금의 모양이고, 그룹이 생기면 그중 하나가 남는다.

### 3.7 시간 레일은 상세에 자리를 내준다

`.tdy-body`가 `minmax(0, 1fr) 340px`이고(`04-today.css:117`), 상세가 열리면
`page-grid`가 그 바깥에서 480px을 더 가져간다 — **1280폭에서 세 열**이 된다.

레일은 **상세가 칸일 때 물러난다.** 레일은 곁눈질하는 보조이고 상세는 방금 연 것이므로,
좁아져야 하는 쪽은 열지 않은 쪽이다 [추론]. 상세를 닫으면 돌아온다.

### 3.8 짓지 않는 것

- **전구 아이콘** — 무엇인지 못 봤다
- **체크박스 색** — 참조 앱이 우선순위로 칠하는지 리스트 색으로 칠하는지 판별 불가.
  우리 행의 체크박스는 지금 한 색이고, 근거가 생기기 전까지 그대로다
- **`Today's Habit`** — 참조 앱의 그룹 이름 하나가 습관 기능을 가리키는데, 이 앱에는
  습관이 없다. 그룹 이름을 흉내 내는 것은 없는 기능의 자리를 그리는 일이다

---

## 4. 단계

| 단계 | 내용 | 왜 이 순서인가 |
|---|---|---|
| **1** | 그룹을 `viewGroups`로 — `기한 지남 / 오늘 / 완료됨`, 축은 아직 날짜 고정 | **완료**(§7). 화면의 뼈대가 바뀌는 유일한 단계이고 도메인은 이미 있다 |
| **2** | 껍질 벗기기 — `오늘 할 일` 카드와 행 카드를 없애고 구분선으로 | **완료**(§8). 1단계의 그룹이 자리를 잡은 뒤에 그 그룹을 그리는 규칙을 바꾼다 |
| **3** | 머리 — 제목 한 줄 + `⋯`. 검색이 `⋯` 뒤로 | **완료**(§9). `↕`는 **짓지 않았다** — 5단계가 고를 축을 주기 전까지는 아무 일도 하지 않는 컨트롤이다(§9.1) |
| **4** | 추가 한 줄 — `InlineCapture`가 목록 첫 줄이 되고 `추가` 버튼이 사라진다 | **완료**(§10). 머리가 정리된 뒤라야 추가가 어디로 갈지 정해진다 |
| **5** | `↕`의 축 선택 — 날짜 ↔ 내 계획. `Plan Today`류가 축을 따라간다 | **완료**(§11) |
| **6** | `기한 지남`의 `오늘로 미루기` + 되돌리기 | 그룹에 붙는 행동이므로 그룹이 먼저 |
| **7** | 레일이 상세에 자리를 내준다 | 나머지와 독립. 언제 해도 된다 |

1단계만으로도 값이 있다 — 오늘 목록이 **왜 이것들이 여기 있는지** 말하기 시작한다.

---

## 5. 열린 질문

| | 질문 | 언제 |
|---|---|---|
| ~~Q1~~ | ~~`오늘` 그룹의 이름~~ — **닫혔다.** `오늘`도 머리를 단다(사용자 확정). 세 그룹이 같은 모양이다 |  |
| Q2 | 체크박스 색이 우선순위인가 리스트인가 | 스크린샷 한 장 더 |
| ~~Q6~~ | ~~열린 행에 배경을 줄까~~ — **닫혔다**(§9.4). 준다. `openedTaskId`가 `AppPages`에서 내려오고 `.is-open`이 accent-soft를 칠한다 |  |
| Q3 | `오늘로 미루기`가 옮기는 것은 `dueDate`인가 `startDate`까지인가. 범위를 가진 작업의 기한만 옮기면 시작이 끝보다 뒤가 된다 | 6단계 |
| ~~Q4~~ | ~~축을 고른 것을 기억하는가~~ — **닫혔다**(§11.2). `AppSettings.todayGroupAxis` 하나. 기기가 아니라 계정에 붙는다 |  |
| ~~Q5~~ | ~~검색이 `⋯` 뒤로 들어가면 단축키와 어떻게 만나는가~~ — **닫혔다**(§9.3). 문제는 `/`가 아니라 **Cmd+K**였고, 이 페이지가 그 키를 돌려줬다 |  |

---

## 6. 이 문서가 확인하지 않은 것

- 참조 앱의 정렬 메뉴와 `⋯`가 무엇을 여는지. 스크린샷은 둘 다 닫힌 상태다
- 목록이 길 때 참조 앱이 무엇을 하는지 — 스크린샷의 날은 할 일이 셋이다
- 행에 마우스를 올렸을 때 나오는 것. §8.5가 우리 행 메뉴에 대해 물은 것과 같은 질문이
  여기에도 있다
- **두 Today를 합치는 결정**(§2.4). 이 문서는 열지 않았고, 끝나도 그대로 남는다

---

## 7. 1단계 구현 기록 (2026-09-01)

### 7.1 그룹을 만드는 자리를 바꿨을 뿐이다

`FocusQueue`가 `BUCKETS`로 나누던 것을 `viewGroups`의 `groupIdOf`로 바꿨다. 세 번째
호출자가 된 것이지 네 번째 어휘를 만든 것이 아니다.

정렬은 **건드리지 않았다.** `groupTasks`를 쓰지 않고 `groupIdOf`만 쓴 이유가 그것이다 —
그 함수는 나누면서 정렬도 하는데, Today의 순서는 계획서 §7.6(계획의 `sortKey` → 시각)
이고 그것은 `collectTodayEntries`가 이미 적용해서 보낸다. 1단계가 다시 정렬하면 **이미
답해진 질문에 두 번째 답**을 주는 셈이다.

### 7.2 시험이 이름과 다른 답을 냈다 — 그리고 그게 맞았다

`날짜 없이 오늘로 계획된 작업`을 넣은 시험의 이름은 "`오늘`에 들어가야 한다"였는데
실행 결과는 **`날짜 없음`**이었다. `groupIdOf`가 틀린 게 아니다 — **틀린 질문을 한
것이다.**

`groupIdOf`는 작업 일반에 대해 답한다: 마감이 없으면 `날짜 없음`, 다음 주면 `이후`.
보드에서는 맞는 답이다. 그런데 이 화면의 모든 작업은 셋 중 하나의 이유로 여기 있고
(§12.5.1), 그중 둘이 하필 그 라벨을 만든다:

| 작업 | `groupIdOf` | 실제로 여기 있는 이유 |
|---|---|---|
| 마감 없이 오늘로 계획됨 | `날짜 없음` | **읽는 사람이 오늘 할 일로 넣었다** |
| 다음 주 마감, 오늘로 계획됨 | `이후` | 〃 |

`날짜 없음`은 읽는 사람이 한 일의 **반대**를 말한다 — 앱이 언제인지 모른다는 뜻이니까.
그래서 이 화면에는 `todayGroupOf`가 하나 있다: **늦지도 끝나지도 않았으면 `오늘`이다.**
그러면 정확히 계획서 §7.5의 셋이 남는다.

이 규칙을 `viewGroups`에 넣지 않고 화면에 둔 이유는, 그것이 성립하는 근거가 **Today의
멤버십에 계획이 포함된다는 것**이기 때문이다. 매트릭스 칸이나 Inbox 열에는 해당하지
않고, `viewGroups`는 그 두 보드의 규칙이기도 하다.

### 7.3 `완료` 코드 경로가 하나 사라졌다

전에는 완료가 그룹 순회 **바깥의 두 번째 블록**이었다. `groupIdOf`가 완료를 날짜보다
먼저 답하므로 이제 그냥 마지막 그룹이고, `showCompleted` 토글은 **그것을 그릴지**만
정한다. 무엇이 담기는지가 아니라.

덕분에 "지난주 마감이던 것을 오늘 끝냈다"가 `기한 지남`이 아니라 `완료`로 간다 —
전에도 그랬지만 그건 두 블록이 우연히 합의한 결과였고, 지금은 한 함수의 규칙이다.

### 7.4 이름 하나를 고쳤다

그룹 라벨의 i18n 키가 `matrix.group.*`였다. 모듈은 이미 `matrixGroups.ts` →
`viewGroups.ts`로 이름을 옮겼는데(그 파일의 머리말이 왜인지 적어 두었다) 키는 그대로였고,
**Today가 세 번째 사용처가 되는 순간**이 그 이름을 고칠 때다. `view.group.*`으로 바꿨다 —
키 7개 × 2개 언어, 호출부 3곳.

그리고 카탈로그의 고아 시험이 일을 했다: `today.doneToday`와 `todayv.bucket.{now,next,
later}` 넷이 읽는 곳을 잃었다. **5단계가 버킷 축을 되살릴 때 다시 필요하지만 지금은
지웠다** — "나중에 쓸 것"은 그 시험이 막으려는 바로 그 상태다. 되살릴 때 세 줄을 다시
적는 편이 예외를 두는 것보다 싸다.

### 7.5 버킷은 살아 있다

`BUCKETS` 상수와 행의 `이동` 메뉴, 1/2/3 키는 그대로다. §3.4가 버킷을 다른 축으로
남기기로 했고, 스토어가 여전히 그 필드를 들고 있다 — 쓰는 곳을 없애면 값이 사라지는
게 아니라 **닿을 수 없게** 된다.

### 7.6 검증

- 유닛 **2,198개 통과**(신규 7: 그룹의 이름·순서·개수·빈 그룹·완료 우선·토글) · `tsc` 통과
- 실제 앱(dev, en, `/app`): 다섯 작업을 심고 `Overdue 2 · Today 1 · Completed 1`.
  2026-08-20 마감이던 완료 작업이 `Completed`에 있다
- 콘솔 오류는 업데이터의 CORS 하나뿐

### 7.7 남은 것

2단계 — 껍질 벗기기. `오늘 할 일` 카드와 행 카드를 없애고 구분선으로 바꾼다.

---

## 8. 2단계 구현 기록 (2026-09-01)

### 8.1 표면만 바꿨다 — 아무것도 옮기지 않았다

이 단계는 **겉면 하나**다. 마크업의 자리는 그대로고, 무엇이 표면을 갖는지만 바뀐다.

| | 전 | 후 |
|---|---|---|
| 목록 | `.tdy-card` — 배경·테두리·그림자·패딩 | 표면 없음. 목록이 곧 페이지다 |
| 행 | 테두리 1px · 반경 12 · `--bg-surface` · 8px 간격 | 표면 없음. 간격 0, 사이에 선 하나 |
| 행 hover | 테두리가 accent로 | 배경이 `--bg-surface-muted`로 |
| `받은함 정리` · 시간 레일 | 카드 | **그대로 카드** |

`받은함 정리`가 카드로 남는 이유는 §3.1에 적은 그대로다 — 그것은 오늘의 할 일이
아니라 옆에 있는 다른 일이고, 참조 앱에 대응이 없으니 벗기라고 말하는 근거도 없다.

### 8.2 선은 제목이 시작하는 곳에서 시작한다

참조 앱의 구분선은 행의 왼쪽 끝이 아니라 **체크박스 다음부터**다(§1.4). 체크박스 열이
목록 전체를 관통하는 한 줄로 읽히는데, 선이 그것을 가로지르면 그 열이 사다리의 가로대로
쪼개진다.

`12px 패딩 + 22px 상자 + 12px 간격 = 46px`. 실측으로 확인했다 — 두 번째 행의 제목이
행의 왼쪽에서 **정확히 46px**에 있고, 선도 거기서 시작한다.

### 8.3 첫 시도는 선을 하나도 그리지 않았다 [실측]

`.tdy-row + .tdy-row::before`로 썼는데 **아무것도 매치되지 않았다.** 행마다
`.motion-task-row` 래퍼가 하나씩 있어서 — 들어오고 나갈 때 애니메이션을 하려고 —
**행들은 서로 형제가 아니다.**

브라우저에서 계산값을 읽어 보고서야 알았다(`left: auto`, `border: 0px`). 셀렉터를
`.tdy-rows > * + * > .tdy-row::before`로 바꿨다. CSS가 조용히 아무 일도 안 하는 종류의
실패라, 눈으로 보고 지나갔으면 "선이 원래 얇은가 보다"로 끝났을 것이다.

### 8.4 hover가 선을 먹는다

행에 배경이 들어오면 그 위아래 선이 **칠해진 사각형을 가로지르는 줄**로 보인다. 그래서
hover한 행의 위 선과 아래 선을 투명하게 한다 — 칠이 하나의 모양으로 읽힌다.

### 8.5 설계가 빠뜨린 것 — 열린 행

§1.4가 **열린 행만 배경이 밝다**고 관찰해 놓고 §3.6이 그것을 결정하지 않았다. 매트릭스는
`selectedTaskId`를 받아 `.is-open`을 그리는데(그리고 작업 모듈도), Today는 그 값을 받지
않는다.

2단계에서 짓지 않았다 — 설계에 없는 것을 구현하면서 채우는 것이 이 문서들이 피하는
일이다. §5에 열린 질문으로 넣는다.

### 8.6 남은 거짓말 하나

목록의 머리가 아직 `.tdy-card-head`라는 이름을 쓴다. 카드가 아닌데 카드의 머리 클래스를
쓰는 셈이고, 그 클래스는 `받은함 정리`와 시간 레일이 **아직 진짜로 카드라서** 공유하고
있다. 3단계가 이 머리를 페이지 머리로 옮기면서 이 사용처가 없어진다.

### 8.7 검증

- 유닛 2,198개 통과(신규 0 — 표면은 jsdom이 재지 못한다) · `tsc` 통과
- 디자인 불변 E2E 9개 통과(표면 위계 · 반경 스케일 · 수직 리듬)
- 실제 앱(dev, `/app`) 계산값: 목록 `border 0 · background transparent · box-shadow
  none · padding 0`, 행 `border 0 · background transparent`, 행 간격 `0`,
  구분선 `left 46px · right 12px · 1px solid`, 첫 행에는 선 없음

### 8.8 남은 것

3단계 — 머리. 제목 한 줄 + `↕` + `⋯`, 검색이 `⋯` 뒤로.

---

## 9. 3단계 구현 기록 (2026-09-01)

### 9.1 `↕`는 짓지 않았다

단계 표가 3단계에 `↕`를 적어 뒀는데, **5단계 전까지 그것이 고를 축이 없다.** 아무 일도
하지 않는 컨트롤을 먼저 놓고 나중에 배선하는 것은 이 리포가 방금 지운 것과 같은
모양이다(설정 화면 Phase 10, "아무 일도 하지 않던 토글"). 5단계가 축을 만들 때 함께
붙인다.

그래서 3단계의 머리는 **제목 · 날짜 · `⋯`** 셋이다.

### 9.2 머리에서 나간 것 둘

| | 어디로 |
|---|---|
| 검색 입력 | `⋯ › 오늘 목록에서 찾기`. 열면 목록 위에 한 줄로 나오고 포커스가 간다. Escape는 **닫으면서 비운다** — 비어 있는 채로 남은 상자는 두 번 치워야 하는 필터다 |
| 큰 `+ 추가` 버튼 | 없앴다. `InlineCapture`가 이미 같은 일을 하고, 4단계가 그것을 목록의 첫 줄로 만든다 |

그리고 목록이 갖고 있던 `⋯`가 페이지의 것이 됐다. `오늘 할 일`이라는 제목도 함께
사라졌다 — `오늘`이라는 페이지 제목 아래의 `오늘 할 일`은 같은 낱말 두 번이다.

### 9.3 Q5의 답은 `/`가 아니라 Cmd+K였다

문서가 물은 것은 `/`였는데, 실측해 보니 문제는 다른 키였다. **`Cmd/Ctrl+K`에 window
리스너가 둘 걸려 있었다** — `TodayPage`가 자기 검색에 포커스를 주고, `App`이 전역 명령
메뉴를 여는 것(§10.6). 둘 다 실행됐다.

입력이 화면에 있는 동안에는 넘어갈 수 있는 일이었다. 입력이 `⋯` 뒤로 들어가면 **보이지도
않는 것에 포커스를 주는 단축키**가 된다.

그래서 이 페이지가 그 키를 돌려줬다. 메뉴가 그 키의 주인이다 — 레일이 앱의 검색 입구
하나를 갖는 것과 같은 이유이고, 작업 헤더에 두 번째 검색 버튼이 서 있던 것을 P0-6이
없앤 것과 같은 중복이다.

### 9.4 Q6 — 열린 행에 배경을 준다

§8.5가 열어 둔 것을 닫았다. `openedTaskId`가 `AppPages`에서 내려오고(매트릭스가 이미
같은 값을 `selectedTaskId`로 받는다) 행이 `.is-open`을 단다.

이게 필요해진 것은 상세가 **옆칸**이 됐기 때문이다. 상세가 자기 스크림을 가진
오버레이였을 때는 무엇에서 왔는지 화면에 보였고, 지금은 목록 옆의 판이라 **돌아갈 줄이
없다.**

CSS 순서가 걸렸다: `.tdy-row:hover`와 `.tdy-row.is-open`은 **특이도가 같다.** 먼저 쓴
쪽이 지므로 처음 배치에서는 열린 행을 가리키면 회색으로 죽었다. hover 규칙 **뒤로**
옮겼다.

### 9.5 측정이 한 번은 거짓말을 했다

`.is-open`을 넣고 계산값을 읽었더니 `rgba(0,0,0,0)` — 규칙은 시트에 있고, `matches()`가
참이고, 특이도도 높은데 배경이 없었다. 한참 파고들다 전체 새로고침을 하니 정상이었다.
**HMR이 갈아끼운 시트가 낡아 있었던 것**이고 앱의 문제가 아니었다.

2단계에서 계산값이 진짜 버그를 잡았고(구분선이 하나도 안 그려졌다) 3단계에서는 없는
버그를 만들어 냈다. 둘의 차이는 **새로고침을 했는가**뿐이다 — CSS를 계산값으로 잴 때는
전체 새로고침 뒤에 잰다.

### 9.6 리듬 시험이 잡은 것

`⋯`가 카드 머리 밖으로 나오자 `verticalRhythm`이 깨졌다: `.ff-icon-btn`은 **28px**이고
컨트롤 그리드는 32다.

28은 §4.87의 **섹션 액션** 크기다 — 매트릭스 칸의 두 아이콘과 보드 열의 `+`가 옆에 있는
22px 배지에 맞춘 숫자다. 페이지 머리의 `⋯`는 30px 제목 옆에 서 있으므로 그 조항이
아니라 §11.2의 컨트롤이다. 32로 올렸다.

전에 안 걸린 이유가 재미있다 — 이 메뉴는 **큐가 비어 있지 않을 때만** 그려졌고, 시험
계정의 그 날은 할 일이 없었다.

### 9.7 검증

- 유닛 2,198개 통과 · `tsc` 통과 · 디자인 불변 E2E 9개 통과
- 고아 키 둘(`todayv.addTaskAria` · `todayv.focusQueue`) 삭제
- 실제 앱(dev, en, `/app`): 머리가 `Today · Tuesday, September 1, 2026 · ⋯`,
  머리 안에 입력 없음. `⋯` → 다섯 줄, `Filter today's lists` → 목록 위에 입력이 나오고
  **포커스가 간다**. 행을 누르면 `?task=`가 붙고 그 행이 `rgba(0, 122, 255, 0.1)`로 칠해진다

### 9.8 남은 것

4단계 — 추가 한 줄. `InlineCapture`가 목록의 첫 줄이 되고, 포커스가 가면 펼쳐진다.

---

## 10. 4단계 구현 기록 (2026-09-01)

### 10.1 자리를 옮긴 것이 아니라 접었다

`InlineCapture`는 이미 목록 위에 있었다. 문제는 위치가 아니라 **항상 펼쳐져 있었다는
것**이다 — 카드 하나, 목적지 토글, `추가` 버튼, 그 아래 힌트 한 줄이 아무도 타이핑하지
않는 날에도 서 있었다.

| | 접힘 | 펼침 |
|---|---|---|
| 표면 | 없음. 아래 행들과 같은 46px, 같은 패딩 | 카드로 돌아온다 — 그때 담는 것이 폼이고, 폼은 가장자리를 원한다 |
| placeholder | `작업 추가` | `빠르게 적어두기 — 예: …` |
| 버튼 | 0개 | 목적지 토글 + `추가` |
| 힌트 | 없음 | `Enter 저장 · Alt+Enter 자세히 입력 · Esc 지우기` |
| `+` 아이콘 | 22px, 배경 없음 | 26px 칩 |

펼치는 조건은 `포커스 || 내용 있음`이다. 두 번째가 필요한 이유는, 적어 놓고 잠깐 다른
곳을 클릭한 것도 **여전히 쓰는 중**이기 때문이다. 그 아래에서 `추가` 버튼을 접으면
말하는 도중에 컨트롤을 빼앗는 셈이 된다.

Escape는 한 키가 두 일을 한다: 내용이 있으면 비우고 열린 채로, 비어 있으면 접는다.
힌트 줄이 약속하는 그대로다.

### 10.2 빈 날의 버튼이 가리키는 곳을 바꿨다

큐가 비었을 때 나오던 `작업 추가` 버튼은 **전체 폼(`QuickAddTaskModal`)을 열고 있었다.**
바로 위에 추가 줄이 있는데 빈 화면에 두 번째 추가 경로를 두는 셈이라, 이제 그 줄에
포커스를 준다. 버튼이 아니라 **이정표**다.

머리에 있던 큰 `추가` 버튼은 3단계에서 이미 사라졌으므로, 이 화면의 추가 경로는 하나다.

### 10.3 브라우저가 이 단계를 잴 수 없었다

포커스로 열리는 줄인데, 이 프로젝트가 모는 브라우저 창은 **표시되지 않는 탭**이다:

```
document.hasFocus()  → false
visibilityState      → "hidden"
input.focus()        → activeElement는 옮겨지고 focusin은 발화하지 않음
```

전체 새로고침을 해도, 탭을 앞으로 가져와도 같았다. 포커스를 받을 수 없는 페이지에서는
**포커스가 하는 일 전부**를 잴 수 없다. §9.5의 낡은 시트와는 다른 종류다 — 그건 측정
방법이 틀렸고, 이건 측정할 수 없는 환경이다.

그래서 이 단계의 증거는 jsdom이다. `userEvent`가 진짜 포커스 이벤트를 보낸다. 세션에서
같은 벽을 만난 세 번째다(Escape 키 §8, Cmd+K, 그리고 이것) — **키보드와 포커스는
Playwright나 jsdom으로 잰다**가 이 리포의 규칙이 될 만하다.

### 10.4 검증

- 유닛 **2,204개 통과**(신규 6: 접힘 · 펼침 · 다시 접힘 · 내용이 있으면 유지 ·
  Escape 두 단계 · Enter와 Alt+Enter가 그대로) · `tsc` 통과
- 디자인 불변 E2E 9개 통과
- 실제 앱(dev, en, `/app`) 접힌 상태: `placeholder "Add task"`, 높이 48,
  `border transparent · background transparent · box-shadow none`, 버튼 0개, 힌트 없음

### 10.5 남은 것

5단계 — `↕`와 축 선택(날짜 ↔ 내 계획). 3단계가 `↕`를 미뤄 둔 이유가 이것이고,
`Plan Today`류가 축을 따라가는 것도 여기다.

---

## 11. 5단계 구현 기록 (2026-09-01)

### 11.1 규칙이 화면에서 나왔다

1단계는 `todayGroupOf`를 `FocusQueue` 안에 두었다. 그때는 축이 하나뿐이라 분기가 아니라
보정이었다. 축이 둘이 되는 순간 그것은 **설정되는 규칙**이 되고, `viewGroups.ts`가 자기
머리말에 적어 둔 문장이 그대로 적용된다 — *"컴포넌트에 사는 그룹핑 규칙은 설정하려면
다시 써야 하는 규칙이다."*

그래서 `domain/view/todayGroups.ts`가 생겼다: 축 둘, 축별 순서, 한 작업의 그룹, 그리고
그룹 이름의 i18n 키.

**`viewGroups`에 축을 하나 더 넣지 않았다.** 그 모듈은 매트릭스와 Inbox 보드의 것이기도
하고, 그 둘에는 계획이 없다. `plan`이 Today를 읽는 정당한 방법인 이유는 **Today의
멤버십에 계획이 들어 있다는 것**이고, 그것은 다른 어디에도 해당하지 않는다.

### 11.2 Q4 — 값 하나, 계정에

`AppSettings.todayGroupAxis`. 칸별이 아니라 하나인 이유는 Today가 목록 하나이기
때문이고, 기기가 아니라 계정에 붙는 이유는 그 옆의 `matrixQuadrantViews`가 대는 이유와
같다 — 데스크톱에서 고른 읽는 방식은 노트북에서도 골라져 있어야 한다.

로드할 때 검사한다. 모르는 값(더 새 클라이언트, 혹은 망가진 저장소)은 **없는 것으로**
읽히고, 없는 것은 `date`다.

### 11.3 세 버튼이 보이는 곳이 좁아졌다

`Plan Today` · `모두 나중으로` · `계획 지우기`는 이제 **`내 계획` 축일 때만** 나온다.

이 셋은 1단계 이후로 **보이지 않는 것을 고치고 있었다.** 그룹이 날짜가 된 뒤에도 메뉴에
남아서, 누르면 아무 변화도 화면에 나타나지 않는 버튼이었다. 3단계가 그것을 알면서도
옮기기만 한 이유는 §9에 적었다 — 그때 빼면 `Plan Today`에 입구가 아예 없어진다.
축이 생긴 지금이 그것을 되돌릴 자리다.

### 11.4 `↕`는 `⋯`과 다른 메뉴다

같은 `MoreMenu` 컴포넌트가 글리프만 바꿔 두 번 선다. `⋯`은 목록에 **작용하는** 것들을
담고, `↕`은 목록의 **모양**을 바꾼다 — 참조 앱도 둘을 나란히 두지 한쪽을 다른 쪽 안에
넣지 않는다(§1.1).

고른 축에 `✓`가 붙는다. 라디오 그룹을 새로 만들지 않은 것은 §15.44의 같은 이유다 —
메뉴 한 줄이 이미 상태를 말할 수 있으면 그것으로 족하다.

### 11.5 1단계가 지운 키 셋이 돌아왔다

`todayv.bucket.{now,next,later}`. 1단계에서 읽는 곳을 잃어 고아 시험이 지웠고, 이제
읽는 곳이 생겨 다시 적었다 — §7.4가 예고한 그대로다. 세 줄이었다. 예외를 두었으면
그동안 아무도 읽지 않는 키 셋이 카탈로그에 남아 있었을 것이다.

`today.doneToday`는 **돌아오지 않았다.** 완료 그룹은 양쪽 축에서 `view.group.completed`를
쓴다 — 같은 그룹이므로 같은 낱말이다.

### 11.6 검증

- 유닛 **2,208개 통과**(신규 4: 계획 축의 그룹 · 완료 우선 · 빈 상자 · 날짜를 보지 않음)
  · `tsc` 통과 · 디자인 불변 E2E 9개 통과
- 실제 앱(dev, en, `/app`):
  - 머리에 `↕`(32px)과 `⋯`(32px)
  - `↕` → `✓ Group by date` · `Group by my plan`
  - `내 계획` 선택 → 그룹이 `Now · Next · Completed`로 바뀌고 저장소에
    `todayGroupAxis: "plan"`
  - 그 축의 `⋯` → 다섯 줄(계획 셋 포함), 날짜 축의 `⋯` → **두 줄**

### 11.7 남은 것

6단계 — `기한 지남`의 `오늘로 미루기`와 되돌리기. 7단계 — 레일이 상세에 자리를 내준다.

