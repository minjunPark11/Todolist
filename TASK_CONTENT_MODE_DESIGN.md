# 제목 옆의 아이콘 — 설명과 체크리스트 사이

> 상태: **1~3단계 구현됨 · Q3만 열려 있음** · 2026-08-31
> (1단계 = 아이콘을 제목 우측으로, 커밋 `f445659`. 2단계 = §10(Q4). 3단계 = §11(Q1 · Q2),
> §12가 그 후속 — 레거시 패널이 체크리스트를 편집한다. 남은 것은 §7 Q3.)
> 대상: 작업 상세의 본문을 **설명 ↔ 체크리스트**로 바꾸는 컨트롤

## 0. 근거 수준

| 등급 | 무엇 |
|---|---|
| **관찰** | 사용자가 준 스크린샷 3장 (우리 앱 1 · 참조 앱 전/후 2) |
| **실측** | 우리 소스 — `contentMode.ts`, `TaskDrawer.tsx`, `usePlannerData.setTaskContentMode` |
| **스펙** | `TICKTICK_TASK_DETAIL_SPEC.md` §11 (§11.1~§11.20) |
| **추론** | 위 셋으로 메운 것 |

---

## 1. 가장 중요한 발견 — 이건 새 기능이 아니다

**§11.4가 이 아이콘을 이미 그려 뒀다.**

```text
Task Title                      ☷
                                ↑
                          Content mode
```

그리고 변환 기계도 전부 있다:

| 스펙 | 코드 |
|---|---|
| §11.8~§11.12 텍스트 → 항목 (불릿·번호·마크다운 체크박스) | `checkItemDraftsFromText` ✅ |
| §11.17~§11.19 항목 → 텍스트 (`- [x]` 형태로 되돌릴 수 있게) | `descriptionFromCheckItems` ✅ |
| §11.13 활성 내용은 한 종류만 | `setTaskContentMode` ✅ |
| §11.14 한 트랜잭션, 중간 상태 없음 | `setData` 한 번 ✅ |
| §11.15 Undo 하나로 되돌림 | `setData`가 스냅샷을 밀어 넣는다 ✅ |
| §11.20 체크박스처럼 보이는 텍스트를 몰래 읽지 않음 | 사용자가 요청할 때만 실행 ✅ |

**빠진 것은 §11.4 하나 — 진입점의 자리와 모양이다.** 지금은 본문 헤더 안에
`메모 | 체크리스트` 라벨 두 개짜리 세그먼티드로 앉아 있다([TaskDrawer.tsx:418](src/components/tasks/TaskDrawer.tsx:418)).

즉 이 작업은 **구현이 자기 스펙으로 돌아가는 일**이지, 없던 것을 만드는 일이 아니다.

---

## 2. 스크린샷에서 읽은 것 [관찰]

### 2.1 우리 것

```text
메모                      [ 메모 | 체크리스트 ]
┌──────────────────────────────────────────┐
│                                          │
└──────────────────────────────────────────┘
```

라벨이 두 번 나온다 — 왼쪽에 "메모", 오른쪽 버튼에도 "메모". 그리고 컨트롤이
**본문 안**에 있어서 제목과 멀다.

### 2.2 참조 앱 — 전

제목 `AIM Final exam`, 본문에 평문 `TB226`. 우측 상단에 `☰` 아이콘 하나.

### 2.3 참조 앱 — 후

- 본문이 `설명`(회색 placeholder) **그리고** 그 아래 `☐ TB226`
- 상세 상단 좌측에 문서 아이콘이 **새로 생김**
- **리스트의 그 행**도 바뀐다 — 체크박스가 목록/노트 아이콘으로

---

## 3. D1 — 설명과 체크리스트는 함께 있을 수 있는가

**스크린샷 2.3이 우리 스펙과 정면으로 어긋나는 유일한 지점이다.**

§11.13은 명시한다: *"현재 active content는 한 종류만 유지한다"*. 그런데 참조 앱의
전환 후 화면에는 **빈 `설명` 필드와 체크리스트가 같이** 있다.

| | **A. 하나만 (지금 · §11.13)** | **B. 둘 다** |
|---|---|---|
| 전환의 뜻 | 내용이 **이동**한다 | 표시만 바뀐다 |
| 변환 필요 | 있다 (이미 구현됨) | 없어진다 |
| "이 작업의 내용은?" | 언제나 하나 | 다시 질문이 된다 |
| 영향 범위 | 없음 | 검색 · 매트릭스 `hasBody` · 템플릿 · 동기화가 전부 "둘 중 어느 쪽"을 묻게 된다 |
| 마이그레이션 | 없음 | 필요 |

**판단: A를 유지한다.** 이유는 보수성이 아니라 이미 치른 값이다 — §11.7의 무손실
변환과 §11.15의 단일 Undo가 **"한 종류만"이라는 전제 위에 서 있다.** 두 벌을 허용하면
되돌리기가 무엇을 되돌리는지부터 다시 정의해야 한다.

**다만 2.3이 보여주는 필요는 진짜다.** "이 체크리스트가 무엇에 대한 것인지" 한 줄
적을 자리가 없다. 그건 §11.13을 깨지 않고도 답할 수 있다 — 우리에겐 `notes`와
`description`이 **둘 다 있다**([types.ts](src/types.ts) `Task.notes`, `Task.description`).
`contentMode`가 고르는 것은 `description`뿐이므로, 체크리스트 모드의 머리말은
`notes`가 맡을 수 있다. **이건 별개 결정이고 이 문서의 범위 밖이다** — 여기서는
"필요는 실재하고, 길이 있다"까지만 적는다.

---

## 4. D2 — 아이콘 하나인가, 메뉴인가

`☰`를 눌렀을 때 무엇이 나오는지는 **스크린샷에 없다**(§8).

| | A. 토글 | B. 메뉴 |
|---|---|---|
| 동작 | 누르면 즉시 반대 모드 | 두 줄짜리 메뉴에서 고름 |
| 클릭 | 1 | 2 |
| 상태 표현 | `aria-pressed` / 아이콘 모양 | 체크 표시 |
| 오작동 시 | 되돌리기 하나 | 잘못 열어도 안전 |

**판단: A.** 상태가 둘뿐이고, 변환이 Undo 하나로 되돌아간다(§11.15). 두 상태짜리
선택에 메뉴를 여는 것은 클릭을 하나 더 받는 대가로 아무것도 사지 않는다.

세 번째 모드(노트 등)가 생기면 그때 B로 바꾼다 — 그 전환은 아이콘 자리를 그대로 두고
`onClick`만 바뀌는 일이다.

---

## 5. D3 — 아이콘은 "지금"인가 "누르면"인가

아이콘 하나가 두 가지를 말할 수 있고, 섞으면 아무 말도 안 하게 된다.

**규칙: 아이콘은 지금 상태를 그리고, 접근성 라벨이 동작을 말한다.**

| 지금 | 아이콘 | `aria-label` | `aria-pressed` |
|---|---|---|---|
| 설명 | 문서 | "체크리스트로 바꾸기" | `false` |
| 체크리스트 | 체크 목록 | "메모로 바꾸기" | `true` |

이유: 아이콘은 **읽지 않고 보는 것**이라 화면의 현재를 반영해야 하고, 라벨은
**읽는 것**이라 누르면 무슨 일이 나는지 말해야 한다. 툴팁(`title`)은 라벨과 같은
문장을 쓴다.

---

## 6. D4 — 리스트 행은 달라져야 하는가

참조 앱은 전환 후 **행의 아이콘까지** 바꾼다(2.3). 우리는:

| 화면 | 지금 |
|---|---|
| 매트릭스 카드 | `hasBody` 아이콘 있음 — 설명이든 메모든 "뒤에 뭔가 더 있다"만 말한다 |
| 리스트 행 (`TaskRowContent`) | ~~**아무 표시 없음**~~ → 매트릭스 §28 이후 같은 `hasBody` 아이콘 (§10.4) |

**판단: 이번 범위에 넣지 않는다.** 이유는 셋이다. (1) 사용자가 요청한 것은 상세의
컨트롤이다. (2) 2.3의 행 아이콘 변화가 **체크리스트 때문인지 확신할 수 없다** —
참조 앱에는 작업↔노트 변환이라는 별개 기능이 있고, 그쪽 결과일 수 있다(§8).
(3) 행에 그린다면 아이콘보다 **진행률(2/5)**이 나을 텐데, 그건 `checklistProgress`를
행마다 부르는 문제라 성능 판단이 따로 필요하다.

열어 둔 질문으로 §7 Q2에 남긴다.

---

## 7. 열린 질문

| | 질문 | 언제 |
|---|---|---|
| Q1 | ~~체크리스트 모드의 머리말을 `notes`가 맡을까~~ **아니다 — `notes`는 다른 패널의 필드다. §11.1에서 종결** | — |
| Q2 | ~~리스트 행이 체크리스트를 표시할까 — 아이콘인가 진행률인가~~ **아이콘은 있다(매트릭스 §28) · 진행률은 아니다. §11.4에서 종결** | — |
| Q3 | 아이콘만 남기면 발견 가능성이 떨어진다. 첫 사용자를 위한 안내가 필요한가 | 구현 중 |
| Q4 | ~~빈 설명에서 체크리스트로 바꾸면 항목이 0개다(§11.6). 화면이 무엇을 말해야 하는가~~ **아무 말도 하지 않는다. §10에서 종결** | — |

---

## 8. 확인하지 않은 것

- **`☰`가 토글인지 메뉴인지 모른다.** 누른 후의 화면이 스크린샷에 없다. D2는 우리 판단이다.
- **참조 앱에서 설명과 체크리스트가 정말 공존하는지** — 스크린샷 한 장의 해석이다.
  `설명`이 placeholder인지 실제 필드인지도 확정하지 못했다.
- **행 아이콘이 바뀐 원인** — 체크리스트 전환인지, 별개의 작업↔노트 변환인지.
- 참조 앱이 전환을 Undo로 되돌리는지.

---

## 9. 단계

| | 내용 |
|---|---|
| **1** | 아이콘을 제목 우측으로 (§11.4의 자리). 세그먼티드 제거, 상태·라벨은 §5의 규칙 |
| **2** | Q4 — 항목이 0개로 전환됐을 때 화면이 하는 말 |
| **3** | (별개) Q1 · Q2 |

1단계는 **컨트롤을 옮기는 일이지 동작을 바꾸는 일이 아니다.** `onSetContentMode`는
그대로고, 변환·트랜잭션·Undo는 손대지 않는다 — 그래서 되돌리기 쉽고, 기존 테스트가
그대로 지켜 준다.

---

## 10. 2단계 구현 기록 (2026-08-31)

### 10.1 Q4의 답 — 화면은 아무 말도 더 하지 않는다

§7 Q4는 "빈 설명에서 체크리스트로 바꾸면 항목이 0개다. 화면이 무엇을 말해야 하는가"였다.
스펙을 다시 읽으니 답이 이미 두 번 적혀 있었다.

| | 무엇을 그리라고 적혀 있나 |
|---|---|
| §11.6 (빈 설명 → 체크리스트) | `즉시 빈 Checklist UI` = `☐ Add an item...` · **별도 confirm 불필요** |
| §11.21 (빈 체크리스트) | `☐ Add an item...` 또는 `+ Add item` **을 표시한다** |

둘 다 **행 하나**를 그리라고 말하고, 문장은 어디에도 없다. 우리 화면에는 그 행이 있었고
**그 아래에 "아직 항목이 없습니다."가 한 줄 더** 있었다. 코드 주석은 그것을 §11.21이라고
적어 뒀는데, §11.21이 요구한 것은 추가 행이지 안내 문장이 아니다.

그래서 지웠다. **비어 있다는 말은, 방금 그것을 비운 버튼을 누른 사람에게 하는 말로는
같은 없음을 두 번 말하는 것이다.** i18n 키(`tasks.checklist.empty`)와 CSS도 함께 걷었다 —
고아 키는 다음 사람이 지워도 되는지 물어야 하는 물건이다.

### 10.2 그리고 `0/0`도 지웠다

헤딩 옆의 진행 표시가 빈 체크리스트에서 `0/0`을 그리고 있었다. 이 앱은 **이미 이 판단을
내려 놓았다** — MCP 투영의 `progressOf`가 항목 0개인 작업의 진행률을 아예 빼면서 이유를
적어 뒀다:

> 0/0을 보고하는 것은 "진행이 없다"고 말하는 것인데, 사실은 "이 작업에는 부분이 없다"이다.

같은 화면 두 블록 아래의 `하위 작업` 헤딩도 개수가 0이면 숫자를 그리지 않는다. 즉 이
`0/0`은 결정이 아니라 세 번째 자리에서만 다르게 굴던 것이다.

### 10.3 커서는 첫 항목이 들어갈 자리로 간다

§11.6이 "즉시 빈 체크리스트 UI, confirm 없음"이라고 적은 흐름에서, 버튼을 누른 다음 할 일은
**타이핑**이다. 그런데 그 입력칸은 눈앞에 있으면서 클릭을 한 번 더 요구했다.

**변환이 빈 목록을 남겼을 때만** 커서를 그 행에 둔다. 조건 둘:

- **변환일 때만.** 체크리스트 상태로 그냥 열린 작업에서 포커스를 뺏는 것은 아무도 하지 않은
  질문에 대한 대답이다.
- **비었을 때만.** 설명이 항목을 채웠다면 읽을 것은 그 결과지, 그 아래 빈 줄이 아니다.

드로어는 "방금 변환한 작업의 id"를 들고 있다. 불리언이 아닌 이유는 드로어가 작업을 바꿔도
**다시 마운트되지 않기 때문**(§1.26)이고, 그러면 그 플래그가 다음 작업까지 따라간다.

### 10.4 §6(D4)의 전제 하나가 그사이 바뀌었다

이 문서 §6은 "리스트 행에는 아무 표시가 없다"를 근거로 행 표시를 범위 밖에 뒀다. 그 문장은
이제 사실이 아니다 — `TICKTICK_MATRIX_DESIGN.md` §28이 매트릭스 카드를
`TaskRowContent`로 합치면서 **본문이 있다는 아이콘이 모든 행에 생겼다.**

§7 Q2("리스트 행이 체크리스트를 표시할까 — 아이콘인가 진행률인가")의 절반은 그래서 이미
답이 됐다: 아이콘은 있다. 남은 절반은 **체크리스트를 다른 본문과 구별해서 진행률로 그릴지**
이고, 그건 §6이 적은 대로 `checklistProgress`를 행마다 부르는 문제라 여전히 별개다.

### 10.5 검증

- 유닛 2,135개 통과(신규 6개) · `tsc` 통과
- 실제 앱(dev, ko): 빈 체크리스트 헤딩에 숫자 없음 · 문장 없음 · 토글 직후 커서가 `항목 추가`
  행에 있음. `- Prepare slides / - Print handouts`를 적고 토글하면 항목 2개가 생기고
  헤딩이 `0/2`가 되며 **커서는 움직이지 않는다**

### 10.6 남은 것

- 3단계 — Q1(체크리스트 모드의 머리말을 `notes`가 맡을까) · Q2의 나머지 절반
- Q3(아이콘만 남은 컨트롤의 발견 가능성)은 이번에도 답하지 않았다. 지금 아는 것은
  1단계·2단계 어디서도 그 질문이 스스로 답해지지 않았다는 사실뿐이다

---

## 11. 3단계 — Q1 · Q2 (2026-08-31)

### 11.1 Q1의 답 — 아니오. `notes`는 빈 필드가 아니다

§3은 "체크리스트 모드의 머리말은 `notes`가 맡을 수 있다"고 적었다. 근거는 `contentMode`가
`description`만 고른다는 것이었고, 그건 맞다. 틀린 것은 **`notes`가 비어 있는 자리라는
가정**이었다.

이 앱에는 상세 패널이 **둘** 있다.

| | 어디에 뜨나 | 무엇을 편집하나 | 뭐라고 부르나 |
|---|---|---|---|
| `TaskDrawer` | 작업 목록 화면 | `description` | **메모** |
| `TaskDetail` | 오늘 · 캘린더 · 프로젝트 (`AppPages`) | `description` **와** `notes` | 설명 · **메모** |

즉 `notes`는 이미 편집기와 뜻을 가진 필드이고, 하필 그 이름이 드로어가 `description`에게
주는 이름과 같다. 여기에 "체크리스트의 머리말"을 얹으면 **한 필드가 어느 화면에서 열렸느냐에
따라 다른 것**이 된다 — 오늘 페이지에서는 자유 메모, 드로어에서는 체크리스트 제목.

그리고 §8이 적었듯 참조 앱 쪽 근거는 스크린샷 한 장이고, 그 `설명` 줄이 실제 필드인지
placeholder인지도 확정하지 못했다. **확정되지 않은 필요를 위해 확정된 필드의 뜻을 흔들지
않는다.** 필요 자체는 여전히 실재하지만, 그 답은 두 패널이 "본문"에 대해 합의한 뒤에
나온다 — 지금은 합의가 없다.

### 11.2 그리고 그 조사가 데이터 손실 하나를 찾았다

`TaskDetail`은 `contentMode`를 **모른다.** 그래서 체크리스트 작업에도 설명 편집기를 그렸고,
거기 적은 것은:

1. 드로어에는 **절대 나타나지 않는다** (드로어는 체크리스트를 그린다)
2. 다음번 `메모로 바꾸기` 한 번에 **사라진다** — `descriptionFromCheckItems`가 항목들로
   `description`을 덮어쓴다

실제 앱에서 재현했다. 오늘 페이지에서 체크리스트 작업(`Standup`, 항목 2개)을 열어 설명에
`ask about the deploy window`를 적으니 저장은 됐고(`description`에), 드로어에는 흔적이
없었으며, 드로어에서 노트 모드로 한 번 토글하자 `- [x] Yesterday\n- [ ] Blockers`가 그
자리를 차지했다. 적었고, 어디에도 안 보였고, 사라졌다.

`contentMode.ts`의 첫 문단이 "silent data loss is forbidden(§11.7)"이라고 적어 둔 바로 그
일이, 그 모듈이 아니라 **그 모듈을 모르는 화면**에서 일어나고 있었다.

### 11.3 고친 방법 — 없는 필드의 편집기를 없앤다

체크리스트 모드에서 `TaskDetail`은 설명 편집기 대신 **체크리스트를 읽기 전용으로** 그린다
(체크 상태 포함) + 한 줄로 어디서 고치는지 말한다.

매트릭스 §27.3이 같은 모양의 결정을 먼저 했다: **보이지 않을 것밖에 만들 수 없는 컨트롤은
컨트롤이 아니다.** 거기서는 칸의 `+`를 지우고 설명 한 줄을 남겼고, 여기서는 설명 편집기를
지우고 본문을 그린다.

**읽기 전용인 이유.** 여기서 항목을 켜고 끄게 하면 추가·삭제·순서는 왜 없느냐가 곧바로
따라온다. 절반만 편집되는 표면은 그 자체로 또 하나의 불일치다. 이 패널이 체크리스트를
편집해야 하는지는 **별개 판단**이고, 그때는 `ChecklistEditor`를 그대로 옮기면 된다.

`notes`(메모) 필드는 손대지 않았다 — §11.1대로 그 필드는 이 패널의 것이고 뜻이 바뀌지 않는다.

### 11.4 Q2의 나머지 절반 — 진행률은 아직 아니다

§10.4가 적었듯 아이콘 쪽은 이미 답이 됐다(매트릭스 §28). 남은 것은 "체크리스트를 진행률로
그릴까"인데, **아니오, 지금은.**

행 컴포넌트는 `task` 하나만 받는다. `2/5`를 말하려면 계정의 체크 항목이 행마다 필요하고,
그건 세 호출자(`TasksModule` · `TaskBoard` · `MatrixPage`)가 전부 나르는 prop이다. 그 값을
치르는 근거는 참조 앱 스크린샷 한 장이고, §8이 그 변화가 체크리스트 때문인지 **확정하지
못했다**고 적어 뒀다. 근거가 그만큼일 때 세 호출자를 바꾸지 않는다.

### 11.5 검증

- 유닛 2,140개 통과(신규 5개) · `tsc` 통과
- 실제 앱(dev, ko): 오늘 페이지에서 체크리스트 작업을 열면 `☑ Yesterday` · `☐ Blockers`와
  안내 한 줄이 보이고 **설명 입력칸이 없다.** 메모 필드는 그대로

### 11.6 남은 것

- §7에 남은 것은 Q3(아이콘만 남은 컨트롤의 발견 가능성)뿐이다. Q1은 여기서, Q2는 §11.4에서,
  Q4는 §10에서 닫혔다. Q3은 1·2·3단계 어디서도 스스로 답해지지 않았다
- 두 패널이 "본문"에 합의하지 않은 상태는 그대로다. `TaskDetail`이 체크리스트를 편집하게
  할지, 아니면 이 패널 자체가 드로어로 대체될지가 그 합의의 형태이고, 둘 다 이 문서 밖이다

---

## 12. 읽기 전용을 걷어냈다 (2026-08-31)

### 12.1 §11.3이 남긴 절반

3단계는 레거시 패널의 설명 편집기를 체크리스트로 바꿨지만 **읽기 전용**이었고, 그 이유는
"절반만 편집되는 표면은 그 자체로 불일치"였다. 맞는 말인데, 그 결과가 **아무것도 편집되지
않는 표면**이었다 — 오늘·캘린더·프로젝트 페이지에서 체크리스트는 보이지만 **체크할 수
없었고**, 안내문이 시킨 "목록 화면에서 편집하세요"는 지금 이 화면에 있는 사람이 실행할 수
있는 문장이 아니다.

그래서 절반이 아니라 **전부**를 준다. 드로어가 쓰는 `ChecklistEditor` 그대로다 — 두 번째
구현이 아니라 같은 컴포넌트이고, 이 문서 §11.3이 "그때는 그대로 옮기면 된다"고 적어 둔
그대로였다. 안내문과 그 i18n 키·CSS는 함께 나갔다.

### 12.2 그러다 이 패널이 44%로 살고 있었다는 걸 알았다

체크리스트를 넣으니 항목 입력칸이 67px이 됐다. 원인은 이번 변경이 아니었다.

```
.detail-panel { grid-template-columns: minmax(160px, 1fr) auto minmax(160px, 1fr) auto; }  ← 위쪽 규칙
.detail-panel { display: grid; gap: 16px; }                                                ← 아래쪽 규칙
```

아래 규칙이 열 템플릿을 **되돌리지 않았다.** 이 패널의 자식은 하나뿐이므로 그 네 열은 첫
트랙(160px)에 내용을 가두는 것 말고는 아무 일도 한 적이 없다. **359px 패널 안에서 제목도,
속성 행도, 메모도 160px 안에 살고 있었다.**

이게 지금까지 안 보인 이유는 그 안의 것들이 전부 **자기 폭을 주장할 줄 알았기** 때문이다 —
`textarea`도 `select`도 기본 너비가 있다. 체크리스트 행은 그런 게 없어서, 컨테이너가 주는
만큼만 차지하고 67px에서 멈췄다. **폭을 주장하지 않는 내용이 처음 들어온 날 드러난 버그다.**

한 줄로 고쳤다: `grid-template-columns: minmax(0, 1fr)`.

### 12.3 검증

- 유닛 2,144개 통과(신규 2개, 읽기 전용 시험 하나는 편집 시험으로 교체) · `tsc` 통과 ·
  E2E 118개 통과
- 실제 앱(dev, ko): 오늘 페이지에서 `Standup`을 열어 `Blockers`를 체크 → 저장소의
  `checked: true`. 제목·일정·속성·하위 작업·메모가 전부 패널 폭을 쓴다

### 12.4 남은 것

- 두 패널은 이제 **같은 본문을 같은 방법으로** 편집한다. 남은 차이는 `notes`(레거시 전용)와
  드로어의 나머지 절반이고, 그건 §11.6이 적은 "패널 하나로 합칠 것인가"라는 더 큰 질문이다
- 이 패널의 나머지도 160px 시절의 배치를 물려받았을 수 있다. 폭이 넓어진 지금 다시 볼
  가치가 있지만, 이번에는 재보지 않았다
