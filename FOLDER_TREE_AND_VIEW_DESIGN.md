# 폴더는 폴더처럼 보이고, 폴더가 화면이 된다

> 상태: **구현됨** · 2026-09-03 (§11의 셋과 §13의 셋 모두 답을 받았다.
> 구현하며 정해진 것은 §12, 접기는 §13)
> 대상: `components/tasks/{TasksSidebar,TaskQuickAdd,TasksModule}.tsx` ·
> `domain/tasks/{createResolver,sidebarFolders,scopeRegistry}.ts` ·
> `domain/view/` (새 파일 하나) · `styles/17-tasks-module.css`
>
> 사용자가 요구한 것 셋:
> 1. 폴더 줄에는 폴더 아이콘, 리스트 줄에는 리스트 아이콘 (1번 이미지)
> 2. 폴더에서 할 일을 만들 때 리스트를 **고르지 않아도** 맨 위 리스트로 들어간다
> 3. 폴더 화면에는 하위 리스트의 것이 다 뜬다 — 리스트별로 (2번 이미지)

---

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 사용자가 준 이미지 2장 | [관찰] |
| **실측** | 파일과 줄 번호를 적은 것. 전부 이 리포의 현재 코드 | [실측] |
| **결정** | 사용자가 고른 것 셋, 그리고 그것을 성립시키려면 함께 정해야 하는 것들 | [결정] |
| **추론** | 근거는 있으나 실물로 확인하지 않은 것 | [추론] |

### 0.1 두 이미지는 **우리 앱이 아니다** [실측]

이걸 먼저 못 박아야 이 문서를 나중에 읽는 사람이 헷갈리지 않는다. 두 이미지는
**TickTick**이다. 근거 셋:

- `ZZ Folder` · `ZZ List A` · `ZZ List B` ·
  `ZZ Very Long List Name For Truncation Test` 는
  [TICKTICK_COMPONENT_04_FOLDER_TREE.md](TICKTICK_COMPONENT_04_FOLDER_TREE.md) §0.1이
  **참조 앱 계정에 직접 만든 측정용 데이터**다. 그 문서에 이름과 목적("세 번째는 §22
  말줄임 측정용")이 그대로 적혀 있다.
- 2번 이미지의 퀵애드는 `Add Task to "…"`라고 쓴다. 우리 `tasks.addPlaceholderIn`은
  `Add a task to {{list}}`다 — 관사도 다르고 따옴표도 없다.
- 우리 사이드바는 **어떤 줄에도 아이콘이 없다**(§2.1). 1번 이미지에 아이콘이 있으니
  우리 것일 수 없다.

즉 이 둘은 **목표**이지 현재 상태가 아니다. 셋 다 아직 없는 기능이다.

---

## 1. 이미지가 보여 준 것 [관찰]

### 1.1 사이드바 (1번 이미지)

| 줄 | 앞 | 라벨 | 뒤 |
|---|---|---|---|
| `ZZ Folder` | **펼침 삼각형** + **폴더 글리프** | 이름 | (비어 있음 — 자식이 다 비어서 count가 0) |
| `ZZ Very Long Li…` | **줄 세 개 글리프(☰)** | 말줄임된 이름 | — |
| `학교` | ☰ | 이름 | **파란 점** + `13` |
| `해야 하는 일` | ☰ | 이름 | **빨간 점** |

읽히는 것 셋:

1. **종류는 앞에서 말한다.** 폴더냐 리스트냐가 들여쓰기가 아니라 글리프로 구분된다.
2. **색은 뒤에서 말한다.** 리스트 색은 라벨 왼쪽이 아니라 **count 옆**에 있다.
3. **폴더는 접힌다.** 삼각형이 그 표시다. (COMPONENT_04 §2.1이 실측해 둔 것과 일치:
   열림은 `icon-sidebar-folder-open`, 닫힘은 `icon-folder` — **글리프 자체가 바뀐다**.)

### 1.2 폴더 화면 (2번 이미지)

```
+ Add Task to "ZZ Very Long List Name For Truncation Test"   ← 맨 위 리스트가 이미 골라져 있다

⌄ ZZ Very Long List Name For Truncation Test   1
  ☐ asd
      ZZ Very Long List Na…                    ← 행 밑에 또 리스트 이름
  ─────────────────────────
⌄ ZZ List B   1
  ☐ asd
      ZZ List B
⌄ ZZ List A   1
  ☐ asdadd
      ZZ List A
```

- **리스트별 머리**에 이름과 개수, 접는 화살표.
- 퀵애드가 **어느 리스트로 갈지 이미 말하고 있다** — 그것이 요구 2의 정체다.
- 각 행이 자기 리스트 이름을 **한 번 더** 쓴다. 이건 우리가 따라가지 않는다(§5.4).

---

## 2. 실측 — 지금 무엇이 있고 무엇이 없는가

### 2.1 사이드바 줄에는 아이콘이 **하나도** 없다

`TasksSidebar.tsx`의 `row()`가 그리는 것은 전부다:

```
[색 점?] [라벨] [count>0?]
```

폴더 줄과 리스트 줄은 **같은 `.tm-row`**이고, 둘을 가르는 것은
`.tm-row.is-indented { padding-left: 26px }`(`17-tasks-module.css:132`) **하나뿐**이다.
그래서 지금 사이드바는 "이것은 폴더다"라고 **말한 적이 없다** — 안쪽에 무언가가 있으면
폴더였겠거니 하는 것이지, 빈 폴더는 애초에 그려지지도 않는다
(`inside.length === 0`이면 `return null`).

색 점은 **라벨 앞**에 있다(`row()`의 첫 자식). 1번 이미지와 반대다.

### 2.2 폴더는 접히지 않는다 — 그런데 접힘 상태는 이미 저장된다

`groups.map`이 언제나 자식을 다 그린다. 접는 컨트롤이 없다.

그런데 `sanitizeSidebarFolder`는 M0 passthrough라 **`collapsed`를 이미 보존한다** —
`sidebarFolders.test.ts:36`이 그것을 명시적으로 검증하고 있다. 즉 레코드는 준비되어
있고 읽는 쪽이 없을 뿐이다.

### 2.3 폴더 스코프는 **이미** 하위 리스트의 태스크를 다 모은다

```ts
// scopeQuery.ts, case "folder"
return isActive(task, ctx.lists, opts.finished) && !!list && folderIdFor(list) === scope.id;
```

요구 3의 절반은 이미 되어 있다. **없는 것은 그룹핑**이다: 리스트 뷰는 평평한
`<ul className="tm-list">` 하나이고(`TasksModule.tsx`), 어떤 스코프에서도 머리를 넣지
않는다.

### 2.4 그런데 **보드 뷰는 이미 그 모양이다** ★

이게 이번 설계에서 가장 중요한 실측이다. `boardAxis.ts`의 `listAxisColumns`:

- 폴더 스코프면 **폴더 안의 모든 live 리스트가 컬럼**이 된다. 비어 있어도.
  주석이 이유까지 적어 두었다 — *"폴더에서 빈 컬럼은 노이즈가 아니라 '여기 말고 또
  어디로 갈 수 있나'에 대한 답"*.
- 순서는 `groupRank("list", …)` — 타임라인과 **공유하는** 맵.
- 그리고 `createInListColumn`이 *"컬럼이 리스트를 지목하므로, 스코프 혼자서는 못 하던
  일을 할 수 있다"* 며 `requiredBeforeCommit`에서 `list`를 **뺀다**.

즉 **"폴더는 리스트별로 나뉘고, 자리마다 만들기가 가능하다"는 이 앱에 이미 있다.**
보드에만 있고 리스트 뷰에 없을 뿐이다. §5는 새 개념을 만드는 것이 아니라 **있는 모양을
리스트 뷰로 옮기는 것**이다.

### 2.5 태스크 행은 리스트 이름을 낼 줄 안다 (쓰는 데가 하나뿐)

`TaskRowContent`에 `listName` prop이 있고 `.tm-task-list`로 그린다. 넘기는 곳은
`MatrixPage.tsx` 하나. Tasks 모듈은 넘기지 않는다.

### 2.6 폴더에서 만들기는 리스트 선택을 **강제한다**

```ts
// createResolver.ts, case "folder"
// §12.4: "하위 List 선택 필수". A choice from outside this Folder is not a
// choice for this Folder, so it is refused rather than honoured.
return { targetListId: chosen || null, requiredBeforeCommit: chosen ? [] : ["list"], … };
```

`canCommit`이 `requiredBeforeCommit.length === 0`을 요구하므로 **Enter가 막힌다.**
`TaskQuickAdd`는 `리스트 선택…`이 기본값인 `<select>`를 그리고, 주석이 규칙을 그대로
적어 놓았다: *"a Folder holds several Lists and the app must not pick one silently."*

요구 2는 **이 규칙을 뒤집자는 것**이다. 그래서 §4는 뒤집을 근거부터 댄다.

### 2.7 "폴더 안 리스트의 순서"에 대한 답이 **셋** 있다 [실측]

| 어디 | 무엇으로 정렬 |
|---|---|
| 사이드바 | **정렬하지 않는다.** `ctx.lists`를 그대로 훑어 `byFolder`에 담는다 |
| 보드 | `groupRank("list")` — `list.order`, 동률이면 이름 |
| `sidebarFolders.listsInFolder` | `sidebarSortKey ?? order`, 동률이면 이름 — **호출하는 곳이 없다** |

세 번째는 만들어 놓고 아무도 안 쓰는 고아 함수다. 지금은 리스트 뷰에 그룹이 없어서
이 어긋남이 화면에 안 보이지만, §5가 그룹을 만드는 순간 **같은 폴더의 보드 컬럼 순서와
리스트 그룹 순서가 다를 수 있다.** §5.2가 이걸 하나로 만든다.

---

## 3. 결정 A — 줄의 종류를 그리는 아이콘

### 3.1 글리프 둘, 앱이 이미 쓰는 손으로

`components/tasks/treeIcons.tsx` (새 파일). `schedule/icons.tsx`가 정해 둔 규격을
그대로 따른다 — **viewBox 24, stroke 1.9, `currentColor`, `aria-hidden`**. 그 파일의
서문이 이유를 이미 다 적어 두었고(이모지를 걷어낸 이유 셋), 여기서 이모지 📁 ☰ 를 쓰면
그 셋을 그대로 되살리는 셈이다.

| 이름 | 무엇 | 언제 |
|---|---|---|
| `FolderIcon` | 닫힌 폴더 | 폴더 줄 |
| `ListIcon` | 가로줄 셋 | 리스트 줄 |

~~**열림/닫힘으로 글리프를 바꾸지 않는다**~~ — **§13.3에서 뒤집혔다.** 이 판을 쓸 때는
폴더를 접지 않기로 했었고("접힘이 없는데 열린 폴더 글리프만 있는 것은 상태를 말하지
않는 그림"), 접기가 §13으로 들어오면서 짝이 생겼다. `FolderIcon` / `FolderOpenIcon`.

### 3.2 색 점은 오른쪽으로 간다 [결정]

`row()`의 자식 순서가 이렇게 바뀐다:

```
지금:  [색 점] [라벨] [count]
이후:  [글리프] [라벨] [색 점] [count]
```

[관찰] 1번 이미지가 그렇게 그린다. 그리고 그래야 하는 이유가 따로 있다: **앞자리는
한 가지 질문만 답해야 한다.** 앞에 점과 글리프가 같이 서면 "이게 무슨 종류인가"와
"어느 리스트인가"가 한 칸을 나눠 쓴다.

**대안 — 글리프를 리스트 색으로 칠하고 점을 없앤다** [기각]. 한 칸으로 두 가지를 다
말할 수 있어 매력적이지만, 색칠된 ☰ 와 안 칠해진 ☰ 는 **다른 종류의 줄로 읽힌다.**
`listColorHex`는 색이 없으면 `""`를 내므로(=대부분의 리스트) 화면에는 칠해진 것과
안 칠해진 것이 섞이고, 그 차이가 실제로는 아무 의미가 없다.

### 3.3 들여쓰기는 그대로 둔다

`is-indented`의 26px은 남는다. 글리프가 종류를 말하고 들여쓰기가 **소속**을 말한다 —
둘은 다른 사실이고, 폴더가 여럿일 때 어느 폴더 밑인지는 여전히 위치로만 알 수 있다.

### 3.4 스마트 리스트도 아이콘을 받는다 [결정]

이건 범위 확장이 아니라 **§3.2의 결과**다. `오늘`·`다음 7일`·`기본함`은 같은 `row()`가
그리고, 아래 리스트 줄들만 글리프를 얻으면 **위 세 줄의 라벨과 아래 줄들의 라벨이 서로
다른 x에 선다.** 한 열의 텍스트가 두 위치에서 시작하는 사이드바가 된다.

답은 둘뿐이고, 둘 중 하나를 골라야 한다:

| | 결과 |
|---|---|
| (a) 세 줄도 글리프를 받는다 | 열이 하나로 맞는다. 글리프 셋을 더 그린다 |
| (b) 리스트 줄만 받고, 스마트 줄은 빈 글리프 칸을 둔다 | 열은 맞지만 위 세 줄에 이유 없는 여백이 생긴다 |

**(a)를 고른다.** 2번 이미지도 `Next 7 Days`와 `Inbox`에 아이콘을 준다. `완료`·`휴지통`
두 줄까지 포함해 다섯 개를 더 그린다 — `SunIcon`은 `schedule/icons.tsx`에 이미 있으니
그것을 옮겨 공유할지, 새로 그릴지는 구현 때 정한다(중복 글리프 둘은 만들지 않는다).

### 3.5 아이콘은 이름이 아니다

전부 `aria-hidden`. 줄의 접근 가능한 이름은 지금처럼 라벨 텍스트다. 스크린 리더에
"폴더 학교"라고 읽히게 만들려면 `aria-label`을 손대야 하는데, 그러면 **보이는 이름과
읽히는 이름이 달라진다.** 계층은 §8에서 `role="tree"`를 다룰 때 함께 본다.

---

## 4. 결정 B — 폴더에서 만들면 맨 위 리스트로 간다

### 4.1 §12.4를 뒤집는 근거 [결정]

`createResolver.ts`가 지키던 규칙은 이것이다:

> *a Folder holds several Lists and the app must not pick one silently; a Task
> filed somewhere the user did not choose is worse than a second question.*

이 문장에서 일이 되게 하는 단어는 **`silently`**다. 규칙이 막으려던 것은 "고르지 않고
정해지는 것"이 아니라 **"고르지 않았는데 어디로 갔는지 모르는 것"**이다.

그 조건이 이번 변경으로 둘 다 사라진다:

1. 퀵애드의 placeholder가 **`{리스트 이름}에 할 일 추가`**라고 쓴다. 이 문장은 이미
   `tasks.addPlaceholderIn`으로 존재하고, `TaskQuickAdd`가 `resolution.targetListId`로
   그것을 그린다 — 지금 폴더에서만 `targetListId`가 `null`이라 일반 문구로 떨어진다.
   기본값이 생기면 **타이핑을 시작하기도 전에 목적지가 적혀 있다.**
2. 폴더 화면이 **리스트별로 나뉜다**(§5). 만들어진 태스크가 어느 머리 밑에 떨어지는지
   같은 화면에서 보인다.

그리고 이 앱은 **이미 이 판단을 한 번 했다**: `createInListColumn`이 보드 컬럼에서
`requiredBeforeCommit`의 `list`를 뺀다(§2.4). 근거가 *"컬럼이 리스트를 지목하니까"*였다.
placeholder가 지목하는 것도 같은 종류의 지목이다.

**뒤집지 않는 것**: 선택은 여전히 가능하다. `<select>`는 남는다. 바뀌는 것은 **비어
있는 것이 거절이 아니라 기본값이 되는 것**뿐이다.

### 4.2 "맨 위"의 정의 [결정]

**사용자가 사이드바에서 보고 있는 그 순서의 첫 번째**다. 만든 순서도, 가나다순도
아니다. 구현으로는 §5.2가 정하는 **하나의 순서 함수**의 `[0]`.

이게 중요한 이유: 사용자가 "맨 위"라고 말할 때 보고 있는 것은 사이드바다. 도메인이
다른 순서를 쓰면 "맨 위"가 화면의 맨 위가 아니게 된다.

### 4.3 도메인은 이렇게 바뀐다

```ts
// createResolver.ts, case "folder"
case "folder": {
  const inside = ctx.folderListIds ?? [];
  // 고른 것이 이 폴더 것이면 그것. 아니면 맨 위. 폴더가 비었으면 여전히 없다.
  const chosen = ctx.chosenListId && inside.includes(ctx.chosenListId) ? ctx.chosenListId : "";
  const target = chosen || inside[0] || "";
  return {
    targetListId: target || null,
    requiredBeforeCommit: target ? [] : ["list"],
    patch: {},
    enabled: true,
  };
}
```

`folderListIds`는 이미 이 순서로 넘어온다 — `TasksModule`이 `lists.filter(...)`로 만들고,
§5.2가 그 필터를 정렬된 함수로 바꾸면 `[0]`이 곧 맨 위가 된다.

**"이 폴더 밖의 선택은 거절한다"는 그대로 남는다.** 그건 `silently`와 무관한, 여전히
옳은 규칙이다.

### 4.4 `<select>`에서 빈 항목이 사라진다

`리스트 선택…`(`tasks.addPickList`)은 **고르지 않은 상태**를 뜻했다. 이제 고르지 않은
상태가 없으므로 그 항목은 거짓말이 된다. select의 `value`는 `chosenListId`가 아니라
**`resolution.targetListId`**를 읽는다 — 그러면 아무것도 안 했을 때 맨 위 리스트가
선택된 채로 보이고, 컨트롤이 placeholder와 같은 말을 한다.

`tasks.addPickList` 키는 지워진다(카탈로그 테스트가 고아 키를 잡는다).

### 4.5 리스트가 하나도 없는 폴더는 그대로 거절한다

`inside`가 비면 `target`은 `""`이고 `requiredBeforeCommit`은 `["list"]`로 남는다.
사이드바는 빈 폴더를 그리지도 않지만(§2.1), **URL로는 닿을 수 있다.** 그때 만들기가
막히는 것은 옳다 — 넣을 데가 없다.

---

## 5. 결정 C — 폴더 화면은 리스트별로 묶인다

### 5.1 그룹핑 규칙은 도메인에

`domain/view/listGroups.ts` (새 파일).

```ts
export interface ListGroup { list: List; tasks: Task[]; }
/** 순수. 리스트 순서는 인자로 받는다 — 시계도 안 읽고 정렬 정책도 정하지 않는다. */
export function groupTasksByList(tasks: Task[], order: readonly List[], lists: List[]): ListGroup[];
```

컴포넌트가 아니라 도메인인 이유는 `todayGroups.ts`가 자기 서문에 적어 둔 것과 같다 —
*"a grouping rule that lives in a component is one that has to be rewritten to be
configured"*. 그리고 이 축은 나중에 태그·필터 스코프에서도 말이 되는 축이다.

소속은 `listIdFor(task, lists)`로 읽는다. **폴더 스코프에서는 미아 그룹이 생길 수 없다**
— `scopeQuery`가 이미 `folderIdFor(list) === scope.id`인 태스크만 내주므로, 모든 행의
리스트가 정의상 이 폴더 안에 있다. 그래서 보드가 필요로 했던 "덮이지 않은 행" 바구니가
여기엔 필요 없다.

### 5.2 순서는 **한 함수**가 답한다 [결정]

§2.7의 세 답을 하나로 합친다. 살리는 것은 **고아였던 `sidebarFolders.listsInFolder`**다:
`sidebarSortKey ?? order`, 동률이면 이름. `groupRank("list")`의 규칙(`order`, 이름)을
포함하면서 사용자가 사이드바에서 끌어 옮긴 순서까지 존중한다.

세 곳이 전부 이 함수를 읽는다:

| 어디 | 지금 | 이후 |
|---|---|---|
| 사이드바의 `byFolder` | 정렬 없음 | `listsInFolder(folder.id, treeLists)` |
| `TasksModule`의 `folderLists` | `lists.filter(...)` | 같은 함수 |
| 새 리스트 그룹 | — | 같은 함수 |

**부작용을 미리 밝힌다**: 폴더 스코프의 **보드 컬럼 순서가 바뀔 수 있다.**
`listAxisColumns`는 `groupRank("list")`로 정렬하는데, 어떤 리스트에 `sidebarSortKey`가
있으면 두 답이 갈린다. 같은 폴더를 두 뷰로 열었을 때 순서가 다른 것보다는 한쪽이
바뀌는 편이 낫다 — 그리고 그 판단은 `groupRank`의 주석이 이미 하고 있다:
*"the same records in a different order on two screens is two truths, not two views."*

이건 요구 셋 바깥의 변경이므로 §11에 결정 대기로 남긴다.

### 5.3 빈 리스트도 머리를 낸다 [결정]

이 앱이 이미 답해 둔 질문이다. `listAxisColumns`의 주석:

> *a FOLDER is a set of Lists, so every live List in it gets a column even when
> it holds nothing. An empty column there is not noise, it is the answer to
> "where else could this go".*

리스트 뷰가 다른 답을 내면 같은 폴더가 두 뷰에서 다른 개수의 리스트를 갖게 된다.
같은 문장을 그대로 쓴다.

### 5.4 행 밑에 리스트 이름을 또 쓰지 않는다 [결정 · 관찰과 다름]

2번 이미지는 각 행 밑에 자기 리스트 이름을 쓴다. 우리는 안 쓴다 — **바로 위 머리가
이미 그 이름이다.** 한 줄 안에서 같은 사실을 두 번 말하는 것이고, 방금
`SCHEDULE_TIME_FIELD_DESIGN.md` §12.2에서 `1일 전 오전 9시 (오전 9:00)`를 고친 것과
정확히 같은 종류의 중복이다.

`TaskRowContent`의 `listName`은 건드리지 않는다(§2.5). 매트릭스처럼 **그룹이 없는**
화면에서는 여전히 옳다.

### 5.5 어느 뷰에 걸리나

**리스트 뷰에만.** 폴더 스코프의 `allowedViews`는 `ALL_VIEWS`이고(`scopeRegistry.ts`),
보드는 이미 리스트를 컬럼으로 쓰며(§2.4), 간트는 자기 축이 따로 있다.

드래그 재정렬은 문제가 되지 않는다 — 폴더 스코프는 `canManualReorder: false`라
리스트 뷰에서 애초에 행을 끌 수 없다. 그룹 경계를 넘는 드롭이라는 질문 자체가 생기지
않는다.

### 5.5.1 태스크가 하나도 없는 폴더는 그대로 "비어 있습니다"

§5.3은 **일이 들어 있는 폴더**의 빈 리스트를 지키자는 말이다. 폴더 전체가 비어 있으면
지금처럼 빈 상태 문구 하나가 뜬다 — 할 말이 하나뿐인 화면에 머리 셋을 세워 두는 것보다
낫고, 그 분기는 이미 있다(`rows.length === 0 && state.view === "list"`).

### 5.6 머리는 무엇을 갖나

```
⌄ ZZ List B   1                       ← 이름, 개수
```

- **개수**는 그 그룹의 행 수. `queryScopeCount`를 다시 부르지 않는다 — 이미 손에 있는
  배열의 길이이고, 두 번 세면 두 답이 생긴다(§12.14의 취지).
- **화살표**는 §8. 이번엔 안 그린다.
- 머리를 눌러 **그 리스트로 이동**할 수 있게 할지는 §11에 남긴다. 그럴듯하지만,
  개수만 있는 정적인 머리와 어디론가 가는 머리는 다른 약속이다.

---

## 6. 표기와 i18n

새로 필요한 키는 놀랄 만큼 적다. 그룹 머리의 이름은 **리스트 이름 그 자체**이고,
`listDisplayName(list, t("tasks.defaultList"), t("tasks.inbox"))`가 이미 그것을 준다.

| 키 | en | ko | 어디 |
|---|---|---|---|
| `tasks.listGroup` | `{{list}}, {{n}} tasks` | `{{list}}, {{n}}개` | 그룹의 `aria-label` |

지워지는 키: `tasks.addPickList` (§4.4).

---

## 7. 구현 단계

| # | 무엇 | 크기 | 배포 가능? |
|---|---|---|---|
| 1 | `listsInFolder` 하나로 통일 (§5.2) — 사이드바·`folderLists`가 그것을 읽는다 + 테스트 | 작음 | ✅ 화면은 그대로, 순서만 확정 |
| 2 | `treeIcons.tsx` + `row()`에 글리프 칸, 색 점을 뒤로 (§3) | 중간 | ✅ |
| 3 | 스마트 리스트 다섯 줄의 글리프 (§3.4) | 작음 | ✅ |
| 4 | `createResolver`의 폴더 기본값 + select의 빈 항목 제거 (§4) | 작음 | ✅ |
| 5 | `domain/view/listGroups.ts` + 테스트 | 작음 | — |
| 6 | 폴더 리스트 뷰의 그룹 머리 (§5) | 중간 | ✅ |
| 7 | CSS — 글리프 칸, 그룹 머리 | 작음 | — |
| 8 | `npx tsc -b` + 전체 테스트 | — | — |

1번을 먼저 하는 이유: 2·4·6이 전부 "순서"에 기대는데, 그것이 세 군데에서 다르게
답하고 있는 상태(§2.7)에서 셋을 올리면 어긋남이 어느 단계에서 들어왔는지 알 수 없다.

---

## 8. 범위 밖

- ~~폴더 접기/펴기~~ — **§13이 되었다.** 여기 적혀 있던 "셋을 한 번에"가 그대로
  지켜졌다: 사이드바 삼각형 · 폴더 글리프 전환 · 폴더 화면 그룹 접기.
- **드래그로 리스트를 폴더에 넣기.** `sidebarFolderId`를 쓰는 일이고, 사이드바에는
  아직 드래그가 없다.
- **폴더 이름 바꾸기·삭제.** `renameSidebarFolder`는 있으나 여는 문이 없다. 별개 설계.
- **`role="tree"` / `aria-level`.** 참조 앱도 안 쓴다(COMPONENT_04 §3: `treeitem` 0개).
  §3.5의 이유로 이번 아이콘 변경과 묶지 않는다.
- **태그·필터 스코프의 리스트 그룹핑.** `listGroups.ts`는 그것을 할 수 있게 생겼지만,
  켜는 것은 그 화면들의 설계다.

---

## 9. 테스트

| 무엇 | 어디 |
|---|---|
| `listsInFolder`가 `sidebarSortKey`를 `order`보다 먼저 본다 | `sidebarFolders.test.ts` |
| 사이드바의 폴더 자식 순서 == `listsInFolder` | `tasksSidebar.test.tsx` |
| 폴더 줄에 폴더 글리프, 리스트 줄에 리스트 글리프, 둘은 다른 svg | 같은 곳 |
| 사이드바 어디에도 `\p{Extended_Pictographic}`가 없다 | 같은 곳 (`scheduleIcons.test.tsx`의 방식) |
| 색 점이 count 쪽에 있다 (라벨 앞이 아니라) | 같은 곳 |
| 폴더에서 아무것도 안 고르고 Enter → **맨 위 리스트**로 생성 | `createResolver.test.ts` |
| 폴더 안의 다른 리스트를 고르면 그것으로 | 같은 곳 |
| 폴더 **밖**의 리스트를 고르면 맨 위로 떨어진다(거절은 유지) | 같은 곳 |
| 리스트가 없는 폴더는 여전히 `requiredBeforeCommit: ["list"]` | 같은 곳 |
| placeholder가 `{맨 위 리스트}에 할 일 추가`라고 쓴다 | `taskQuickAdd.test.tsx` |
| `groupTasksByList`가 빈 리스트도 그룹으로 낸다 | `listGroups.test.ts` |
| 그룹 순서 == `listsInFolder` 순서 | 같은 곳 |
| 폴더 리스트 뷰가 리스트마다 머리와 개수를 그린다 | `tasksModule` 계열 테스트 |
| 그 화면의 행에는 리스트 이름이 **한 번만** 나온다 (§5.4) | 같은 곳 |
| 같은 폴더의 보드 컬럼 순서 == 리스트 그룹 순서 | 같은 곳 |

---

## 10. 결정된 것

1. 글리프는 앞, 색 점은 뒤 (§3.2).
2. 스마트 리스트도 글리프를 받는다 — 안 그러면 라벨 열이 두 군데서 시작한다 (§3.4).
3. 폴더 기본 대상은 **사이드바 순서의 첫 리스트**, 선택은 남는다 (§4).
4. 빈 리스트도 그룹 머리를 낸다 — 보드가 이미 그렇게 답했다 (§5.3).
5. 행 밑의 리스트 이름은 넣지 않는다 (§5.4).
6. 접기 셋(폴더 글리프 전환·사이드바 삼각형·그룹 화살표)은 **한 번에** 한다 (§8).

## 11. 물어본 셋, 받은 답 [결정 · 2026-09-03]

| # | 질문 | 답 |
|---|---|---|
| 1 | §5.2의 부작용 — 폴더 **보드 컬럼 순서**가 `sidebarSortKey`를 따르게 바뀐다 | **받아들인다.** 한 폴더를 두 뷰로 열었을 때 순서가 갈리는 것보다, 한쪽이 사이드바를 따라가는 편이 낫다 |
| 2 | 그룹 머리를 누르면 그 리스트로 이동하나 | **이동하지 않는다.** 머리는 이름과 개수만 말하는 정적인 것이다 (§5.6) |
| 3 | 접기 셋을 이번에 함께 하나 | **이 판이 끝난 뒤에 정한다.** §8은 그대로 범위 밖 |

---

## 12. 구현하며 정해진 것 (2026-09-03)

§7의 여덟 단계를 순서대로 구현했다. 설계에 없던 판단 넷.

### 12.1 사이드바에서 **사라지던 리스트**를 찾았다

§5.2를 하려고 `byFolder`를 다시 쓰다가 나온 것이다. 그 맵은 `folderIdFor(list)`를
키로 담는데, 그리는 쪽은 `groups`(살아 있는 폴더들)만 훑는다. 그래서 **보관되거나
삭제된 폴더를 가리키는 리스트는 아무 데도 그려지지 않았다** — 계정에는 있고 화면에는
없는 상태.

`grouped` 집합에 들지 못한 리스트는 최상위로 내려보낸다. 그룹이 없는 리스트가 있어야 할
자리가 거기다. 테스트로 못 박았다(`tasksSidebar.test.tsx`).

### 12.2 태그와 필터도 글리프를 받았다

§3.4는 스마트 리스트 다섯 줄만 말했는데, 태그·필터 줄도 같은 `row()`가 그린다.
그것들만 빼면 §3.4가 막으려던 것(한 열의 라벨이 두 x에서 시작하는 것)이 그 두 섹션에서
그대로 일어난다. 글리프 둘을 더 그렸다.

### 12.3 태스크가 없는 폴더는 머리를 세우지 않는다 (§5.5.1)

실물에서 확인하기 전에는 몰랐던 분기다. 본문에 §5.5.1로 적었다.

### 12.4 행 하나를 함수로 뽑았다

리스트 뷰가 두 모양(평평한 것 / 리스트별)이 되면서 60줄짜리 `<li>`가 두 자리에
필요해졌다. `taskRow(task)` 하나로 뽑았다 — 복사본 둘은 언젠가 서로 달라진다.

### 12.5 확인한 것 [실측 · 브라우저]

폴더 하나(`SchoolSchool`)와 리스트 둘(`Home`·`Work`)을 만들어 확인했다:

- 사이드바: 폴더 글리프 / ☰ 리스트 글리프 / 스마트 리스트 다섯 줄 전부 글리프,
  `Home`의 파란 점은 **count 쪽**에 있다.
- 폴더 화면: `Home 1` · `Work 2` 머리, 각 행이 제 머리 밑에.
- 퀵애드: `Add a task to Home` — **아무것도 고르지 않았는데** 목적지가 적혀 있고,
  그대로 Enter를 치면 `Home`에 들어간다. select는 `Home`이 선택된 채로 열린다.

그 테스트 데이터는 **개발 서버(localhost:5180)의 localStorage**에만 있다. 설치본과
origin이 다르므로 사용자의 실제 데이터와 섞이지 않는다. 지우지 않았다.

### 12.6 남은 것

접기 셋(§8)은 여전히 범위 밖이고, 사용자가 "이 판 이후에 정한다"고 했다.

---

## 13. 접기 (2026-09-03)

§8이 "셋을 한 번에"라고 미뤄 둔 것. 사용자가 답한 셋과, 선례가 이미 답해 둔 것들.

### 13.0 물어볼 필요가 없던 것 [실측]

| 무엇 | 답 | 어디서 |
|---|---|---|
| 컨트롤의 그림 | `components/common/Caret.tsx` | 네 군데가 이미 쓴다. 회전(글자 교체 아님)이라 트윈되고, `aria-hidden`이며 감싼 버튼이 `aria-expanded`를 단다 |
| 접히는 그룹의 모양 | `<button aria-expanded>` + `Caret` + 이름 + 개수 | 보드의 `완료` 그룹(`TaskBoard.tsx`) |
| 애니메이션 | 캐럿의 회전만. 높이는 즉시 | `01-base.css`의 `[data-reduce-motion]`이 이미 그 회전을 끈다. 가변 길이 목록의 높이를 재는 일은 하지 않는다 |
| `SidebarFolder.collapsed`를 쓸 수 있나 | **못 쓴다** | 사이드바의 그룹은 두 종류(`SidebarFolder` + 도메인 `Folder`)이고 후자에는 그 필드가 없다. 같은 생김새의 두 줄이 서로 다르게 행동하는 것은 §4.3이 반복을 무릅쓰고 막은 바로 그것이다 |

### 13.1 저장은 계정 설정에 [결정]

`AppSettings.collapsedFolderIds?: string[]`. M0 additive — 없으면 아무것도 접히지
않은 것이고, 그것이 지금의 동작이다.

계정인 이유: **`sidebarCollapsed`(사이드바 전체)가 이미 거기 있고 동기화된다.** 접힘을
localStorage에 두면 "사이드바를 접은 것은 따라오는데 폴더를 접은 것은 안 따라오는",
설명할 수 없는 두 규칙이 생긴다.

두 종류 폴더를 한 배열이 덮는다. 배열 하나라 도메인 `Folder`든 `SidebarFolder`든
같은 방식으로 접힌다.

**지우지 않는다.** `pruneScopeViewOptions`가 하는 정리를 여기엔 두지 않는다 — 사라진
폴더의 id가 남아 있어도 그것은 문자열 하나이고, 어떤 화면도 그것을 읽지 않는다.
`scopeViewOptions`가 정리되는 이유는 그 값이 **설정**이라 복원된 리스트가 되찾아야
하기 때문인데, 여기 담긴 것은 설정이 아니라 "지금 접혀 있다"는 사실뿐이다.

### 13.2 삼각형은 별도 버튼이고, 줄은 그대로 폴더로 간다 [결정]

폴더 화면이 이제 실제로 쓸모 있으므로(§5), 줄을 누르는 것이 여전히 "거기로 간다"여야
한다. 접기는 그 왼쪽의 12px 캐럿이 맡는다.

**기하**: 캐럿이 폴더 줄 왼쪽에 16px(12 + 4 gap)을 더한다. 그래서 `is-indented`도
26 → 42px로 **같이** 늘어난다. 한 단계는 한 단계이고, 부모와 자식의 관계는 그대로다.

### 13.3 폴더 글리프도 열림/닫힘으로 바뀐다 [결정 · 사용자]

`FolderIcon`(닫힘)과 `FolderOpenIcon`(열림). §3.1이 "접기를 넣을 때 함께"라고 미뤄
둔 것이 이것이다.

캐럿과 중복이라는 반론이 있었고 사용자가 참조 쪽을 골랐다. 근거는 **트리가 길 때 눈이
먼저 닿는 것은 12px 캐럿이 아니라 16px 글리프**라는 것.

### 13.4 폴더 화면의 리스트 그룹도 접히고, 기억된다 [결정 · 사용자]

`ScopeViewOptions`에 `collapsedListIds: string[]`. 그 폴더의 키 아래 들어가므로
폴더마다 따로 기억된다.

보드의 `완료` 그룹은 기억하지 않는데 이건 기억하는 이유: `완료`는 주변부의 더미이고
폴더의 리스트들은 **본문**이다. 리스트 여덟 개짜리 폴더에서 둘만 보고 일하는 사람이
올 때마다 여섯 개를 다시 접어야 하는 것은 일이다.

§11 답 2("머리는 이동하지 않는다")는 그대로다. 머리는 이제 버튼이지만, 그것이 하는
일은 **접기**이지 이동이 아니다. `<h3><button aria-expanded>…</button></h3>` — 제목이면서
컨트롤이다.

### 13.5 접힌 폴더 안의 리스트로 이동해도 저절로 펼치지 않는다 [결정]

탐색기의 "reveal"을 하지 않는다. 이유 셋:

1. 탐색할 때마다 **동기화되는 계정 설정에 쓰기**가 일어난다.
2. 접은 것은 일부러 접은 것이다. 뒤에서 되열리는 것이 제일 성가시다.
3. 지금 어디인지는 **헤더가 말한다** — 사이드바가 유일한 답이 아니다.

대신 남는 것 하나: 접힌 폴더 안의 리스트를 보고 있으면 사이드바에 `is-current`가
하나도 없다. 그 폴더 줄에 "안에 현재 위치가 있다"는 표시를 다는 것은 **새로운 시각
개념**이라 이번에 만들지 않는다. §14에 후속으로 남긴다.

### 13.6 구현 단계

| # | 무엇 | 크기 |
|---|---|---|
| 1 | `AppSettings.collapsedFolderIds` + normalize + 도메인 헬퍼 둘 | 작음 |
| 2 | `FolderOpenIcon`, 사이드바의 캐럿 버튼과 접힘 렌더, 들여쓰기 26→42 | 중간 |
| 3 | `ScopeViewOptions.collapsedListIds` + sanitize | 작음 |
| 4 | 폴더 화면 그룹 머리를 접히는 버튼으로 | 작음 |
| 5 | CSS | 작음 |
| 6 | `npx tsc -b` + 전체 테스트 + 실물 | — |

## 14. 남은 것

- 접힌 폴더가 **현재 위치를 품고 있다**는 표시 (§13.5).
- 드래그로 리스트를 폴더에 넣기 (§8).
- 폴더 이름 바꾸기·삭제의 문 (§8).
