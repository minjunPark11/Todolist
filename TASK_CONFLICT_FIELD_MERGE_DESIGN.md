# 같은 작업을 두 기기에서 고쳤을 때, 겹치지 않은 것은 묻지 않는다

> 상태: **M1 구현 완료 (동작 변화 없음) · M2~M4 미착수** · 2026-09-10
> 요청: "다른 서비스들은 이거 어떻게 처리해?" → "그냥 1번 설계 하자"
> 선행: `usePlannerData.ts`의 구글 다리, `domain/sync/taskRevisionSession.ts`
> 계기: 기기 충돌 4개가 구글 동기화 전체를 몇 주간 세웠고, 그 4개 중 몇 개는
> 애초에 질문일 필요가 없었다.

---

## 0. 근거 수준

이 문서의 코드 인용은 2026-09-10 `main`(`fa6339b`) 기준이고 전부 직접 읽은
것이다. **동작 관찰은 없다** — 필드 단위 병합은 아직 한 줄도 없다. "이렇게 하면
충돌이 줄어든다"는 아래 §1의 추론이지 측정이 아니며, 그 측정을 M4가 만든다.

---

## 1. 문제

작업 하나가 통째로 하나의 리비전이다. 그래서 **어느 필드를 고쳤든** 양쪽이 갈리면
충돌이다:

```
base    제목="회의"    마감=9/10
노트북   제목="회의"    마감=9/12     ← 마감만 고침
폰      제목="팀 회의"  마감=9/10     ← 제목만 고침
                                      → 지금은 충돌. 사람에게 묻는다.
```

두 사람이 다툰 것이 없다. 물어볼 것도 없다. 그런데 물어보고, 답할 때까지 그
작업은 양쪽 버전을 들고 앉아 있는다.

명령 기반으로 동기화하는 서비스들(Todoist의 공개 동기화 API가 그 모양이다)에는
이 경우가 아예 없다. "제목을 X로", "마감을 Y로"가 따로 도착해서 둘 다 적용된다.
진짜 충돌은 **같은 필드를 양쪽에서 다르게** 고친 경우로 줄어들고, 그건 드물다.

행 단위 리비전을 명령 로그로 바꾸는 것은 이 앱의 저장 계층을 다시 쓰는 일이다.
같은 효과의 대부분을 훨씬 싸게 얻는 길이 3-way 병합이다.

---

## 2. 지금 참인 것

### 2.1 base가 없다

```ts
// taskRevisionSession.ts, capture()
state.pending[id] = { data, expectedRevision: previous?.expectedRevision ?? remote?.revision ?? 0 };
```

`expectedRevision`은 **번호**다. 그 리비전의 **내용**은 어디에도 남지 않는다.
그래서 지금은 local과 remote가 다르다는 것만 알고, **누가 무엇을 바꿨는지는
모른다.** base 없이 필드별로 비교하면, 양쪽 다 건드리지 않은 필드까지 임의로
한쪽을 고르게 된다. 이것이 이 설계의 단 하나의 구조적 걸림돌이다.

다행히 자리는 이미 있다. 위 줄의 `??` 사슬이 "첫 capture에서 고정하고 이후
capture는 유지한다"를 이미 하고 있고, base는 정확히 같은 수명을 가진다.

### 2.2 충돌은 두 곳에서 태어난다

```ts
// reconcile()  — 원격 리비전이 기대와 다를 때
state.conflicts[id] = { local: pending.data, remote: remote ?? null };

// flush()의 오류 분기 — 쓰기가 리비전 충돌로 거절됐을 때
if (state.pending[id]) state.conflicts[id] = { local: state.pending[id].data, remote: rowOf(id) ?? null };
```

병합은 이 두 곳 **앞에** 놓여야 한다. 화면에 이미 뜬 질문을 나중에 조용히
치우는 것이 아니라, 애초에 질문이 되지 않게 하는 것이다.

### 2.3 Task는 47개 필드이고 그중 일부는 서로 묶여 있다

`status`가 `"done"`이면 `completedAt`이 있어야 한다. `previousStatus`는
`wontDoAt`·`archivedAt`과 함께 움직인다. **필드별로 독립 병합하면 어느 쪽도
쓴 적 없는 레코드가 나온다** — `status:"done"`인데 `completedAt:""` 같은.

---

## 3. 결정

### D1. base는 재구성하지 않고 기록한다

`pending[id]`에 `base`를 더한다. `expectedRevision`과 같은 `??` 사슬로 첫
capture에서 고정한다.

```ts
state.pending[id] = {
  data,
  expectedRevision: previous?.expectedRevision ?? remote?.revision ?? 0,
  base: previous?.base ?? remote?.data ?? null,
};
```

충돌이 태어나는 순간에 옛 행에서 주워오는 방법도 있지만 **틀린다.** capture와
flush 사이에 `refresh`가 여러 번 들어오면 `state.rows`는 이미 base를 지나쳐
있다. 기대 리비전을 고정하는 그 순간에 같이 붙잡는 것만이 정확하다.

### D2. base가 없으면 오늘과 똑같이 동작한다

이미 저장된 체크포인트에는 `base`가 없다. 이행 코드를 쓰지 않는다 — 없으면
병합을 시도하지 않고 지금처럼 통째로 충돌로 만든다. 다음 편집이 새 pending을
만들면서 base가 붙는다. **이행 실패라는 경우 자체를 없앤다.**

### D3. 병합 단위는 필드가 아니라 **필드 묶음**이다

§2.3 때문이다. 묶음 하나는 통째로 한쪽에서 온다. 어느 쪽도 안 건드린 묶음은
base에서 온다.

### D4. 이 빌드가 모르는 필드가 양쪽에서 다르면 병합하지 않는다

`normalizeTask`의 M0 전방 호환 때문에 계정은 이 빌드가 모르는 필드를 들고 있을
수 있다. 모르는 필드는 어느 묶음에 속하는지도 알 수 없고, 알려진 필드와 묶여
있을 수도 있다.

그래서 보수적으로 간다: **알려지지 않은 필드가 local과 remote 사이에서 하나라도
다르면 병합을 포기하고 묻는다.** 값이 같으면(양쪽이 동의) 상관없다.

대가는 분명하다 — 새 빌드가 새 필드를 쓰기 시작한 계정은, 옛 빌드에서는 병합이
멈춘다. 그것을 받아들인다. **틀린 병합보다 물어보는 것이 낫고, 물어보는 것은
오늘의 동작이다.** 이 결정은 되돌리기 쉽고(모르는 필드를 묶음에 등록하면 된다)
그 반대는 어렵다.

### D5. 구글 바인딩 필드는 병합 대상이 아니다 — 항상 remote

`googleEventId` · `googleEtag` · `googleSyncedAt` · `googleMetadataKey`는
사람의 의도가 아니라 **원격 매핑의 상태**다. 데이터베이스 행이 그것의 권위다.
`createdAt`도 마찬가지로 base에서 온다 — 만들어진 시각은 편집이 아니다.

### D6. `updatedAt`은 병합의 입력도, 판정 기준도 아니다

"최신 것이 이긴다"를 **명시적으로 거절한다.** `updatedAt`은 어느 기기의 벽시계
문자열이고, 시계는 어긋난다. 이 대화가 시작된 이유가 바로 8시간 어긋난 시계였다.
병합 결과의 `updatedAt`은 병합한 시각으로 새로 쓴다.

### D7. 병합 결과는 remote 리비전 위의 새 pending이고, 재시도는 유한하다

```
merge → pending{ data: merged, expectedRevision: remote.revision, base: remote.data } → flush 재시도
```

다른 기기가 계속 쓰고 있으면 이 왕복이 끝나지 않을 수 있다. **flush 한 번당
병합 재시도 2회**로 묶고, 넘으면 충돌로 세운다. 라이브락은 조용한 고장이다.

### D8. 어느 쪽이든 삭제는 병합하지 않는다

`local === null`이거나 remote가 tombstone이면 오늘처럼 묻는다. 삭제는 필드가
아니다 — "지웠다"와 "고쳤다" 사이에 자동으로 고를 수 있는 답이 없다.

### D9. 이미 물어본 질문은 병합으로 치우지 않는다

`conflicts[id]`가 이미 있으면 병합하지 않는다. 사람이 그 항목을 열어 두 버전을
읽고 있는 중일 수 있다. 읽는 중에 사라지는 질문은, 답한 적 없는 답이 된다.
한 번 물었으면 답을 받고 닫는다.

---

## 4. 병합 규칙

`merge(base, local, remote)`는 묶음마다 넷 중 하나다.

| base 대비 | 결과 |
|---|---|
| 양쪽 다 안 바꿈 | base |
| local만 바꿈 | local |
| remote만 바꿈 | remote |
| 양쪽 다 바꿈, 값이 같음 | 그 값 |
| 양쪽 다 바꿈, 값이 다름 | **병합 포기 → 충돌** |

마지막 한 줄이 이 설계의 전부다. 나머지는 물어볼 것이 없는 경우다.

비교는 묶음 전체를 하나의 값으로 본다. 배열(`tags`, `repeatDays`, `exdates`)도
지금은 통째로 하나의 값이다 — 태그의 합집합 같은 것은 **의미의 선택**이고
(한쪽이 뗀 태그를 다른 쪽이 안 건드렸다고 되살릴 것인가?) 이 문서의 범위 밖이다.

---

## 5. 묶음

| 묶음 | 필드 |
|---|---|
| 내용 | `title` `description` `contentMode` `notes` |
| 일정 | `dueDate` `startDate` `startTime` `endTime` |
| 반복 | `repeatType` `repeatInterval` `repeatDays` `repeatEndDate` `exdates` `recurrenceId` `occurrenceOf` |
| 생애 | `status` `completedAt` `archivedAt` `wontDoAt` `previousStatus` `deletedAt` |
| 위치 | `projectId` `listId` `sectionId` `statusId` `categoryId` `parentTaskId` `order` |
| 중요도 | `priority` `pinnedAt` |
| 태그 | `tags` |
| 대기 | `isSomeday` `waitingReason` `waitingFollowUpDate` |
| 집중 | `estimatedMinutes` `actualSeconds` `activeSessionId` `lastFocusedAt` |
| 차단 | `blockedByTaskId` |
| 알림 | `reminder` |
| 바인딩 (D5) | `googleEventId` `googleEtag` `googleSyncedAt` `googleMetadataKey` |
| 고정 | `id` `createdAt` (base) · `updatedAt` (병합 시각) |

**생애**가 가장 큰 묶음인 것은 실수가 아니다. §2.3의 이유로 이 여섯은 같이
움직여야 한다. 한쪽이 완료하고 다른 쪽이 휴지통에 넣었으면 그건 진짜 질문이다.

이 표는 코드 안에 있어야 하고, **Task에 필드가 늘 때 표에 빠지면 D4가 잡는다** —
모르는 필드로 취급되어 병합이 멈추지, 조용히 틀리지 않는다. 표의 빠짐이
안전한 쪽으로 실패한다는 것이 이 배치의 핵심이다.

---

## 6. 그래도 묻는 것

- 같은 묶음을 양쪽이 다르게 바꿨다 (§4 마지막 줄)
- 어느 쪽이든 삭제 (D8)
- 이 빌드가 모르는 필드가 갈렸다 (D4)
- 이미 물어본 질문 (D9)
- 병합 재시도 2회를 넘었다 (D7)

---

## 7. 단계

**M1 — base를 기록한다. ✅** `pending[id].base`. 병합은 아직 없다. 동작 변화 0.
체크포인트에 필드 하나가 늘고, 없으면 오늘과 같다(D2). 이것만 먼저 내보내면
사람들의 체크포인트에 base가 쌓이기 시작하고, M3이 켜질 때 이미 병합할 재료가
있다.

구현하며 나온 것 하나 — `flush`가 pending을 **스프레드**해서 쓰기 요청을 짓고
있었다(`{ id, ...pending, writeId }`). 그대로 두면 base가 매 쓰기마다 네트워크로
나가고 durable outbox에도 저장된다. 요청을 필드별로 짓도록 고쳤고, 테스트가
그것을 고정한다. 이 설계에서 base가 "그냥 하나 더 담는 필드"가 아닌 첫 자리다.

그리고 base를 놓는 자리가 네 곳이라는 것도 구현에서 확정됐다 — `capture`,
`preserveConflict`, `resolveConflict`의 "내 것 유지", 그리고 `flush`가 쓰기 성공
뒤에 expectedRevision을 옮기는 자리. 마지막 것을 빠뜨리면 pair가 조용히
거짓말한다.

**M2 — 순수 함수와 테스트.** `mergeTaskRevisions(base, local, remote)`.
아무 데도 연결하지 않는다. 테스트가 고정할 것:
- 서로 다른 묶음을 고치면 둘 다 산다
- 같은 묶음을 다르게 고치면 병합하지 않는다
- 어느 쪽도 안 건드린 묶음은 base에서 온다
- 모르는 필드가 갈리면 병합하지 않는다 (D4)
- **결과는 언제나 한쪽 또는 base가 실제로 쓴 값들의 조합이다** — 이것이 이
  함수가 지켜야 할 단 하나의 불변식이다. 아무도 쓴 적 없는 값이 나오면 실패다

**M3 — 연결한다.** §2.2의 두 자리. D7의 재시도 한도, D9의 이미 물어본 것 제외.

**M4 — 보이게 한다.** 검토 패널이 이번 패스에서 몇 개를 자동으로 합쳤는지
말한다. 조용한 병합은 조용한 손실과 화면에서 구별되지 않는다. 그리고 이 숫자가
§0이 없다고 한 그 측정이다 — 병합이 실제로 몇 개를 없애는지, 물어보는 것이 몇 개
남는지.

---

## 8. 이 설계가 하지 않는 것

- **명령 기반 동기화로 가지 않는다.** 그것이 근본 답이지만 저장 계층을 다시
  쓰는 일이고, 이 설계는 그 효과의 대부분을 훨씬 작은 변경으로 가져온다.
- **버전 기록을 만들지 않는다.** "묻지 않되 잃지도 않게"의 다른 절반은 되돌릴
  수 있게 하는 것이고, 그건 별개의 기능이다. 이 설계는 잃지 않는 쪽만 한다 —
  묶음이 갈리면 언제나 묻기 때문이다.
- **태그를 합치지 않는다** (§4).
- **`updatedAt`으로 이기고 지지 않는다** (D6).

## 9. 아직 답이 없는 것

- **M1을 얼마나 앞서 내보낼 것인가.** base가 쌓이는 데 시간이 필요하고, 그
  전에 M3을 켜면 대부분의 충돌이 D2로 떨어져 병합이 안 된 것처럼 보인다.
  최소 한 번의 배포 간격을 두는 것이 맞아 보이지만 근거는 없다.
- **`order`가 위치 묶음에 있는 것이 맞는가.** 다른 리스트로 옮기면 순서도
  바뀌니 같이 움직이는 게 맞아 보이는데, 같은 리스트 안에서 순서만 바꾼 것이
  위치 변경과 충돌하게 된다. 실제로 그런 편집이 얼마나 겹치는지 모른다.
- **§1의 전제가 참인가.** 이 문서는 "겹치지 않는 편집이 흔하다"를 가정한다.
  그 4개가 실제로 어떤 모양이었는지 아무도 보지 않았다. M4가 답한다.
