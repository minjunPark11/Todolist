# 기본함 보드 — 열을 만들고, 이름 짓고, 지우기

> 상태: **1단계 구현됨 · 2~5단계 미착수** · 2026-08-29
> 대상: `기본함`(Inbox) 보드 뷰의 **열(column)** — 헤더의 `+`, `⋯`, 그리고 `새로운 열`

## 0. 근거 수준

이 문서의 근거는 셋이고, 등급이 다르다.

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 사용자가 준 참조 앱 스크린샷 3장 (열 보드 · `+` 펼침 · `⋯` 메뉴) | [관찰] |
| **실측** | 우리 앱 소스 — `board.ts`, `TaskBoard.tsx`, `TasksModule.tsx` | [실측] |
| **추론** | 위 둘로 메운 것 | [추론] |

`TICKTICK_COMPONENT_13_KANBAN_BOARD.md`(2026-08-20)가 같은 화면(`#p/inbox/kanban`)을
실측해 두었으므로 치수는 그 문서를 인용하고 다시 재지 않는다. **이 문서가 다루는 것은
치수가 아니라 열의 정체다.**

---

## 1. 스크린샷에서 읽은 것

### 1.1 열 헤더 [관찰]

```
미분류  1              +   ⋯        일정  1   +  ⋯       언젠가  2  +  ⋯     + 새로운 열
```

- 이름, 개수, 그리고 컨트롤 **둘**. C13 §6이 실측한 `+`는 18×18, hover 배경 28×28.
- `⋯`는 C13이 못 본 것이다 — 그 회차는 hover만 했고 메뉴를 열지 않았다(C13 §8).
- 마지막 열 오른쪽에 **`+ 새로운 열`**, 파란 글자. 열 자체를 만드는 유일한 입구.

### 1.2 `+` 를 눌렀을 때 [관찰]

열 헤더 **바로 아래**에 파란 테두리 입력이 열린다.

```
미분류  1                    +   ⋯
┌──────────────────────────────────┐
│ ⌶ 할일 추가              🗓  ⌄   │
└──────────────────────────────────┘
```

오른쪽에 날짜 아이콘과 여는 표시(⌄) 둘. 카드는 **입력 아래**에 생긴다.

> 매트릭스 Phase 4(§19.2)가 이미 같은 결론에 도달했다 — 입력은 위에 열리고 카드는 아래에
> 생긴다. 같은 패턴을 두 번째로 확인한 것이므로 **이 앱의 규칙으로 승격해도 된다.**

### 1.3 `⋯` 메뉴 [관찰]

다섯 줄, 구분선 없음:

| | 항목 | 성격 |
|---|---|---|
| 1 | 이름 바꾸기 | 열 자신 |
| 2 | 왼쪽에 열 추가 | 이웃 생성 |
| 3 | 오른쪽에 열 추가 | 이웃 생성 |
| 4 | 이동 ▸ | 서브메뉴(드릴인) |
| 5 | 삭제 | 파괴 |

**2·3이 따로 있다는 것이 이 메뉴의 논지다.** "새 열"이 아니라 "이 열의 왼쪽/오른쪽"이다 —
열에 **순서가 있고, 그 순서가 저장된다**는 뜻이다.

### 1.4 열 안 [관찰]

각 열에 접히는 `완료 5` 그룹과 `추가 보기`가 있다. 카드는 체크박스 · 제목 · 날짜 줄이고,
지난 날짜는 빨강(`8월 27일`, `4월 20일`), 메모 아이콘과 반복 아이콘이 날짜 옆에 붙는다.

> 매트릭스 Phase 2·3이 만든 것과 같은 구조다(칸 안의 완료 그룹, 추가 보기).

---

## 2. 우리 앱 감사 [실측]

### 2.1 있는 것

`src/domain/tasks/board.ts` · `src/components/tasks/TaskBoard.tsx`가 이미 보드를 그린다.
열, 카드, 드래그, 드롭, 그리고 **드래그를 못 쓰는 사람을 위한 `이동` 셀렉트**까지 있다.

### 2.2 없는 것 — 스크린샷 넷 전부

| 스크린샷 | 우리 |
|---|---|
| 헤더의 `+` | **없다.** 헤더는 `<h3>` + 개수뿐 |
| 헤더의 `⋯` | **없다** |
| `+ 새로운 열` | **없다** |
| 열 안의 완료 그룹 · 추가 보기 | **없다** (매트릭스에는 있다) |

### 2.3 그리고 없는 진짜 이유

`board.ts:79`:

```ts
export const INBOX_COLUMNS: BoardColumn[] = [
  { id: "unsorted",  labelKey: "tasks.bucketUnsorted" },
  { id: "scheduled", labelKey: "tasks.bucketScheduled", requiresDate: true },
  { id: "someday",   labelKey: "tasks.bucketSomeday" },
];
```

**기본함의 열 셋은 상수다.** 그리고 소속은 저장되지 않는다 — `inboxBucketOf()`가 작업의
`isSomeday`와 `dueDate`에서 **계산한다.** 열은 레코드가 아니라 **질의**다.

리스트 보드 쪽은 반대다. `ListSection`은 진짜 레코드고 `sanitizeListSection`도 있다.
그런데 **그것을 만드는 UI가 없다** — 저장소 전체에서 섹션을 만드는 코드는 레거시 마이그레이션
(`legacyWorkflowStatus.ts`) 하나뿐이다. 즉 리스트 보드의 열도 사용자가 만들 수 없다.

---

## 3. 핵심 발견 — 메뉴 다섯 줄 중 넷이 대답할 수 없는 질문이다

가상 열에 `⋯` 메뉴를 그대로 붙이면 이렇게 된다:

| 메뉴 | 가상 열에서 무슨 일이 일어나는가 |
|---|---|
| 이름 바꾸기 | `일정`을 "이번 주"로 바꾼다. 열의 뜻은 **여전히 "마감일이 있음"**이다. 이름이 거짓말이 된다 |
| 삭제 | `언젠가`를 지운다. `isSomeday`가 켜진 작업들은? **화면 어디에도 없다** |
| 왼쪽/오른쪽에 열 추가 | 새 열의 **규칙이 없다.** 아무 작업도 들어올 수 없는 빈 열이 생긴다 |
| 이동 | 순서를 저장할 자리가 없다 (`INBOX_COLUMNS`는 배열 리터럴이다) |

대답되는 것은 **`+` 하나뿐**이다.

두 번째 줄은 익숙해야 한다. **바로 지난주에 매트릭스에서 고친 버그**다 — 타이핑하고,
저장되고, 어느 화면에도 없다(`TICKTICK_MATRIX_DESIGN.md` §27.3). 그때는 두 필터의
상호작용이 원인이었고, 여기서는 열의 삭제가 원인이다.

---

## 4. D1 — 열은 규칙인가, 통인가

이것이 이 문서의 유일한 결정이고, 나머지 전부가 여기서 따라 나온다.

| | **A. 통 (레코드)** | **B. 규칙 (데이터)** |
|---|---|---|
| 소속 | 작업이 `columnId`를 든다 | 작업의 필드가 조건에 맞는다 |
| 참조 앱 | **이쪽으로 보인다** — 메뉴 다섯 줄이 전부 자명해진다 | |
| 이름 바꾸기 | 자명 | 자명 (이름과 규칙이 별개다) |
| 삭제 | 작업은 남고 소속만 지워진다 | 작업이 **나머지 줄**로 떨어진다 |
| 열 추가 | 빈 통 | 규칙을 고른다 |
| 마감일을 넣으면 | **아무 일도 안 일어난다** — 통은 날짜를 모른다 | `일정` 열에 자동으로 들어간다 |
| `일정`에 드롭하면 | 통만 바뀐다. 작업엔 날짜가 없다 | 날짜를 묻고, 쓴다 (지금 동작) |
| Gate 7 | 두 보드가 같은 명령이 된다 — **§6.24가 금지한 것** | 두 명령이 유지된다 |

**A는 참조 앱에 가깝고, B는 이 앱이 이미 가진 것에 가깝다.**

A로 가면 잃는 것이 분명하다. 지금 기본함 보드는 **일정 상태의 편집기**다 — 카드를 `일정`에
끌면 날짜를 묻고, `언젠가`에 끌면 `isSomeday`를 켜고 날짜를 지운다(§6.23). 통으로 바꾸면
그 드래그는 아무 뜻도 없어지고, `일정` 열에 날짜 없는 작업이 앉는다. **이름이 반대 방향으로
거짓말을 시작한다.**

### 4.1 판단: B, 그리고 그 기계는 이미 있다

`src/domain/view/matrixRules.ts`가 **9일 전에** 정확히 이 문제를 풀었다:

| 필요한 것 | 이미 있는 것 |
|---|---|
| 열의 규칙을 데이터로 | `MatrixQuadrantRule` (리스트 · 태그 · 날짜 · 우선순위) |
| 작업이 이 열에 맞는가 | `matchesRule(task, rule)` |
| 드롭이 무엇을 쓰는가 | `patchForQuadrant(task, rule)` → 못 쓰면 `null` |
| 아무 열에도 안 맞는 작업 | 매트릭스의 **나머지 줄** (§25) |
| 받을 수 없는 드롭을 미리 말하기 | 거부하는 드롭존 (§25.3) |
| 이름 · 설명 · 색 편집 | `MatrixQuadrantEditor` (§26) |
| 열 안의 완료 그룹 · 추가 보기 | `groupMatrixTasks` (§17) |

**삭제의 답이 특히 그렇다.** 매트릭스는 "어느 칸에도 맞지 않는 작업 N개"를 이미 그린다.
열을 지운 작업들이 갈 곳이 이미 설계되어 있다 — 새로 발명할 것이 아니라 **연결할 것**이다.

`patchForQuadrant`의 `null`은 Gate 7의 거부를 일반화한 것이기도 하다. `일정` 열의 규칙이
`날짜 있음`이면 날짜 없이는 커밋할 수 없고, 그것이 지금 `requiresDate`가 하는 일이다.

---

## 5. 그래도 A가 이기는 자리 하나 — 순서

규칙 기반 열에도 **순서는 규칙에서 안 나온다.** `왼쪽에 열 추가`가 뜻을 가지려면 열마다
`sortKey`가 저장되어야 하고, 이건 A·B 어느 쪽이든 필요하다. `ListSection`이 이미
`sortKey`를 갖고 있고 그 정렬 규칙(§sections.ts)도 검증되어 있으므로 거기서 가져온다.

---

## 6. 단계

| | 내용 | 왜 이 순서인가 |
|---|---|---|
| **1** | 헤더의 `+` — 열 안에서 작업 만들기. **가상 열 그대로** · **완료**(§9) | 유일하게 지금 구조에서 대답되는 것. 매트릭스 Phase 4의 패턴을 그대로 옮기면 되고, 다음 단계 전부와 독립이다 |
| **2** | 열 안의 완료 그룹 · 추가 보기 | `groupMatrixTasks`를 보드에 연결. 도메인은 이미 순수하고 매트릭스 전용이 아니다 |
| **3** | 열 규칙을 데이터로 — `INBOX_COLUMNS` → 저장되는 규칙 넷, **UI 없음** | 매트릭스 Phase 6a와 같은 모양. 화면을 안 건드리므로 되돌리기 쉽다 |
| **4** | 나머지 줄 + 거부하는 드롭존 | **삭제보다 먼저.** 지울 수 있게 만들기 전에, 지운 것이 어디로 가는지 화면에 있어야 한다 |
| **5** | `⋯` 메뉴 다섯 줄 + `새로운 열` | 여기서야 전부 대답된다 |

**4를 5보다 앞에 두는 것이 이 계획의 유일한 비직관적 부분이고, 매트릭스에서 6b를 6c보다
앞에 둔 것과 같은 이유다**(§23.9). 작업을 숨길 수 있는 컨트롤보다, 숨은 작업을 보여주는
줄이 먼저 있어야 한다.

---

## 7. 열린 질문

| | 질문 | 언제 |
|---|---|---|
| Q1 | `isSomeday`는 규칙이 된 뒤에도 필드로 남는가, 아니면 `언젠가` 열의 소속으로 대체되는가 | 3단계 |
| Q2 | 리스트 보드의 열(`ListSection`)도 규칙이 되는가, 통으로 남는가. 둘이 갈라지면 `TaskBoard` 하나가 두 모델을 그려야 한다 | 3단계 |
| Q3 | 매트릭스의 `matrixRules.ts`를 보드가 쓰려면 이름이 맞지 않는다. `viewRules`로 옮길까 | 3단계 |
| Q4 | `이동 ▸` 서브메뉴는 무엇을 여는가 — 다른 열로? 다른 리스트로? 스크린샷으로는 판별 불가 | 5단계 |
| Q5 | `+`의 날짜 아이콘과 `⌄`가 각각 무엇을 여는지 못 봤다 (스크린샷은 닫힌 상태) | 1단계 |

---

## 8. 이 문서가 확인하지 않은 것

- 참조 앱의 `⋯` 메뉴를 **직접 열어보지 않았다.** 스크린샷 한 장이 전부이고, 항목이 비활성화되는
  조건(예: 마지막 열의 삭제)은 모른다.
- 참조 앱에서 `일정` 열이 규칙인지 통인지 **판별하지 못했다.** 메뉴의 존재가 통을 시사할 뿐이고,
  날짜를 넣은 작업이 자동으로 그 열에 들어가는지 확인하지 않았다. **D1의 근거 중 절반이 여기 있다.**
- 우리 보드가 완료된 작업을 지금 어떻게 다루는지 — `queryScopeTasks`가 스코프마다 다르게 거른다.
  2단계 착수 전 확인할 것.

---

## 9. 1단계 구현 기록 (2026-08-29)

### 9.1 바뀐 것

| 파일 | 무엇 |
|---|---|
| `domain/tasks/board.ts` | `createInInboxBucket` · `createInListSection` — 열이 **좁히는** 생성 |
| `components/tasks/TaskBoard.tsx` | 헤더의 `+`, 그 아래 열리는 입력, 날짜가 필요한 열의 날짜 칸 |
| `components/tasks/TasksModule.tsx` | `createInColumn` — 보드의 **세 번째 어댑터** |
| `styles/17-tasks-module.css` | `+` 28px, accent 테두리 입력 |
| `e2e/verticalRhythm.spec.ts` | `.tm-column-add`를 §4.87 절에 편입 |

### 9.2 열은 소유자를 다시 정하지 않는다

§12.16이 이 파일의 첫 줄에서 금지한 것이 정확히 이것이다 — 진입점이 스스로 주인을 계산하는 것.
그래서 `createInColumn`은 `resolveCreateContext`를 **먼저** 부르고, 열은 그 답을 좁히기만 한다.
드래그에서 스코프가 명령을 정하고 열이 대상을 정하는 것과 같은 분업이다.

실제 앱에서 만든 작업 셋의 저장된 레코드가 그 증거다:

| 열 | `dueDate` | `isSomeday` | `sectionId` | `listId` |
|---|---|---|---|---|
| 미분류 | `""` | `false` | **안 씀** | `list-inbox` |
| 일정 | `2026-09-01` | `false` | **안 씀** | `list-inbox` |
| 언젠가 | `""` | `true` | **안 씀** | `list-inbox` |

`listId`가 셋 다 같다 — 열이 셋인데 주인은 스코프가 한 번 정했다.

### 9.3 `일정` 열은 날짜를 묻고, 없으면 만들지 않는다

§6.25는 드롭에 대해 쓰였지만 규칙은 열의 것이지 제스처의 것이 아니다. 날짜 없이 `일정`에
타이핑하면 그 작업은 **옆 칸(미분류)에 태어난다** — 매트릭스 §27.3의 버그가 다른 문으로
들어오는 것이다. 그래서 날짜 칸이 폼의 일부이고(사후 경고가 아니라 질문), 없으면 제출
버튼이 꺼지며, `createInInboxBucket`이 `targetListId: null`을 돌려 **폼을 안 거치는 경로도**
막는다. 폼은 규칙이 아니기 때문에 규칙을 도메인에도 둔다.

### 9.4 판단 둘

**1. 입력은 헤더 아래, 카드는 그 아래.** 참조 앱 스크린샷과 매트릭스 §19.2가 같은 답에
도달했으므로 이 앱의 규칙으로 삼았다. 방금 타이핑한 것이 움직이지 않는다.

**2. `+`는 28px이지 32px이 아니다.** C13 §6이 실측한 참조 앱은 18px 글리프 + 28px 배경이고,
§4.87이 섹션 액션에 주는 값도 28이다. 두 근거가 같은 숫자를 가리켰고, 매트릭스의 칸 헤더가
이미 그 절에 앉아 있다 — 보드와 매트릭스가 같은 이유로 같은 크기를 쓴다.

### 9.5 검증

- 도메인 유닛 6개 · 컴포넌트 유닛 7개 (`taskBoardAdd.test.tsx`) · 전체 2022개 통과
- `tsc` 통과 · E2E 118개 통과
- 실제 앱(dev, ko) `/inbox?view=board`: 세 열 모두 `+` 28×28, 위 표의 레코드 확인

### 9.6 다음 (2단계)

열 안의 완료 그룹과 `추가 보기`. `groupMatrixTasks`가 이미 순수 함수이고 매트릭스 전용이
아니므로 연결이 주된 일이다. 다만 §8의 마지막 줄이 먼저다 — 보드가 완료된 작업을 지금
어떻게 거르는지(`queryScopeTasks`) 확인하지 않았다.
