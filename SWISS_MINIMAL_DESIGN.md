# 스위스는 각진 모서리가 아니라 자(尺)다

> 상태: **Phase 1~4 · §9 로그인 · §10 색 · §11 간격 · §12 타임라인 · §13 접근성 · §14 남은 것 정리** · 2026-09-05~06
> (I1=B 4px · I2=B 날카롭게 · I3=B 의미색 유지 · I4=A **Pretendard** · I5=6단 · I6=A 전면 ·
>  I7=A vitest · I8=C 150/200ms — 사용자 승인)
> 요청: "Minimalism & Swiss 으로 하고 싶은데 설계부터 해보자"
> 대상: `src/styles/*.css` (23개 파일 · 15,957줄) · `index.html` · `01-base.css`의 토큰 190개
> 선행 문서: `DESIGN-apple.md`(현행 토큰의 출처) · `TICKTICK_VISUAL_LANGUAGE_PLAN.md` ·
> `09-calendar-redesign.css`(최종 오버라이드 레이어)
> 출처: `ui-ux-pro-max` 스킬 — styles.csv "Minimalism & Swiss Style" · "Swiss Modernism 2.0" ·
> typography.csv "Minimal Swiss"(Inter/Inter)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 아래 §2·§3·§4의 모든 숫자 — `src/styles/*.css` 23개 파일 전수 grep | [실측] |
| **인용** | 스킬 CSV가 규정하는 스위스의 값 | [인용] |
| **결정** | 사용자 — "Minimalism & Swiss 으로 하고 싶다" | [결정] |
| **추론** | 굵기 550~850이 어느 페이스로 떨어지는지(브라우저 라운딩 규칙) | [추론] |

---

## 1. 스킬이 말하는 스위스를 글자 그대로 적용하면 리라이트다 [인용]

styles.csv의 "Minimalism & Swiss Style" 항목은 이렇게 규정한다.

```
--border-radius: 0px
--shadow: none
Primary Colors: Monochromatic, Black #000000, White #FFFFFF
--accent-color: single primary only
```

이 네 줄을 이 리포에 그대로 적용하면 각각 **340곳 · 71곳 · 132색 · 액센트 외 전부**를 건드린다
(§2). 그리고 그건 `DESIGN-apple.md`부터 `TICKTICK_FIDELITY_AUDIT.md`까지 60여 개 문서가 쌓아
올린 Apple/TickTick 정합성을 버리는 일이다.

**그래서 이 문서는 그렇게 하지 않는다.** 이유는 미학적 타협이 아니라 스위스 자체에 있다.
국제 타이포그래피 양식의 핵심은 검정과 흰색이 아니라 **모든 값이 하나의 자에서 나오는 것**이다.
모서리 각짐은 그 결과 중 하나지, 그 자체가 목적이 아니다.

이 앱은 스위스에 **외관으로 실패하고 있지 않다. 체계로 실패하고 있다.**

> **주의 — CSV 안에서 그림자 규정이 갈린다.** 같은 "Minimalism & Swiss Style" 행에서
> `Design System Variables` 열은 `--shadow: none`인데 `Effects & Animation` 열은
> **"sharp shadows if any"**다. 후자가 맞다고 본다: 그림자를 금지하는 양식은 CSV상
> **Flat Design**(`box-shadow: none`, `--border-radius: 2px`)이고, 그건 다른 행이다.
> 스위스가 배제하는 건 그림자가 아니라 **부드러운 그림자로 만든 가짜 깊이**다. → **I2**

---

## 1.5 데이터베이스는 이 이전(移轉)을 지지한다 [인용]

styles.csv에 **현행 스타일의 행이 그대로 있다.** 53번 "Bento Grids":

```
Primary Colors: Off-white #F5F5F7, Clean White #FFFFFF, Text #1D1D1F
Design System Variables: --page-bg: #F5F5F7, --card-radius: 24px, --shadow: soft
Era/Origin: 2020s Apple/Linear
```

`01-base.css`의 `--bg-app: #f2f2f7` · `--color-ink: #1d1d1f`. **같은 값이다.**
이 앱은 취향 없이 표류한 게 아니라 문서화된 양식 위에 정확히 서 있다.
그러므로 이 작업의 정체는 "정리"가 아니라 **Bento Grids → Minimalism & Swiss 이전**이다.

그리고 두 행의 `Do Not Use For`가 이 이전의 근거를 대신 써준다.

| 행 | Best For | Do Not Use For |
|---|---|---|
| **Bento Grids** (현행) | Product features, dashboards, personal sites, marketing summaries, galleries | **Long-form reading, data tables, complex forms** |
| **Minimalism & Swiss** (목표) | **Enterprise apps, dashboards, documentation sites, SaaS platforms, professional tools** | Creative portfolios, entertainment, playful brands |

이 앱의 본체는 **태스크 리스트(data table)와 스케줄 에디터(complex form)**다.
현행 양식이 명시적으로 쓰지 말라는 두 가지가 화면의 대부분이다. 목표 양식은 그 둘을 Best For에 적는다.

### 1.5.1 다만 "Productivity Tool" 행은 스위스를 1순위로 두지 않는다 [인용]

ui-reasoning.csv 46번:

```
Style_Priority:  Flat Design + Micro-interactions
Key_Effects:     Quick actions (150ms) + Task animations
Decision_Rules:  {"must_have": "keyboard-shortcuts"}
Anti_Patterns:   Complex onboarding + Slow performance
```

세 가지가 나온다.

1. **Flat Design은 스위스와 충돌하지 않고 좁힌다.** Flat의 `--border-radius: 2px`(CSS 열은 `0-4px`)와
   스위스의 `0px`이 겹치는 구간이 **0~4px**다. → **I1의 근거**
2. **속도가 어긋난다.** 스위스는 `200-250ms`, Productivity Tool은 `150ms`. → **I8**(신규)
3. `must_have: keyboard-shortcuts` — **이미 충족.** `App.tsx`가 전역 `keydown`과 `metaKey/ctrlKey`를
   배선하고 있다 [실측]. 이 작업에서 할 일 없음.

## 2. 자가 없다 [실측]

23개 CSS 파일 전수 조사:

| 축 | 현행 고유값 | 스위스가 요구하는 것 |
|---|---|---|
| `font-size` | **20종** (9~46px, `12.5px` 포함) | 5~6단 모듈러 스케일 |
| `font-weight` | **13종** (400·500·550·570·600·620·650·680·700·750·800·850·900) | 3단 |
| `border-radius` | **14종** (1·2·3·4·5·6·7·8·10·12·15·22·36·999px) | 1종 또는 1스케일 |
| `padding`/`margin`/`gap` 리터럴 | **20종 이상** (3·5·7·9·11·18·22px 포함) | 8px 배수 |
| `box-shadow` | **71개 선언**, 13개 파일 | 헤어라인, 그림자 아님 |
| hex 색 | **132종** (토큰 파일 밖에서만 84종) | 흑·백·회 + 액센트 1 |

`border-radius` 340개 선언 중 토큰(`--rounded-*`)을 쓰지 않고 리터럴을 박은 것이 대부분이다.
`--space-md: 17px`은 4의 배수도 8의 배수도 아니다 — Apple 스펙에서 온 값이 스케일에 섞였다.

**토큰은 190개나 있다. 지키는 규칙이 없을 뿐이다.**

---

## 3. 굵기 13종 중 9종은 화면에 없다 [실측]

```html
<!-- index.html:11 -->
href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap"
```

로드되는 페이스는 **300 / 400 / 600 / 700 넷**이다. CSS가 쓰는 굵기는 열셋이다.
그리고 `01-base.css:352`가 `font-synthesis: none`이다 — 브라우저가 없는 굵기를 합성하지도 못한다.

즉 나머지를 적은 자리는 가장 가까운 로드된 페이스로 떨어진다. **Phase 3에서 실제로 쟀다** —
Pretendard로 같은 문자열을 굵기마다 그려 폭을 비교했다. 같은 폭이면 같은 페이스다 [실측]:

| 선언한 굵기 | 그려지는 페이스 |
|---|---|
| 300 · 400 · **500** | **400** |
| **550** · **570** · 600 | **600** |
| **620** · **650** · **680** · 700 · **750** · **800** · **850** · **900** | **700** |

열셋을 적어서 **셋이 그려진다.** 계층을 만들려고 적은 차이의 대부분이 화면에 도달한 적이 없다.

> **이 표가 Phase 3의 치환 규칙이다. 그리고 그것을 모르고 한 번 틀렸다.**
> 17-tasks-module.css의 `650` 세 자리를 "650이면 semibold겠거니" 하고 `--weight-medium`(600)으로
> 보냈다. 실제로는 **700으로 그려지던 자리**여서, 그 치환은 스케일 정리가 아니라 세 곳을 가늘게
> 만드는 디자인 변경이었다. 재고 나서 `--weight-bold`로 되돌렸다.
> 숫자가 무엇으로 렌더되는지는 추론할 것이 아니라 잴 것이다.

스위스는 계층을 굵기와 크기 **둘로만** 만든다. 그 둘이 지금 신뢰할 수 없다.

---

## 4. 그리고 그 Inter는 오프라인에서 오지 않는다 [실측]

이건 데스크톱 앱이다(Tauri 2, `src-tauri/`). 그런데 활자는 런타임에 구글 CDN에서 받는다.

```
$ find public src -iname "*.woff*" -o -iname "*.ttf" -o -iname "*.otf"
(빈 결과)
```

번들된 폰트 파일이 **0개**다. `display=swap`이므로 오프라인 실행 시 폴백 스택으로 그려진다 —
Windows에서는 Segoe UI다. 비행기에서 켜면 앱이 다른 서체로 뜬다.

거기에 스택이 갈라져 있다:

| 위치 | 선언 | 적용 대상 |
|---|---|---|
| `01-base.css:345` | `-apple-system, BlinkMacSystemFont, "SF Pro Text", "Inter", ...` | 앱 전체 |
| `09-calendar-redesign.css:502` | `Inter, ui-sans-serif, system-ui, ...` | `.mini-focus-window` |

**맥에서 열면 미니 타이머만 Inter, 본 창은 SF Pro다** — 스택의 순서가 뒤집혀 있다.
(파일 이름과 달리 캘린더 규칙이 아니다. 09번 파일 487~570행이 미니 포커스 창을 그린다.)

그리고 미니 타이머는 **둘로 존재한다** [실측]:

| | 무엇 | 폰트 |
|---|---|---|
| `MiniFocusTimerWindow.tsx` | 앱 번들 안의 React 컴포넌트 | 09번:502 (Inter 우선) |
| `public/mini-focus.html` | 독립 정적 페이지, 인라인 `<style>` 106줄 | **폰트 링크 0개** → 항상 시스템 폰트 |

같은 창의 두 구현이 서로 다른 서체로 그려진다. 후자는 온라인이어도 Inter를 못 받는다.

활자를 체계의 축으로 삼는 양식을 고르면서 활자가 실행 환경마다 달라지는 상태는 그대로 둘 수 없다.
이건 취향이 아니라 **선결 조건**이다. → **I4**

---

### 4.1 그런데 Inter로는 못 고친다 — 이 앱은 한국어로 돈다 [실측]

`src/i18n/ko.ts`에 한글 **6,754자**. `OAuthConsentPage.tsx:33`이 기본 로케일을 `"ko"`로 잡는다.

**Inter에는 한글이 없다.** 번들해도 화면의 거의 모든 글자는 OS 폴백으로 떨어진다 —
Windows면 맑은 고딕, macOS면 Apple SD Gothic Neo. §4가 고치려던 "환경마다 서체가
달라진다"가 그대로 한글로 옮겨갈 뿐이다.

→ **Pretendard**. Inter 골격 위에 그려진 한국어 서체라 라틴과 한글을 한 패밀리가 덮는다.

**가변형(2,009KB)이 아니라 정적 3개(2,286KB)를 받은 이유**: 가변은 100~900을 전부 그려낸다.
§3의 `550·570·620·650·680·750·850`이 지금은 접혀 있다가 **살아나서**, Phase 3 전까지
오히려 더 들쭉날쭉해진다. 정적 3개는 §5.2의 3단을 규칙이 아니라 **물리적 제약**으로 만든다.
277KB의 값은 그것이다.

> **남은 것 하나 — 이 문서 범위 밖.** `index.html`이 `lang="en"`인데 앱은 한국어로 돈다.
> 스크린리더 발음에 영향이 있다. 활자 작업이 아니라 i18n 이슈라 여기서 고치지 않았다.

## 5. 제안: 자를 세우고, 강제한다

스위스를 "값을 바꾸는 일"이 아니라 **"값의 가짓수를 줄이는 일"**로 정의한다.
아래는 §2의 실측 사용량에서 뽑은 자다.

### 5.1 타입 스케일 — 20종 → 6단

현행 상위 사용량: `13px`(105) · `12px`(93) · `11px`(41) · `14px`(40) · `16px`(16) · `15px`(16).
실제로 앱이 쓰는 폭은 11~16이고 나머지는 꼬리다.

```
--type-xs:  11px   캡션 · 메타 · 배지
--type-sm:  12px   보조 라벨
--type-md:  13px   기본 UI 텍스트 (최다 사용)
--type-lg:  15px   본문 · 태스크 제목
--type-xl:  18px   섹션 제목
--type-2xl: 24px   화면 제목
```

`14px` 40곳은 13 또는 15로 흡수, `10px`/`9px`은 11로, `12.5px`은 12로. → **I5**

#### 5.1.1 그런데 기본값이 스케일 밖이었다 [실측]

`01-base.css`의 body가 `font-size: 14px`이었다. **앱에서 가장 많이 상속되는 크기가 위 6단에
없다** — 그대로 두면 스케일이 거짓말이 된다.

**사용자 확정 (I5-A): body를 13px로 내린다.** `13px`은 선언 105회로 압도적 1위였고, 기본값이
14였기 때문에 그 선언들의 상당수가 **14를 13으로 되돌리려고** 존재했다. 기본값을 옮기면 그
자리들이 잉여가 되고, 나중에 지울 수 있다. 대가는 크기를 선언하지 않은 텍스트가 1px 작아지는
것이고, 그것이 전부다.

#### 5.1.2 글리프는 타입 스케일이 아니다 [결정]

`×` · `⋯` · `+` 같은 문자 아이콘도 `font-size`로 크기를 잡는다. 하지만 그건 **읽는 크기가
아니라 아이콘 치수**다. 타입 스케일에 얹으면 `×`가 "본문"이라고 불리게 되고, 다음 사람이
그걸 고친다.

```
--icon-glyph: 16px;
```

17-tasks-module.css에서 `14·16·17·20px` 네 값 아홉 자리가 이 하나로 접혔다.

### 5.2 굵기 — 13종 → 3단

```
--weight-normal: 400
--weight-medium: 600
--weight-bold:   700
```

로드된 페이스만 쓴다. `500`은 400 또는 600으로, `550~680`은 600으로, `750~900`은 700으로.
300은 UI 텍스트에 쓰지 않는다(13px에서 가늘어 대비 미달).

### 5.3 간격 — 8px 기반, 6단

```
--space-1:  4px    (아이콘-라벨 등 8 미만이 필요한 자리만)
--space-2:  8px
--space-3: 12px
--space-4: 16px    ← 현행 --space-md: 17px 을 여기로
--space-6: 24px
--space-8: 32px
--space-12: 48px
```

`3·5·7·9·11·18·22px`은 인접 단으로 흡수.

### 5.4 모서리 · 그림자 · 색 → 결정 필요

I1 · I2 · I3 참조. §1.5가 앞의 둘을 좁혔다 — 모서리는 **0~4px 구간**, 그림자는 **제거가 아니라
날카롭게**. 남은 건 그 구간 안의 한 점을 고르는 일이다.

### 5.5 전환 속도 → I8

스위스 `200-250ms` vs Productivity Tool `150ms`. 이 앱은 하루에 체크박스를 수십 번 누르는 도구다.

---

## 6. 바꾸지 않는 것

여기서 손대지 않겠다고 미리 못 박는다. 스위스라는 이름으로 쓸려나가기 쉬운 것들이다.

- **priority 색** (`--priority-high/medium/low`) — 장식이 아니라 의미다. 단색화하면 정보가 사라진다.
- **`[data-accent]` 사용자 액센트 선택** — 기능이다. "액센트는 하나"는 *동시에* 하나라는 뜻이지 고정이라는 뜻이 아니다.
- **`--rounded-*` · `--color-*` 토큰 이름** — 50여 규칙이 참조한다. 이름은 두고 **값만** 바꾼다.
- **레이아웃 · IA · 사이드바 200px · `main{zoom}`** — 이 문서는 시각 언어만 다룬다.
- **캘린더/타임라인의 기하** — `CALENDAR_APPLE_METRICS.md`가 실측으로 정한 값들.

---

## 7. 강제 수단 — 문서만으로는 안 지켜진다 [실측]

340개의 리터럴 `border-radius`가 그 증거다. `DESIGN-apple.md`는 이미 스케일을 정해뒀고,
지켜지지 않았다. 같은 결말을 반복하지 않으려면 자를 **읽어서 세는 것**이 있어야 한다.

lazy 안: **vitest 하나.** 새 의존성 없이 이미 있는 러너로.
stylelint를 새로 붙이지 않는 이유: 설정 파일 + 플러그인 + CI 배선이 테스트 한 개보다 크다. → **I7**

### 7.1 다만 총합 하나로 잠그면 안 된다 [실측]

이 문서의 초안은 "CSS 전부에서 세고 기준 숫자보다 늘면 실패"였다. **리포가 이미 그 패턴을
기각해 뒀다** — `src/i18n/catalogue.test.ts`가 총합을 거절한 이유를 적어놨다:

> 109를 통과선으로 두면 화면 하나를 지운 다음 사람을 그 자리에서 세운다.
> 삭제가 카탈로그에 닿기 전에 **숫자를 먼저 올리기** 때문이다. 그 반론은 0에서 죽는다.

여기서는 아직 0이 될 수 없다 — Phase 1 직후 실측 **407곳**이 남아 있다. 그래서 총합이 아니라
**파일당 하나**를 잠갔다. 파일은 Phase 3의 커밋 단위이고(§8), 파일별 숫자는 서로를 밀지 않으며,
하나씩 0이 되어 목록이 비면 그때 catalogue.test.ts가 말하는 그 불변식이 된다.

숫자는 **천장인 동시에 바닥**이다. 넘으면 후퇴라 실패하고, 밑돌아도 실패한다 — 고쳐놓고 숫자를
안 내리면 그 자리가 다음에 조용히 되돌아가기 때문이다. 밑돌아 실패할 때 적어 넣을 숫자를
메시지가 알려준다.

### 7.2 구현: `src/styles/scale.test.ts` [실측]

| 카테고리 | 남은 위반 | 통과 기준 |
|---|---|---|
| `border-radius` | 172 | `0` · `4px` · `50%` · `100%` · `9999px` · `var()` |
| `font-size` | 155 | 6단 + `10px`(`--label-size`의 기록된 예외) · `var()` |
| `font-weight` | 57 | `400` · `600` · `700` · `var()` |
| `box-shadow` | 23 | `none` · `var(--shadow-*)` · **링** |
| **합** | **407** | 19개 파일 |

**링은 세지 않는다.** offset도 blur도 0인 box-shadow(`0 0 0 3px …`)는 그림자가 아니라 포커스
링이거나 헤어라인이다. I2-B가 걷어내는 것은 흐림이 만든 가짜 깊이이지 링이 아니고, 포커스 링은
접근성이라 애초에 협상 대상이 아니다.

`styles.css`의 `@import` 목록에 있으면서 `CEILING`에 없는 파일은 **0이어야 한다** — 새 CSS
파일이 목록을 우회해 들어오는 길을 막는다.

### 7.3 그리고 화면을 재는 스펙이 이미 있었다 [실측]

`e2e/radiusScale.spec.ts`가 `SCALE = [6, 8, 10, 12]`를 **실제 브라우저에서** 7개 라우트와
다이얼로그·Task Detail·커맨드 메뉴까지 훑으며 강제하고 있었다. 그 헤더가 이 문서의 §7.2에
대한 정확한 반론을 이미 적어놨다:

> 스타일시트가 선언한 모양이 아니라 **화면에 도달한** 모양을 검사한다 — 규칙은 덮어쓰일 수도,
> 브레이크포인트에 갇힐 수도, 아예 렌더되지 않을 수도 있고, grep에 보이는 것은 그 중 첫째뿐이다.

**Phase 1의 토큰 변경 시점부터 이 스펙은 빨간불이었다.** `npm test`(vitest)는 `src/**`만 돌고
e2e는 `npm run test:e2e`라, 조용히 깨져 있었다. Phase 3 첫 파일에서야 코드 주석이 이 파일을
가리켜서 발견했다.

`SCALE`을 이행값 `[4, 6, 8, 10, 12]`로 넓히고, 좁힐 시점을 주석에 적었다 — **§7.2의 CEILING이
전부 비는 날 `[4]`로**. 둘은 중복이 아니다: 이쪽은 선언을 읽고 저쪽은 화면을 잰다.

---

**양방향 확인 [실측]**: `22-floating.css`에 `border-radius: 7px; font-weight: 550`을 심으면
`3곳 (천장 1)`로 실패하며 파일:줄과 값을 찍는다. 반대로 그 파일의 리터럴 그림자 하나를
토큰으로 바꾸면 `1 → 0로 줄었다`며 내려 적을 숫자를 알려준다.

---

## 8. 단계

| Phase | 내용 | 코드 |
|---|---|---|
| **0** | 자 확정 — I1~I7 답하기 | 0줄 |
| ~~**1**~~ | ~~Pretendard 로컬 번들 + CDN link 제거 + `09-...css:502` 삭제 + 새 토큰 정의~~ **완료** | 4파일 +142/−43 |
| ~~**2**~~ | ~~회귀 기준선 테스트~~ **완료** — `src/styles/scale.test.ts`, 파일당 천장 19개 | 132줄 |
| **3** | 파일별 치환 — 진행 중 | 큼, 기계적 |

**Phase 3 진행**

**19개 파일 전부 0.** `scale.test.ts`의 `CEILING`이 비었고, 그때부터 이 테스트가 지키는 것은
천장이 아니라 §7.1이 말한 불변식이다. 약속대로 `e2e/radiusScale.spec.ts`의 `SCALE`도
`[4, 6, 8, 10, 12]`에서 **`[4]`**로 좁혔고, 통과한다.

### 8.3 화면에서 잰 결과 [실측]

6개 라우트(`/today` `/calendar` `/focus` `/board` `/planning` `/settings`)를 돌며
렌더된 모든 엘리먼트를 잰 값:

| 축 | 이행 전 | 이행 후 |
|---|---|---|
| `border-radius` | 14종 | **2종** — `4px` · `9999px`(알약) |
| `font-weight` | 13종 선언 / 3종 렌더 | **3종** — 400 · 600 · 700 |
| `font-size` | 20종 | **9종** — 6단 + `10px`(라벨) + `16px`(글리프) + `34px`(표시) |
| `box-shadow` | 71개 선언 | 크롬에 **0개** (떠 있는 레이어에만) |

### 8.4 자가 닿지 않는 곳 세 군데 [실측]

브라우저에서 재다가, grep으로는 볼 수 없는 자리가 셋 나왔다.

1. **토큰의 정의.** `--density-title: 20px` · `--density-radius: 10px`이 이름 뒤에 숨어
   있었다. 파일은 위반 0곳으로 통과했고 `.foc-header h1`을 재보고서야 드러났다.
   → `scale.test.ts`가 토큰 정의도 검사한다(§7.2의 `tokenScale`).
2. **UA 기본값.** `<small>`은 크기를 선언하지 않으면 `font-size: smaller`로 부모의
   0.8333배 — 13px 안에서 **10.8333px**이 된다. 어떤 선언에도 없는 값이다.
   → `01-base.css`에 `small { font-size: var(--type-sm) }` 한 규칙.
3. **SVG 내부.** `icons.tsx`의 `fontSize="8"`은 24×24 viewBox의 사용자 단위이지 px이
   아니다. 벡터 아이콘의 내부는 타이포 스케일이 다스리는 곳이 아니므로 그대로 둔다.

### 8.5 앱 밖의 파일 하나

`public/mini-focus.html`은 앱의 CSS를 한 줄도 읽지 않는 독립 페이지라 `scale.test.ts`가
닿지 않는다(§4의 두 구현 중 하나). 자를 그 파일의 `:root`에 한 벌 복제해 같은 이름으로
두었다 — 두 곳을 나란히 읽는 사람이 같은 낱말을 보게 하려는 것이다.

| 파일 | 이전 | 지금 |
|---|---|---|
| `17-tasks-module.css` | 97 | **0** |
| `02-calendar.css` | 80 | **0** — `.auth-*`는 기록된 예외 (§8.1) |
| `06-space-detail.css` | 35 | **0** |
| `05-spaces.css` | 33 | **0** — 표시 3단 신설 (§8.6) |
| `18-schedule-editor.css` | 31 | **0** |
| `08-calendar-categories.css` | 29 | **0** |
| `03-planning.css` | 25 | **0** |
| `09-calendar-redesign.css` | 20 | **0** |
| 나머지 11개 | 51 | **0** |
| `01-base.css` | 3 | **0** |
| **합** | **411** | **0** |

### 8.6 자를 두 번 늘렸다

이행 중에 6단으로 재면 틀리는 것이 둘 나왔고, 둘 다 자를 버리는 대신 **층위를 나눴다.**

| 토큰 | 무엇 | 왜 타입 스케일이 아닌가 |
|---|---|---|
| `--icon-glyph: 16px` | `×` `⋯` `+` 문자 아이콘 | 읽는 크기가 아니라 아이콘 치수. 6단에 얹으면 `×`가 "본문"이라 불린다 |
| `--display-sm/md/lg` | Focus의 시계·경과 시간 | `clamp(46px, 5.5vw, 62px)` 타이머를 6단에 얹으면 24px이 되어 앱의 이름값이 사라진다 |

`--display-sm`은 24px을 다시 적지 않고 `var(--type-2xl)`의 별칭이다 — §2가 지적한 것이
정확히 "세 개의 값이 아니라 세 개의 거의 같은 값"이었으므로.

### 8.7 이행이 깨뜨린 기존 스펙 셋 [실측]

셋 다 옛 값을 숫자로 못 박고 있었고, 셋 다 **숫자 대신 토큰과 비교**하도록 고쳤다.
그래야 자가 다음에 움직일 때 스펙이 그 뒤를 따라다니지 않는다.

| 스펙 | 못 박은 값 | 원래 묻던 것 |
|---|---|---|
| `radiusScale.spec.ts` | `SCALE = [6,8,10,12]` | 모든 모양이 한 자 위에 있는가 |
| `componentLanguage.spec.ts` | `card.radius === "10px"` | 이름이 무언가를 그리는가 |
| `overlayScrollbar.spec.ts` | `radius === "7px"` | 폭 6px에 반경 7px — 모서리가 아니라 캡슐이었다 |
| `ganttBars.spec.ts` | `border-radius: "999px"` | 알약인가 |

`radiusScale.spec.ts`는 **Phase 1의 토큰 변경 시점부터 빨간불이었다.** `npm test`(vitest)는
`src/**`만 돌고 e2e는 `npm run test:e2e`라 조용히 깨져 있었다 (§7.3).

### 8.1 02-calendar.css는 두 화면을 담고 있었다 [실측]

셀렉터로 갈라보니 이 파일은 하나가 아니었다.

| 묶음 | 위반 | 성격 |
|---|---|---|
| `.gcal-*` | 49 | 캘린더 크롬. rem 18종(0.62~1.15)이 폰트 크기의 전부였다 — 기계적 |
| `.auth-*` | 23 | **로그인 화면.** 아래 |
| 그 외 (`.toast` `.board-*` `.migration-box` …) | 8 | 기계적 |

`.gcal-*`와 그 외 57곳은 처리했다. rem은 root 16px 기준 px로 환산해 **최근접 단**으로 접었고,
표면의 제목 넷(`newtask-head h2` · `popover-title` · `year-month-title` · `board-column-header h2`)만
`--type-xl`로 승격했다. 승격 규칙을 "셀렉터에 title/h2가 있으면"으로 잡았다가
`.gcal-sidebar-section h3`(11.5px)와 `.gcal-tb-title`(12.5px, 이벤트 칩 안)이 18px로 뛰는 것을 보고
버렸다 — **이 앱에서 `title`은 대개 조밀한 컴포넌트 안의 작은 라벨이다.** 이미 제목 크기인 것만
제목으로 올린다.

남은 `.auth-*` 23곳은 다른 결정이 필요하다:

```
.auth-screen::after   border-radius: 42% 58% 58% 42% / 48% 36% 64% 52%   ← 블롭
.auth-card            border-radius: 36px · box-shadow: 0 34px 90px rgba(82,116,171,.22)
.auth-brand h1        font-size: clamp(2.35rem, 8vw, 3.25rem) · weight 750
.auth-submit          box-shadow: 0 16px 28px rgba(39,97,255,.24) · weight 750
```

유기적 블롭 · 36px 카드 · **색을 가진 그림자** · 유동 히어로 타이포. 이건 스위스의 반대편에 있는
시각 언어이고, 우연이 아니라 로그인 화면이 의도적으로 그렇게 그려진 것이다.

**사용자 결정: 예외로 명시한다.** 로그인은 앱 크롬이 아니라 앱 밖의 한 장이다. 이 화면을
스위스로 옮기는 것은 정리가 아니라 리디자인이고, 그 결정은 이 문서의 것이 아니다.

**다만 숫자 23으로 두지 않았다.** "23곳까지 봐준다"는 천장은 *어떤* 23곳이든 통과라서, 캘린더에
새 위반이 생기고 로그인에서 하나가 빠지면 조용히 지나간다. `scale.test.ts`에 셀렉터로 붙은
`EXEMPT` 목록을 만들었다 — `e2e/radiusScale.spec.ts`의 `ALLOWED_OFF_SCALE`과 같은 장치이고
같은 이유다: **적어두지 않은 예외가, 여섯 개짜리 스케일이 스무 개로 돌아가는 방법이다.**

그래서 02-calendar.css는 0이 되어 CEILING 목록에서 빠졌다. 예외는 파일이 아니라 `.auth-`에 붙어
있고, 그 자리를 떠나지 않는다.

**양방향 확인 [실측]**: `.gcal-probe { border-radius: 7px }`를 심으면 잡히고,
`.auth-probe { border-radius: 33px; font-size: 1.37rem }`은 통과한다.

0이 된 파일은 `CEILING` 목록에서 **빠진다** — 그때부터 "목록에 없는 파일은 0"이라는
불변식이 지킨다(§7.1). 숫자가 하나씩 사라지는 것이 이 단계의 진척이다.
| ~~**4**~~ | ~~E2E + 화면 실측~~ **완료** — vitest 2,701 · tsc 0 · playwright 215/0 · §8.3 | — |

Phase 1이 Phase 3보다 먼저인 이유: 활자가 흔들리는 상태에서 크기·굵기를 정리하면
무엇이 개선인지 볼 수 없다.

---

## 9. 결정할 것

| # | 질문 | 선택지 | 기본 제안 |
|---|---|---|---|
| **I1** | 모서리 | A) `0px` 전면 · B) `4px` 단일값 · C) 현행 6/8/10/12 유지, 리터럴만 토큰화 | **B** — DB가 구간을 좁혔다(§1.5.1): 스위스 `0px` ∩ Flat `0-4px`. C는 현행 Bento Grids(`24px`)에서 사실상 안 움직임 |
| **I2** | 그림자 71개 | A) 전부 제거, 1px 헤어라인 · B) 진짜 떠 있는 레이어(모달·팝오버·드롭다운)만 남기고 **날카롭게**(blur↓ spread 0) · C) 4단 elevation 스케일 정의 | **B** — CSV `Effects` 열이 "sharp shadows if any"(§1). A는 Flat Design의 규정이지 스위스가 아님 |
| **I3** | 색 132종 | A) 흑백회 + 액센트 1 (tint 색 제거) · B) tint·priority는 의미로 남기고 *장식* 색만 제거 | **B** — §6 |
| ~~**I4**~~ | 활자 | ~~A) 번들, 스택 통일 · B) 시스템 스택 유지~~ | **A로 확정, 단 Inter가 아니라 Pretendard** — §4.1 |
| **I5** | 타입 스케일 | §5.1의 6단으로 · 아니면 단 수 조정 | 6단 |
| **I6** | 적용 범위 | A) 23개 파일 전면 · B) 새로 만지는 화면부터 점진 | **A** — B는 두 양식이 공존하는 기간이 길어짐. 단 Phase 3은 파일 단위로 커밋 분리 |
| **I7** | 강제 | A) §7의 vitest 기준선 · B) stylelint 도입 · C) 문서만 | **A** |
| **I8** | 전환 속도 | A) `150ms` (Productivity Tool 행) · B) `200-250ms` (Swiss 행) · C) 분리 — 체크/호버 150ms, 레이어 열림 200ms | **C** — 하루 수십 번 누르는 체크박스에 250ms는 굼뜨고, 팝오버가 150ms면 튄다 |

---

## 10. 규모 추정

- Phase 1: `index.html` 3줄 · `09-calendar-redesign.css` 1줄 · `01-base.css` 토큰 블록 1개 · woff2 2개
- Phase 2: 테스트 파일 1개 (~40줄)
- Phase 3: 15,957줄 중 **약 800~1,000줄** (radius 340 + shadow 71 + font-size/weight ~350 + spacing 리터럴 일부)

Phase 3은 크지만 전부 값 치환이다. 구조·선택자·컴포넌트는 건드리지 않는다.


---

## 9. 로그인 화면 — 예외를 지운다

§8.1은 `.auth-*`를 예외로 남겼다. 이유는 방치가 아니라 의도였기 때문이다. 그 의도를 옮기는
일은 값 치환이 아니라 **다시 그리는 일**이고, 그래서 별도의 결정이었다.

### 9.1 무엇이 있었나 [실측]

425줄, 하드코딩 hex **27종**, rgba 리터럴 14개, 토큰 사용 27번.

| | 있던 것 |
|---|---|
| 배경 | radial 2 + linear 1의 3중 그라디언트 |
| 장식 | `::before` `::after` 420×420 유기적 블롭 둘 (`border-radius: 42% 58% 58% 42% / …`) |
| 카드 | radius 36px · `backdrop-filter: blur(20px)` · 색 그림자 `rgba(82,116,171,.22)` |
| 워드마크 | `clamp(2.35rem, 8vw, 3.25rem)` · weight 750 |
| 제출 | `linear-gradient(135deg, #4677ff, #2761ff)` · 색 그림자 `rgba(39,97,255,.24)` |
| 다크 | 규칙 **1개** — `[data-theme="dark"] .auth-screen { color: #17213a }` |

마지막 줄이 이 화면의 상태를 요약한다. 라이트와 **같은 색을 다시 적은 것**이어서, 다크
모드를 지원하는 것처럼 보이면서 아무것도 하지 않고 있었다.

### 9.2 결정과 결과

| | 결정 | 결과 |
|---|---|---|
| 배경 | 단색 + 구조 선 (사용자) | 그라디언트 3층·블롭 2개 제거, `--bg-app` 위 96px 헤어라인 격자 |
| 다크 | 토큰으로 지원 (사용자) | hex 27종 → 토큰. 다크가 딸려왔고 그 한 줄짜리 위장 규칙은 지웠다 |
| 카드 | 스위스 | 36px → `--radius-sm` · `backdrop-filter` 제거 · 색 그림자 → `--shadow-panel` |
| 워드마크 | 표시 스케일 | `clamp(…)` → `--display-md`. 자간 `0.16em`은 남긴다 — 워드마크의 정체이지 스케일의 일이 아니다 |
| 제출 | 액센트 하나 | 그라디언트·색 그림자 제거, 평평한 `--accent`. 화면에서 액센트를 칠한 면은 이것 하나뿐이다 |
| 컨트롤 | 한 높이 | 입력 58px·버튼 60px → **둘 다 48px**. 크기로 서열을 만드는 대신 색 하나로 만든다 |

바꾸지 않은 것: 마크, 카드 한 장이라는 배치, 폼의 순서. **언어를 바꾼 것이지 구조가 아니다.**

### 9.3 덤으로 나온 버그 하나 [실측]

`.auth-check`가 `<label>`이라서 `.auth-gate-form label`(특이도 0,1,1)이 그 아래의
`display: flex`(0,1,0)를 이기고 있었다. 체크박스와 "로그인 유지"가 나란히 서지 못하고
**세로로 쌓여 있었다** — 이 재작성 이전부터. 타입을 붙여(`label.auth-check`) 특이도를 맞췄다.

### 9.4 검증 [실측]

라이트/다크 둘 다 브라우저에서 확인했다. 카드 위 대비는 제목 15.63:1 · 라벨 11.18:1 ·
부제 5.22:1로 전부 4.5:1을 넘는다. 다크에서 입력이 밝게 뜨는 것처럼 보였으나 축소
스크린샷의 리샘플링 아티팩트였고, 계산값은 카드와 같은 면(`#1c1c1e`)이다.

그리고 **`scale.test.ts`의 `EXEMPT`가 비었다.** 예외를 늘리는 것보다 지우는 것이 낫다.


---

## 10. 색 — "밋밋하다"의 정체는 액센트가 아니었다

스위스로 옮기고 나서 사용자가 화면이 밋밋하다고 했다. 팔레트를 뽑기 전에 **재 봤고**, 원인은
색이 지루해서가 아니었다.

### 10.1 세 가지 실측

**① 표면 세 개가 한 면이다.**

| | 대비비 |
|---|---|
| `--bg-app` ↔ `--bg-context-sidebar` | **1.012** |
| `--bg-context-sidebar` ↔ `--bg-rail` | 1.029 |

1.00이 구별 불가다. 사이드바와 캔버스는 눈에 한 면이었고, 세 개의 회색이 하나의 일을 하고
있었다. 다크도 같았다 — 1.019와 1.022.

**② 액센트가 AA에 미달한다.** `#007aff`는 흰 면 위에서 **4.02:1**. 글자로 쓰이는 자리(링크 ·
"Sign up" · 날짜 배지)가 4.5를 못 넘는다. 이 작업 이전부터 그랬다.

**③ CTA 역할이 없다.** colors.csv의 96개 팔레트가 전부 `Primary / Secondary / CTA` 세 역할을
나누는데, 이 앱은 `--accent` 하나가 "선택됨"과 "주 행동"을 겸한다.

### 10.2 후보 다섯을 재고 나서 [실측]

| 후보 | 액센트 글자 | 흰글자/CTA면 | 바닥↔사이드 |
|---|---|---|---|
| 현행 | 4.02 ✗ | 4.02 ✗ | 1.012 |
| **A 현행 + 역할분리** | **5.59** ✓ | 5.18 ✓ | **1.066** |
| B Portfolio 모노 | 5.17 ✓ | 5.17 ✓ | 1.053 |
| **C Productivity (스킬이 처음부터 밀던 것)** | **3.74 ✗** | **2.80 ✗** | 1.033 |
| D SaaS General | 5.17 ✓ | 2.80 ✗ | 1.047 |

**스킬이 첫 검색부터 추천하던 C가 제일 나쁘다.** 액센트 3.74 · CTA 2.80으로 현행보다도 낮다.
colors.csv에는 대비 검증이 없다 — 팔레트를 그대로 받으면 접근성이 내려간다. **사용자 결정: A.**

### 10.3 그런데 A의 액센트를 다크에 그대로 쓰면 나빠진다 [실측]

| | 흰 면 위 | 다크 카드(`#1c1c1e`) 위 |
|---|---|---|
| `#007aff` (현행) | 4.02 | 4.24 |
| `#0064d2` (A) | **5.59** | **3.04** |
| `#0a84ff` | — | **4.67** |

명도를 내리는 것이 라이트를 고치고 다크를 깨뜨린다. 액센트는 테마마다 달라야 한다.

그리고 그 값은 **이미 리포 안에 있었다**: 다크 블록의 `--accent-soft: rgba(10, 132, 255, …)`가
`#0a84ff`를 가리키고 있다. soft와 active만 다크용으로 맞춰져 있고 **`--accent` 본체는 한 번도
재정의된 적이 없었다.** 잠복해 있던 누락이다.

### 10.4 CTA는 넣지 않는다 [결정]

사용자가 처음에 CTA를 함께 요청했고, 재고 나서 세 가지를 확인한 뒤 철회했다.

1. **리포가 제2의 브랜드 색을 의도적으로 죽인 이력이 있다.** `componentLanguage.spec.ts`:
   > Today의 primary는 `--tdy-navy`였다. 두 색의 primary 둘이 존재하는 것이 이 단계가 끝내려는
   > 것이고, 액센트는 사용자가 바꿀 수 있는 쪽이다 (§11.3)
2. **액센트는 사용자가 고른다.** `[data-accent]`가 blue/purple/green/pink/**orange**를 준다.
   오렌지를 고른 사용자에게 고정 오렌지 CTA는 두 색을 한 덩어리로 뭉갠다.
3. **주원인이 이미 해결됐다.** 1.012 → 1.066이면 표면이 갈라진다.

부족하면 그때 얹는 것이 순서다. 예외를 늘리는 것보다 지우는 것이 낫다는 §9의 원칙과 같다.

### 10.5 결과

| | 라이트 전 → 후 | 다크 전 → 후 |
|---|---|---|
| 액센트 대비 | 4.02 → **5.60** | 4.24 → **4.67** |
| 바닥 ↔ 사이드 | 1.012 → **1.066** | 1.022 → **1.048** |
| 사이드 ↔ 레일 | 1.029 → **1.068** | 1.019 → **1.038** |
| 바닥 ↔ 카드 | 1.116 → 1.131 | 1.117 → 1.081 |

다크가 1.07까지 못 가는 것은 `--bg-surface`가 `#1c1c1e`로 고정이라 그 아래 남은 명도 폭이
좁기 때문이다. 현행의 두 배가 그 안에서의 최대치다.

`componentLanguage.spec.ts`가 액센트를 `rgb(0, 122, 255)`로 두 곳에서 못 박고 있어 토큰과
비교하도록 고쳤다 — §8.7의 넷에 이어 다섯째다. **스펙이 값을 적으면, 그 값이 움직일 때마다
스펙이 뒤를 따라다닌다.**


---

## 11. 간격 — 한 문제가 아니라 셋이었다

radius · font-size · font-weight · box-shadow는 Phase 3에서 훑었지만 `padding`/`margin`/`gap`은
손대지 않았다. 재 보니 **487개 선언이 스케일(4·8·12·16·24·32·48) 밖**이었고, 토큰을 쓰는 것은
23곳뿐이었다.

### 11.1 그런데 셋이 섞여 있었다 [실측]

| 묶음 | 개수 | 성격 |
|---|---|---|
| **6px(135) · 10px(127)** | 262 | 노이즈가 아니라 **두 번째 사다리**. 앱이 2px 단위로 돈다 |
| **2px(76) · 3px(23) · 1px(17)** | 116 | 헤어라인 · 광학 보정 |
| 14(47) · 20(22) · 18(17) · 28(7) · 22(5) · 홀수값 | ~109 | 진짜 드리프트 |

**6/10을 8/12로 올리면 태스크 행이 28.5px → 32.5px, 14% 높아진다** [실측 — 세 안을 나란히
렌더해 재봤다]. 한 화면의 태스크가 20개면 17개가 된다.

그리고 `20-density.css`가 이렇게 적어놨다:

> §11.2의 "compact productivity density"가 목표이고, Tasks Module의 자기 숫자(30px 행,
> 13px 라벨)가 legacy 페이지들이 맞춰 가야 할 기준

**조밀함은 문서로 정해진 목표이고, 자는 이번에 만든 것이다.** 자 하나를 얻자고 명시된 목표를
버리는 거래는 하지 않는다. **사용자 결정: 6/10은 사다리로 인정하고 드리프트만 스냅.**

### 11.2 스냅한 것과 남긴 것

```
5→4  7→6  9→8  11→10  13→12        (2px 사다리 위로)
14→16  18→16  20→24  22→24  28→32   (4px 스케일 위로)
26→24  36→32
```

133개 선언 + `--density-card-pad: 14px → 16px`(§8.2와 같은 자리 — 이름 뒤에 숨어 간격 스캔에
잡히지 않았다).

**남긴 것:**

- **1 · 2 · 3px** — 헤어라인과 광학 보정. `--icon-glyph`·`--display-*`를 타입 스케일에서 뺀
  것과 같은 논리로, "얼마나 떨어뜨릴까"가 아니라 "선이 몇 px인가"의 문제다.
- **6 · 10px** — §11.1의 결정.
- **68 · 52 · 90 · 110px** — 떠 있는 바를 피하는 여백 확보와 고정 헤더 상쇄. 리듬이 아니라
  다른 요소의 높이에 묶인 값이다.

### 11.3 파생값을 드리프트로 착각했다 [실측]

`.ff-timeline-bar.is-marker`의 `margin-left: -7px`를 −6으로 스냅했다가 되돌렸다.
바로 위에 `width: 14px`이 있고, **−7은 그 절반이다** — 45° 회전한 마커를 오늘 선 위에
광학적으로 앉히는 값이라 간격 사다리와 무관하게 14를 따라간다.

규칙: **인접한 치수의 절반이거나 배수인 값은 드리프트가 아니라 파생값이다.**

### 11.4 그리고 이번 실패는 내 것이 아니었다 [실측]

`e2e/ganttCompleted.spec.ts` 3개가 깨졌는데, 변경을 전부 stash하고 돌려도 **깨끗한 트리에서
똑같이 깨진다.** 어제(2026-09-05, 토)는 통과했다.

원인은 스펙이 절대 날짜와 상대 날짜를 섞은 것이다: `NOW = "2026-08-18"`은 고정인데 픽스처는
`dayOffset(-5)~(-2)`로 오늘 기준이다. **2026-09-06은 일요일**이라 주가 넘어가면서, 지난 주에
있던 완료 막대가 보이는 주 밖으로 나갔다. 매주 일요일마다 깨지는 시한폭탄이고, 이 문서의
작업 범위가 아니라 별도 과제로 넘겼다.


---

## 12. 타임라인을 세세히 보다가 스캐너의 틈을 찾았다

§8.3의 화면 실측은 6개 라우트를 돌았지만 **타임라인은 그 안에 없었다.** 그 화면을 띄우려면
리스트와 기간이 있는 작업이 필요한데, 그때는 빈 계정으로 훑고 있었다.

### 12.1 롱핸드 반경은 스캐너가 못 본다 [실측]

타임라인의 첫 막대가 `border-radius: 2px`로 그려지고 있었다. `scale.test.ts`는 이 파일을
위반 0곳으로 통과시켰고, `e2e/radiusScale.spec.ts`도 통과했다(그 스펙의 라우트 목록에도
타임라인이 없다).

```css
/* 12-timeline.css:528 */
.ff-timeline-bar.is-clipped-start { border-top-left-radius: 2px; ... }
```

**`border-top-left-radius`에는 `border-radius`라는 문자열이 없다.** 스캐너의 정규식은
`(?<![-\w])border-radius`를 찾으므로 롱핸드 네 형제를 통째로 지나쳤다.

전수 조사하니 **노출은 네 곳뿐**이고 전부 이 규칙이다(`font` 축약형으로 크기·굵기를 한 번에
세팅하는 자리는 0곳). 틈은 좁았지만 틈이었으므로 막았다 — 스캐너가 롱핸드도 센다.

### 12.2 그리고 그 2px은 드리프트가 아니었다

같은 파일이 이유를 적어놨다:

> 잘린 끝은 화살표를 단다: 온전해 보이는 막대는 3일짜리 작업으로 읽힌다 (D4)

**모서리를 둥글게 하는 값이 아니라 반대로 각지게 만드는 신호**다. 그래서 값을 고치지 않고
`EXEMPT`에 이유와 함께 적었다 — §9에서 비운 목록이 다시 한 줄을 갖는다. 예외는 늘리지 않는
것이 원칙이지만, **적어둔 예외는 예외가 아니라 결정이다.**

양방향 확인: 그 줄을 지우면 `12-timeline.css`에서 2곳이 잡히고, 다른 파일에
`border-top-left-radius: 7px`를 심으면 그것도 잡힌다.

### 12.3 §11.3의 마커를 이제야 화면에서 봤다 [실측]

`margin-left: -7px`를 −6으로 스냅했다가 되돌린 그 값은, 되돌릴 때까지 **한 번도 렌더된 적이
없었다.** `.ff-timeline-bar.is-marker`는 기간이 0인 작업(시작=마감)에만 나오는데 그런 작업이
계정에 없었다. 하나 만들어 재보니:

```
width 14px · margin-left -7px · transform rotate(45°)
-7 + 14/2 = 0        ← 앵커에 대한 중심 오차
```

−6이었으면 오차 1px로 날짜선에서 어긋난 채 나갔을 것이다. **이 자리를 덮는 테스트는 없다.**

### 12.4 타임라인의 나머지는 깨끗했다 [실측]

행 높이 32px(`--density-control-h`) · 막대 반경 4px · 막대 글자 대비 15.04 · 잘림 0 ·
가로 넘침 0. 막대의 `color: #111111`은 하드코딩이지만 주석이 `BLOCK_INK_DARK`라고 적고
`tintForDarkInk`가 그 값을 기준으로 맞춘다고 밝혀둔 자리다 — 토큰이 아니라 JS와 짝인 상수다.


---

## 13. 대비를 처음으로 전수 조사했다

§10은 액센트 하나를 재고 고쳤다. 6개 라우트를 **데이터를 넣고** 라이트·다크 양쪽으로 훑은
것은 이번이 처음이고, **139곳이 AA에 미달하고 있었다.**

### 13.1 같은 색이 글자와 면에서 반대 방향을 요구한다

§10이 놓친 것이 이것이다 — 액센트를 *글자*로만 재고 *면*으로는 재지 않았다.

| | 글자로 | 면으로(흰 글자) |
|---|---|---|
| 이전 `#007aff` | 4.02 ✗ / 다크 4.24 ✗ | 4.02 ✗ |
| §10 라이트 `#0064d2` | **5.59 ✓** | **5.59 ✓** |
| §10 다크 `#0a84ff` | **4.66 ✓** | **3.65 ✗ ← 내가 악화시켰다** |

라이트는 어둡게 내려서 둘 다 좋아졌지만, 다크는 밝게 올려서 글자만 좋아지고 면은 나빠졌다.
`--accent-fill`을 세워 다크에 `#0071e3`을 준다 — 흰 글자 4.70이면서 카드와도 3.62로 갈라지는
유일한 구간이고, 이미 `--color-primary-focus`라는 이름으로 리포에 있던 값이다.
흰 글자를 얹는 17개 규칙만 바꿨다. 점·선·토글 10곳은 액센트 그대로다.

### 13.2 그리고 그 규칙은 의미색에도 똑같이 적용된다

`--danger`·`--warning`·`--success`를 글자로 쓰면 흰 면에서 3.55·2.20·2.22다. 읽기용 짝은
이미 tint 계열에 있었으므로 이름을 세워 그 사실을 적었다(`--danger-ink` 등).

**그런데 잉크를 라이트 기준으로만 정의했다가 다시 틀렸다.** `--danger-ink: #c0271d`는 어두운
카드 위에서 **2.65** — 고치려던 면색보다도 안 보인다. 잉크는 "더 어두운 색"이 아니라 "읽히는
색"이고, 그 방향은 테마마다 반대다. 다크는 Apple의 다크 시맨틱 색(`#ff453a` 등)으로 간다.

### 13.3 --text-tertiary가 94곳이었다

`#8e8e93`은 흰 면에서 3.26이었고, §10에서 표면을 벌리며 사이드바 위에서 2.71까지 더 내려갔다.
tertiary 글자가 실제로 앉는 네 면 중 가장 어두운 것(사이드바 `#e7eaf0`)에서 4.5를 넘도록
`#68686d`로 내렸다 — 카드 5.54 · 바닥 4.90 · 사이드바 4.60. 다크는 이미 5.22라 그대로 둔다.

### 13.4 결과와, 내 측정이 두 번 틀렸다는 것

**라이트 0곳 · 다크 0곳.** (139 → 0)

다만 그 숫자에 이르는 동안 측정이 두 번 거짓말을 했다.

1. **반투명 배경을 합성하지 않았다.** 부모를 타고 올라가다 첫 색을 만나면 그것을 배경으로
   잡아서, `rgba(0,100,210,0.1)` 위의 `#0064d2`가 대비 1.00으로 나왔다. 초기 76곳의 절반이
   이 허위였다. 불투명 배경만 찾도록 고쳤다.
2. **HMR 잔상과 테마 적용 전 한 프레임.** CSS를 고친 뒤 하드 리로드 없이 재면 옛 값이
   나온다. `.tm-section-action`이 다크에서 라이트 색으로 두 번 잡혔는데, 하드 리로드 후
   다시 재니 5.91로 통과였다.

**교훈: CSS를 고친 뒤의 브라우저 측정은 하드 리로드 없이는 증거가 아니다.**

---

## 14. 미뤄둔 것들을 닫는다

### 14.1 간격에 가드가 없었다

§11이 133곳을 스냅했지만 `scale.test.ts`에는 `padding`/`margin`/`gap` 검사가 **0줄**이었다.
§7이 "문서만으로는 안 지켜진다, 340개 리터럴이 그 증거"라고 적어놓고 그 축만 비워둔 셈이다.

가드를 붙이자 **12곳이 더 잡혔다** — §11의 변환기가 px만 보고 rem을 보지 않아서
`23-oauth-consent.css`의 열한 자리를 통째로 지나쳤다. 그리고 `12-timeline.css`의
`margin-left: -7px`(§11.3의 파생값)도 잡혀서, 이유와 함께 EXEMPT에 적었다.

`--space-*` 토큰으로 치환하지는 않는다. 가드가 값을 강제하므로 이름은 덧칠이고,
radius·타입과 달리 간격은 값이 중앙에서 바뀔 일이 없다.

### 14.2 10px은 예외가 아니라 최소 단이었다

§5.1이 "`--label-size`의 기록된 예외"로 미뤄두고 "Phase 3에서 재보고 결정한다"고 적었다.
재보니 **열한 자리가 이 크기를 쓰는데 대문자 라벨은 하나뿐**이고 나머지는 체크박스 글리프 ·
미니월 요일 · 시간 라벨 · 배지다. 이름이 틀렸던 것이지 예외였던 것이 아니다.

`--type-2xs: 10px`으로 세우고 리터럴 10곳을 토큰으로 바꿨다. **6단이 아니라 7단이다.**

### 14.3 죽은 토큰 하나

`--density-icon-lg: 18px`은 정의만 있고 사용처가 0이었다. "글리프는 한 크기"라는 §5.1.2와도
어긋나므로 지웠다.

### 14.4 미니 타이머

렌더를 처음 확인했다. Pretendard가 오고 토큰이 해석되며(4px · 13px · 48px · 날카로운 그림자)
카드가 제대로 그려진다. 바탕과 글자색만 아직 `#f5f7fb`/`#111827`로 앱과 달라서, 이미 복제해둔
토큰 목록에 `--bg-app`·`--text-primary`를 더해 맞췄다.


### 14.5 "구성상 초록"이라고만 했던 것을 실제로 돌렸다

커밋을 나눌 때 각 커밋이 초록이 되도록 순서를 짰다고 적었지만, 체크아웃해서 돌려본 적은
없었다. 돌렸다.

| 검증 | 범위 | 결과 |
|---|---|---|
| vitest | 브랜치의 9개 커밋 전부 | 2,700~2,701 통과 · 실패 0 |
| playwright | 위험했던 둘 (`feat(design)` 토큰 도입 · `refactor(styles)` 리터럴 치환) | 각 212 통과 |

두 커밋의 실패 3개는 모두 `ganttCompleted.spec.ts`이고, §11.4가 밝힌 그 시한폭탄이다 —
오늘이 일요일이라 **모든 커밋에서, 그리고 깨끗한 트리에서도** 똑같이 깨진다. 어제 같은
스펙이 통과했던 것이 그 증거다. **이 브랜치가 만든 실패는 0이다.**
