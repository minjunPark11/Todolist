# 드리프트 가드 — 같은 사실을 두 곳에 적지 않기

## 0. 요약

이 저장소는 **자(ruler)를 테스트로 지킨다.** `scale.test.ts`가 스케일 밖 리터럴을
0으로 잠갔고, `radiusScale` · `verticalRhythm` · `surfaceHierarchy`가 grep이 못
보는 것 — 화면에 실제로 도달한 픽셀 — 을 잰다. 그 장치들이 잡는 것은 **값의
가짓수**다.

잡지 못하는 것이 하나 있다. **같은 사실이 두 곳에 적혀 있고 한쪽만 고쳐지는 것.**
값은 전부 자 위에 있는데도 두 자리가 서로 다른 말을 한다. 이 문서는 그 종류를
위한 장치 넷을 정한다.

원칙은 하나다 — **동기화하지 말고, 사실이 한 곳에만 있게 한다.** 두 곳에 두고
맞추는 일은 사람이 기억해야 하고, 사람은 기억하지 못한다. 이 문서의 네 장치는
전부 그 한 줄의 적용이다.

---

## 1. 증거 — 실제로 일어난 네 건

설계가 아니라 사고 기록이다. 넷 다 한 세션 안에서 나왔다.

### 1.1 `@container gcalbar` — 620 과 640

`02-calendar.css`가 컨테이너 폭 `≤640`에서 툴바 자손들에게 `width: 100%`를
걸고, `27-calendar.css`가 `≤620`에서 툴바의 고정 높이를 풀었다. 주석은 "임계값은
그쪽과 같아야 한다"고 적고 있었고, 값은 달랐다.

더 나빴던 것은 **27쪽 블록이 애초에 한 번도 적용되지 않았다**는 점이다.
`container`가 `.gcal-toolbar` 자신에게 붙어 있었고 `@container`는 자손에만
걸린다. 그래서 두 값이 어긋난 것조차 드러나지 않았다 — 한쪽이 죽어 있으면
불일치는 증상을 바꿀 뿐 신호를 내지 않는다.

뷰포트 769px에서 날짜 제목이 0px로 눌렸다. `CALENDAR_REFERENCE_PARITY_DESIGN.md`
§7-1.

### 1.2 §4.4 보정표 — 흰색 기준

미달 회색 12자리를 4.50~4.54로 끌어올린 표다. 그 수치는 **흰색 위에서 잰
값**이었고, 이 앱의 면은 `--bg-app #f6f6f4` · `--bg-rail #f1f1ef`다. 실제 면에서
`--text-faint`는 4.17 · 3.99로, 보정을 받은 자리가 하나도 없었다.

표의 숫자는 **코드에서 계산할 수 있는 값**이다. 사람이 한 번 재서 Markdown에
적었고, 그 뒤로 아무도 다시 재지 않았다. `POLISHED_REFERENCE_PARITY_DESIGN.md`
§4.4.1.

### 1.3 767 과 768

같은 "좁다"는 경계를 `19-app-shell.css`는 767로, 다섯 파일은 768로 적었다.
정확히 768px에서 레일만 남고 나머지가 좁은 폭 규칙으로 넘어가 있었다.

### 1.4 문서가 코드를 앞질러 낡는다

이 세션에서 **내가 직접 만든** 어긋남이다.

- `design.md` §9-1이 `720`을 브레이크포인트로 셌다. 그 값이 있던 파일을 그 뒤에
  내가 지웠고, 문서를 같이 고치지 않았다.
- `styles.css` 주석이 다섯 접두사가 "`src/` 전체에서 0회"라고 했다. 실제로 잰
  것은 `.tsx`/`.ts`뿐이었고, `21-components.css`에는 `.ovs-card-head`가 남아
  있었다.

**장치가 없으면 이 문서조차 낡는다.** 그것이 §4의 G4가 존재하는 이유다.

---

## 2. 진단 — 두 종류의 중복

| | 어디에 | 예 | 지금 잡히나 |
|---|---|---|---|
| **A. 코드↔코드** | CSS 두 자리가 같은 임계점을 각자 적음 | 620/640, 767/768 | ✗ |
| **B. 문서↔코드** | Markdown이 코드의 값을 베껴 적음 | §4.4 대비표, §9-1 목록 | ✗ |

A는 `scale.test.ts`가 못 잡는다. 620도 640도 767도 768도 전부 자 위의 값이기
때문이다. 자는 **값의 가짓수**를 재지 **같은 뜻인지**를 묻지 않는다.

B는 어떤 테스트도 보지 않는다. Markdown은 실행되지 않는다.

---

## 3. 결정 셋

**3.1 없앨 수 있으면 없앤다.** 사실이 두 곳에 있어야 할 이유가 대개 없다.
§4.4의 대비 수치는 토큰에서 계산되는 값이므로 문서가 아니라 테스트가 가져야
한다. 임계값이 두 파일에 필요하면 토큰 하나를 만들어 둘이 그것을 부른다.

**3.2 없앨 수 없으면 테스트가 같음을 단언한다.** 산문이 숫자를 말해야 할 때가
있다 — 근거를 설명하는 자리다. 그때는 그 숫자에 표식을 달고 테스트가 읽는다.

**3.3 둘 다 아니면 스냅샷이라고 적는다.** `DESIGN_ELEMENT_INVENTORY.md`처럼
감사 시점의 기록인 문서가 있다. 최신인 척하지 않게 하고, 정본이 어디인지를
가리킨다. 이미 그렇게 해 뒀다.

세 번째가 가장 약하다. 그러나 "낡았다고 적힌 문서"는 "낡은 줄 모르는 문서"보다
낫다.

---

## 4. 장치 넷

### G1 — 대비는 문서가 아니라 테스트가 잰다 *(§1.2를 잡는다)*

`25-reference.css`의 텍스트 토큰 23개 · 배경 토큰 13개를 읽어, 각 텍스트 토큰이
**놓일 수 있는 가장 어두운 면**에서 기준을 넘는지 단언한다.

- 글자: 4.5:1 (WCAG AA)
- 그래픽으로 선언된 것: 3:1 — `--text-prop-icon` · `--check-border`. 목록은
  테스트 안에 이유와 함께 적는다(§4.4의 예외 2건).
- 라이트/다크 각각.

이것이 들어오면 §4.4의 보정표는 **근거를 설명하는 산문**이 되고 숫자의 정본은
테스트가 된다. 숫자가 틀리면 문서가 아니라 빌드가 말한다.

`scale.test.ts`처럼 grep이 아니라 계산이므로, 토큰 값이 바뀌면 자동으로 다시
잰다. §4.4가 흰색으로 쟀다는 사실은 이 장치가 있었다면 그날 잡혔다.

### G2 — 브레이크포인트도 자 위에 올린다 *(§1.1 · §1.3을 잡는다)*

`styles.css`가 부르는 모든 CSS에서 `@media` · `@container`의 폭 임계값을 전부
뽑아, 허용된 사다리 안에 있는지 검사한다. 지금 쓰이는 값은 이렇다.

| 값 | 곳 | 짝 |
|---|---|---|
| 600 | 1 | — |
| 639 | 1 | (640-1) |
| 680 | 2 | — |
| 768 | 7 | — |
| 900 | 8 | 901 |
| 960 | 3 | 961 |
| 1023 | 2 | 1024 |
| 1120 | 1 | — |
| 1240 | 2 | — |
| 1279 | 2 | (1280-1) |
| 1350 | 1 | — |

**0에서 시작할 수 있다.** 열한 개를 사다리로 선언하고, 열두 번째가 들어오면
실패한다. 새 값이 정말 필요하면 표에 이유와 함께 한 줄을 더한다 — `scale.test.ts`
주석이 못박은 그 방식이다: *"숫자를 적는 것이 아니라, 고치거나 EXEMPT에 이유를
적는 것이 답이다."*

`@container`는 이름별로 따로 본다. 같은 컨테이너 이름에 **두 개의 서로 다른
임계값**이 있으면 그 자체로 실패다 — §1.1이 정확히 그것이었다.

덧붙여 §1.1의 더 깊은 원인도 잡는다: **`@container` 블록의 셀렉터가 그 컨테이너를
선언한 셀렉터와 같으면 실패.** 자기 크기를 물어 자기를 고치는 규칙은 성립하지
않는데, 브라우저는 조용히 무시할 뿐 말해주지 않는다.

### G3 — 고아 셀렉터 *(§1.4의 `.ovs-card-head`를 잡는다)*

CSS가 정의한 클래스는 `.tsx`/`.ts` 어딘가에서 불려야 한다. `componentLanguage.spec.ts`가
이미 규칙을 적어 뒀다 — *"별칭은 어딘가에서 불려야 그 줄을 얻는다."* 그 규칙을
파일 단위가 아니라 셀렉터 단위로 자동화한다.

**0에서 시작할 수 없다.** 지금 155개다.

| 파일 | 고아 |
|---|---|
| `21-components.css` | 32 |
| `05-spaces.css` | 32 |
| `03-planning.css` | 30 |
| `08-calendar-categories.css` | 22 |
| `12-timeline.css` · `02-calendar.css` | 8 · 8 |
| 나머지 일곱 파일 | 23 |

그래서 `scale.test.ts`가 407곳에서 시작했을 때 쓴 방법을 그대로 쓴다 — **총합
하나가 아니라 파일당 천장 하나.** 파일은 커밋 단위이고, 파일별 숫자는 서로를
밀지 않으며, 하나씩 0이 되면 목록에서 빠진다.

허용 목록은 이유를 요구한다. `.ff-card`와 `.sdv-btn` 계열이 그 첫 항목이다 —
호출부는 없지만 컴포넌트 언어의 정본이고 `componentLanguage.spec.ts`가 불변식으로
지킨다. **이유 없이는 목록에 올릴 수 없다.**

### G4 — 산문이 숫자를 말할 때 *(§1.4의 `720`을 잡는다)*

없앨 수 없는 B형 중복을 위한 최소 장치다. 문서가 코드의 사실을 말해야 하면
표식을 단다.

```markdown
브레이크포인트는 <!--@ breakpoints --> 600 · 639 · 680 · 768 · 900 <!--@--> 이다.
```

테스트가 `@` 블록만 읽어 코드에서 같은 값을 뽑아 비교한다. **표식이 없는 산문은
검사하지 않는다** — 그래야 도입 비용이 0이고, 거짓 실패가 없다.

지원할 추출기는 처음엔 셋이면 된다: `breakpoints`(미디어/컨테이너 임계값),
`token <이름>`(CSS 변수 값), `files`(`styles.css`의 `@import` 목록). §1.4의 두
건은 첫째와 셋째였다.

---

## 5. 단계

| | 하는 일 | 0에서 시작 | 파일 |
|---|---|---|---|
| **D1** | G2 — 브레이크포인트 사다리 + 컨테이너 규칙 | ✅ | `src/styles/breakpoints.test.ts` |
| **D2** | G1 — 대비 불변식 | ✅ | `src/styles/contrast.test.ts` |
| **D3** | G3 — 고아 셀렉터, 파일별 천장 | ✗ (155) | `src/styles/orphans.test.ts` |
| **D4** | G4 — 산문 표식 | ✅ | `src/docs/claims.test.ts` |
| **D5** | §4.4 보정표를 산문으로 되돌리고 숫자의 정본을 D2로 옮긴다 | — | 문서 |

넷 다 `src/` 아래에 둔다. `vitest.config.ts`의 `include`가
`["src/**/*.test.ts", "src/**/*.test.tsx"]`이므로 저장소 루트나 `docs/`에 둔
테스트는 실행되지 않는다 — D4가 문서를 읽더라도 테스트 파일 자체는 `src/`에
있어야 한다. (이 줄은 초고에서 `docs/claims.test.ts`라고 잘못 적었다가 고친
자리다. 이 문서가 다루는 바로 그 종류의 실수다.)

D1과 D2를 먼저 하는 이유는 **0에서 시작할 수 있어서**다. 천장이 필요한 D3는
그 뒤다 — 천장은 빚이고, 빚 없는 장치를 먼저 세워야 천장이 한시적이라는 것이
분명해진다.

D5는 D2가 들어온 뒤에만 의미가 있다. 숫자를 지우려면 지울 곳이 먼저 있어야
한다.

---

## 6. 검증

- `vitest run` — 네 테스트가 현재 트리에서 통과한다(D3는 천장과 함께).
- **회귀 재현** — 각 장치가 §1의 사고를 실제로 잡는지, 값을 일부러 되돌려
  확인한다. 잡지 못하면 그 장치는 설계가 틀린 것이다.
  - G2 ← `27-calendar.css`의 640을 620으로
  - G2 ← `19-app-shell.css`의 768을 767로
  - G2 ← `container`를 `.gcal-toolbar`로 되돌리기
  - G1 ← `--text-faint`를 `#72777E`로
  - G3 ← `.ovs-card-head` 되살리기
  - G4 ← `design.md` §9-1에 `720` 되넣기
- 실행 시간 — 넷 다 파일 읽기와 산술뿐이다. 브라우저가 필요 없으므로 유닛에
  둔다. `scale.test.ts`와 같은 자리다.

---

## 6-1. 구현 기록 — D1 · D2

들어갔다. `src/styles/breakpoints.test.ts`(5) · `src/styles/contrast.test.ts`(8),
둘 다 **천장 없이 0에서** 시작한다.

**회귀 재현 — §6이 요구한 여섯 중 다섯을 확인했다.** 값을 되돌리고 실패하는지 봤다.

| 되돌린 것 | 잡은 테스트 | 메시지 |
|---|---|---|
| `27-calendar` 640 → 620 | 컨테이너 사다리 · 이름당 하나 | `gcalbar: 640px(02-calendar.css:39) vs 620px(27-calendar.css:498)` |
| `19-app-shell` 768 → 767 | 뷰포트 사다리 | `19-app-shell.css:355 767px` |
| `container`를 `.gcal-toolbar`로 | 자기참조 | `@container gcalbar → .gcal-toolbar` |
| `--text-faint` → `#72777E` | 라이트 텍스트 | `on --bg-rail #f1f1ef = 3.99` |
| `--bg-sidebar` 그림자 정의 | 다크 텍스트 · 그래픽 | `--text-primary #f5f5f7 on --bg-sidebar #ffffff = 1.09` |

여섯째(`720`)는 D4의 몫이라 아직이다.

### 구현하며 드러난 것

**(1) 잠복해 있던 토큰 하나.** G1을 켜자 다크에서 `--bg-sidebar`가 `#ffffff`로
나왔다. `01-base.css`가 라이트 #ffffff(:90)와 다크 #1c1c1e(:557) 한 쌍으로
갖고 있는데, `25-reference.css`의 `:root` 한 줄이 그 쌍을 깨고 있었다 —
`[data-theme="dark"]`와 `:root`는 같은 (0,1,0)이고 25-reference가 나중에
로드된다. 브라우저에서 재서 확인했다(`--bg-sidebar: #ffffff`).

§5.3이 `--accent`에 대해 똑같은 함정을 적어 뒀다 — *"여기 한 줄을 적으면 그
넷을 전부 덮어서 고른 색이 화면에 도달하지 않는다. 실제로 그렇게 죽여놨었다."*
같은 실수가 표면 토큰에서 한 번 더 있었고, 이번에는 소비자가 없어
(`--ff-sidebar`를 부르는 자리 0곳) 화면에 닿지 않았을 뿐이다. 그림자 정의를
걷어냈다.

**이것이 G1을 만든 이유 그 자체다.** 사람이 한 번 재서 적은 표는 이 토큰을
영영 보지 못한다. 계산하는 쪽은 켜는 순간 본다.

**(2) 분류가 검사보다 중요하다.** 처음엔 텍스트 토큰을 전부 모든 면에 대보게
했더니 `--toast-ink #ffffff`가 흰 면 위에서 1.0으로 떨어졌다 — 화면에 없는
조합이다. 그래서 넷으로 나눴다: TEXT(4.5) · GRAPHIC(3.0) · PAIRS(정해진 면) ·
DECORATIVE(기준 없음, 이유 필수). 그리고 **넷 중 어디에도 없는 새 회색이
들어오면 실패**하는 테스트를 하나 더 뒀다. 분류되지 않은 토큰을 조용히 건너뛰는
것은 §4.4가 흰색 하나만 보고 통과를 선언한 것과 같은 모양의 침묵이다.

**(3) 틀린 줄 번호는 없는 것보다 나쁘다.** 주석 안의 `@container` 인용이
임계값으로 세어지는 것을 막으려고 주석을 지웠더니, 뒤따르는 offset이 전부 밀려
`27-calendar.css:498`을 `:397`이라고 불렀다. 개행만 남기고 같은 길이의 공백으로
갈아끼우는 것으로 고쳤다.

### DECORATIVE에 남은 한 줄

`--hint-glyph`뿐이다. `.tm-drawer-prop::after`의 `›` 하나이고 `opacity: .42`와
`pointer-events: none`이 걸려 있다. 행 자체가 이미 눌리는 대상이라 WCAG 1.4.11이
장식을 제외하는 자리다. 다만 raw 값 `#8e939a`는 `--bg-rail` 위에서 2.73이고
0.42가 곱해지면 더 내려간다 — **"장식이니 괜찮다"는 판단이지 통과가 아니므로**
사람이 다시 볼 수 있게 남겨 뒀다.

---

## 7. 하지 않을 것

- **모든 산문을 검사하지 않는다.** G4는 표식을 단 곳만 본다. 문서 전체를 파싱하려
  들면 거짓 실패가 쌓이고, 거짓 실패가 쌓이면 아무도 읽지 않는다.
- **고아를 자동으로 지우지 않는다.** G3는 세고 보고할 뿐이다. `.ff-card`처럼
  일부러 남기는 이름이 있고, 그 판단은 사람의 것이다.
- **e2e로 옮기지 않는다.** 여기 넷은 전부 소스를 읽어 답이 나온다. 브라우저가
  필요한 검사는 이미 `radiusScale` · `verticalRhythm` · `surfaceHierarchy`가
  맡고 있고, 둘은 잡는 것이 다르다.
- **155를 이번에 0으로 만들지 않는다.** D3는 천장을 세우는 일이지 빚을 갚는
  일이 아니다. 갚는 것은 파일별로, 그 파일을 만지는 커밋에서.
