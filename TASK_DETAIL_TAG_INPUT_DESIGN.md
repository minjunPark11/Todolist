# 본문은 쓴 만큼만 차지하고, `#`은 그 자리에서 태그를 부른다

> 상태: **설계 · 2차** · 2026-09-08 (리뷰 반영 — D1·D3·D4·D6은 조건이 붙었고 D5는 갈렸다. §11)
> 요청: "내용에 `#`을 치면 태그 종류가 뜨게. `+`도 저렇게 예쁘게. 그리고 본문에 최소
> 입력 칸이 정해져 있는 게 아니라, 내용이 없으면 칸이 없는 것으로 쳐서 바로 밑에
> 태그가 붙게."
> 대상: `components/kit.tsx` · `components/tasks/TagPicker.tsx` ·
> `components/tasks/TaskDrawer.tsx` · `domain/tags/inlineTagQuery.ts`(신규) ·
> `styles/17-tasks-module.css` · `styles/01-base.css` · `e2e/radiusScale.spec.ts`
> 선행 문서: `TASK_TAG_CHIPS_DESIGN.md`(칩과 색 — 이 문서는 그 위에 입력을 얹는다) ·
> `QUICK_ADD_INPUT_BOX_DESIGN.md` §6(퀵애드의 태그 선택기 — §4가 그 컴포넌트를 재사용한다) ·
> `SWISS_MINIMAL_DESIGN.md` I1-B(반지름 스케일 — §5가 그 예외 목록을 건드린다)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 4장 — TickTick의 `#` 자동완성 · TickTick의 알약 `+` · 우리 상세(빈 본문이 90px를 잡고 태그를 아래로 민다) · TickTick 상세(본문이 한 줄, 태그가 바로 밑) | [관찰] |
| **실측** | `01-base.css`의 `textarea { min-height: 90px }`가 전역이다 · 본문은 `rows={3}` · `DeferredTextarea`는 `kit.tsx`의 공용이고 노트도 같은 것을 쓴다 · `TagOptions`가 이미 이름 목록 위의 선택기다 · `.tm-tag-add`는 24px 점선 사각형 · `e2e/radiusScale`의 스케일은 `[4]` 하나이고 20px 이상만 잡는다 | [실측] |
| **추론** | 태그가 붙은 화면이 그 스펙에 한 번도 안 나와서 알약 칩이 아직 안 걸렸다 (§5.2) | [추론] |
| **결정** | 사용자 — `#` 자동완성 · 알약 `+` · 본문 최소 높이 없음 | [결정] |
| **미확인** | 캐럿 좌표에 메뉴를 붙일 것인가, 필드 아래에 붙일 것인가 (§3.4) | [미확인] |

---

## 1. 세 요청은 한 화면의 세 층이다

```
┌ 상세 ───────────────────────────┐
│ asd                             │  제목
│ aa                              │  ③ 본문 — 쓴 만큼만
│ [학교] (+)                      │  ② 알약 +
└─────────────────────────────────┘
   ↑ ① 본문에서 `#`을 치면 여기 붙을 태그를 고른다
```

①은 기능, ②는 모양, ③은 레이아웃이다. 셋 다 독립이라 순서를 바꿔도 되고, 그래서
§8이 셋을 따로 놓는다.

---

## 2. 결정 요약

| # | 결정 | 근거 절 |
|---|---|---|
| **D1** | 본문의 `#`은 **토큰을 먹고 태그를 붙인다.** 붙이기 전용 — 토글이 아니다 | §3.2, §3.8 |
| **D2** | 고르는 목록은 **`TagOptions` 그대로**다. 새 선택기를 만들지 않는다 | §3.3 |
| **D3** | 메뉴는 **필드 아래**, 레이어 위에 뜬다. 캐럿 좌표를 계산하지 않는다 | §3.4 |
| **D4** | `+`는 칩과 같은 높이의 **알약**이 된다. 점선은 유지 | §5.1 |
| **D5** | 본문의 최소 높이를 **없앤다.** 빈 본문은 **한 줄**(플레이스홀더)이고, 0이 아니다 | §6, §11 |
| **D6** | 칩과 `+`를 **반지름 예외 목록에 적는다.** 스펙이 아직 못 본 것을 적어두는 것이다 | §5.2 |

---

## 3. 본문의 `#` — 태그를 부른다

### 3.1 무엇이 이미 있나 [실측]

퀵애드가 같은 일을 이미 한다. `splitInlineTags`가 `물 사기 #장보기`를 제목과 태그로
가르고, `TagOptions`가 이름 목록 위의 선택기다. **없는 것은 타이핑 중의 목록뿐이다.**

### 3.2 규칙 [D1]

```
aa #학|          ← 본문에 `#학`까지 쳤다
      └ 메뉴: 학교

(고르면)
aa |             ← 토큰이 사라지고
[학교] (+)        ← 칩이 붙는다
```

**토큰은 본문에 남지 않는다.** 남기는 안은 기각한다 — 우리 본문은 서식 없는
`textarea`라 TickTick처럼 글 안에서 칩으로 그릴 수 없고, `#학교`라는 **글자**로 남으면
그 줄은 태그도 문장도 아닌 것이 된다. 게다가 그 글자는 구글 설명으로 나간다
(`1427880`이 방금 걷어낸 바로 그 모양이다).

그러므로 규칙은 퀵애드와 **같다**: `#`은 타이핑 문법이고 표시가 아니다. 한 문장으로
설명되는 규칙이 앱에 하나만 있게 된다.

### 3.3 목록은 있는 것을 쓴다 [D2]

`TagOptions`는 이미 이름 목록 위에서 돌고, 필터·새 태그 만들기·거절 사유를 전부
가지고 있다(`TagPicker.tsx`). 본문 편집기는 **질의 문자열과 콜백만** 넘긴다.

### 3.4 어디에 뜨나 [D3]

캐럿 좌표에 붙이려면 textarea의 내용을 그대로 복제한 그림자 요소를 만들어 그 안에서
캐럿 위치를 재야 한다 — 글꼴·줄바꿈·스크롤을 전부 따라 해야 하고, 하나라도 어긋나면
메뉴가 엉뚱한 곳에 뜬다.

**필드 아래 왼쪽에 붙인다.** 상세 패널은 폭이 좁고 본문은 몇 줄이라, 캐럿과 필드
아래의 거리는 대부분 한두 줄이다. 정확도의 값이 그 복잡도를 못 이긴다.

**단, 흐름 안에 그리지 않는다** — 레이어 위에 띄운다. `.tm-drawer-scroll`이
`overflow-y: auto`라(`17-tasks-module.css`) 그 안에 그린 메뉴는 패널 밑변에서 잘리고,
본문이 패널 아래쪽에 있을 때가 정확히 메뉴가 필요한 때다. 앱의 `Popover`가 이미 그
일을 한다 — 레이어 스택에 등록되므로 Esc와 바깥 클릭도 §19.93이 판정한다(Q2).

### 3.5 질의를 읽는 규칙 [순수]

`inlineTagQuery(text, caret)`가 답한다. 캐럿 **바로 앞**을 보고:

| 상태 | 답 |
|---|---|
| 캐럿 앞에 단어를 시작하는 `#`이 있고 그 뒤가 공백 없는 글자들 | `{ query, from, to }` |
| `#`이 단어 중간이다 (`C#`) | `null` — §4.2의 규칙과 같다 |
| `#`과 캐럿 사이에 공백이 있다 | `null` — 토큰이 끝났다 |
| `#`이 없다 | `null` |

순수 함수 하나, 픽스처로 증명한다. 이것이 정확하면 나머지는 메뉴를 여닫는 일뿐이다.

### 3.6 한글 조합 중에는 고르지 않는다 [실측]

`#학`을 치는 동안 IME는 조합 중이다. 조합 중의 Enter는 **후보 선택**이지 태그 선택이
아니다 — `CreateListModal`이 §1.9/§3.26에서 이미 같은 함정을 밟았고, 같은
`isComposing` 가드를 쓴다. 목록은 조합 중에도 갱신한다(보이는 편이 낫다). 막는 것은
**확정 키**뿐이다.

### 3.7 고르는 순간 두 가지가 일어난다 — 그리고 지금은 둘째를 할 수단이 없다 [실측]

1. 태그가 붙는다 (즉시, 저장까지 간다)
2. 토큰을 지운 본문이 필드에 쓰인다 (그 편집이 평소의 디바운스를 탄다)

2번이 문제다. `useDeftedTextField`가 밖으로 내주는 것은 `value`와 이벤트 핸들러들뿐이고
(`hooks/useDeferredTextField.ts:188`), `DeferredTextarea`는 `onChange`조차 자기가
쥐고 있다(`kit.tsx:107`). **밖에서 값을 바꿀 문이 없다.**

그러므로 이 문서는 그 문을 하나 낸다:

```ts
// useDeferredTextField가 하나 더 돌려준다
replace(from: number, to: number, text: string): void
```

초안이 "순서만 지키면 된다"고 쓴 것은 **틀렸다.** 순서는 필요조건이고, 값을 쓸 수단이
없으면 순서를 지킬 대상 자체가 없다. 리뷰가 지적한 race는 이 구멍이다.

`replace`는 드래프트만 바꾸고 커밋은 평소의 디바운스에 맡긴다 — 태그 하나 고를 때마다
본문을 저장으로 밀어 넣으면, 글을 쓰는 중에 저장이 튀는 필드가 된다.

### 3.8 붙이기 전용이다 [D1]

`TagPicker`가 쓰는 콜백은 `onToggle`이다 — **이미 달린 태그를 고르면 뗀다.** 본문에서
`#학교`를 치고 목록의 `학교`를 고르는 사람은 그것을 붙이려는 것이지 떼려는 것이 아니다.
토글을 그대로 쓰면 이미 붙어 있는 태그를 고를 때 토큰만 사라지고 태그가 떨어진다.

그러므로 인라인 경로는 **붙이기 전용**으로 감싼다:

| 상태 | 하는 일 |
|---|---|
| 아직 안 붙었다 | 붙인다 + 토큰을 지운다 |
| 이미 붙어 있다 | **아무것도 안 붙이고** 토큰만 지운다 |

`TagOptions`(D2)는 목록을 그리는 컴포넌트일 뿐이므로 그대로 쓰고, **선택의 의미만**
이 감싸개가 정한다. 목록에서 이미 붙은 태그는 체크 상태로 보이므로, 그것을 골랐을 때
아무 일도 안 일어나는 것이 읽히는 결과다.

---

## 4. 무엇을 하지 않는가

- **본문 안에 칩을 그리지 않는다.** `textarea`에는 요소를 넣을 수 없고, 서식 있는
  편집기로 바꾸는 것은 이 요청의 백 배짜리 작업이다.
- **`#` 자동완성을 제목에 붙이지 않는다.** 제목은 한 줄이고 퀵애드가 이미 그 자리다.
- **없는 태그를 본문에서 만들지 않는다** — 는 열어둔다(§9 Q1). `TagOptions`의 만들기
  줄이 딸려 오므로 막는 편이 오히려 코드가 는다.

---

## 5. `+`와 반지름

### 5.1 알약 `+` [D4]

지금은 24px 점선 **사각형**이다(`.tm-tag-add`, `radius-sm`). [관찰] 둘째 장의 `+`는
옆 칩과 **같은 높이의 알약**이고, 점선 테두리는 그대로다 — 점선이 "여기에 더한다"를
말하고, 알약이 "더해지는 것은 칩이다"를 말한다.

```
[ 학교 ]  ( + )     ← 같은 높이, 같은 곡률
```

계약을 값으로 적는다. 옆에 서는 두 요소의 높이가 1px만 어긋나도 줄이 흔들린다:

| 값 | 칩 | `+` | 왜 |
|---|---|---|---|
| 높이 | `line-height: 18px` + 상하 1px = **20px** | **20px** 고정 | 같은 줄에 서므로 같아야 한다 |
| 곡률 | `--rounded-pill` | `--rounded-pill` | §5.2에 함께 신고한다 |
| 좌우 padding | 8px | **0** — 폭은 `min-width`가 만든다 | `+` 하나는 가운데 서야 한다 |
| 폭 | 내용만큼 (최대 10em) | **24px** `min-width` | 손가락이 닿는 최소, §4.86의 타깃과 별개로 시각 폭 |
| 테두리 | 없음 (채움) | 점선 1px 유지 | 점선이 "더한다"를 말한다 |

`+` 자체의 탭 타깃은 `--density-tap`이 따로 보장하므로, 위의 24px은 **보이는 폭**이고
누를 수 있는 넓이가 아니다. 둘을 한 값으로 합치면 밀도 설정이 칩 줄을 밀어낸다.

### 5.2 그런데 알약은 스케일 밖이다 [추론 → 실측]

`e2e/radiusScale`의 스케일은 **`[4]` 하나**다. 20px 이상인 모든 것이 4px여야 하고,
예외는 목록에 적힌 것뿐이다 — 배지와 카운트는 §11.2가 알약을 지켜준 자리라 목록에
있다(`.gcal-cat-badge`, `.ff-projbadge`).

**`.tm-tag-chip`은 이미 알약인데 목록에 없다.** 그 스펙이 태그 붙은 화면을 한 번도
그리지 않아서(픽스처의 작업에 태그가 없다) 지금까지 안 걸렸을 뿐이다. 0.22.14가 들고
나간 미신고 위반이다.

그러므로 D6은 새 예외가 아니라 **밀린 신고**다. 둘 다 적는다:

```ts
".tm-tag-chip", // 태그는 배지다 — §11.2가 알약을 지켜주는 그 자리
".tm-tag-add",  // 그 배지를 더하는 버튼이므로 같은 모양이어야 한다
```

적으면서 스펙이 태그를 실제로 그리게 만든다(§8의 2단계). 예외를 적어놓고 화면에
없는 것은 예외가 아니라 죽은 줄이다.

**그리고 예외는 검사를 끄는 줄이다.** 목록에 적는 순간 그 요소의 곡률은 아무도 안 본다 —
알약이 아니라 실수로 6px이 되어도 스펙은 초록이다. 그래서 예외와 **짝이 되는 단언**을
같이 넣는다:

```ts
// 예외로 빠진 두 모양이 정말 알약인지, 그 스펙이 직접 잰다
expect(radius).toBeGreaterThanOrEqual(height / 2);
```

빼는 줄 하나에 재는 줄 하나. 그래야 예외가 "이 모양은 다르다"이지 "이 모양은 안 본다"가
되지 않는다.

---

## 6. 본문은 쓴 만큼만 [D5]

### 6.1 지금 무엇이 90px를 만드나 [실측]

```css
/* 01-base.css — 전역 */
textarea { min-height: 90px; resize: vertical; }
```

여기에 `rows={3}`이 겹친다. 그래서 [관찰] 셋째 장처럼 빈 본문이 세 줄을 잡고, 태그가
그만큼 아래로 밀린다.

### 6.2 규칙

- 최소 높이는 **한 줄**이다 — 0이 아니다(§11에서 리뷰와 갈린 지점).
- 상세의 본문과 노트에 한해 전역 90px를 끈다.
- 내용이 늘면 **같이 자란다.** 스크롤바가 생기지 않는다 — 상세는 이미 패널이 스크롤되고,
  그 안에서 또 스크롤되는 상자는 두 개의 스크롤을 만든다.

자라게 하는 법은 하나뿐이다. `textarea`는 자기 내용 높이를 CSS로 말할 수 없으므로
`scrollHeight`를 읽어 높이를 쓴다. `DeferredTextarea`(공용)에 `autoGrow` 프롭 하나로
넣는다 — 노트도 같은 것을 쓰므로 둘이 한 번에 해결된다.

`rows={3}`은 남긴다. **자바스크립트가 돌기 전의 높이**이고, 그 한 순간을 세 줄로
잡아두면 첫 그림에서 아래가 밀렸다가 올라오는 일이 없다… 는 것이 초안이었는데
틀렸다: 세 줄로 그렸다가 한 줄로 줄이면 그게 바로 그 튐이다. **`rows={1}`으로 내린다.**

### 6.3 D5가 §3(스페이서)과 만나는 곳

`TASK_TAG_CHIPS_DESIGN.md` §3이 남은 높이를 스크롤 끝의 스페이서에 줬고, 그 스페이서를
누르면 본문에 캐럿이 간다. 본문이 한 줄로 줄면 **그 스페이서가 훨씬 커진다** — 즉
[관찰] 넷째 장의 빈 화면 대부분이 "누르면 본문이 열리는 자리"가 된다. 두 결정이 서로를
돕는다.

---

## 7. 무엇이 바뀌는가

| 파일 | 변화 |
|---|---|
| `domain/tags/inlineTagQuery.ts` | **신규.** 캐럿 앞의 `#질의`를 읽는 순수 함수 (§3.5) |
| `components/kit.tsx` | `DeferredTextarea`에 `autoGrow` (§6.2) |
| `components/tasks/TaskDrawer.tsx` | 본문에 `#` 메뉴를 얹고, `rows={1}`, `autoGrow` |
| `components/tasks/TagPicker.tsx` | `TagOptions`를 그대로 재사용 — 변경 없음 또는 프롭 하나 |
| `styles/01-base.css` | 전역 `textarea` 최소 높이를 상세에서 끄기 |
| `styles/17-tasks-module.css` | `.tm-tag-add`를 알약으로 (§5.1) |
| `e2e/radiusScale.spec.ts` | 예외 두 줄 + 태그가 있는 화면을 실제로 그리기 (§5.2) |

---

## 8. 단계

| # | 항목 | 선행 |
|---|---|---|
| 1 | 본문 높이 — 전역 최소 끄기 · `rows={1}` · `autoGrow` (§6) | 없음 |
| 2 | `+` 알약 + 반지름 예외 두 줄 + 스펙이 태그를 그리게 (§5) | 없음 |
| 3 | `inlineTagQuery` + 본문의 `#` 메뉴 (§3) | 없음 |

셋 다 독립이다. 1이 가장 짧고 화면이 가장 크게 바뀌므로 먼저 간다.

---

## 9. 열린 질문

| # | 질문 | 기본값 제안 |
|---|---|---|
| Q1 | 본문의 `#`으로 **없는 태그를 만들** 수 있게 할 것인가 | 할 수 있게. `TagOptions`가 이미 그 줄을 그리고, 막는 쪽이 코드가 는다 |
| Q2 | 메뉴가 열려 있을 때 `Esc`는 메뉴만 닫는가 상세도 닫는가 | 메뉴만. 레이어 스택이 이미 그렇게 판정한다(§19.93) |
| Q3 | 노트에도 `#`을 넣는가 | 넣지 않는다. 태그를 다는 자리가 셋이면 어디서 단 것인지 아무도 모른다 |
| Q4 | 자동완성에 아무것도 안 걸리면 | 메뉴를 닫는다. 빈 메뉴는 고장으로 읽힌다 |
| Q5 | 빈 본문을 **0 높이**로 접을 것인가 (§11) | 접지 않는다. 한 줄 플레이스홀더 — 0이면 설명을 처음 쓰는 길이 화면에서 사라진다 |

---

## 10. 실패 모드

| 무엇 | 결과 | 대응 |
|---|---|---|
| 본문 가운데에서 `#`을 침 | 그 자리의 토큰만 먹는다 | `inlineTagQuery`가 `from`/`to`를 준다 |
| 조합 중 Enter | 후보 선택이 태그 선택으로 새는 것 | §3.6 `isComposing` 가드 |
| 아주 긴 본문 | `autoGrow`가 매 입력마다 `scrollHeight`를 읽는다 | 한 요소의 동기 레이아웃 한 번. 문제가 되면 그때 잰다 |
| 태그를 고른 뒤 본문이 되돌아감 | 미저장 편집 손실 | §3.7의 순서 — 태그 먼저, 본문 쓰기 나중 |
| 반지름 스펙이 여전히 태그를 못 봄 | 예외 줄이 죽은 줄이 됨 | §5.2 — 스펙이 태그를 그리게 만든다 |
| 이미 붙은 태그를 목록에서 고름 | 토글이면 태그가 떨어진다 | §3.8 — 붙이기 전용 감싸개 |
| 본문이 패널 아래쪽에 있을 때 메뉴 | 스크롤 상자에 잘린다 | §3.4 — 레이어 위에 띄운다 |

---

## 11. 리뷰 반영 (2차)

| # | 지적 | 결과 |
|---|---|---|
| D1 | toggle이 아니라 attach 의미 · description 저장 race | **받는다.** §3.8이 붙이기 전용을 정하고, §3.7이 race의 정체를 다시 쓴다 — 순서 문제가 아니라 **밖에서 값을 쓸 문이 없다**는 것이었다 |
| D2 | UI 재사용, 선택 semantics만 감싸기 | **받는다.** §3.3 + §3.8. `TagOptions`는 그리기만 하고 의미는 감싸개가 정한다 |
| D3 | overlay로 띄워야 함 | **받는다.** §3.4 — `.tm-drawer-scroll`의 `overflow-y: auto`가 잘라낸다. 앱의 `Popover`를 쓴다 |
| D4 | 폭/padding까지 contract 명시 | **받는다.** §5.1에 높이·곡률·padding·폭·테두리를 값으로 적었다 |
| D5 | empty+unfocused는 **0 높이**가 원 요청에 정확 | **반만 받는다** — 아래 |
| D6 | 실제 pill인지 별도 검증 | **받는다.** §5.2 — 예외 한 줄에 재는 단언 한 줄 |

### D5는 왜 0이 아닌가

요청의 글자를 보면 지적이 맞다: "내용이 없으면 칸이 없는 것으로." 그런데 0으로 접으면
**"Add description"이 화면에서 사라진다.** 그러면 설명을 처음 쓰는 길은 빈 자리를
누르는 것뿐인데, 그건 아무 데도 안 적혀 있는 길이다.

한 줄(플레이스홀더만, 상자 없음)이 그 글자와 이 값을 둘 다 지킨다 — **칸은 없고 줄만
있다.** [관찰] 넷째 장도 본문이 있는 작업이라 0인지 한 줄인지 구별해 주지 않는다.

0을 원하면 성립하기는 한다. 스페이서가 이미 클릭을 본문으로 보내므로
(`TASK_TAG_CHIPS_DESIGN.md` §3.2) 쓸 방법이 없지는 않다. 다만 **처음 쓰는 사람에게
보이는 안내가 하나도 없다.** 이건 취향이 아니라 발견 가능성의 문제라 §9 Q5로 남긴다.
