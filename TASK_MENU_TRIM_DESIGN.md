# ⋯ 메뉴에서 넉 줄을 뺀다 — 그리고 그것들이 남기고 가는 것

> 상태: **구현됨** · 2026-09-04 (D1 = A · **D2 = B**, 사용자 결정)
> (사용자가 행의 `⋯`를 눌러 나온 메뉴 한 장을 캡처하고, 위 넉 줄을 지목했다:
> `고정` · `복제` · `템플릿으로 저장` · `링크 복사`. "전역적으로".)
> 대상: `domain/tasks/actions.ts` · `hooks/useTaskCommands.ts` ·
> `components/tasks/TaskDrawer.tsx` · `App.tsx` · `i18n/{en,ko}.ts`

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 1장 — 행 메뉴 전체(`고정`·`복제`·`템플릿으로 저장`·`링크 복사` │ `집중 시작`·`작업 기록` │ 우선순위 넷 │ 날짜 셋 │ `완료`·`안 함으로 표시` │ …) | [관찰] |
| **실측** | `actions.ts:137-155`(정의) · `:178`(휴지통 목록) · `:207-210`(pin 가용성) · `useTaskCommands.ts:185-220` · `App.tsx:306`·`:1035`·`:1400` · `TaskDrawer.tsx:421` · `e2e/contextMenu.spec.ts:54` | [실측] |
| **결정** | 사용자가 고른 것 하나: 이 넉 줄을 어디서든 없앤다 | [결정] |
| **추론** | 넉 줄이 사라진 뒤 남는 기능들(고정 상태·템플릿·복제 명령)의 처리 | [추론] |

---

## 1. "전역적으로"가 실제로 뜻하는 것 [실측]

이 앱에는 **메뉴가 하나뿐이다.** 행의 `⋯`, 우클릭, 그리고 디테일의 `⋯`가 전부
`domain/tasks/actions.ts`의 한 배열에서 만들어진다(§15.63이 지키려던 것). 그러므로
"전역적으로 지운다"는 **정의 다섯 줄을 지우는 일**이다 — 표면마다 지우고 다니는 일이
아니다.

다섯인 이유: `고정`은 두 항목(`pin` / `unpin`)이고 상태에 따라 한쪽만 보인다(`:207`).

```ts
{ id: "pin",            group: "quick" }   // ← 삭제
{ id: "unpin",          group: "quick" }   // ← 삭제
{ id: "duplicate",      group: "quick" }   // ← 삭제
{ id: "saveAsTemplate", group: "quick" }   // ← 삭제
{ id: "copyLink",       group: "quick" }   // ← 삭제
```

**`quick` 그룹은 이 다섯이 전부다.** 그룹이 통째로 비므로 구분선도 저절로 사라진다 —
`taskActions()`가 빈 그룹을 떨어뜨린다(`:257`). `TaskActionGroupId`와 `GROUP_ORDER`에서
`"quick"`이라는 단어도 같이 지운다: 아무도 속하지 않는 그룹은 다음 사람이 읽을 때
"여기 뭔가 있었나" 하고 찾게 만드는 자리다.

### 1.1 메뉴는 이렇게 남는다

| 지금 | 뒤 |
|---|---|
| 고정 · 복제 · 템플릿으로 저장 · 링크 복사 | *(그룹 삭제)* |
| 집중 시작 · 작업 기록 | 그대로 |
| 높음 · 보통 · 낮음 · 우선순위 없음 | 그대로 (메뉴를 여는 표면이 얹는다) |
| 오늘로 · 내일로 · 날짜 지우기 | 그대로 (같음) |
| 완료 · 안 함으로 표시 | 그대로 |
| 휴지통으로 | 그대로 |

넉 줄이 아니라 **다섯 줄이 사라지고 구분선 하나가 준다.**

---

## 2. 넉 줄이 남기고 가는 것 — 결정 넷

지우는 것은 쉽다. 문제는 **그 항목이 유일한 입구였던 기능들**이다.

### D1 — 고정: 이미 고정된 작업은 어떻게 되는가 [결정 필요]

`pinnedAt`은 데이터에 남아 있고, `TaskDrawer.tsx:421`이 그것을 읽어 `고정됨` 배지를
그린다. 메뉴만 지우면 **이미 고정된 작업은 영원히 고정된 채로 남는다** — 배지는 보이고
푸는 방법은 없다.

| | **A. 배지도 같이 지운다** (권장) | B. 배지를 남긴다 | C. `unpin`만 남긴다 |
|---|---|---|---|
| 화면 | 고정이라는 개념이 사라진다 | 풀 수 없는 배지가 남는다 | 고정된 작업에만 `고정 해제`가 보인다 |
| 데이터 | `pinnedAt` 그대로(마이그레이션 없음) | 그대로 | 그대로 |
| 사용자의 문장과 | 맞다 | 어긋난다 | "지우자"의 절반만 |

**A를 권한다.** `pinnedAt`을 **읽는 곳은 이 배지와 메뉴 둘뿐**이므로(정렬은 아무도 읽지
않는다 — `mutations.ts:81`의 주석이 "순서는 뷰가 정한다"고 못 박아 두었다), 둘을 지우면
고정은 화면에서 완전히 사라진다. 필드와 `작업 기록`의 `고정했습니다` 항목은 **남긴다**:
기록은 그때 실제로 있었던 일이고, 지난 일을 지우는 것은 이 작업의 범위가 아니다.

### D2 — 템플릿: 만들 수 없는 기능을 어떻게 할 것인가 [결정 필요]

`saveAsTemplate`이 템플릿을 만드는 **유일한 경로**다. 쓰는 곳은 빠른 추가의
`템플릿에서 시작` 메뉴이고, 그 메뉴는 `templates.length > 0`일 때만 나타난다
(`TaskQuickAdd.tsx:334`).

| | **A. 픽커는 남긴다** (권장) | B. 템플릿 기능 전체 삭제 |
|---|---|---|
| 이미 템플릿을 가진 계정 | 계속 쓴다 | 조용히 잃는다 |
| 템플릿이 없는 계정 | 아무것도 안 보인다(지금과 같음) | 같음 |
| 코드 | `templates.ts`·동기화 테이블 그대로 | 도메인·동기화·정규화까지 연쇄 |

**권장은 A였다. 사용자가 B를 택했다** [결정 · 2026-09-04] — "없애고".

그래서 B를 하되, **경계를 데이터 앞에서 그었다.** 지운 것은 기능이고, 남긴 것은 기록이다:

| 지웠다 | 남겼다 |
|---|---|
| 빠른 추가의 `템플릿` 메뉴와 `TemplateChoices` | `AppData.taskTemplates` (계정에 있는 행) |
| `planner.saveTaskAsTemplate` · `deleteTaskTemplate` · `createTaskFromTemplate` | `normalize`의 `sanitizeTaskTemplate` — 저장된 행이 로드는 되어야 한다 |
| `templateFromTask` · `buildFromTemplate`과 그 테스트 | 동기화 표의 `task_templates` 매핑 |
| i18n 키 넷 | |

**B가 "사용자 데이터를 지운다"고 적었던 부분이 여기서 지켜졌다.** 메뉴 한 줄이
없어졌다고 남의 기록을 재작성하지 않는다 — 마이그레이션은 없고, 동기화 계약도 그대로다.
화면에서 템플릿은 완전히 사라졌고, 계정에 남은 행은 조용히 앉아 있다.

### D3 — 링크 복사: 휴지통의 특례 [실측]

`TRASHED_ACTIONS`(`actions.ts:178`)가 휴지통 작업에 남기는 넷 중 하나가 `copyLink`다.
주석이 그 이유를 적어 두었다 — *"복원하려는 것을 남에게 보여 주는 방법"*. 항목이
사라지므로 이 목록에서도 빠지고, 휴지통 메뉴는 **`작업 기록` · `복원` · `영구 삭제`
셋**이 된다.

`?task=` 주소 자체는 그대로다. 링크를 **복사하는 버튼**이 없어질 뿐, 주소창의 URL은
여전히 그 작업을 여는 링크다.

### D4 — 명령과 배선: 어디까지 걷어내는가 [추론]

| 대상 | 처리 |
|---|---|
| `useTaskCommands`의 `pin`/`unpin`/`duplicate`/`saveAsTemplate`/`copyLink` arm 5개 | 삭제 |
| 그 hook의 입력 `onDuplicate`·`onSaveAsTemplate`·`onDeleteTemplate`·`linkFor` | 삭제 (호출자 `App.tsx:306`·`TasksModule.tsx:608`에서도) |
| `copyTaskLink` 헬퍼 + `tasks.linkCopied`·`linkCopyFailed` 키 | 삭제 |
| `App.tsx:1035` `handleDuplicateTask` | **이미 죽어 있다** [실측] — 정의되고 아무도 부르지 않는다. 같이 삭제 |
| `planner.duplicateTask` · `domain/tasks/duplicate.ts` | **남긴다.** 되돌리기(`discardDuplicate`)를 포함한 규칙 뭉치이고, 반복 일정의 다음 회차 생성이 같은 자리를 다시 필요로 할 가능성이 실재한다. 참조가 없어지는 것과 지우는 것은 다른 결정이다 |
| `pinTask`/`unpinTask` 뮤테이션 | 위와 같은 이유로 남긴다 (테스트가 이미 있다) |
| i18n `tasks.menu.pin/unpin/duplicate/saveAsTemplate/copyLink` | 삭제. `tasks.pinned`·`tasks.templateSaved`·`undoDuplicated`는 D1·D2의 답에 따라 |

**D4의 원칙:** 지우는 것은 **메뉴에서 도메인 쪽으로 한 겹**까지다 — 항목, 명령, 그
명령만을 위해 존재하던 배선. 도메인의 규칙 함수는 남긴다. 그것이 이 변경을
되돌리기 쉬운 상태로 두는 값이고, 지금 지워도 다음 사람이 다시 쓰지 못한다.

---

## 3. 구현 단계

| 단계 | 무엇 | 파일 |
|---|---|---|
| **1** | 정의 다섯 줄 · `quick` 그룹 · `TRASHED_ACTIONS`의 `copyLink` | `domain/tasks/actions.ts` |
| **2** | 명령 arm 다섯 · 죽은 입력 · `copyTaskLink` | `hooks/useTaskCommands.ts` · `App.tsx` · `TasksModule.tsx` |
| **3** | `고정됨` 배지 (D1-A) | `components/tasks/TaskDrawer.tsx` |
| **4** | i18n 키 정리 | `i18n/{en,ko}.ts` |

1단계만으로 화면에서는 이미 사라진다 — 2~4는 그 뒤에 남는 죽은 코드의 처리다.

---

## 4. 테스트

| 파일 | 무엇 |
|---|---|
| `e2e/contextMenu.spec.ts:54` | 기대 목록에서 `Pin`·`Duplicate`·`Save as template`·`Copy link` 넉 줄 삭제. **이 단언이 이 변경의 회귀 테스트다** — 목록을 통째로 적어 두었기 때문에 하나만 되살아나도 깨진다 |
| `domain/tasks/actions.test.ts:57` | `Pin`↔`Unpin` 교체 테스트 삭제, 대신 "다섯 중 어느 것도 어떤 상태에서도 나오지 않는다"로 |
| `taskActionsMenu.test.tsx` | 넉 줄을 누르는 케이스가 있으면 삭제 |
| `taskDetailPane.test.tsx` | 디테일 `⋯`의 목록 단언 갱신 |
| 신규 | 휴지통 작업의 메뉴가 `작업 기록`·`복원`·`영구 삭제` 셋이다 (D3) |

---

## 4.1 구현하며 정해진 것 [실측 · 2026-09-04]

1. **`quick` 그룹은 예상대로 통째로 비었다** — `taskActions()`가 빈 그룹을 떨어뜨리므로
   구분선까지 저절로 사라졌다. `TaskActionGroupId`와 `GROUP_ORDER`에서도 이름을 지웠다.
2. **`useTaskCommands`의 입력 넷이 같이 죽었다** — `onDuplicate` · `onSaveAsTemplate` ·
   `onDeleteTemplate` · `linkFor`. 넷 다 이 다섯 arm 말고는 부르는 곳이 없었다.
3. **`App.tsx`의 `handleDuplicateTask`는 정말로 죽어 있었다** [실측] — 정의만 있고 호출이
   없다. 같이 지웠다.
4. **`domain/tasks/duplicate.ts`와 `pinTask`/`unpinTask` 뮤테이션은 남겼다** (§D4의 원칙).
   참조는 없지만 규칙과 테스트가 붙어 있는 코드다.
5. **테스트 여섯 개가 사라지고 둘이 생겼다** — 없어진 다섯 동작의 테스트를 지우고,
   "다섯 중 어느 것도 어떤 상태에서도 나오지 않는다" 하나와 "`pinnedAt`이 있는 기록도
   같은 메뉴를 받는다" 하나를 넣었다. 후자가 D1의 회귀 테스트다.

---

## 5. 바뀌지 않는 것

- **우선순위 넷 · 날짜 셋** — 메뉴를 여는 표면(`TasksModule.taskMenuAt`)이 얹는 것이고
  레지스트리 밖이다.
- **`집중 시작` · `작업 기록` · `완료` · `안 함으로 표시` · `휴지통으로` · `복원` ·
  `영구 삭제`** — 그대로.
- **디테일이 여는 넷**(`하위 할일`·`태그`·`차단`·`메모`) — `add` 그룹, 그대로.
- **`?task=` 주소와 그 복사 가능성** — 버튼만 없어진다(D3).
- **작업 기록의 `고정했습니다` 항목** — 지난 일 (D1).
