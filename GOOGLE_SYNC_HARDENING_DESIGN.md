# 구글 동기화 보강 — 중복 · 좀비 · 회차 취소 · 경쟁 · 404 · 이벤트 종류

> 상태: **구현 완료** (H1~H5) · 2026-09-08 · P0 셋 + P1 셋
> 요청: 이벤트 생성 중복 방지 · 410 후 full sync 정리 · 반복 일정의 cancelled 분리 ·
> 외부 이벤트 PATCH 순서 보장 · 404 의미 분리 · 편집 가능한 구글 이벤트 제한
> 대상: `domain/calendar/googleSync/*` · `lib/googleCalendar*` · `hooks/useGoogle*Sync.ts` ·
> `lib/externalCalendars.ts` · `types.ts`
> 선행 문서: `GOOGLE_CALENDAR_SYNC_DESIGN.md`(§4~§9) · `GOOGLE_SYNC_LOGIC_REFERENCE.md`(지금 코드가 하는 일)

---

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 코드에서 잰 것 — `runOutbound`의 create가 id 없이 POST한다 · `applyInboundPlan`이 부재를 절대 지우지 않는다 · `plan.upsert`에 에코가 빠져 있다 · `sanitizeEvent`가 화이트리스트다 · `updateExternalEvent`에 직렬화가 없다 | [실측] |
| **결정** | 요청자 — 여섯 항목과 그 우선순위 | [결정] |
| **규격** | Google Calendar API — 클라이언트 지정 `id`(base32hex, 5~1024) · 409 duplicate · `nextSyncToken`은 마지막 페이지에만 · `recurringEventId` + `originalStartTime` · `eventType` | [규격] |
| **추론** | 각 실패가 실제로 얼마나 자주 나는가 · 회차 취소가 마스터보다 먼저 오는 빈도 | [추론] |

---

## 1. 지금 무엇이 깨지는가 [실측]

설계 없이 "고친다"로 들어가면 여섯 개가 서로 다른 크기의 문제처럼 보인다. 실제로는
**세 개가 데이터를 잃거나 만들어내고**, 세 개는 잘못된 결론을 내린다.

| # | 지금 | 실제로 벌어지는 일 |
|---|---|---|
| P0-1 | create가 id 없이 POST한다 (`googleCalendarOutbound.ts:129`) | 구글이 이벤트를 만들었는데 응답이 유실되면(모바일 전환 · 타임아웃) 태스크에 id가 안 적힌다 → 다음 패스가 **같은 일정을 하나 더 만든다.** 사용자가 지워도 태스크는 여전히 자격이 있어 또 만든다 |
| P0-2 | 410 후 전체 재나열이 **아무것도 지우지 않는다** (`applyInboundPlan`) | 커서가 만료된 동안 구글에서 지운 일정이 앱에 **영원히 남는다.** 되살릴 방법이 캘린더 체크 해제밖에 없다 |
| P0-3 | `status: cancelled`를 전부 "그 id를 지워라"로 읽는다 (`planInbound:78`) | 반복 일정의 **한 회차를 취소하면 그 취소가 반영되지 않는다.** 취소 행의 id는 `<masterId>_20260910T...`이고 우리가 가진 레코드는 마스터라, 지울 대상이 없어 조용히 no-op이 된다 |
| P1-1 | `updateExternalEvent`가 직렬화 없이 매 제스처마다 나간다 | 드래그 직후 리사이즈처럼 연속 편집하면 두 번째가 **낡은 etag로 412**를 맞는다. 그 다음 판정이 구글 시각과 로컬 시각의 비교라, 결과가 시계 편차에 달린다 — 지면 두 번째 편집이 사라지고, 이기면 재시도가 첫 편집을 덮을 수도 있다 |
| P1-2 | 404/410을 언제나 "그 이벤트가 없다"로 읽는다 | **캘린더가 통째로 사라지면**(공유 해제 · 사용자가 삭제) 모든 이벤트가 하나씩 404를 맞는다. 경로 A에서는 전용 캘린더가 지워졌을 때 **모든 태스크의 매핑이 풀리고**, 이어지는 create도 전부 실패해 매 패스가 같은 일을 영원히 반복한다 |
| P1-3 | `accessRole`만 보고 편집을 연다 (`googleCalendarSources.ts:70`) | 생일 · Gmail에서 만들어진 일정 · focusTime은 소유 캘린더에 있어도 일반 PATCH를 거부한다. 격자는 끌게 해주고 구글이 거절하며, 사용자에게는 "됐다가 되돌아갔다"로 보인다 |

---

## 2. 결정 요약 [결정]

| # | 결정 | 절 |
|---|---|---|
| **D1** | 이벤트 id를 **보내기 전에 예약**하고 로컬에 적는다. 재시도는 같은 id, 409는 "이미 우리 것"으로 읽는다 | §3 |
| **D2** | 부재를 삭제로 읽는 것은 **완결이 증명된 전체 나열에서만**. 그 판정은 이름이 다른 별도 함수가 하고, 지우는 것은 **로컬 미러뿐** | §4 |
| **D3** | `cancelled`를 두 갈래로 — `recurringEventId`가 있으면 **마스터의 exdate**, 없으면 삭제 | §5 |
| **D4** | 쓰기는 **eventId별로 직렬화**한다. 큐에 들어간 편집은 실행 시점에 최신 레코드를 다시 읽는다 | §6 |
| **D5** | 404를 만나면 **캘린더의 생사를 한 번 묻는다.** 캘린더가 죽었으면 이벤트에 손대지 않는다 | §7 |
| **D6** | 편집 가능 = `accessRole ∈ {owner, writer}` **그리고** `eventType`이 `default`(부재 포함) | §8 |

---

## 3. P0-1 · 이벤트 생성 중복 방지

### 3.1 규격이 허락하는 것 [규격]

`events.insert`는 클라이언트가 정한 `id`를 받는다. 문자셋은 base32hex(`0-9`, `a-v`),
길이 5~1024, **캘린더 안에서 유일**해야 한다. 같은 id로 두 번 만들면 두 번째는
**409 Conflict**다. 이 두 가지가 idempotent create의 전부다.

### 3.2 필드 하나를 더한다

```
googleReservedEventId?: string   // 보내기로 한 id. 성공 여부와 무관하다
```

§4.2가 세운 불변("`googleEventId`의 유무가 곧 생성 성공 여부다")을 **깨지 않기 위해**
필드를 나눈다. 예약 id를 `googleEventId`에 미리 적으면 그 문장이 거짓이 되고,
`outboundAction`이 만들어지지도 않은 이벤트를 update로 분류한다.

`normalizeTask`가 미지 필드를 먼저 spread하므로 마이그레이션은 없다 — 기존 세 필드와 같다.

### 3.3 id를 어떻게 만드는가

`crypto.randomUUID()`의 하이픈을 지우고 `ff`를 앞에 붙인다 → `ff` + 32 hex = 34자.
hex는 base32hex의 부분집합이라 그대로 합법이다.

**태스크 id에서 결정적으로 파생하지 않는다** [결정]. 구글은 삭제된 이벤트의 id를 한동안
붙들고 있어서, 휴지통에 갔다가 복구된 태스크가 같은 id로 다시 만들어지려 하면 **영구적인
409**에 걸린다. 우리에게 필요한 성질은 "태스크에서 계산 가능"이 아니라 "재시도가 같은
id를 쓴다"이고, 그건 저장으로 충분하다.

### 3.4 순서 — 예약이 요청보다 먼저 디스크에 닿아야 한다

```
1. planOutbound  →  create 더미
2. 예약 없는 것에 id를 만들어 붙이고, 그것을 먼저 앱 상태에 적는다
3. 그 id를 실어 events.insert
4. 응답에 따라 googleEventId 확정 / 409 흡수 / 아무것도 안 함
```

2와 3의 순서가 이 항목의 전부다. 뒤집으면 지금과 같은 창이 그대로 남는다.
`applyGoogleSync`에 `reserved` 배열을 더해 2를 처리하고, 그 값을 상태에서 다시 읽지 않고
**계획에 실어 그대로** 3으로 넘긴다 — React 상태 왕복을 한 번 기다리는 설계는 패스를
비동기 두 단계로 쪼개고, 그 사이에 또 하나의 창을 만든다.

예약을 적는 것은 `updatedAt`을 건드리지 않는다. 건드리면 `needsUpdate`가 참이 되어
방금 만든 이벤트를 다음 패스가 곧바로 다시 쓴다.

### 3.5 응답 표

| 응답 | 결론 |
|---|---|
| 200 / 201 | `googleEventId` = 예약 id, etag · `googleSyncedAt` 기록, **예약 필드 해제** |
| **409** | 우리 예약 id가 이미 그 캘린더에 있다 = **지난번 insert가 사실은 성공했다.** GET으로 읽어 매핑을 흡수한다. 그 이벤트가 `cancelled` 상태면(복구된 태스크) `status: "confirmed"` + 본문으로 PATCH해 되살린다 |
| 401 | 패스 중단. 예약은 남는다 |
| 그 외 · 네트워크 | 아무것도 적지 않는다. **예약은 남아** 다음 패스가 같은 id로 재시도 |

409를 "실패"가 아니라 "이미 우리 것"으로 읽는 것이 중복 방지의 나머지 절반이다.

### 3.6 남는 창 [추론]

예약을 상태에 적은 직후, 디스크에 내려가기 전에 프로세스가 죽으면 예약이 사라진다.
그때의 동작은 **지금과 같다**(중복 가능). 창은 밀리초 단위이고, 없애려면 패스가 저장
완료를 기다려야 하는데 그건 §3.4가 기각한 구조다. 이 문서는 이 창을 남긴다.

---

## 4. P0-2 · 410 후 전체 나열의 정리

### 4.1 §7.1과 정면으로 부딪히는 항목이다

§7.1은 "부재를 삭제로 읽지 말라"이고, 이 항목은 "전체 나열에서는 부재가 삭제다"이다.
둘 다 옳다. 갈라지는 지점은 **완결이 증명됐는가** 하나뿐이므로, 설계는 그 증명을 코드
구조로 만든다.

### 4.2 완결의 정의 — 넷을 전부 만족할 때만 [규격]

| 조건 | 왜 |
|---|---|
| `syncToken` 없이 시작한 나열일 것 | 증분 응답은 "바뀐 것"만 담는다. 그 부재는 "안 바뀜"이다 |
| 모든 페이지를 받았을 것 (`MAX_PAGES`에 걸리지 않음) | 잘린 목록의 부재는 잘린 것이다 |
| 마지막 페이지에서 **`nextSyncToken`을 받았을 것** | 구글이 "여기까지가 전부"라고 말한 유일한 증거다 |
| 모든 페이지가 200일 것 | 하나라도 실패면 그 패스는 아무 결론도 내지 않는다 |

넷이 모이면 `complete: true`가 되고, 아니면 **오늘과 정확히 같이 동작한다**(아무것도 안 지움).

### 4.3 "본 것"에는 에코가 포함돼야 한다 [실측]

`plan.upsert`를 그대로 쓰면 안 된다. `planInbound`는 etag가 같은 항목을 **에코로
건너뛰면서 upsert에 넣지 않는다.** 응답에 있었는데 upsert에 없는 것이다. 그것을
"부재"로 읽으면 **우리가 방금 쓴 이벤트가 지워진다** — 가장 흔한 이벤트부터.

그래서 `planInbound`가 `seen: string[]`을 따로 낸다: cancelled · echo · upsert를 통틀어
**응답에서 id를 본 모든 항목**. 정리는 오직 이 집합으로만 한다.

### 4.4 이름이 곧 사전 조건이다

```ts
// runInbound가 complete를 증명했을 때만 호출된다.
export function pruneAfterFullListing(
  current: readonly ExternalCalendarEvent[],
  calendarId: string,
  seen: ReadonlySet<string>,
): ExternalCalendarEvent[]
```

`planInbound`는 지금처럼 **로컬 집합을 받지 않는다.** 부재를 물을 수 있는 함수는 이
하나뿐이고, 이름이 그 조건을 말한다. §7.1 가드의 모양(시그니처로 막는다)을 그대로 잇는다.

### 4.5 무엇을 지우는가

**로컬 미러의 그 캘린더 이벤트만.** 태스크는 이 경로에서 영원히 건드리지 않는다.
잘못 지워도 다음 폴링이 다시 가져오므로 최악의 결과가 "잠깐 사라졌다가 돌아온다"이다 —
태스크를 휴지통에 넣는 것과는 위험의 종류가 다르다.

### 4.6 기각 [결정]

- **커서 만료 시 그 캘린더의 로컬 이벤트를 다 지우고 다시 채우기** — 부재=삭제의 최악판이고,
  화면이 매번 깜빡인다.
- **매 폴링마다 정리하기** — 증분 응답에 그 판정을 걸 근거가 없다(§4.2 첫 줄).

---

## 5. P0-3 · 반복 일정의 cancelled 분리

### 5.1 구글이 보내는 두 종류의 cancelled [규격]

| 모양 | 뜻 | 지금 | 되어야 할 것 |
|---|---|---|---|
| `status: cancelled`, `recurringEventId` 없음 | 그 이벤트(또는 시리즈 전체)가 삭제됐다 | 삭제 ✓ | 그대로 |
| `status: cancelled`, `recurringEventId` + `originalStartTime` 있음 | **그 회차만 취소됐다** | id가 안 맞아 no-op | 마스터의 **exdate**에 추가 |

### 5.2 exdate는 이미 우리 모델에 있다 [실측]

`ExternalCalendarEvent.exdates`가 있고, `expandIcsOccurrences`가 그것을 읽어
`localDateTimeParts(...).date`로 환산해 그 날의 회차를 건너뛴다. 새 개념을 들일 필요가 없다 —
**취소 행의 `originalStartTime`을 마스터의 `exdates`에 넣으면 끝난다.**

### 5.3 적용 순서 [실측]

`applyInboundPlan`이 **upsert를 먼저, 취소를 나중에** 적용해야 한다. 같은 응답에 마스터와
회차 취소가 함께 오는 경우(전체 나열이 늘 그렇다) 순서가 뒤집히면 아직 없는 마스터에
exdate를 붙이려다 놓친다.

그래도 마스터가 없는 경우는 남는다(증분 응답이 취소 행만 보낼 때). v1은 **그때 아무것도
하지 않는다** — 다음 전체 나열이 마스터와 함께 다시 가져온다. 큐를 만들지 않는 이유는
그 큐가 또 하나의 "적용되지 않은 상태"가 되기 때문이다 [결정].

### 5.4 회차를 대체하던 레코드도 함께

그 회차에 override 레코드(`recurrenceId`를 가진 것)를 들고 있었다면 그것도 사라져야 한다.
override의 `externalUid`는 취소 행의 id와 같으므로 **지금의 id 기반 삭제가 이미 그 절반을
한다.** 바뀌는 것은 "그 위에 exdate도 붙인다"는 것뿐이다.

---

## 6. P1-1 · 외부 이벤트 PATCH 순서 보장

### 6.1 무엇이 깨지는가 [실측]

`updateExternalEvent`는 낙관적으로 그린 뒤 `await`한다. 두 번째 제스처가 그 사이에
들어오면 자기도 낙관적으로 그리고, **첫 쓰기가 아직 새 etag를 가져오지 못한** 레코드를
읽어 같은 낡은 etag로 PATCH한다. 결과는 412이고, 그 다음은 구글의 `updated`(첫 쓰기가
만든 시각)와 우리 `updatedAt`(낙관적 갱신이 넣은 **로컬 시계**)의 비교다. 두 시계의
편차가 결과를 정한다 — 설계가 의도한 판정이 아니다.

### 6.2 eventId별 큐

전역 뮤텍스가 아니라 **이벤트 하나당 사슬 하나**다. 느린 쓰기 하나가 다른 일정의 편집을
막을 이유가 없다.

```
enqueue(eventId, edit)
  └ 앞의 쓰기가 끝난 뒤에 실행된다
      └ 실행 시점에 최신 레코드를 다시 읽는다  ← 핵심
          └ 그 레코드의 etag로 PATCH
```

핵심은 큐가 **편집(제스처가 말한 것)을 들고 있고, 레코드는 실행 순간에 읽는다**는 것이다.
레코드를 들고 있으면 큐가 낡은 etag를 그대로 보존하게 되어 아무것도 나아지지 않는다.

### 6.3 롤백 대상도 바뀐다

지금은 "호출 시점의 레코드"로 되돌린다. 큐에서는 그것이 이미 낙관적으로 그려진 중간
상태일 수 있으므로, 큐 항목이 **마지막으로 서버가 확인해준 레코드**를 들고 그것으로
되돌린다.

### 6.4 병합은 하지 않는다 [결정]

세 개가 밀려 있으면 마지막 위치만 보내는 것이 요청 수에는 낫지만, 시간 이동 · 이름 변경 ·
메모 수정은 서로 다른 필드라 "마지막이 이긴다"로 합칠 수 없다. v1은 **직렬화만** 한다.

---

## 7. P1-2 · 404 의미 분리

### 7.1 두 가지 404

| 무엇 | 지금의 결론 | 옳은 결론 |
|---|---|---|
| 이벤트가 그 캘린더에 없다 | 매핑 해제 / 격자에서 제거 | 같다 |
| **캘린더 자체가 없거나 접근 불가** | 위와 똑같이 처리 → 이벤트를 하나씩 잃는다 | 캘린더를 실패로 표시하고 **이벤트에 손대지 않는다** |

### 7.2 판정 — 한 번만 묻고 패스 안에서 기억한다

404를 만나면 `GET /calendars/{calendarId}`를 **패스당 한 번** 부른다.

- 200 → 캘린더는 살아 있다 → 지금의 결론(이벤트가 없다) 그대로.
- 404 / 403 → 캘린더가 죽었다 → **그 캘린더에 대한 이 패스의 모든 판정을 중단**하고,
  `syncStatus: "failed"` + `lastError`를 적는다. 매핑도 이벤트도 건드리지 않는다.

`syncStatus`와 `lastError`는 `ExternalCalendar`에 이미 있다 [실측].

### 7.3 전용 캘린더가 사라졌을 때 (경로 A)

지금은 모든 태스크의 매핑이 풀리고, 이어지는 create가 전부 실패하며, 다음 패스가 같은
일을 반복한다. §7.2를 적용하면 첫 404에서 "전용 캘린더 없음"으로 결론이 나고 패스가
멈춘다. 복구는 이미 있다 — `ensureDedicatedCalendar`가 404를 보면 새로 만든다. 그것을
**연결 시점뿐 아니라 이 실패에서도** 부르면 스스로 낫는다.

### 7.4 403은 404와 다르다 [규격]

403은 "있지만 권한이 없다"이다. 결론은 같다(이벤트에 손대지 않는다). 다만 사용자에게 할
말이 다르므로 `lastError`에 그대로 남긴다 — 공유가 해제된 캘린더와 삭제된 캘린더는
사용자가 할 수 있는 일이 다르다.

---

## 8. P1-3 · 편집 가능한 구글 이벤트 제한

### 8.1 `accessRole`은 캘린더의 성질, `eventType`은 이벤트의 성질 [규격]

`eventType`은 `default` · `birthday` · `fromGmail` · `focusTime` · `outOfOffice` ·
`workingLocation`. 뒤의 다섯은 소유한 캘린더에 있어도 일반 PATCH를 받지 않거나 제한된
필드만 받는다.

**v1: `default`만 편집 가능.** 나머지는 읽기 전용으로 그린다.

### 8.2 부재는 `default`로 읽는다 [결정]

구글이 `eventType`을 안 보낸 경우 보수적으로 막으면, 필드가 빠지는 순간 **모든 일정이
편집 불가**가 된다. 반대로 열어두면 실패는 구글의 400 하나이고 그건 `failed` → 롤백으로
화면에 보인다. 조용한 전면 잠금보다 시끄러운 개별 실패가 낫다.

### 8.3 어디에 저장하고 어디서 판정하는가

- `ExternalCalendarEvent`에 `eventType?: string`을 더한다.
- `toExternalEvent`가 읽고, `readOnly = !writable || eventType !== "default"`로 합친다.
- **`sanitizeEvent`의 화이트리스트에 반드시 더한다** [실측]. 이 함수는 알던 필드만 복사하므로,
  빠뜨리면 새 필드가 **리로드마다 조용히 사라진다.** 이 문서에서 가장 놓치기 쉬운 한 줄이다.

### 8.4 사용자에게 하는 말

지금 팝오버는 "읽기 전용"과 "고치면 구글에 반영됩니다" 둘뿐이다. 이유가 셋이 되었으므로
문구도 갈린다 — 캘린더가 읽기 전용인 것, **이 종류의 일정이 편집 불가인 것**(생일 · Gmail 일정),
그리고 반복이라 격자가 답할 수 없는 것(이미 있음). i18n 키 하나를 더한다.

---

## 9. 단계

| 단계 | 무엇 | 왜 이 순서 | 상태 |
|---|---|---|---|
| **H1** | P0-3 회차 취소 · P1-3 eventType | 순수 계층 + 인바운드 매핑만 건드린다. 픽스처로 증명되고, 되돌리기 쉽다 | **완료** — `inboundPlan.ts`(`cancelledOccurrences`) · `inboundShape.ts`(`googleEventType`) · `externalCalendars.ts` |
| **H2** | P0-2 전체 나열 정리 | `planInbound`가 `seen`을 내야 하므로 H1의 인바운드 변경 위에 얹는다 | **완료** — `pruneAfterFullListing` · `InboundOutcome.complete` |
| **H3** | P1-2 404 분리 | 인바운드·아웃바운드 양쪽의 실행기를 건드린다. H2가 끝난 뒤라야 회귀를 구분할 수 있다 | **완료** — `lib/googleCalendarAccess.ts` · `calendarMissing` · `calendarGone` |
| **H4** | P0-1 예약 id | 태스크 모델 · 패스 순서 · `applyGoogleSync`가 함께 움직인다. 가장 넓다 | **완료** — `googleReservedEventId` · `reserveEventIds` · 409 흡수 |
| **H5** | P1-1 쓰기 큐 | 앱 계층 하나. 다른 넷과 겹치지 않으므로 언제든 끼울 수 있다 | **완료** — `lib/googleEventWriteQueue.ts` |

### 9.1 구현하면서 설계에서 달라진 것

- **H3** — 인바운드에는 프로브를 넣지 않았다. `events.list`가 404/403을 받는 것 자체가 그
  캘린더에 대한 답이고, 그때 인바운드는 이미 아무 결론도 내지 않는다(`failed`). 프로브가
  필요한 곳은 이벤트 하나를 지목한 요청뿐이다.
- **H4** — 409를 GET으로 흡수하려던 초안을 PATCH로 바꿨다. 흡수 뒤에 참이어야 하는 것이
  둘(etag를 든다 · 태스크가 지금 말하는 것을 담는다)이고 요청 하나로 둘 다 된다. GET이면
  `googleSyncedAt`이 한 버전 낡은 본문과의 합의를 주장한다.
- **H5** — 큐가 `base`와 `drawn` 두 버전을 든다. 초안은 "실행 시점에 최신 레코드를 읽는다"만
  적었는데, 그것만으로는 성공한 쓰기가 자기 스냅샷으로 화면을 되돌려 뒤에 줄 선 편집을
  지운다. 성공은 내용이 아니라 버전 표시만 옮겨야 한다.

P0가 셋인데 H4가 마지막인 것은 **위험의 크기가 아니라 결합의 크기**를 따른 것이다.
H1~H3은 각각 파일 두세 개이고, H4는 태스크 레코드의 수명주기를 건드린다.

## 10. 무엇으로 증명하는가

순수 계층에 넣을 수 있는 것은 전부 순수 계층에서 픽스처로 증명한다 — 이 기능의 나머지가
이미 그렇게 되어 있다.

| 항목 | 반드시 있어야 할 테스트 |
|---|---|
| P0-1 | 같은 예약 id로 두 번 계획해도 계획이 같다 · 409가 매핑 흡수로 이어진다 · 실패는 예약을 남긴다 |
| P0-2 | **에코가 `seen`에 들어간다**(빠지면 우리 자신의 쓰기가 지워진다) · `complete: false`면 아무것도 안 지운다 · 페이지 상한에 걸린 나열은 정리하지 않는다 |
| P0-3 | 회차 취소가 마스터의 exdate가 된다 · 마스터 없는 취소는 no-op이다 · 시리즈 전체 취소는 삭제다 · upsert가 취소보다 먼저 적용된다 |
| P1-1 | 연속 두 편집이 순서대로 나가고, 두 번째가 첫 번째의 etag를 쓴다 |
| P1-2 | 캘린더가 404면 이벤트가 하나도 사라지지 않는다 |
| P1-3 | `birthday`는 `readOnly`다 · `eventType` 부재는 편집 가능이다 · **저장 후 리로드해도 값이 남는다** |

## 11. 열린 질문

| # | 질문 | 기본값 제안 |
|---|---|---|
| Q1 | 409에서 되살린 이벤트(취소 상태였던 것)의 `created`를 우리 것으로 볼 것인가 | 구글 것을 그대로 쓴다 |
| Q2 | 전체 나열 정리가 한 번에 지우는 개수에 상한을 둘 것인가 | v1은 두지 않는다. 로컬 미러뿐이고 다음 폴링이 되돌린다 |
| Q3 | 캘린더 실패를 사용자에게 어디서 보여줄 것인가 | 설정의 소스 목록 행 + 레일 동기화 상태 |
| Q4 | `focusTime` · `outOfOffice`를 나중에 편집 가능하게 열 것인가 | 필요해지면. 각자 전용 필드가 있어 별도 매핑이 필요하다 |
