# 태그는 색이 있는 칩이고, 자리는 표면이 정한다

> 상태: **설계 · 3차** · 2026-09-08 (T1~T9 — 사용자 결정 셋: 색은 **편집 대화상자**에서 ·
> 칩은 **행에서는 제목 아래, 매트릭스에서는 오른쪽**)
> 요청: "태그가 내용 바로 아래에 붙어 있는 식으로. 태그의 색도. Edit을 누르면 색을
> 추가할 수 있게, **Parent Tag는 없이**. 칩은 TickTick처럼 제목 아래 왼쪽."
> 대상: `components/tasks/TagChip.tsx`(신규) · `components/tasks/TagEditModal.tsx`(신규) ·
> `components/tasks/TasksSidebar.tsx` · `components/tasks/TagPicker.tsx` ·
> `components/tasks/TaskRowContent.tsx` · `components/tasks/TaskDrawer.tsx` ·
> `domain/tags/tags.ts` · `hooks/usePlannerData.ts` · `styles/17-tasks-module.css`
> 선행 문서: `CALENDAR_FILL_READABILITY_DESIGN.md`(대비 — §6이 그 함수를 부른다) ·
> `TASK_ROW_TWO_LINES_DESIGN.md`(행의 둘째 줄 — §7이 그 줄에 얹는다) ·
> `TICKTICK_COMPONENT_06_TASK_LIST_ROW.md` §7 · `TICKTICK_COMPONENT_07_TASK_DETAIL_PANEL.md` §3

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 6장 — 우리 상세(태그가 바닥) · TickTick 상세(내용 바로 아래) · TickTick 행+상세(채운 알약) · TickTick 사이드바(행마다 ⋯ 와 색 점) · Edit Tag 대화상자(이름 · 색 9칸 · Parent Tag) · TickTick 한 줄 행(본문 없음 — 칩이 오른쪽) | [관찰] |
| **실측** | `toggleTaskTag`가 색 없이 Tag를 만든다 · `Tag.color`를 **쓰는 UI가 없다** · 칩이 두 벌이다 · `.tm-drawer-content`가 `flex: 1 0 auto`다 · `readableInk.ts`에 대비 함수 둘이 있다 · `CreateListModal`이 **없음+프리셋 8+커스텀** 색 줄을 이미 그린다 · 사이드바 행은 메뉴 없는 버튼 하나다 · `usePlannerData`에 `updateTag`가 없다 | [실측] |
| **추론** | 이름 변경이 `tagIdFor`의 id 충돌을 만든다 (§5.5) | [추론] |
| **결정** | 사용자 — 색은 편집 대화상자에서 · Parent Tag 없음 · 칩은 제목 아래 왼쪽 · 매트릭스 카드에도 | [결정] |
| **미확인** | 다크 모드에서 채움인지 틴트인지 (§6.3) | [미확인] |

---

## 1. 진단 — 셋 다 원인이 다르다 [실측]

### 1.1 태그가 바닥에 붙은 것은 순서 문제가 아니다

DOM 순서는 **이미 맞다.** 스크롤 영역은 내용 → 하위작업 → **태그** → 의존성 → 노트
순이다(`TaskDrawer.tsx:622`, `:732`). 바닥으로 민 것은 오늘 들어간 한 줄이다:

```css
.tm-drawer-content { flex: 1 0 auto; }   /* 본문이 남은 높이를 전부 먹는다 */
```

그러므로 §3이 정할 것은 태그의 자리가 아니라 **남은 높이를 누가 먹는가**다.

### 1.2 칩이 두 벌이다

| 어디 | 클래스 | 모양 |
|---|---|---|
| 상세 | `.tm-tag-chip` | 회색 알약 + × |
| 행/카드 | `.tm-task-tag` | 색 **글씨**, `#이름`, 오른쪽 무리 |

같은 것의 두 그림이고, 둘 다 [관찰]의 알약이 아니다.

### 1.3 색이 어디서도 오지 않는다

```ts
// domain/tags/tagPicker.ts:185 — 태그가 만들어지는 유일한 경로
tags: [...tags, { id: tagId, name: displayName, createdAt: now, updatedAt: now }]
```

`color`가 없고, 그것을 **정하는 화면도 없다.** 사이드바는 `tag.color || var(--tm-tag-dot)`로
읽기만 한다(`TasksSidebar.tsx:317`). 그래서 칩을 색칠하는 코드만 쓰면 전부 회색이 나온다.

1차 설계는 이 구멍을 이름 해시 자동색으로 메우려 했다. 사용자가 **편집 대화상자**로
답했으므로 자동색은 폐기한다 — 색은 고른 사람의 것이고, 고르지 않은 태그는 중립이다.
[관찰] 넷째 장의 사이드바도 그렇게 생겼다: 색 점은 색을 준 태그에만 있다.

---

## 2. 결정 요약

| # | 결정 | 근거 절 |
|---|---|---|
| **T1** | 남은 높이는 본문이 먹지 않는다. 위에서부터 쌓이고 공백은 마지막 아래 | §3.1 |
| **T2** | 그 공백을 눌러도 본문에 캐럿이 간다 | §3.2 |
| **T3** | 칩은 채운 알약, 이름만. `#`을 뗀다 | §4.2 |
| **T4** | 행과 상세는 같은 칩. 차이는 ×(삭제) 하나 | §4.1 |
| **T5** | 색은 **태그 편집 대화상자**에서 고른다. 자동색 없음, 고르기 전엔 중립 회색 | §5 |
| **T6** | 대비는 `readableInk.ts`를 그대로 부른다 | §6 |
| **T7** | 대화상자는 **이름 + 색 + 저장/취소**. Parent Tag 없음 | §5.2 |
| **T8** | 리스트 행에서 칩은 **자기 줄**을 갖는다. 제목 아래 왼쪽. 태그가 있으면 행이 두 줄이 된다 | §7.2 |
| **T9** | **매트릭스 카드는 오른쪽** — 자리는 그대로, 데이터만 넘긴다 | §7.5 |

---

## 3. 상세의 배치

### 3.1 남은 높이는 마지막 아래로 [T1]

`.tm-drawer-content`의 `flex`를 뗀다. 스크롤 영역 **마지막에 스페이서**를 두고 그것이
남은 높이를 먹는다.

```
tm-drawer-scroll (flex column)
├─ 제목
├─ 내용            ← 자기 글만큼
├─ 하위작업 / 태그 / 의존성 / 노트
└─ tm-drawer-filler   flex: 1
```

섹션 순서는 한 줄도 건드리지 않는다. 순서는 이미 [관찰] 둘째 장의 순서였다.

기각한 대안 둘: **태그를 본문 위로**(순서가 뒤집힌다) · **본문에 큰 min-height**(공백의
자리만 바뀌고 태그는 여전히 그 아래로 밀린다).

### 3.2 빈 자리를 눌러도 본문이다 [T2]

본문이 패널을 채우던 동안 아래쪽 빈 곳을 누르면 캐럿이 갔다. T1이 그 값을 없애므로
스페이서가 클릭을 받아 본문에 포커스를 준다. 체크리스트 모드에서는 아무것도 하지
않는다 — 빈 곳을 눌렀는데 마지막 항목에 캐럿이 튀는 것은 요청한 적 없는 편집이다.

---

## 4. 칩

### 4.1 한 벌로 [T4]

`TagChip` 하나. `onRemove`가 있으면 ×를 그리고, 없으면 안 그린다. 행에서 태그를 떼는
기능은 만들지 않는다 — 지울 수 있게 하는 순간 행이 편집 표면이 된다.

### 4.2 모양 [T3]

```
[ 학교 ]        ← 채운 알약, 이름만, 12px
[ 학교 × ]      ← 상세에서만
```

`#`을 뗀다. 그 기호는 **칩이 색 없는 회색 글씨였기 때문에** 붙였다 — 리스트 이름과 같은
회색으로 나란히 서면 구별이 안 됐다(커밋 `98b461b`). 알약이 그 일을 대신하면 기호는
순수 손해다: 두 글자 이름 앞에서 폭의 3분의 1을 쓴다.

퀵애드 입력의 `#`은 남는다. 그것은 타이핑 문법이지 표시가 아니다(`splitInlineTags`).

### 4.3 토큰

칩은 두 값만 받는다. 계산은 바깥에서 한 번 한다.

```css
.tm-tag-chip { background: var(--tag-fill); color: var(--tag-ink); }
```

---

## 5. 색 — 태그 편집 대화상자

### 5.1 저장 형식은 리스트와 같다

`Tag.color`에 **프리셋 키 또는 `#RRGGBB`**를 넣는다. `List.color`가 쓰는 규칙 그대로이고,
읽기도 `parseListColor` / `listColorHex`를 그대로 쓴다(`domain/tasks/listColor.ts`).

한 화면에서 리스트는 8색, 태그는 다른 8색인 것은 두 색 언어를 쓰는 일이다. 팔레트를
공유하면 "이 색이 무슨 뜻인가"가 한 번만 학습된다.

빈 문자열 = 색 없음. 중립 회색으로 그린다.

### 5.2 대화상자 [T7]

[관찰] 다섯째 장에서 **Parent Tag 줄만 뺀다** — 이 앱에 태그 계층이 없고, 만드는 것은
이 문서의 요청이 아니다.

```
┌ 태그 편집 ─────────────────────┐
│ [🏷] 일정                       │  ← 이름
│                                 │
│ 색   ⊘ ● ● ● ● ● ● ● ◉         │  ← 없음 + 프리셋 8 + 커스텀
│                                 │
│              [ 저장 ] [ 취소 ]  │
└─────────────────────────────────┘
```

**색 줄은 새로 만들지 않는다.** `CreateListModal`이 정확히 이 줄을 이미 그린다 —
`["", ...LIST_COLOR_PRESETS]`와 `CustomColorPicker`(그림의 무지개 고리)
(`CreateListModal.tsx:68`, `:242`, `:260`). 그 마크업을 꺼내 쓴다.

### 5.3 어디서 여는가

사이드바 태그 행의 **⋯**. 지금 그 행은 메뉴 없는 버튼 하나이므로(`TasksSidebar.tsx:127`)
트레일링 슬롯에 ⋯ 를 붙인다 — [관찰] 넷째 장이 같은 자리에 그린다.

메뉴 항목은 **편집** 하나로 시작한다. 삭제(`removeTag`는 이미 있다)는 §9 Q2다.

### 5.4 필요한 것 하나

`usePlannerData`에 `updateTag(tagId, patch)`가 없다. `updateList`와 같은 모양으로
하나 더한다.

### 5.5 이름을 바꾸면 id가 충돌한다 [추론 · 이 문서에서 가장 중요한 줄]

태그 id는 이름에서 나온다: `tagIdFor("학교")` = `tag-학교`(`tags.ts:41`). 이름을 바꿔도
**id는 그대로 둬야 한다** — 링크(`TaskTag.tagId`)가 그것을 가리키고 있다.

그러면 이런 일이 생긴다:

1. `학교`(id `tag-학교`)를 `학교2026`으로 바꾼다. id는 여전히 `tag-학교`
2. 나중에 누가 `학교`라는 태그를 새로 만든다
3. `toggleTaskTag`가 이름으로 기존 태그를 찾는데(`tagPicker.ts:165`) 이름이 바뀌었으니
   못 찾고, `tagIdFor("학교")` = **`tag-학교`** 로 새 레코드를 만든다
4. 같은 id를 가진 Tag가 둘이 된다

그러므로 이름 변경을 넣으려면 `toggleTaskTag`의 id 파생을 **충돌 안전하게** 고쳐야
한다: 파생 id가 이미 다른 이름의 태그에 잡혀 있으면 빈 자리를 찾을 때까지 접미사를
붙인다. 몇 줄이고, 이름 변경 없이는 절대 일어나지 않던 일이므로 이 문서가 갚아야 할
빚이다.

`Task.tags`의 레거시 문자열은 **다시 쓰지 않는다.** 링크가 정본이고(`tagNamesForTask`),
`backfillTaskTags`가 옛 문자열로 만드는 id는 어차피 같은 `tag-학교`라서 이름이 바뀐
그 레코드를 가리킨다. 즉 표시에는 영향이 없다.

---

## 6. 대비 — 이미 푼 문제 [T6]

`CALENDAR_FILL_READABILITY_DESIGN.md`가 함수 둘을 남겼다:

| 함수 | 무엇 |
|---|---|
| `darkenForWhiteInk(hex)` | 흰 글씨가 5.0:1로 읽힐 만큼 어둡게. 색상·채도는 유지 |
| `tintForDarkInk(hex)` | 짙은 글씨가 4.5:1로 읽힐 만큼 옅게 |

칩 글씨는 12px이라 큰 글씨 예외가 없다. 두 함수 모두 그 기준 위에서 만들어졌으니
대비는 어느 쪽이든 이미 보장된다. 남은 것은 배경이다:

| 테마 | 쓰는 것 | 왜 |
|---|---|---|
| 라이트 | `tintForDarkInk` + 짙은 잉크 | 흰 종이 위 옅은 알약. 행에 칩이 셋이어도 조용하다 |
| 다크 | `darkenForWhiteInk` + 흰 잉크 | 어두운 배경에 lightness 0.9 알약은 손전등이다 |

두 값을 한 번에 계산해 **둘 다 CSS 변수로** 넘기고 테마가 고른다. 컴포넌트가 테마를
읽지 않으므로 전환에 리렌더가 없다. 다크에서 채움이 맞는지는 화면을 보고 확정한다(§9 Q1).

---

## 7. 자리는 표면이 정한다 [T8, T9]

### 7.1 결정 [결정]

| 표면 | 선택자 | 칩의 자리 |
|---|---|---|
| 리스트 행 | `.tm-task` | **제목 아래 줄, 왼쪽** |
| 보드 카드 | `.tm-task.is-card` | 제목 아래 줄, 왼쪽 |
| 매트릭스 카드 | `.ff-matrix-card` | **오른쪽**, 지금 자리 그대로 |

앞 판(3차)은 "칩은 리스트 이름과 한 무리이고, 둘째 줄이 있을 때만 내려간다"였다. 사용자가
**표면으로** 갈랐으므로 그 규칙은 폐기한다 — 본문 표시 여부는 이제 칩의 자리와 무관하다.

### 7.2 리스트 행 — 제목 아래 줄

```
□ tnrwp                          Inbox  Sep 4
  [학교] [일정]                            ← 태그가 있으면 생기는 줄
```

본문 표시가 켜져 있으면 본문이 먼저다:

```
□ tnrwp                          Inbox  Sep 4
  aa
  [학교]
```

칩은 **자기 줄**을 갖는다. 본문 줄에 얹지 않는다 — 본문은 한 줄로 잘린 산문이고
(`firstLine`), 그 끝에 칩을 붙이면 잘린 자리가 어디인지 읽을 수 없게 된다.

리스트 이름과 날짜는 **첫 줄 오른쪽에 남는다.** 3차가 "칩과 리스트 이름은 한 무리"라고
쓴 것은 참조 화면 두 장을 화해시키려던 것이었고, 사용자의 결정이 그 화해를 필요 없게
만들었다. 무리를 가르지 않으므로 `.tm-task-tips`는 지금 모양 그대로다 — **칩만 빠져
나와 자기 줄로 간다.**

### 7.3 대가 [실측]

**태그가 있는 행은 두 줄이 된다.** 40px 행이 태그를 가진 것만 높아지고, 목록의 리듬이
행마다 달라진다. 이것이 이 결정의 값이자 대가이고, 사용자가 그림을 보고 고른 것이다.

`.tm-task:has(.tm-task-body)`가 이미 두 줄 행의 정렬(체크박스를 첫 줄에 붙임)을 정의해
뒀다. 칩 줄도 같은 처리를 받아야 하므로 그 선택자에 칩 줄을 더한다 — 새 규칙이 아니라
같은 규칙의 대상 하나 추가다.

### 7.4 보드 카드도 아래 [결정]

사용자가 가른 것은 "일반 task"와 "매트릭스"이고, 보드 카드는 그 사이에 있다. 선택자가
다르므로(`.tm-task.is-card` vs `.ff-matrix-card`) 어느 쪽으로든 한 줄이면 된다.

**행과 같이 아래** [결정]. 보드는 작업 모듈의 같은 표면이고 카드 폭이 사분면보다 넓다.
매트릭스만 오른쪽으로 남는다.

### 7.5 매트릭스 — 데이터 하나만 [T9]

칩의 자리는 지금 그대로이므로 매트릭스에서 바꿀 것은 **레이아웃이 아니라 데이터**다.
`MatrixPage`가 `tags`만 받고 `taskTags`를 못 받는다(`MatrixPage.tsx:95`). `AppPages`에서
프롭 하나 넘기면 카드가 칩을 그린다.

앞선 "매트릭스에는 넣지 않는다"의 근거는 "사분면이 이미 우선순위를 말한다"였는데, 사분면이
말하는 것은 우선순위이지 태그가 아니다. 뒤집는 것이 맞다.

## 8. 무엇이 바뀌는가

| 파일 | 변화 |
|---|---|
| `components/tasks/TagChip.tsx` | **신규.** 한 벌의 칩. `onRemove`가 있으면 × |
| `components/tasks/TagEditModal.tsx` | **신규.** 이름 + 색 + 저장/취소 (§5.2) |
| `components/tasks/TasksSidebar.tsx` | 태그 행 트레일링에 ⋯ , 편집 열기 |
| `components/tasks/TagPicker.tsx` | 자기 칩 마크업을 버리고 `TagChip` |
| `components/tasks/TaskRowContent.tsx` | `#이름` 글씨 → `TagChip`. 행에서는 tips 밖 자기 줄로, 매트릭스에서는 tips 안에 (§7) |
| `components/MatrixPage.tsx` · `app/AppPages.tsx` | `taskTags` 프롭 하나 — 카드가 칩을 받는다 (§7.4) |
| `components/tasks/TaskDrawer.tsx` | 스크롤 끝 스페이서, 클릭이 본문 포커스 |
| `domain/tags/tagPicker.ts` | id 파생을 충돌 안전하게 (§5.5) |
| `hooks/usePlannerData.ts` | `updateTag` |
| `styles/17-tasks-module.css` | 칩 한 벌로 통합, `.tm-task-tag` 삭제, `.tm-drawer-content` flex 제거, 스페이서, 둘째 줄 |

`.tm-task-tag`는 **삭제한다.** 오늘 들어간 규칙이고 대체가 아니라 폐기다.

---

## 9. 단계

| # | 항목 | 선행 |
|---|---|---|
| 1 | 상세 배치 — 스페이서 · 본문 flex 제거 · 빈 자리 클릭 (§3) | 없음 |
| 2 | `TagChip` + 대비 변수 + 세 표면(상세·행·카드) + meta 무리 가르기 (§4, §6, §7) | 없음. 1과 독립 |
| 3 | `updateTag` · 충돌 안전 id · 사이드바 ⋯ · 편집 대화상자 (§5) | 2 (색이 보여야 고른 값이 보인다) |

3이 끝나기 전까지 모든 칩은 중립 회색이다. 그것이 이 순서의 유일한 어색함이고, 반대로
가면(대화상자 먼저) 고른 색이 아무 데도 안 보인다.

---

## 10. 열린 질문

| # | 질문 | 기본값 제안 |
|---|---|---|
| Q1 | 다크 모드는 채움인가 틴트인가 (§6) | 채움. 화면 보고 확정 |
| Q2 | ⋯ 메뉴에 **삭제**도 넣는가 (`removeTag`는 이미 있다) | 넣지 않는다. 편집부터. 삭제는 링크까지 지우므로 확인 모달이 따로 필요하다 |
| Q3 | ~~보드 카드는 어느 쪽인가~~ | **해소.** 행과 같이 아래 (§7.4) |
| Q4 | 한 행에 태그가 다섯이면 | 각 칩 8em 말줄임. 개수 제한 없음 |
| Q5 | ~~매트릭스 카드에도 넣는가~~ | **해소.** 넣는다 — T9, §7.4 |

---

## 11. 실패 모드

| 무엇 | 결과 | 대응 |
|---|---|---|
| `Tag.color`가 파싱 불가능한 값 | `listColorHex`가 `""` | 중립 회색. 값은 지우지 않는다 (`parseListColor`의 규칙 그대로) |
| 이름 변경 뒤 같은 이름을 새로 만듦 | 같은 id의 Tag가 둘 | §5.5 — id 파생을 충돌 안전하게 |
| 태그 색이 리스트 색과 겹침 | 실제로 겹친다 (같은 팔레트) | 알약과 점은 모양이 다르다. 색만으로 구별하게 두지 않는다 |
| 태그가 많은 행 | 칩이 제목 폭을 먹는다 | Q4 — 8em 말줄임 |
| 매트릭스 카드에 칩 + 리스트 이름 + 날짜 | 한 칸 너비에 셋이 겹친다 | 카드는 이미 접힌다(§7.4). 실제 화면에서 확인할 것 |
| 체크리스트 모드에서 빈 자리 클릭 | 아무 일 없음 | §3.2 — 의도된 무동작 |
