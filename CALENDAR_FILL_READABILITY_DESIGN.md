# 글씨는 흰색 하나로, 색은 그 흰색이 읽히는 만큼만

> 상태: **구현됨** · 2026-09-04 (G1 = B · G2 = 5.0 · G3 = A · G4 = 블록만 · G5 = 수용, 사용자 승인 — §9.1은 구현하며 옮겨진 경계)
> (사용자가 한 장을 주었다: 주간 격자에 회색 블록 셋, 그 위 검은 글씨가 잘 안 읽힌다.)
> 요청: "글씨를 흰색으로 하되, 색 선택을 흰 글씨가 잘 보이는 색들로 하게끔."
> 대상: `domain/calendar/{itemColor,readableInk}.ts` · `domain/tasks/listColor.ts` ·
> `components/calendar/eventColorVars.ts` · `styles/10-calendar-apple.css`
> 선행 문서: `CALENDAR_TASK_CHECKBOX_DESIGN.md` §3(잉크 자동 선택을 도입한 문서 —
> 이 문서가 그 결정을 좁힌다) · `CALENDAR_COLOR_SOURCE_AND_VIEW_OPTIONS_DESIGN.md`
> §3.4 E1(Inbox 중립 회색 — 스크린샷의 회색이 이것이다)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 1장 | [관찰] |
| **실측** | `NEUTRAL_LIST_COLOR = #8e8e93` · `LIST_COLOR_PRESETS` 8색 · `readableInkOn`이 회색에 검정을 고른다 | [실측] |
| **계산** | 9색 각각의 흰/검정 대비, 목표 대비까지 필요한 어둡기, 블록–캔버스 분리도 (§2, §3, §4) | [계산] |
| **결정** | 사용자 — 잉크는 흰색, 팔레트는 흰 글씨가 읽히는 것으로 | [결정] |

---

## 1. 스크린샷의 회색은 팔레트가 아니다 [실측]

먼저 진단을 정확히 해야 한다. [관찰]의 블록 셋은 **전부 회색**인데, 그 회색은 여덟
프리셋 중 하나가 아니라 **Inbox의 중립색**이다.

```ts
// domain/calendar/itemColor.ts
export const NEUTRAL_LIST_COLOR = "#8e8e93";
if (list.kind === "inbox") return NEUTRAL_LIST_COLOR;
```

`a` · `as` · `as`는 리스트를 지정하지 않고 빠르게 적은 태스크이고, `listIdFor`가 그런
태스크를 Inbox로 보낸다. 그래서 화면이 회색으로 덮였다.

`#8e8e93`의 대비는 흰 글씨 3.26 : 1, **검은 글씨 5.79 : 1** [계산]. `readableInkOn`은
규칙대로 검정을 골랐고 4.5:1도 넘겼다 — **기준은 통과했는데 눈에는 흐리다.** 중간 밝기의
무채색 위 검은 글씨는 "비활성"으로 읽히기 때문이고, 이건 대비비가 잡아내지 못하는 종류의
문제다. 사용자의 지적이 맞다.

즉 고칠 것이 둘이다: **잉크 규칙**(§2)과 **Inbox가 회색이어야 하는가**(§5).

---

## 2. 잉크를 흰색으로 고정하면 무엇이 걸리나 [계산]

지금 팔레트로 흰 글씨를 고정하면 여덟 중 여섯이 4.5:1 아래다.

| 프리셋 | hex | 흰 글씨 | 검정 글씨 |
|---|---|---|---|
| lime | `#99d52a` | **1.76** ❌ | 10.71 |
| yellow | `#ffb224` | **1.80** ❌ | 10.47 |
| orange | `#f76b15` | **2.97** ❌ | 6.36 |
| green | `#30a46c` | **3.16** ❌ | 5.98 |
| blue | `#0a84ff` | **3.65** ❌ | 5.18 |
| red | `#e5484d` | **3.91** ❌ | 4.82 |
| purple | `#8e4ec6` | 5.18 ✅ | 3.65 |
| indigo | `#5b5bd6` | 5.37 ✅ | 3.52 |
| (Inbox) grey | `#8e8e93` | **3.26** ❌ | 5.79 |

그래서 "흰 글씨로 고정"은 **팔레트를 손대는 일과 한 몸**이다. 사용자의 제안이 정확히
그 둘을 묶은 이유가 이것이다.

---

## 3. 팔레트를 바꾸는 두 가지 방법 — 결정 G1

| 안 | 무엇 | 커스텀 hex는? |
|---|---|---|
| **A** | 여덟 프리셋의 hex를 어두운 값으로 **교체** | ❌ 안 덮인다 |
| **B** | 어떤 색이든 목표 대비까지 **자동으로 어둡게** (순수 함수) | ✅ 덮인다 |

> **권장 B.** 결정적인 이유는 커스텀 색이다. `List.color`는 프리셋 키뿐 아니라
> `#RRGGBB`도 받고, 리스트 만들기 화면에 `<input type="color">`가 있다 [실측]. A로 가면
> 옅은 노랑을 손으로 고른 사용자에게서 글씨가 다시 사라진다 — 규칙이 아니라 목록이라서,
> 목록 밖의 값을 막지 못한다.
>
> B는 함수 하나다. HSL의 명도만 낮춰 목표 대비에 닿을 때까지 내리므로 **색상(hue)은
> 유지된다**. 프리셋이든 커스텀이든 같은 대우를 받는다.

목표 대비 5.0으로 계산한 결과 [계산]:

| 프리셋 | 지금 | 바뀐 뒤 | 흰 글씨 |
|---|---|---|---|
| red | `#e5484d` | `#d91f25` | 5.0 |
| orange | `#f76b15` | `#bc4c06` | 5.0 |
| yellow | `#ffb224` | `#9a6400` | 5.0 |
| lime | `#99d52a` | `#577918` | 5.1 |
| green | `#30a46c` | `#257e53` | 5.0 |
| blue | `#0a84ff` | `#006dda` | 5.0 |
| indigo | `#5b5bd6` | `#5b5bd6` | 5.4 (그대로) |
| purple | `#8e4ec6` | `#8e4ec6` | 5.2 (그대로) |
| (Inbox) grey | `#8e8e93` | `#6e6e73` | 5.1 |

**정직하게 적어 둘 대가: 노랑과 라임이 정체성을 잃는다.** `#ffb224`(노랑)는
`#9a6400`(황토)이 되고 `#99d52a`(라임)는 `#577918`(올리브)이 된다. 흰 글씨가 읽히는
노랑은 존재하지 않으므로 — 노랑은 밝아서 노랑이다 — 이건 우회로가 없는 교환이다.
나머지 여섯은 알아볼 수 있게 남는다.

### 3.1 결정 G2 — 목표 대비를 얼마로

| 목표 | 노랑 | 라임 | 성격 |
|---|---|---|---|
| 4.6 | `#a26900` | `#5c8019` | 기준선을 겨우 넘김. 11px 글씨에는 빠듯하다 |
| **5.0** | `#9a6400` | `#577918` | 여유가 있고 팔레트도 아직 밝다 |
| 5.5 | `#905d00` | `#527217` | 확실하지만 전체가 눈에 띄게 어두워진다 |

> **권장 5.0.** 블록 글씨는 11px/10px이고 기준선(4.5)에 딱 붙이면 안티에일리어싱만으로도
> 체감이 달라진다. 5.5는 여덟 색이 다 같은 어두운 채도로 몰려 서로 구별이 어려워진다.

---

## 4. 다크 테마에서 손해가 나는가 [계산]

블록은 두 테마에서 같은 색으로 칠해지므로, 어둡게 하면 어두운 캔버스(`#101012`)와의
분리가 줄어든다. 실제로 재 보면:

| | 지금 (다크) | 바뀐 뒤 (다크) | 지금 (라이트) | 바뀐 뒤 (라이트) |
|---|---|---|---|---|
| lime | 10.78 | 3.76 | 1.76 | **5.06** |
| yellow | 10.54 | 3.80 | 1.80 | **5.00** |
| orange | 6.40 | 3.78 | 2.97 | **5.03** |
| blue | 5.21 | 3.80 | 3.65 | **5.00** |
| indigo | 3.54 | 3.54 | 5.37 | 5.37 |

두 가지를 말해 준다.

1. **다크에서 3.5~3.8 : 1로 내려가지만 문제는 아니다.** 비문자 요소의 기준은 3:1이고,
   이건 격자선에 둘러싸인 큰 면이다.
2. **오히려 고르게 된다.** 지금은 라임이 10.8로 튀고 인디고가 3.5로 가라앉아, 같은
   격자에서 어떤 블록은 소리치고 어떤 블록은 물러난다. 바뀐 뒤에는 전부 3.5~3.8이고
   **차이는 색상뿐**이 된다 — `CALENDAR_APPLE_DESIGN.md` §D1이 "색은 어느 달력인가만
   말한다"고 한 그 상태다.

---

## 5. Inbox는 회색이어야 하는가 — 결정 G3

E1이 "Inbox는 이름을 붙인 적 없는 시스템 리스트라 색으로 말할 것이 없다"고 중립 회색을
줬다. [관찰]이 보여주는 것은 그 판단의 대가다: 리스트를 지정하지 않고 적은 태스크가
전부 Inbox로 가므로, **많은 사용자에게 회색이 기본 경험이 된다.**

| 안 | 무엇 |
|---|---|
| **A** | 어두운 회색(`#6e6e73`) 유지 |
| **B** | Inbox에도 색을 준다 (예: 파랑) |
| **C** | Inbox도 id 해시로 색을 배정 (다른 리스트와 같은 규칙) |

> **권장 A.** 회색은 "아직 어디에도 안 넣었다"를 말하고 그건 사실이다 — 색을 주면 Inbox가
> 여덟 리스트 중 하나처럼 보이고, 태스크를 리스트로 옮길 이유가 화면에서 사라진다.
> 어두운 회색에 흰 글씨는 [관찰]의 흐릿함과 전혀 다르게 읽힌다.
> C는 특히 나쁘다: Inbox는 계정마다 id가 다르므로 사람마다 Inbox 색이 달라진다.

---

## 6. 무엇을 어디까지 어둡게 하나 — 결정 G4

`List.color`가 그려지는 자리는 셋이다.

| 자리 | 크기 | 글씨를 이고 있나 |
|---|---|---|
| 캘린더 블록·칩 (`--ev-color`) | 큼 | **예** |
| Tasks 사이드바의 점 | 8px | 아니오 |
| 리스트 만들기의 스와치 | 20px | 아니오 |

> **권장: 블록만.** 어둡게 하는 이유는 오직 **그 위에 흰 글씨가 얹히기 때문**이다. 점과
> 스와치는 글씨를 이고 있지 않으므로 밝은 채로 두는 편이 낫다 — 8px 점은 밝을수록 잘
> 보이고, 스와치는 사용자가 고르는 색 그 자체여야 한다.
>
> 대가: 노랑 리스트의 사이드바 점은 노랑인데 캘린더 블록은 황토다. 같은 색상이라 서로
> 이어지지만 같은 값은 아니다. 셋을 다 어둡게 하면 일관되지만, 그러면 **팔레트 전체가
> 어두워지는 결정**이 되고 그건 캘린더 하나의 가독성 문제가 요구하는 것보다 크다.

---

## 7. 코드

```ts
// domain/calendar/readableInk.ts 에 추가
/**
 * 흰 글씨가 목표 대비로 읽힐 때까지 색상을 유지한 채 어둡게 만든다.
 * 이미 충분히 어두우면 그대로 돌려준다 (indigo·purple).
 */
export function darkenForWhiteInk(hex: string, target = WHITE_INK_TARGET): string;
export const WHITE_INK_TARGET = 5;
```

부르는 곳은 한 군데다 — `colorForTask`가 돌려주는 값을 통과시키면
`eventColorVars`가 그 값을 `--ev-color`로 내보내고, 블록·칩·완료 틴트·체크박스 테두리가
전부 같은 색을 쓴다.

`readableInkOn`은 **지우지 않는다.** 어둡게 한 색에 대해서는 항상 `"light"`를 돌려주므로
CSS는 그대로 `--ev-ink-auto`를 쓰면 되고, 어떤 병적인 입력에서 어둡게 하기가 실패하더라도
잉크가 따라 바뀌어 글씨가 사라지지 않는다. 규칙이 아니라 안전망으로 남는다.

---

## 8. 무엇을 테스트하는가

**단위 (vitest)**
- `darkenForWhiteInk`: 프리셋 8색 + Inbox 회색 + 흰색·노랑 같은 극단값이 전부 목표 대비 이상
- 색상 보존: 어둡게 한 색의 hue가 원본의 ±3° 안
- 멱등성: 이미 어두운 색(indigo)은 그대로 — 두 번 통과시켜도 값이 안 변한다
- `readableInkOn(darkenForWhiteInk(x))`는 언제나 `"light"` (§7의 안전망이 실제로 안전망인지)
- `colorForTask`가 어둡게 한 값을 돌려준다

**E2E (playwright)**
- 라임 리스트의 블록 글씨가 흰색이고 배경 대비가 4.5 이상 (지금은 검은 글씨가 나온다)
- Inbox 태스크의 블록이 어두운 회색 + 흰 글씨
- 사이드바 점은 밝은 채로 남는다 (G4의 경계가 실제로 지켜지는지)

---

## 9. 결정 목록

| | 질문 | 안 | 권장 |
|---|---|---|---|
| **G1** | 팔레트를 어떻게 흰-글씨-안전하게 | A 프리셋 교체 / B 자동 어둡게 | **B** |
| **G2** | 목표 대비 | 4.6 / 5.0 / 5.5 | **5.0** |
| **G3** | Inbox 색 | A 어두운 회색 / B 파랑 / C 해시 배정 | **A** |
| **G4** | 어디까지 어둡게 | 블록만 / 점·스와치까지 | **블록만** |
| **G5** | 노랑·라임이 황토·올리브가 되는 것 | 받아들인다 / 그 둘을 다른 색상으로 교체 | **받아들인다** |

### 9.1 G4의 경계가 한 칸 옮겨졌다 — 구현하며

설계는 "블록만 어둡게, 점과 스와치는 밝게"라고 썼다. 구현하고 나니 기존 E2E 하나가
그걸 반박했다:

```
the left column › paints its swatch the colour the blocks are
  "A row that disagreed with its own blocks would be worse than no row."
```

**캘린더의 왼쪽 열은 점이 아니라 블록의 범례다.** 격자 바로 옆에 서서 "이 색 = 이
리스트"를 말하는 것이 그 열의 유일한 일이므로, 스와치가 밝고 블록이 어두우면 그 문장이
깨진다. 그래서 `buildCalendarCategories`의 personal 그룹도 `darkenForWhiteInk`를
통과한다.

밝은 채로 남는 것은 **다른 화면에 있는 것들**이다 — Tasks 사이드바의 8px 점과 리스트
만들기의 스와치. 옆에 맞출 블록이 없고, 8px 점은 밝을수록 잘 보인다. G4가 지키려던
것("글씨를 이지 않는 것은 어둡게 하지 않는다")은 그대로이고, 어느 쪽이 "글씨를 인
면과 한 화면에 있는가"로 선이 다시 그어졌을 뿐이다.

### 9.2 실제로 만들어진 것

| 무엇 | 어디 |
|---|---|
| 어둡게 하기 | `domain/calendar/readableInk.ts`의 `darkenForWhiteInk` + `WHITE_INK_TARGET = 5` |
| 채움에 적용 | `domain/calendar/itemColor.ts`의 `colorForTask` — 유일한 통과 지점 |
| 왼쪽 열 스와치 | `lib/calendar/categoryModel.ts` (§9.1) |
| 회귀 방지 | `readableInk.test.ts` 7개 · `itemColor.test.ts` 1개 · `e2e/calendarCheckbox.spec.ts` 2개 |

`readableInkOn`은 남는다. 어둡게 한 색에는 언제나 `"light"`를 돌려주므로 CSS는 그대로
`--ev-ink-auto`를 쓰고, 어둡게 하기가 어떤 입력에서 실패하더라도 잉크가 따라 바뀌어
글씨가 사라지지 않는다 — 규칙이 아니라 안전망으로.
