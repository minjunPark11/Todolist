# 집중을 화면의 중심에 둔다 — Spatial Focus Field

> 상태: **대체됨 — `FOCUS_TABS_AND_RECORD_DESIGN.md` 를 본다** · 2026-09-10
> 세 번째 목업이 원·3열·바닥 리본을 쓰지 않는다. 이 문서에서 살아남은 것은 셋이다 —
> 앵커(§4), 몰입을 감쇄로 처리한다는 결론(§5), 그리고 Phase 0의 선수정(§9).
> 요청: 첨부 목업(`01_spatial_focus_field_v2.html`)의 배치로 집중 탭 메인 화면을 다시 짠다. **노트는 뺀다.**
> 대상: `components/FocusPage.tsx`(1,408줄) · `styles/24-focus.css`(898줄)
> 선행 문서: `FOCUS_LAYOUT_DESIGN.md`(직전 배치 — 2열·카드) ·
> `SWISS_MINIMAL_DESIGN.md`(자 — §5 스케일, §11 간격·표면, I1-B 반지름, I2-B 그림자)
> 가드: `e2e/radiusScale.spec.ts` · `e2e/surfaceHierarchy.spec.ts` · `src/styles/scale.test.ts` ·
> `e2e/focusTimerV2.spec.ts` · `e2e/overlayScrollbar.spec.ts`

---

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 목업을 브라우저에서 띄워 잰 값 · 현재 화면을 띄워 잰 값 · 저장소의 스펙과 토큰 | [실측] |
| **관찰** | 목업 한 장이 보여주는 의도 | [관찰] |
| **결정** | 사용자 — 이 배치, 노트 제외 | [결정] |
| **추론** | 목업이 보여주지 않는 것(상태 여섯 · 좁은 폭 · 키보드 · 기록 뷰) | [추론] |

---

## 1. 목업이 실제로 바꾸는 것 [실측]

1440×900에서 잰 골격이다.

```
stage      1340×864 @ 82,18      창 안쪽에 떠 있는 액자 (모서리 28px, 반투명)
 ├ head    1278×73               eyebrow+제목 | 모드 토글 | 액션 3
 ├ workspace 1278×637            [메모 300] [중앙 576] [큐 342]
 │   ├ anchor  390×44            원 위에 뜬 작업 선택 알약
 │   └ core    500×500           원. 안에 상태·시계·보조시간·주 버튼·힌트
 └ ribbon   1278×66              오늘 집중 / 완료한 작업 / 평균 세션 + 저장 상태
```

**세 층이다** — 머리(맥락과 전역 조작), 몸(작업 하나 + 다음 순서), 바닥(오늘의 합계).
지금 화면은 두 층이고(머리 + 2열), 요약이 오른쪽 열 바닥에 갇혀 있다.

### 1.1 지금과의 차이

| | 지금 | 목업 |
|---|---|---|
| 타이머 | 왼쪽 카드 안, 사각형 면 | **원**. 화면의 광학적 중심 |
| 작업 선택 | 타이머 스택 안의 한 줄 | 원 **위에 뜬** 앵커 — 원이 무엇을 재는지 먼저 말한다 |
| 주 버튼 | 시계 아래, 카드 안 | 원 **안** |
| 모드 토글 | 타이머 스택 맨 위, 파랑 채움 | 머리 가운데, 무채색 |
| 요약 | 오른쪽 열 바닥 (530폭) | 바닥 전폭 리본 |
| 큐 | 오른쪽 카드, 56px 행 | 오른쪽, 타임라인 척추 + 카드 |
| 몰입 | 어두운 오버레이(별도 토큰 세트) | **같은 화면을 디밍** |
| 노트 | 왼쪽 카드 하단 | (요청에 따라 제외) |

---

## 2. 목업의 시각 언어는 우리 자와 충돌한다 [실측]

`FOCUS_LAYOUT_DESIGN.md` §14가 같은 일을 한 번 했다 — 시안의 요소를 넷으로 쪼개
"반전인 것"과 "아닌 것"을 갈랐다. 같은 방식으로 목업을 잰다.

| 목업이 쓰는 것 | 값 [실측] | 우리 자 | 판정 |
|---|---|---|---|
| 모서리 | 28·18·17·15·15·14·13·12·12·11·10px **(11개)** | I1-B: `[4]` 하나 | **반전** |
| 글자 굵기 | 520·650·680·720·730·760·770·780·800 **(9개)** | §5.2: 400·600·700 | **반전** |
| 글자 크기 | 10.5·14·16·17·26·28px | §5.1: 10·11·12·13·15·18·24 | **반전** |
| 그림자 | 12곳, 그중 **2곳이 파란 그림자** | I2-B: 떠 있는 것만, §11.38: 색 없음 | **반전** |
| 배경 격자·방사 글로우 | 36px 격자 + `radial-gradient` | 없음 | **반전** |
| `backdrop-filter: blur` | 6곳 | 없음 | **반전** |
| 원 | `border-radius: 50%` | — | **반전 아님** (§2.2) |
| 면과 헤어라인 | 카드 = 면 + 1px 선 | §11.2 "경계는 헤어라인이 만든다" | **반전 아님** |
| 3열 + 바닥 리본 배치 | — | 자에 배치 규정 없음 | **반전 아님** |
| 원 위의 앵커 알약 | `--radius-pill` | I1이 pill을 형태로 남겨둠 | **반전 아님** |

`e2e/radiusScale.spec.ts`의 `SCALE = [4]`와 `surfaceHierarchy.spec.ts`의
"화면의 어떤 그림자도 색을 갖지 않는다"가 위 표의 1·4행을 **실행 중인 브라우저에서**
막는다. 목업을 그대로 옮기면 두 스펙이 빨개진다 — grep이 아니라 렌더된 화면을 재는
스펙이라 우회할 방법이 없다.

### 2.1 그래서 무엇을 하나

**배치는 전부 채택하고, 시각 언어는 자 위에서 다시 그린다.** 목업이 만들어내는
인상 — 넓은 여백, 중심의 원, 주변으로 밀려난 보조 정보 — 은 모서리 28px과 파란
그림자에서 오는 것이 아니라 **배치와 여백**에서 온다. 세 가지만 옮기면 그 인상은
남는다.

1. 원이 화면의 광학적 중심을 차지한다
2. 보조 정보(큐·요약)가 주변부로 물러나고 위계가 낮아진다
3. 몰입은 새 화면이 아니라 **같은 화면의 감쇄**다

모서리는 4px, 그림자는 없음(카드는 헤어라인 + `--bg-surface-muted`, §14의 관용구
그대로), 글자는 스케일 안, 글로우와 블러는 쓰지 않는다.

### 2.2 원은 반전이 아니다 — 선례가 있다

`radiusScale.spec.ts`의 허용 목록은 "모서리가 아니라 형태"인 것들을 이미 담고 있다:
`.ff-check`(완료 컨트롤은 원), `.ff-color-swatch`(색은 점), `.gcal-col-date`(달력의
하루는 원). 집중 코어는 같은 자리다 — 둥근 사각형이 아니라 **원이라는 형태**이고,
그 형태가 "이것이 지금 도는 하나"라는 의미를 진다.

> 허용 목록에 한 줄을 추가한다:
> `".focus-core", // 집중의 코어는 원이다 — 모서리가 아니라 형태(§2.2)`

주의: 이 스펙은 `%` 반지름을 **자동으로 봐주지 않는다**(`!radius.includes("%") && …`).
20px보다 큰 원은 반드시 허용 목록에 이유와 함께 적어야 한다.

---

## 3. 노트를 빼면 왼쪽이 빈다 [결정 필요]

목업의 3열에서 왼쪽 300px은 노트 전용이다. 그것을 빼면 세 갈래가 생긴다.

| | 무엇 | 원의 위치 | 대가 |
|---|---|---|---|
| **A. 빈 여백으로 남긴다** | `1fr · 코어 · 큐`. 왼쪽은 아무것도 두지 않는다 | 스테이지의 **정중앙** | 왼쪽이 빈다. 그런데 그 빔이 목업의 인상 그 자체다 |
| B. 2열로 접는다 | `코어 · 큐` | 남은 공간의 가운데 = 왼쪽으로 이동 | 원이 중심이 아니게 된다. 목업의 요점이 무너진다 |
| C. 왼쪽에 다른 것을 넣는다 | 오늘의 세션 목록 / 최근 기록 | 정중앙 | 새 기능이다. 요청 범위 밖이고, "하나에 집중"과 반대 방향 |

**추천: A.** 원이 정중앙에 있는 것이 이 배치의 전부이고, 대칭을 지키는 가장 싼 방법이
왼쪽을 비우는 것이다. 지금 화면의 문제 중 하나가 "카드는 늘어나는데 내용은 안 늘어나
빈 액자가 된다"는 것이었는데(직전 평가 §3), **의도된 여백은 빈 액자와 다르다** — 액자
선이 없으면 빔은 결핍이 아니라 공간으로 읽힌다. 그래서 A에서는 왼쪽에 **아무 테두리도
그리지 않는다.**

### 3.1 노트 자체를 없애는 것은 아니다 [추론 — 확인 필요]

"노트는 빼고"를 **메인 화면에서 뺀다**로 읽었다. 세션 메모 기능 자체는 남긴다:

- 실행 중: 원 **밖** 아래의 텍스트 버튼 `메모` → 기존 `focus-note-dialog`
- 지난 세션: 기록 줄에서 여는 길이 이미 있다 (`FocusPage.tsx`의 주석이 이 다이얼로그를
  남겨둔 이유로 적어둔 그 길)

기능까지 삭제하는 의도였다면 §8에서 되돌린다. `Phase 3.5`가 "다이얼로그에서 자리로"
옮긴 것을 되돌리는 셈이므로, 그 문서의 결론을 뒤집는다는 사실을 적어둔다.

---

## 4. 원 안에 무엇이 들어가는가 — 상태는 여섯이다 [추론]

목업은 한 상태(실행 중)만 보여준다. 실제 화면은 여섯이고, 원의 지름은 고정인데
상태별 내용 높이가 다르다. **원 안은 다섯 줄까지**로 못박고 나머지는 원 밖으로 내린다.

| 상태 | 원 위 앵커 | 원 안 (위→아래) | 원 밖 아래 |
|---|---|---|---|
| 유휴 | 작업 선택 ⌄ | — · `00:00` · 오늘 누적 · **집중 시작** | 힌트 한 줄 |
| 유휴 + 플로우 | 작업 선택 ⌄ | n/N 완료 · `00:00` · 오늘 누적 · **다음 집중 시작** | 마치기 |
| 실행 중 | 작업명(읽기 전용) | ● 집중 중 · 시계 · 오늘 누적 · **일시정지** | 종료 · 메모 |
| 일시정지 | 〃 | ● 일시정지됨 · 시계 · 오늘 누적 · **재개** | 종료 · 메모 |
| 복구됨 | 〃 | ● 복구됨 · 시계 · 오늘 누적 · **재개** | 공백 확인 · 종료 · 메모 |
| 휴식 | 숨김 | ● 휴식 중 · 짧은/긴 휴식 · 시계 · **휴식 시작/일시정지** | 휴식 종료 |
| 결과 | 숨김 | ● 기록됨 · 누적 시간 · **다시 집중** | 작업 연결 · 작업 완료 · 메모 · 마치기 |

**작업명은 앵커에만 있다.** 처음 이 표를 쓸 때 실행 중 상태의 작업명을 앵커와 원 안에
둘 다 적었는데, 그리고 보니 같은 문자열이 32px 간격으로 두 번 나온다. 앵커가 이기는
이유는 그것이 여섯 상태에서 **자리가 변하지 않는 유일한 요소**이기 때문이다 — 유휴에서
고르는 자리와 실행 중에 읽는 자리가 같아야 "원이 무엇을 재는지"가 한 곳에서 답해진다.
그 대가로 원 안은 어느 상태에서나 네 줄이 되고, 지름이 고정인 원에 그것이 맞다.

규칙 셋:

1. **원 안의 주 버튼은 언제나 하나**이고, 원 안은 언제나 네 줄이다. 지금 화면은 실행 중에 주 버튼이 없고 가장 강한
   색이 빨간 `종료`인데(직전 평가 §7), 이 배치에서는 원 안의 하나가 자동으로 주 버튼이
   된다 — 위계가 자리에서 결정된다.
2. **`종료`는 danger를 벗는다.** 성공적 완료이지 파괴가 아니다. 원 밖의 중립 텍스트
   버튼으로 내린다. danger는 되돌릴 수 없는 삭제에만 남긴다.
3. **복구 안내와 에러는 원 밖, 리본 위**의 한 줄 배너로 올린다. 지금은 둘 다
   `--color-primary-tint`를 쓰고 있어서 모드 토글의 트랙과 같은 색인데(직전 평가 §6),
   여기서 에러는 danger 계열, 복구는 warning 계열로 가른다.

### 4.1 결과 상태는 원을 비우지 않는다

결과 화면의 액션은 넷이다(다시 집중 · 작업 연결 · 작업 완료 · 마치기). 넷을 원 안에
넣으면 원이 대화상자가 된다. `다시 집중` 하나만 원 안에 두고 나머지 셋은 원 밖에 둔다 —
그래야 "원 = 지금 도는 하나"라는 규칙이 여섯 상태에서 깨지지 않는다.

---

## 5. 이 배치가 고치는 것

직전 평가에서 잰 문제 열 개 중 여섯이 배치 자체로 사라진다.

| 직전 평가 | 이 배치에서 |
|---|---|
| ① 몰입 라이트 테마 대비 **1.09** — 시계가 안 보임 | **원인이 사라진다.** 몰입이 별도 토큰 세트가 아니라 같은 화면의 디밍이므로 `--bg-surface-muted`를 덮어쓸 일이 없다 |
| ③ 카드는 늘어나고 내용은 안 늘어남(빈 곳 155·270px) | 원은 자기 지름을 갖고, 왼쪽 여백은 의도된 것이다 |
| ⑥ tint 하나가 토글·복구·에러를 겸함 | §4의 규칙 3 |
| ⑦ 모드 토글과 CTA가 같은 파랑 / 실행 중 주 버튼 없음 | 토글은 무채색, 주 버튼은 원 안 하나 |
| ⑧ "No task assigned"가 24px 헤드라인 | 작업명은 원 안의 보조 줄(12–13px). 없으면 앵커가 "작업 선택"으로 남고 원 안에서는 그 줄이 빠진다 |
| ⑨ 유휴 상태의 비활성 노트 영역 | 노트가 메인에서 빠진다 |

②(다크 주 버튼 대비 3.65)와 ④(기록 뷰 정렬), ⑤(900/901 언어 전환), ⑩(큐의 `0s`)은
배치가 고쳐주지 않는다. §7·§9에서 함께 처리한다.

---

## 6. 이 배치가 새로 만드는 문제 [실측·추론]

### 6.1 원의 바깥 링이 옆 칸을 침범한다 [실측]

목업의 링은 `.center-field::after`로 그려지고 폭이 `min(675px, 108%)`다. 열보다 넓다.

| 폭 | 중앙 칸 | 링 | 좌우 침범 | 큐까지 간격 | 결과 |
|---|---|---|---|---|---|
| 1440 | 576 | 621 | 23px | 30px | 통과 |
| 1280 | 532 | 574 | 21px | 16px | **5px 겹침** |
| 1100 | 509 | 550 | 20px | 12px | **8px 겹침** |
| 1024 | 433 | 468 | 17px | 12px | **5px 겹침** |

1280 아래에서 점선 링이 큐 카드 밑으로 들어간다. §7.1의 격자로 그리면 링은 코어 열의
크기가 되고(414 + 좌우 20 = 454) 큐까지 24px이 남는다 — 침범 0 [실측]. 링을 쓸 거라면
**중앙 칸 안에 가두고**
(`inset` 기반, 칸을 넘지 않게), 링 자체를 `overflow: hidden`인 컨테이너에 넣거나 폭을
`min(100%, …)`로 잠근다. 우리 자에서는 링을 한 겹만 쓰거나(헤어라인 하나) 아예 빼는
쪽이 맞다 — 세 겹의 동심원은 §11.38이 말하는 "장식"에 가깝다.

### 6.2 몰입의 디밍은 포커스를 지우지 않는다 [추론]

`body.immersive`가 주변을 `opacity:.10; filter:blur(1px)`로 감쇄하는데, 그 요소들은
여전히 DOM에 있고 여전히 탭 순서에 있다. 키보드 사용자는 **보이지 않는 컨트롤로
탭이 들어간다.** 감쇄한 영역에 `inert`를 걸어야 한다. 이건 지금의 어두운 오버레이
방식에는 없던 문제다(오버레이가 `position: fixed`로 위를 덮으므로).

### 6.3 좁은 폭이 없다 [실측]

목업은 `min-width: 1024px`에 `body{overflow:hidden}`으로 좁은 폭을 아예 포기한다.
이 앱은 Pixel 5까지 검사한다(`playwright.config.ts`의 세 프로젝트). 그러므로 좁은 폭
설계는 목업 밖에서 새로 정해야 한다 — §7.2.

### 6.4 `Space` 단축키 [추론]

목업이 새로 들인다. 지금 `FocusPage`의 키 처리는 `Escape` 하나뿐이다. 도입한다면:
입력/선택 요소에 포커스가 있을 때, 그리고 다이얼로그(picker·설정·공백·메모)가 열려
있을 때는 발동하지 않아야 한다. 목업 스크립트는 앞의 절반만 막는다.

### 6.5 리본의 숫자는 어디로 가는가 [추론]

바닥 리본의 세 숫자는 지금 오른쪽 열의 `오늘의 세션 요약`과 같은 값이다. 리본이
전폭이 되면 눈에 더 들어오므로, **누르면 기록 뷰로** 가는 것이 자연스럽다. 그러면
머리의 `기록` 버튼과 길이 둘이 된다 — 둘 다 두되 리본 쪽을 주 경로로 본다.

---

## 7. 격자와 치수

### 7.1 데스크톱 (≥1024)

```
.focus-page-v2                      height:100%; grid-template-rows: auto minmax(0,1fr) auto
 ├ .focus-page-header               auto            제목 | 모드 토글 | 액션
 ├ .focus-field                     minmax(0,1fr)   grid-template-columns: minmax(0,1fr) auto minmax(0,1fr)
 │   ├ (왼쪽: 비움 — 테두리 없음)
 │   ├ .focus-core-column           auto            앵커 + 원 + 원 밖 액션
 │   └ .focus-queue                 max-width:380   justify-self: start. 헤더 + 스크롤러
 └ .focus-ribbon                    auto            3 수치 + 상태
```

- **원의 지름**: `clamp(320px, min(46vh, 34vw), 460px)`. 목업의 `min-width:390px`은
  1024×720에서 세로를 넘기므로 쓰지 않는다. 하한을 320으로 두면 시계(66–96px)와 주
  버튼(52px)이 들어간다.
- **바깥 두 열은 같은 몫이어야 한다.** `1fr · auto · 380`으로는 원이 가운데에 서지
  않는다 — 왼쪽 `1fr`이 남는 전부를 먹어 원을 오른쪽으로 민다. 세 열을
  `1fr · auto · 1fr`로 두고 큐를 세 번째 열 안에서 `max-width: 380px;
  justify-self: start`로 잡아야 두 바깥 열이 같아지고 원이 정중앙에 선다.
  1440에서 재보면 칸의 중심과 원의 중심이 둘 다 745다 [실측].
- **왼쪽**: 비운다. 아무 테두리도 그리지 않는다.
- **스크롤 모델은 바꾸지 않는다.** `main:has(.focus-page-v2)`의 `height:100dvh` +
  큐만 `overflow-y:auto` + `overscroll-behavior: contain` — `FOCUS_LAYOUT_DESIGN.md`
  §3의 네 교훈과 `OverlayScrollbar`의 요구가 그대로 유효하다.
- **스테이지 액자는 그리지 않는다.** 목업의 `28px` 반투명 액자는 반전이고, 그 액자가
  하던 일(창 가장자리에서 내용을 떼어놓기)은 페이지 패딩이 이미 한다.

### 7.2 좁은 폭 (<1024) [추론 — 목업에 없음]

한 열로 접고 **원은 유지한다.** 원이 이 화면의 정체성이므로 좁은 폭에서 사각형으로
되돌리면 두 개의 디자인이 된다(직전 평가 ⑤가 지적한 그 문제를 되풀이하는 것).

```
제목 · 모드 토글          (토글은 전폭 2분할로)
앵커
원                        지름 clamp(240px, 62vw, 340px)
원 밖 액션
리본                      3열 유지 (숫자 셋은 좁아도 셋이다)
큐                        아래로. 페이지가 흐른다 — 이 폭에서는 붙잡을 격자가 없다
```

경계는 **1024**를 쓴다. 지금 24-focus.css가 쓰는 901이 아니라 1024인 이유: 원(320) +
간격 + 큐(320) + 왼쪽 대칭 여백(320) + 레일(50) + 패딩 = 1010이 실제 하한이고,
`surfaceHierarchy.spec.ts`가 이미 1024를 "세 열이 존재하는 폭"으로 쓴다.

### 7.3 색과 표면

| 요소 | 값 | 근거 |
|---|---|---|
| 페이지 | `--bg-surface` | 지금과 같다. 레일 < 캔버스 순서 유지 (`surfaceHierarchy`) |
| 원 | `--bg-surface-muted` + 1px `--border-subtle`, 그림자 없음 | §14의 카드 관용구를 원에 그대로 |
| 앵커 알약 | `--bg-surface` + 헤어라인 + `--radius-pill` | I1이 남긴 형태 |
| 큐 행 | 면 없음. 헤어라인 구분선 | 카드 셋을 세로로 쌓으면 상자 액자다(§14의 판단) |
| 주 버튼 | `--color-primary`, **다크에서 대비 4.5 이상이 되게 조정** | 지금 3.65로 AA 미달(직전 평가 ②) |
| 모드 토글 | 트랙 `--surface-2`, 선택 `--bg-surface` + 헤어라인 | 파랑을 CTA에 양보 |
| 리본 | 위 헤어라인 하나, 면 없음 | §11.2 |

---

## 8. 결정할 것

| # | 질문 | 선택지 | 추천 |
|---|---|---|---|
| D1 | 왼쪽 열 | A 빈 여백 · B 2열 · C 다른 것 | **A** (§3) |
| D2 | "노트는 빼고"의 범위 | 메인에서만 제거(다이얼로그 유지) · 기능 삭제 | **메인에서만** (§3.1) |
| D3 | 동심원 링 | 없음 · 헤어라인 한 겹 · 목업대로 세 겹 | **한 겹** (§6.1) |
| D4 | `Space` 단축키 | 도입 · 안 함 | 도입 + 가드 (§6.4) |
| D5 | 기록 진입 | 리본 클릭 · 머리 버튼 · 둘 다 | **둘 다** (§6.5) |
| D6 | 기록 뷰 | 이번 범위 · 다음 | 이번에 정렬만 고친다 (직전 평가 ④는 한 줄 수정) |
| D7 | 자를 다시 여나 | 배치만 채택 · 집중 탭만 예외 · 자 전면 개정 | **배치만** (§2.1) |

---

## 9. 단계

각 단계는 그 자체로 초록이고, 앞 단계를 되돌리지 않는다.

| Phase | 무엇 | 닿는 곳 |
|---|---|---|
| **0** | 직전 평가의 ①②④ 선수정 — 몰입 토큰 누락, 다크 주 버튼 대비, 기록 뷰 정렬 | `24-focus.css` |
| **1** | 격자 전환: 3열(빈 왼쪽) + 바닥 리본. 요약을 오른쪽 열에서 리본으로 이동. 노트 제거 | `24-focus.css` · `FocusPage.tsx` |
| **2** | 코어: 원 + 앵커. 상태 여섯을 §4 표대로 원 안/밖으로 배분. `종료`의 danger 해제 | 〃 |
| **3** | 몰입을 디밍으로 교체 + `inert`. 어두운 오버레이와 그 토큰 블록 삭제 | 〃 |
| **4** | 큐를 타임라인 척추로. `0s` 열은 값이 있을 때만 | 〃 |
| **5** | 좁은 폭(§7.2) · `Space` · 리본→기록 | 〃 |

Phase 0을 먼저 두는 이유: ①은 지금 사용자가 겪는 회귀이고, 이 재설계가 그것을
구조적으로 없애기는 하지만 Phase 3까지 가야 없어진다. 그 사이를 버려두지 않는다.

---

## 10. 검증

- `npm test` · `npm run typecheck` · `npm run build` (api 번들 무변경 확인)
- `e2e/focusTimerV2.spec.ts` · `focusDetail.spec.ts` · `globalFocusBar.spec.ts` —
  버튼 이름과 역할로 찾으므로 배치가 바뀌어도 통과해야 한다. 통과하지 않으면 그것은
  **접근 가능한 이름이 바뀌었다**는 뜻이고, 스펙이 아니라 마크업을 본다.
- `e2e/radiusScale.spec.ts` — 원을 허용 목록에 이유와 함께 추가(§2.2). 그 외에
  새 예외가 생기면 그건 자를 넘은 것이다.
- `e2e/surfaceHierarchy.spec.ts` — 색 있는 그림자 0, 레일 < 캔버스 유지.
- `src/styles/scale.test.ts` — 위반 0 유지(파일별 천장은 비어 있다).
- `e2e/overlayScrollbar.spec.ts` — 큐 스크롤러가 유일한 내부 스크롤러라는 사실 유지.
- 대비 실측: 라이트/다크 × 유휴/실행/몰입에서 시계·주 버튼·앵커·큐 제목. 주 버튼은
  두 테마 모두 **4.5 이상**(지금 다크 3.65).
- 링 침범 실측: 1440·1280·1100·1024에서 링이 중앙 칸을 넘지 않는다(§6.1 표를 다시 잰다).
