# TickTick 역설계 #12 — Modal / Dialog

대상: TickTick Web (ticktick.com/webapp), 사용자 실제 계정
분석 컴포넌트: **모달 다이얼로그(`.popup` + `.popup-cover`)** — 측정 대상은 `목록 추가` 다이얼로그
작성일: 2026-08-20

Component 01~11에서 다룬 내용은 다시 분석하지 않고, 비교가 필요한 지점에서만 인용한다.

**대상 선정 이유**: Component 11에서 "떠 있는 레이어는 별도 규칙 세트를 쓴다"를 확인했다. 모달은 그 규칙을 검증·확장할 두 번째 표본이고, 우리 앱의 모달 4벌 난립(`DESIGN_ELEMENT_INVENTORY.md` §2.10.6)에 기준을 준다.

## 0. 측정 조건 · 안전 조치 · 복구

| 항목 | 값 |
|---|---|
| viewport | **1387 × 713 CSS px** (dpr 2) |
| 테마 / 로케일 | dark / ko |
| 측정 대상 | `리스트` 섹션 헤더의 `+` → **목록 추가 다이얼로그** |
| 측정 방법 | `getBoundingClientRect()` · `getComputedStyle()` · 실제 마우스 hover · 실제 클릭 |

### 0.1 안전 조치와 결과

이 다이얼로그는 **확인을 누르면 리스트가 생성된다.** 따라서:

- 폼에 **아무 값도 입력하지 않았다.**
- `추가` 버튼을 누르지 않았다(hover만 했다).
- **`취소`로 닫았다.**

**확인 결과: 아무것도 생성되지 않았다.** 닫은 뒤 `#project-ul`의 항목 수가 그대로였고(리스트 행 8개, `ZZ` 항목 4개), `.popup-cover`는 `display: none`으로 되돌아갔다.

### 0.2 복구 상태

| 항목 | 상태 |
|---|---|
| 모달 | 닫힘 (`popup` 제거, `popup-cover` `display: none`) |
| viewport | 1387 × 713 |
| Rail / 사이드바 | 50 / 240 (`leftListWidth = "240"`, `isLeftListHide = "false"`) |
| 화면 | 기본함(`#p/inbox/kanban`) |
| Component 04 테스트 데이터 | `ZZ Folder` + 리스트 3개 유지 |

**판정 규칙**: 수치는 전부 실측. 해석은 `[Inference]`로 분리. 못 잰 것은 §7에 적는다.

---

## 1. 구조

```
DIV.popup-cover                                   position: fixed
│     1387 × 713 (뷰포트 전체)   z-index: 1050
│     background: rgba(0,0,0,0)  ← 딤 없음
│     닫히면 display: none
│
└ DIV.popup.fast-transition.project-modal-detail-view.pb-0
  │     .projectModalDetailView_a_rsk .editModal_2nsos .z-[1] .!outline-none
  │     743 × 425 (콘텐츠 741 × 423 + 보더 1)
  │     bg rgb(28,28,28) · border 1px rgba(255,255,255,0.1) · radius 16
  │     box-shadow rgba(0,0,0,0.2) 0 16px 40px
  │     display: inline-flex · overflow: hidden · z-index: 1
  │     인라인 style="max-height: unset; max-width: unset; overflow: hidden;"
  │
  ├ DIV.popup-header                              741 × 56 @323,109
  │     position: absolute · padding 0 20px · z-index 10 · 배경·보더 없음
  │     ├ H1.popup-title.primary-text.text-def    (내용 비어 있음)
  │     └ DIV.popup-close.close                   18 × 18 @1026,128
  │           색 rgb(71,114,250) · 아이콘 #close
  │
  └ DIV.popup-body                                741 × 423
      └ DIV.flex.overflow-hidden
          ├ DIV.flex-auto.flex.flex-col           **381** × 423   ← 폼 컬럼
          │   ├ H1.mx-auto.font-bold  "목록 추가"   68 × 22 @479,125   16px / 700
          │   ├ DIV.project-detail-panel.flex-auto  333 × 276 @347,172
          │   │   └ DIV.row.relative × 6
          │   └ DIV.flex-none.flex.justify-end     333 × 30 @347,486
          │       ├ BUTTON "취소"                   76 × 30 @518
          │       └ BUTTON "추가"                   76 × 30 @604
          └ DIV.rounded-select-trigger.bg-primary-5  **360** × 423 @704   ← 일러스트 컬럼
              └ IMG                                324 × 321
```

**[Observed] 2열 모달이다.** 왼쪽 381px가 폼, 오른쪽 360px가 일러스트다.

**[Observed] 제목이 두 곳에 있다.** `.popup-header` 안의 `H1.popup-title`은 **비어 있고**, 실제로 보이는 제목은 body 안의 `H1.mx-auto.font-bold`다. 후자는 `mx-auto`로 **가운데 정렬**된다.

**[Observed] 헤더가 `position: absolute`다.** 즉 body 위에 겹쳐 떠 있고, 닫기 버튼만 담는다.

---

## 2. Surface — 메뉴와 또 다르다

Component 11의 메뉴와 나란히 놓으면 **떠 있는 레이어끼리도 규칙이 갈린다.**

| 항목 | **메뉴 (C11)** | **모달 (C12)** | 같은가 |
|---|---|---|---|
| 배경 | `rgb(36,36,36)` (Rail 색) | **`rgb(28,28,28)`** (화면 배경색) | **다름** |
| 보더 | **1px 실색** `rgb(54,54,54)` | **1px 알파** `rgba(255,255,255,0.1)` | **다름** |
| radius | **12** | **16** | **다름** |
| **box-shadow** | `rgba(0,0,0,0.2) 0 16px 40px` | **완전히 동일** | **같음** |
| z-index | 1050 (자기 자신) | cover 1050 / 패널 1 | 실질 동일 |
| backdrop | **없음** | **cover는 있으나 투명** | 둘 다 딤 없음 |
| transition | 0s | 0s | 같음 |

**[Observed] 떠 있는 두 레이어가 공유하는 것은 그림자 하나뿐이다.** 배경·보더·radius가 전부 다르다.

**[Observed] 모달에 딤(어두운 배경)이 없다.** `.popup-cover`는 뷰포트를 덮지만 배경이 `rgba(0,0,0,0)`이다.

**[Inference]** cover는 **바깥 클릭을 잡는 용도**로만 존재하고 시각적 역할이 없다. 딤을 쓰지 않는 것은 뒤 화면을 계속 보여주려는 선택으로 보이지만, 모달의 경계가 1px 알파 보더뿐이라 **배경과의 분리가 그림자에만 의존한다.**

**[Observed] radius가 이 시리즈에서 네 번째 값이다** — 섹션 헤더 6 · 행 10 · 메뉴 12 · **모달 16**.

---

## 3. Geometry

### 3.1 패널

| 항목 | 값 |
|---|---|
| 외곽 | **743 × 425** |
| 콘텐츠 박스 | 741 × 423 |
| 폼 컬럼 | **381** |
| 일러스트 컬럼 | **360** |
| 헤더 높이 | **56** (absolute) |

**폭은 뷰포트에 반응한다.** Component 04에서 viewport 763일 때 같은 모달이 **390** 폭이었고, 지금 viewport 1387에서 **743**이다. 인라인 style에 `max-width: unset`만 있고 computed `max-width: none`이라 **규칙 자체는 확인하지 못했다**(뷰포트를 제어할 수 없다).

### 3.2 폼 컬럼 내부

| 요소 | x | y | w | h |
|---|---|---|---|---|
| 제목 H1 | 479 | 125 | 68 | 22 |
| 폼 패널 | **347** | 172 | **333** | 276 |
| 행 1 (이름 입력) | 347 | 172 | 333 | **36** |
| 행 2 (목록 색상) | 347 | 234 | 333 | **18** |
| 행 3 (보기 유형) | 347 | 272 | 333 | **36** |
| 행 4 (폴더) | 347 | 328 | 333 | **32** |
| 행 5 (목록 유형) | 347 | 372 | 333 | **32** |
| 행 6 (스마트 목록) | 347 | 416 | 333 | **32** |
| 버튼 줄 | 347 | 486 | 333 | 30 |

**행 사이 간격: 26 · 20 · 20 · 12 · 12** — **균일하지 않다.**

**[Observed] 행 높이도 36 / 18 / 36 / 32 / 32 / 32로 제각각이다.** 컨트롤 종류(입력 / 색상 칩 / 세그먼트 / 드롭다운)에 따라 다르다.

**[Inference]** 행마다 컨트롤이 달라서 높이가 다른 것은 자연스럽지만, **간격까지 5종으로 갈리는 것은 규칙이라기보다 개별 조정으로 보인다.** Component 02(섹션 패딩 불균일)·Component 04(섹션 padding 위치별 상이)에서 본 패턴의 반복이다.

폼 컬럼의 좌우 인셋: 381 컬럼 안에서 폼이 x=347부터 333 폭 → **좌 24 / 우 24**.

---

## 4. Typography

| 항목 | 값 |
|---|---|
| 제목 (`H1.mx-auto.font-bold`) | **16px / 700**, 가운데 정렬 |
| 폼 라벨 | **13px / `rgba(255,255,255,0.8)`**, 라벨 열 폭 **120** |
| 버튼 텍스트 | **14px / 400** |
| 유효성 메시지(빈 상태) | 11px, `rgb(224,49,49)` (danger red) |

**[Observed] 라벨 13px / 80% 알파는 Component 11의 메뉴 항목과 정확히 같은 조합이다.**

**[Inference]** 13px + 80%가 **떠 있는 레이어 안의 보조 텍스트** 규격으로 쓰이는 것으로 보인다. 표본 2개라 규칙으로 확정하지 않는다.

**[Observed] 위험 색 `rgb(224,49,49)`이 여기서 처음 실제로 쓰인다.** Component 06에서 우선순위 high 토큰으로 확인했던 값과 같다(`--color-priority-high`). 반면 Component 11의 메뉴에서는 삭제 항목에 위험 색이 없었다.

---

## 5. Buttons

| 항목 | **취소 (secondary)** | **추가 (primary)** |
|---|---|---|
| 크기 | **76 × 30** | **76 × 30** |
| radius | **8** | **8** |
| padding | `0 16px` | `0 16px` |
| font | 14px / 400 | 14px / 400 |
| background | **투명** | **`rgb(71,114,250)`** (accent) |
| border | **1px `rgba(255,255,255,0.1)`** | 1px 투명 |
| color | **`rgba(255,255,255,0.6)`** | `rgb(255,255,255)` |
| 간격 | 취소 우측 594 → 추가 좌측 604 = **10** | |
| 정렬 | `justify-end` — 우측 정렬, 폼 우측 끝(680)에 맞음 | |
| **transition (선언)** | `color, background-color, border-color … / 0.2s / cubic-bezier(0.4,0,1,1)` | 동일 |
| **hover 실측** | **변화 없음** | **변화 없음** |

**[Observed] 두 버튼 모두 200ms 색 전환을 선언해 두고, 실제 hover에서는 아무 값도 변하지 않는다.** 배경·색·보더를 각각 확인했다.

**[Inference]** Component 10의 퀵애드(투명 1px 보더를 예약해 두고 어떤 상태에서도 색을 넣지 않음)와 같은 패턴이다. **"선언은 있는데 상태 변화가 없다"가 이 제품에서 반복된다.** 다만 `:active`나 disabled 상태에서 쓰일 가능성은 배제할 수 없다(미측정).

**[Observed] primary 버튼과 닫기(X) 버튼이 같은 accent 색을 쓴다.**

---

## 6. Fidelity Specification

```
MODAL / DIALOG

Cover
  position            : fixed, 뷰포트 전체
  z-index             : 1050
  background          : 투명 — 딤 없음
  역할                : 바깥 클릭 수집만
  닫힘 시             : display: none

Panel
  background          : 화면 배경색과 동일 (불투명)
  border              : 1px, 전경색 10% 알파
  radius              : 16px
  box-shadow          : 0 16px 40px rgba(0,0,0,0.2)   ← 메뉴와 동일한 값
  overflow            : hidden
  display             : inline-flex
  transition          : 없음
  width               : 뷰포트에 반응 (규칙 미확정)

Header
  height              : 56
  position            : absolute (body 위에 겹침)
  padding             : 0 20px
  배경 / 보더         : 없음
  내용                : 닫기 버튼만 (제목은 body에 별도로 있다)
  close button        : 18 × 18, accent 색, 우측 인셋 20

Body — 2열
  좌: 폼 컬럼          : 폭 381, 좌우 인셋 24
  우: 일러스트 컬럼    : 폭 360, 배경 accent 5% 알파, 이미지 324 × 321

Title
  16px / 700, 가운데 정렬 (mx-auto)

Form row
  라벨                : 13px / 전경색 80% 알파, 라벨 열 폭 120
  행 높이             : 컨트롤에 따라 18 ~ 36
  행 간격             : 균일하지 않음 (관찰값 26 / 20 / 20 / 12 / 12)
  유효성 메시지        : 11px, danger red

Buttons
  크기                : 76 × 30, radius 8, padding 0 16
  font                : 14px / 400
  secondary           : 배경 투명 + 1px 전경색 10% 보더 + 전경색 60% 텍스트
  primary             : accent 배경 + 흰색 텍스트, 보더 투명
  배치                : 우측 정렬, 두 버튼 간격 10
  hover               : 변화 없음 (전환은 선언돼 있으나 값이 바뀌지 않는다)
```

---

## 7. 이 분석이 확인하지 않은 것

- **폭 결정 규칙** — 뷰포트 763에서 390, 1387에서 743. 백분율인지 media query인지 확정 못 함(뷰포트 제어 불가).
- **Escape / cover 클릭 dismissal** — `취소` 버튼으로만 닫았다. cover가 투명하고 클릭 수집용으로 보이지만 실제 동작은 미확인.
- **버튼 `:active` / disabled 상태** — 누르면 리스트가 생성되므로 시도하지 않았다.
- **입력 후 상태** — 유효성 메시지가 실제로 뜨는 모습, 이름 입력 시 primary 버튼 활성화 여부 등 전부 미측정.
- **다른 모달과의 동일성** — 확인 다이얼로그, 설정 모달 등이 같은 `.popup` 프리미티브를 쓰는지 확인하지 않았다. Component 04에서 본 `.popup-body`가 같은 클래스이므로 가능성은 높다.
- **모달 진입/퇴장 애니메이션** — 클래스에 `fast-transition`이 있으나 computed transition은 0s였다. 열릴 때 별도 애니메이션이 있는지는 확인 못 했다.
- **포커스 트랩 / 초기 포커스** — 미측정.
- **라이트 테마**, **원본 CSS 규칙**(CORS) — 01~11과 동일.

---

## 8. Component 01~12 갱신

### 8.1 "떠 있는 레이어" 규칙 — 표본 2개로 다시 좁힌다

Component 11에서 "떠 있는 레이어 = 불투명 배경 + 실색 보더 + 그림자"로 정리했는데, 모달이 그중 **두 가지를 깬다.**

| 속성 | 메뉴 | 모달 | 공통인가 |
|---|---|---|---|
| 불투명 배경 | ✔ `rgb(36,36,36)` | ✔ `rgb(28,28,28)` | **공통 (값은 다름)** |
| 실색 보더 | ✔ `rgb(54,54,54)` | **✘ 알파 10%** | **공통 아님** |
| 그림자 | ✔ | ✔ **같은 값** | **공통** |
| radius | 12 | 16 | 공통 아님 |
| 딤 backdrop | 없음 | 없음(투명 cover) | **공통** |

**[Observed] 떠 있는 레이어에서 확실히 공통인 것은 두 가지다 — 불투명 배경, 그리고 `0 16px 40px rgba(0,0,0,0.2)` 그림자.** 딤을 쓰지 않는 것도 둘 다 같다.

### 8.2 Conflict / Revision Candidate (R-21)

| # | 기존 서술 | 12의 관찰 | 성격 |
|---|---|---|---|
| **R-21** | **C11 §8.1** — "떠 있는 레이어는 불투명 배경 + **실색 보더** + 그림자를 쓴다" | 모달 보더는 **알파 10%**다. 실색 보더는 메뉴만의 처리였다 | **정정.** 떠 있는 레이어의 공통 규칙에서 "실색 보더"를 빼야 한다 |

### 8.3 반복되는 패턴 — "선언은 있는데 변화가 없다"

| 컴포넌트 | 관찰 |
|---|---|
| C10 퀵애드 | 1px 투명 보더를 예약해 두고 hover·focus 어디서도 색이 들어가지 않음 |
| C11 메뉴 | `role="menuitem"` + `tabindex="-1"`이 있으나 Arrow 키 동작 없음 / `deleteItem` 클래스가 있으나 위험 색 없음 |
| **C12 모달** | **버튼에 200ms 색 전환이 선언돼 있으나 hover에서 아무 값도 변하지 않음** |

**[Inference]** 마크업·클래스·전환이 먼저 들어가고 그에 대응하는 시각/동작 구현이 빠진 지점이 여러 곳에서 반복된다. **우리가 TickTick을 베낄 때 "클래스가 있으니 기능도 있겠지"라고 가정하면 안 된다는 실무적 함의**가 있다.

### 8.4 radius 스케일 — 4단

| 값 | 쓰이는 곳 |
|---|---|
| **6** | 섹션 헤더 트리거(C03), 아바타(C08) |
| **8** | 메뉴 항목 hover(C11), 모달 버튼(C12), 헤더 아이콘 hover 배경(C09) |
| **10** | 사이드바 행(C01·C04), 태스크 행 배경(C06), 퀵애드 입력(C10) |
| **12** | 메뉴 패널(C11) |
| **16** | 모달 패널(C12) |

**[Inference]** 대략 **"작은 컨트롤 6~8 → 행 10 → 떠 있는 패널 12~16"**의 경향이 보인다. 다만 헤더 트리거가 6이고 같은 크기의 행이 10인 등 예외가 있어 규칙으로 확정하지 않는다.

---

## Appendix A — 우리 앱에 적용할 때 다르게 할 것 (관찰이 아닌 제안)

**아래는 TickTick 동작이 아니다.**

1. **딤 backdrop을 넣는다.** TickTick 모달은 딤이 없어 뒤 화면과의 분리가 그림자에만 의존한다. 우리는 전경색이 아니라 검정 40~50% 정도의 딤을 권한다 — 특히 다크 테마에서 모달 배경(28)과 화면 배경(28)이 **같은 색**이면 경계가 1px 보더뿐이다.
2. **버튼 hover를 실제로 구현한다.** 200ms 전환만 선언하고 값이 안 바뀌는 상태는 그대로 베끼면 안 된다.
3. **폼 행 간격을 규칙화한다.** 26/20/20/12/12는 규칙이 아니다. 우리는 한 값(예: 16)으로 두고 그룹 경계에서만 키운다.
4. **제목을 한 곳에만 둔다.** TickTick은 빈 `.popup-title`과 실제 `H1`이 따로 있다. 우리는 헤더의 제목 하나로 통일하고 `aria-labelledby`로 연결한다.
5. **떠 있는 레이어의 토큰을 하나로 정한다.** 메뉴 12 / 모달 16, 실색 보더 / 알파 보더처럼 갈라진 것을 우리가 따라갈 이유는 없다. **`--radius-lg` + `--border-subtle` + `--shadow-panel` 조합 하나**로 통일한다.
6. **그림자 값은 채택할 만하다.** `0 16px 40px rgba(0,0,0,0.2)`는 두 레이어에서 일관되게 쓰였고, 우리 `--shadow-lg`(`0 8px 24px rgba(0,0,0,0.13)`)보다 부드럽다. 다크에서 특히 자연스럽다.
7. **일러스트 컬럼은 선택 사항이다.** 폭의 절반을 이미지에 쓰는 것은 온보딩 성격이 강한 다이얼로그에만 맞는다. 우리 앱의 리스트 생성 모달은 폼만으로 충분하다.

---

## 9. 이 문서가 남긴 상태

| 항목 | 상태 |
|---|---|
| Component 01~11 문서 | **수정하지 않음.** 정정 후보는 §8.2에만 기록 |
| 우리 앱 코드 | **수정하지 않음** |
| TickTick 데이터 | **변경 없음.** 폼 미입력, `추가` 미클릭, `취소`로 닫음 — 생성된 항목 0개 |
| 모달 | 닫힘 (`popup-cover`가 `display: none`) |
| 화면 / 셸 | 기본함 · Rail 50 · 사이드바 240 · viewport 1387×713 |
| Component 04 테스트 데이터 | `ZZ Folder` + 리스트 3개 유지 |
