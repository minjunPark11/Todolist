# 타임라인 — 날짜 없는 작업을 끌어다 놓기 (`Arrange tasks`)

> 참조 앱의 타임라인 화면 캡처 2장(같은 순간의 전체/확대)에서 시작한다.
> 근거 등급: `[관찰]` 캡처에서 본 것 · `[실측]` 우리 코드/실행 중인 앱에서 잰 것 ·
> `[인용]` 다른 문서에서 가져온 것 · `[추론]` 위 셋으로부터 지은 것.

---

## 0. 이 문서가 무엇을 근거로 삼는가

캡처는 **정지 화면 두 장**이다. 움직임은 하나도 보지 못했다 — 칩을 끌어다 놓는 순간도,
놓은 뒤에 막대가 어떻게 생기는지도, 패널이 어떻게 열리고 닫히는지도.

그래서 이 문서는 **패널이 있다는 사실**과 **그 안에 무엇이 들어 있는지**만 [관찰]로
쓰고, "끌어다 놓으면 일정이 잡힌다"는 것부터가 이미 [추론]이다. 사용자의 요청
("이런 식으로 할 수 있게 하자")이 그 추론의 근거다.

---

## 1. 캡처에서 읽은 것 [관찰]

### 1.1 `Arrange tasks` 패널

```
Arrange tasks                    No Date ⌄
┌────────────────────────────────────────┐
│ Get the details            (파랑)      │
│ Start on the project       (올리브)    │
│ Give the presentation      (보라)      │
│ Make a plan                (금색)      │
│ Build the powerpoint deck  (보라)      │
│ Produce the output         (진홍)      │
│ Send out the recording     (보라)      │
└────────────────────────────────────────┘
```

- 제목은 굵고, 같은 줄 오른쪽 끝에 **`No Date ⌄`**
- 칩은 패널 폭을 꽉 채우고, 모서리가 둥글고, 흰 글자가 왼쪽 정렬이다
- **색이 칩마다 다르다.** 무엇을 뜻하는지는 캡처로 알 수 없다

### 1.2 패널은 격자를 **덮는다**

날짜축의 `27 28 29 30`이 패널 **위쪽에 흐린 색으로 남아 있다.** 축은 잘리지 않았고
패널이 그 아래 격자를 가린 것이다. `20`–`26`은 밝고 `27` 이후는 흐리다.

### 1.3 나머지

- 머리 오른쪽: `Today` · `Week ⌄` 알약 버튼 둘
- 패널 **바깥** 오른쪽 가장자리에 아이콘 둘(달력꼴, 과녁꼴) — 세로로 쌓여 있다
- 화면 아래에 색 그라데이션 띠와 둥근 손잡이 하나
- 막대 하나가 인라인 편집 중이다: `|Wrap up the project`, 캐럿이 보인다

---

## 2. 우리가 이미 가진 것 [실측]

### 2.1 데이터는 있다. 자리와 손잡이가 없다

`TaskGanttView`가 이미 정확히 같은 가름을 한다(`:68`).

```ts
const { onWindow, undated } = useMemo(() => { … spanForItem(item) ? drawn : tray … });
```

`undated`가 곧 `Arrange tasks`의 내용물이다. 참조와 우리의 차이는 데이터가 아니라
**그것을 어디에 어떻게 두는가**뿐이다.

| | 참조 | 우리 |
|---|---|---|
| 자리 | 오른쪽 세로 패널 | 타임라인 **아래** 가로 목록(`.ff-timeline-tray`) |
| 생김새 | 색 칩 | 맨 텍스트 버튼 |
| 하는 일 | (추론) 끌어다 놓아 일정을 준다 | **열어서 날짜를 타이핑하는 것뿐** |

### 2.2 드래그 기계는 이미 있다 — 다만 자기 행 안에서만

- 막대가 HTML5 `draggable`, `DRAG_MIME = "text/timeline"`에 `DragKind` 하나를 싣는다
- 드롭을 받는 것은 `.ff-timeline-cell`인데, **그 칸들은 그 항목 자신의 행에 있다**
- `SpanDrag`(`move` · `resizeStart` · `resizeEnd`)와 `patchForSpanDrag`가 쓰기를 소유한다
- `columnStartDate(window, index)`가 이미 있다 — 칸 번호를 날짜로 바꾸는 함수다

**설계의 핵심 문제가 여기다.** 트레이의 칩은 **행이 없다.** 기존 드롭 대상은 전부
"이 항목의 이 칸"인데, 칩이 필요한 것은 "이 **날짜**"다.

### 2.3 타임라인의 쓰기는 되돌릴 수 없다

```
TasksModule:1228   onUpdateTask={props.drawer.onUpdate}   →  planner.updateTask
```

행 메뉴 쪽에는 이런 주석이 있다 [인용]:

> Everything on it goes through `mutate`, so everything on it can be undone.

타임라인은 그 경로를 쓰지 않는다. **막대를 끌어 옮기면 되돌릴 수 없다.** 날짜가 없던
작업에 날짜를 찍는 드롭은 그중에서도 가장 되돌리고 싶은 것이다.

---

## 3. 결정

### 3.1 패널은 격자를 **좁힌다**, 덮지 않는다

[관찰]은 덮는 쪽이지만 따르지 않는다. 참조의 타임라인은 가로로 스크롤되므로 가려진
열도 밀어서 볼 수 있다. 우리 격자는 **가로 스크롤이 없다**(D3이 그렇게 정했고, 그래서
`Today` 버튼이 한 번에 돌아오는 유일한 길이다) — 덮으면 가려진 열은 **닿을 수 없게 된다.**

같은 그림을 만들려고 우리 격자에서 열을 지워버리는 것은 디자인을 베끼다 기능을 잃는
것이다. 패널은 오른쪽 열로 들어가고 격자는 그만큼 좁아진다.

### 3.2 드롭이 쓰는 것은 `dueDate` 하나

`patchForSpanDrag`의 기존 철학은 **"없던 필드는 없는 채로"**다(`move`가 그렇게 한다).
날짜가 하나도 없는 작업을 어느 날에 놓는 것은 "이 날 한다"는 한 문장이고, 그 한 문장이
쓰는 필드는 `dueDate`다. `spanForItem`은 `dueDate` 하나로 하루짜리 막대를 만든다 —
놓자마자 칩이 있던 자리에서 사라지고 그 날에 막대로 나타난다.

`startDate`까지 함께 쓰지 않는 이유: 시작일은 **선언**이고(`resizeStart`가 그 필드를
쓰는 이유가 그것이다), 칩을 어느 날에 떨어뜨린 사람이 선언한 것은 하루뿐이다.

### 3.3 어느 날인가 — 칸의 첫날

`week` 줌에서 한 칸은 7일이고 `month`는 한 달이다. 놓인 칸이 곧 날짜가 되려면 규칙이
필요한데, 이미 있다 [인용]:

> Day and week columns are both identified by their first day. (`columnLabel`)

`columnStartDate(window, index)`가 그 값을 준다. 새 규칙을 짓지 않는다.

### 3.4 드롭은 되돌릴 수 있어야 한다

§2.3을 이 작업에서 고친다. 타임라인이 `TaskMutation {patch, undo, labelKey}` 경로를
쓰게 되고, **기존 막대 드래그도 함께 되돌릴 수 있게 된다.** 날짜를 지우는 것이
되돌리기의 내용이므로 §9.35대로 `undo`는 "반대 동사"가 아니라 **이전 상태**다.

### 3.5 드래그가 유일한 길이면 안 된다

칩은 지금도 **작업을 여는 버튼**이고 그대로 둔다. 마우스가 없거나 못 쓰는 사람에게
"끌어다 놓으세요"는 길이 아니다. 여는 것이 키보드의 길이고, 상세에서 날짜를 준다.

### 3.6 `No Date ⌄`는 만들지 않는다

우리 패널의 내용은 `spanForItem(item) === null`, 곧 **"날짜 없음"** 하나로 정해져 있다.
참조의 그 드롭다운도 기본값이 `No Date`다. 다른 선택지를 보지 못했으므로 **고를 것이
하나뿐인 선택기**를 만들게 된다 — §16.26 Gate 3이 금지하는 그것이다. 열린 질문으로 둔다.

### 3.7 칩 색은 이번에 하지 않는다

참조의 칩은 색이 다르고 그 출처를 모른다(리스트일 수도, 작업별 색일 수도, 태그일 수도).
우리 리스트는 색을 갖지만 행에서는 일부러 점을 찍지 않기로 한 전례가 있다
(`TaskRowContent`: "점은 한 낱말을 두 번 말하는 것"). 근거 없이 색부터 칠하면 그 결정과
어긋난다. 열린 질문.

---

## 4. 단계

| | 무엇 | 왜 이 순서인가 |
|---|---|---|
| 1 | 트레이 → 오른쪽 패널. 레이아웃과 이름(`Arrange tasks`)만, 드래그 없음 | 자리가 먼저다. 드래그는 놓을 곳이 있어야 만든다 |
| 2 | 도메인: `patchForTrayDrop(task, date)` + 시험 | 규칙이 컴포넌트보다 먼저다 |
| 3 | 칩을 끌어 격자에 놓기. 드롭 대상은 **날짜 레인** | 2가 있어야 3이 쓸 것이 생긴다 |
| 4 | 타임라인의 쓰기를 `mutate` 경로로. 드롭과 기존 막대 드래그가 되돌려진다 | 3까지 하면 되돌릴 것이 생긴다 |
| 5 | 빈 패널의 상태, 좁은 화면에서의 패널 | 나머지가 정해진 뒤 |

3단계의 "날짜 레인"이 이 설계에서 유일하게 새로 짓는 것이다: 항목의 행에 속하지 않고
**열에만 속하는** 드롭 대상. 격자 위에 깔린 투명한 열 하나씩으로, 트레이 드래그가
시작될 때만 살아난다 — 그러지 않으면 막대의 드롭과 서로를 가린다.

---

## 5. 열린 질문

| | 질문 | 언제 |
|---|---|---|
| Q1 | `No Date ⌄`의 다른 선택지는 무엇인가. 하나뿐이면 선택기가 아니다(§3.6) | 드롭다운을 연 캡처 |
| Q2 | 놓은 뒤 막대의 길이. §3.2는 하루로 정했지만 참조가 더 긴 막대를 만들 수도 있다 | 놓은 직후의 캡처 |
| Q3 | 칩 색의 출처 — 리스트인가, 작업별 색인가, 태그인가(§3.7) | 리스트가 보이는 캡처 |
| Q4 | 패널 바깥 오른쪽의 아이콘 둘. 사이드 패널 전환기로 보이나 다른 하나를 못 봤다 | 다른 패널을 연 캡처 |
| Q5 | 아래쪽 그라데이션 띠 — 줌인가 스크러버인가 미니맵인가 | 움직이는 장면 |
| Q6 | 막대의 인라인 편집(`|Wrap up the project`). 격자에서 새 작업을 만드는 것으로 보인다 | 별도 설계 |

---

## 6. 이 문서가 확인하지 않은 것

- **끌어다 놓는 동작 자체를 보지 못했다.** 패널이 있다는 것과 그 내용만 보았고,
  드래그는 사용자의 요청에서 온 [추론]이다
- 패널을 여닫는 방법. 항상 열려 있는지, `⋯`나 오른쪽 아이콘으로 켜는지
- 패널이 계정에 기억되는지, 화면마다 다른지
- 좁은 화면에서 패널이 어떻게 되는지

---

## 7. 1단계 구현 기록 (2026-09-02)

### 7.1 이름을 참조의 것으로 바꿨다

`No dates (3)` → **`Arrange tasks` + 개수**. 참조의 이름이 더 낫다: 이 패널은 무엇이
빠졌는지에 대한 **보고**가 아니라 처리해 나갈 **더미**다. 개수는 이름 **옆**에 붙였다 —
보드 기둥 머리에서 정한 그 규칙이다(`SCOPE_VIEW_OPTIONS_DESIGN.md` §13.6.4:
`Overdue 1`은 두 낱말이고 개수는 자기 칸이 아니다).

칩도 가로로 감기는 알약에서 **세로로 쌓인 전체폭 줄**이 됐다. 알약은 태그처럼 읽히고,
쌓인 줄은 처리할 목록처럼 읽힌다 — `Arrange tasks`가 말하는 것이 후자다.

### 7.2 레이아웃에서 두 번 걸렸다 [실측]

**(가) 패널이 220px 대신 429px로 부풀고 격자가 0px가 됐다.**

원인은 `min-width: auto`. 긴 제목이 `white-space: nowrap`이라 칩의 min-content가
**문자열 전체 폭**이 되고, 그것이 패널의 최소 폭으로 올라가 `flex-basis: 220px`를
이겼다. 말줄임은 **상자가 문자열보다 좁아질 수 있어야** 작동한다.

같은 함정에 대한 주석을 `.tgv-body`에는 이미 써 놓고 정작 패널과 칩에는 빠뜨렸다.

**(나) 1280에서 격자가 202px가 됐는데 라벨 열만 220px이다.**

[실측] 데스크톱 기본 1280에서 타임라인 영역은 **438px**다(상세 열이 예약되어 있다).
패널이 220을 가져가면 격자에 남는 202px은 자기 라벨 열보다 좁다 — 날짜가 설 자리가
없어진다.

그래서 **감게** 했다. 격자에 바닥(`min-width: min(100%, 420px)` = 라벨 220 + 열 일곱)을
주고 `.tgv-body`에 `flex-wrap: wrap`. 바닥을 못 맞추면 패널이 아래로 내려가 원래 자리로
돌아간다 — §4의 5단계가 다룰 일이었지만 기본 화면이 이미 그 경우라 당겨왔다.

| 창 | 격자 | 패널 |
|---|---|---|
| 1800 | 722px | 220px, **나란히** |
| 1280 | 438px | 220px, **아래로** |

**(다) `flex: 1 1 auto`도 함정이었다.** 격자의 기준 크기가 자기 콘텐츠 폭이라
1800에서도 줄을 통째로 차지하고 패널을 밀어냈다. `flex: 1 1 0`으로 바꿔 **남는 폭을
받게** 했다.

### 7.3 검증

- 유닛 **2,303개 통과** · `tsc` 통과
- 새 시험 5개(`taskGanttArrange.test.tsx`, 이 화면의 첫 시험이다): 패널이 격자가 그릴 수
  없는 것만 담는다 · 이름과 개수 · 담을 것이 없으면 패널도 없다 · 칩이 작업을 연다 ·
  격자와 패널이 같은 줄에 있다
- **`tsc`가 유닛이 통과시킨 것을 잡았다**: `projectItems`에 `today`가 빠져 있었다.
  릴리스 빌드가 test 파일까지 검사하므로 태그 push 때 터졌을 것이다
- 기하는 jsdom이 답하지 못해 실행 중인 앱에서 쟀다(§7.2의 숫자들)

---

## 8. 2단계 구현 기록 (2026-09-02)

### 8.1 지을 것이 거의 없었다

§4는 2단계를 "도메인 규칙 + 시험"으로 잡았는데, 규칙의 **양쪽 끝이 이미 있었다**.

| 필요한 것 | 이미 있던 것 |
|---|---|
| 열 번호 → 날짜 | `columnStartDate(window, index)` — 주석까지 *"where a bar dropped on it should begin"* |
| 마감일 쓰기 + 되돌리기 | `setTaskDueDate(task, date)` — `patch: { dueDate }`, `undo`는 **이전 상태**(§9.35) |

§3.2가 "`dueDate` 하나를 쓴다"로 정한 것이 이미 존재하는 뮤테이션과 정확히 같았다.
설계가 2단계를 과대평가했다.

### 8.2 그래서 실제로 지은 것은 둘 사이의 방어선이다

`patchForTrayDrop(task, window, columnIndex)`는 `patchForSpanDrag` 옆에 앉고 **같은 모양**을
낸다(`Partial<Task>`). 뮤테이션 모양으로 만들지 않은 이유: 이웃이 아직 그렇지 않고,
**되돌리기는 4단계의 일**이다. 그때 둘을 함께 옮기면 `board.ts`가 한 번에 일관돼진다.
지금 이것만 뮤테이션으로 만들면 파일 안에 두 관습이 생긴다.

지은 것은 규칙 하나와 방어선 둘이다.

- 열의 **첫날**. `columnLabel`이 이미 같은 규칙을 쓰므로 새로 짓지 않았다
- 창 밖의 열은 날짜가 없다 → 빈 패치
- **이미 그 날인 작업**을 그 날에 놓으면 빈 패치. 빈 패치도 `updatedAt`과 전송 한 줄을
  치른다 — resize 갈래가 지키는 그 방어선이다

### 8.3 검증

- 유닛 **2,303 + 6개 통과** · `tsc` 통과
- 시험 6개: 마감일만 쓴다 · 하루가 아닌 줌에서 열의 첫날 · 같은 날 드롭은 빈 패치 ·
  창 밖의 열도 빈 패치 · 없던 시작일은 없는 채로

---

## 9. 3단계 구현 기록 (2026-09-02)

### 9.1 열에만 속하는 드롭 대상

§4가 예고한 대로, 이 설계에서 새로 지은 것은 이것 하나다. 기존 드롭 대상은 전부 어떤
항목의 **자기 행에 있는 칸**이고, 칩은 행이 없다 — 칩이 말해야 하는 것은 **날짜**다.

`.ff-timeline-lanes`는 `.ff-timeline` 위에 절대 배치된 열 `N`개다. 연결선 오버레이가
이미 같은 상자를 기준으로 삼고 있어서 위치 기준은 새로 만들지 않았다. 왼쪽 시작점은
`calc(var(--timeline-label-width) + 8px)` — 라벨 열 더하기 행 자신의 간격, 곧 트랙이
시작되는 자리다.

[실측] 실행 중인 앱에서 레인과 열이 픽셀 단위로 맞는다: 레인 12개 / 열 12개,
`lane[0].x === col[0].x === 154`, `lane[11].right === col[11].right === 304`,
레인 높이 96 = 격자 높이 96.

### 9.2 칩이 공중에 있을 때만 존재한다

레인은 격자 전체를 덮으므로, 계속 띄워두면 모든 막대 위에 드롭 대상 시트가 깔린다 —
막대의 클릭과 자기 드래그를 전부 삼킨다. `dragstart`에 켜고 `dragend`에 끈다.

### 9.3 MIME을 따로 뒀다

막대는 `text/timeline`, 칩은 `text/timeline-tray`. 값 하나로 구분하지 않은 이유는
**한쪽만 아는 드롭 대상이 다른 쪽을 잘못 읽지 못하게** 하기 위해서다. 막대의 칸은
칩 드래그에서 아무것도 못 찾고, 레인은 막대 드래그에서 아무것도 못 찾는다.

### 9.4 실행 중인 앱이 찾아낸 결함

드롭에 성공하면 작업이 패널을 떠나므로 **칩이 언마운트되고 그 `onDragEnd`도 함께
사라진다.** `dragend`가 닿을 곳이 없어져서 [실측] **레인 12개가 격자 위에 그대로
남았다.**

유닛 시험은 이것을 못 잡았다 — 시험이 `dragEnd`를 직접 쏘고 있었기 때문이다. 실제
브라우저에서 끝까지 따라가 본 것이 잡았다.

고침: 드롭 처리기 자신이 `setDraggingChip(false)`를 한다. `onDragEnd`는 **취소된
드래그**를 계속 맡는다 — 그 경우엔 칩이 그대로 있고 드롭은 일어나지 않는다.
그리고 `dragEnd`를 **일부러 쏘지 않는** 시험을 하나 추가했다.

### 9.5 검증

- 유닛 **2,315개 통과** · `tsc` 통과
- 시험 7개: 칩이 공중에 뜨기 전엔 레인이 없다 · 드래그가 끝나면 걷힌다 ·
  **드롭 뒤에도 걷힌다(§9.4)** · 놓인 열의 첫날을 쓴다 · 칩이 아닌 드롭은 무시한다 ·
  칩은 여전히 작업을 여는 버튼이다 · 쓸 수 없는 타임라인은 드래그를 주지 않는다
- [실측] 실행 중인 앱: 칩을 들면 레인 12개, 조준한 **하나만** 강조
  (`rgba(0,122,255,0.1)` + 1px inset), 6번째 레인에 놓으니
  `dueDate: 2026-10-04`(그 열이 시작하는 날) · `startDate` 빈 채 · 칩이 패널을 떠나
  막대가 되고 · 마지막 칩이었으므로 패널도 사라졌다 · 레인 0개
- 1·2단계 E2E: **140 통과, 실패 0**(커밋된 트리에서 단독 14.3분)

---

## 10. 4단계 구현 기록 (2026-09-02)

### 10.1 되돌리기는 필드의 **이전 값**이지 반대 동사가 아니다

`dateMutation(task, patch)` 하나가 두 드래그를 다 맡는다. 패치가 건드리는 **바로 그
필드들**의 이전 값을 읽어 `undo`로 싣는다(§9.35).

이 화면에서 그것이 특히 중요한 이유: 두 날짜 필드는 **비어 있을 수 있다.** 마감일이
없던 작업을 어느 날에 놓았다가 되돌리면 `dueDate: ""`가 돌아와야 하고, 그래야 그 작업이
다시 `Arrange tasks`로 **돌아간다.** "날짜를 하루 뒤로" 같은 반대 동사였다면 그 작업은
패널로 돌아가지 못하고 엉뚱한 날에 남았을 것이다.

빈 패치에는 `null`을 낸다. 되돌릴 것이 없는 토스트는 없는 토스트보다 나쁘다.

### 10.2 `board.ts`가 한 번에 일관돼졌다

8단계에서 미뤄둔 것이 여기서 정리됐다. `patchForSpanDrag`와 `patchForTrayDrop`은
**규칙 그대로** 남고(잘 시험되어 있다), 되돌리기는 그 위 한 겹에 있다. 파일에 두
관습이 생기지 않았다.

### 10.3 §2.3에서 적어둔 빚을 갚았다

```
전:  onUpdateTask={props.drawer.onUpdate}   →  planner.updateTask, 되돌리기 없음
후:  onMutateTask={mutate}                  →  패치 + 되돌리기 + 토스트
```

**드롭만이 아니라 원래 있던 막대 드래그도 함께 되돌려진다.** 행 메뉴가 자기에 대해
*"everything on it can be undone"*이라 적어둔 그 성질을 타임라인도 갖게 됐다.

### 10.4 검증

- 유닛 **2,319개 통과** · `tsc` 통과
- 도메인 시험 4개: 이전 값을 싣는다 · 마감일이 **없던** 작업은 없는 채로 돌아간다 ·
  막대 이동은 건드린 두 필드를 다 싣는다 · 바뀐 게 없으면 `null`
- 뷰 시험: 드롭이 `{patch, undo, labelKey}`로 도착하고 `undo`가 `{ dueDate: "" }`
- [실측] 실행 중인 앱, 끝까지:
  - 칩을 5번째 레인에 드롭 → `dueDate: 2026-09-27`, 토스트 **`Date changed. [Undo]`**
  - `Undo` → `dueDate: ""`, 칩이 **`Arrange tasks`로 돌아왔고** 패널이 다시 섰다
  - 막대를 끌어 옮겨도 `2026-10-14 → 2026-10-21`에 같은 토스트가 뜬다 —
    **전에는 되돌릴 수 없던 동작이다**

---

## 11. 5단계 구현 기록 (2026-09-02)

### 11.1 빈 패널은 만들 것이 없었다

§4가 "빈 패널의 상태"를 5단계로 잡아 뒀는데, 패널이 `undated.length > 0`으로 막혀 있어
**빈 패널은 애초에 그려지지 않는다.** §15.5의 관용구 그대로 — 할 말이 없는 것은
회색이 아니라 **없다**. 쓸 카드가 없는 것이 답이었다.

대신 확인해야 할 상태는 그 옆이었다.

| 상태 | 화면 |
|---|---|
| 막대만 | 격자, 패널 없음 |
| **칩만** | 격자가 **행 없이 날짜 열만**, 패널에 칩 |
| 둘 다 없음 | 빈 상태 카드 하나 |

가운데가 이 기능의 **첫 화면**이다 — 아무것도 일정이 없고 놓을 더미만 있는 순간.
행은 0개지만 날짜 열은 서 있어야 놓을 판이 된다. [실측] `/inbox?view=gantt`,
행 0 · 열 14 · 빈 상태 카드 없음 · 패널 있음. 칩을 들면 **레인 14개**가 열 14개와 맞고,
놓으니 막대 1개가 생기고 패널이 사라지고 되돌리기 토스트가 떴다.

### 11.2 힌트가 이 타임라인에 있는 길을 말한다

읽기 전용 타임라인에는 끌어다 놓을 곳이 없으므로 "끌어다 놓으세요"는 **따를 수 없는
안내**다. 쓸 수 있는 곳에서만 `Drag one onto a day, or open it to give it dates.`를
쓰고, 아니면 기존 문장을 그대로 둔다. 여는 길은 §3.5대로 **언제나** 남는다.

### 11.3 좁은 화면 [실측]

| 창 | 라벨 열 | 격자 | 패널 |
|---|---|---|---|
| 1800 | 220px | 722px | 220px, **나란히** |
| 880 | 130px | 790px | 790px, **줄 전체** |
| 678 | 130px | 646px | 646px, **줄 전체** |

900 이하에서 패널은 `flex-basis: 100%`로 줄을 통째로 가진다. 감긴 뒤에도 220px로 서
있으면 옆에 빈 자리를 두고 **아직 이웃이 있는 척하는 열**이 된다.

### 11.4 주석 하나가 틀렸다 [실측]

격자의 바닥을 `calc(var(--timeline-label-width) + 200px)`로 바꾸면서, 주석에
*"고정값 420이면 둘 다 들어갈 화면에서도 패널이 계속 감긴다"* 고 적었다. **틀렸다.**

880px에서 재보니 바닥은 330이고 격자에 남는 폭은 554라 들어가야 하는데도 감긴다 —
900 이하에서는 §11.3의 `flex-basis: 100%`가 **어차피** 패널을 줄 전체로 만들기 때문이다.
900 위에서는 라벨이 220이라 이 식은 대체한 420과 같은 값을 낸다.

변수로 읽는 것 자체는 남길 값어치가 있다(라벨 폭을 바꾸면 바닥이 따라온다). 다만
**오늘 무엇이 감기는지는 바뀌지 않는다** — 주석을 그렇게 고쳤다.

### 11.5 시험이 죽은 선택자를 찾아냈다

`EmptyState`가 그리는 클래스는 `.ff-empty`인데 1단계 CSS가 `.tgv-body > .ff-empty-state`
라고 썼다. **아무것도 매치하지 않는 규칙**이었고, 빈 상태가 유일한 자식일 때는 눈에
띄지 않아 넉 단계를 살아남았다. 시험을 쓰면서 같은 오타를 반복했다가 실패로 잡혔다.

### 11.6 검증

- 유닛 **2,323개 통과** · `tsc` 통과
- 시험 4개: 빈 패널은 안 그린다 · 칩만 있을 때 격자와 날짜 열이 남는다 ·
  빈 상태는 양쪽이 다 빌 때만 · 힌트가 드래그를 **있을 때만** 말한다
- [실측] 실행 중인 앱에서 세 폭(1800 · 880 · 678)과 빈 격자 드롭을 끝까지 확인
