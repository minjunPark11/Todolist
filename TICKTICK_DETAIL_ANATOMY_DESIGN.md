# 상세 패널의 해부 — 무엇이 항상 보이고, 무엇이 불려야 보이나

> 상태: **1단계 구현됨 · §4에 남은 것 넷** · 2026-09-01
> (`TASK_DETAIL_PANEL_MERGE_DESIGN.md` §6이 패널을 하나로 만들었고, 그 하나가 **너무
> 많은 것을 그리고 있었다**. 이 문서는 그 하나가 무엇을 그려야 하는가다.)
> 대상: `components/tasks/TaskDrawer.tsx`의 영역 구성 — 헤더 · 본문 · 푸터

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 사용자가 준 참조 앱 스크린샷 3장 (설명 모드 · 체크리스트 모드 · `⋯` 메뉴) | [관찰] |
| **실측(인용)** | `TICKTICK_COMPONENT_07_TASK_DETAIL_PANEL.md`(2026-08-20) — 같은 패널의 DOM·치수 실측 | [실측] |
| **인용** | `TICKTICK_TASK_DETAIL_SPEC.md` §1.6 · §1.7 · §13.8 · §13.36 | [인용] |
| **추론** | 위로 메운 것 | [추론] |

이번 근거는 **두 겹으로 겹친다.** 스크린샷 셋이 말하는 것과, 넉 달 전 같은 화면을
`getBoundingClientRect`로 잰 문서가 말하는 것이 **같다.** 그래서 이 문서의 판단은
추론이 아니라 확인이다.

---

## 1. 참조 앱의 상세는 네 덩어리다

### 1.1 명세가 셋이라고 적어 뒀다 [인용]

`TICKTICK_TASK_DETAIL_SPEC.md` §1.6, 그대로:

```text
┌─────────────────────────────────────┐
│ A. Property Header                  │
│ □ │ Date / Time              │ ⚑    │
├─────────────────────────────────────┤
│ B. Content Header                   │
│ Task Title                      ☷   │
├─────────────────────────────────────┤
│ C. Content Body                     │
│ Description                         │
└─────────────────────────────────────┘
```

`TaskDetailPane` → `TaskPropertyHeader` · `TaskContentHeader` · `TaskContentBody`.
**그게 전부다.** 리스트도, 태그도, 하위 작업도, 메모도 이 그림에 없다.

### 1.2 실측은 넷이라고 말한다 [실측]

`TICKTICK_COMPONENT_07_TASK_DETAIL_PANEL.md` §3.1이 잰 것:

```text
TaskDetail (flex column)
 ├ Header  48   : [체크] [날짜·시간 (flex-auto)] [우선순위]
 ├ Body    flex : 스크롤 — 제목 → 설명 → 첨부 → 태그 → 댓글
 └ Footer  48   : [리스트 선택 (좌)]            [MD] [댓글] [더보기] (우)
```

명세의 셋에 **푸터**가 하나 더 있다. 그리고 그 푸터가 **리스트와 `⋯`를 들고 있다.**

같은 문서가 태그에 대해 적어 둔 것도 중요하다: `.section.detail-tag-view`는 본문
안에 있지만, 그 태스크에 태그가 없어 **높이 0**이었다(§11 "현재 태스크에 해당
데이터가 없어 높이 0이었다"). 첨부와 댓글도 같다.

### 1.3 스크린샷이 그 둘을 확인한다 [관찰]

사용자가 준 세 장:

| | 무엇이 보이나 |
|---|---|
| 1 (체크리스트) | `☰ │ 📅 5월 20일, 오후 3:00, 104d 기한지남` + 둘째 줄 시간대 · `⚑` — 제목 + `☷` — `설명` placeholder — `☐ TB 329` — 하단 `⇥ 학교` … `A 💬 ⋯` |
| 2 (설명) | `☐ │ 날짜 · ⚑` — 제목 + `☷` — `TB 329` — 같은 하단 |
| 3 (`⋯` 열림) | 하위 할일 추가 · 상단 고정 · 계획 취소 · 태그 ▸ · 첨부 파일 업로드하기 · 포커스 시작 ▸ ／ 할일 활동 · 템플릿으로 저장 · 복사 · 링크 복사 · 메모장 열기 · 노트로 변환 ／ 인쇄 · 삭제 |

**라벨이 붙은 속성 줄이 하나도 없다.** 태그는 메뉴에 있고, 리스트는 하단에 있고,
하위 작업은 메뉴의 **첫 항목**이다.

부수적으로 두 가지가 확인됐다:

- **시간대 둘째 줄이 실재한다** — `5월 20일, 오후 2:00 베이징, UTC+08:00`.
  `TASK_DETAIL_SCHEDULE_BODY_DESIGN.md` §10이 "짓지 않는다"고 끝낸 그 줄이다.
  그 결론은 **여전히 유효하다**: 우리는 작업의 시간대를 저장하지 않는다(§10.2).
  참조 앱이 그린다는 것이 우리가 없는 값을 그릴 수 있다는 뜻은 아니다.
- **체크리스트 모드에서도 `설명`이 남아 있다** [관찰]. 스크린샷 1은 `설명`
  placeholder **위에** 체크 항목을 그린다. 우리는 둘을 맞바꾼다
  (`TASK_CONTENT_MODE_DESIGN.md` §11). 그 문서의 Q1이 물은 것이 이것이고,
  **이 스크린샷이 답이다.** 이 문서에서 바꾸지는 않는다 — §4에 남긴다.

### 1.4 §13.8이 위치를 정하지 않았다 [인용]

우리 드로어의 리스트·태그 줄은 명세 §13.8과 §13.36을 인용하며 서 있었다. 그
§13.8의 마지막 줄이 이렇다:

> 정확한 위치는 전체 Detail layout/Visual System에서 확정한다.

**명세가 요구한 것은 자리가 아니라 도달 가능성이었다.** 우리는 "속성 줄"로 읽었고,
참조 앱은 "푸터"와 "메뉴"로 답했다. §13.36의 `Tags / #research #urgent +`도 예시(`예:`)이지
지시가 아니다 — 그리고 그 예시는 **라벨 없는 칩 줄**에 더 가깝다.

---

## 2. 그래서 우리 드로어에서 다섯 덩어리를 걷어냈다

### 2.1 있던 것

```text
헤더   ☐ 완료 │ 날짜 │ ⚑ │ 고정됨 │ ⋯ │ ×
본문   제목 + ☷
       리스트   [Inbox ▾]        ← 라벨 붙은 속성 줄
       태그     [+]              ←
       선행 작업 [없음 ▾]         ←
       설명 ────────────────     ← 머리말
       [본문]
       하위 작업  0/0             ← 항상, 빈 목록 + 추가 폼
       메모                       ← 항상, 빈 textarea
```

**보통의 작업 하나를 열면 빈 칸이 다섯이었다.** 참조 앱에서 같은 작업은 제목 한 줄과
본문 한 줄이다.

### 2.2 규칙 하나로 정리했다

> **섹션은 그 작업이 그것을 쓰기 때문에 그려진다. `⋯`는 작업이 쓰기 시작하는 방법이다.**

이건 발명이 아니라 **참조 앱의 규칙을 그대로 적은 것**이다: 태그 섹션은 태그가 없으면
높이 0이고(§1.2), `태그 ▸`는 `⋯` 안에 있다(§1.3).

| 무엇 | 어디로 |
|---|---|
| **리스트** | **푸터 좌측** — 참조 앱의 `⇥ 학교` 자리. `ListPicker`는 이미 아이콘+이름 트리거였다 |
| **`⋯`** | **푸터 우측** — 헤더에서 내려왔다 |
| **태그** | 태그가 있으면 본문의 칩 줄(라벨 없음, §13.36의 둘째 형태), 없으면 `⋯ › 태그` |
| **하위 작업** | 자식이 있으면 목록, 없으면 `⋯ › 하위 작업 추가` |
| **선행 작업** | 값이 있거나 이 작업을 기다리는 것이 있으면, 없으면 `⋯ › 선행 작업` |
| **메모** | 내용이 있으면, 없으면 `⋯ › 메모` |
| **본문 머리말** | **지웠다.** placeholder가 이미 그 필드를 부른다 |

체크리스트 진행률 `0/3`은 머리말과 함께 사라지지 않고 **제목 줄로 옮겼다.** 머리말이
말하던 것 중 본문이 스스로 말하지 못하는 유일한 것이라서다.

### 2.3 레지스트리에 넷을 더했다

`addSubtask` · `addTag` · `setBlocker` · `addNote`. 이들은 **작업을 바꾸지 않고 이
패널의 한 구획을 연다** — `activities`가 §25.7부터 해 온 것과 같은 모양이고, 새
아이디어가 아니라 그 모양에 넷이 더 붙은 것이다.

`add` 그룹이 **맨 앞**에 온다. 참조 앱의 `⋯`가 `하위 할일 추가`로 열리기 때문이다.

**행 메뉴는 이 넷을 받지 않는다.** 열 상세가 없는 곳에서 구획을 열 수는 없다.
`TaskActionContext.surface`가 그 구분이고, `promoted`가 아니다 — `promoted`는 "내가
직접 그린다"는 뜻인데 행은 이들을 그리지 않는다.

**라벨은 새로 만들지 않았다.** 네 줄은 각자 여는 구획에 이미 쓰여 있는 단어를 쓴다
(`tasks.addSubtask` · `tasks.tags` · `taskDetail.blockedBy` · `taskDetail.notes`).
`tasks.menu.*` 넷을 새로 적었다가 지웠다 — 한국어로는 두 벌 다 "하위 작업 추가"였고,
그건 §4.2가 거부한 중복 그대로다.

### 2.4 선행 작업은 두 질문이었다

한 구획 안에 두 방향이 있다: 이 작업이 **무엇을 기다리는가**(쓰는 값)와 **무엇이 이
작업을 기다리는가**(파생된 값). 후자만 있는 작업에 빈 `없음 ▾` 선택기를 그리면,
읽는 사람이 대답할 필요 없는 질문을 그려 놓는 것이다. 그래서 선택기는 값이 있거나
`⋯`가 열었을 때만 그린다.

---

## 3. 지나가다 찾은 것 — 제목이 4년째 입력 상자였다

본문을 테두리 없는 글로 바꾸다 보니 제목도 상자 안에 있었다. `.tm-drawer-title`은
`border: 1px solid transparent`로 그렇게 말하고 있었는데 **지고 있었다**:

```css
/* 21-components.css */
input:not([type="checkbox"]):not([type="radio"]):not([type="range"])   /* (0,3,1) */
/* 17-tasks-module.css */
.tm-drawer-title                                                        /* (0,1,0) */
```

필드 규칙이 이긴다. 그래서 상세의 제목은 그 규칙이 들어온 날부터 **폼 입력칸**이었고,
아무도 알아채지 못했다 — 참조 앱의 제목은 테두리 없는 굵은 글씨다.

예외를 **21-components.css 안에**, 필드 규칙 바로 옆에 뒀다. 클래스 넷짜리 선택자
(`(0,4,0)`)이고 `!important`가 아니다 — 다음 사람이 이유 없이 싸울 규칙을 남기지
않으려고. 본문 textarea는 같은 문제가 없었지만(`textarea` 가지의 특정도는 `(0,0,1)`),
**같은 파일 안에 같은 선택자로 두 번 적혀 있었고 뒤엣것이 이겼다.** 둘을 합쳤다.

테두리는 hover와 focus에서 돌아온다. "여기 쓸 수 있나"가 질문이 되는 순간이 그때다.

---

## 4. 검증과 남은 것

### 4.1 검증

- 유닛 2,161개 통과(신규 6개: 메뉴의 네 줄 · 각 구획의 열림과 캐럿) · `tsc` 통과 ·
  E2E 118개 통과
- 실제 앱(dev, ko, 456폭 전체 화면):
  - `승인 받기`(태그·메모·하위 작업 없음) → 그려지는 것은 **제목 · 본문 ·
    `이 작업을 기다리는 항목` · 푸터**뿐
  - `⋯` → `하위 작업 추가 · 태그 · 선행 작업 · 메모` 넷이 맨 앞
  - `메모` 클릭 → 메모 구획이 열리고 **캐럿이 그 안에** 들어간다. 태그·하위 작업도 같다
  - 행의 `⋯` → 넷이 **없다**
  - 제목·본문 모두 테두리 없음, hover에서만 나타남

### 4.2 남은 것

- **체크리스트 모드의 설명**(§1.3). 스크린샷은 설명과 체크리스트를 **함께** 그린다.
  우리는 맞바꾼다. `TASK_CONTENT_MODE_DESIGN.md` Q1이 물은 것이고, 이 스크린샷이
  근거이지만 바꾸는 것은 그 문서의 일이다
- **첨부·댓글**. 참조 앱의 본문에는 둘이 더 있고 푸터에도 `💬`가 있다. 우리에겐 둘 다 없다
- **`A`(마크다운) 푸터 버튼**. 본문이 서식을 갖게 되면 자리는 여기다
- **`계획 취소`·`인쇄`·`노트로 변환`**. 참조 앱의 `⋯`에 있고 우리에겐 없다. 세는 것으로 끝낸다
