# FocusFlow 사용성 개선 설계

작성 기준: `fd083f2` (v0.3.10, 배포 브랜치) 시점의 `src/` 실제 구현.

> **구현 현황** — P0-1(버킷 렌더링), P0-3(Inbox 캡처 경로), P1-7(토스트 큐·되돌리기) 구현 완료. 실제로 만든 것과 설계에서 달라진 판단은 §15에 정리했다. 나머지 항목은 미착수.
>
> **정정** — §8의 "대부분의 액션이 undo 스택에 등록되지 않는다"는 **틀렸다.** `usePlannerData`의 `setData` 래퍼가 모든 사용자 변경에 스냅샷을 push하므로 Ctrl+Z는 이미 삭제·완료까지 되돌린다. 실제 결함은 토스트 쪽이었다.

이 문서는 새 기능을 늘리자는 제안이 아니다. **이미 만들어진 기능이 화면에서 제 값을 못 하고 있는 지점**을 코드 근거와 함께 짚고, 각각을 어떻게 고칠지 UI·상태·구현 단위로 설계한다. `design.md`의 원칙("빠른 입력, 빠른 파악 / 쓰지 않는 기능은 추가하지 않는다")을 그대로 판단 기준으로 삼았다.

---

## 1. 진단 요약

앱을 "캡처 → 정리 → 오늘 실행" 흐름으로 놓고 각 단계를 코드에서 따라가 보면, 로직은 있는데 UI가 그 로직을 노출하지 않는 구간이 반복해서 나온다.

| # | 문제 | 근거 | 영향 | 난이도 | 우선순위 |
|---|------|------|------|--------|----------|
| 1 | "오늘 계획하기"의 결과가 화면에 안 보인다 | `FocusQueue.tsx:30-33`이 `entry.bucket`을 무시하고 평면 렌더 | 매우 큼 | 중 | **P0** |
| 2 | Today 행이 마감·지연·우선순위를 안 보여준다 | `TodayEntry.reason`이 `TodayPage.tsx:177`의 카운트에만 쓰임 | 매우 큼 | 하 | **P0** |
| 3 | Inbox로 캡처하는 경로가 UI에 없다 | `TodayPage.tsx:182-194`가 항상 `status:"todo"` + 오늘 날짜 | 큼 | 하 | **P0** |
| 4 | 검색이 두 개인데 둘 다 반쪽이다 | 사이드바 `/` 검색(`App.tsx:1500-1566`) vs Today `⌘K`(`TodayPage.tsx:108-117`) | 큼 | 중 | **P1** |
| 5 | 단축키를 알 방법이 없고 모달 위에서도 발동한다 | `App.tsx:251-283` | 중 | 하 | **P1** |
| 6 | 여러 건을 한 번에 처리할 수 없다 | 다중 선택 상태가 코드 전체에 없음 | 큼 | 중 | **P1** |
| 7 | 되돌리기가 액션마다 제각각이고 토스트가 덮어써진다 | `App.tsx:514-519`, `716-723` | 중 | 하 | **P1** |
| 8 | 모달 포커스 트랩·라이브 리전 부재 | `kit.tsx:620-677` | 중 | 중 | **P2** |
| 9 | 죽은 컴포넌트가 남아 UI 규칙이 두 벌이다 | `QuickAdd.tsx`, `TaskList.tsx` 등 미참조 | 소 | 하 | **P2** |
| 10 | 첫 실행이 완전한 빈 화면이다 | 시드/온보딩 코드 없음 | 중 | 중 | **P2** |

P0 세 개는 모두 "Today 페이지에서 하루를 시작한다"는 핵심 시나리오 위에 있다. 여기부터 고치는 것이 체감 효과가 가장 크다.

---

## 2. P0-1. Plan Today 결과를 화면에 드러내기

### 현재 문제

`utils/todayView.ts`에는 완성된 3버킷 모델이 있다.

- `defaultBucketFor()` (`todayView.ts:77-88`): 지연·진행중·포커스·고우선순위를 `now`, 대기·저우선순위를 `later`로 분류
- `buildTodayPlan()` (`todayView.ts:301-338`): "오늘 계획하기"가 `now/next/later` 배열과 사유를 생성
- `loadBucketOverrides()` / `saveBucketOverrides()`: 사용자가 바꾼 버킷을 **하루 단위로** 저장

그런데 실제 렌더는 이렇다.

```tsx
// FocusQueue.tsx:30-33
const sorted = [
  ...entries.filter((entry) => !entry.completed),
  ...entries.filter((entry) => entry.completed),
];
```

`bucket`이 등장하지 않는다. 결과적으로 사용자가 "오늘 계획하기 → 미리보기 → 적용"을 누르면 토스트만 뜨고 (`TodayPage.tsx:229`) **리스트는 1픽셀도 변하지 않는다.** 저장된 `overrides`도 다음 렌더에서 아무 데도 쓰이지 않는다. 기능이 있는데 없는 것과 같고, 사용자 입장에선 "버튼을 눌렀는데 아무 일도 안 일어나는" 최악의 피드백이다.

### 설계

Focus Queue를 평면 리스트에서 **3개 섹션 리스트**로 바꾼다. 섹션은 접히지 않고 항상 보이며, 비어 있으면 얇은 드롭 힌트만 남긴다.

```
┌─ Focus Queue ───────────────────────────────┐
│ ● 지금             3        [모두 접기]      │
│   ☐ 세금 신고 서류   ⚠ 2일 지남   업무       │
│   ☐ PR 리뷰         오늘 마감    사이드      │
│   ☐ 발표 자료       진행 중                  │
│ ○ 다음             2                         │
│   ☐ 장보기 목록     내일                     │
│ ○ 나중             1                         │
│   ☐ 회신 대기 건    대기 중                  │
│ ─────────────────────────────                │
│   ☑ 아침 스트레칭                            │
└─────────────────────────────────────────────┘
```

**구조**

- `TodayPage`에서 `visibleEntries`를 `bucket` 기준으로 그룹핑해 `FocusQueue`에 `groups: Record<TodayBucketId, TodayEntry[]>` 형태로 넘긴다.
- 완료 항목은 버킷과 무관하게 리스트 맨 아래 "완료" 구획으로 내려간다 (지금 동작 유지).
- 섹션 헤더에 카운트 배지. 빈 섹션도 헤더는 남긴다 — 자리가 고정돼 있어야 이동 대상이 예측 가능하다.

**버킷 이동 방법 (3가지 모두 제공)**

1. 드래그 앤 드롭: 행을 다른 섹션 위로. 이미 `EisenhowerPage`/캘린더에서 HTML5 DnD를 쓰고 있으므로(`onDragStart` 사용처 참고) 같은 방식으로 통일한다.
2. 키보드: 행에 포커스한 상태에서 `1`/`2`/`3` → 지금/다음/나중. 접근성 및 파워유저 경로.
3. 행 우측 `⋯` 메뉴 (`kit.tsx`의 `MoreMenu` 재사용): "지금으로 / 다음으로 / 나중으로".

세 경로 모두 `setOverrides((current) => ({ ...current, [taskId]: bucket }))` 하나로 수렴한다. 저장은 이미 있는 `saveBucketOverrides` 이펙트(`TodayPage.tsx:78-80`)가 처리한다.

**"오늘 계획하기" 적용 후 피드백**

`handleApplyPlan` (`TodayPage.tsx:211-230`)이 성공했을 때:

- 이동된 행에 0.6초짜리 하이라이트(`.tdy-row.is-replanned`, `reduceMotion`이면 생략)
- 토스트 문구를 결과 수치로 바꾼다: "3개를 지금으로, 2개를 다음으로 옮겼습니다" + **되돌리기** 액션 (7번 항목과 연결 — `pushUndo`로 이전 `overrides` 스냅샷 복원)

**기본값과 override의 관계**

`entry.defaultBucket !== entry.bucket`인 행은 사용자가 직접 옮긴 것이다. 섹션 헤더에 "직접 정리함 N" 같은 문구를 넣진 않는다(노이즈). 대신 자정에 override가 리셋된다는 사실만 브리핑 카드 툴팁에 한 줄 남긴다 — 현재는 리셋 규칙(`todayView.ts:38`)이 완전히 비가시적이다.

### 수용 기준

- 계획 적용 후 리스트 순서/그룹이 즉시 바뀐다.
- 드래그·키보드·메뉴 어느 경로로 옮겨도 새로고침 후 유지된다(같은 날 한정).
- 버킷이 비어도 레이아웃이 흔들리지 않는다.

---

## 3. P0-2. Today 행에 판단 근거 노출

### 현재 문제

`collectTodayEntries`는 행마다 `reason`을 계산한다 — `overdue | progress | focus | waiting | high | medium | low` (`todayView.ts:90-98`). 그런데 이 값은 `TodayPage.tsx:177`에서 지연 건수를 세는 데만 쓰이고, 행 렌더(`FocusQueue.tsx:107-141`)는 **체크박스 + 제목 + 스페이스 이름**이 전부다.

즉 오늘 목록만 봐서는 "3일 지난 세금 신고"와 "그냥 오늘 하려던 일"이 시각적으로 동일하다. 마감·시간 블록·우선순위 중 어느 것도 행에 없다. 사용자는 무엇부터 할지 판단하려고 매번 행을 클릭해 상세 패널을 열어야 한다.

### 설계

행을 **한 줄 유지**하되(밀도가 이 앱의 장점이다), 제목 오른쪽에 최대 2개의 메타 칩만 붙인다. 3개 이상이면 우선순위 규칙으로 잘라낸다.

**칩 우선순위 (높은 것부터, 최대 2개)**

| 조건 | 칩 | 톤 |
|------|-----|-----|
| `dueDate < today` | `⚠ N일 지남` | danger |
| `startTime` 있음 | `14:00` | accent |
| `dueDate === today` | `오늘 마감` | warning |
| `status === "doing"` | `진행 중` | accent |
| `status === "waiting"` | `대기 중` | purple |
| `priority === "high"` | `↑ 높음` | danger-ghost |
| 스페이스 있음 | 스페이스 이름 pill | 프로젝트 색 |

톤 클래스는 `kit.tsx`의 기존 `ff-badge-*` / `ff-pill-*` 체계를 그대로 쓴다. 새 색 토큰을 만들지 않는다.

스페이스 pill은 지금도 있으므로(`FocusQueue.tsx:133-139`) **항상 마지막 슬롯**에 고정하고, 앞 슬롯 하나를 위 표의 최상위 조건이 가져간다. 즉 실질 추가는 칩 한 개다.

**지연 표시 계산**

`reason === "overdue"`일 때 일수는 `dueDate`와 `today`의 차이로 계산한다. `buildSpaceSignals`가 이미 같은 계산을 하고 있으므로(`todayView.ts:251-255`) `utils/date.ts`에 `daysBetween(a, b)`를 하나 빼서 양쪽이 공유한다.

**행 클릭 영역 정리**

현재 행 전체가 `role="button"`인데 내부에 체크박스 버튼이 중첩돼 있다(`FocusQueue.tsx:110-131`). 중첩 인터랙티브 요소는 스크린리더에서 깨진다. 구조를 바꾼다.

```tsx
<div className="tdy-row">                      {/* div, role 없음 */}
  <button className="tdy-check" aria-label="…" />
  <button className="tdy-row-open">            {/* 제목 = 열기 버튼 */}
    <span className="tdy-row-title">{title}</span>
  </button>
  <span className="tdy-row-meta">…칩…</span>
</div>
```

`tdy-row-open`을 `flex:1`로 늘려 클릭 체감 면적은 그대로 두면서 시맨틱만 정상화한다.

### 수용 기준

- 지연 항목이 목록에서 한눈에 구분된다.
- 시간이 지정된 항목은 시각을 행에서 바로 읽을 수 있다(현재는 우측 Time Rail을 봐야만 알 수 있다).
- 행 폭이 좁아져도(모바일 1열) 칩이 줄바꿈 없이 말줄임 처리된다.

---

## 4. P0-3. Inbox 캡처 경로 복원

### 현재 문제

`QuickAddInput`의 주석은 이렇게 말한다.

```ts
// QuickAddTaskModal.tsx:11-13
// 체크됨: 바로 Today 태스크 생성. 체크 해제(기본): 제목만 있는 캡처는 Inbox 항목이 된다.
```

`TodayPage.tsx:180-181`에도 같은 취지의 주석이 있다. 그런데 **그 체크박스는 모달에 존재하지 않고**, 핸들러는 무조건 오늘로 보낸다.

```tsx
// TodayPage.tsx:183-191
onCreateTask({
  title: input.title,
  status: "todo",
  scheduledDate: today,   // ← 항상 오늘
  …
});
```

전체 코드에서 `status: "inbox"`로 만드는 UI는 `EisenhowerPage.tsx:77` 하나뿐이다. 즉 Today에서 `n`을 누르거나 `+ 추가`를 눌러 잡은 생각은 전부 오늘 할 일로 강제 편입된다. 그 결과 Inbox Triage 카드(`InboxTriage.tsx:16-66`)는 사실상 항상 비어 있고, "빠른 캡처 → 나중에 정리"라는 제품 원칙이 끊긴다.

동시에 모달 자체도 캡처용으로는 무겁다. 제목·스페이스·우선순위·마감·메모 5필드 폼이며(`QuickAddTaskModal.tsx:64-120`), "떠오른 걸 3초 안에 넣는다"는 목적과 맞지 않는다.

### 설계

**A. 한 줄 캡처 바 (기본 경로)**

Today 헤더의 `+ 추가` 버튼을 누르면 모달 대신 리스트 최상단에 인라인 입력 행이 열린다.

```
┌────────────────────────────────────────────────────┐
│ ＋ │ 내일 오후 3시 팀 회의 준비          │ [오늘] │
└────────────────────────────────────────────────────┘
   Enter 저장 · ⇧Enter 저장 후 계속 · Esc 취소 · ⌥Enter 자세히
```

- 우측 **[오늘]** 토글이 주석에서 말하던 그 체크박스다. 기본값은 **꺼짐** → `status:"inbox"`, `scheduledDate:""`. 켜면 `status:"todo"` + 오늘 날짜.
- 토글 상태는 `localStorage`에 기억한다(`focusflow-quickadd-today`). 사람마다 캡처 습관이 갈리므로 매번 같은 선택을 반복시키지 않는다.
- `⇧Enter`는 저장 후 입력창을 비우고 유지 — 여러 건 연속 캡처용.
- `⌥Enter`(윈도우 `Alt+Enter`)는 현재 입력값을 들고 기존 5필드 모달을 연다. 모달은 삭제하지 않고 "자세히 입력" 경로로 남긴다.

**B. 자연어 날짜/속성 파싱**

캡처 바에서만 동작하는 얕은 파서를 `utils/quickParse.ts`로 신설한다. 완전한 NLP가 아니라 **명시적인 몇 개 패턴만** 인식하고, 인식한 부분은 입력창에서 칩으로 시각화한 뒤 제목에서 제거한다.

| 입력 | 해석 |
|------|------|
| `오늘`, `내일`, `모레`, `월요일`~`일요일` | `scheduledDate` |
| `3/15`, `2026-03-15` | `dueDate` |
| `오후 3시`, `15:00`, `3pm` | `startTime` |
| `#스페이스명` | `projectId` (이름 부분일치, 유일할 때만) |
| `!높음` / `!!` | `priority: "high"` |

영어 키워드(`today`/`tomorrow`/요일)도 함께 인식한다 — 앱이 ko/en 이중 언어다(`i18n/`).

파싱 실패는 조용히 무시하고 전체를 제목으로 둔다. **파싱 결과는 저장 전에 칩으로 보여주므로 오해석이 침묵의 데이터 오염이 되지 않는다.**

**C. Inbox 카드의 위치**

캡처가 살아나면 Inbox에 항목이 실제로 쌓인다. 현재 Inbox Triage 카드는 Focus Queue 아래에 있는데(`TodayPage.tsx:339`), 항목이 0이면 접힌 한 줄, 1개 이상이면 지금 형태를 유지한다. 5개 이상 쌓이면 카드 헤더를 warning 톤으로 바꿔 "정리가 밀렸다"는 신호를 준다.

### 수용 기준

- 제목만 입력하고 Enter → Inbox에 쌓이고 Focus Queue는 변하지 않는다.
- [오늘] 토글을 켠 뒤 Enter → 즉시 Focus Queue 상단에 나타난다.
- `내일 3시 회의`를 입력하면 제목은 "회의", 날짜/시간 칩이 붙는다.

---

## 5. P1-4. 검색을 커맨드 팔레트 하나로 통합

### 현재 문제

검색이 두 벌이다.

| | 사이드바 검색 | Today 검색 |
|---|---|---|
| 위치 | `App.tsx:1500-1566` | `TodayPage.tsx:274-301` |
| 단축키 | `/` (`App.tsx:267`) | `⌘K` (`TodayPage.tsx:108-117`) |
| 범위 | 태스크·프로젝트·토픽·노트 전역 | 현재 Today 화면의 4개 컬렉션 필터 |
| 키보드 탐색 | **없음** (마우스 클릭만) | N/A |
| 액션 실행 | 이동만 | 필터만 |

같은 "검색"이라는 단어로 두 개의 다른 모델이 돌아가고, 업계 표준인 `⌘K`가 전역이 아니라 한 페이지의 로컬 필터에 묶여 있다. 게다가 전역 검색 결과는 화살표로 못 내려간다 — 결과 목록(`App.tsx:1538-1561`)이 그냥 버튼 나열이다.

### 설계

**⌘K = 전역 커맨드 팔레트**로 승격하고, Today 로컬 검색은 헤더의 필터 입력으로 유지하되 단축키를 `/`로 넘긴다(현재 `/`가 하는 일과 의미가 맞는다: "이 화면에서 찾기").

```
┌─ ⌘K ────────────────────────────────────────┐
│ 🔍 세금                                      │
├──────────────────────────────────────────────┤
│ 할 일                                        │
│  ☐ 세금 신고 서류      ⚠ 2일 지남  업무      │
│  ☐ 세금 자료 정리                            │
│ 스페이스                                      │
│  ● 세무                                       │
│ 명령                                          │
│  ⚡ "세금"으로 새 할 일 만들기                │
│  ⚡ 오늘 계획하기                             │
├──────────────────────────────────────────────┤
│ ↑↓ 이동 · ↵ 열기 · ⌘↵ 새 탭 아님… · esc 닫기 │
└──────────────────────────────────────────────┘
```

**구성**

- 신규 `src/components/CommandPalette.tsx`. `App.tsx`의 `searchResults` useMemo(`App.tsx:223-249`)를 그대로 재사용한다 — 검색 로직은 이미 있으므로 껍데기만 바꾸는 작업이다.
- 상태: `activeIndex` 하나. `ArrowUp/Down`으로 이동, `Enter`로 실행, `Escape`로 닫기. 결과 그룹 경계를 넘어 연속 이동한다.
- 각 항목은 기존 `openTaskInOfficialPage` (`App.tsx:811-839`)로 연결 — 이미 태스크 상태에 따라 올바른 페이지로 보내는 라우팅이 구현돼 있다.
- **명령 섹션**: 검색어가 없을 때는 최근 항목 5개 + 자주 쓰는 명령(오늘 계획하기, Inbox 정리, 새 할 일, 설정 열기)을 보여준다. 검색어가 있으면 "'…'으로 새 할 일 만들기"를 항상 마지막에 둔다 — 찾다가 없으면 곧바로 만드는 흐름.

**빈 상태**

현재 결과 없음은 `"검색 결과 없음"` 한 줄이다(`App.tsx:1537`). 여기에 "'세금'으로 새 할 일 만들기" 버튼을 붙이면 막다른 길이 사라진다.

**사이드바 검색 입력 처리**

사이드바의 입력 박스는 남기되, 클릭/포커스하면 팔레트를 여는 트리거로 바꾼다(입력은 팔레트 안에서). 검색 UI가 두 군데서 각각 결과를 그리는 상황을 없앤다.

---

## 6. P1-5. 단축키 발견성과 충돌 정리

### 현재 문제

```ts
// App.tsx:251-283 요약
Escape        → 선택 해제 + 검색어 초기화
/             → 검색 포커스
t             → Today
i             → Today + 트리아지 열기
n             → Today + 빠른 추가
Ctrl/Cmd+Z    → 전역 실행 취소 (App.tsx:331-342)
⌘K            → Today 검색 (TodayPage.tsx:108-117)
```

문제가 셋이다.

1. **어디에도 안내가 없다.** 설정에도, 툴팁에도, 도움말에도 없다. 사실상 아무도 모른다.
2. **모달 위에서도 발동한다.** 가드가 `isTyping`(input/textarea/select)뿐이라(`App.tsx:253-255`), 모달이 열린 상태에서 버튼에 포커스를 두고 `t`를 누르면 배경 페이지가 Today로 바뀐다.
3. **Escape가 이중으로 동작한다.** 전역 핸들러가 선택·검색어를 지우고, `kit.tsx:28-30`의 `useOutsideClose`도 모달을 닫는다. 모달을 닫으려고 Escape를 눌렀는데 배경의 선택 상태까지 함께 날아간다.

### 설계

**A. 모달 인식 가드**

전역 단축키 진입부에 한 줄을 추가한다.

```ts
if (document.querySelector('[role="dialog"], [aria-modal="true"]')) return;
```

DOM 질의가 마음에 걸리면 `AppModals`/`Modal`이 마운트 시 증가시키는 카운터를 `lib/modalStack.ts`로 두고 참조한다. 어느 쪽이든 **모달이 떠 있는 동안 단일 문자 단축키는 죽는다**는 규칙 하나로 통일한다. `Escape`와 `⌘Z`는 예외(각 모달이 자체 처리).

**B. `?` 단축키 오버레이**

`?`(Shift+/)로 여는 읽기 전용 시트. 그룹별로 나열한다.

```
탐색      t 오늘 · c 캘린더 · p 계획 · s 스페이스 · f 포커스
찾기      ⌘K 커맨드 팔레트 · / 현재 화면에서 찾기
만들기    n 빠른 캡처 · ⇧N 자세히 입력 · i 받은함 정리
할 일     ↑↓ 이동 · x 완료 · 1/2/3 지금·다음·나중 · e 보관
기타      ⌘Z 실행 취소 · ? 이 도움말 · esc 닫기
```

목록은 `lib/shortcuts.ts`에 **선언형 배열 하나**로 두고, 오버레이와 실제 핸들러가 같은 배열을 읽게 한다. 그래야 문서와 동작이 갈라지지 않는다.

**C. 단축키 확장**

위 표에서 새로 추가되는 것: `c`/`p`/`s`/`f` 페이지 이동, `x` 완료 토글, `1/2/3` 버킷 이동, `e` 보관, `↑↓` 행 이동, `⇧N` 상세 모달. 리스트 행 포커스 이동(`↑↓`)은 `roving tabindex` 패턴으로 구현한다 — Focus Queue와 Inbox Triage 양쪽에서 재사용.

**D. 진입점**

사이드바 하단에 `?` 아이콘 버튼(툴팁 "단축키")을 둔다. 키보드를 안 쓰는 사용자도 존재를 알 수 있어야 한다.

---

## 7. P1-6. 다중 선택과 일괄 처리

### 현재 문제

`bulk`, `selectedIds`, `multiSelect` — 코드 전체에 하나도 없다. 모든 조작이 1건 단위다. 그 대가가 가장 크게 드러나는 곳이 Inbox Triage다.

`TriageRow`(`InboxTriage.tsx:146-201`)는 **행 하나당 셀렉트 1개 + 버튼 5개**를 렌더한다. 받은함에 15개가 쌓이면 화면에 버튼이 75개 놓인다. 정리 작업은 원래 "빠르게 훑으며 분류"인데, UI는 매 행마다 정독을 요구한다.

### 설계

**A. 선택 모델**

`useSelection(items)` 훅을 `src/hooks/useSelection.ts`에 만든다.

- `Set<string>` 기반, 마지막 클릭 인덱스(`anchor`) 보관
- 클릭 = 단일 선택 / `⌘`·`Ctrl` 클릭 = 토글 / `Shift` 클릭 = 앵커까지 범위
- 목록이 바뀌면 사라진 id를 자동 정리

Focus Queue, Inbox Triage, 스페이스 태스크 목록에서 공용으로 쓴다.

**B. 벌크 액션 바**

1개 이상 선택되면 화면 하단에서 올라온다(`GlobalFocusBar`와 같은 레이어, 동시에 뜨면 위로 쌓임).

```
┌────────────────────────────────────────────────────────┐
│ 3개 선택됨   [오늘로] [스페이스 ▾] [우선순위 ▾] [보관] │
│                                            [선택 해제] │
└────────────────────────────────────────────────────────┘
```

모든 벌크 액션은 **한 번의 undo로 통째로 되돌아간다** — `pushUndo`에 개별이 아니라 배치 스냅샷을 하나 넣는다(7번과 연결).

**C. Triage 행 단순화**

행의 상시 버튼을 5개 → 2개로 줄인다.

- 상시: **[오늘로]**, **[스페이스 ▾]** (가장 흔한 두 결정)
- 나머지(캘린더에 배치 / 보관 / 받은함 유지)는 `⋯` 메뉴로 이동 (`kit.tsx`의 `MoreMenu` 재사용)

**D. 키보드 트리아지 모드**

Triage 드로어에 포커스가 있을 때:

| 키 | 동작 |
|---|---|
| `j` / `↓`, `k` / `↑` | 행 이동 |
| `1` | 오늘로 |
| `2` | 스페이스 선택 팝오버 열기 |
| `3` | 캘린더에 배치 |
| `e` | 보관 |
| `Space` | 선택 토글 |

처리한 행은 목록에서 빠지고 포커스는 다음 행으로 자동 이동한다(이미 `AnimatePresence`로 exit 애니메이션이 있으므로 시각적으로 자연스럽다). 받은함 20개를 손 안 떼고 정리할 수 있게 된다.

---

## 8. P1-7. 실행 취소와 토스트 일관성

### 현재 문제

**되돌리기가 액션마다 다르다.**

| 액션 | 되돌리기 |
|---|---|
| 태스크 보관 | 토스트 undo 있음 (`App.tsx:716-723`) |
| 프로젝트 보관 | 토스트 undo 있음 (`App.tsx:730-740`) |
| 태스크 삭제 | **없음** (`App.tsx:742-749`) |
| 태스크 완료 | 없음 |
| 계획 적용 | 없음 (`TodayPage.tsx:211-230`) |
| 복제 | 없음 (`App.tsx:725-728`) |
| 외부 캘린더 편집 | `pushUndo` 스택 (`App.tsx:562-573`) — 토스트 안내는 없음 |

`⌘Z` 전역 undo 스택(`lib/undoStack.ts`)이 이미 있는데도 대부분의 액션이 거기 등록되지 않는다. 사용자는 무엇이 되돌아가는지 예측할 수 없다.

**토스트가 덮어써진다.**

```tsx
// App.tsx:514-519
function showToast(nextToast) {
  setToast(nextToast);
  window.setTimeout(() => { … }, 4500);
}
```

단일 상태다. 연속으로 3개를 보관하면 마지막 것만 보이고, **앞의 두 개는 undo 버튼을 누를 기회조차 없이 사라진다.**

### 설계

**A. 규칙 하나로 통일**

> 데이터를 바꾸는 모든 사용자 액션은 (1) `pushUndo`에 역연산을 등록하고, (2) 되돌리기 버튼이 달린 토스트를 띄운다.

`useUndoableAction(label, doFn, undoFn)` 헬퍼를 만들어 호출부가 두 가지를 따로 챙기지 않게 한다. 배경 동기화(외부 캘린더 sync 등)는 지금처럼 스택에서 제외한다 — `App.tsx:559-561`의 판단이 옳다.

**B. 토스트 큐**

`toast: ToastState | null`을 `toasts: ToastState[]` (최대 3개)로 바꾼다.

- 새 토스트는 아래에서 위로 쌓인다
- 각자 자기 타이머(4.5초)를 갖는다
- 되돌리기 버튼이 있는 토스트는 6초로 늘린다 — 4.5초는 문구를 읽고 결정하기에 짧다
- 호버 중에는 타이머를 멈춘다

`AppModals`가 이미 토스트를 렌더하므로(`AppModals.tsx`) 컨테이너만 스택으로 바꾸면 된다.

**C. 삭제 확인의 재해석**

현재 `confirmBeforeDelete` 설정으로 삭제마다 모달을 띄운다(`App.tsx:742-749`). 되돌리기가 모든 곳에서 보장되면 **확인 모달은 대부분 불필요한 마찰**이 된다. 다만 설정을 없애진 않는다:

- 기본값을 "확인 안 함 + 되돌리기 토스트"로 바꾼다
- 되돌릴 수 없는 것(전체 초기화, 프로젝트 영구 삭제)은 설정과 무관하게 항상 확인 모달

---

## 9. P2-8. 접근성 마감

`kit.tsx`의 `Modal`(`620-677`)과 `ConfirmModal`(`500-593`)에 다음이 없다.

| 항목 | 현재 | 설계 |
|---|---|---|
| 포커스 트랩 | 없음 — Tab이 배경으로 샌다 | `useFocusTrap(ref)` 훅 신설, 두 컴포넌트에 적용 |
| `aria-labelledby` | `role="dialog"`만 있고 제목 연결 없음 | `<h2 id>` 생성 후 연결 |
| 닫은 뒤 포커스 복원 | `ConfirmModal`만 부분적 | 열기 직전 `document.activeElement` 저장 → 언마운트 시 복원 |
| 배경 스크롤 잠금 | 없음 | 열려 있는 동안 `body { overflow: hidden }` |
| 라이브 리전 | `Toast`의 `role="status"`뿐 | 앱 루트에 `aria-live="polite"` 리전 하나, 카운트 변화·플랜 적용 결과 announce |

`TodayPage`가 이미 트리아지 열기/닫기에서 포커스 반환을 수동으로 처리하고 있는데(`TodayPage.tsx:97-105`), 이건 `Modal`이 해줘야 할 일이다. 훅으로 올리면 그 수동 코드도 지운다.

`AttentionFromSpaces`의 "숨기기"(`TodayPage.tsx:347`)는 세션 상태에만 저장돼 새로고침하면 되살아난다. 숨김도 하루 단위로 `localStorage`에 저장한다 — 버킷 override와 같은 규칙.

---

## 10. P2-9. 죽은 코드 정리

어디서도 import되지 않는 컴포넌트:

- `src/components/QuickAdd.tsx` (91줄)
- `src/components/TaskList.tsx` (229줄)
- `src/components/motion/MotionDropZone.tsx`
- `src/components/motion/ExpandableAdd.tsx`
- `src/components/motion/MotionPanelShell.tsx`

사용성 관점에서 중요한 이유: `QuickAdd.tsx`는 **`status: "inbox"`를 "프로젝트 없음"과 동일시하는** 옛 모델을 담고 있다(`<option value="">{t("status.inbox")}</option>`). 지금 모델에서 inbox는 상태이지 프로젝트가 아니다. 이런 화석이 남아 있으면 다음 작업자가 어느 쪽이 정답인지 판단할 수 없다.

P0-3에서 캡처 UI를 새로 만들 때 이들을 함께 삭제한다. `MotionDropZone`은 P0-1의 드래그 구현에 재사용할 수 있는지 먼저 확인한 뒤 결정한다.

---

## 11. P2-10. 첫 실행 경험

시드 데이터도, 온보딩도 없다. 로그인 직후 Today는 빈 카드 4개다. `FocusQueue`의 빈 상태(`FocusQueue.tsx:48-60`)가 "할 일 추가 / 스페이스 열기"를 제시하긴 하지만, 스페이스도 비어 있어 두 번째 버튼은 또 다른 빈 화면으로 간다.

**설계 (가볍게)**

- 데이터가 완전히 비었을 때만 Today 상단에 3단계 체크리스트 카드를 띄운다: ① 첫 할 일 캡처 ② 스페이스 하나 만들기 ③ 오늘 계획하기 실행
- 각 항목은 완료되면 체크되고, 셋 다 끝나면 카드가 사라진다(다시 안 뜸, `localStorage` 플래그)
- 가짜 샘플 데이터는 넣지 않는다 — 지우는 수고를 떠넘기는 꼴이고, `design.md`의 "쓰지 않는 기능은 추가하지 않는다"와도 맞지 않는다

---

## 12. 실행 순서

| 단계 | 범위 | 산출물 |
|---|---|---|
| **1** | P0-1, P0-2 | `FocusQueue` 버킷 렌더 + 메타 칩. Today 화면만 손대므로 위험이 낮고 체감이 가장 크다 |
| **2** | P0-3, P2-9 | 인라인 캡처 바 + 자연어 파서 + 죽은 컴포넌트 삭제 |
| **3** | P1-7 | 토스트 큐 + `useUndoableAction`. 4단계의 안전망이므로 먼저 깐다 |
| **4** | P1-6 | `useSelection` + 벌크 액션 바 + Triage 재구성 |
| **5** | P1-4, P1-5 | 커맨드 팔레트 + 단축키 오버레이 + 모달 가드 |
| **6** | P2-8, P2-10 | 포커스 트랩·라이브 리전, 온보딩 체크리스트 |

각 단계는 독립적으로 배포 가능하다. 단계 3을 4보다 앞에 둔 이유는, 벌크 액션이 되돌리기 없이 나가면 사고 위험이 오히려 커지기 때문이다.

---

## 13. 신규/변경 파일

**신규**

```
src/components/CommandPalette.tsx      커맨드 팔레트 (P1-4)
src/components/ShortcutOverlay.tsx     ? 도움말 (P1-5)
src/components/today/InlineCapture.tsx 한 줄 캡처 바 (P0-3)
src/components/BulkActionBar.tsx       벌크 액션 (P1-6)
src/hooks/useSelection.ts              다중 선택 (P1-6)
src/hooks/useFocusTrap.ts              포커스 트랩 (P2-8)
src/lib/shortcuts.ts                   단축키 선언 (P1-5)
src/lib/modalStack.ts                  모달 카운터 (P1-5)
src/utils/quickParse.ts                자연어 파싱 (P0-3)
```

**주요 변경**

| 파일 | 내용 |
|---|---|
| `components/today/FocusQueue.tsx` | 버킷 그룹 렌더, 메타 칩, 행 시맨틱 교정, 선택/키보드 |
| `components/TodayPage.tsx` | 그룹핑, 버킷 이동 핸들러, 캡처 바 연결, 수동 포커스 관리 제거 |
| `components/today/InboxTriage.tsx` | 행 액션 2개 + `⋯`, 키보드 트리아지 |
| `components/kit.tsx` | `Modal`/`ConfirmModal` 접근성, 토스트 큐 대응 |
| `App.tsx` | 단축키 가드·확장, 토스트 배열화, 팔레트 연결, undo 헬퍼 적용 |
| `utils/todayView.ts` | `daysBetween` 분리, 숨긴 시그널 영속화 |
| `i18n/ko.ts`, `i18n/en.ts` | 신규 키 (`todayv.bucket*`, `capture.*`, `palette.*`, `shortcuts.*`, `bulk.*`) |

새 i18n 키는 항상 ko/en 동시에 추가한다 — 한쪽만 넣으면 다른 언어에서 키 문자열이 그대로 노출된다.

---

## 14. 무엇을 하지 않는가

범위를 좁게 유지하기 위해 아래는 명시적으로 제외한다.

- 데이터 모델 변경 — 위 설계는 전부 기존 `Task`/`Project` 필드와 `localStorage` 키만 쓴다. 마이그레이션이 없다.
- 반복 일정 UI 개편 — `repeatType` 등 필드는 있으나 별도 과제다(`docs/Architecture/Responsive_Audit.md`의 CSS 정리와 함께 다룰 사안).
- 협업·공유 기능 — `design.md`의 scope out을 유지한다.
- 새 색/타이포 토큰 — 기존 `ff-*` 디자인 시스템 안에서만 조합한다.

---

## 15. 구현 기록

### 1단계 — 버킷 렌더링 (P0-1)

P0-1을 구현하면서 확인한 사실 하나: **이 기능은 설계·번역·CSS가 모두 이미 존재했고 JSX만 없었다.**

- `todayv.bucket.*`, `todayv.bucketEmpty.*`, `todayv.moveTo.*`, `todayv.reason.*`, `todayv.estimate`, `todayv.hideCompleted/showCompleted`, `todayv.moveAllLater`, `todayv.clearPlan`, `todayv.rowMenuAria` — ko/en 양쪽에 이미 있었다
- `.tdy-bucket`, `.tdy-bucket-head`, `.tdy-bucket-dot-now/next/later`, `.tdy-bucket-count`, `.tdy-bucket-empty`, `.tdy-reason-*` 7종, `.tdy-estimate`, `.tdy-row-time` — 이미 있었다

그래서 실제 추가는 렌더 코드와 CSS 3줄, i18n 키 1쌍(`todayv.toastMovedAllLater`)이 전부다.

**함께 처리한 것**

- 행 메타 노출(P0-2 일부): 사유 칩·예상 시간·시작 시각을 행에 표시
- 행 시맨틱 교정: 중첩 인터랙티브 요소를 없애고 제목을 열기 버튼으로 분리
- `showCompletedInToday`: 큐 메뉴 토글과 Settings 토글이 같은 값을 쓰도록 연결 (기존에는 설정이 아무 동작도 하지 않았음)
- `confirmBeforeDelete`: Settings 동작 탭에 토글 노출 (기존에는 끌 방법이 없었음)
- 버킷을 바꾸는 모든 경로에 되돌리기 토스트

**발견한 기존 버그**

토스트가 AI 런처와 같은 코너에 겹쳐 있었고 런처의 `z-index`가 더 높아서, **토스트의 되돌리기 버튼을 누를 수 없었다.** `elementFromPoint`로 확인한 결과 모든 화면 폭에서 `.ollama-chat-fab`이 클릭을 가로챘다. 보관 취소 등 기존 되돌리기 토스트도 전부 같은 상태였다. 토스트를 런처 왼쪽으로 옮기고, 포커스 바가 떠 있을 때는 런처와 같은 높이로 올리도록 고쳤다.

---

### 2단계 — Inbox 캡처 경로 (P0-3)

1단계와 달리 준비된 CSS는 없었고, 문자열도 `todayv.toastAddedToInbox` 하나뿐이라 대부분 새로 만들었다.

**만든 것**

- `utils/quickParse.ts` — 얕은 자연어 파서. 상대 날짜(오늘·내일·모레·요일, today/tomorrow/weekday) → `scheduledDate`, 명시 날짜(`2026-04-02`, `4/2`, `4월 2일`) → `dueDate`, 시각(`오후 3시`, `15:00`, `3pm`) → `startTime`, `#스페이스`, `!!`/`!높음`. 테스트 14개.
- `components/today/InlineCapture.tsx` — 항상 떠 있는 한 줄 캡처 바. 입력 중 파싱 결과를 칩으로 보여주고, 목적지 토글과 「추가」를 같은 줄에 둔다.

**설계에서 달라진 판단**

| 설계 문서 | 실제 구현 | 이유 |
|---|---|---|
| `+ 추가`를 누르면 인라인 행이 열림 | 캡처 바는 항상 보이고, `n`이 여기로 포커스 | 캡처는 한 동작이어야 한다. 여는 클릭이 먼저 필요하면 모달과 다를 게 없다. 헤더 `+ 추가`는 상세 모달로 그대로 둬서 두 경로가 다 보인다 |
| 토글 기본값 = 꺼짐(받은함) | **켜짐(오늘)** | 기존 동작을 바꾸지 않는 쪽을 골랐다. 받은함은 눈에 보이는 토글 한 번이면 되고, 그 선택은 기억된다 |
| `⇧Enter` 저장 후 계속 | 없음 | `Enter`가 이미 입력창을 비우고 포커스를 유지해서 연속 캡처가 된다. 같은 일을 하는 키를 둘 둘 이유가 없다 |
| 바 없는 `N시`도 파싱 | 파싱 안 함 | `3시`가 03:00인지 15:00인지 확정할 수 없다. 틀린 값을 조용히 넣느니 제목에 남긴다 |
| 받은함 5개 이상이면 카드 warning 톤 | 미구현 | 임계값 근거가 없어 보류 |

**동작 규칙 하나**

파싱된 날짜가 토글보다 우선한다. 토글이 「오늘」인 상태에서 "내일 회의"를 적으면 내일로 간다. 이때 토스트는 `todayv.toastTaskScheduled`로 실제 날짜를 말한다 — 처음엔 "오늘에 추가했습니다"라고 떠서 내일 예약된 일과 어긋났다.

**발견한 기존 버그**

`t`/`i`/`n` 단축키가 `preventDefault()`를 하지 않았다. 지금까지는 모달을 여는 동작이라 티가 안 났지만, `n`이 입력창을 포커스하게 되자 그 `n`이 입력창에 그대로 찍혔다. 세 단축키 모두 기본 동작을 막고, 수정자 키가 눌린 조합은 아예 무시하도록 바꿨다(`⌘N` 같은 브라우저 단축키를 가로채지 않도록).

### 3단계 — 토스트 큐와 되돌리기 (P1-7)

착수하자마자 설계 전제 하나가 틀렸다는 게 드러났다. `usePlannerData`의 `setData` 래퍼(`usePlannerData.ts:534-541`)가 **모든 사용자 변경에 undo 스냅샷을 push**하고 있어서, Ctrl+Z는 이미 삭제·완료·편집을 전부 되돌린다. `useSpaceHubData`도 같다. 그래서 "액션마다 undo를 등록한다"는 계획은 필요 없었고, 남은 진짜 문제는 두 개였다.

**1. 토스트가 덮어써진다**

`toast: ToastState | null` 단일 상태라, 연속 두 번 액션하면 첫 토스트가 되돌리기 버튼째 사라졌다. `lib/toastQueue.ts`(순수 함수 + 테스트 6개)로 큐를 만들고, 각 토스트가 `ToastItem`에서 자기 수명을 갖게 했다.

- 최대 3개, 넘치면 **가장 오래된 것**이 밀려난다(예전처럼 최신이 밀리는 게 아니라)
- 되돌리기가 달린 토스트는 7초, 나머지는 4.5초 — 4.5초는 읽고 판단하기에 짧다
- 호버/포커스 중에는 타이머가 멈춘다

**2. 되돌리기가 순서에 안전하지 않았다**

전역 `popUndo()`는 항상 **가장 최근** 그룹을 되돌린다. 토스트가 여러 개 떠 있는 상태에서 오래된 토스트의 되돌리기를 누르면 엉뚱한 액션이 취소된다. 그래서 토스트마다 자기 복원 클로저를 들고 있게 했다.

삭제는 하드 삭제라 복원할 대상이 없어서, 지우기 **전에** 행을 캡처해 두고 되돌리기가 그것만 다시 넣는다 — `restoreDeletedTask(task, subtasks, childTaskIds)`, `restoreDeletedProject(project, taskIds)`. 스토어 전체 스냅샷을 되돌리는 방식이었다면 토스트가 떠 있는 동안 사용자가 한 다른 작업까지 날아갔을 것이다. 복제(`duplicateTask`)도 되돌리기가 붙었다(사본 삭제).

**발견한 기존 버그 둘**

- **모달이 드로어 밑에 깔렸다.** `.ff-modal-backdrop`이 `z-index: 100`인데 `.tdy-detail-overlay`는 `1200`이다. Today에서 할 일을 열고 「삭제」를 누르면 확인 모달이 보이기는 하는데 **클릭이 안 됐다** — Today 상세에서 할 일을 지우는 게 아예 불가능했다. 모달을 1400, 토스트 스택을 1500으로 올려 드로어(1200~1300) 위로 뺐다.
- 2단계에서 고친 AI 런처 겹침과 같은 계열의 문제다. 이 앱의 `z-index`는 70~230 대와 1099~1300 대로 두 층이 따로 자라 있어서, 나중에 한 번 정리할 값이다.

**하지 않은 것**

`confirmBeforeDelete` 기본값은 `true` 그대로 뒀다. 설계 문서는 "되돌리기가 보장되면 확인 모달은 불필요한 마찰"이라고 봤지만, 지금까지 이 설정에 UI가 없었으므로 모두가 기본값에 있다 — 기본값을 뒤집으면 전원이 예고 없이 삭제 확인을 잃는다. 삭제가 되돌려지게 됐으니 이제 사용자가 직접 끄면 된다(1단계에서 노출한 동작 탭 토글).
