# Google 연결 및 자동 동기화 재설계

- 작성: 2026-09-10
- 상태: 구현 전 설계. 기존 M3·M4 이후의 별도 작업이다.
- 목적: 사용자는 연결과 일반 설정만 관리한다. 이미 수정한 내용을 다시 승인하지 않는다.
- 범위: Google 자동 병합, 시간대 자동 적용, 예외 처리, 설정 항목 이동. 이 문서 작성은 운영 데이터 변경이나 배포를 포함하지 않는다.

## 1. 현재 문제와 확인 근거

- `src/domain/calendar/googleSync/taskInboundPlan.ts`: 한쪽만 변경하면 자동 처리하지만, 양쪽이 기준과 다르면 서로 다른 필드 수정도 충돌 처리한다. 기준이 없는 경우도 충돌이다.
- `src/components/calendar/GoogleTaskReviewPanel.tsx`: 충돌 외에 가져오기 후보, 복원, 반복 변경, 과거 작업 전송 등을 한 화면에서 다룬다. 표시된 19건의 실제 원인과 비율은 아직 조사하지 않았다.
- `src/components/calendar/GoogleCalendarCard.tsx`: 시간대를 별도로 선택하며 변경 후 앱 시간대를 역으로 바꾼다. 수동 동기화 버튼이 검토 패널에도 존재한다.
- `supabase/migrations/036_google_sync_timezone_repin.sql`: 미해결 검토가 있으면 시간대 변경을 거부한다. UI만 제거해서는 해결되지 않는다.
- `src/components/SettingsPage.tsx`: Google 외에 캘린더 공유, 외부 캘린더 구독, AI 접근 권한도 연결 및 공유에 있다.
- 기기 간 M3·M4 병합과 Google 병합은 별도 경로다. 한쪽 구현을 다른 쪽 완료로 간주하지 않는다.

## 2. 최종 정보 구조

| 현재 항목 | 최종 위치 / 처리 |
|---|---|
| 연결 및 공유 메뉴 | ‘연결’로 이름 변경. 내부 탭 ID `connections`와 OAuth 복귀 경로는 유지 |
| Google 캘린더 카드 | 연결 화면의 유일한 서비스 카드 |
| Google 시간대 선택 | 제거. 일반 설정의 계정 시간대를 사용 |
| 상시 검토 목록 | 제거. 자동 처리 후 정말 필요한 예외만 ‘확인 필요’로 진입 |
| 지금 동기화 / 새로고침 및 동기화 | 상시 버튼 제거. 보조 메뉴의 ‘다시 동기화’ 하나로 통합 |
| 캘린더 공유 링크 관리 | 캘린더 화면의 ‘공유’ 메뉴. 현재 공유 범위를 유지하며 목록·프로젝트 공유로 바꾸지 않음 |
| 외부 캘린더 구독 / 표시 / 갱신 / 삭제 | 캘린더 화면의 ‘캘린더 관리’ |
| 연결된 AI 권한 조회 / 해제 | 계정 및 동기화 > 접근 권한 |
| 동기화 지원 범위 설명 | 연결 관리의 도움말. 실제 지원 범위는 그대로 유지 |

공유 링크·구독·권한은 기능을 삭제하지 않는다. 새 진입점이 구현되고 검증된 뒤 기존 설정 항목을 제거한다. 기존 공유 링크를 재발급하거나 구독을 다시 만들지 않는다.

## 3. 연결 화면

```text
연결

Google 캘린더                         [⋯]
연결된 계정
자동 동기화 중 · 마지막 완료 시각
```

- 미연결: 짧은 기능 설명과 ‘Google 캘린더 연결’ 버튼.
- 연결됨: 계정, 동기화 상태, 마지막 성공 시각. 연결 여부와 동기화 성공 여부를 혼동하지 않는다.
- 보조 메뉴: 다시 동기화, 연결 관리, 도움말. 계정 변경은 기존 연결 교체 프로토콜이 지원하는 범위에서 구현하며 단순 연결 해제로 대체하지 않는다.
- 연결 해제는 연결 관리 안에 두고 기존 확인 절차를 유지한다.
- 일시적 네트워크 실패는 자동 재시도. 인증 만료 시 ‘다시 연결’, 자동 판단 불가 시 ‘확인 필요 N건’만 표시한다.
- 자동 완료마다 토스트를 띄우지 않는다. 필요하면 최근 처리 내역을 펼쳐 확인한다.
- 미처리 항목이나 시간대 전환이 남으면 ‘모두 동기화됨’으로 표시하지 않는다.

## 4. 자동 반영 규칙

마지막으로 양쪽이 합의한 기준값, 현재 앱 값, 현재 Google 값을 비교한다. 같은 사용자의 수정이라는 이유만으로 어느 쪽이 최신인지 추정하지 않는다.

| 상황 | 기본 처리 |
|---|---|
| 한쪽만 변경 | 변경된 쪽 자동 반영 |
| 양쪽 결과가 같음 | 기준 갱신 후 완료 |
| 서로 다른 필드 묶음 변경 | 자동 병합 후 양쪽 반영 |
| 같은 필드 묶음을 양쪽에서 변경 | 선후 관계를 증명할 수 있으면 후속 수정 반영. 그렇지 않으면 해당 항목만 예외 |
| 반복 규칙을 한쪽만 변경 | 지원하는 규칙이면 자동 반영. 같은 작업의 날짜·시간과 함께 검증 |
| 연결 대상이 확실한 신규 작업/일정 | 현재 지원 범위 안에서 자동 생성·연결 |
| 제목·시간만 같은 중복 후보 | 동일 작업으로 단정하지 않음. 안정적인 연결 정보로 확인되지 않으면 예외 |
| 삭제·복원과 편집이 경쟁 | 삭제 의도와 수정 순서를 입증할 수 있는 경우만 자동 처리. 애매하면 예외 |
| 기준값이 없는 기존 항목 | 서버 연결·이력으로 기준을 복원할 수 있으면 재판정. 불가능하면 예외 |

병합 묶음은 제목, 설명, 일정(시작/종료 날짜·시각·종일 여부·반복 규칙)으로 정의한다. 일정은 유효하지 않은 조합을 만들지 않도록 통째로 검증한다. 앱 전용 필드는 Google 값으로 지우지 않는다.

Google의 updated 시각, 기기의 시계, 서버에 늦게 도착한 순서만으로 ‘마지막 수정’을 판단하지 않는다. 비교 가능한 이력이 없으면 무조건 최신값 선택은 구현하지 않는다. 이를 가능하게 하려면 먼저 변경 이력과 인과관계 기록이 필요하다.

### 자동 처리의 신뢰성

- 기존 revision·etag·연결 generation 검증 및 중복 실행 방지를 유지한다.
- 반영 직전 값이 바뀌면 다시 읽고 재판정한다. 재시도는 제한하고 실패 시 대기열로 넘긴다.
- 한 항목의 예외가 독립적인 다른 작업의 동기화를 막지 않게 한다. 연결 교체 등 공유 상태를 바꾸는 작업은 별도 잠금이 필요하다.
- 자동 반영 전 값과 처리 이유를 서버에서 복구 가능한 이력으로 남긴다. 기기 localStorage만으로 복구를 보장하지 않는다.
- 복원은 과거 값을 무조건 덮어쓰는 것이 아니라 현재 revision에 대한 새 수정으로 적용한다.
- 이력의 보존 기간·저장 비용·정리 작업은 구현 단계에서 스키마와 함께 확정하고 사용자에게 실제 기간을 표시한다.

## 5. 시간대는 일반 설정 하나로 관리

### 기준

- 일반 설정에서 저장된 계정의 유효 시간대가 목표값이다. Google 카드는 이를 변경하지 않는다.
- 목표 시간대와 실제 적용 완료 시간대는 내부적으로 구분한다. UI에는 선택기를 두 개 제공하지 않는다.
- 기기 자동 감지 모드에서도 각 기기가 Google 시간대를 직접 재설정하지 않는다. 승인된 계정 설정 갱신만 서버의 전환 작업을 만든다. 서로 다른 지역 기기의 접속만으로 시간대가 왕복하지 않아야 한다.
- 이 자동 모드의 현행 계정 저장 방식을 먼저 확인하고, 시간대 변경 이벤트를 계정 revision과 연결한다.

### 자동 전환 절차

1. 최초 연결 시 계정 시간대를 적용한다.
2. 일반 설정에서 시간대가 바뀌면 서버에 목표 시간대와 설정 revision을 기록한다.
3. 동기화 작업과 전환 작업을 직렬화하고, 기존 기준으로 진행 중인 쓰기를 마무리한다.
4. 미해결 항목을 자동 병합 규칙으로 재판정한다. 실제로 전환을 막는 상태와 단순 안내 대상을 구분한다.
5. 남은 예외를 이전 시간대·원본 일정·revision과 함께 보존하여 전환 후에도 재판정 가능하게 만든다. 이 경로가 구현되기 전에는 기존 서버 차단 조건을 무작정 제거하지 않는다.
6. 기존 가져온 일정의 재계산과 동기화 기준 변경을 재개 가능한 작업으로 완료한다.
7. 적용 완료 시간대를 확정한 후 일반 동기화를 재개한다. 오프라인·중단·재접속 시 중복 변경 없이 이어서 처리한다.

시간 있는 Google 일정은 원래 instant를 보존하며 새 시간대에서 표현한다. 종일 일정은 날짜가 밀리지 않아야 한다. 앱에서 만든 벽시계 시각과 반복 일정은 기존 시간대 변경 정책을 명시적으로 점검하고, 단순 UTC 오프셋 덧셈으로 일괄 변경하지 않는다. DST와 반복 예외도 검증한다.

사용자에게는 ‘시간대 적용 중’, ‘적용 지연 · 재시도’처럼 상태만 알린다. 일반 설정에서 변경하기 전에 기존 일정에 미치는 영향을 짧게 설명한다. Google 설정에서 다시 같은 값을 선택하게 하지 않는다.

## 6. 기존 검토 항목 전환

- 현재 표시된 19건을 일괄 승인하거나 삭제하지 않는다. 서버에서 유형별 원인을 먼저 집계한다.
- 기준값·연결 정보·시간대 정보를 확보한 후 새 정책으로 자동 재판정한다.
- 안전하게 처리 가능한 항목은 자동 반영하고 기존 검토 상태를 완료 처리한다.
- 이전에 사용자가 명시적으로 제외한 항목은 다시 가져오지 않는다.
- 남은 예외만 차이와 영향이 보이는 상세 화면에 표시한다. 결과를 바꾸는 선택은 의미가 분명한 버튼으로 제공한다.
- 예외 화면은 평소 방문할 설정 화면이 아니다. 상태 표시에서 해당 문제로 바로 이동한다.

## 7. 구현 순서와 완료 기준

### A1. 판정 기준·이력 준비

- Google 검토 유형 집계, 자동 처리 가능 범위 확인.
- Google 병합 함수를 UI와 분리하고 기존 기기 병합 규칙 중 재사용 가능한 부분을 공유.
- 서버 변경 이력·복구·revision 보호를 준비.
- 완료: 한쪽 변경/같은 변경/다른 필드 변경/같은 필드 경쟁/기준 누락의 판정 테스트 통과.

### A2. Google 자동 병합

- 자동 병합과 지원 범위 내 반복 변경 자동 반영.
- 기존 검토 항목 재판정, 제외 의도 보존, 독립 항목 계속 동기화.
- 완료: 네트워크 응답 유실·재시도·동시 수정에서 중복이나 변경 유실이 없고 복구 가능.

### A3. 시간대 자동 전환

- 계정 설정을 기준으로 서버 전환 작업 구현, 기존 repin 차단 조건을 새 상태 모델로 대체.
- 완료: UTC→Asia/Shanghai, DST, 종일 일정, 반복 일정, 중단 재개, 다중 기기, 검토 잔존 상태 검증.

### A4. 화면 이동·간소화

- 공유/외부 구독을 캘린더로, AI 권한을 계정으로 이동.
- 연결 화면에는 Google 연결 관리와 상태만 유지. 수동 검토·시간대 설정의 상시 노출 제거.
- 완료: 기존 공유 링크·구독·권한 해제·OAuth 복귀가 유지되고 모바일·키보드 접근 통과.

### A5. 전환 검증·배포

- 스키마와 서버를 먼저 호환되게 배포한 뒤 자동화와 UI를 단계적으로 활성화한다.
- 기존 클라이언트가 새 기준을 잘못 덮어쓰지 않도록 호환성 또는 최소 버전 제한을 마련한다.
- 자동화 중지 시에도 미처리 원본과 복구 이력이 남아야 한다. 실패를 숨기고 성공으로 표시하지 않는다.
- 전체 CI, 기존 계정 전환, 신규 연결, 연결 해제, 복구 경로를 검증한 뒤 운영 반영.

## 8. 최종 사용자 경험

일반 설정에서 시간대를 한 번 설정하고 Google을 연결한다. 이후 수정은 자동으로 전달되고 합쳐진다. 연결 화면을 반복 방문하거나 같은 변경을 다시 승인하지 않는다. 자동으로 결정할 근거가 부족한 예외만 설명과 함께 알린다.
