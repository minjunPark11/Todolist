# 날짜 팝오버와 본문 — 이미 있는 것과, 없는 것

> 상태: **1~4단계 처리됨 · 본문 `+`만 보류** · 2026-08-31
> (§9가 1~3단계의 구현 기록이고, §10이 4단계 — 시간대 둘째 줄은 **짓지 않기로** 끝났다.
> 이 앱은 작업의 시간대를 저장하지 않는다(§10.2). 남은 것은 §6.2가 보류한 본문 줄의 `+`.)
> 대상: 작업 상세의 **날짜/시간 선택기**(스크린샷 A)와 **본문 줄의 `+`**(스크린샷 B)

## 0. 근거 수준

| 등급 | 무엇 |
|---|---|
| **관찰** | 참조 앱 스크린샷 2장 |
| **실측** | 우리 소스 — `ScheduleEditor.tsx`, `MonthCalendar.tsx`, `scheduleFormatting.ts`, `SchedulePicker.tsx` |
| **추론** | 나머지 |

> **여기서는 앱을 띄워 눌러 보지 못했다.** 브라우저 창이 0×0으로 접혀 팝오버가 열리지
> 않았고, 대신 렌더 코드를 읽어 대조했다. 치수·간격에 관한 판단은 이 문서에 없다.

---

## 1. 결론 먼저 — A는 거의 다 있다

스크린샷 A의 항목을 우리 `ScheduleEditor`와 하나씩 대면:

| 스크린샷 A | 우리 | |
|---|---|---|
| `날짜 \| 지속 시간` 탭 | `sched-tabs` (`date`/`duration`) | ✅ |
| 빠른 선택 아이콘 4개 | `QUICK_DATES` = 오늘·내일·+7일·오늘 밤 | ✅ |
| 월 달력, 월요일 시작 | `MonthCalendar` — `(getUTCDay()+6)%7`이므로 월요일 시작 | ✅ |
| `‹ ○ ›` 월 이동 | `onStepMonth` · `onShowMonth` | ✅ |
| 🕐 시간 `5:00 PM` › | `SummaryRow` time | ✅ |
| ⏰ 알림 `정각에` › | `SummaryRow` reminder | ✅ |
| ↻ 반복 › | `SummaryRow` repeat | ✅ |
| `확인` · `삭제` | `sched-confirm` · `sched-clear` | ✅ |

**즉 이 화면은 만들 것이 아니라 다듬을 것이다.** 아래가 실제로 다른 넷이다.

---

## 2. G1 — 아이콘이 이모지다

```ts
const QUICK_ICONS: Record<QuickDateKey, string> = {
  today: "☀️", tomorrow: "🌅", plus7: "📅", tonight: "🌙",
};
```

요약 줄도 `🕐` `🔔` `🔁`다. 참조 앱은 **선화(line-art) 글리프**이고, 이 앱의 나머지도
전부 SVG다(매트릭스 카드, 캘린더, 레일).

이모지의 문제는 취향이 아니다:

- **플랫폼마다 다른 그림이 나온다.** Windows·macOS·Android의 🌅는 서로 다른 그림이고,
  우리가 고른 것은 그중 하나도 아니다
- **색을 못 준다.** 나머지 아이콘은 `currentColor`를 따라 다크/라이트와 비활성 상태를
  같이 따라가는데, 이모지만 혼자 컬러로 남는다
- **글자라서 폰트 크기를 따라간다.** 20-density의 타이포 조정이 아이콘 크기를 바꾼다

**판단: SVG로 교체한다.** `+7`은 참조 앱처럼 달력 글리프 안의 숫자로 그린다 — 그 숫자가
"일주일 뒤"라는 뜻을 아이콘 하나로 말하는 유일한 방법이다.

## 3. G2 — 빠른 선택에 라벨이 있다

우리는 아이콘 + 글자(`sched-quick-label`), 참조 앱은 **아이콘만** 넉 줄 없이 한 줄.

| | 아이콘만 | 아이콘 + 라벨 (지금) |
|---|---|---|
| 폭 | 좁다 — 팝오버가 달력 폭에 맞는다 | 넓다 |
| 처음 보는 사람 | 🌙이 "오늘 밤"인지 "언젠가"인지 모른다 | 읽으면 안다 |

**판단: 라벨을 유지한다.** 참조 앱의 🌙은 실제로 우리도 스크린샷만으로는 뜻을 확정하지
못했다(§7) — 그 애매함이 정확히 라벨이 있어야 하는 이유다. 다만 라벨을 **작게, 아이콘
아래로** 내려 폭을 달력에 맞춘다.

## 4. G3 — 확인과 삭제의 자리가 반대다

참조: `[ 확인 ]` `[ 삭제 ]` — 확인이 **왼쪽**, 파란 채움.
우리: 삭제가 왼쪽, 확인이 오른쪽.

작은 차이지만 한쪽이 틀렸다. **판단: 우리 순서를 유지한다.** 파괴적인 것을 주 동작에서
멀리 두는 것이 이 앱의 다른 다이얼로그(매트릭스 편집기, 리스트 삭제)와 같은 규칙이고,
한 화면의 규칙을 참조 앱과 맞추려고 나머지 화면과 어긋나게 만들 이유가 없다.

**대신 `삭제`의 뜻은 손본다.** 우리 `sched-clear`는 "일정을 지운다"이고 참조 앱의 `삭제`도
같아 보이는데, 라벨이 작업 삭제로 읽힐 위험이 있다. `일정 지우기`가 정확하다.

## 5. G4 — 트리거가 한 줄이고, 늦었다는 말을 안 한다

참조 앱의 트리거는 **두 줄**이다:

```text
📅 5월 20일, 오후 3:00, 101d 기한지남      ← 빨강
   5월 20일, 오후 2:00, UTC+08:00         ← 회색
```

우리 `formatScheduleTrigger`는 `8월 26일 · 오후 6:00` 하나만 만든다. 빠진 것 둘:

**(1) 얼마나 늦었는지.** `101d 기한지남`은 날짜를 빼는 수고를 대신해 준다. 목록의 행은
이미 지난 날짜를 빨강으로 그리지만, 상세는 그 숫자를 말할 자리가 있다.

**(2) 원래 시간대.** 두 번째 줄은 같은 시각을 **작성된 시간대**로 다시 적은 것이다.
비행기를 타는 사람에게만 필요해 보이지만, 이 앱은 `AppSettings.timezone`을 이미 저장하고
`normalize.ts`가 "기계의 시간대는 사용자의 시간대가 아니다"라고 못박아 뒀다 — 그 구분이
화면에 나타나는 유일한 자리가 여기다.

**판단: (1)은 넣고, (2)는 열어 둔다.** 늦은 일수는 계산이 이미 있고 한 줄이면 된다.
시간대 줄은 **저장된 시간대와 표시 시간대가 다를 때만** 뜻이 있으므로, 그 조건을 먼저
정해야 한다(§7 Q2).

---

## 6. 스크린샷 B — 본문 줄의 `+`

본문 `TB 329` 아래 빈 줄 왼쪽에 `+`가 있다. 캐럿이 그 줄에 있을 때 나타난다.

**우리 본문은 `DeferredTextarea` 하나다.** 평문 `textarea`이므로 줄이라는 개념이 없고,
줄 옆에 무언가를 그릴 자리도 없다.

### 6.1 저 `+`가 무엇인지 우리는 모른다

참조 앱에서 눌러 본 화면이 없다. 가능성 셋이고 값이 전부 다르다:

| | 무엇 | 우리에게 드는 비용 |
|---|---|---|
| a | 블록 삽입 메뉴 (이미지·표·구분선) | **리치 텍스트 에디터**. `textarea`를 버리고 문서 모델을 도입하는 일 |
| b | 체크리스트 항목 추가 | 작다 — 이미 `contentMode`와 `CheckItem`이 있다 |
| c | 하위 작업 추가 | 이미 아래에 그 자리가 있다 |

### 6.2 판단 — 지금은 짓지 않는다

a라면 **이 앱이 아직 하지 않기로 한 선택**을 뒤집는 일이다. `description`은 지금 평문
문자열이고, 그것을 문서 모델로 바꾸면 동기화·검색·템플릿·변환(§11)이 전부 따라온다.
b·c라면 **이미 있는 컨트롤과 겹친다** — 본문 옆의 `+`와 아래의 `하위 작업 추가`가 같은
일을 하면 어느 쪽이 진짜인지 묻게 된다.

**어느 쪽이든 지금 지을 근거가 없다.** 참조 앱에서 그 `+`를 눌러 본 화면 한 장이면
결정되므로, §7 Q1로 남긴다.

---

## 7. 열린 질문

| | 질문 | 필요한 것 |
|---|---|---|
| Q1 | 본문의 `+`가 여는 것은 무엇인가 | 참조 앱에서 눌러 본 스크린샷 한 장 |
| Q2 | ~~시간대 둘째 줄은 언제 뜨는가 — 항상인가, 저장된 시간대가 지금과 다를 때인가~~ **다를 때만. 다만 저장하는 것이 없어 뜰 일이 없다 — §10에서 종결** | — |
| Q3 | 빠른 선택의 🌙은 "오늘 밤"인가 "언젠가"인가 | 참조 앱 확인 |
| Q4 | `지속 시간` 탭이 참조 앱에서 무엇을 그리는가 — 우리 `duration` 모드와 같은가 | 스크린샷 |

---

## 8. 단계

| | 내용 | 크기 |
|---|---|---|
| **1** | 이모지 7개를 SVG로 (G1) | 작다, 순수 표시 |
| **2** | 트리거에 늦은 일수 (G5-1) · `삭제` → `일정 지우기` (G4) | 작다 |
| **3** | 빠른 선택 라벨을 아이콘 아래로 (G2) | 작다, CSS |
| **4** | (Q2 답한 뒤) 시간대 둘째 줄 | **짓지 않았다**(§10) — 저장된 시간대가 없다 |
| **—** | 본문의 `+` | **보류** (Q1) |

1~3은 전부 표시 계층이고 `scheduleEditorReducer`를 건드리지 않는다. 그래서 되돌리기 쉽고,
스케줄 도메인의 기존 테스트가 그대로 지켜 준다.

---

## 9. 구현 기록 (2026-08-29)

### 9.1 C1 — 아이콘 (G1)

이모지 일곱을 선화로 바꿨다(`components/schedule/icons.tsx`). 그리고 **여덟 번째를
찾았다** — 트리거의 `::before { content: "🗓" }`. CSS 안의 글리프는 `currentColor`를
따라갈 수 없으므로, 트리거가 빨강이 되는 순간(§9.2) 혼자 남았을 것이다.

테스트는 마크업이 아니라 성질을 고정한다: 렌더된 에디터 안에 `Extended_Pictographic`
문자가 **하나도 없을 것**, 그리고 모든 획이 `currentColor`일 것.

### 9.2 C2 — 늦은 일수 (G4-1)

`overdueDays(schedule, today)`를 도메인에 넣고 트리거가 그것을 말한다. 판단 둘:

**범위는 끝에서 잰다.** 월요일에 시작한 사흘짜리 일은 화요일에 늦은 것이 아니다.

**끝난 일은 절대 늦었다고 하지 않는다.** 날짜는 그대로 있고 그중 하나는 지났지만, 그건
더 이상 누구의 시간에 대한 주장도 아니다 — `matrixGroupOf`가 완료된 작업을 "기한 초과"에
넣기를 거부하는 것과 같은 규칙이다. 트리거가 스케줄이 아니라 `isCompleted`에게 묻는 이유는
스케줄이 그것을 알 수 없기 때문이다.

숫자는 픽셀뿐 아니라 **접근성 이름에도** 들어간다. 눈으로만 보이는 색과 숫자는 반쪽짜리
문장이다.

`일정 삭제` → `일정 지우기`. 이 버튼이 없애는 것은 레코드가 아니라 날짜이고, `삭제`는 이
앱이 작업을 없앨 때 쓰는 말이다.

### 9.3 C3 — 라벨 (G2): 고칠 것이 없었다

§3이 "라벨을 작게, 아이콘 아래로 내린다"고 적었는데, **아이콘 아래는 이미 그랬다**
(`flex-direction: column`). 그리고 폭은 재보니 문제가 아니었다 — 앱의 실제 폰트로
`measureText`한 값:

| | 가장 긴 라벨 | 쓸 수 있는 칸 |
|---|---|---|
| 영어 | `Tomorrow` **57.5px** | **63.5px** |
| 한국어 | `오늘 밤` **39.4px** | **63.5px** |

**그래서 크기를 건드리지 않았다.** 재보지 않고 11px로 줄였다면 근거 없이 작아진 글자만
남았을 것이다.

대신 아직 닿지 않은 결함 하나를 막았다: `repeat(4, 1fr)`은 `minmax(auto, 1fr)`이라 트랙이
내용보다 작아지지 않는데, 그 내용이 `nowrap` 라벨이고 팝오버는 320px 고정이다. 지금 어떤
번역도 그 선을 넘지 않지만, 넘는 번역이 오면 **라벨이 잘리는 대신 격자가 팝오버 밖으로
나간다.** `minmax(0, 1fr)` + 말줄임이 그 가드다.

### 9.4 검증

- 유닛 2080개 통과 · `tsc` 통과 · E2E 118개 통과
- 신규: 아이콘 5개 · `overdueDays` 4개 · 트리거 6개
- **팝오버를 실제로 열어 보지는 못했다.** 브라우저 창이 0×0이라 트리거의 클릭이 전달되지
  않는다. 폭은 `measureText`로, 나머지는 마운트된 컴포넌트로 확인했다.

---

## 10. 4단계 — 시간대 둘째 줄 (2026-08-31)

### 10.1 Q2의 답, 그리고 그 답이 필요 없어진 이유

§7 Q2는 "시간대 둘째 줄은 언제 뜨는가 — 항상인가, 저장된 시간대가 지금과 다를 때인가"였다.
답은 **다를 때만**이다. 시간대가 읽는 사람의 것과 같으면 화면의 모든 시각이 이미 그
시간대이고, 그걸 작업마다 되풀이하는 것은 아무것도 한정하지 않는 단서다. 그리고 **시각이
없는 날짜에는 뜨지 않는다** — `8월 26일`은 어느 시간대에서나 같은 날이고, 모호한 것은
`오후 6:00`이다.

그런데 그 규칙을 구현하려고 저장 경로를 따라가 보니 **뜰 수 있는 경우가 하나도 없었다.**

### 10.2 이 앱에는 작업의 시간대가 없다

| | 무엇 |
|---|---|
| `Schedule.timezone` | 타입에 있다. `normalizeSchedule`이 보존하고 `scheduleEquality`가 비교한다 |
| `Task` 레코드 | **필드가 없다** (`startTime`·`endTime`만 있다) |
| `scheduleFromTask` | `timezone: null`을 **하드코딩**한다 (`taskSchedule.ts`) |
| `planScheduleUpdate` | 시간대를 쓰지 않는다 — 쓸 자리가 없다 |

즉 `Schedule.timezone`은 **값 타입이 나를 수는 있지만 이 앱이 채운 적 없는 자리**다.
트리거에 시간대 라벨을 붙였다면 모든 계정의 모든 작업에서 `null`을 받아 아무것도 그리지
않는 코드가 됐을 것이다.

**그래서 짓지 않았다.** 매트릭스 §27.3이 "보이지 않을 것밖에 만들 수 없는 컨트롤은
컨트롤이 아니다"라고 적은 것의 다른 방향이다 — 여기서는 **말할 것이 없는 라벨**이다.

### 10.3 지어 봤고, 되돌렸다 — 그 과정에서 남긴 것

`foreignZoneOf(schedule, viewerZone)`(다를 때만·시각이 있을 때만·변환 없이 IANA 이름과
`GMT+09:00` 오프셋)와 트리거 표시까지 만든 뒤에야 §10.2를 확인했다. 되돌리면서 두 가지를
남겼다.

**하나. 규칙은 이 문서에 있다.** 다시 지을 때 §10.1이 조건이고, 오프셋은 **그 일정의
날짜에서** 계산해야 한다(여름·겨울이 다르다). 그리고 **변환하지 않는다** —
`Schedule.timezone`의 주석이 벽시계 시각을 순간으로 바꾸는 일을 알림 스케줄러(Phase 11)의
몫으로 못박아 뒀고, 여기서 먼저 하면 날짜 선택기가 날짜를 하루씩 옮기기 시작한다.

**둘. 바닥에 주석을 남겼다.** `scheduleFromTask`의 `timezone: null` 옆에, 그것이 기본값이
아니라 **바닥**이라는 것과 무엇이 바뀌어야 채워지는지 적었다. 다음 사람이 같은 조사를
반복하지 않도록.

### 10.4 그리고 둘째 줄은 어차피 우리 트리거의 모양이 아니다

참조 앱의 트리거는 **블록 두 줄**이다. 우리 것은 속성 행 안의 **컨트롤**이고, 이 앱은
컨트롤 높이를 격자에 고정한다 — `e2e/verticalRhythm.spec.ts`가 모든 버튼을 재고, 40px는
레일만 쓸 수 있다. 두 줄짜리 트리거는 화면에서 유일하게 격자를 벗어난 버튼이 된다.

그러므로 언젠가 이 정보를 그린다면 자리는 **날짜 옆**이지 아래가 아니다. 늦은 일수(§9.2)가
이미 그렇게 앉아 있다.

### 10.5 검증

- 코드 변경은 주석 8줄뿐 — 유닛 2,140개 · `tsc` 통과 (동작 변경 없음)
- 되돌린 것: `foreignZoneOf`와 그 유닛 7개, 트리거 표시, CSS

### 10.6 남은 것

- **§7에서 닫힌 것은 Q2뿐이다.** Q1(본문의 `+`)은 §6.2가 "지금은 짓지 않는다"로 보류했고,
  Q3(🌙이 오늘 밤인가 언젠가인가)·Q4(`지속 시간` 탭)는 여전히 참조 앱 스크린샷 한 장이면
  끝나는 것들이다 — 우리가 판단할 수 있는 종류가 아니다
- 시간대가 실제로 존재하는 곳은 **외부 캘린더(ICS) 이벤트**다(`ExternalCalendarEvent.timezone`,
  `utils/calendarItems.ts`의 `splitByZonedDay`). 이 라벨이 정말 필요해지는 첫 자리는
  작업 상세가 아니라 캘린더일 수 있다

---

## 11. 지속 시간 탭이 날짜 탭과 같아 보였다 (2026-09-03)

### 11.1 [실측] 두 탭의 차이는 클릭의 뜻 하나뿐이었다

`ScheduleEditor`는 두 탭에 **같은 것**을 그린다: 퀵 버튼 넷, 월 달력, `시간 · 알림 · 반복`
세 줄. 다른 것은 **달력을 눌렀을 때 무슨 일이 일어나는가**뿐이다 — 날짜 탭은 한 번에
확정, 지속 시간 탭은 시작 → 끝 두 번(`selectDate`). 그 차이가 화면에 나타나는 자리는
`종료일을 선택하세요` 힌트 한 줄이고, 그것도 시작을 고른 다음에만 나온다.

**탭을 바꿨는데 화면이 그대로면 바뀐 게 없다고 읽는 것이 맞다.** 사용자가 "지금은 날짜와
지속시간이 같아"라고 한 것은 정확한 관찰이다.

### 11.2 결정 — 지속 시간 탭은 자기 화면을 갖는다

```
[ 날짜 | 지속 시간 ]
  시작        2026-09-06
  종료        2026-09-07
  하루 종일          (●)
 ────────────────────
  ⏰ 알림            >
  🔁 반복            >
 ────────────────────
 [ 지우기 ]      [ 확인 ]
```

- **퀵 버튼은 이 탭에 없다.** `오늘` · `내일` · `7일 후` · `오늘 밤`은 전부 **하루**를
  답하는 버튼이고, 기간에는 답이 되지 않는다. 날짜 탭에서는 그대로 남는다.
- **월 달력은 사라지지 않고 뒤로 간다.** 시작/종료를 누르면 이 편집기가 이미 시간·알림·
  반복에 쓰는 그 **패널**로 달력이 열린다. 열린 달력은 반대쪽 끝을 계속 칠하므로 범위가
  보이고, 두 번 클릭해서 범위를 긋던 동작은 **어느 쪽을 고치는지 말하고 하는 것**이 된다
  — 지금은 시작을 다시 누르면 끝이 조용히 지워진다(`selectDate`의 마지막 줄).
- **하루 종일은 스위치다.** 켜짐 = 시간 없음. 끄면 `시간` 줄이 나타나고, 그 줄이 기존
  `TimePanel`을 연다(그 패널은 이미 "시작은 첫날, 끝은 마지막 날"을 안다).

### 11.3 도메인은 한 줄도 안 바뀐다

`startDate` + `dueDate`가 이미 기간이고, `startTime`은 첫날에 `endTime`은 마지막 날에
붙는다(감사 결정 1-b/1-c). **"하루 종일"은 새 필드가 아니라 두 시간이 비어 있는 상태다** —
저장에 플래그를 더하면 같은 사실이 두 곳에 있게 되고, 언젠가 둘이 어긋난다.

그래서 스위치는 **파생값**이다: 켜져 있음 = `startTime === null && endTime === null`.
켜면 시간을 지우고, 끄면 시간 패널을 연다 — 시간을 고르지 않고 나오면 스위치는 도로
켜진 채다. 거짓말을 하지 않는 유일한 배선이고, "끔"이 뜻하는 것(시간이 있다)을 만들 수
있는 방법이 그것뿐이기 때문이다.

**새로 필요한 것은 하나**: 한쪽 끝만 고치는 명령. 지금은 `selectDate` 하나가 "다음 클릭"의
의미를 stage로 추론한다. 시작 칸과 끝 칸은 어느 쪽인지 **말하고** 들어오므로 추론이
필요 없고, 순서가 뒤집히면(시작 > 끝) 상대를 끌고 온다 — 명시적으로 고친 칸이
그 자리에서 무효가 되는 것보다 낫다.

### 11.4 안 하는 것

- **날짜 탭**은 손대지 않는다.
- 시작/종료를 **텍스트 입력**으로 만들지 않는다. 참조의 그 칸도 눌러서 고르는 것이고,
  우리에겐 이미 달력이 있다.
- 저장에 `allDay` 플래그를 더하지 않는다(§11.3).

### 11.5 구현 결과 (2026-09-03)

설계대로. **도메인에 더한 것은 함수 하나**(`setRangeDate`)와 **패널 두 개**(`start`/`end`)
뿐이고, 저장 모양은 한 글자도 안 바뀌었다.

`setRangeDate`가 `selectDate`와 다른 지점이 이 작업의 전부다: 그리드는 "다음 클릭이
무슨 뜻인지" stage로 추론해야 하고, 그래서 **범위가 완성된 상태에서 누르면 끝을 조용히
버린다.** 칸은 어느 쪽인지 말하고 들어오므로 추론이 없고, 가리키지 않은 쪽을 건드리지
않는다. 시험이 그 차이를 나란히 박아 뒀다.

**실제 앱에서 확인:**

| 한 일 | 결과 |
|---|---|
| `지속 시간` 탭 | 퀵 버튼·월 달력 **없음**. `시작일 / 종료일 / 하루 종일`, 그 아래 `알림 / 반복` |
| `종료일` 누름 | `‹ 종료일` 패널에 월 달력, 9월 7일 누르니 **패널이 닫히며 종료일 2026-09-07** |
| 시작일을 9월 20일로 (종료일보다 뒤) | **종료일이 20일로 끌려옴** — 하루짜리 기간 |
| `하루 종일` 스위치 (켜짐 상태에서 누름) | 시간 패널이 열림 → 오전 9:00 선택 → 뒤로 |
| 그 뒤 | 스위치 **꺼짐**, `시간 오전 9:00` 줄이 나타남 |

- 유닛 **2,382개 통과**(신규 7) · `tsc` 통과
- 문자열 넷: `schedule.rangeStart` · `rangeEnd` · `allDay` · `noDate`

---

## 12. 네 번째 단축은 `오늘 밤`이 아니라 `다음 달`이다 (2026-09-03)

### 12.1 무엇이 바뀌나

`오늘 / 내일 / 7일 후 / 오늘 밤` → `오늘 / 내일 / 7일 후 / **다음 달**`.

넷은 **오늘로부터의 거리**로 읽히는 줄이었는데(오늘 → 내일 → 일주일), 마지막 하나만
거리가 아니라 **그날의 시각**이었다. `다음 달`은 그 줄을 끝까지 같은 축으로 만든다.

### 12.2 따라 나온 정리 — 시계가 통째로 빠진다

`오늘 밤`은 넷 중 **유일하게 시간을 쓰는** 단축이었다(§5.10: 나머지 셋은 이미 있는 시간을
건드리지 않는다). 그것이 없어지면서 같이 나간 것:

- `tonightTime()` — 18시, 저녁이 시작된 뒤에는 다음 30분, 23시부터는 23:59. **이미 지난
  시각으로 예약되는 것**을 막던 반올림이었다.
- `withTonightTime()` — 09:00–11:00 블록을 저녁으로 옮길 때 11:00 종료를 버리던 규칙.
- `QUICK_DATE` 액션의 **`now` 인자**와 편집기의 `nowTime()`. 넷 다 날짜만 답하므로 지금
  몇 시인지 묻는 단축이 하나도 없다.

### 12.3 `다음 달`은 **한 달 뒤 같은 날**이다 (경계 하나 주의)

나머지 셋이 전부 오늘 기준 오프셋이므로 이것도 그렇게 읽는다. 다만 `Date.setMonth`로
쓰면 **1월 31일 + 1달 = 3월 3일**이 된다 — 2월이 넘쳐서 그 넘침이 다음 달로 밀린다.
"다음 달" 버튼이 두 달 뒤를 답하면 없는 것만 못하므로, **그 달의 마지막 날로 자른다**:

| 오늘 | 결과 |
|---|---|
| 1월 31일 | 2월 28일 (윤년이면 29일) |
| 3월 31일 | 4월 30일 |
| 12월 15일 | **다음 해** 1월 15일 |

### 12.4 아이콘 — 달이 아니라 달력

`오늘 밤`은 초승달이었다. 이제 이 버튼도 **날짜**를 답하므로 옆의 셋과 같은 종류의
그림이어야 한다: `7일 후`가 쓰는 달력에 숫자 대신 **다음을 가리키는 꺾쇠**를 넣었다
(`CalendarNextMonthIcon`). 넷이 오늘 → 내일 → 일주일 → 한 달로 **한 방향**을 가리킨다.

### 12.5 검증

- 유닛 **2,380개 통과** · `tsc` 통과. 시험도 함께 옮겼다 — `tonightTime`의 시험 셋이
  나가고, **잘라내기 경계 셋**(1/31, 윤년, 12월)이 들어왔다
- 실제 앱(dev, ko): 오늘이 9월 3일일 때 `다음 달`을 누르면 격자가 **2026년 10월**로
  넘어가고 3일이 선택된다. 시간은 건드리지 않는다
