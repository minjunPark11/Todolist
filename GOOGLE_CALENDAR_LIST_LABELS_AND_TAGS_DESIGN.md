# 구글 캘린더 — 리스트는 라벨로, 태그는 설명으로

> 상태: **M3 코드 구현 · 배포/실계정 검증 전** · 2026-09-08 (L1~L7 전부 사용자 결정 — §2)
> 요청: "Google에는 FocusFlow 캘린더 하나만 만든다. FocusFlow List → Google custom label +
> 동일한 색상. Tag → description 하단에 표시. 제목에는 어떤 접두사도 붙이지 않는다."
> 대상: `domain/calendar/googleSync/eventShape.ts` · `domain/calendar/googleSync/labelPlan.ts`(신규) ·
> `domain/calendar/googleSync/tagBlock.ts`(신규) · `lib/googleCalendarOutbound.ts` ·
> `hooks/useGoogleOutboundSync.ts` · `types.ts`
> 선행 문서: `GOOGLE_CALENDAR_SYNC_DESIGN.md` — 이 문서는 그 설계의 **§2 "구글에 못 산다"
> 칸을 두 칸만 줄인다.** 나머지 규칙(전용 캘린더 · LWW · delete wins · 툼스톤)은 그대로 상속한다.

## 0. 근거 수준

### 구현 시 확인 및 보완 (2026-09-08)

- [공식 Calendars 규격](https://developers.google.com/workspace/calendar/api/v3/reference/calendars) 확인: 클라이언트가 UUID를 지정할 수 있고, 생략하면 Google이 발급한다. 구현은 리스트 ID에서 일정하게 만드는 앱 전용 UUID를 사용한다. 이름은 API 한도인 50자까지 보낸다.
- `calendars.get → update`에서 기존 캘린더 속성과 사용자가 만든 라벨을 보존한다. `If-Match` 충돌은 한 번 다시 읽고 재시도한다. 생성 응답에 라벨이 실제로 있는지 확인한 후에만 매핑을 저장한다.
- 모든 400/403을 미지원으로 저장하지 않는다. 할당량·권한·입력 오류는 재시도 대상으로 남기고, 라벨 기능 미지원이라는 명시적 응답만 `labels_supported=false`로 저장한다. 수동 동기화와 재연결은 다시 검사한다.
- 이벤트의 insert/patch 요청에만 지원 시 `eventLabelVersion=1`을 붙인다. GET/DELETE에는 불필요한 매개변수를 추가하지 않는다.
- `googleMetadataKey`로 마지막 전송한 태그/라벨 내용을 기록한다. 기존 일정도 한 번 갱신하며, Task의 수정 시각이 바뀌지 않는 태그 레코드 수정도 감지한다. 다음 패스에서 동일한 내용은 다시 보내지 않는다.
- 현재 로그인 계정의 원격 데이터 로드가 성공한 뒤에만 라벨/이벤트 전송을 시작한다. 초기 빈 데이터나 이전 계정의 리스트를 기준으로 라벨을 정리하지 않는다.
- 공백이 포함된 태그가 파서 규칙을 깨뜨리지 않도록 공백과 `%`를 percent-encoding한다 (`#two%20words`). 일반 한글 태그는 그대로 보인다. 태그 블록을 다시 쓰기 전에 기존 블록을 제거하며, 마지막 태그를 지우면 설명을 명시적으로 갱신한다.
- 200개 한도에는 사용자 라벨도 포함한다. 남은 자리에 최근 수정한 리스트부터 넣고 초과 개수를 안내한다. 앱에는 별도의 리스트 최근 사용 시각이 없어 `updatedAt`을 기준으로 사용한다.
- 현재 M2는 Google 설명을 **ExternalCalendarEvent**로 읽으며 **Task 본문으로 되돌려 쓰지 않는다**. 따라서 외부 일정의 본문은 그대로 보존한다. Task로 가져오는 경로를 추가할 때는 §4.5의 `stripTagBlock`을 반드시 적용해야 한다. 현재 아웃바운드는 이미 이 제거 함수를 거친다.
- 배포 전 `supabase/migrations/020_google_calendar_labels.sql` 적용이 필요하다. v0.22.12의 마이그레이션 019도 선행되어야 한다. 실계정 롤아웃 지원 여부는 아직 검증하지 않았다.

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 코드에서 잰 것 — 아웃바운드가 보내는 필드는 4개뿐이다 · `Project.color`는 hex다 · `normalizeProject`가 미지 필드를 spread한다 · `Task.tags`는 이름이고 시스템 마커가 섞여 있다 | [실측] |
| **결정** | 사용자 — 캘린더 하나 유지 · 리스트는 라벨 · 태그는 설명 하단 · 제목 무가공 · 폴백은 색 없음 | [결정] |
| **규격** | Google Calendar API — `labelProperties.eventLabels[]` · `eventLabelId` · `eventLabelVersion=1` (2026-07-01 출시) | [규격] |
| **미확인** | 개인(consumer) 계정 롤아웃 범위 · 라벨 id를 우리가 정하는지 구글이 발급하는지 | [미확인] |

---

## 1. 지금 구글에 나가는 것 — 네 개뿐이다 [실측]

`toGoogleEventBody`(`domain/calendar/googleSync/eventShape.ts:165`)가 만드는 본문 전부:

| 필드 | 출처 |
|---|---|
| `summary` | `task.title` |
| `description` | `task.description` (있을 때만) |
| `start` / `end` | `dueDate` + `startTime`/`endTime`, 시간 없으면 종일 |
| `recurrence` | RRULE, 단방향 (§8) |

`tags` · `projectId` · `priority`는 `SyncableTask` 인터페이스(`eventShape.ts:17`)에 **읽는
필드로 들어있지도 않다.** 그래서 지금은 "업무" 리스트의 `#급함` 태스크가 구글에서 제목만
있는 이벤트 하나로 뜨고, 모든 리스트가 FocusFlow 캘린더 기본색 한 가지로 같아 보인다.

선행 문서 §2가 리스트와 태그를 "구글에 못 산다" 칸에 넣은 것은 **당시에는 사실이었다.**
이벤트 색은 `colorId` 11색 고정 팔레트뿐이었고, 임의 hex를 받지 않았다. 그 전제가
2026-07-01에 바뀌었다 — §3.1.

`extendedProperties.private`에 우겨넣는 안은 선행 문서 §2가 이미 기각했고, **이 문서도
기각을 유지한다.** 이 설계가 쓰는 것은 전부 구글 UI에서 보이고 손댈 수 있는 필드다.

---

## 2. 결정 요약 [결정]

| # | 결정 | 근거 절 |
|---|---|---|
| **L1** | 전용 캘린더는 **여전히 하나**. 리스트별 캘린더로 가르지 않는다 | §3.6 |
| **L2** | 리스트 → **이벤트 라벨**. 라벨 이름 = 리스트 이름, 라벨 색 = 리스트 색 그대로 | §3 |
| **L3** | 라벨을 못 쓰는 계정은 **색 없음**. `colorId` 11색 근사로 떨어지지 않는다 | §3.7 |
| **L4** | 라벨은 **FocusFlow가 소유**한다. 구글에서의 라벨 변경은 들여오지 않는다 | §3.8 |
| **L5** | 태그 → `description` **하단 블록**, 고정 ASCII 마커로 감싼다 | §4 |
| **L6** | 태그 블록은 **두 조건을 모두 만족할 때만** 잘라낸다 (마커 + 태그 모양) | §4.2 |
| **L7** | 제목에는 **아무것도 붙이지 않는다** | §5 |

---

## 3. 리스트 → 라벨

### 3.1 구글 쪽 규격 [규격]

2026-07-01 출시된 Labels API. 캘린더마다 **자기 팔레트를 정의**하고 그 항목을 이벤트에
붙인다. 기존 `colorId`의 11색 고정 팔레트를 대체한다.

| 무엇 | 어디 | 비고 |
|---|---|---|
| 라벨 정의 | `Calendars` 리소스의 `labelProperties.eventLabels[]` | `calendars.update` — 캘린더당 최대 **200개** |
| 라벨 한 개 | `{ id, backgroundColor, name }` | `backgroundColor`는 **hex**. `name`은 선택 |
| 이벤트에 부착 | `Events`의 `eventLabelId` | 단수 — **이벤트 하나에 라벨 하나** |
| 활성화 | 모든 요청에 `eventLabelVersion=1` | **켜면 `colorId`는 무시된다** |

두 가지가 이 설계를 결정한다:

- **hex를 그대로 받는다.** `Project.color`가 `#007AFF` 형태의 hex다
  (`domain/plannerData/normalize.ts:259`). 근사 없이 "동일한 색상"이 성립한다.
- **이벤트당 라벨은 하나다.** 그래서 리스트와 태그가 라벨 자리를 두고 경쟁할 수 없다.
  리스트가 라벨을 갖고 태그가 설명으로 가는 배분은 취향이 아니라 **규격이 정한 것**이다.

### 3.2 우리 쪽 매핑 필드 [결정]

`Project`에 `googleLabelId: string` 하나를 더한다. 선행 문서 §4.2가 `Task`에
`googleEventId`를 더한 것과 같은 자리, 같은 이유다.

```ts
export interface Project {
  // …
  /** 이 리스트를 나타내는 구글 이벤트 라벨. "" = 아직 안 만들어짐. */
  googleLabelId?: string;
}
```

`normalizeProject`가 `...project`를 먼저 spread하므로(`normalize.ts:254`) **테이블 변경도,
마이그레이션도 없다.** 이 필드를 모르는 구버전 클라이언트는 지우지 않고 실어 나른다 — M0.

상태 머신은 두지 않는다. `googleLabelId`의 유무가 곧 "라벨이 만들어졌는가"이고, 없으면
다음 패스가 만든다. `Task.googleEventId`와 똑같은 암묵적 재시도다.

### 3.3 라벨 id는 누가 정하는가 [미확인]

라벨 id가 UUID 형식이라는 것만 확인됐고, **우리가 정해서 보내는지 구글이 발급하는지는
착수 시점에 확인해야 한다.** 어느 쪽이든 설계는 서지만 코드가 달라진다:

| 경우 | 하는 일 |
|---|---|
| 우리가 정한다 | `projectId`에서 결정론적으로 UUID를 만들어 쓴다. 두 기기가 같은 리스트에 같은 라벨을 만든다 |
| 구글이 발급한다 | `calendars.update` **응답**에서 읽어 `Project.googleLabelId`에 적는다 |

후자를 기본으로 짠다. 전자로 밝혀지면 코드가 줄어들 뿐 규칙은 그대로다. **어느 쪽이든
진실은 `Project.googleLabelId`에 적힌 id 하나이고, 이름으로 라벨을 다시 찾지 않는다** —
리스트 이름은 바뀌는 값이라 키가 될 수 없다.

### 3.4 색 변환

`Project.color`는 hex지만 **6자리라는 보장이 없다.** 저장소에 `#000` 같은 3자리가 있다
(`domain/spaces/lifecycle.test.ts:196`). 구글에 보내기 전에 정규화한다:

- 3자리 → 6자리로 확장 (`#0a5` → `#00aa55`)
- 6자리 → 그대로
- 그 외 (빈 값 · 잘못된 값) → `normalizeProject`의 기본색 `#007AFF`

순수 함수 하나(`labelPlan.ts`의 `toHex6`)로 두고 픽스처로 증명한다. 색 변환이 네트워크
너머에서 처음 검증되는 일은 없어야 한다.

### 3.5 라벨 패스 — 이벤트보다 먼저 돈다

라벨은 **Calendars 리소스 쓰기**다. 지금 `runOutbound`는 events 엔드포인트만 안다
(`lib/googleCalendarOutbound.ts:93`). 그래서 패스가 하나 늘고, **반드시 이벤트 쓰기보다
먼저** 돈다 — 존재하지 않는 라벨 id를 이벤트에 붙일 수 없기 때문이다.

```
planLabels(projects, calendar.labelProperties)  ← 순수
        ↓  calendars.update (필요할 때만)
  Project.googleLabelId 패치 기록
        ↓
planOutbound(tasks, tombstones)                 ← 기존
        ↓  events.insert / patch / delete
```

`planLabels`가 내는 것은 세 더미다:

| 더미 | 언제 | 하는 일 |
|---|---|---|
| `create` | 살아 있는 리스트인데 `googleLabelId`가 없다 | 라벨 추가 |
| `update` | id는 있는데 이름이나 색이 구글 쪽과 다르다 | 라벨 수정 |
| `remove` | 라벨은 있는데 리스트가 삭제/보관됐다 | 라벨 제거 |

**호출 절약이 `isEmptyPlan`과 같은 방식으로 필요하다.** `labelProperties`는 배열 통짜
업데이트라, 계산한 배열이 구글이 들고 있는 배열과 같으면 요청을 아예 보내지 않는다.
그렇지 않으면 창에 포커스가 갈 때마다 캘린더를 쓰게 된다.

`remove`는 **라벨만 지운다.** 그 라벨을 달고 있던 이벤트는 건드리지 않는다 — 리스트를
지웠다고 일정을 지우는 것은 선행 문서 §7.1이 금지하는 추론이다. 라벨을 잃은 이벤트는
색 없는 이벤트가 된다.

### 3.6 왜 캘린더를 가르지 않는가 [결정]

리스트별 캘린더가 색을 나누는 다른 방법이지만, **태스크가 리스트를 옮길 때 이벤트를
삭제하고 다시 만들어야 한다.** `googleEventId`가 갈아엎어지고, 그 id에 매달린 `googleEtag`
· `googleSyncedAt` · 툼스톤이 전부 리셋된다. 리스트 이동이 동기화 상태를 초기화하는 셈이다.

라벨은 `eventLabelId` 한 필드 PATCH다. 게다가 선행 문서 §4.1이 전용 캘린더 하나에서 얻은
성질 — 경계가 눈에 보이고, 체크 하나로 통째로 끌 수 있다 — 이 그대로 유지된다.

### 3.7 라벨을 못 쓰는 계정 — 색 없음 [결정]

Labels API는 나온 지 두 달이고, **개인 계정 전면 롤아웃 여부가 문서로 확인되지 않는다**
[미확인]. 판정과 폴백이 필요하다.

- **판정**: 첫 라벨 패스의 `calendars.update`가 400/403으로 거절되면 라벨 미지원으로 본다.
  결과를 `google_calendar_connections`에 `labels_supported`로 적어 **매 패스마다 다시 묻지
  않는다.** 재연결하거나 카드에서 수동 동기화를 누르면 다시 시험한다.
- **폴백은 색 없음이다.** `colorId` 11색 근사로 떨어지지 않는다. 근사색은 리스트 색과
  미묘하게 다른 색을 내놓고, 그건 "왜 색이 틀리지"라는 버그 리포트만 만든다. 색이 없으면
  적어도 **왜 없는지 설명할 수 있다.**
- 미지원일 때 `eventLabelVersion=1`을 **보내지 않는다.** 켠 채로 두면 `colorId`가 무시되는
  규격이라 어차피 색을 못 쓰지만, 지원 여부에 따라 요청 모양이 갈리는 편이 정직하다.
- 설정 카드에 한 줄 나온다: *"이 구글 계정은 이벤트 라벨을 지원하지 않아 리스트 색이
  표시되지 않습니다. 일정은 정상적으로 동기화됩니다."*

태그 블록(§4)은 **이 판정과 무관하게 항상 나간다.** 설명 필드는 늘 있었다.

### 3.8 라벨은 단방향이다 [결정]

선행 문서 P3이 "구글에서 온 것은 `ExternalCalendarEvent`, `Task` 모델을 건드리지 않는다"고
정했다. 그래서 구글에서 이벤트의 라벨을 바꿔도 **태스크의 리스트는 옮겨가지 않는다.**

되돌려놓지도 않는다. `needsUpdate`가 `updatedAt > googleSyncedAt`일 때만 PATCH하므로
(`outboundPlan.ts:68`), 태스크를 건드리지 않는 한 어긋난 채로 남는다. 다음번에 그 태스크를
편집하는 순간 우리 라벨로 덮인다.

이건 버그가 아니라 **정해야 할 규칙**이고, 여기서 정한다: **라벨은 FocusFlow가 소유한다.**
구글에서 바꾼 라벨은 반영되지 않으며, 다음 편집 때 덮인다. 연결 시점 고지(선행 문서 Q3)에
한 줄 추가한다.

---

## 4. 태그 → description 하단

### 4.1 왜 여기가 위험한가 [실측]

`toGoogleEventBody`는 **매번 description을 통째로 다시 쓴다**(`eventShape.ts:169`). 태그
줄을 붙이는 순간 description은 "사용자가 쓴 글 + 앱이 만든 파생 데이터"가 한 필드에 섞인
상태가 된다. 그 상태에서 두 가지가 따라온다:

1. **지금**: 구글에서 태그 줄을 지워도 다음 편집 때 되살아난다. 지워지지 않는 줄이다.
2. **M2(인바운드 되돌려쓰기)를 붙이면**: 태그 줄이 태스크 본문으로 들어가고, 다음
   아웃바운드가 그 위에 또 태그 줄을 붙인다. **누적 중복**으로 터진다.

그래서 **파서를 먼저 정한다.** 지금 이 규칙을 박아두면 M2가 안전해지고, 안 박아두면 M2에서
반드시 데이터가 더럽혀진다. 이 절이 이 문서에서 가장 중요한 부분이다.

### 4.2 마커 규격 [결정]

블록은 설명의 **맨 끝**에 붙고, 이 모양이다:

```
<사용자가 쓴 본문>

--- FocusFlow ---
#급함 #기획
```

정확히:

| 요소 | 값 | 왜 |
|---|---|---|
| 앞 여백 | `\n\n` | 본문과 붙지 않게 |
| 마커 줄 | `--- FocusFlow ---` | **고정 ASCII. 절대 번역하지 않는다** |
| 태그 줄 | `#` 붙은 이름을 공백으로 이은 한 줄 | 사람이 읽고, 파서가 검증한다 |

**마커를 번역하지 않는 것이 규격의 핵심이다.** 언어 설정을 바꿨다고 마커가 달라지면 그
이전에 쓴 블록이 전부 고아가 되고, 파서가 못 찾은 블록 위에 새 블록이 쌓인다. 이 문자열은
UI 문자열이 아니라 **데이터 구분자**이므로 카탈로그에 넣지 않는다.

### 4.3 파서 — 두 조건을 모두 만족할 때만 잘라낸다 [결정]

`tagBlock.ts`가 두 함수를 낸다. 순수하고, 서로 역이다.

```ts
/** 본문 + 태그 → 구글로 보낼 설명. 태그가 없으면 본문 그대로. */
export function withTagBlock(description: string, tags: readonly string[]): string;

/** 구글에서 읽은 설명 → 사용자 본문만. 블록이 없으면 그대로. */
export function stripTagBlock(description: string): string;
```

`stripTagBlock`이 무언가를 잘라내려면 **둘 다** 참이어야 한다:

1. 마커 줄이 있고, 그 뒤로 **딱 한 줄만** 남는다 (블록이 문자열의 끝이다)
2. 그 한 줄이 태그 모양이다 — `^#\S+( #\S+)*$`

하나라도 어긋나면 **아무것도 자르지 않는다.** 사용자가 우연히 `--- FocusFlow ---`라고 쓴
줄을 중간에 넣었다면 조건 1이 막고, 마커 뒤에 산문을 이어 썼다면 조건 2가 막는다. 잘못
자르는 것보다 못 자르는 것이 낫다 — 못 자르면 블록이 하나 더 붙어 눈에 띄고, 잘못 자르면
사용자가 쓴 글이 조용히 사라진다.

여러 개가 쌓인 경우(구버전이 만든 블록 위에 새 블록이 붙은 경우) **끝에서부터 반복해서
벗긴다.** 한 번의 `strip`이 남긴 결과가 다시 조건을 만족하면 또 벗긴다. 이미 더럽혀진
데이터를 스스로 청소하는 경로다.

태그가 하나도 없으면 `withTagBlock`은 **블록을 붙이지 않고, 있던 블록을 없앤다.** 마지막
태그를 지운 태스크의 설명에 빈 마커가 남으면 안 된다.

### 4.4 어느 태그가 나가는가 [실측]

태그는 이 저장소에 **두 형태로 산다.** `TaskTag` 조인이 정본이고(§13.32), `Task.tags`의
문자열 배열이 구버전 호환으로 남아 있다(`domain/tags/tags.ts:10`). 게다가 `Task.tags`에는
`space:<id>` · `group:<id>` 같은 **레거시 마커가 섞여 있다** — 지우면 모든 태스크를 다시
써야 해서 일부러 남겨둔 문자열이다(`tags.ts:24`).

그러므로 블록에 들어가는 태그는:

1. `TaskTag` 링크가 가리키는 `Tag.name` — 정본
2. 더하기 `Task.tags` 중 `isUserTag(name)`이 참인 것 — 조인이 아직 안 생긴 구버전 데이터
3. `tagKeyFor`로 중복 제거 (대소문자·공백 차이는 같은 태그다)
4. 이름 순 정렬

**4번이 없으면 블록이 매 패스마다 순서를 바꿔** 쓸데없는 PATCH를 만든다. 정렬은 성능
문제이지 미관 문제가 아니다.

`isUserTag`를 통과 못 한 `space:`/`group:` 마커가 구글 설명에 나타나는 일은 없어야 한다.
사용자가 만든 적 없는 문자열이다.

### 4.5 M2가 이걸 어떻게 읽어야 하는가

M2가 구글의 설명 편집을 태스크로 들여올 때 **반드시 `stripTagBlock`을 먼저 통과시킨다.**
비교도 저장도 벗겨낸 본문으로 한다.

따라서 M2 시점의 규칙은: **설명은 양방향, 태그 블록은 단방향.** 구글에서 태그 줄을 고쳐도
태스크의 태그는 바뀌지 않는다. 라벨(§3.8)과 같은 이유이고, 같은 문장으로 고지한다.

---

## 5. 제목 — 아무것도 붙이지 않는다 [결정]

`[업무] 기획서 마감` 같은 접두사는 **기각한다.** 제목은 구글에서도 편집되는 필드이고,
접두사를 붙이면 "사용자가 구글에서 지움 → 다음 패스가 되살림"이라는 끝나지 않는 싸움이
생긴다. 라벨 색이 이미 그 일을 하므로 접두사는 순수 손해다.

`summary`는 `task.title` 그대로다. 이 문서는 그 줄을 건드리지 않는다.

---

## 6. 무엇이 바뀌는가

| 파일 | 변화 |
|---|---|
| `types.ts` | `Project.googleLabelId?: string` 한 필드. 테이블 변경 없음 (§3.2) |
| `domain/calendar/googleSync/tagBlock.ts` | **신규.** `withTagBlock` · `stripTagBlock` (§4.3) |
| `domain/calendar/googleSync/labelPlan.ts` | **신규.** `planLabels` · `toHex6` (§3.4, §3.5) |
| `domain/calendar/googleSync/eventShape.ts` | `SyncableTask`에 태그·라벨 입력 추가. `description`에 블록, `eventLabelId` 부착 |
| `lib/googleCalendarOutbound.ts` | `calendars.update` 경로 추가. 모든 이벤트 요청에 `eventLabelVersion=1` (지원 계정에 한해) |
| `hooks/useGoogleOutboundSync.ts` | 라벨 패스를 이벤트 패스 앞에 끼우고, `Project` 패치를 결과로 올려보냄 |
| `lib/googleCalendar.ts` | `GoogleConnection`에 `labelsSupported` (§3.7) |
| `supabase/migrations/` | `google_calendar_connections.labels_supported boolean` 한 칼럼 |
| `components/calendar/GoogleCalendarCard.tsx` | 라벨 미지원 안내 한 줄 (§3.7) |

`eventShape.ts`의 순수성은 유지한다. 태그 이름 목록과 라벨 id는 **이미 해결된 값으로**
넘어온다 — 이 파일이 `TaskTag` 조인을 뒤지기 시작하면 픽스처로 증명하던 성질을 잃는다.

---

## 7. 실패 모드

| 무엇 | 결과 | 대응 |
|---|---|---|
| `calendars.update` 실패 | 라벨이 안 생김. 이벤트는 라벨 없이 나감 | 다음 패스가 다시 만든다 (`googleLabelId`가 비어 있으므로) |
| 라벨 미지원 계정 | 색 없음 | §3.7 — 한 번 판정하고 카드에 고지 |
| 리스트 200개 초과 | 구글이 거절 | 초과분은 라벨 없이. 카드에 고지 (§9 Q3) |
| 사용자가 구글에서 라벨 변경 | 어긋난 채 남았다가 다음 편집 때 덮임 | §3.8 — 규칙으로 정함 |
| 사용자가 구글에서 태그 줄 삭제 | 다음 편집 때 되살아남 | §4.1 — 알려진 성질 |
| 사용자가 본문에 마커와 같은 줄을 씀 | 조건 1·2가 막는다 (§4.3) | 잘못 자르느니 안 자른다 |
| 이미 중복 블록이 쌓인 설명 | 반복 strip이 청소 (§4.3) | 자동 복구 |

---

## 8. 단계

### M3 — 라벨과 태그

| # | 항목 | 선행 |
|---|---|---|
| 1 | `tagBlock.ts` — 마커 · 파서 · 라운드트립 테스트 (§4.2~4.4) | 없음. 순수 |
| 2 | `labelPlan.ts` — `toHex6` · `planLabels` 세 더미 (§3.4, §3.5) | 없음. 순수 |
| 3 | `Project.googleLabelId` + 정규화 (§3.2) | 없음 |
| 4 | 라벨 id 발급 방식 확인 후 `calendars.update` 실행기 (§3.3) | 1·2·3 |
| 5 | `eventShape`에 블록·라벨 결합, `eventLabelVersion=1` (§6) | 4 |
| 6 | 미지원 판정 · 폴백 · 카드 고지 (§3.7) | 5 |

**1과 2가 먼저 가는 것은 우연이 아니다.** 둘 다 순수하고 구글 계정 없이 픽스처로 증명된다.
선행 문서 M1이 `eventShape`와 OAuth를 그 순서로 쌓은 것과 같은 이유다.

### M2와의 순서

M3은 **M2(인바운드)와 독립이다.** 둘 다 아웃바운드 위에 얹히고 서로를 기다리지 않는다.
다만 M2가 먼저 오면 §4.5의 `stripTagBlock` 통과를 **그때 반드시 넣어야 한다** — 안 넣으면
§4.1의 두 번째 시나리오가 그대로 일어난다.

---

## 9. 열린 질문

| # | 질문 | 기본값 제안 |
|---|---|---|
| Q1 | 라벨 id를 우리가 정하는가 구글이 발급하는가 (§3.3) | 발급받아 적는 쪽으로 짜고, 착수 시 규격 확인 |
| Q2 | 개인 계정에서 Labels API가 되는가 (§3.7) | 판정 코드로 답한다. 문서를 기다리지 않는다 |
| Q3 | 리스트가 200개를 넘으면 무엇을 버리는가 | 최근 사용 순으로 200개. 실제로 닿을 일이 없다 |
| Q4 | 태그 블록을 **끄는** 설정이 필요한가 | v1에는 두지 않는다. 필요해지면 카드에 토글 하나 |
| Q5 | 보관(archived)된 리스트의 라벨을 지울 것인가 | 지운다 (§3.5 `remove`). 이벤트는 그대로 둔다 |
