# TickTick 스타일 아이젠하워 매트릭스 — 설계

> 상태: **Phase 1~6c 구현됨 · 열린 질문 없음** · 2026-08-31
> (§16~§30이 각 단계의 구현 기록이다. §15의 다섯 질문은 모두 닫혔다 — Q1은 §27, Q2·Q3은
> Phase 1, Q4는 §28, Q5는 §30.)
> 근거: TickTick 데스크톱 앱 **스크린샷 2장** (매트릭스 전체 화면, 사분면 ⋯ 메뉴)
> 결정됨: **D1 = 우선순위 모델**, **D2 = 완료를 각 칸에 표시** (사용자 확정)
> 이 문서의 앱 쪽 주장은 전부 `file:line`으로 근거를 단다.

## 0. 근거 수준에 관한 경고

Component 01~14 문서들은 실제 TickTick 웹을 열어 `getBoundingClientRect()`로 잰
결과다. **이 문서는 그렇지 않다.** 스크린샷 두 장을 읽은 것이고, 거기서 보이는 것만
관찰이며 나머지는 추론이다.

| 표기 | 뜻 |
|---|---|
| **[관찰]** | 스크린샷에 실제로 보인다 |
| **[추론]** | 보이는 것에서 논리적으로 따라 나온다. 틀릴 수 있다 |
| **[미확인]** | 스크린샷으로는 알 수 없다. §14에 모아 둔다 |

픽셀 값(패딩·높이·색상 코드)은 **하나도 재지 못했다.** 시각 사양이 필요하면 실제
앱을 띄워 Component 문서들과 같은 방식으로 따로 재야 한다.

---

## 1. 가장 중요한 발견

**TickTick의 사분면은 "중요×긴급"이 아니다. 우선순위 하나다.**

우리 앱은 두 축에서 사분면을 파생한다 (`src/utils/eisenhower.ts:32,36`):

```
중요 = priority가 high 또는 medium
급함 = dueDate가 오늘이거나 지남
```

TickTick은 그렇지 않다. 증거 셋:

| # | [관찰] | 두 축 모델이라면 | 실제 |
|---|---|---|---|
| 1 | Q2(중요·안급함)에 `LAC Final exam` — **기한 초과** 그룹 | 기한이 지났으니 급함 → Q1 | Q2 |
| 2 | Q4(둘 다 아님)에 `cv english 작성` — **오늘** 그룹 | 오늘 마감이니 급함 → Q3 | Q4 |
| 3 | Q3(안중요·급함)에 `LAC Lab 5`·`AIM Lecture 6` — **날짜 없음** 그룹 | 날짜가 없으면 급할 수 없음 | Q3 |

결정적인 것은 체크박스 색이다 [관찰]: Q2 항목은 노랑, Q3는 파랑, Q4는 회색. TickTick의
체크박스 색은 우선순위 색이다. 사분면과 체크박스 색이 정확히 1:1로 대응한다.

```
Q1 = 우선순위 높음    Q2 = 중간    Q3 = 낮음    Q4 = 없음
```

날짜는 **배치에 전혀 관여하지 않는다.** 사분면 *안에서* 그룹으로만 쓰인다.

그래서 ⋯ 메뉴에 **편집**이 있다 [관찰] — 각 사분면이 사용자가 고칠 수 있는 필터이기
때문이다. 파생 규칙이라면 편집할 것이 없다.

---

## 2. 스크린샷에서 읽은 것 전부

### 2.1 레이아웃

[관찰]

- 2×2 그리드. 각 칸은 독립된 카드.
- 칸 헤더 = 로마숫자 뱃지(색) + **설명형 제목**. "중요하고 급한 일" — 행동 지시가 아니라
  분류의 이름이다.
- 색: Ⅰ 빨강 · Ⅱ 주황 · Ⅲ 파랑 · Ⅳ 초록.
- **축 라벨이 없다.** "급함 / 안 급함"을 화면 어디에도 쓰지 않는다. 칸 이름이 그 일을 한다.
- **칸 헤더에 개수가 없다.** 개수는 칸 *안의 그룹* 헤더에 붙는다.
- `+` 와 `⋯` 는 스크린샷에서 **Q1에만 보인다** → 호버한 칸에만 나타난다 [추론].

### 2.2 칸 안의 그룹

[관찰] 각 칸의 작업은 접히는 그룹으로 묶인다. 그룹 헤더 = 꺾쇠(∨) + 이름 + 개수.

관찰된 그룹과 순서:

| 칸 | 그룹 |
|---|---|
| Ⅰ | 완료 5 |
| Ⅱ | 기한 초과 3 → 완료 5 |
| Ⅲ | 기한 초과 1 → 날짜 없음 13 |
| Ⅳ | 기한 초과 1 → 오늘 1 → 날짜 없음 3 → 완료 5 |

순서 규칙 [추론]: **기한 초과 → 오늘 → 날짜 없음 → 완료.** 완료는 언제나 마지막.

⋯ 메뉴가 "그룹화하기: **날짜**"라고 말하므로, 이 그룹들은 날짜 축의 값이다 [관찰].

### 2.3 완료 그룹

[관찰] Q1의 완료 그룹은 5개를 보여주고 그 아래 **추가 보기**가 있다. 항목들은 흐리게,
체크박스는 채워진 상태.

[추론] 완료는 5개씩 끊어 보여주고 나머지는 "추가 보기"로 더 부른다. 헤더의 "5"는 전체
개수가 아니라 현재 보이는 개수일 가능성이 크다 — 전체 개수라면 5개를 다 보여준 뒤
"추가 보기"가 있을 이유가 없다.

### 2.4 작업 행

[관찰] 왼쪽부터: 체크박스(우선순위 색 테두리) · 제목 · …여백… · 리스트 이름(학교/기본함) ·
아이콘 · 날짜.

- 아이콘: 📄(메모 있음), ↻(반복) — `SU Meeting`에 반복 아이콘이 보인다.
- 날짜는 **기한이 지났으면 빨강**. 아니면 회색("오늘"은 파랑).
- 완료 행은 전체가 흐려지고 리스트 이름만 남는다.

### 2.5 ⋯ 메뉴 (스크린샷 2)

[관찰] 네 줄. 각 줄 = 아이콘 + 라벨 + (오른쪽) 현재값 + 꺾쇠.

| 줄 | 아이콘 | 현재값 | 꺾쇠 |
|---|---|---|---|
| 편집 | ✏️ | — | 없음 |
| 그룹화하기 | ▭ | 날짜 | › |
| 정렬하기 | ⇅ | 날짜 | › |
| 정렬 순서 | ↕ | 오름차순 | › |

"편집"만 꺾쇠가 없다 → 서브메뉴가 아니라 별도 화면/다이얼로그를 연다 [추론].

### 2.6 `+` 버튼

[미확인] 스크린샷에 누른 결과가 없다. [추론] 그 칸의 조건(= 우선순위)을 미리 채운 채
인라인 입력을 여는 것이 자연스럽다. TickTick의 다른 화면들이 그렇다.

---

## 3. 현재 앱 감사

### 3.1 도메인 — 좋은 상태다

`src/utils/eisenhower.ts` (150줄). 사분면을 **저장하지 않고 파생**한다. 이 성질은 D1
전환 후에도 유지된다 — 파생의 입력이 두 필드에서 한 필드로 줄 뿐이다.

| 함수 | 위치 | D1 이후 |
|---|---|---|
| `isMatrixImportant` | `:32` | 삭제 대상 |
| `isMatrixUrgent` | `:36` | 삭제 대상 (매트릭스에서는) |
| `getMatrixPosition` | `:43` | 우선순위 1:1 매핑으로 축소 |
| `patchForQuadrant` | `:82` | **priority만 쓴다.** dueDate 조작 전부 삭제 |
| `draftForQuadrant` | `:134` | priority만 |
| `MatrixGroup` 타입 | `:21` | `neither`/`unclassified` 소멸, `completed`는 그룹 축으로 이동 |

### 3.2 화면

`src/components/MatrixPage.tsx` (372줄).

| 지점 | 현재 | 필요 |
|---|---|---|
| `:87` | `if (isCompleted(task)) continue` — 완료를 **아예 제외** | D2로 뒤집힘 |
| `:89` | 사분면 = `getMatrixPosition` | D1로 규칙 교체 |
| `:191` | `compareCards` — 마감일→우선순위 고정 정렬 | 사용자 정렬로 교체 |
| `:249` | 칸 헤더 = 로마숫자 + 행동형 제목 + 힌트 | 설명형 제목, `+`/`⋯` 추가 |
| `:257` | 헤더에 개수 뱃지 | 제거 (개수는 그룹으로) |
| `:281` | 하단 `ExpandableAdd` "+ 작업 추가" | 헤더 `+`로 이동 |
| `:63` | 리스트 스코프 `<select>` | TickTick엔 없음. §15 결정 필요 |
| — | 축 라벨 4개 (`:143~152`) | TickTick엔 없음 |
| — | 그룹·접기·개수·추가 보기 | **전부 신규** |

### 3.3 재사용 가능한 것

- `src/domain/view/viewSpec.ts:20` `GroupAxis`, `:32` `SortKey` — 그룹/정렬 어휘가 이미 있다.
  다만 `dueDate` 축(`:193`)은 **날짜 문자열 그대로** 묶는다. TickTick의 버킷(기한초과/
  오늘/없음)이 아니다 → 새 축 필요(§7).
- `src/components/common/ContextMenu.tsx:24,46,51` — 메뉴 표면. **서브메뉴와 우측
  현재값 표시가 없다** → 확장 필요(§8).
- 드래그로 칸 이동, 인라인 추가는 이미 동작한다.

---

## 4. D1 — 사분면은 우선순위다

### 4.1 새 규칙

```ts
// 저장하지 않는다. 우선순위에서 읽는다.
Q1 ← priority "high"
Q2 ← priority "medium"
Q3 ← priority "low"
Q4 ← priority "none"
```

역방향(드래그·`+`)은 우선순위 한 필드만 쓴다.

### 4.2 이 전환이 고치는 것

현재 `patchForQuadrant`는 급한 칸 밖으로 끄는 드래그에서 **마감일을 지운다**
(`eisenhower.ts:115` — `patch.dueDate = ""`). 사용자가 정한 날짜가 카드 한 번 옮기면
사라진다. 새 모델에서는 드래그가 날짜를 건드리지 않으므로 이 부작용이 통째로 없어진다.

`eis.qIV`가 "미분류 · 보류함"이라는 어색한 겸업을 하고 있는데(`ko.ts:708`), 새 모델에서
Q4 = `priority "none"` = **정확히 미분류**다. 이름이 규칙과 일치하게 된다.

### 4.3 이 전환이 만드는 문제 — 정직하게

**`TaskDetail`에 같은 필드를 고치는 컨트롤이 두 개가 된다.**

```
src/components/TaskDetail.tsx:181   <select> 우선순위   ← task.priority를 직접 쓴다
src/components/TaskDetail.tsx:194   <select> 사분면     ← patchForQuadrant를 거쳐 쓴다
```

지금은 두 축의 조합이라 별개의 컨트롤일 이유가 있었다. D1 이후에는 **같은 값의 다른
이름**이다. 나란히 놓인 두 셀렉트가 하나를 바꾸면 다른 하나도 움직이는 화면은, 사용자가
버그로 읽는다.

선택지:

| 안 | 내용 | 평가 |
|---|---|---|
| **A** | 사분면 셀렉트를 **삭제**한다 | 권장. 우선순위 셀렉트가 이미 그 일을 한다. 라벨만 "우선순위 (매트릭스 칸)"으로 보강 |
| B | 우선순위 셀렉트를 삭제하고 사분면만 남긴다 | 리스트·보드의 우선순위 표시와 어휘가 갈라진다 |
| C | 둘 다 둔다 | 같은 필드를 두 번 묻는 화면. 반대 |

**A를 제안한다.** 다만 `TaskDetail.tsx:151`의 기존 주석이 "우선순위를 고칠 유일한 길이
사분면이었다"는 과거를 기록하고 있으므로, 삭제 시 그 주석도 함께 갱신해야 한다.

### 4.4 다른 두 호출자

| 호출자 | 지금 | D1 이후 |
|---|---|---|
| `CalendarRightTaskPanel.tsx:94` | 캘린더 우측 패널을 사분면별로 나눠 그린다 | 우선순위별 4구획이 된다. 의미는 통하지만 `caltasks.qI`("지금 집중") 같은 라벨은 다시 써야 한다 |
| `viewSpec.ts:197` | `quadrant` 그룹 축 | `priority` 축(`:191`)과 **완전히 중복**된다. 축 하나를 없애거나, `quadrant`를 `priority`의 별칭으로 남길지 결정 필요 |

`viewSpec`의 중복은 §15의 열린 질문으로 남긴다 — 뷰 스펙은 사용자 데이터에 저장되므로
축을 지우면 저장된 스펙이 깨진다(`normalizeSortKey`(`:41`)와 같은 종류의 문제).

---

## 5. D2 — 완료는 각 칸 안에

`getMatrixPosition`은 완료를 무조건 Q4로 보낸다(`eisenhower.ts:53`). 스크린샷에서는
Q1·Q2·Q4가 **각자** 완료 그룹을 갖는다 [관찰]. 즉 TickTick은 완료 여부를 배치에 쓰지
않고, 완료된 작업도 자기 우선순위의 칸에 남는다.

따라서 `getMatrixPosition`의 완료 특례는 **사라진다.** 완료는 사분면이 아니라 **그룹
축의 값**이 된다.

MatrixPage의 완료 제외(`:87`)도 함께 사라진다. 지금 그 자리 주석은 "다음에 뭘 할지 보는
판에 끝난 일은 자리가 없다"고 적혀 있다 — 그 판단을 뒤집는 것이므로, 주석을 지우지 말고
**왜 뒤집는지로 고쳐 쓴다**: 체크한 카드가 화면에서 즉시 사라지면 방금 무엇을 체크했는지
확인할 수 없고, 되돌리려면 다른 화면으로 가야 한다.

---

## 6. 칸 안의 그룹

### 6.1 날짜 버킷

`viewSpec`의 `dueDate` 축은 날짜 문자열로 묶는다(`:193`) — 날짜가 30개면 그룹이 30개다.
필요한 것은 버킷이다.

```ts
// 신규: domain/view/dateBucket.ts (순수)
type DateBucket = "overdue" | "today" | "tomorrow" | "thisWeek" | "later" | "none";
```

[관찰]된 것은 `overdue` · `today` · `none` 셋뿐이다. `tomorrow`/`thisWeek`/`later`는
[미확인]이며 §14에 남긴다 — 스크린샷의 계정에는 미래 날짜 작업이 없었다.

완료는 날짜 버킷과 **직교**한다. 완료된 작업은 날짜가 무엇이든 완료 그룹으로 간다
[관찰: Q1의 완료 항목들이 4월 날짜를 갖고도 완료 그룹에 있다].

그룹 순서: `overdue → today → tomorrow → thisWeek → later → none → completed`.

### 6.2 접기와 개수

- 그룹 헤더는 클릭으로 접힌다 [관찰: 모든 헤더에 ∨].
- 접힘 상태는 **어디에 저장하나** → §15 열린 질문. 기기 로컬(UI 상태)이 자연스럽다.
- 개수는 그룹 헤더에만. 칸 헤더에는 없다 [관찰].

### 6.3 추가 보기

완료 그룹만 상한을 둔다. 상한 5, 초과분은 "추가 보기"로 5개씩 [추론].

---

## 7. 그룹화 · 정렬 상태

사분면마다 독립적으로 갖는다 [추론 — 메뉴가 칸별로 열리므로].

```ts
// AppSettings에 추가 (M0 규칙: 추가 전용, 구버전 클라이언트가 통과시킨다)
matrixQuadrantViews?: Record<"I"|"II"|"III"|"IV", {
  groupBy: "dueDate" | "list" | "tag" | "none";
  sortKey: "dueDate" | "priority" | "title" | "createdAt" | "manual";
  sortOrder: "asc" | "desc";
}>;
```

`AppSettings`는 `settings` 테이블의 `app_settings` 행으로 이미 동기화된다
(`usePlannerData`의 저장 경로). 새 테이블은 필요 없다.

기본값: `groupBy: "dueDate"`, `sortKey: "dueDate"`, `sortOrder: "asc"` [관찰 — 메뉴가
그렇게 표시한다].

---

## 8. ⋯ 메뉴 — ContextMenu 확장

`ContextMenuItem`(`ContextMenu.tsx:24`)에 두 가지가 없다.

```ts
export interface ContextMenuItem {
  // ...기존
  /** 라벨 오른쪽에 현재 값을 적는다. "날짜", "오름차순". */
  value?: string;
  /** 있으면 꺾쇠를 그리고, 열면 이 항목들로 바뀐다. */
  submenu?: ContextMenuItem[];
}
```

서브메뉴 구현 방식 두 가지:

| 안 | 내용 | 평가 |
|---|---|---|
| **A. 드릴인** | 같은 팝오버 안에서 내용을 교체하고 상단에 "‹ 뒤로" | 권장. `FloatingMenu` 하나로 끝나고, 좁은 화면·터치에서도 동작한다 |
| B. 플라이아웃 | 옆으로 두 번째 팝오버 | TickTick과 같은 모양이지만 위치 계산·화면 경계 처리가 붙는다. `ContextMenu.tsx:9~18` 주석이 그 종류의 위치 추측을 걷어낸 기록이다 |

**A를 제안한다.** 겉모습은 다르지만 고르는 동작은 같고, 이미 지운 문제를 다시 들이지
않는다.

---

## 9. `+` 버튼

헤더의 `+`가 그 칸 **맨 위**에 인라인 입력을 연다. 저장 시 그 칸의 우선순위를 갖고
태어난다(`draftForQuadrant`, D1 이후 priority만).

지금의 하단 `ExpandableAdd`(`MatrixPage.tsx:281`)는 제거한다 — 같은 일을 하는 두 입구가
된다.

---

## 10. 작업 행 재구성

| 요소 | 현재 | 목표 |
|---|---|---|
| 체크박스 | `ff-check` 단색 | 우선순위 색 테두리 |
| 제목 | ✅ | ✅ |
| 리스트 | 색점 + 이름 | 이름만 (오른쪽 정렬) |
| 반복 · 메모 아이콘 | 없음 | `repeatType !== "none"`, `notes`/`description` 유무로 |
| 날짜 | `ff-board-chip`, "기한 지남"/"오늘"/M.DD | 실제 날짜("5월 20일"), 지났으면 빨강 |

`MatrixCard`(`MatrixPage.tsx:293`)와 Tasks Module의 `TaskRowContent`가 별개로 존재하는데,
이 기회에 합칠지는 §15 열린 질문 — Component 06/13이 "같은 컴포넌트를 두 형태로"라고
관찰한 것과 같은 문제다.

---

## 11. 마이그레이션 · 호환

**데이터 변경 없음.** 사분면은 여전히 파생값이고, 저장되는 것은 `AppSettings`의 필드
하나뿐이다. 기존 작업 레코드는 손대지 않는다.

다만 **사용자가 보는 결과는 바뀐다.** 지금 Q1에 있던 "중간 우선순위 + 오늘 마감" 작업은
Q2로 옮겨간다. 마이그레이션할 것이 없다는 말은 사용자가 놀라지 않는다는 뜻이 아니다.
릴리스 노트에 한 줄이 필요하다.

---

## 12. 위험

| # | 위험 | 완화 |
|---|---|---|
| R1 | 스크린샷 2장이 근거의 전부. 우선순위 모델이 오독일 가능성 | §1의 증거 3+1개가 서로 독립적이다. 그래도 구현 전 실제 앱에서 우선순위를 바꿔 카드가 칸을 옮기는지 1회 확인을 권한다 |
| R2 | `TaskDetail`의 두 셀렉트 중복(§4.3) | D1과 **같은 커밋**에서 처리. 나중으로 미루면 그 사이 릴리스가 버그로 읽힌다 |
| R3 | `viewSpec`의 `quadrant` 축이 `priority`와 중복 | 축을 지우면 저장된 뷰 스펙이 깨진다. 별칭으로 남기는 쪽이 안전 |
| R4 | 완료를 칸에 넣으면 칸이 길어져 스크롤이 는다 | 완료 그룹 접기 기본값 = 접힘, 상한 5 |
| R5 | 픽셀 사양이 없어 "TickTick 같지 않다"는 인상만 남을 수 있음 | 구조가 맞으면 시각은 뒤에 조정 가능. 필요하면 Component 문서 방식으로 실측 세션을 따로 |

---

## 13. 단계 계획

| Phase | 내용 | 검증 |
|---|---|---|
| **1** | D1 도메인 — 규칙 교체, `patchForQuadrant`/`draftForQuadrant` 축소, 완료 특례 제거. `TaskDetail` 사분면 셀렉트 삭제(§4.3 A). 캘린더 패널은 `priority.*` 라벨을 그대로 쓴다 | **완료**(§16). 축 라벨도 함께 제거했다 — §15 Q2 종결 |
| **2** | 날짜 버킷 + 칸 안 그룹 · 개수 · 접기 · 완료 그룹 · 추가 보기 | **완료**(§17) |
| **3** | ⋯ 메뉴 — `ContextMenu` 드릴인 서브메뉴, 현재값 표시, `AppSettings` 저장 | **완료**(§18) |
| **4** | 헤더 `+` 인라인 추가, 하단 add 제거, 행 재구성(아이콘·색·날짜) | **완료**(§19). E2E 1개 — 세 뷰포트 통과 |
| **5** | 편집 — 칸의 **이름 · 설명 · 색**만(§20 A안). 조건은 열지 않는다 | **완료**(§21) |
| 6 | 규칙 프리셋 두 개 중 선택(§20.7 B안) | **완료**(§26) — 이 줄이 "열지 않았다"로 남아 있었던 것은 §23.9가 6a·6b·6c로 다시 계획한 뒤 이 표를 갱신하지 않았기 때문이다 |

Phase 5는 지금 열지 않는다. TickTick의 편집은 사분면 조건을 임의 필터로 바꾸는 기능인데,
그것까지 받으면 사분면이 "저장된 필터 4개"가 되어 파생 성질(§3.1)을 잃는다. 그 교환은
따로 판단할 문제다.

---

## 14. 이 문서가 확인하지 않은 것

1. **픽셀 값 전부** — 패딩, 행 높이, 색상 코드, 폰트 크기. 하나도 재지 않았다.
2. `+` 를 눌렀을 때 실제로 무엇이 열리는지.
3. 미래 날짜 작업의 그룹 이름 — 내일/이번 주/다음 주가 실제로 있는지.
4. "추가 보기"가 5개씩 더 부르는지, 전부 펼치는지.
5. 완료 그룹 헤더의 숫자가 전체 개수인지 표시 개수인지.
6. 그룹 접힘 상태가 저장되는지, 계정 단위인지 기기 단위인지.
7. 편집 화면의 내용.
8. 빈 사분면의 모습.
9. 하위 작업이 매트릭스에 어떻게 나오는지 (현재 앱은 제외 — `MatrixPage.tsx:83`).
10. 리스트 스코프 필터가 TickTick에 있는지 (현재 앱에는 있다 — `MatrixPage.tsx:63`).

---

## 15. 열린 질문

| # | 질문 | 언제 필요한가 |
|---|---|---|
| Q1 | ~~리스트 스코프 `<select>`를 유지할까?~~ **유지. §27에서 종결** | — |
| Q2 | 축 라벨 4개를 지울까? 지우면 "왜 이 칸인가"를 칸 이름만으로 설명해야 한다 | Phase 2 |
| Q3 | `viewSpec`의 `quadrant` 축을 `priority`의 별칭으로 남길까, 폐기할까 | Phase 1 |
| Q4 | ~~`MatrixCard`와 `TaskRowContent`를 한 컴포넌트로 합칠까~~ **합쳤다. §28에서 종결** | — |
| Q5 | ~~칸 제목을 `eis.*`(설명형)로 바꾸면 `matrix.*` 키 8개가 고아가 된다. 지울까 유지할까~~ **반대로 일어났다 — `eis.*` 아홉을 지웠다. §30에서 종결** | — |

---

## 16. Phase 1 구현 기록 (2026-08-28)

### 16.1 바뀐 것

| 파일 | 내용 |
|---|---|
| `src/utils/eisenhower.ts` | 전면 재작성. `quadrantOf`/`priorityForQuadrant`가 규칙의 전부. `isMatrixImportant`·`isMatrixUrgent`·`MatrixPosition`·`MatrixGroup` 삭제 |
| `src/components/TaskDetail.tsx` | 사분면 셀렉트 삭제(§4.3 A). 우선순위 셀렉트 하나만 남았다 |
| `src/components/MatrixPage.tsx` | `quadrantOf` 사용, 축 라벨 제거, 헤더 주석 갱신 |
| `src/styles/03-planning.css` | 격자를 2×2로. 축 라벨용 24px 열과 헤더 행이 사라졌다 |
| `src/components/calendar/CalendarRightTaskPanel.tsx` | 구획 이름을 `priority.*`로 |
| `src/domain/view/viewSpec.ts` | `quadrant` 축 유지 + 중복 사유 주석 (§15 Q3 종결) |
| i18n | `caltasks.qI~qIV`, `matrix.urgent/notUrgent/important/notImportant` 삭제. `eis.qIV`·`matrix.subtitle` 개정 |

테스트: `eisenhower.test.ts` 전면 개정(1,900개 통과).

### 16.2 계획에서 벗어난 판단 셋

**1. 축 라벨을 Phase 1에서 지웠다.** §15 Q2로 미뤄 뒀던 것인데, 미룰 수 없었다. "급함 /
안 급함"은 칸이 그 질문에서 파생된다는 주장이고, D1 이후 그 파생은 일어나지 않는다. 다음
단계까지 화면이 거짓말을 하게 두는 것보다 지우는 편이 낫다. `matrix.subtitle`도 같은
이유로 다시 썼다.

**2. 완료된 작업은 아직 화면에 넣지 않았다.** D2는 확정이고 도메인에서는 이미 반영됐다 —
`quadrantOf`는 완료를 보지 않으므로 끝난 작업도 자기 우선순위의 칸에 속한다. 다만
`MatrixPage`는 여전히 걸러낸다. 앉을 자리("완료" 그룹)가 Phase 2에 오기 때문이고, 지금
통과시키면 어제 끝낸 일이 오늘 할 일 사이에 평평하게 섞인다. 필터는 남되 주석은 새 이유로
고쳐 썼다.

**3. 캘린더 패널에 새 이름을 짓지 않았다.** 계획은 "라벨 조정"이었지만, 사분면이 우선순위인
이상 별도의 이름은 한 필드에 대한 두 번째 어휘가 된다. 기존 `priority.*`를 그대로 읽는다.

### 16.3 확인

실제 앱에 네 작업을 심고 확인했다.

| 작업 | 이전 규칙 | 지금 |
|---|---|---|
| 높음 · 기한 지남 | Ⅰ | Ⅰ |
| **중간 · 기한 지남** | **Ⅰ** (기한 지남 = 급함) | **Ⅱ** |
| **낮음 · 날짜 없음** | **Ⅳ** (안 중요·안 급함) | **Ⅲ** |
| **없음 · 오늘** | **Ⅲ** (오늘 = 급함) | **Ⅳ** |

축 라벨 0개, 부제는 새 문장. 사용자가 보는 배치가 실제로 바뀐다는 §11의 경고가 이 표다.

### 16.4 다음 (Phase 2)

날짜 버킷 그룹 · 개수 · 접기 · 완료 그룹 · 추가 보기. 완료를 통과시키는 것은 그룹이
생기는 그 커밋에서 함께 한다.

---

## 17. Phase 2 구현 기록 (2026-08-28)

### 17.1 바뀐 것

| 파일 | 내용 |
|---|---|
| `src/domain/view/matrixGroups.ts` | 신규. 날짜 버킷 + `groupMatrixTasks` |
| `src/components/MatrixPage.tsx` | 완료 필터 제거, 칸마다 그룹 렌더, 접기·개수·추가 보기. 칸 헤더의 개수 뱃지 제거 |
| `src/styles/03-planning.css` | 그룹 헤더·본문·추가 보기 |
| i18n | `matrix.group.*` 6개 + `matrix.showMore` |

테스트 24개 추가(총 1,924). `matrixGroups.test.ts`(규칙) + `matrixPage.test.tsx`(화면).

### 17.2 계획과 달라진 판단 셋

**1. "이번 주" 버킷을 넣지 않았다.** §6.1은 `overdue → today → tomorrow → thisWeek →
later → none`을 제안했지만 `thisWeek`은 주 시작 요일 규칙(일요일/월요일 — 이 앱에 설정이
있다)을 필요로 한다. 그 버킷이 존재한다는 근거는 미래 날짜 작업이 하나도 없는 계정의
스크린샷뿐이다(§14-3). "이후"가 그 작업들을 옳게 담는다. 누가 실물에서 확인하면 그때 쪼갠다.

**2. "완료" 그룹만 정렬 규칙이 다르다 — 완료 시각 내림차순.** 계획에 없던 것이고, 화면에서
직접 확인하다가 드러났다. 작업을 체크하면 완료 그룹으로 가는데, 마감일 오름차순을 그대로
쓰면 방금 끝낸 일이 지난달에 끝낸 일들 **아래**로 간다. 그룹은 5개에서 잘리므로 그 카드는
아예 화면에 없다 — 완료를 화면에 남기기로 한 이유(방금 무엇을 체크했는지 보이게)가 그대로
무너진다. 완료 그룹 안에서는 마감일이 이미 정산된 값이므로 정렬 기준이 될 이유도 없다.
`completedAt`(없으면 `updatedAt`) 내림차순으로 고정한다.

**3. 카드의 날짜 라벨을 실제 날짜로 바꿨다.** "기한 지남"이라고 적힌 카드가 "기한 초과"라는
헤더 아래 다섯 개 있으면 같은 사실을 여섯 번 말하는 것이다. 대신 어느 날인지를 보여준다 —
이 화면에서 달리 알 방법이 없는 쪽이다.

### 17.3 완료 카드를 흐리게 하는 방법

`.ff-matrix-card { opacity }`는 **동작하지 않는다.** 행이 framer-motion 요소라 motion이
`opacity`를 인라인 스타일로 쓰고, 스타일시트는 인라인을 이길 수 없다. 카드가 아니라 카드의
**내용**(`.ff-matrix-card-main`, `.ff-check`)에 걸어야 한다.

### 17.4 검증 환경에 관한 기록

브라우저 패인에서 확인하는 동안 **`requestAnimationFrame`이 한 번도 발화하지 않는다**
(`document.visibilityState === "visible"`인데도). framer-motion은 rAF로 도는 라이브러리라,
그 안에서 새로 마운트된 카드는 전부 `initial`(opacity 0)에 멈춰 있다.

이걸 제품 버그로 잘못 읽고 `AnimatePresence`를 한 번 걷어냈다가 되돌렸다. **패인에서 잰
opacity·transform 값은 신뢰할 수 없다.** DOM 구조·텍스트·개수는 신뢰할 수 있고, 애니메이션이
얽힌 것은 jsdom 테스트로 확인하는 편이 맞다.

### 17.5 다음 (Phase 3)

⋯ 메뉴 — `ContextMenu` 드릴인 서브메뉴 + 현재값 표시, 사분면별 그룹화·정렬을
`AppSettings`에. `groupMatrixTasks`는 비교 함수를 이미 인자로 받으므로 정렬 설정이 붙을
자리는 열려 있다.

---

## 18. Phase 3 구현 기록 (2026-08-28)

### 18.1 바뀐 것

| 파일 | 내용 |
|---|---|
| `src/components/common/ContextMenu.tsx` | `value`(현재값) + `submenu`(드릴인). §8 A안 |
| `src/domain/view/matrixGroups.ts` | `MatrixQuadrantView` + `sanitizeMatrixView` + `matrixComparator`. `groupMatrixTasks`가 비교 함수 대신 view를 받는다 |
| `src/types.ts` | `AppSettings.matrixQuadrantViews` (선택 필드, M0) |
| `src/domain/plannerData/normalize.ts` | 저장된 view 정화 |
| `src/components/MatrixPage.tsx` | 칸 헤더의 ⋯ 버튼 + 메뉴 조립 |
| `src/app/AppPages.tsx` | `updateAppSettings`로 저장 |
| i18n · CSS | 메뉴 어휘 11개, 현재값·꺾쇠·뒤로 스타일 |

테스트 16개 추가(총 1,940).

### 18.2 D1이 만든 결과 하나 — 정렬 목록에서 우선순위가 빠진다

**칸 안에서 우선순위로 정렬하는 것은 아무 일도 하지 않는다.** D1 이후 한 칸의 모든 작업이
그 칸의 우선순위를 갖기 때문이다. 목록에 넣으면 눌러도 화면이 그대로인 컨트롤이 되므로 뺐다.
남은 것은 **마감일 · 제목 · 만든 날짜** 셋이다. `sanitizeMatrixView`는 저장된
`sortKey: "priority"`를 기본값으로 접는다.

### 18.3 서브메뉴는 드릴인이다 (§8 A안 확정)

같은 팝오버 안에서 내용이 바뀌고 상단에 "‹ 정렬 순서"가 생긴다. 참조 앱은 옆으로 날아가는
플라이아웃이지만, `ContextMenu.tsx` 상단 주석이 **이 메뉴의 자체 위치 계산을 걷어낸 기록**
이다 — 폭을 208px로 가정하고 뒤집기를 결정하다 화면 밖으로 나가던 코드였다. 플라이아웃은
그 계산을 다시 들이는 일이고, 드릴인은 표면 하나로 같은 질문을 한다.

### 18.4 그룹화를 끄면 헤더가 없다

`groupBy: "none"`이면 미완료 전부가 `all` 그룹 하나로 묶이는데, **거기엔 헤더를 그리지
않는다.** 칸 전체를 덮는 "전부"라는 제목은 한 줄을 쓰고 아무 말도 하지 않는다. "완료"는
헤더를 유지한다 — 그건 여전히 구분이다.

### 18.5 검증

실제 앱에서:

- ⋯ → `그룹화하기: 날짜 ›` / `정렬하기: 마감일 ›` / `정렬 순서: 오름차순 ›` (스크린샷 2와 같은 모양)
- `정렬 순서` → 드릴인, "‹정렬 순서" 뒤로 버튼, 현재값에 체크 표시
- `내림차순` 선택 → 메뉴 닫힘, `appSettings.matrixQuadrantViews.I.sortOrder === "desc"` 저장
- **새로고침 후에도** 메뉴가 "내림차순"으로 열린다

### 18.6 다음 (Phase 4)

헤더 `+` 인라인 추가(하단 add 제거), 행 재구성 — 우선순위 색 체크박스, 반복·메모 아이콘.


---

## 19. Phase 4 구현 기록 (2026-08-28)

### 19.1 바뀐 것

| 파일 | 내용 |
|---|---|
| `src/components/MatrixPage.tsx` | 헤더 `+` → `QuadrantQuickAdd`(칸 맨 위), 하단 `ExpandableAdd` 제거, `MatrixCard` 재구성 |
| `src/styles/03-planning.css` | `+` 버튼 · 인라인 입력 · 한 줄 카드 · 리스트/아이콘/날짜 · 우선순위 색 체크박스 |
| `src/i18n/*` | `matrix.add` → `matrix.addAria`, `matrix.card.repeats`, `matrix.card.hasNotes` |
| `src/components/matrixPage.test.tsx` | 테스트 12개 |
| `e2e/matrixQuickAdd.spec.ts` | Ⅱ에 입력 → 그 칸에 나타나고 `priority: "medium"`으로 저장 |

죽은 코드 `compareCards`/`PRIORITY_RANK`도 걷어냈다. Phase 2가 정렬을 `groupMatrixTasks`
안으로 옮긴 뒤 호출자가 없었는데, **한 칸 안의 동점을 우선순위로 깬다**고 적혀 있었다 —
D1 이후 한 칸의 모든 작업이 같은 우선순위이므로 아무 일도 하지 않는 규칙이다(§18.2와 같은 종류).

`ExpandableAdd`(`src/components/motion/ExpandableAdd.tsx`)와 그 CSS(`08-calendar-categories.css`
`.expandable-add*`)는 이제 **호출자가 없다** — 매트릭스가 마지막 사용처였다. 지우지 않고 남겼다:
Phase 4가 요청받은 범위가 아니고, 별도로 판단할 문제다.

테스트 12개 추가(총 1,952). 타입 체크 통과.

### 19.2 입력은 위에 열리고, 카드는 아래에 생긴다

`+`가 칸 **맨 위**에 입력을 연다. 컨트롤 바로 옆이기 때문이다. 그런데 날짜 없이 만든 작업은
"날짜 없음" 그룹으로 가고, 그 그룹은 **맨 아래**다. 타이핑한 자리와 카드가 나타나는 자리가
다르다.

고치지 않았다. 입력을 카드가 갈 자리에 열려면 제목을 다 치기 전에 그 자리를 알아야 하는데,
그건 날짜를 정하기 전에는 알 수 없다. 반대로 새 작업을 무조건 위에 붙이면 **칸의 정렬 규칙을
새 작업만 어기는** 예외가 하나 생긴다 — 그 예외가 어긋난 위치보다 설명하기 어렵다.

### 19.3 체크박스의 색은 칸의 색이다

§10은 "우선순위 색 테두리"라고 적었다. D1 이후 **한 칸의 모든 카드가 같은 우선순위**이므로,
이것은 카드마다 칸 머리말을 한 번씩 되풀이하는 색이다.

그래도 넣었다. 단, 우선순위에서 따로 계산하지 않고 셀이 이미 들고 있는 `--q-color`를
상속시켰다(CSS 두 줄, JS 0줄). 그러면 이 색은 "이 카드의 우선순위"라는 **별도 주장**이 아니라
칸의 색이고, 같은 것을 두 줄 위 머리말이 **글자로** 말하고 있다 — 색만이 지고 있는 정보가
없으므로 TICKTICK_COMPONENT_06 §6.1이 거부한 그 설계가 아니다.

### 19.4 완료 카드의 체크박스가 비어 있었다

Phase 2부터 있던 것: 완료 그룹의 행은 취소선이 그어지고 흐려지는데 **체크박스는 빈 동그라미**
였다. `MatrixCard`가 `checked` 클래스를 붙인 적이 없다. 카드에서 유일하게 누를 수 있는
것이 나머지 전부와 반대되는 말을 하고 있었다. `isCompleted`로 `checked` + "✓"를 붙이고,
aria-label도 `tasks.completeTask` / `tasks.reopenTask`로 갈랐다.

### 19.5 끝난 일에 빨강은 경보가 아니다

완료된 카드의 지난 마감일이 `is-overdue` 빨강으로 남았다. "기한 초과"는 **가서 하라는 말**
인데 그 일은 이미 끝났다. `overdue = !done && …` 로 막았다.

참조 앱은 여기서 더 간다 — §2.4의 관찰은 "완료 행은 전체가 흐려지고 **리스트 이름만 남는다**",
즉 날짜를 아예 지운다. 거기까지는 안 갔다. 끝난 뒤에 그 행을 다시 보는 이유가 대개
**언제까지였는지**이고, 이 화면에서 그걸 말하는 곳은 그 자리뿐이다.

### 19.6 검증

- 유닛 27개(이 파일), 전체 1,952개 통과 · `tsc` 통과
- E2E `matrixQuickAdd.spec.ts` — desktop · tablet · mobile 세 뷰포트 통과
- 실제 앱(dev, ko): 칸마다 `+`/`⋯` 두 컨트롤, `+`가 그 칸에만 입력을 열고 포커스가 가며,
  입력 테두리가 칸 색(Ⅱ = `rgb(0,122,255)`)이다. 체크박스 테두리 Ⅰ 빨강 · Ⅱ 파랑 ·
  Ⅲ 주황 · Ⅳ 회색. 날짜가 "8월 25일"로 그려지고 지난 것만 빨강, 완료된 것은 회색.
  리스트는 이름만("학교"), 색점 없음. 반복·메모 아이콘이 해당 카드에만.

### 19.7 다음 (Phase 5)

§13대로 **열지 않는다.** 사분면 편집은 칸의 조건을 임의 필터로 바꾸는 기능이고, D1이 그
조건을 우선순위 한 필드로 고정했다. 받으면 사분면이 "저장된 필터 4개"가 되어 파생 성질을
잃는다 — 별도 판단 문제다.

남은 것은 §15의 열린 질문 하나: `MatrixCard`와 Tasks Module의 `TaskRowContent`가 여전히
별개다. 이번에 `MatrixCard`가 체크 상태·아이콘·날짜를 각자 다시 구현했으므로, 그 seam은
Phase 4 이전보다 **더 비싸졌다**.


---

## 20. 편집 — Phase 5 설계 (2026-08-28)

⋯ 메뉴의 네 번째 줄. §8이 "꺾쇠가 없으니 서브메뉴가 아니라 별도 화면"이라고 [추론]했고,
§13이 "D1이 조건을 고정하므로 범위 재확인 후"라며 미뤄둔 그 줄이다.

이번에는 스크린샷이 아니라 **참조 앱의 공식 문서**를 읽었다. 근거 수준이 §0보다 한 단계
올라간 첫 절이다.

### 20.1 먼저 — D1의 근거가 절반 틀렸다

§4는 이렇게 적었다:

> 참조 앱이 우선순위만 읽는다. 마감이 사흘 지난 medium 작업이 거기서는 Ⅱ에 있다.

문서를 읽으니 그건 **참조 앱이 하는 일**이 아니라 **참조 앱의 프리셋 1**이다. 두 개가 있다:

| | 이름 | 규칙 |
|---|---|---|
| 조합 1 | Matrix → Priority | 우선순위 4개 ↔ 칸 4개. 드래그하면 우선순위가 바뀐다 |
| 조합 2 | Matrix → Time + Priority | 중요 = 우선순위, 긴급 = 시간. 드래그하면 **시간과 우선순위가 둘 다** 바뀐다 |

그리고 조합 2에는 우리가 몰랐던 성질이 있다: **시간이 흐르면 칸이 저절로 바뀐다.**
("As time passes, tasks that were originally not urgent become urgent due to approaching
deadlines. And these tasks will also automatically be adjusted to the urgent matrix.")

즉 Phase 1에서 우리가 지운 옛 규칙(우선순위 × 마감일)은 **틀린 규칙이 아니라 조합 2**였다.
우리는 두 프리셋 중 하나를 지우고 다른 하나를 하드코딩했다. D1의 세 근거 중 살아남는 것은
2번(드래그가 마감일을 지웠다)과 3번("판단 안 함"과 "안 중요함"이 Ⅳ를 나눠 썼다)이고,
1번("참조 앱이 그렇게 한다")은 **"참조 앱의 기본 프리셋이 그렇게 한다"로 약해진다.**

이건 D1을 되돌리자는 말이 아니다. 편집이 무엇을 여는 문인지가 달라진다는 말이다 —
"없던 기능을 만드는 일"이 아니라 **"우리가 지운 쪽을 되돌려 놓을 자리를 만드는 일"** 이다.

### 20.2 참조 앱의 편집이 실제로 여는 것 [문서]

- **이름**: ⋯ → Edit Matrix에서 칸 이름을 고친다
- **규칙**: 네 개의 차원 — **List · Tag · Time · Priority**
  - List/Tag: 어떤 리스트/태그의 작업만 이 매트릭스에 보일지
  - Time: 칸마다 시간 범위. 예로 긴급 = "오늘 ~ 내일, 기한 초과", 비긴급 = "모레 ~ 7일 후".
    **범위 밖의 작업은 아예 안 보인다** — 문서는 이걸 기능으로 설명한다
  - Priority: 칸마다 어떤 우선순위를 보일지
- **쓰기 방향도 규칙을 따른다**: 드래그도, 그 칸에 **새로 만드는 것도**. 조합 2에서 "긴급 &
  중요"에 만든 작업은 "오늘 + 높은 우선순위"로 태어난다

마지막 줄은 우리 Phase 4를 사후 검증해 준다. 조합 1에서는 우선순위 하나만 쓰는 것이 맞고,
`draftForQuadrant`가 정확히 그렇게 한다(§19).

### 20.3 §13의 걱정은 절반만 맞다

§13은 "받으면 사분면이 저장된 필터 4개가 되어 파생 성질(§3.1)을 잃는다"고 적었다.

**파생 성질은 잃지 않는다.** 규칙이 저장될 뿐 소속은 여전히 계산된다. 작업 레코드에
`quadrant` 필드가 생기는 게 아니다. §3.1이 지키려던 것 — 칸과 필드가 어긋날 수 없다 —
은 그대로다.

잃는 것은 다른 것이고, 더 구체적이다. 지금 `quadrantOf`는 **전함수이자 가역**이다.

### 20.4 실제로 깨지는 것 셋

**P1 · 전체성.** 지금은 모든 작업이 정확히 한 칸에 들어간다(`?? "IV"` 폴백까지 포함해서).
Time 규칙이 들어오면 **0개 칸에 맞는 작업**이 생긴다 — 참조 앱은 이걸 의도한 기능이라고
말한다. 규칙이 겹치면 **2개 칸에 맞는 작업**도 생긴다. 화면에서 사라진 작업을 사용자가
어디서 찾는지, 이 문서는 답을 갖고 있지 않다.

**P2 · 역함수.** 드래그는 "이 작업이 저 칸의 규칙에 맞도록" 쓰는 패치다.
`priority: high`는 역이 하나다. `dueDate ∈ [오늘, 내일]`은 역이 **여럿**이다 — 어느 날?
참조 앱은 하나를 고른다(오늘). 고르는 순간 그건 규칙이 아니라 관습이다.

**P3 · 필터의 역은 패치가 아니다.** Ⅰ의 규칙이 `리스트 = 일 AND 우선순위 = 높음`이면,
개인 작업을 Ⅰ로 끌었을 때 **그 작업을 일 리스트로 옮겨야** 한다. 드래그 한 번이 소속을
바꾼다. Tag 규칙이면 태그를 붙인다. 이건 §4.2의 2번(드래그가 마감일을 지웠다)과 **같은
종류의 사고**이고, D1이 존재하는 이유 그 자체다.

### 20.5 선택지 셋

| 안 | 편집이 여는 것 | P1 | P2 | P3 | 평가 |
|---|---|---|---|---|---|
| **A** | 칸의 **이름 · 힌트 · 색**. 규칙은 안 건드림 | 안 열림 | 안 열림 | 안 열림 | **권장 · Phase 5** |
| **B** | **프리셋 두 개 중 선택** (조합 1 / 조합 2) | 안 열림 | 열림(조합 2에서만) | 안 열림 | Phase 6 후보 — 별도 판단 |
| C | 임의 조건(List/Tag/Time/Priority) | 열림 | 열림 | 열림 | 열지 않는다 |

**A를 먼저 한다.** 스크린샷의 네 번째 줄이 생기고, 그 줄이 여는 것이 셋 중 유일하게
**아무것도 깨뜨리지 않으면서 실제로 쓸모가 있는** 것이기 때문이다. "지금 하기"라는 이름이
모든 사람에게 맞지는 않는다.

### 20.6 A안 설계

**무엇을 편집하나.** 칸당 세 가지: 이름, 힌트 한 줄, 색.

**어디에 저장하나.** `AppSettings.matrixQuadrantViews`를 늘린다. 이미 칸별 설정을 담고
있고, `sanitizeMatrixView`(§18.1)라는 정화 지점이 있다.

```ts
interface MatrixQuadrantView {
  groupBy: MatrixGroupAxis;
  sortKey: MatrixSortKey;
  sortOrder: MatrixSortOrder;
  name?: string;   // 비어 있으면 t("matrix.qI")
  hint?: string;   // 비어 있으면 t("matrix.qIHint")
  color?: string;  // 비어 있으면 CSS의 --q-color 기본값
}
```

셋 다 **선택 필드(M0)** 이고 빈 값이 "기본값"을 뜻한다. 저장된 이름이 없는 계정은 지금과
글자 하나 다르지 않고, 언어를 바꾸면 이름도 따라 바뀐다 — **사용자가 직접 쓴 이름만이
언어를 따라가지 않는다.** 그게 맞다. 사용자가 한국어로 쓴 이름을 영어로 번역할 방법은 없다.

**어떤 표면인가.** 서브메뉴가 아니라 **다이얼로그**. §8이 꺾쇠 없음에서 [추론]한 그대로이고,
드릴인 서브메뉴는 선택지 목록을 위한 것이지 자유 입력을 위한 것이 아니다(§18.3).
`ContextMenu`에 텍스트 입력을 넣으면 §18.3이 걷어낸 위치 계산 문제가 다른 얼굴로 돌아온다.

**다이얼로그 내용** (한 화면, 필드 셋 + 되돌리기):

```
Ⅰ 칸 편집
  이름     [지금 하기            ]
  설명     [중요하고 급한 일       ]
  색       ● ● ● ● ● ●   (프리셋 6개)
  [기본값으로 되돌리기]        [취소] [저장]
```

"기본값으로 되돌리기"가 필요한 이유: 세 필드를 지우면 기본값으로 돌아간다는 것을 빈 입력칸이
말해주지 않는다. 명시적인 버튼 하나가 그 규칙을 눈에 보이게 한다.

**이름이 바뀌면 함께 움직여야 하는 것.** 지금 `t("matrix.qI")`를 읽는 자리 전부다:

| 자리 | 지금 |
|---|---|
| 칸 머리말 | `MatrixPage.tsx` `ff-matrix-cell-title` |
| `+` aria-label | `matrix.addAria`의 `{{quadrant}}` |
| ⋯ aria-label · 메뉴 제목 | `matrix.menuAria`의 `{{quadrant}}` |
| 캘린더 우측 패널 | `CalendarRightTaskPanel.tsx` — §4.4에 따라 지금은 `priority.*`를 쓴다 |

앞의 셋은 한 함수(`quadrantLabel(quadrant, views, t)`)로 모으면 된다. 네 번째는 **일부러
따라가지 않는다** — 캘린더 패널은 우선순위로 나눈 것이지 매트릭스 칸을 그린 게 아니고,
사용자가 Ⅰ을 "화요일 마감"이라고 이름 붙였을 때 캘린더가 그 말을 하면 거짓말이 된다.

**색.** `--q-color`는 지금 CSS에서 칸별로 박혀 있다(`.ff-matrix-cell-I { --q-color: var(--danger) }`).
저장된 색이 있으면 인라인 스타일로 덮어쓴다. Phase 4가 체크박스·입력 테두리를 이 변수에
매달아 놨으므로(§19.3) **한 곳만 바꾸면 셋이 함께 움직인다.**

**테스트.** 유닛 6개 정도 — 저장된 이름이 머리말과 두 aria-label에 함께 들어가는지, 빈
이름이 기본값으로 읽히는지, `sanitizeMatrixView`가 이상한 색·너무 긴 이름을 접는지,
되돌리기가 세 필드를 함께 비우는지, 언어를 바꿔도 사용자 이름은 그대로인지.

### 20.7 B안 — 필요해지면

프리셋 두 개. 화면 하나 위(⋯가 아니라 매트릭스 전체의 설정)에 있어야 한다 — 규칙은
칸 하나의 성질이 아니라 네 칸이 함께 지키는 약속이기 때문이다.

바뀌어야 할 것:

- `quadrantOf(task)` → `quadrantOf(task, rule)`. `patchForQuadrant`·`draftForQuadrant`도
- 두 규칙 **모두 전함수이고 가역**이다. 그래서 P1·P3이 열리지 않는다
- P2는 열린다. 조합 2의 드래그는 마감일을 쓴다 — §4.2가 죽인 그 동작이다.
  다만 **사용자가 그 규칙을 골랐을 때만** 그렇다. 고른 규칙이 "긴급 = 시간"이라면 드래그가
  시간을 쓰는 것은 놀람이 아니라 **약속의 이행**이다. §4.2의 사고는 아무도 고르지 않은
  규칙이 조용히 날짜를 지운 것이었다
- `viewSpec`의 `quadrant` 축(§4.4 · Q3)이 다시 `priority`와 구별된다

이건 Phase 5가 끝난 뒤 **따로 판단한다.** A와 달리 도메인 함수 셋의 서명이 바뀐다.

### 20.8 C안을 열지 않는 이유

P3 하나로 충분하다. 리스트를 조건에 넣는 순간 **드래그 한 번이 작업의 소속을 바꾼다.**
이 앱이 D1을 만든 이유가 정확히 "드래그가 사용자가 정한 무언가를 조용히 덮어쓴다"였다.
참조 앱이 그 교환을 받아들였다는 사실은 그것이 옳다는 증거가 아니라, 그 앱이 다른 선택을
했다는 사실일 뿐이다.

### 20.9 이 조사가 답한 것

**§15 Q1 — "리스트 스코프 셀렉트를 유지할까? TickTick 매트릭스에는 안 보인다".**
답: 안 보이는 이유는 **리스트가 규칙 안에 있기** 때문이다(20.2). 참조 앱은 그걸 칸별로,
영구히 저장한다. 우리 것은 화면 전체에, 세션 동안만 건다. **둘 다 같은 필요를 푼다.**
C안을 열지 않는 한 우리 쪽이 그 필요를 감당하는 유일한 장치이므로 — **유지한다. Q1 종결.**

### 20.10 여전히 확인하지 않은 것

1. **편집 다이얼로그의 실제 생김새.** 문서에 이미지가 있으나 텍스트로 읽히지 않았다.
   필드 순서·색 편집 가능 여부는 [미확인]이다. 이름은 [문서]로 확인됐다
   — **§20.11에서 정정된다.**
2. 규칙이 **겹치거나 비었을 때** 참조 앱이 무엇을 하는지 (P1). 문서는 "안 보인다"까지만 말한다
3. 스크린샷의 `정렬하기` 현재값이 **"날짜"** 인데 우리는 **"마감일"** 이다. 우리 쪽이 더
   정확하다(정렬 키가 `dueDate`다). 어휘를 맞출 이유가 있는지는 판단 안 함


---

## 21. Phase 5 구현 기록 (2026-08-28)

§20 A안. ⋯ 메뉴의 네 번째 줄이 생겼고, 그 줄이 여는 것은 **칸의 이름 · 설명 · 색**이다.
조건(List/Tag/Time/Priority)은 열지 않았다 — §20.4의 P1·P2·P3 그대로다.

### 21.1 바뀐 것

| 파일 | 내용 |
|---|---|
| `src/domain/view/matrixGroups.ts` | `MatrixQuadrantView`에 `name?`·`hint?`·`color?`, `sanitizeLabel`, `MATRIX_QUADRANT_COLORS`, `matrixQuadrantLabels` |
| `src/components/MatrixQuadrantEditor.tsx` | **새 파일** — 다이얼로그 |
| `src/components/MatrixPage.tsx` | ⋯의 `편집` 줄, `labelsFor`, 칸에 `--q-color` 인라인 |
| `src/styles/03-planning.css` | 다이얼로그 필드 · 색 스와치 |
| i18n · 테스트 | 어휘 7개, 테스트 11개 |

### 21.2 색은 새로 만들지 않았다

`MATRIX_QUADRANT_COLORS`는 `LIST_COLOR_PRESETS`를 그대로 쓴다. 이 앱에 "여덟 색"이라는
답은 이미 있고, 두 번째 팔레트를 만들면 나중에 색 하나를 더할 때 고쳐야 할 곳이 둘이 된다.
저장 형식도 리스트와 같으므로 `listColorHex`가 그대로 읽는다.

**칸에는 `--q-color` 하나만 덮어쓴다.** Phase 4가 체크박스 테두리와 인라인 입력 테두리를
이 변수에 매달아 놨기 때문에(§19.3), 한 줄이 로마숫자·체크박스·입력 테두리 셋을 함께
움직인다. 검증에서 확인했다 — Ⅰ을 남색으로 바꾸니 셋 다 `rgb(91,91,214)`가 됐다.

### 21.3 "비어 있음"과 "설정한 적 없음"은 같은 상태다

세 필드 모두 **선택 필드이고, 빈 값이면 키 자체를 안 쓴다**(`...(name ? { name } : {})`).
`name: ""`을 저장하면 아무도 표현하지 않은 선호를 계정이 평생 들고 다닌다. 그래서
"기본값으로 되돌리기"는 세 필드를 비우는 것이고, 저장하면 키가 사라진다.

편집기는 `sanitizeMatrixView`를 **저장 직전에 한 번 더 통과시킨다**. 저장된 레코드가 지나는
문과 같은 문이므로, 다이얼로그와 동기화된 레코드가 "공백만 있는 이름"이나 41자에 대해
서로 다른 답을 갖는 일이 없다.

### 21.4 이름은 언어를 따라가지 않는다

사용자가 쓴 이름은 `t()`를 거치지 않는다. "화요일 마감"을 영어로 옮길 방법이 없고,
추측하는 것이 그대로 두는 것보다 나쁘다. 언어를 바꾸면 **이름을 지은 칸만 그대로**이고
나머지는 따라 바뀐다 — 이건 버그가 아니라 이 필드가 사용자의 말이라는 뜻이다.

### 21.5 이름은 세 자리에서 함께 움직인다

`matrixQuadrantLabels`가 규칙을 갖고 `MatrixPage`의 `labelsFor`가 한 번만 부른다.
그 결과가 가는 곳: 머리말, `+`의 aria-label, ⋯의 aria-label과 메뉴 제목.

**캘린더 우측 패널은 일부러 따라가지 않는다.** §20.6대로다 — 그건 우선순위로 나눈 화면이지
매트릭스 칸을 그린 게 아니고, Ⅰ을 "화요일 마감"이라 부른 사람에게 캘린더가 그 말을 하면
거짓말이 된다.

### 21.6 저장 버튼은 폼 밖에 있다

다이얼로그는 `kit.tsx`의 `Modal`을 쓰는데, footer가 body 밖에 렌더된다. Enter로 저장하려면
폼 안에 submit 버튼이 있어야 하는데, 숨긴 submit을 하나 두면 **스크린 리더에는 "저장" 버튼이
두 개**가 된다(테스트가 먼저 잡았다 — `getByRole("button", { name: "저장" })`이 둘을 찾았다).
footer의 저장에 `type="submit" form={formId}`를 달았다. `form` 속성이 정확히 이 경우를
위한 것이다.

### 21.7 검증

- 유닛: 이 파일 34개 · `matrixGroups` 25개, 전체 통과 · `tsc` 통과
- 실제 앱(dev, ko):
  - ⋯ → `편집` / `그룹화하기 날짜 ›` / `정렬하기 마감일 ›` / `정렬 순서 오름차순 ›`
    — **스크린샷의 네 줄과 같은 모양**, 편집만 꺾쇠 없음
  - 다이얼로그: 제목 "지금 하기 칸 편집", 이름/설명 입력의 placeholder가 기본값,
    색 9개(기본 + 여덟), 되돌리기가 처음엔 비활성, 포커스가 이름 칸
  - "  화요일 마감  " → 트림되어 저장, 머리말·`+`·⋯ 라벨이 함께 바뀜
  - 남색 선택 → `--q-color: #5b5bd6`, 로마숫자·체크박스·입력 테두리 셋 다 따라감
  - **새로고침 후에도 유지**, 손대지 않은 Ⅱ는 여전히 기본 파랑

### 21.8 다음

Phase 6(§20.7 B안 — 규칙 프리셋 두 개)은 **열지 않았다.** `quadrantOf`·`patchForQuadrant`·
`draftForQuadrant` 셋의 서명이 바뀌고, 조합 2의 드래그가 마감일을 쓰는 §4.2 문제가 다시
열린다 — 사용자가 그 규칙을 고른 경우에 한해서지만. 별도 판단 문제다.

§20.10의 [미확인] 셋은 그대로다. 특히 참조 앱 편집 다이얼로그의 실제 생김새를 못 봤으므로,
**이 다이얼로그의 필드 순서와 색 편집 존재 여부는 우리 판단이지 관찰이 아니다.**


---

## 22. 편집 다이얼로그의 실물 (2026-08-28)

사용자가 참조 앱의 **데스크톱 `행렬 편집` 다이얼로그 스크린샷**을 제공했다. §20.10 1번이
[미확인]으로 남겨둔 바로 그것이다.

### 22.1 스크린샷에서 읽은 것 [관찰]

```
행렬 편집 👑
┌────────────────────────────────┐
│ 🔴 중요하고 급한 일              │   ← 배너 (읽기 전용? 편집 가능? [미확인])
└────────────────────────────────┘
리스트      [ 전체              ▾ ]
태그        [ 전체              ▾ ]
날짜        [ 전체              ▾ ]
우선 순위   ○전체 ☑높음 ☐중간 ☐낮음 ☐없음
작업 유형   ◉전체 ☐과제 ☐노트
기본값 복원          예시  [ 저장 ] [ 취소 ]
```

### 22.2 §20.6이 틀렸다 — 편집에는 이름 칸이 없다

§20.6은 "참조 앱의 편집은 이름을 고친다"를 [문서] 근거로 적었다. 근거는 검색 스니펫
("select 'Edit Matrix' to modify the name")이었고, **실물에는 이름 입력이 없다.** 규칙뿐이다.

무엇이 맞는지 이 문서는 결론을 못 낸다. 가능한 설명 셋 — 모바일 편집에는 이름이 있다 /
상단 배너가 사실은 편집 가능한 이름이다 / 스니펫이 틀렸다. **셋 다 [미확인]이다.**

따라서 **Phase 5로 만든 것(§21)은 참조 앱의 편집이 아니다.** 이름·설명·색을 고치는,
우리가 설계한 다른 화면이다. 그 자체가 나쁘다는 뜻은 아니지만 §21의 출처는 정정되어야 한다:
**§21은 관찰의 구현이 아니라 판단의 구현이다.**

### 22.3 문서가 말하는 것과 말하지 않는 것

| | 공식 문서 | 스크린샷 |
|---|---|---|
| 리스트 / 태그 | ✅ "List/Tag" 차원 | ✅ 드롭다운 둘 |
| 날짜 | ✅ "Time" 차원, 범위 예시까지 | ✅ 드롭다운 |
| 우선순위 | ✅ "Priority" 차원 | ✅ **다중 선택 체크박스** |
| **작업 유형(과제·노트)** | ❌ **한 글자도 없다** | ✅ 다섯 번째 줄 |
| 기본값 복원 · 예시 버튼 | ❌ | ✅ |
| 👑 유료 여부 | ❌ 언급 0회 | ✅ 왕관 |
| 컨트롤 종류·필드 순서 | ❌ | ✅ |

문서의 데스크톱 편집 설명은 **접힌 섹션 안의 이미지 7장뿐이고 텍스트가 0바이트다.**
(`How to Editing Matrix on Desktop?` / `How to Use Actions in the Quadrant Matrix on Desktop?`)
"We provide 4 dimensions"이라고 써놓고 세 줄만 나열한다 — List와 Tag를 둘로 세야 4가 된다.
즉 **작업 유형은 문서가 세지도 않은 차원이다.**

### 22.4 스크린샷이 §20.4에 더한 것 — P2가 우선순위에도 걸린다

§20.4 P2는 "역함수가 여럿인 것은 날짜"라고 적었다. **우선순위도다.**

스크린샷의 우선 순위는 **체크박스**다. 한 칸이 `높음 + 중간`을 함께 담을 수 있다. 그러면:

- 칸 ↔ 우선순위 **1:1이 깨진다.** D1의 전제 그 자체다
- `patchForQuadrant`가 답할 수 없는 질문을 받는다 — 두 우선순위를 받는 칸으로 카드를
  끌었을 때 **어느 쪽을 쓰나?**
- `draftForQuadrant`도 같은 질문을 받는다(§19.2의 "새 작업이 어느 그룹에 생기나"와 다른,
  더 근본적인 것)
- §18.2("칸 안에서 우선순위 정렬은 아무 일도 안 한다")가 **되살아난다.** 한 칸에 우선순위가
  둘이면 그 정렬은 다시 의미를 갖는다

그리고 `작업 유형: 노트` — 이 앱에 그런 종류는 **없다.** `Task.contentMode`는
`"description" | "checklist"`이고, 그건 본문의 형태이지 레코드의 종류가 아니다.
이 줄을 그대로 받으려면 없는 개념을 먼저 만들어야 한다.

### 22.5 그래서 지금 상태

C안(§20.5)을 짓겠다는 결정은 아직 내려지지 않았다. 내려진다면 이 절이 그 설계의 입력이고,
답해야 할 것은 최소 넷이다:

1. 규칙이 **0개 칸**에 맞는 작업을 만들었을 때 그 작업은 어디서 보이나 (P1)
2. 규칙이 **2개 칸**에 맞을 때 어느 칸인가 (P1)
3. 다중 값을 받는 칸으로의 드래그는 **무엇을 쓰나** (P2·22.4)
4. 리스트/태그 조건이 걸린 칸으로의 드래그는 **소속을 바꾸나, 거부하나** (P3)

넷 중 어느 것도 스크린샷과 문서로는 답이 안 나온다. 참조 앱의 답을 보려면 그 앱을 직접
써봐야 한다.


---

## 23. C안 설계 — 칸의 조건 (2026-08-28)

§20.5가 "열지 않는다"고 한 것을 연다. §20.4의 P1·P2·P3은 사라지지 않는다 — **답이 주어질
뿐이다.** 이 절은 그 답 넷과, 그것들이 강제하는 구조다.

전제를 먼저 말한다: **이 단계 이후 사분면은 저장된 필터 4개다.** §13이 걱정한 그것이 맞다.
파생이라는 성질(§20.3)은 남지만 — 소속은 여전히 계산된다 — **전함수도 아니고 1:1도
아니게 된다.** 그 대가로 얻는 것이 "일 리스트만 보는 매트릭스"다.

### 23.1 규칙의 모양

```ts
export interface MatrixQuadrantRule {
  /** 빈 배열 = "전체". 이 차원에 조건 없음. */
  listIds: string[];
  tagIds: string[];
  dateBuckets: DateBucket[];
  priorities: TaskPriority[];
}
```

**빈 배열 하나가 "전체"를 뜻한다.** 별도의 `any` 표식을 두지 않는 이유는 §21.3과 같다 —
"조건 없음"과 "빈 조건"이 다른 상태로 표현 가능하면 둘이 어긋날 수 있다.

**날짜는 범위가 아니라 `DateBucket` 집합이다.** 참조 앱은 "오늘 ~ 모레" 같은 범위를 쓰지만,
범위를 받으면 상대 날짜 축의 대수(모레 + 7일 후 = ?)를 새로 만들어야 한다. 우리에겐 Phase 2가
만든 `dateBucketOf`(`overdue | today | tomorrow | later | none`)가 이미 있고, **칸 안의
그룹 머리말이 쓰는 어휘와 같다.** 규칙과 화면이 같은 단어를 쓰면 "기한 초과만 받는 칸"이
말 그대로 보인다.

**작업 유형(과제·노트)은 넣지 않는다.** 이 앱에 노트라는 레코드 종류가 없다(§22.4).
필터 한 줄을 위해 없는 개념을 만드는 것은 꼬리가 개를 흔드는 일이다. 규칙이 데이터가 되면
나중에 필드 하나 더하는 일이므로, 지금 자리를 비워두는 비용이 낮다.

저장 위치는 `AppSettings.matrixQuadrantRules` — `matrixQuadrantViews`와 **별개 필드**다.
view는 "어떻게 그리나", rule은 "무엇이 들어오나"로 서로 다른 질문이고, 하나를 고쳐도 다른
하나가 안 움직여야 한다.

### 23.2 기본값은 지금과 글자 하나 다르지 않다

```ts
DEFAULT_MATRIX_RULES = {
  I:   { listIds: [], tagIds: [], dateBuckets: [], priorities: ["high"] },
  II:  { …, priorities: ["medium"] },
  III: { …, priorities: ["low"] },
  IV:  { …, priorities: ["none"] },
};
```

이게 참조 앱의 **조합 1**이고 우리의 D1이다. 규칙을 한 번도 안 건드린 계정은 아무것도
저장하지 않고 지금과 똑같이 동작한다(M0 가법 규칙).

그리고 **조합 2가 공짜로 따라온다** — 규칙이 데이터가 되는 순간 §20.7의 B안은 별도 단계가
아니라 저장된 규칙 한 벌이다:

```ts
PRESET_TIME_AND_PRIORITY = {
  I:   { dateBuckets: ["overdue", "today", "tomorrow"], priorities: ["high", "medium"] },
  II:  { dateBuckets: ["later", "none"],                priorities: ["high", "medium"] },
  III: { dateBuckets: ["overdue", "today", "tomorrow"], priorities: ["low", "none"] },
  IV:  { dateBuckets: ["later", "none"],                priorities: ["low", "none"] },
};
```

스크린샷의 `예시` 버튼은 이 프리셋 고르기로 읽는다 [추론]. 빈 화면에서 조건 다섯 줄을
처음부터 채우게 하는 것보다, **동작하는 한 벌을 얹고 거기서 고치게** 하는 편이 낫다.

### 23.3 P1 답 — 0개 칸에 맞는 작업

**칸에는 안 그린다. 대신 격자 아래 한 줄이 개수를 말한다.**

```
┌─────────┬─────────┐
│    Ⅰ    │    Ⅱ    │
├─────────┼─────────┤
│    Ⅲ    │    Ⅳ    │
└─────────┴─────────┘
› 어느 칸에도 맞지 않는 작업 12개
```

접혀 있고, 0이면 줄 자체가 없다. 펼치면 그 작업들이 목록으로 나온다.

**다섯 번째 칸을 만들지 않는 이유**: 이 파일 맨 위의 주석이 "네 개의 열은 매트릭스가
아니다"라고 적어놨다. 격자 안의 다섯 번째 상자는 같은 죄다. 이건 사분면이 아니라
**나머지**이고, 격자 밖에 그리는 것이 그 사실을 말한다.

**그냥 사라지게 두지 않는 이유**: 참조 앱은 이걸 기능이라고 설명한다("먼 미래 작업이 더
이상 표시되지 않아 지금에 집중할 수 있다"). 맞는 말이지만 **"내 작업이 어디 갔지"는 할 일
앱이 낼 수 있는 최악의 버그**이고, 사라짐과 버그는 화면에서 구별되지 않는다. 개수 한 줄이
그 둘을 가른다.

### 23.4 P1 답 — 2개 이상에 맞는 작업

**읽기 순서(Ⅰ→Ⅱ→Ⅲ→Ⅳ)의 첫 칸이 가져간다.** 양쪽에 그리지 않는다 — 같은 카드가 두 번
있으면 한쪽에서 체크한 것이 다른 쪽에서 움직이고, 그건 사용자가 버그로 읽는다.

Ⅰ이 이기는 것이 맞는 이유: 겹침이 생겼을 때 **더 급한 칸으로 올리는 쪽이 안전**하다.
"지금 하기"에 잘못 올라온 일은 눈에 띄어 고쳐지고, "나중에"로 잘못 내려간 일은 안 보인다.

**겹침은 막지 않고 알린다.** 두 규칙이 겹치는지는 정확히 계산된다 — 차원마다 집합 교집합을
구하고 전 차원에 AND를 걸면 된다(빈 배열은 전체집합). 그런데 **저장을 거부하지는 않는다.**
사용자가 원하는 설정을 앱이 거절하는 것보다, 예측 가능하게 해소하고 **편집기에서 그 사실을
말하는** 편이 낫다:

> ⚠ 이 규칙은 Ⅱ와 겹칩니다. 겹치는 작업은 Ⅰ에 나타납니다.

### 23.5 P2·P3 답 — 드래그가 쓰는 것과 안 쓰는 것

**드래그는 우선순위와 날짜만 쓴다. 리스트와 태그는 절대 안 쓴다.**

| 차원 | 이미 맞으면 | 안 맞으면 |
|---|---|---|
| 우선순위 | 안 씀 | 집합에서 **가장 높은 것**을 씀 |
| 날짜 | 안 씀 | 버킷의 **대표 날짜**를 씀 (`today`→오늘, `tomorrow`→내일, `overdue`→어제, `later`→오늘+7) |
| 날짜 `none`만 받는 칸 | — | **거부** (아래) |
| 리스트 · 태그 | 안 씀 | **거부** |

세 가지가 여기서 결정된다.

**(1) 마감일은 지우지 않는다.** 규칙이 "날짜 없음"만 받는 칸이라도, 드롭으로 사용자가 정한
마감일을 지우지 않는다. §4.2의 2번 — D1이 존재하는 이유 — 이 정확히 그 사고였다.

**(2) 소속은 드롭으로 안 바뀐다.** 카드를 상자에 떨어뜨리는 것이 "이 작업을 일 리스트로
옮긴다"를 뜻할 수는 없다. 태그도 같다.

**(3) 그래서 받을 수 없는 칸이 생긴다 — 그 칸은 떨어지기 전에 거부한다.** 우선순위와 날짜만
써서 규칙을 만족시킬 수 없으면, `dragover`에서 **드롭존이 켜지지 않고** 커서가 "안 됨"을
말한다. 그리고 이유를 말한다:

> 이 칸은 '일' 리스트의 작업만 받습니다

**떨어진 뒤 조용히 실패하거나 소속을 바꾸는 것보다, 떨어지기 전에 거절하는 것이 낫다.**
참조 앱이 이 경우 무엇을 하는지는 [미확인]이고, 우리는 이쪽을 고른다.

`patchForQuadrant`의 기존 계약은 유지된다 — 빈 패치는 "이미 맞음"이다. 새로 생기는 것은
**`null` = "이 칸은 이 작업을 받을 수 없음"** 이라는 세 번째 답이다.

### 23.6 새로 만드는 것도 같은 규칙을 따른다

`draftForQuadrant`도 규칙을 읽는다. `+`로 Ⅰ에 만든 작업은 Ⅰ의 규칙을 만족하는 필드를
갖고 태어난다 — 조합 1이면 `priority: high` 하나, 조합 2면 `priority: high` + 오늘 날짜.
참조 앱의 문서가 정확히 이렇게 설명한다(§20.2).

**리스트 조건이 걸린 칸의 `+`는 그 리스트에 만든다.** 드래그와 다른 이유: 새 작업에는
사용자가 정해둔 소속이 없으므로 덮어쓸 것이 없다.

### 23.7 이 변경이 되살리는 것 둘

**§18.2 — 칸 안의 우선순위 정렬.** 한 칸이 우선순위 여럿을 받을 수 있게 되면 그 정렬은
다시 의미를 갖는다. `MATRIX_SORT_KEYS`에 `priority`를 되돌려야 하고, `sanitizeMatrixView`가
저장된 `sortKey: "priority"`를 기본값으로 접는 코드는 제거된다.

**§4.4 Q3 — `viewSpec`의 `quadrant` 축.** 우선순위와 완전히 중복이라 열린 질문으로 남아
있었는데, 규칙이 들어오면 다시 구별된다.

### 23.8 다시 열리는 질문 — 리스트 스코프 셀렉트

§20.9가 Q1을 "유지"로 종결한 근거는 **C안을 안 열기 때문**이었다. 열면 근거가 사라진다.

이제 작업이 화면에서 안 보이는 이유가 **둘**이다 — 상단 셀렉트가 걸러냈거나, 칸의 규칙이
안 맞거나. 사용자는 어느 쪽인지 알아야 한다.

지금 답을 정하지 않는다. 둘의 차이는 실재한다(셀렉트는 세션 동안의 임시 시야, 규칙은 칸마다
저장되는 영구 결정). 다만 **§23.3의 나머지 줄이 이유를 구분해서 말해야 한다** —
"어느 칸에도 맞지 않는 작업"과 "지금 보고 있는 리스트 밖의 작업"은 다른 문장이다.
**Q1을 다시 연다.**

### 23.9 단계

| | 내용 | 왜 이 순서인가 |
|---|---|---|
| **6a** | 규칙 도메인 — `MatrixQuadrantRule`, `matchesRule`, `quadrantOf(task, rules)`, `patchForQuadrant` → `null` 가능, 프리셋 둘. **UI 없음** | **완료**(§24) |
| **6b** | 나머지 줄(§23.3) + 거부하는 드롭존(§23.5) | **완료**(§25) |
| **6c** | 편집 다이얼로그 — 다섯 줄(작업 유형 제외 넷), 겹침 경고, 프리셋, 기본값 복원 | **완료**(§26) |

6b를 6c보다 먼저 놓는 것이 이 계획의 유일한 비직관적 부분이고, 가장 중요한 부분이다.

### 23.10 Phase 5는 버리지 않는다

§21의 이름·설명·색 편집기는 그대로 두고, C안의 조건 네 줄이 **같은 다이얼로그에 합쳐진다.**
참조 앱의 실물에 이름 칸이 없다는 사실(§22.2)은 그것이 나쁘다는 증거가 아니다 — 칸을
"화요일 마감"이라 부를 수 있는 것은 조건을 고칠 수 있게 된 뒤에 **더** 쓸모 있어진다.
조건을 바꾼 칸에 원래 이름이 남아 있으면 그 이름이 거짓말이 되기 때문이다.

합쳤을 때의 다이얼로그:

```
Ⅰ 칸 편집
  이름       [ 지금 하기            ]
  설명       [ 중요하고 급한 일      ]
  색         ○ ● ● ● ● ● ● ● ●
  ───────────────────────────────
  리스트     [ 전체              ▾ ]
  태그       [ 전체              ▾ ]
  날짜       [ 전체              ▾ ]
  우선순위   ☐전체 ☑높음 ☐중간 ☐낮음 ☐없음
  ⚠ 이 규칙은 Ⅱ와 겹칩니다 — 겹치는 작업은 Ⅰ에 나타납니다
  [기본값으로 되돌리기] [프리셋]    [취소] [저장]
```

위 셋은 **이름**, 아래 넷은 **내용**. 구분선 하나가 그 차이를 말한다.

### 23.11 확인하지 않은 것

1. 참조 앱이 **0개·2개 칸 문제를 어떻게 푸는지** — §23.3·§23.4는 우리 답이지 관찰이 아니다
2. 리스트/태그 조건이 걸린 칸에 드롭했을 때 참조 앱의 동작 — §23.5의 거부도 우리 답이다
3. `예시` 버튼이 정말 프리셋인지 [추론]
4. 상단 배너(`🔴 중요하고 급한 일`)가 읽기 전용인지 이름 입력인지 (§22.1)


---

## 24. Phase 6a 구현 기록 (2026-08-28)

규칙이 데이터가 됐다. **화면은 한 줄도 안 바뀌었다.**

### 24.1 바뀐 것

| 파일 | 내용 |
|---|---|
| `src/domain/view/matrixRules.ts` | **새 파일** — 규칙 · 프리셋 둘 · 정화 · `matchesMatrixRule` · `quadrantForTask` · `patchForRule` · `draftForRule` · `matrixRulesOverlap` |
| `src/types.ts` | `AppSettings.matrixQuadrantRules` (선택 필드, M0) |
| `src/domain/plannerData/normalize.ts` | 저장된 규칙 정화 |
| `src/domain/view/matrixRules.test.ts` | **새 파일** — 테스트 29개 |

**`MatrixPage`는 손대지 않았다.** `quadrantOf`를 그대로 부른다.

### 24.2 왜 화면에 연결하지 않았는가

§23.9가 6b(나머지 줄)를 6c(편집기)보다 먼저 놓은 것과 **같은 이유**다.

기본 규칙에서 `quadrantForTask`는 전함수이므로 `null`이 안 나온다 — 딱 하나, **이 빌드가
모르는 우선순위**만 빼고. `quadrantOf`는 그것을 `?? "IV"`로 받아 Ⅳ에 넣는다. 규칙 경로는
`null`을 준다. 즉 지금 연결하면, 다른 클라이언트가 쓴 우선순위를 가진 작업이 **네 칸 어디에도
안 그려진다.** 나머지 줄이 없는 상태에서.

작업이 계정에 있는데 화면 어디에도 없는 것 — §23.3이 "할 일 앱이 낼 수 있는 최악의 버그"라고
쓴 그것이다. 그래서 도메인만 얹고, 받을 곳이 생기는 6b에서 연결한다.

### 24.3 테스트가 지키는 것 하나

첫 describe이 전부다:

```ts
expect(quadrantForTask(subject, DEFAULT_MATRIX_RULES, ctx)).toBe(quadrantOf(subject));
```

**새 기계를 옛 동작에 못 박는다.** 6a의 주장 전체가 "아무도 보는 것이 안 바뀐다"이고, 그건
기본 규칙이 `quadrantOf`와 정확히 같은 답을 낼 때만 참이다. 전함수인 것과 네 규칙이 서로
겹치지 않는 것도 함께 고정했다.

### 24.4 §23의 답 넷이 코드가 된 자리

| 답 | 자리 |
|---|---|
| 0개 → `null` | `quadrantForTask` |
| 2개 → 읽기 순서 첫 칸 | 같은 함수의 루프. `MATRIX_QUADRANTS` 순서가 곧 규칙이다 |
| 다중 값 → 가장 강한 것 | `strongestPriority` · `WRITABLE_BUCKETS` |
| 리스트·태그 → 거부 | `patchForRule`이 `null` |

`WRITABLE_BUCKETS`에 **`none`이 없는 것**이 §4.2를 지키는 한 줄이다. "날짜 없음"만 받는
칸을 만족시키려면 `dueDate`를 지워야 하는데, 그건 드래그가 사용자의 마감일을 삭제하는
일이다. 그래서 그 칸은 **드롭을 아예 못 받는다**(`null`).

`overdue`가 목록 맨 끝인 것도 판단이다 — 어제 날짜를 써넣는 것은 작업을 **일부러 늦게 만드는**
일이라, 칸이 다른 아무것도 요구하지 않을 때만 맞다.

### 24.5 판단 셋

**태그는 OR다.** 규칙의 태그 중 **하나라도** 있으면 통과. AND로 하면 태그 두 개짜리 규칙은
거의 항상 빈 칸이 되고, 편집기의 그 줄은 "포함할 태그 목록"으로 읽히지 "요구할 집합"으로
읽히지 않는다.

**모르는 리스트 id는 안 지운다.** 이 모듈엔 리스트 표가 없어서 확인할 방법이 없고, 지우면
다른 클라이언트가 해석할 수 있는 조건을 이 빌드가 조용히 삭제하는 것이 된다
(`parseListColor`가 모르는 색에 대해 하는 것과 같은 선택). 해석 안 되는 id는 그냥 아무것도
매칭하지 않는다.

**`draftForRule`은 리스트와 태그를 쓴다.** 드롭과 반대다 — 새 작업에는 덮어쓸 소속이 없다.

### 24.6 검증

- `matrixRules.test.ts` 29개 통과 · `tsc` 통과
- 전체 스위트 통과
- 화면 검증 없음 — **바뀐 화면이 없다**. 그것이 이 단계의 주장이고, §24.3이 그것을 지킨다

### 24.7 다음 (6b)

§23.3의 나머지 줄과 §23.5의 거부하는 드롭존. **그리고 그때 `MatrixPage`를 규칙 경로로
연결한다** — `quadrantForTask`의 `null`을 받을 곳이 생기는 순간이다.


---

## 25. Phase 6b 구현 기록 (2026-08-28)

6a가 일부러 남겨둔 연결을 붙였다. 그 전에 **받을 곳을 먼저 지었다.**

### 25.1 바뀐 것

| 파일 | 내용 |
|---|---|
| `src/domain/view/matrixRules.ts` | `dropOutcomeForRule` + `MatrixDropRefusal`. `patchForRule`은 그 위의 얇은 껍데기가 됐다 |
| `src/components/MatrixPage.tsx` | 규칙 경로로 연결 · `UnmatchedStrip` · 거부하는 드롭존 |
| `src/app/AppPages.tsx` | `quadrantRules` 전달 |
| `src/styles/03-planning.css` | 나머지 줄 · 거부 상태 |
| i18n · 테스트 | 어휘 4개, 테스트 7개 |

`utils/eisenhower`의 `quadrantOf`·`patchForQuadrant`·`draftForQuadrant`는 **지우지 않았다.**
캘린더 우측 패널과 `viewSpec`이 아직 부른다(§4.4). 매트릭스 화면만 규칙 경로로 옮겼다.

### 25.2 거부는 이름을 갖는다

`patchForRule`이 `null`을 주는 것으로는 부족했다. **설명할 수 없는 거부는 버그와 구별되지
않는다** — "그냥 안 놓아지던데요"가 그 버그 리포트다. 그래서 `MatrixDropRefusal`이
`"list" | "tag" | "dueDate"` 셋 중 하나를 답하고, 칸이 그 문장을 말한다:

> 이 칸은 마감일 없는 작업만 받습니다. 끌어 놓아도 마감일을 지우지 않습니다

세 문장 모두 **무엇을 안 하는지**로 끝난다. 거부의 이유가 곧 이 앱의 약속이기 때문이다.

### 25.3 거부는 떨어지기 전에 말한다

`dragOver`에서 `preventDefault()`를 **부르지 않는 것**이 커서에 "안 됨"을 띄우는 방법이다.
칸은 흐려지고 테두리가 점선이 되며 이유 한 줄이 머리말 아래에 뜬다.

같은 질문을 `onDrop`이 다시 한다. 브라우저가 놓기를 막았더라도 도메인이 마지막 문지기이고,
거부된 드롭에서 `handleDrop`은 **아무것도 안 한다** — 리스트나 태그를 쓰는 대체 경로가 없다.

### 25.4 나머지는 격자 밖이다

`.ff-matrix-unmatched`는 `.ff-matrix` **바깥**에 있다. 테스트가 그것을 직접 확인한다:

```ts
expect(document.querySelector(".ff-matrix")?.contains(strip)).toBe(false);
```

2x2 안의 다섯 번째 상자는 이 화면이 존재하는 이유(파일 맨 위 주석의 "네 개의 열은 매트릭스가
아니다")를 스스로 어기는 일이다. 이건 사분면이 아니라 **나머지**이고, 그려지는 자리가 그것을
말한다.

**기본으로 접혀 있다.** 설정에 대한 보고서이지 할 일 목록이 아니므로, 매번 펼쳐져 있으면
모든 세션이 "당신이 내린 결정에 대한 설명"으로 시작한다. 0이면 줄 자체가 없다.

**펼치면 드래그 가능한 같은 카드다.** 여기서 나가는 길은 받아줄 칸으로 끌어다 놓는 것이고,
그건 화면의 나머지가 쓰는 것과 같은 동작이다. 읽기만 되는 목록은 문제를 보고하면서 해결책은
주지 않는다.

### 25.5 규칙 경로로 옮겼는데 기존 테스트가 하나도 안 깨졌다

`matrixPage.test.tsx`의 34개가 그대로 통과했다. §24.3이 도메인에서 고정한 등가성이
화면에서도 성립한다는 뜻이다 — 6a의 주장이 6b에서 검증됐다.

### 25.6 검증

- 유닛 41개(이 파일) · 전체 **1,999개 통과** · `tsc` 통과
- 실제 앱(dev, ko), Ⅰ = "기한 초과 + 높음" · Ⅱ = "일 리스트만"으로 규칙을 넣고:
  - Ⅰ에 지난 마감 높은 일 하나, Ⅱ에 일 리스트 작업 하나
  - 나머지 둘이 **"› 어느 칸에도 맞지 않는 작업 2개"** 에, 격자 밖에, 접힌 채로
  - 그 줄을 펼쳐 "집 작업"을 Ⅱ로 끌자 Ⅱ가 `is-refusing`이 되고 이유가 뜨며
    `dragover`의 `defaultPrevented`가 `false` — 커서가 "안 됨"
  - 같은 카드에 대해 Ⅰ은 거부하지 않는다 (우선순위와 날짜만으로 만족시킬 수 있다)
  - **규칙을 지우자 우선순위 매핑으로 돌아가고 나머지 줄이 사라진다**

### 25.7 다음 (6c)

편집 다이얼로그. §23.10대로 Phase 5의 이름·설명·색과 **한 다이얼로그로 합친다** — 위 셋은
이름, 아래 넷은 내용, 구분선 하나가 그 차이를 말한다. 겹침 경고(`matrixRulesOverlap`)와
프리셋 둘은 이미 도메인에 있다.

§23.8이 다시 연 질문 — 상단 리스트 스코프 셀렉트 — 은 아직 답하지 않았다. 지금은 작업이
안 보이는 이유가 둘인데(셀렉트가 걸렀거나, 규칙이 안 맞거나) **나머지 줄은 후자만 센다.**
전자는 여전히 조용하다. 6c에서 편집기가 리스트 조건을 쓸 수 있게 되면 이 둘이 나란히
놓이므로, 그때 답해야 한다.


---

## 26. Phase 6c 구현 기록 (2026-08-28)

편집 다이얼로그가 조건까지 연다. §22의 스크린샷이 목표였고, 다섯 줄 중 **넷**을 지었다.

### 26.1 바뀐 것

| 파일 | 내용 |
|---|---|
| `src/components/MatrixQuadrantEditor.tsx` | 구분선 · 조건 네 줄 · 겹침 경고 · 프리셋 · `RuleDimension` |
| `src/components/MatrixPage.tsx` | `onChangeQuadrantRule` · `onApplyRulePreset` · `tags` 전달 |
| `src/app/AppPages.tsx` | 규칙 저장, 프리셋은 네 칸을 한 번에 |
| `src/styles/03-planning.css` | 구분선 · 체크박스 줄 · 경고 · 프리셋 상자 |
| i18n · 테스트 | 어휘 11개, 테스트 7개 |

### 26.2 구분선 하나가 이 다이얼로그의 논지다

위 셋(이름 · 설명 · 색)은 **아무것도 못 망가뜨린다.** 아래 넷(리스트 · 태그 · 날짜 ·
우선순위)은 망가뜨릴 수 있다 — 조건 하나가 `quadrantForTask`를 부분함수로 만들고, 드래그가
쓸 수 없는 것을 요구할 수 있다. 그 대가는 다른 곳에서 치른다(§25의 나머지 줄, 거부하는 칸).
이 다이얼로그는 그 대가가 **발생하는 자리**이므로, 자기 자신의 어느 절반이 어느 쪽인지를
말한다: **"이 칸에 들어오는 것"**.

### 26.3 "전체"는 아홉 번째 값이 아니라 빈 선택이다

`RuleDimension`은 "전체"를 **옵션처럼 그리되 빈 배열로 저장한다**(§23.1). 고르면 나머지가
지워지고, 마지막 하나를 끄면 저절로 "전체"로 돌아온다. 표현이 하나이므로 둘이 어긋날 수 없다.

**참조 앱의 드롭다운 대신 체크박스를 썼다.** 다중 선택 드롭다운은 이 앱에 없는 위젯이고,
고정된 우선순위 네 개를 담자고 그걸 만드는 것은 한 줄에 들어가는 것에 기계를 붙이는 일이다.
리스트·태그도 같은 모양으로 두고 **넘치면 스크롤한다** — 리스트가 서른 개인 계정에서 저장
버튼이 다이얼로그 밖으로 밀려나면 안 된다.

**태그 줄은 태그가 없으면 아예 안 그린다.** 죽은 옵션 하나짜리 줄은 줄이 아니다.

**날짜의 다섯 단어는 칸 안 그룹 머리말이 쓰는 그 단어다** — "기한 초과 · 오늘 · 내일 · 이후 ·
날짜 없음". §23.1이 범위 대신 버킷을 고른 이유가 화면에서 보인다.

### 26.4 겹침은 타이핑 중에 말하고, 막지는 않는다

`matrixRulesOverlap`을 매 편집마다 돌려서 **저장 전에** 경고한다. 저장한 뒤에 알리는 것은
너무 늦게 알리는 것이다. 그러나 저장을 막지는 않는다(§23.4) — 사용자가 요청한 배치를 앱이
거절하는 것보다, 예측 가능하게 해소하고 **어떻게 해소되는지 말하는** 편이 낫다.

> 계획 세우기 칸과 겹칩니다. 겹치는 작업은 지금 하기 칸에 나타납니다

승자는 읽기 순서로 계산한다(`winnerOf`). 경고가 규칙(§23.4)과 다른 답을 말하면 그 경고는
거짓말이 되므로, 같은 순서를 쓴다.

### 26.5 프리셋은 이 다이얼로그에서 유일하게 밖으로 손을 뻗는다

칸 하나를 편집하는 화면인데 **네 칸을 모두 바꾼다.** 규칙은 한 칸의 성질이 아니라 네 칸이
함께 지키는 약속이기 때문이다(§20.7). 그래서 접혀 있고, 펼치면 먼저 그 사실을 글자로 말한다:
**"네 칸의 규칙을 모두 바꿉니다."**

이걸로 **§20.7의 B안이 별도 단계 없이 끝났다.** 조합 2 — Phase 1에서 지운 그 규칙 — 이
사용자가 고르는 항목으로 돌아왔다.

### 26.6 검증

- 유닛 48개(이 파일) · `tsc` 통과 · 전체 스위트 통과
- 실제 앱(dev, ko):
  - ⋯ → 편집 → **구분선 "이 칸에 들어오는 것"** 아래 리스트 · 날짜 · 우선순위 세 줄
    (태그는 태그가 없어 안 그려짐 — 의도대로)
  - Ⅰ의 기본값이 우선순위 "높음 ✓", 나머지 줄은 "전체 ✓"
  - "보통"을 체크하자 즉시 **"계획 세우기 칸과 겹칩니다. 겹치는 작업은 지금 하기 칸에
    나타납니다"**, 저장 버튼은 활성 그대로
  - 프리셋 → "네 칸의 규칙을 모두 바꿉니다." → `시간 + 우선순위` 선택 시 네 칸이 한 번에
    저장되고 다이얼로그가 닫히며, 화면이 조합 2로 재배치된다 (지난 마감 높은 일 → Ⅰ,
    나머지 셋 → Ⅱ, 나머지 줄 없음 — 조합 2는 전함수다)
  - 규칙을 지우면 D1으로 돌아온다

### 26.7 남은 것

**§22.5의 다섯 번째 줄, 작업 유형(과제 · 노트)은 짓지 않았다.** 이 앱에 노트 레코드가 없다
(§22.4). 규칙이 데이터가 된 지금은 필드 하나 더하는 일이므로, 노트가 생기면 그때 붙인다.

**§23.8 — 리스트 스코프 셀렉트.** 이제 편집기가 리스트 조건을 쓸 수 있으므로 §23.8이 예고한
상황이 실제로 왔다: 작업이 안 보이는 이유가 둘이고, **나머지 줄은 규칙 쪽만 센다.** 상단
셀렉트가 걸러낸 작업은 여전히 아무 말 없이 사라진다. §15 Q1은 열린 채로 남는다.

**§22.2의 미확인도 그대로다.** 참조 앱 다이얼로그의 상단 배너가 읽기 전용인지 이름 입력인지
모르고, 컨트롤 종류(드롭다운 대 체크박스)는 우리 판단이다.


---

## 27. Q1 종결 — 리스트 피커는 남는다 (2026-08-28)

§15 Q1은 세 번 답이 바뀐 질문이다. §20.9가 "유지"로 닫았지만 그 근거는 **"C안을 안 열기
때문"** 이었고, §23.8이 C안을 열면서 그 근거를 스스로 무효로 만들었다. 이번이 진짜 답이다.

### 27.1 답: 유지한다. 이유는 시간이다

피커와 규칙은 같은 일을 하지 않는다.

| | 피커 | 규칙 |
|---|---|---|
| 범위 | 네 칸 전부 | 칸 하나 |
| 수명 | 세션 동안 | 저장, 기기 간 동기화 |
| 뜻 | "지금 이것만 보자" | "이 칸은 이런 것이다" |

"5분만 일 리스트만 보자"는 피커로 **클릭 하나**다. 규칙으로 하면 칸 넷을 고치고, 끝나면
넷을 되돌려야 한다. 참조 앱에 피커가 없다는 사실(§20.9)은 그쪽이 그 필요를 규칙에 넘겼다는
뜻이지, 그 필요가 없다는 뜻이 아니다.

**그리고 피커의 숨김은 스스로를 설명한다.** 그 컨트롤이 화면 맨 위에 있고 "집"이라고 적혀
있다. 규칙의 숨김은 그렇지 않다 — ⋯ 메뉴 뒤 다이얼로그 안에 있다. §25의 나머지 줄이
**규칙 쪽만 세는 것은 이 비대칭 때문이고, 그건 옳다.**

### 27.2 그러나 둘이 만나는 자리에 조용한 곳이 둘 있었다

**(1) 나머지 줄의 개수가 스코프 안에서 세어진다.** 리스트를 고르면 "12개"가 "2개"가 되는데
왜인지는 아무도 말하지 않았다. 한 번 클릭에 반토막 나는 숫자는 **두 번 믿을 수 없는 숫자**다.
스코프가 걸려 있으면 문장이 바뀐다 — "**'일'에서** 어느 칸에도 맞지 않는 작업 2개".

**(2) 서로를 배제하는 칸이 그냥 비어 있었다.** 피커가 "집"이고 Ⅰ의 규칙이 `리스트 = 일`이면
그 칸은 절대 아무것도 못 담는다. 답의 절반은 `<select>`에, 나머지 절반은 다이얼로그 안에
있었고 **둘이 만나는 자리가 없었다.** 이제 칸이 직접 말한다:

> '집' 리스트를 보는 중입니다. 이 칸은 다른 리스트만 받으므로 여기에는 아무것도 들어올 수 없습니다

### 27.3 이 질문을 끝까지 민 덕에 나온 버그

**그 칸의 `+`로 만든 작업은 저장되고 사라졌다.**

`handleAdd`는 스코프의 리스트를 먼저, 규칙의 리스트를 나중에 편다 — 규칙이 이긴다(§23.6).
피커가 "집", Ⅰ의 규칙이 "일"이면 거기 타이핑한 작업은 **일 리스트에 만들어지고, 곧바로
스코프에 걸려 화면에서 사라진다.** 나머지 줄도 못 잡는다: 스코프 필터가 사분면 판정보다
먼저 돌기 때문에 그 작업은 `unmatched`에도 안 들어간다.

타이핑하고, 저장되고, 어느 화면에도 없다. §23.3이 "할 일 앱이 낼 수 있는 최악의 버그"라고
부른 바로 그것이, 규칙이 아니라 **두 필터의 상호작용**에서 나왔다.

**고친 방법: 그 칸의 `+`를 없앤다.** 보이지 않을 것밖에 만들 수 없는 컨트롤은 컨트롤이
아니다. 설명 한 줄이 그 자리에 남는다.

### 27.4 검증

- 유닛 52개(이 파일) · `tsc` 통과 · 전체 스위트 통과
- 실제 앱(dev, ko), Ⅰ = `리스트 = 일` 규칙:
  - 피커 "전체 리스트" → Ⅰ에 `+` 있음, 설명 없음
  - 피커 "집" → Ⅰ의 `+`가 **사라지고** 설명 한 줄이 뜬다. Ⅱ는 그대로
  - 리스트를 안 거는 규칙만 있으면 네 칸 모두 영향 없음

### 27.5 남은 열린 질문

§15에서 Q1이 빠지고, 남은 것은 **Q4**(`MatrixCard`와 `TaskRowContent` 통합)뿐이다.
§19.7이 적었듯 Phase 4가 카드에 체크 상태·아이콘·날짜를 각자 다시 구현했으므로 그 seam은
더 비싸졌다.

---

## 28. Q4 종결 — 행은 하나다 (2026-08-31)

§15 Q4는 Phase 4 이후로 열려 있던 마지막 질문이었다. 답은 합친다이고, 합치고 나서야
보인 것이 셋 있다.

### 28.1 바뀐 것

| 파일 | 무엇 |
|---|---|
| `src/components/tasks/TaskRowContent.tsx` | 팁 묶음(리스트 이름 · 반복 · 본문 · 날짜), 날짜 서식·늦음 판정, `listName` · `showPriority` · `today` |
| `src/components/MatrixPage.tsx` | `MatrixCard`가 90줄에서 표면 하나로 — 드래그와 두 판단(`listName`, `showPriority={false}`)만 남았다 |
| `src/styles/17-tasks-module.css` | `.tm-task-tips` · `.tm-task-tip` · `.tm-task-list` · `.tm-task-due.is-overdue`, 카드 표면의 줄바꿈 |
| `src/styles/03-planning.css` | 매트릭스 카드 규칙을 `.ff-matrix-card .tm-task-*`로 이전 |
| `src/i18n/*` | `matrix.card.*` → `tasks.card.*` (더 이상 매트릭스의 것이 아니다) |
| `src/components/tasks/taskRowContent.test.tsx` | 신규 9개 |
| `src/components/matrixPage.test.tsx` | 선택자 이전 + "카드는 행에서 열린다" 1개 |

### 28.2 세 화면이 그리던 세 개의 작업

합치기 전에 한 작업이 어디에 있느냐에 따라 다른 것이었다.

| | 리스트 | 보드 카드 | 매트릭스 카드 |
|---|---|---|---|
| 체크박스 | `input` | `input` | 자기 `button` + "✓" |
| 우선순위 깃발 | ✅ | ✅ | 없음(D1) |
| 리스트 이름 | 없음 | 없음 | ✅ |
| 반복 · 본문 아이콘 | 없음 | 없음 | ✅ |
| 날짜 | `2026-08-25` (저장값 그대로) | 같음 | `8월 25일` |
| 지난 날짜 | 아무 말 없음 | 없음 | 빨강 |

**이 표에서 진짜 결정은 두 줄뿐이다.** 깃발이 매트릭스에 없는 것은 D1이고, 리스트 이름이
매트릭스에만 있는 것은 그 화면만 네 리스트를 한 격자에 모으기 때문이다. 그래서 그 둘은
props가 됐다(`showPriority`, `listName`). **나머지 네 줄은 결정이 아니라 표류였고**,
COMPONENT_06 §7이 참조 앱의 행에서 이미 잰 것이다 — 날짜·메모·반복 셋이 한 행에 붙고
행 높이는 40으로 남는다. 우리는 셋 중 하나만 그리고 있었다.

### 28.3 카드가 잃은 것 하나 — 칸 색 체크박스(§19.3)

매트릭스의 체크박스는 `--q-color` 테두리를 쓰던 커스텀 버튼이었다. 합친 행의 상자는
네이티브 `input`이고, **브라우저는 체크 안 된 상자의 테두리를 스타일시트에 넘겨주지
않는다.** 되찾으려면 `appearance: none`으로 두 화면 모두의 체크박스를 다시 그려야 하는데,
그건 체크박스 구현을 둘로 만드는 일 — 이 작업이 없애려던 바로 그것이다.

그래서 색을 뺐고, 그것이 감당되는 이유는 §19.3이 스스로 적어 뒀다: 그 색은 **두 줄 위
머리말이 글자로 말하는 것을 되풀이하는 색**이었다. 칸의 색은 머리말·테두리·입력칸에
그대로 남아 있다. COMPONENT_06 §6.1이 "우선순위를 체크박스 색으로"를 거부한 것과도 같은
방향이다.

### 28.4 카드가 잃은 것 둘 — "아무 데나 눌러 열기"

카드는 `role="button"`이었고 그 안에 체크박스와 (이제) 제목 버튼이 있다. **컨트롤 안의
컨트롤**은 키보드가 설명할 수 없고 스크린 리더가 두 번 읽는다. 카드에서 그 역할을 걷고
행의 제목 버튼을 유일한 입구로 뒀다 — 그 버튼이 체크박스를 뺀 카드의 모든 픽셀을 차지하므로
포인터로는 달라진 것이 거의 없고, 키보드로는 탭 두 번이 각각 무엇인지 말한다.

### 28.5 보드 카드가 감추고 있던 결함

합치자마자 보드 카드의 **제목이 사라졌다**. 원인은 이번 변경이 아니었다: 카드는
`[체크박스][행][열 이동 select]`인 한 줄이고, 260px 열에서 select가 92px을 가져간다.
행에 남는 것이 91px이라 제목(`flex: 1`)이 0으로 눌린 것이다. 팁이 하나(날짜)일 때는
"제목이 잘린다"였고, 셋이 되자 **제목이 없다**가 됐다.

고친 것은 두 줄이다. **카드에서는 팁이 제목 아래로 내려가고**(행에는 없는 세로 여유가
카드에는 있다), **select가 자기 줄을 갖는다**. 카드의 폭을 두고 다투는 것이 작업의 말과
우리가 붙인 컨트롤이면, 이기는 쪽은 작업의 말이다 — select는 참조 앱의 카드에 아예 없는,
드래그를 못 쓰는 사람을 위한 우리 경로다(§16.30). 대신 카드가 한 줄 높아졌다.

### 28.6 리스트와 보드가 얻은 것

- 반복하는 일과 **본문이 있는 일**이 행에서 보인다(COMPONENT_06 §7의 셋 중 둘)
- 날짜가 `2026-08-25`가 아니라 `8월 25일`이다
- **지난 마감일이 빨갛다.** 리스트에서 늦은 일은 지금까지 아무 표시가 없었다

### 28.7 검증

- 유닛 2,125개 통과(신규 10개) · `tsc` 통과 · E2E 118개 통과 — `taskRow.spec.ts`의
  17px·행 높이 기하가 그대로 통과한 것이 이번 변경의 안전선이다
- 실제 앱(dev, ko): 매트릭스 카드 36.5px·팁 우측 정렬·완료 카드 흐림과 취소선, 리스트 행
  35px·17px 상자(합치기 전과 같은 값), 보드 카드는 제목 → 팁 → select 세 줄
- 다크/라이트는 토큰을 그대로 쓰므로 따로 재지 않았다

### 28.8 남은 것

- **§15에 남은 것은 Q5뿐이다.** Q1은 §27, Q2·Q3은 Phase 1, Q4는 여기서 닫혔다. Q5(칸
  제목을 `eis.*`로 옮기면 `matrix.*` 키가 고아가 된다)는 Phase 1이 문제의 키 넷을 이미
  지웠고(§16.1) 이번에 `matrix.card.*` 둘이 `tasks.*`로 옮겨 갔지만, **표를 닫을 만큼
  세어 보지는 않았다**
- 보드 카드가 항상 두 줄 이상이 됐다. 밀도가 문제가 되면 답은 select를 숨기는 것이 아니라
  **열 폭**이다 — 260px은 이 카드가 그리게 된 것에 비해 좁다
- `.tm-task-title`에는 아직 하한이 없다. 카드는 줄바꿈으로 피했지만, 창이 아주 좁은
  리스트 행에서는 여전히 팁이 이기고 제목이 준다

---

## 29. 규칙 어휘가 이 문서를 떠났다 (2026-08-31)

기본함 보드가 `matrixRules.ts`를 쓰기 시작하면서 그 파일 이름이 거짓말이 됐다
(기본함 문서 §7 Q3). 규칙의 **어휘**는 `src/domain/view/viewRules.ts`로 나갔고, 이 문서가
말하는 사분면 넷 — 기본값·프리셋 둘·읽는 순서·저장 게이트 — 만 `matrixRules.ts`에 남았다.

§24·§25가 적은 이름 중 다음은 이제 다른 곳에 있다:

| 이 문서가 적은 이름 | 지금 |
|---|---|
| `MatrixQuadrantRule` | `ViewRule` (`viewRules.ts`) |
| `matchesMatrixRule` · `sanitizeMatrixRule` | `matchesRule` · `sanitizeRule` |
| `matrixRulesOverlap` | `rulesOverlap` |
| `MatrixDropRefusal` · `MatrixRuleDraft` | `DropRefusal` · `RuleDraft` |
| `EMPTY_MATRIX_RULE` | `EMPTY_RULE` |

`quadrantForTask` · `DEFAULT_MATRIX_RULES` · `TIME_AND_PRIORITY_MATRIX_RULES` ·
`MATRIX_RULE_PRESETS` · `resolveMatrixRules` · `sanitizeMatrixRules`는 이름 그대로 남았다 —
그것들은 정말로 이 화면의 것이다. 동작은 하나도 바뀌지 않았다. 자세한 근거와 분리의 경계는
`TICKTICK_INBOX_COLUMNS_DESIGN.md` §15에 있다.

---

## 30. Q5 종결 — 세어 봤다 (2026-08-31)

### 30.1 질문이 걱정한 방향과 반대로 일어났다

§15 Q5: *"칸 제목을 `eis.*`(설명형)로 바꾸면 `matrix.*` 키 8개가 고아가 된다. 지울까
유지할까"*.

바뀐 적이 없다. 칸 제목은 `matrix.q*`로 남았고(§21이 그 위에 사용자 이름을 덮을 수 있게
했다), **고아가 된 것은 `eis.*` 쪽**이다 — 이전 화면의 설명형 제목들이 아무도 부르지 않는
채로 남아 있었다.

| | 무엇 | 상태 |
|---|---|---|
| `matrix.q*` (8개) | 칸 이름과 설명 | **살아 있다** — `MatrixPage`가 `matrix.q${quadrant}`로 부른다 |
| `eis.qI~qIVHint` + `eis.overdue` (9개) | 옛 설명형 제목 | **고아** → 지웠다 |
| `eis.today` · `eis.minutes` | 캘린더 오른쪽 패널이 쓴다 | 살아 있다 |

`eis.qIV` 위에 있던 주석("'둘 다 아님'이라고 부르지 않는 이유")도 함께 나갔다. 그 판단은
지금 `matrix.qIVHint`가 **자기 문장으로** 하고 있다 — en은 `Neither — and anything not
judged yet`, ko는 `둘 다 아닌 일 · 아직 판단하지 않은 일`. 죽은 키에 붙은 주석이 살아 있는
키의 결정을 대신 설명하고 있었다.

### 30.2 세는 방법과, 그 방법이 놓치는 것

키 992개를 `src/**` + `e2e/**`(카탈로그 자신은 빼고)에서 문자열로 찾고, 런타임에 조립되는
것들(`` t(`matrix.q${quadrant}`) `` 같은)은 **접두사 가족 단위로 살아 있다고** 쳤다.

이 방식은 **거짓 고아를 만들지 않는다** — 부르는 자리가 있으면 반드시 잡힌다. 대신
**놓치는 고아가 있다**: `tasks.` 같은 넓은 접두사가 동적으로 쓰이면 그 아래 전부가 살아
있는 것으로 계산된다. 그러니 아래 숫자는 **하한**이다.

### 30.3 이 저장소의 고아는 109개다

`eis.*` 아홉을 지운 뒤, 남은 고아를 이름공간별로:

| 이름공간 | 개수 | 짐작 |
|---|---|---|
| `list` | 24 | |
| `tree` | 19 | 사이드바 트리의 이전 세대 |
| `taskDetail` · `spaceHub` · `app` · `todayv` | 각 11 | `spaceHub`은 제거된 Spaces의 잔재(`SPACE_REMOVAL_IA.md`) |
| `spaces` | 10 | 같은 제거 |
| `folders` 4 · `sidebar` 3 · `focus` 2 · `kit` 2 · `settings` 1 | 12 | |

**매트릭스 쪽은 0이다.** Q5가 물은 범위는 여기서 끝난다. 나머지 109개는 다른 문서들이
지운 화면들의 잔재이고, 한 번에 지우는 것은 이 문서의 일이 아니다 — 다만 **세어 두었으니
다음 사람은 조사부터 하지 않아도 된다.**

### 30.4 그리고 카탈로그에 시험을 하나 붙였다

`src/i18n/catalogue.test.ts`: 두 언어가 **같은 키 집합**을 정의할 것, **같은 키를 두 번**
적지 않을 것, 그리고 `eis.q*`가 **돌아오지 않을 것**.

고아 수를 시험으로 고정하지는 않았다. 지금 109개인 숫자를 통과선으로 박으면 다음 사람이
화면 하나를 지울 때마다 이 시험이 그 사람을 막는다 — 세는 일은 문서의 일이고, 시험이 지킬
값어치가 있는 것은 **짝**이다. 한쪽 언어에만 추가된 키는 절반의 독자에게만 영어로 보이는
화면이고, 언어를 바꿔 보기 전까지 아무도 모른다.

### 30.5 검증

- 유닛 2,143개 통과(신규 3개) · `tsc` 통과 · 삭제한 키 9개 × 2언어
