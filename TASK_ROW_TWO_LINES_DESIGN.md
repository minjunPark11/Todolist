# 행이 두 줄일 때 — 요약 한 줄, 깃발 없이, 선 하나

> 상태: **구현됨** · 2026-09-03 (§4의 네 단계 전부 · §5의 테스트 갱신 포함)
> (사용자가 캡처 넷을 주었다: 폴더 화면의 이중선 · 행의 깃발 · `상세 보기`를 켠 행 ·
> 참조 앱의 두 줄 행.)
> 대상: `components/tasks/TaskRowContent.tsx` · `components/tasks/TaskCheck.tsx` ·
> `styles/17-tasks-module.css` · `i18n/{en,ko}.ts`
> 선행 문서: `TASK_PRIORITY_CHECKBOX_DESIGN.md`(§4·Q1) ·
> `FOLDER_TREE_AND_VIEW_DESIGN.md`(§5·§13.4) · `TASK_CONTENT_MODE_DESIGN.md`(§3.8)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 4장 — ① 폴더 화면, 그룹 경계마다 선 두 개 ② 행 오른쪽의 주황·빨강 깃발 ③ `상세 보기`를 켠 행: 제목이 사라지고 회색 본문만 남았다 ④ 참조 앱: 제목 아래 작은 회색 한 줄 | [관찰] |
| **실측** | `17-tasks-module.css:333`·`:346`·`:477`·`:935`·`:4048` · `TaskRowContent.tsx:167`·`:208`·`:214`·`:229` · `TaskCheck.tsx` · `01-base.css:145`·`:349` | [실측] |
| **결정** | 사용자가 고른 것 셋 — 선은 하나 · 깃발은 없앤다 · 요약은 제목 아래 작게 | [결정] |
| **추론** | 두 줄 행의 높이·패딩·체크박스 정렬 오프셋 | [추론] |

**세 건이 한 문서에 있는 이유:** 셋 다 **리스트 행이 그려지는 방식** 하나를 건드리고,
③을 고치면 행이 두 줄이 되므로 ②의 자리(제목 끝)와 ①의 경계선이 같이 재정의된다.
따로 고치면 두 줄 행에서 깃발이 어디로 가는지를 두 번 결정하게 된다.

---

## 1. ① 선이 두 개다

### 1.1 원인 [실측]

폴더 화면은 List마다 `<section class="tm-listgroup">`을 그리고
(`TasksModule.tsx:1471`), 경계에 **두 규칙이 동시에** 선을 긋는다:

| 선 | 규칙 | 위치 |
|---|---|---|
| 위쪽 | `.tm-task { border-bottom: 1px solid var(--border-subtle) }` (`:346`) | 그룹 **마지막 행 바로 밑** |
| 아래쪽 | `.tm-listgroup + .tm-listgroup { border-top: 1px }` (`:333`) | 다음 그룹 헤딩 **위** |

사이는 `.tm-listgroups { gap: 12px }` (`:288`) — 그래서 12px 떨어진 선 두 줄이 된다.
스크린샷 ①의 간격과 일치한다. **버그는 규칙 하나가 잘못된 것이 아니라, 하나의 경계에
주인이 둘인 것이다.**

`:330`의 주석이 이미 이 모순을 적어 두었다: *"아래 행들은 이미 자기 구분선을 갖고 있다"* —
그렇게 적고서 선을 하나 더 그었다.

### 1.2 D1 — 어느 쪽을 남기는가 [결정]

| | **A. 행의 선을 남긴다** (채택) | B. 그룹의 선을 남긴다 |
|---|---|---|
| 경계선의 자리 | 마지막 행에 **붙어서** | 다음 헤딩 위, 12px 띄워서 |
| 접힌 그룹 사이 | 선 없음, 여백 12px | 선 있음 |
| 스크린샷 ②와 | 같다(행은 선으로 끝난다) | 다르다 |
| 다른 화면에 주는 영향 | 없다(`.tm-task`는 그대로) | 플랫 리스트·검색·휴지통의 마지막 행에서 선을 빼야 한다 |
| 고칠 곳 | 규칙 **한 줄** | 규칙 하나 + 스코프 셋 |

**A.** 구분선은 **행의 것**이다 — 앱의 모든 리스트에서 이미 그렇고, 참조 앱도 행을 선으로
끝낸다. 그룹은 선이 아니라 **여백과 헤딩**으로 구분된다.

접힌 그룹이 연달아 있으면 그 사이엔 선이 없다 — 참조 앱이 섹션을 그리는 방식 그대로이고,
헤딩 자신이 이미 경계다.

### 1.3 바꿀 것

```css
/* 17-tasks-module.css:330-336 */
/* 경계선은 행의 것이다(§1.2 D1). 그룹의 마지막 행이 이미 선으로 끝나는데
   다음 헤딩 위에 한 줄 더 그으면 12px 떨어진 선 두 줄이 된다 —
   하나의 경계에 주인이 둘이었다. 그룹을 나누는 것은 여백과 헤딩이다. */
.tm-listgroup + .tm-listgroup {
  padding-top: 8px;
}
```

`gap: 12px` + `padding-top: 8px` = 그룹 사이 20px. 선은 마지막 행 밑의 한 줄.

---

## 2. ② 깃발을 없앤다

### 2.1 이건 되돌리기다, 새 결정이 아니다 [실측]

`TASK_PRIORITY_CHECKBOX_DESIGN.md` Q1이 이 자리를 열어 두었다:

> 1. ~~깃발을 남길 것인가~~ — **남긴다** [결정 · 2026-09-03]. 색 + 모양 두 채널(§4.4).
>    **나중에 참조 앱처럼 하고 싶어지면 `TaskRowContent.tsx:196`의 조건 한 줄이다.**

그 문서 §1.1도 참조 앱을 이렇게 실측해 두었다: *"깃발 배지는 어디에도 없다 — 우선순위는
오직 체크박스 색으로만 말해진다."* 사용자가 지금 그쪽을 택했다. [결정]

### 2.2 D2 — 그러면 색만 남는데, §4.4는 어떻게 되는가

깃발은 **색이 아닌 두 번째 채널**이었다(모양 + `aria-label`). 없애면 화면에서
우선순위는 **체크박스 테두리 색 하나**로만 말해진다. 세 부류가 이걸 못 읽는다:

1. 색을 구분하지 못하는 사용자 — 보통(`#ff9500`)과 높음(`#ff3b30`)이 가장 위험하다.
2. 스크린 리더 — 깃발의 `aria-label={t('priority.high')}`가 유일한 낭독원이었다.
3. `forced-colors: active` — `:465`가 테두리를 `CanvasText`로 되돌린다. **색이 아예 없다.**

**답: 픽셀을 쓰지 않는 채널로 옮긴다 — 체크박스의 접근 이름.** [결정]

```
before: "Complete Read ch. 4"
after : "Complete Read ch. 4 (High)"        // priority !== "none" 일 때만
```

- 화면 폭은 0px 든다.
- 2·3번이 그대로 해결된다.
- 1번은 **남는다** — 이건 되돌리기의 비용이고, 사용자가 참조 앱의 그림을 택한 값이다.
  값을 더 치겠다면 `⋯` 메뉴와 상세 헤더가 우선순위를 **글자로** 말한다(§2.4). 행에서만
  색이 유일 채널이 된다.

`TaskCheck.tsx`의 주석과 `taskRowContent.test.tsx:239`("leaves the priority out of the
box's name")가 이 결정으로 **뒤집힌다** — 둘 다 "깃발이 이미 말한다"를 근거로 삼고 있고,
깃발이 없어지면 근거가 사라진다. 주석과 테스트를 같이 갱신한다.

이름은 `TaskRowContent`가 조립한다. `TaskCheck`는 `label`을 받는 그대로 그린다 —
상세 헤더의 `□ 완료`는 옆에 깃발 트리거가 **남아 있으므로**(§2.4) 같은 접미사를 붙이면
한 사실을 두 번 읽게 된다.

### 2.3 지울 것

| 파일 | 무엇 |
|---|---|
| `TaskRowContent.tsx:209-224` | 깃발 블록 전체 + 그 위 주석 |
| `17-tasks-module.css:475-499` | `.tm-task-priority` · `.is-high` · `.is-medium` · `.is-low` |
| `17-tasks-module.css:537-543` | `.tm-task-priority + .tm-task-tips` (감싼 카드에서 auto 마진 둘이 부딪히던 규칙) |
| `17-tasks-module.css:586-591` | `.tm-task.is-card .tm-task-priority` |
| `i18n` | 없다 — `priority.*`는 메뉴·피커·새 접근 이름이 계속 쓴다 |

`showPriority` prop은 **남는다.** 이제 체크박스 색과 접근 이름 접미사를 한 스위치로
끈다 — 매트릭스의 사분면이 이미 우선순위인 자리에서 카드마다 그걸 반복하지 않는다는
이유는 그대로다(`MatrixPage.tsx:960`).

### 2.4 손대지 않는 것 [범위 밖]

우선순위를 **바꾸는** 자리들이다. 장식이 아니라 컨트롤이므로 남는다.

- `.tm-priority-trigger` — 상세 헤더의 깃발 버튼(피커를 연다)
- `.tm-priority-option .tm-priority-glyph` — 피커 팝오버
- `.tm-menu-flag` — `⋯`·우클릭 메뉴의 색 견본(옆에 `높음`이라는 **글자**가 있다)
- `01-base.css:680`의 레거시 `.priority-*` 배지 — 캘린더 계열, 이 문서 밖

---

## 3. ③ `상세 보기`를 켜면 제목이 사라진다

### 3.1 원인 — flex 산수 [실측]

`TASK_CONTENT_MODE_DESIGN.md` §3.8이 설계한 것은 이미 있다. `showDetails`가 켜지면
`TaskRowContent:167`이 본문 첫 줄을 만들고 `:229`가 그린다. **문제는 CSS다:**

```css
.tm-task-open { display: flex; align-items: center; gap: 12px; flex: 1; min-width: 0 }  /* :935 — 줄바꿈 없음 */
.tm-task-title { flex: 1; min-width: 0; ... }                                           /* :500 → flex: 1 1 0% */
.tm-task-body  { display: block; width: 100%; ... }                                     /* :4048 → basis 100% */
```

줄바꿈이 없는 flex 한 줄에 **폭 100%를 요구하는 항목**이 들어왔다. 남는 공간이 음수가
되므로 `flex-grow`는 적용되지 않고, 제목의 사용 폭은 **자기 basis인 0%** 그대로가 된다.
`overflow: hidden`이 나머지를 감춘다 → **제목 0px, 회색 본문만 남는 행.** 스크린샷 ③이
정확히 그 그림이다(깃발이 체크박스 바로 옆에 붙어 보이는 것도 같은 이유 — 제목이
폭 0이라 뒤 항목이 앞으로 당겨졌다).

**카드에서는 멀쩡했다.** `.tm-task.is-card .tm-task-open`(`:579`)과
`.ff-matrix-card .tm-task-open`(`03-planning.css:1106`)이 `flex-wrap: wrap`이라
본문이 둘째 줄로 내려간다. 그래서 §3.8은 **카드에서만 검증된 채** 들어왔다 —
`taskActionsMenu.test.tsx:916-952`는 DOM에 본문 span이 있는지만 보고, 폭은 보지 않는다.

### 3.2 원하는 그림 [관찰 ④]

```
┌────────────────────────────────────────────────┐
│ □  asd                              ⚑ 오늘     │   ← 14px, 제목 + 오른쪽 팁
│    ZZ Very Long List Na…                       │   ← 12px, 회색, 한 줄, 말줄임
└────────────────────────────────────────────────┘
```

- 요약은 **제목의 왼쪽 경계**에서 시작한다(체크박스 아래가 아니라).
- 한 줄이고, 넘치면 말줄임 — 이미 `.tm-task-body`가 그렇다.
- 체크박스는 **첫 줄에 맞춰** 정렬된다. 행 전체의 수직 중앙이 아니다.

### 3.3 D3 — 줄바꿈은 조건부다 [결정]

`.tm-task-open`을 항상 `flex-wrap: wrap`으로 두면 본문이 없는 행 수백 개의 레이아웃
규칙이 같이 바뀐다(제목 basis가 0이라 실제로 줄이 넘어가진 않지만, 그렇다는 것을 읽는
사람이 매번 확인해야 한다). **본문이 그려질 때만** 켠다 — `TaskRowContent`가
`.tm-task-open`에 `has-body`를 붙인다. 한 줄 변경이고, 유닛 테스트가 볼 수 있는 사실이다.

`:has()`도 가능하지만(리포에 선례 있음 — `03-planning.css:828` 등) 클래스를 택한다:
행을 그리는 컴포넌트가 이미 그 element를 갖고 있어서, 조건을 **아는 쪽이 말하는** 편이 싸다.

### 3.4 바꿀 것

**`TaskRowContent.tsx`** — `:200` 근처, `.tm-task-open` 버튼의 className:

```tsx
className={`tm-task-open${bodyLine ? " has-body" : ""}`}
```

**`17-tasks-module.css`** — `.tm-task-open`(`:935`) 뒤에:

```css
/* 두 줄이 되는 행(§3.1). 폭 100%를 요구하는 본문이 줄바꿈 없는 flex 한 줄에
   들어오면 남는 공간이 음수가 되고, 그때 제목은 자기 basis인 0%에 머문다 —
   제목이 사라지고 본문만 남던 행이 그것이었다. 줄을 넘길 수 있게 해 주면
   본문이 둘째 줄로 내려가고 제목은 첫 줄을 그대로 쓴다.
   `row-gap: 0`은 본문 자신의 `margin-top: 2px`와 겹치지 않기 위한 것. */
.tm-task-open.has-body {
  flex-wrap: wrap;
  row-gap: 0;
}

/* 첫 줄에 맞춘 체크박스(§3.2). 두 줄 행에서 상자를 행의 중앙에 두면 제목보다
   10px 아래에 앉는다 — 상자가 가리키는 것은 제목이므로, 제목의 줄에 맞춘다.
   3px = (21 − 15) / 2: 14px × 1.5 = 21px 줄 높이 안에서 15px 상자를 세운다. */
.tm-task:has(.tm-task-body) {
  align-items: flex-start;
  padding-block: 4px;
}

.tm-task:has(.tm-task-body) .tm-task-check {
  align-self: flex-start;
  align-items: flex-start;
  padding-top: 3px;
}

.tm-task:has(.tm-task-body) .tm-task-handle,
.tm-task:has(.tm-task-body) .tm-task-menu {
  align-self: flex-start;
}
```

`.tm-task`에는 `:has()`를 쓴다 — `<li>`는 `TasksModule`·`TaskBoard`가 각각 그리고 있어서
클래스로 하려면 세 곳에 같은 조건을 심어야 한다. 조건이 **한 벌**이라는 것이 여기선 더 싸다.

**`.tm-task-body`(`:4048`)** — `line-height: 1.35` 추가. 12px × 1.5 = 18px는 요약 한 줄에
필요 이상이고, 두 줄 행의 높이를 그만큼 밀어 올린다.

### 3.5 높이 [추론]

| | 한 줄 | 두 줄 |
|---|---|---|
| 내용 | 21px | 21 + 2 + 16 = 39px |
| 패딩 | 0 | 4 + 4 = 8px |
| 합 | `min-height: 36px`가 이긴다 → **36px** | **47px** |

`min-height: 36px`(`:344`)는 그대로 둔다 — 두 줄 행은 그 아래로 내려가지 않으므로
경쟁하지 않는다. 좁은 포인터 규칙(`@media (pointer: coarse)`, `:2604`)의 44px 바닥도
이미 넘는다.

**구현 후 실측** [실측 · 2026-09-03] — 브라우저에서 잰 값:

| | 예측 | 실측 |
|---|---|---|
| 한 줄 행 | 36px | **36px** |
| 두 줄 행 | 47px | **48.2px** (첫 줄이 12px 아이콘 팁 때문에 21이 아니라 22) |
| 요약의 왼쪽 경계 | 제목과 같음 | **0px 차이** |
| 체크박스 중심 − 제목 중심 | 0 | **0px** |

조정 없이 둔다.

### 3.6 D4 — 리스트 이름은 둘째 줄로 내려가지 않는다 [결정]

스크린샷 ④의 둘째 줄은 참조 앱에서는 **리스트 이름**이다. 우리 리스트 이름은 오른쪽
팁 묶음(`.tm-task-list`)에 있다. 그대로 둔다:

- 둘째 줄은 **요약**의 자리다. 요약과 리스트 이름을 같은 줄에 넣으면 서로 폭을 빼앗고,
  요약은 말줄임으로 끝나는 반 문장이 된다.
- 리스트 이름은 `.tm-task-tips`의 다른 셋(반복·본문 표시·날짜)과 **같은 종류의 사실**이다 —
  행이 자기에 대해 덧붙이는 말. 하나만 빼내면 묶음의 이유가 없어진다.
- 사용자가 가리킨 것은 ④의 **모양**(제목 아래 작은 회색 한 줄)이지 그 줄의 내용이 아니다.

### 3.7 토글은 그대로

`상세 보기`는 스코프별 뷰 옵션이고 기본값은 꺼짐이다(`scopeViewOptions.ts:61`).
켜면 요약 한 줄, 끄면 본문 아이콘 하나 — `TaskRowContent:257`의 `hasBody && !showDetails`
그대로. 이 문서는 **켰을 때 무엇이 그려지는지**만 고친다.

---

## 4. 구현 단계

각 단계가 그 자체로 배포 가능하다.

| 단계 | 무엇 | 파일 | 크기 |
|---|---|---|---|
| **1** | 그룹 경계의 선 하나 (§1.3) | `17-tasks-module.css` | 한 줄 삭제 |
| **2** | 두 줄 행 (§3.4) | `TaskRowContent.tsx` · `17-tasks-module.css` | 클래스 하나 + 규칙 넷 |
| **3** | 깃발 삭제 (§2.3) | `TaskRowContent.tsx` · `17-tasks-module.css` | 블록 하나 + 규칙 넷 삭제 |
| **4** | 체크박스 접근 이름에 우선순위 (§2.2) | `TaskRowContent.tsx` · `TaskCheck.tsx` 주석 · `i18n/{en,ko}.ts` | 키 하나 |

**2를 3보다 먼저 한다.** 스크린샷 ③에서 깃발이 잘못된 자리에 보였던 것은 제목이 폭 0이라
생긴 착시다 — 2를 먼저 고쳐야 3이 지우는 것이 무엇인지 화면으로 확인된다.

### 4.1 새 i18n 키 (단계 4)

```ts
// en
"tasks.completeTaskPriority": "Complete {{title}} ({{priority}})",
// ko
"tasks.completeTaskPriority": "{{title}} 완료 ({{priority}})",
```

`priority`에는 이미 있는 `priority.high|medium|low`를 번역해서 넣는다.
`reopenTask`는 그대로 — 끝난 일의 상자는 회색이고, 우선순위를 말하지 않는다.

---

## 5. 갱신할 테스트

| 파일 | 지금 | 뒤 |
|---|---|---|
| `taskRowContent.test.tsx:194` | 깃발이 `showPriority`에 따라 있고 없다 | 깃발은 **어느 쪽이든 없다**. 남는 것은 체크박스의 클래스와 이름 |
| `taskRowContent.test.tsx:239` | "상자 이름에 우선순위를 넣지 않는다" | **뒤집힌다** — `Complete … (High)`, `none`일 때는 접미사 없음 |
| `taskRowContent.test.tsx` (신규) | — | `showDetails`가 켜진 행의 `.tm-task-open`에 `has-body`가 붙는다 · 본문이 없으면 붙지 않는다 |
| `matrixPage.test.tsx:404` | 카드에 `.tm-task-priority`가 없다 | 선택자 자체가 사라진다 → 카드의 `.tm-check`가 `is-none`인지로 대체 |
| `e2e/taskRow.spec.ts:133-153` | 우선순위를 High로 바꾸면 `.tm-task-priority`가 하나 생긴다 | 같은 조작 뒤 **체크박스가** `is-high`가 되고 이름이 `(High)`로 끝난다 |
| `e2e/contextMenu.spec.ts:105` | `.tm-task-priority.is-high`가 보인다 | `.tm-task .tm-check.is-high` |
| `e2e` (신규) | — | ①의 회귀: 폴더 화면에서 그룹 경계의 선이 **하나**다 (`.tm-listgroup + .tm-listgroup`의 계산된 `border-top-width`가 `0px`) |
| `e2e` (신규) | — | ③의 회귀: `상세 보기`를 켠 뒤 제목 span의 폭이 0보다 크다 — **이 버그를 잡는 유일한 단정.** DOM 존재만 보는 단정은 이 버그를 통과시켰다 |

---

## 6. 바뀌지 않는 것

- **행의 구분선 자체** — `.tm-task { border-bottom }`은 그대로. §1은 두 번째 선만 지운다.
- **우선순위 팔레트** — `--priority-*` 네 토큰(`01-base.css:145`)과 체크박스의 색 규칙.
- **우선순위를 바꾸는 경로** — 피커 · `⋯` 메뉴 · 우클릭 · `⌘Z`.
- **`showDetails`의 의미와 기본값** · **첫 줄만 읽는 규칙**(`firstLine`).
- **카드·매트릭스의 두 줄 그림** — 이미 `flex-wrap`이라 §3은 아무것도 바꾸지 않는다.
- **`min-height: 36px`** — 한 줄 행의 높이.

---

## 7. 열린 질문

1. ~~**두 줄 행 47px**~~ — **닫힘**. 실측 48.2px, 정렬은 오차 0 (§3.5). 조정하지 않았다.
2. **색만으로 전달되는 우선순위**(§2.2 1번) — 깃발을 없앤 대가. 나중에 값을 치고 싶어지면
   픽셀을 쓰지 않는 채널이 하나 더 있다: 체크박스 테두리의 **모양**(높음만 두껍게 등).
   이번에 하지 않는다.
3. **접힌 그룹이 연달아 있을 때** 선이 전혀 없다 — 참조 앱과 같지만, 폴더에 접힌 List가
   여섯 개일 때 화면에서 확인해 볼 것.
