# 오른쪽 열을 비우고, 한 줄로 모으기

> 상태: **구현됨** · 2026-09-04 (H1 = A · H2 = 예 · H3 = A · H4 = 예 · H5 = A · H6 = A, 사용자 승인 — §7.1은 구현 전 정정)
> (사용자가 한 장을 주었다: 참조 캘린더 화면 — 오른쪽 패널이 없고, 네비게이션과 제목이
> 툴바 왼쪽에, 뷰 전환·검색·`⋯`이 오른쪽에, `+ 새 일정`이 사이드바 맨 위에 있다.)
> 요청: "아이젠하워 배치하는 우측 패널은 없애고, 이 이미지처럼 배치."
> 대상: `components/CalendarView.tsx` · `components/calendar/{CalendarToolbar,CalendarTitleRow,CalendarLeftSidebar,CalendarRightTaskPanel}.tsx` ·
> `styles/{02-calendar,09-calendar-redesign,10-calendar-apple}.css`
> 선행 문서: `CALENDAR_GEOMETRY_DESIGN.md` §R2(제목을 툴바 밖 자기 줄로 옮긴 결정 —
> **이 문서가 그것을 되돌린다**) · `CALENDAR_FILL_READABILITY_DESIGN.md`(직전 —
> §6의 충돌) · `CALENDAR_V3_DESIGN.md` §7(우측 패널을 만든 문서)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 참조 스크린샷 1장 | [관찰] |
| **실측** | `CalendarRightTaskPanel`이 밖에서 끌어오는 **유일한** 통로다 · 툴바는 `☰ +` / 가운데 뷰 전환 / `⋯` · 제목은 `gcal-title-row`라는 별도 줄에 있다 · 툴바 `min-height: 52px`, 제목 줄 26/32 | [실측] |
| **결정** | 사용자 — 우측 패널 제거, 참조대로 배치 | [결정] |
| **추론** | 참조의 `2026년 9월 ▾`가 무엇을 여는가 · 검색 필드의 범위 | [추론] |

---

## 1. 우측 패널을 없애면 사라지는 것 [실측]

패널은 "예정 없는 일을 아이젠하워 사분면으로 묶어 격자에 끌어다 놓는" 화면이다
(`CALENDAR_V3_DESIGN.md` §7). 지우는 것은 화면 하나가 아니다.

```ts
// CalendarRightTaskPanel.tsx — 밖에서 들어오는 드래그의 시작점
onDragStart: (event: DragEvent, taskId: string) => void;
// CalendarView.tsx:529 — 그 계약
event.dataTransfer.setData("text/plain", taskId);
```

`grep`으로 확인한 결과 **`setData("text/plain", taskId)`를 부르는 곳은 그 패널
하나뿐**이다. 격자 쪽 `onDropTime` · `onDropCell` · `onDropAllDay`는 남아 있어도
**시작할 수 있는 것이 없어진다** — 즉 "예정 없는 태스크를 캘린더에 끌어다 시간을 준다"는
동작이 통째로 사라진다.

참조 화면에도 그런 자리는 없다 [관찰]. 그 앱에서 일정을 만드는 길은 `+ 새 일정`과
격자를 끌어 만드는 것 둘이고, 우리에게도 그 둘은 이미 있다.

### 1.1 결정 H1 — 사라지는 동작을 어떻게 하나

| 안 | 무엇 | 대가 |
|---|---|---|
| **A** | 그냥 없앤다. 드롭 핸들러도 같이 정리 | "이미 적어 둔 일에 시간 주기"가 캘린더에서 불가능해진다 |
| **B** | 없애되 드롭 핸들러는 남긴다 | 아무도 시작할 수 없는 코드가 남는다 — 더 나쁘다 |
| **C** | 사이드바 아래쪽으로 옮긴다 | 사이드바가 필터 + 목록 두 가지 일을 하게 된다 |
| **D** | `⋯`(View Options) 옆에 토글로 남긴다 | 참조의 배치를 얻으면서 동작도 지킨다. 패널 코드는 남는다 |

> **권장 A.** 요청이 "없애자"이고, 그 동작을 대신할 길이 이미 둘 있다 — 태스크에 날짜와
> 시간을 주는 일은 **일정 편집기**(`ScheduleEditor`)가 하고, 캘린더에서는 격자를 끌어
> 만든다. 패널은 세 번째 길이었고, 세 번째 길을 없애는 것이 요청이다.
>
> B는 절대 하지 않는다. 시작점 없는 드롭 핸들러는 다음 사람이 "왜 안 되지"를 묻게 만드는
> 코드다. A를 고르면 `handleDragStart` · `handleDragEnd` · `dragPreview` ·
> `draggingTaskId` · `onDrop*` · `MotionDropZone`의 태스크 경로까지 함께 걷어낸다.
>
> **D는 진짜 대안이다.** "패널을 아예 지운다"가 아니라 "기본으로 접어 둔다"에 가깝고,
> 아이젠하워 분류를 쓰던 사람에게는 D가 맞다. 다만 요청의 문장은 "없애고"였다.

---

## 2. 참조와 우리 배치의 차이 [관찰] vs [실측]

| | 참조 | 지금 |
|---|---|---|
| 사이드바 맨 위 | `☰` · **`+ 새 일정`** · `→`(접기) | 없음 (`☰`와 `+`는 툴바에) |
| 툴바 왼쪽 | `‹ › 오늘 ›` + **`2026년 9월 ▾`** | `☰` `+` |
| 툴바 가운데 | — | `일 주 월 년` |
| 툴바 오른쪽 | `일 주 월 년` + **검색 필드** + `⋯` | `⋯` |
| 제목 줄 | **없음** (툴바에 합쳐짐) | 별도 줄, 26/32 |
| 오른쪽 열 | **없음** | 접힌 태스크 패널 |

바꿀 것이 넷이다: 툴바 한 줄로 합치기(§3) · `+`를 사이드바로(§4) · 검색 필드(§5) ·
오른쪽 열 제거(§1).

---

## 3. 제목을 툴바로 되돌린다 — 결정 H2

이건 **직전 결정을 뒤집는 일**이므로 그 이유를 적어 둔다.

`CALENDAR_GEOMETRY_DESIGN.md` §R2는 제목을 툴바 밖으로 꺼냈다:

> "이것은 툴바 안의 15px 헤딩이었고, 그래서 캘린더에서 가장 큰 글자가 읽고 있는 기간이
> 아니라 날짜 칸이 되었다. Calendar.app은 여기에 26pt를 쓴다."

R2의 문제 제기는 **크기**였지 **줄**이 아니었다. 참조는 제목을 툴바에 두면서도 크게
쓴다 [관찰] — 즉 "툴바 안"과 "충분히 큰 제목"은 함께 갈 수 있다. 되돌리는 것은 R2의
판단이 아니라 R2가 고른 수단이다.

얻는 것: **세로 공간 한 줄**(제목 줄의 `padding: 2px 2px 10px` + 32px 행 ≈ 44px).
주간 격자는 세로가 곧 시간이므로 44px는 한 시간에 가깝다.

```
[ ‹ › 오늘 ›   2026년 9월 ▾ ]        [ 일 주 월 년 ]  [ 🔍 일정 검색 ]  [ ⋯ ]
```

### 3.1 `2026년 9월 ▾`의 `▾`는 무엇인가 — 결정 H3

[추론]. 참조 스크린샷만으로는 알 수 없다.

| 안 | 무엇 |
|---|---|
| **A** | 아무것도 안 연다. 캐럿을 그리지 않는다 |
| **B** | 월 점프 — 사이드바 미니 달력과 같은 일 |
| **C** | 뷰 전환 — 오른쪽 세그먼트와 중복 |

> **권장 A.** B는 미니 달력이 이미 하고 있고, 사이드바를 접었을 때만 의미가 생긴다 —
> 그 조건부 가치를 위해 팝오버를 하나 더 만들 이유가 약하다. C는 명백한 중복이다.
> 캐럿은 누를 수 있어 보이는데 아무 일도 안 하면 안 되므로, **그리지 않는 쪽**이 맞다.
> 사이드바 접기가 흔한 사용이 되면 그때 B로 만든다.

---

## 4. `+ 새 일정`을 사이드바 맨 위로 — 결정 H4

지금 `+`는 툴바의 아이콘 버튼이고 이름이 툴팁에만 있다. 참조는 사이드바 맨 위의
**글자가 붙은 버튼**이다.

> **권장: 옮긴다.** 셋 다 이유가 된다.
> 1. 툴바 왼쪽은 §3에서 네비게이션과 제목이 차지한다 — `+`가 있을 자리가 없다.
> 2. 만들기는 이 화면에서 가장 자주 하는 일이고, 아이콘만 있는 32px보다 이름이 붙은
>    버튼이 낫다.
> 3. `TICKTICK_COMPONENT_10_QUICK_ADD.md`가 이미 "만들기는 이름을 갖는다"를 기록해 두었다.
>
> 사이드바를 접었을 때는 `☰`만 남는 지금의 레일 모양을 유지하고, 그 레일에 `+`를 아이콘으로
> 되돌린다 — 접힌 상태에서 만들기가 사라지면 안 된다.

---

## 5. 검색 필드 — 결정 H5

참조 툴바에 `🔍 일정 검색`이 있다 [관찰]. 우리에게는 **레일의 전역 검색**(`/`)이 이미
있고, 그것은 태스크를 찾는다.

| 안 | 무엇 |
|---|---|
| **A** | 만들지 않는다. 전역 검색이 이미 태스크를 찾는다 |
| **B** | 툴바에 필드를 두고, 입력하면 격자에서 안 맞는 블록을 흐리게 한다 |
| **C** | 필드를 두되 전역 검색 다이얼로그를 여는 버튼으로 |

> **권장 A — 이번 범위에서 제외.** B는 새 기능이고(검색어에 따른 격자 필터링, 빈 결과
> 처리, 범위 밖 결과 안내), 요청은 "배치"였다. C는 필드처럼 보이는데 타이핑이 안 되는
> 컨트롤이라 최악이다.
>
> 자리는 비워 둔다: §3의 툴바 오른쪽은 `[뷰 전환] [⋯]`이 되고, 검색이 생기는 날 그 사이에
> 들어간다. 참조를 따라 **아무 일도 안 하는 필드를 그리지 않는 것**이
> `TICKTICK_NAV_SHELL_REDESIGN_SPEC.md` §1.5의 정신이다.

---

## 6. 참조의 블록은 우리가 어제 버린 스타일이다 — 결정 H6

[관찰]의 블록은 **연한 틴트에 검은 글씨, 왼쪽에 색 막대**다. 우리는 바로 직전 커밋에서
그 반대로 갔다 — 꽉 찬 채움에 흰 글씨, 막대 없음
(`CALENDAR_FILL_READABILITY_DESIGN.md`).

그 변경의 출발점이 **"회색 블록의 검은 글씨가 안 읽힌다"는 사용자의 지적**이었다. 참조의
스타일은 그 지적이 가리키던 바로 그 스타일이다.

| 안 | 무엇 |
|---|---|
| **A** | 지금 스타일 유지. 배치만 참조를 따른다 |
| **B** | 참조의 블록 스타일로 되돌린다 |

> **권장 A.** 요청의 문장이 "배치를 하고 싶다"였고, 블록 스타일은 배치가 아니다. 그리고
> B는 하루 전에 고친 문제를 되돌린다 — 참조의 연한 블록이 읽히는 이유는 그 앱의 색이
> 우리 것보다 옅고 글씨가 진하기 때문이고, 우리 팔레트에서 같은 조합은 §2의 표대로
> 여덟 중 여섯이 기준 미달이었다.
>
> 다만 **이건 물어봐야 한다.** 참조를 고른 사람이 그 블록 모양까지 원한 것일 수 있고,
> 그렇다면 A는 요청을 절반만 들은 것이 된다.

---

## 7. 바뀌는 코드

| 무엇 | 어디 |
|---|---|
| 우측 패널 제거 | `CalendarRightTaskPanel.tsx` 삭제 · `CalendarView`의 `taskPanelCollapsed` · `handleDragStart/End` · `dragPreview` · `draggingTaskId` · `onDrop*` 배선 |
| 툴바 재구성 | `CalendarToolbar.tsx` — 왼쪽에 nav+제목, 오른쪽에 뷰 전환 + `⋯` |
| 제목 줄 제거 | `CalendarTitleRow.tsx` 삭제, `gcal-title-row` 규칙 정리 |
| 만들기 이동 | `CalendarLeftSidebar.tsx`에 `+ 새 일정`, 접힌 레일에 아이콘 |
| CSS | `09-calendar-redesign.css`의 R2 규칙 · `.gcal-taskpanel*` 전부 · 그리드 열 정의 |

`WeekView` · `MonthView`의 드롭 존은 **격자 안 블록 이동**에도 쓰이므로 그 경로는 남는다
— 걷어내는 것은 바깥에서 들어오는 `text/plain` 경로뿐이다. 구현할 때 그 둘을 분리하는
것이 이 작업에서 가장 조심할 곳이다.

### 7.1 그 문장이 틀렸다 — 구현 직전 정정

"걷어내는 것은 바깥에서 들어오는 `text/plain` 경로뿐"이 아니다. **월간 뷰가 자기 칩을
다른 날로 옮길 때 같은 `text/plain` 계약을 쓴다** (`MonthView.tsx:154`의
`onDragStartCapture` → `handleDragStart` → `dropCell`). 패널과 월간 뷰가 한 배선을
공유하고 있었다.

그래서 실제로 죽는 것은 **주간 격자 쪽 드롭 경로**다 — 주간 뷰의 블록은 포인터로
움직이므로(`startMove`), 주간 격자의 HTML5 드롭은 오직 패널만이 먹일 수 있었다.

| 무엇 | 운명 | 왜 |
|---|---|---|
| `handleDragStart` · `dragOverId` · `over`/`leave` · `dropCell` · `moveToDay` | **남는다** | 월간 칩을 다른 날로 옮기는 길 |
| `dropTime` · `dropAllDay` · `handleDragHover` · `dragPreview` · `draggingTaskId` | 죽는다 | 주간 격자 전용이었고 패널만이 시작할 수 있었다 |
| `WeekView`의 `dragOverId`·`onOverSlot`·`onLeaveSlot`·`onDragHover`·`onDropTime`·`onDropAllDay`·`dragPreview`·`draggingTaskTitle` | 죽는다 | 위와 같음 |

종일 셀의 `MotionDropZone`은 남는다 — 포인터로 블록을 종일 칸에 올릴 때
(`onMoveItemToAllDay`) 그 강조를 쓰기 때문이다. 시간 열의 `MotionDropZone`은
`isOver={false}`가 되고 포인터 핸들러만 남는다.

### 7.2 함께 사라진 문자열

`caltasks.*` 여섯 개(패널 전체)와, 카탈로그 테스트가 잡아낸 고아 여섯 개:
`calendar.showSidebar`(툴바의 `☰`) · `calendar.dropScheduled` ·
`calendar.dropScheduledOverlap` · `calendar.dropInvalid`(주간 드롭 토스트) ·
`eis.today` · `eis.minutes`(패널 행).

### 7.3 실제로 만들어진 것

| 무엇 | 어디 |
|---|---|
| 패널 제거 | `CalendarRightTaskPanel.tsx` 삭제 · `CalendarView`에서 §7.1의 죽는 목록 |
| 툴바 한 줄 | `CalendarToolbar.tsx` 다시 씀 · `CalendarTitleRow.tsx` 삭제 |
| 만들기 이동 | `CalendarLeftSidebar.tsx`의 `.gcal-sidebar-head` + 접힌 레일의 `+` |
| CSS | `10-calendar-apple.css`에 V4 절 · 죽은 규칙 43개 삭제(`08` 24 · `09` 14 · `02` 5) |
| 회귀 방지 | `e2e/calendarLayout.spec.ts` 9개 |

---

## 8. 무엇을 테스트하는가

**E2E (playwright)**
- 오른쪽 패널이 없다 (`.gcal-taskpanel` 0개)
- 툴바 한 줄에 nav · 제목 · 뷰 전환 · `⋯`이 모두 있다
- 제목 줄이 사라져 격자가 위로 올라왔다 — 격자 상단 y좌표가 이전보다 작다
- 사이드바에서 `+ 새 일정`으로 만들기가 열린다
- 사이드바를 접어도 만들기에 닿을 수 있다
- **격자 안 드래그는 그대로 동작한다** (§7의 위험 지점 — 블록 이동·리사이즈)
- 격자를 끌어 새 일정을 만드는 것도 그대로 (`calendarCreate.spec.ts` 회귀)

**삭제할 테스트**
- 우측 패널을 다루는 기존 스펙이 있다면 함께 삭제 (구현 시 확인)

---

## 9. 결정 목록

| | 질문 | 안 | 권장 |
|---|---|---|---|
| **H1** | 우측 패널의 드래그 기능 | A 없앤다 / B 핸들러만 남긴다 / C 사이드바로 / D `⋯` 토글로 | **A** |
| **H2** | 제목을 툴바로 되돌리나 (R2 뒤집기) | 예 / 아니오 | **예** |
| **H3** | `2026년 9월 ▾`의 캐럿 | A 안 그린다 / B 월 점프 / C 뷰 전환 | **A** |
| **H4** | `+ 새 일정`을 사이드바로 | 예 / 아니오 | **예** |
| **H5** | 검색 필드 | A 제외 / B 격자 필터 / C 전역검색 버튼 | **A (제외)** |
| **H6** | 참조의 연한 블록 스타일 | A 지금 유지 / B 참조로 되돌림 | **A** — 확인 필요 |
