# Planning(아이젠하워)과 우선순위 언어 정리

- 작성일: 2026-08-14
- 기준: `baed412`, 브랜치 `fix/store-write-amplification`
- 선행: `HORIZONS_DESIGN.md` **D12** — 「Planning과의 관계, 지금 정하지 않는다. Phase 3 이후 실제로 써보고 판단한다」
- 대상: `src/utils/eisenhower.ts`, `EisenhowerPage.tsx`, `TaskDetail.tsx`, `CalendarRightTaskPanel.tsx`

---

## 0. 미뤄둔 결정에 답한다

D12는 판단 근거를 **사용 경험**으로 못박고 미뤘다. Phase 3은 물론 S2·S3·S4까지 지났으니 미룰 조건이 사라졌다.

> **사용자 답 (2026-08-14): "가끔 쓴다."**

→ **Planning은 남는다.** 제거 설계는 폐기하고, 중요도 정의를 고치는 쪽으로 간다.

---

## 1. 진단 — D12의 진술은 실제보다 넓었다

D12는 이렇게 적었다:

> Planning은 `priority === "high"`가 아니면 전부 Q4로 떨어지는 문제가 확인됐고

절반만 맞다. Q4의 실제 라벨을 보면 다르다 (`i18n/en.ts`):

```
eis.qIV      "Unsorted & parked"
eis.qIVHint  "Not judged yet · on hold · completed"
```

**Q4-unclassified는 "아직 판단하지 않음"의 자리로 의도된 것이다.** `priority`의 기본값이 `none`이므로, 아무도 손대지 않은 태스크가 거기 모이는 건 **옳은 동작**이다. 그건 고칠 것이 아니다.

### 1.1 진짜 결함 — 판단한 것을 판단하지 않은 것으로 취급한다

문제는 좁고 날카롭다. `eisenhower.ts:21`:

```ts
important = priority === "high"
```

`priority`는 4단계인데 사분면은 이진으로 읽는다. 그 결과:

| priority | 사용자가 한 일 | 매트릭스가 말하는 것 | 맞나 |
|---|---|---|---|
| `none` | 아무 판단 안 함 | 아직 판단 안 함 | ✔ |
| `low` | **"안 중요하다"고 판단** | 아직 판단 안 함 | ✘ |
| `medium` | **"중간쯤 중요"라고 판단** | 아직 판단 안 함 | ✘ |
| `high` | "중요하다"고 판단 | 중요 | ✔ |

**코드는 이미 이걸 알고 있었다.** `TaskDetail.tsx:168`의 주석이 그대로 적어놨다 — Low로 두면 곧바로 Unsorted로 읽힌다고. 알면서 감수한 것이지 모르고 지나간 게 아니다.

### 1.2 Q4가 두 가지를 겸하고 있다

고전 아이젠하워에서 Q4는 **"중요하지도 급하지도 않음"**(제거·위임 대상)이다. 이 앱의 Q4는 **"미분류 + 보류 + 완료"**다. 둘은 다른 것인데 `unclassified` 한 그룹에 섞여 있다 — 판단해서 낮게 매긴 일과 아직 안 본 일이 같은 칸에 앉아 있다.

### 1.3 지난 논의의 과장 정정

직전 대화에서 **"무엇이 중요한가를 말하는 방법이 네 가지"**라고 했다. **과장이었다.** 다시 보면 축이 서로 다르다:

| | 무엇을 말하나 | importance인가 |
|---|---|---|
| `Task.priority` | 중요도 | **그렇다 — 유일한 저장 필드** |
| 사분면 | 중요도 × 긴급도 | 파생. `priority`의 소비자 |
| Today 버킷 | 오늘 하루 안의 **순서** | 아니다 |
| 지평 | **시간 스케일** | 아니다 |

실제로 겹치는 건 **`priority` ↔ 사분면 둘뿐**이고, 그건 파생 관계라 원래 겹쳐야 한다. 나머지 둘은 다른 축이며 정리할 대상이 아니다.

---

## 2. 확정 결정

### D1. Planning은 남는다
사용자 결정(§0). 제거 논의는 닫는다. 다시 열려면 새 근거가 필요하다.

### D2. `important = high | medium`
사용자가 내린 판단을 매트릭스가 무시하지 않는다. `medium`은 "중간쯤 중요"이지 "모르겠음"이 아니다.

`low`와 `none`은 중요하지 않은 쪽으로 남는다 — `low`는 **판단해서** 낮은 것이므로 D3에서 자리를 따로 준다.

### D3. Q4의 두 의미를 가른다
`unclassified` 그룹을 둘로 나눈다:

| 그룹 | 조건 | 뜻 |
|---|---|---|
| 미판단 | `priority === "none"` | 아직 안 봤다 |
| 둘 다 아님 | `priority === "low"` | 봤고, 낮게 매겼다 |

이래야 Q4가 고전 매트릭스의 Q4(둘 다 아님)와 이 앱의 받은편지함(미판단)을 동시에 할 수 있다.

### D4. 강등 대상은 `medium`이 아니라 `low`다 — D2가 만드는 회귀
`patchForQuadrant`는 Q1/Q2에서 나갈 때 `high → medium`으로 낮춘다. 주석이 이유를 적어놨다 — "somewhat important"를 살리려는 것.

**D2를 넣는 순간 이 줄이 버그가 된다.** `medium`이 여전히 important이므로 카드가 Q1/Q2에 그대로 남는다 — **드래그가 아무 일도 안 하는 것처럼 보인다.**

→ 강등은 `low`로. 의미도 맞다: 중요 칸에서 빼는 행위는 "안 중요하다"는 판단이고, 그게 `low`다.

### D5. `priority`가 importance의 정본이다
사분면은 계속 **파생**한다(기존 규율 유지 — "Nothing stores the quadrant itself"). 지평과 Today 버킷은 importance가 아니므로 이 설계의 사정권 밖이다(§1.3).

### D6. `priority`는 4단계를 유지한다
매트릭스가 이진으로 읽는다고 필드를 이진으로 줄이지 않는다. 정렬·표시에 4단계가 쓰이고, D3이 4값을 전부 의미 있는 자리에 앉힌다.

---

## 3. Phase 계획

### P1 — 중요도 정의 수정 (+ 회귀 차단)

| # | 작업 |
|---|---|
| P1.1 | `isMatrixImportant`를 `high \| medium`으로 (D2) |
| P1.2 | `patchForQuadrant` 강등 대상을 `low`로 (D4) |
| P1.3 | **`eisenhower.ts` 테스트 신설** |

P1.3은 미룰 수 없다. 이 파일은 **테스트가 하나도 없는데 순수 함수이고 소비처가 3곳**이다(`EisenhowerPage` · `TaskDetail` · `CalendarRightTaskPanel`). 분류 규칙을 손대면서 안전망이 없는 상태로 두는 건 앞뒤가 안 맞는다.

### P2 — Q4 분리 (D3)
`MatrixGroup`에 그룹 하나 추가 + i18n.

**표시 순서는 미판단 → 둘 다 아님 → 보류 → 완료.** 초안은 "판단이 끝난 것을 위에"라고 적었지만 구현하면서 뒤집었다 — 네 그룹 중 **행동을 요구하는 건 미판단 하나뿐**이고 나머지는 멈춰 있는 상태다. 분류를 마친 것을 위에 올리면 정작 처리할 것이 아래로 밀린다. 기존에 사용자가 보던 그룹의 자리와 열림 상태도 그대로 유지된다.

### P3 — 보류
판단 진입을 쉽게 만드는 일(빠른 우선순위 지정 등). **실제로 부족함을 느낀 뒤에** 한다. 지금은 근거가 없다.

---

## 3-A. P1·P2에서 나온 결함 1건

**새 그룹의 태스크가 화면에서 통째로 사라질 뻔했다.** `MatrixGroup`에 `neither`를 추가해도 `npx tsc -b`는 통과한다 — `EisenhowerPage`가 유니온을 망라하지 않고 **`IV_GROUPS` 배열을 하드코딩**해서 렌더하기 때문이다. 그 배열에 없는 그룹은 헤딩만 없는 게 아니라 `tasksFor()` 필터에서 걸러져 **태스크가 안 보인다.** 타입이 잡아주지 않는 종류의 누락이라 주석으로 못박았다.

**부수 정정:** `eis.qIVHint`가 "아직 판단하지 않았거나, 보류·완료된 작업"이었다. `neither`가 생겼으니 사실이 아니다 — 문구에 추가했다.

**검증 (실측, 우선순위 전 구간 7개 시드)**

| 태스크 | 자리 | |
|---|---|---|
| high + 오늘 마감 | Q1 | ✔ |
| high, 마감 없음 | Q2 | ✔ |
| **medium, 마감 없음** | **Q2** | ✔ D2 — 이전엔 Q4 미분류였다 |
| low + 오늘 마감 | Q3 | ✔ |
| none | Q4 **미분류** | ✔ |
| **low, 마감 없음** | Q4 **안 중요하고 안 급함** | ✔ D3 |
| waiting | Q4 보류 | ✔ |

7개 전부 어딘가에 있다 — 사라진 것 없음.

**D4 (실측):** 상세에서 medium 태스크의 사분면을 II→IV로 바꾸니 `priority: "low"`가 저장되고 카드가 실제로 "안 중요하고 안 급함"으로 이동했다. 구 규칙이었다면 `medium`이 되어 **Q2에 그대로 남는다** — 아무 일도 안 하는 조작이 됐을 것이다.

**테스트가 회귀를 잡는지 확인했다.** 강등 대상을 `medium`으로 되돌려 보니 3개가 실패한다(왕복 테스트 포함). 테스트가 형식적이지 않다는 증거다.

`npx tsc -b` 통과, 325개 테스트 통과(신규 15개 — `eisenhower.ts` 최초 테스트), 신규 콘솔 에러 없음.

---

## 4. 하지 않을 것

| 항목 | 이유 |
|---|---|
| 사분면을 필드로 저장 | 기존 규율. 파생이라 어긋날 수 없다 (D5) |
| `priority` 값 개수 변경 | D3이 4값을 전부 쓰이게 만든다 (D6) |
| Today 버킷 동기화 | 하루 만에 리셋되는 임시값이다. 기기에 남는 게 맞다 |
| 지평을 importance 축으로 | 시간 스케일이다. 다른 축을 겹치면 둘 다 흐려진다 (§1.3) |
| Planning 제거 | 사용자 결정 (D1) |
| Q4 "미판단"을 없애기 | 기본값 `none`의 정직한 자리다. 결함이 아니다 (§1) |
