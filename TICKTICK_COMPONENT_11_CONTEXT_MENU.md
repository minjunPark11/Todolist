# TickTick 역설계 #11 — Context Menu / Dropdown

대상: TickTick Web (ticktick.com/webapp), 사용자 실제 계정
분석 컴포넌트: **드롭다운/컨텍스트 메뉴 프리미티브(`.dropdown-menu`)**
작성일: 2026-08-20

Component 01~10에서 다룬 내용은 다시 분석하지 않고, 비교가 필요한 지점에서만 인용한다.

**대상 선정 이유**: 사이드바 행·섹션 헤더·태스크 행·헤더 바·상세 패널 어디서나 같은 프리미티브가 나온다. 우리 앱은 메뉴 어휘가 4벌로 갈라져 있어(`DESIGN_ELEMENT_INVENTORY.md` §2.10.6) 통합 기준이 필요하다.

## 0. 측정 조건 · 안전 조치 · 복구

| 항목 | 값 |
|---|---|
| viewport | **1387 × 713 CSS px** (dpr 2) |
| 테마 / 로케일 | dark / ko |
| 측정 대상 메뉴 | **사이드바 리스트 행의 `⋯` 메뉴** (편집 / 상단 고정 / 복사 / 공유 / 기록 보관소 / 삭제) |
| 측정 방법 | `getBoundingClientRect()` · `getComputedStyle()` · 실제 마우스 hover · 실제 Arrow/Escape 키 |

### 0.1 안전 조치

이 메뉴에는 **삭제** 항목이 있다. 따라서:

- 대상을 **내가 만든 테스트 리스트 `ZZ List A`**로 골랐다(사용자의 실제 리스트가 아니다).
- **메뉴 항목을 하나도 클릭하지 않았다.** hover만 했고, 그것도 첫 항목(편집)에만 걸었다.
- 측정 후 `ZZ` 항목 4개가 그대로 있는 것을 확인했다.

### 0.2 측정 방법의 한계 — 위치는 무효

실제 마우스로 `⋯` 버튼을 클릭했으나 **두 번 모두 메뉴가 열리지 않았고**(좌클릭·우클릭 각 1회), 결국 `element.click()`로 앱의 핸들러를 직접 호출해 열었다.

그 결과 **메뉴가 포인터 좌표 없이 열려 (0, 4)에 배치**됐다. 따라서:

- **크기 · 색 · 타이포 · 항목 기하 · 상태 변화는 유효하다**(위치와 무관).
- **배치 규칙(어느 기준점에 붙는지, 화면 경계에서 뒤집히는지)은 측정하지 못했다.** 클래스 이름에 `dropdown-menu-placement-bottomLeft`가 있다는 사실만 기록한다.

### 0.3 복구

프로그램으로 연 메뉴라 **Escape로도, 바깥 클릭으로도 닫히지 않았다**(§5.2). 페이지를 새로고침해 정리했다.

| 항목 | 상태 |
|---|---|
| 열린 드롭다운 | **0개** |
| viewport | 1387 × 713 |
| Rail / 사이드바 | 50 / 240 (`leftListWidth = "240"`) |
| 화면 | 기본함(`#p/inbox/kanban`) |
| `ZZ` 테스트 데이터 | **4개 그대로** |

**판정 규칙**: 수치는 전부 실측. 해석은 `[Inference]`로 분리. 못 잰 것은 §7에 적는다.

---

## 1. 구조

```
DIV.dropdown-menu.l-dropdown.list-menu
│   .kanban-click-ignore .timeline-click-ignore .calendar-click-ignore
│   .mobile-detail-click-ignore .dropdown-menu-placement-bottomLeft
│       164 × 214   position: absolute   z-index: 1050
│       bg rgb(36,36,36) · border 1px rgb(54,54,54) · radius 12 · padding 4px 0
│       box-shadow: rgba(0,0,0,0.2) 0 16px 40px
│
└ UL.dropdown-menu-menu.dropdown-menu-menu-root.dropdown-menu-menu-vertical
  │       162 × 204   overflow: auto   시각 속성 없음
  │
  ├ LI.dropdown-menu-menu-item                    154 × 34   margin 0 4px
  │   └ A                                          13px / rgba(255,255,255,0.8)
  │       [role="menuitem"] [tabindex="-1"]
  ├ LI.dropdown-menu-menu-item.pinMenuAction       "상단 고정"
  ├ LI.dropdown-menu-menu-item                     "복사"
  ├ LI.dropdown-menu-menu-item                     "공유"
  ├ LI.dropdown-menu-menu-item                     "기록 보관소"
  └ LI.dropdown-menu-menu-item.deleteItem          "삭제"
```

**[Observed] backdrop / mask 요소가 없다.** 메뉴 뒤에 클릭 차단 레이어를 깔지 않는다.

**[Observed] `role`은 항목에만 있다.** `role="menuitem"`은 각 항목에 있지만 **컨테이너에 `role="menu"`가 없다.**

**[Inference]** `kanban-click-ignore` · `timeline-click-ignore` 같은 클래스가 붙어 있는 것으로 보아, **각 뷰가 "이 요소 안의 클릭은 바깥 클릭으로 치지 말라"고 표시하는 방식**으로 dismissal을 구현한 것으로 보인다. backdrop이 없는 이유이기도 하다.

---

## 2. Geometry

| 요소 | width | height | 비고 |
|---|---|---|---|
| 메뉴 박스 | **164** | **214** | 6항목 기준. `padding: 4px 0` |
| 내부 UL | 162 | 204 | 박스 − 보더 2 |
| **항목** | **154** | **34** | `margin: 0 4px` |
| 항목 스텝 | — | **34** | 간격 0, 구분선 없음 |

산술:

```
164 = 154(항목) + 4×2(항목 마진) + 1×2(보더)
214 = 34×6(항목) + 4×2(박스 상하 패딩) + 1×2(보더)
```

**[Observed] 항목 사이에 구분선이 없다.** 6항목이 34px 스텝으로 붙어 있고, 파괴적 동작(삭제)도 시각적으로 분리돼 있지 않다.

---

## 3. Surface / Boundary — 이 시리즈에서 처음 나오는 조합

| 항목 | 값 | 시리즈 내 위치 |
|---|---|---|
| **배경** | **`rgb(36,36,36)`** | **Rail과 같은 색**(C02·C08). 본문/사이드바 표면(28,28,28)이 아니다 |
| **보더** | **1px `rgb(54,54,54)`** | **알파가 아니라 실색.** 이 시리즈에서 처음 |
| **radius** | **12px** | 행 10 · 섹션 헤더 6에 이은 세 번째 값 |
| **box-shadow** | **`rgba(0,0,0,0.2) 0 16px 40px`** | **떠 있는 레이어의 그림자.** 시리즈에서 두 번째 |
| z-index | **1050** | 지금까지 최대(Rail 5, detail dragger 10) |
| backdrop-filter | none | |
| transition | `all / 0s` | 즉시 |

**[Observed] 메뉴는 지금까지 본 어떤 표면과도 다른 규칙을 쓴다.** 배경은 Rail 색을 재사용하고, 보더는 알파 하이라인이 아닌 실색이며, 그림자가 있고, radius가 12다.

**[Inference]** 알파 배경(예: 흰색 8%)을 쓰면 뒤의 콘텐츠가 비쳐 메뉴 글자가 읽히지 않는다. **떠 있는 레이어만 불투명 표면 + 실색 보더 + 그림자라는 별도 규칙 세트를 쓰는 것**으로 보인다. Component 09에서 좁은 폭 오버레이 사이드바에 그림자가 붙었던 것과 같은 방향이다.

---

## 4. Typography

| 항목 | 값 |
|---|---|
| 항목 텍스트 | **13px / 400** |
| 색 | **`rgba(255,255,255,0.8)`** (80%) |
| line-height | `normal` |
| 파괴적 항목(삭제) | **색이 같다** — `deleteItem` 클래스는 있으나 빨강이 아니다 |

**[Observed] 13px은 이 시리즈에서 처음 나온 크기다.** 지금까지: 12(섹션 헤더·날짜 힌트) · 14(행 라벨·태스크 제목·본문) · 19(상세 제목) · 20(본문 헤더).

**[Observed] 80% 알파도 처음이다.** 알파 단계가 이제 3 · 5 · 6 · 8 · 10 · 20 · 30 · 40 · 60 · **80** · 100%로 11단이 됐다.

**[Observed] 삭제 항목에 위험 색이 없다.** `deleteItem` 클래스는 붙어 있지만 computed color는 다른 항목과 동일하다.

**[Inference]** 클래스만 두고 색을 주지 않은 것은 의도적 절제이거나 미구현으로 보인다. 어느 쪽인지는 알 수 없다. 다만 **삭제가 구분선도 색도 없이 목록 맨 아래에 다른 항목과 똑같이 놓여 있는 것**은 오조작 위험이 있는 배치다.

---

## 5. States

| Property | 휴지 | **hover** |
|---|---|---|
| 항목 배경 | `rgba(0,0,0,0)` | **`rgba(255,255,255,0.05)`** |
| **항목 radius** | **0px** | **8px** |
| 항목 클래스 | `dropdown-menu-menu-item` | **`+ dropdown-menu-menu-item-active`** |
| 텍스트 색 | 80% | **변화 없음** |
| transition | `all / 0s` | 즉시 |

**[Observed] radius가 hover에서만 생긴다.** 휴지 상태에서는 0이고, hover 시 8px 둥근 배경이 나타난다.

**[Observed] hover 배경 5%는 본문 계열의 값이다**(태스크 행 hover 5%, 헤더 아이콘 hover 배경 5% — C06·C09). 사이드바 계열의 3%가 아니다.

### 5.1 키보드 — 동작하지 않는다

| 조작 | 결과 |
|---|---|
| **ArrowDown** (포인터가 메뉴 위) | **변화 없음** — 활성 항목이 바뀌지 않음 |
| **ArrowDown** (포인터를 치운 뒤) | **변화 없음** — 활성 항목 0개 유지 |
| 포커스 위치 | `UL.dropdown-menu-menu` |
| 항목 `tabindex` | **−1** (전부) |

**[Observed] `role="menuitem"`과 `tabindex="-1"`이 있어 roving tabindex 패턴처럼 보이지만, 실제로 Arrow 키가 동작하지 않는다.** 마크업만 있고 키보드 핸들러가 없다.

### 5.2 Escape / 바깥 클릭 — 판단 유보

| 조작 | 결과 |
|---|---|
| **Escape** | **닫히지 않음** |
| **바깥 클릭**(헤더 빈 영역) | **닫히지 않음** |

**단, 이 메뉴는 프로그램 `click()`으로 열었다**(§0.2). 앱이 실제 포인터 시퀀스를 기준으로 dismissal을 등록한다면, 그 등록이 일어나지 않았을 수 있다. **따라서 "Escape로 닫히지 않는다"를 TickTick의 동작으로 단정하지 않는다 — 미확정으로 남긴다.**

---

## 6. Fidelity Specification

```
CONTEXT MENU / DROPDOWN

Surface (떠 있는 레이어의 별도 규칙)
  background          : 불투명 표면색 (Rail과 같은 단계 — 본문보다 밝은 쪽)
  border              : 1px, 실색 (알파 하이라인 아님)
  radius              : 12px
  box-shadow          : 0 16px 40px rgba(0,0,0,0.2)
  padding             : 4px 0
  z-index             : 1050
  backdrop            : 없음 (클릭 차단 레이어를 깔지 않는다)
  transition          : 없음

Item
  height              : 34
  width               : 메뉴 폭 − 8 (좌우 margin 4)
  step                : 34, 간격 0, 구분선 없음
  padding             : 0 (내부 <a>가 채운다)
  typography          : 13px / 400
  color               : 전경색 80% 알파
  normal              : 배경 없음, radius 0
  hover               : 배경 전경색 5% 알파, radius 8
  파괴적 항목         : 전용 클래스는 있으나 색·구분선 없음 (다른 항목과 동일하게 보인다)

Semantics (관찰된 그대로)
  항목                : role="menuitem", tabindex="-1"
  컨테이너            : role="menu" 없음
  키보드              : Arrow 키 동작하지 않음
  dismissal           : 미확정 (§5.2)

크기 산술
  메뉴 폭  = 항목 폭 + 항목 margin×2 + border×2
  메뉴 높이 = 항목 높이×N + padding×2 + border×2
```

---

## 7. 이 분석이 확인하지 않은 것

- **배치 규칙** — 어느 기준점에 붙는지, 화면 경계에서 뒤집히는지, 오프셋이 얼마인지. 프로그램으로 열어 (0,4)에 놓였으므로 전부 미측정(§0.2).
- **Escape / 바깥 클릭 dismissal** — §5.2, 측정 방법의 한계로 미확정.
- **다른 메뉴들과의 동일성** — 섹션 헤더 `+` 메뉴, 태스크 행 `⋯` 메뉴, 헤더 바 정렬/더보기, 상세 패널 더보기, 계정 드롭다운. Component 04·05에서 본 `.dropdown-menu-menu-item` 클래스가 같으므로 **같은 프리미티브일 가능성이 높으나 전수 비교하지 않았다.**
- **아이콘이 있는 메뉴 항목** — 이 메뉴는 텍스트만이었다. 아이콘·단축키·체크 표시가 붙는 변형은 미확인.
- **하위 메뉴(submenu)** — `dropdown-menu-menu-root`라는 이름이 하위 메뉴의 존재를 시사하나 확인하지 않았다.
- **`:active`(누름)** — 항목을 누르면 실행되므로 시도하지 않았다.
- **라이트 테마**, **원본 CSS 규칙**(CORS) — 01~10과 동일.

---

## 8. Component 01~11 갱신

### 8.1 "떠 있는 레이어"는 다른 규칙을 쓴다

지금까지 "없다"고 정리했던 두 규칙이 메뉴에서 모두 깨진다.

| 규칙 | 흐름 안 요소(01~10) | **떠 있는 레이어** |
|---|---|---|
| box-shadow | **없음** (9/9) | **있음** — 메뉴 `0 16px 40px rgba(0,0,0,0.2)`, 좁은 폭 사이드바(C09) |
| 경계 | 1px **알파** 하이라인 (5·6·10%) | **1px 실색** `rgb(54,54,54)` |
| 배경 | 표면색 또는 알파 채움 | **불투명 표면색(Rail 단계)** |
| radius | 행 10 · 섹션 헤더 6 | **12** |

**[Inference] "그림자 없음"은 전역 규칙이 아니라 흐름 안 요소의 규칙이다.** 떠 있는 것(메뉴·오버레이 사이드바)은 그림자·실색 보더·불투명 배경을 쓴다. 이제 시리즈 전체를 관통하는 규칙으로 남는 것은 **색 체계(전경색 × 알파)**와 **SVG 스프라이트** 정도다.

### 8.2 Conflict / Revision Candidate (R-19 ~ R-20)

| # | 기존 서술 | 11의 관찰 | 성격 |
|---|---|---|---|
| **R-19** | **C01~C08 §Shared Rules** — "그림자 없음"이 9/9로 가장 견고한 후보 중 하나 | 메뉴에 **`0 16px 40px rgba(0,0,0,0.2)`** 그림자가 있다 | **범위 확정.** C09의 오버레이 사이드바에 이어 두 번째. 규칙은 **"흐름 안 요소에는 그림자가 없다"**로 좁혀야 한다 |
| **R-20** | **C02 §3 / C07 §13.1** — "경계는 1px 알파 하이라인(5·6·10%)" | 메뉴 보더는 **알파가 아니라 실색 `rgb(54,54,54)`** | **범위 축소.** 알파 하이라인은 흐름 안 경계의 규칙이고, 떠 있는 레이어는 실색을 쓴다 |

### 8.3 알파 스케일 — 11단으로 갱신

3 · 5 · 6 · 8 · 10 · 20 · 30 · 40 · 60 · **80** · 100%
(80%는 이번에 메뉴 항목 텍스트에서 처음 관찰)

---

## Appendix A — 우리 앱에 적용할 때 다르게 할 것 (관찰이 아닌 제안)

**아래는 TickTick 동작이 아니다.**

1. **파괴적 항목을 시각적으로 분리한다.** TickTick은 `삭제`를 구분선도 색도 없이 다른 항목과 똑같이 둔다. 우리는 위에 구분선 1개 + `--danger` 색을 준다. 우리 `21-components.css`에 이미 `.ff-context-menu-item.is-danger`가 있으니 그대로 쓰면 된다.
2. **키보드 내비게이션을 실제로 구현한다.** TickTick은 `role="menuitem"`과 `tabindex="-1"`만 있고 Arrow 키가 동작하지 않는다. 마크업만 흉내 내지 말고 Arrow/Home/End/Escape를 붙인다.
3. **컨테이너에 `role="menu"`를 준다.** 항목에만 role이 있으면 보조기술이 메뉴로 인식하지 못한다.
4. **Escape로 반드시 닫는다.** TickTick 쪽은 미확정이지만, 우리는 확실히 구현한다.
5. **떠 있는 레이어의 규칙 세트를 따로 정의한다.** 이번 관찰이 유용한 지점이다 — 흐름 안 요소는 알파 하이라인 + 그림자 없음, **떠 있는 레이어는 불투명 배경 + 실색 보더 + 그림자**. 우리 토큰에 `--shadow-panel`과 `--border-strong`이 이미 있으므로 그 조합으로 정리하면 된다.
6. **항목 hover radius 8은 채택할 만하다.** 메뉴 radius 12 안쪽에서 8이면 여백 4와 맞아떨어진다(12 − 4 = 8).
7. **13px은 우리 스케일에 없다.** 우리는 12나 14 중 하나로 맞추는 편이 스케일이 덜 늘어난다.

---

## 9. 이 문서가 남긴 상태

| 항목 | 상태 |
|---|---|
| Component 01~10 문서 | **수정하지 않음.** 충돌·보강 후보는 §8.2에만 기록 |
| 우리 앱 코드 | **수정하지 않음** |
| TickTick 데이터 | **변경 없음.** 메뉴 항목을 하나도 클릭하지 않았다 |
| 열린 메뉴 | 0개 (새로고침으로 정리) |
| 화면 / 셸 | 기본함 · Rail 50 · 사이드바 240 · viewport 1387×713 |
| Component 04 테스트 데이터 | `ZZ Folder` + 리스트 3개 유지 |
