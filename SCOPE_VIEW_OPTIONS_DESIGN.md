# 스코프의 `⋯` — 무엇을 보여 줄지 고르는 자리

> 상태: **설계 · 전 단계 구현됨** · 2026-09-02
> (사용자가 참조 앱의 스코프 메뉴와 `View Options` 대화상자, 그리고 `Show Input Box`와
> `Show Date by`가 켜졌을 때의 화면을 캡처해 주었다.)
> 대상: `components/tasks/TasksModule.tsx`의 헤더 · `TaskBoard.tsx`의 열 · 카드의 날짜

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 4장 — 스코프 `⋯` 메뉴 · `View Options` 대화상자 · `Show Input Box` 켠 보드 · `Show Date by = Countdown` | [관찰] |
| **실측** | `scopeRegistry.ts` · `TasksModule.tsx` 헤더 · `TaskBoard.tsx` · `TaskRowContent.tsx` · `AppSettings` | [실측] |
| **결정** | 사용자가 이 문서 착수 전에 고른 것 셋 | [결정] |
| **추론** | 위로 메운 것 | [추론] |

스크린샷은 **메뉴가 열린 한 순간**과 **대화상자가 열린 한 순간**이다. 각 항목이
무엇을 여는지, 하위 선택지가 몇 개인지는 `Show Date by`(둘을 봤다)를 빼면 **보이지
않았다.**

---

## 1. 참조 앱이 보여 준 것 [관찰]

### 1.1 스코프의 `⋯`

머리 오른쪽 끝, `↕` 옆. 누르면:

```
View
 [목록]  [보드]  [타임라인]     ← 아이콘 셋, 현재 것이 눌린 상태
─────────────────────────
 ☑  Hide Completed
 ≔  Show Details
 ⚙  View Options
─────────────────────────
 +  Add Section
 ⤢  List Activities
```

**뷰 선택기가 메뉴 안에 있다.** 우리는 머리에 글자 버튼 셋으로 두고 있다.

세 덩어리다: 무엇으로 볼 것인가(View) · 무엇을 보여 줄 것인가(셋) · 이 리스트에
대한 행동(둘). 구분선이 그 셋을 가른다.

### 1.2 `View Options` 대화상자

```
View Options                              ✕
┌────────────────────────────────────────┐
│ Show Date by              Task Time  ⌄ │
├────────────────────────────────────────┤
│ Kanban Size                 Medium   ⌄ │
├────────────────────────────────────────┤
│ Show Input Box                    ◯──● │
└────────────────────────────────────────┘
```

세 줄, 각자 자기 카드. 앞의 둘은 값을 여는 셀렉트, 셋째는 토글.

### 1.3 `Show Input Box`가 켜졌을 때

보드의 **각 열 맨 위에 `+ Add Task` 줄**이 선다. 열 머리(`미분류 0 ⋯`) 바로 아래,
카드들 위. 세 열 전부에 있다.

꺼졌을 때의 화면은 **보지 못했다** — 캡처는 켠 상태와 켜는 순간 둘이다.

### 1.4 `Show Date by`

같은 카드(`SU Meeting`)의 같은 자리가 값에 따라 다르게 읽힌다:

| 값 | 카드가 말하는 것 |
|---|---|
| `Task Time` | `Next Mon` |
| `Countdown` | `5 days Left` |

날짜가 사라지는 게 아니라 **같은 날짜를 다르게 읽는다.** 그리고 그 옆의 반복
아이콘과 알림 아이콘은 양쪽 다 그대로다.

### 1.5 카드가 본문을 그린다

`SU Meeting` 아래 `DBc06 sdfsd sdf sdf sdf`. 제목 아래 한 줄, 흐린 글씨. 메뉴의
`Show Details`가 이것을 켜고 끄는 것으로 보인다 [추론] — 라벨과 화면을 잇는 근거는
이름뿐이다.

---

## 2. 우리는 어디까지 있나 [실측]

| 화면이 요구하는 것 | 우리 상태 |
|---|---|
| 스코프 `⋯` | **없다.** 리스트 스코프에 `보관`·`삭제` 글자 버튼, 휴지통에 `비우기` 아이콘 |
| 메뉴 안의 뷰 아이콘 셋 | 머리에 글자 버튼 셋(`.tm-views`), `?view=`로 주소에 저장 |
| `Hide Completed` | 보드의 열마다 `완료` 그룹(접힘·5개 상한). **스코프 단위 토글은 없다** |
| `Show Details` | 카드는 본문 **아이콘**만 그린다(`hasBody`). 본문 텍스트는 안 그린다 |
| `View Options` | 없다 |
| `Show Input Box` | 열 **머리의 `+`** 가 인라인 폼을 연다(`.tm-column-add`) |
| `Show Date by` | 날짜만 그린다. 남은 일수 표기가 없다 |
| `Kanban Size` | 없다. 앱 전역 밀도 설정은 따로 있다 |
| `Add Section` | `listSections`가 리스트 스코프의 보드 열이다. 만드는 입구는 보드의 `새로운 열` |
| `List Activities` | 작업별 기록(`activityFor`)만 있다. **리스트별은 없다** |

### 2.1 뷰가 둘 이상인 스코프는 둘뿐이다 [실측]

이것이 이 설계에서 제일 중요한 실측이다.

```
ONE_LIST_VIEWS = ["list", "board", "gantt"]   ← inbox, list
LIST_ONLY      = ["list"]                     ← today, upcoming, folder, tag,
                                                 filter, completed, wontDo, trash
```

즉 **`오늘`·`다음 7일`·`태그`에는 고를 뷰가 없다.** 지금 머리는 `allowedViews.length
> 1`일 때만 선택기를 그린다.

그리고 `Kanban Size`는 보드가 있는 곳에서만 뜻이 있으므로, 같은 둘에서만 의미를 갖는다.

---

## 3. 결정

### 3.1 `⋯`는 완료·안 함·휴지통을 뺀 모든 스코프에 [결정 + 추론]

사용자가 든 것은 `오늘 · 다음 7일 · 기본함 · 리스트 · 태그`다. 우리 레지스트리에는
`폴더`와 `필터`도 있고, 둘 다 **사용자가 만든 작업 모음**이라는 점에서 리스트·태그와
같은 종류다. 넣는다.

빠지는 셋은 `완료 · 안 함 · 휴지통`이다. 셋 다 **끝난 일의 목록**이고, 메뉴의 절반이
그 셋에 대해 뜻을 잃는다 — 완료 목록에서 `완료 숨기기`는 목록을 비우는 버튼이다.

### 3.2 뷰 선택기가 메뉴로 들어간다 [결정]

머리는 `제목 · 개수 · ↕ · ⋯`만 남는다 — Today가 §3.2에서 간 자리와 같은 모양이다.

**고를 것이 없으면 `View` 구획을 그리지 않는다.** §2.1대로 여덟 스코프 중 여섯은 뷰가
하나뿐이고, 하나짜리 라디오는 §16.26 Gate 3이 이미 거부한 것이다 — 지금 머리가
`length > 1`로 거는 조건 그대로를 메뉴가 물려받는다.

### 3.3 값은 스코프마다 [결정]

`AppSettings.scopeViewOptions?: Record<string, ScopeViewOptions>` — 키는 스코프의
주소 조각(`today` · `list:<id>` · `tag:<id>` …), 값은 셋이다.

```ts
interface ScopeViewOptions {
  dateBy: "taskTime" | "countdown";
  kanbanSize: "small" | "medium" | "large";
  showInputBox: boolean;
  hideCompleted: boolean;
  showDetails: boolean;
}
```

없으면 기본값으로 읽는다 — `matrixQuadrantViews`가 칸에 대해, `inboxColumns`가 열에
대해 하는 것과 같은 모양이고, 그래서 **마이그레이션이 없다**(M0 추가 필드).

스코프마다인 이유는 참조 앱이 그렇기도 하지만 그보다, **리스트마다 원하는 것이
다르기 때문**이다. 회의 리스트는 남은 일수가 궁금하고 장보기 리스트는 아니다.

지워진 리스트의 키가 남는 문제가 있다. `normalize`에서 **살아 있는 스코프의 것만
남긴다** — §32가 고아 키 109개를 지운 것과 같은 청소를 로드 때 한 번 한다.

### 3.4 `Show Input Box`가 열 머리의 `+`를 대체한다 [결정]

| 켜짐 | 열 맨 위에 `+ Add Task` 줄 |
| 꺼짐 | 아무 추가 입구도 없다 |

머리의 `+`는 사라진다. 한 열에 추가 입구가 하나가 되고, 캡처와 같아진다.

**꺼진 상태를 못 봤다는 것**(§1.3)이 이 결정의 유일한 약점이다. 참조 앱이 꺼진
상태에서도 머리에 무언가를 남기는지는 알 수 없다. 우리는 남기지 않는다 — 토글의 이름이
`입력칸 보이기`이고, 꺼도 다른 모양으로 남는다면 그 토글은 무엇을 끈 것인지 말하지
못한다.

`Add Task` 줄은 Today가 §3.3에서 만든 접히는 줄과 **같은 컴포넌트가 아니다.** 저쪽은
파싱과 목적지 토글을 가진 캡처이고, 이쪽은 열 하나에 제목 하나를 넣는 폼이다. 지금
`.tm-column-add`가 여는 폼을 줄로 옮기는 일이다.

### 3.5 `Show Date by` — 같은 날짜, 다른 읽기

`taskTime`(기본)은 지금 그리는 것 그대로. `countdown`은 같은 값을 남은 일수로 읽는다.

```
오늘         → 오늘
내일         → 1일 남음
5일 뒤       → 5일 남음
3일 지남     → 3일 지남
```

**날짜를 대체하는 것이지 덧붙이는 것이 아니다**(§1.4의 같은 자리). 그리고 늦은 것의
빨간색은 양쪽 다 그대로다 — 색은 날짜의 상태를 말하지 표기 방식을 말하지 않는다.

도메인 함수 하나(`countdownLabel(dueDate, today)`)이고, 행과 카드가 같은 것을 부른다.
`TaskRowContent`가 이미 `dueLabel`을 만드는 한 자리를 갖고 있으므로 그 자리에서 갈린다.

### 3.6 `Kanban Size` — 보드에만, 그리고 카드의 높이가 아니라 **열의 폭**

캡처는 `Medium` 하나만 보여 준다. 나머지 선택지 이름은 [추론]이다: `Small · Medium ·
Large`.

무엇이 바뀌는지도 캡처로는 모른다. **열의 폭**으로 정한다 — 보드가 옆으로 스크롤하는
화면이고(§15), 크기 선택이 답할 만한 질문은 "한 화면에 열이 몇 개 보이나"이기
때문이다 [추론]. 카드의 높이는 내용이 정한다.

보드가 없는 스코프에서는 이 줄을 **그리지 않는다.** 대화상자가 세 줄일 때와 두 줄일
때가 있는 셈이고, 뜻이 없는 컨트롤을 회색으로 두는 것보다 낫다(§15.5).

### 3.7 `Hide Completed` — 스코프의 질의에 걸린다

지금 보드의 열마다 있는 `완료` 그룹을 **끄는** 토글이다. 목록 뷰에서는 완료된 행을
질의에서 뺀다.

`Today`의 `완료 표시` 토글과 같은 종류이고, 그쪽은 `AppSettings.showCompletedInToday`
하나로 산다. 두 화면이 각자의 값을 갖는 것은 §3.3의 결정과 일관된다 — 다만
**Today의 값은 옮기지 않는다.** 그 화면은 이 모듈이 아니다.

### 3.8 `Show Details` — 카드가 본문 한 줄을 그린다

지금은 본문이 있다는 **아이콘**만 있다(§2). 켜면 아이콘 자리에 본문 첫 줄이 온다.

한 줄로 자른다. 카드가 본문 길이만큼 자라면 열이 카드 셋을 담다 하나를 담게 되고,
그것은 크기 선택이 답할 질문을 카드가 제멋대로 답하는 일이다.

### 3.9 짓지 않는 것

- **`List Activities`** — 리스트별 기록이 우리에게 없다. 작업별은 있고, 리스트 단위로
  모으는 것은 이 문서보다 큰 일이다. 메뉴에 **넣지 않는다**
- **`Add Section`** — 보드의 `새로운 열`이 이미 그 일을 한다. 같은 것을 두 자리에서
  고르게 하는 것이 이 리포가 계속 지워온 중복이다. 메뉴에 넣지 않는다

둘 다 세어 두고 §5에 남긴다.

---

## 4. 단계

| 단계 | 내용 | 왜 이 순서인가 |
|---|---|---|
| **1** | `ScopeViewOptions` 도메인 — 타입 · 기본값 · 스코프 키 · 로드 때의 고아 청소. UI 없음 | **완료**(§7). 다섯 값이 어디 사는지가 나머지 전부의 전제다 |
| **2** | 스코프 `⋯` — 뷰 선택기를 머리에서 메뉴로. 아직 다른 항목 없음 | **완료**(§8) |
| **3** | `View Options` 대화상자 + `Show Input Box` | **완료**(§9) |
| **4** | `Show Date by` | **완료**(§10) |
| **5** | `Kanban Size` | **완료**(§11) |
| **6** | `Hide Completed` · `Show Details` | **완료**(§12) |

1·2단계가 이 설계의 뼈대다. 3단계부터는 서로 독립이므로 순서를 바꿔도 된다.

---

## 5. 열린 질문

| | 질문 | 언제 |
|---|---|---|
| Q1 | `Show Input Box`가 꺼졌을 때 참조 앱의 열 머리에 무엇이 남는가. 캡처는 켠 상태뿐이다(§1.3) | 스크린샷 한 장 더 |
| Q6 | 메뉴의 `View`를 캡처처럼 **아이콘 한 줄**로 그릴 것인가. 2단계는 이 앱의 메뉴 어휘대로 **행 셋 + ✓**으로 그렸다(§8.2) | 스크린샷이 아니라 결정 |
| Q7 | 리스트 스코프의 `보관`·`삭제` 버튼은 머리에 남아 있다. 참조의 셋째 구획(`Add Section` · `List Activities`)에 대응하는 자리인데 §3이 정하지 않았다 | 6단계 |
| ~~Q2~~ | ~~나머지 선택지 이름과 그것이 바꾸는 것~~ — 지었다(§11.1): `작게 208 · 보통 260 · 크게 320`. **[추론]인 채로 남는다** — 캡처는 `Medium` 하나뿐이었다 |  |
| ~~Q3~~ | ~~본문이 없는 카드는 어떻게 되는가~~ — **닫혔다**(§12.2). 아무것도 그리지 않는다. 대체하는 마크도 그 경우엔 없었으므로 조용한 카드는 변하지 않는다 |  |
| Q4 | **두 줄이 아니라 한 줄이었다**(§11.2). 보드가 없는 여섯 스코프의 `View Options`에는 `Show Date by`만 남는다. 한 줄짜리 대화상자가 값을 하는가 — 아니면 그 여섯에서는 메뉴에 직접 얹는가, 아니면 `Show Input Box`가 목록의 빠른 추가까지 뜻하게 넓히는가 | **열려 있음** |
| ~~Q5~~ | ~~휴지통에 있는 동안은 남길 것인가~~ — **닫혔다**(§7.3). 남긴다. 쓸기는 **받은 id 목록**을 보고, 버려진 리스트도 여전히 레코드다 |  |

---

## 6. 이 문서가 확인하지 않은 것

- 각 메뉴 항목이 여는 것. `View Options` 말고는 눌린 상태를 보지 못했다
- `Hide Completed`가 목록 뷰에서 무엇을 하는지. 캡처는 보드다
- 참조 앱이 이 값들을 계정에 저장하는지 기기에 저장하는지. §3.3은 우리 앱의 다른
  스코프 설정이 계정에 붙는다는 이유로 계정을 골랐다

---

## 7. 1단계 구현 기록 (2026-09-02)

### 7.1 키가 레지스트리에서 나온다

`scopeOptionKey(ref)` — `today` · `list:l1` · `tag:t3`. 여기 종류 목록을 적어 두는
대신 **레지스트리의 `hasId`를 읽는다**. 나중에 추가되는 스코프가 고정 스코프의 키와
부딪히는 일을 규칙이 막는다.

옵션이 없는 스코프에는 **빈 문자열**을 돌려준다. 완료·안 함·휴지통에는 값을 넣을
자리가 아예 없다는 뜻이고, 엉뚱한 키가 처음부터 쓰이지 않게 하는 것이 이 반환값이다.

### 7.2 읽기는 필드 하나씩

`sanitizeScopeViewOptions`가 스프레드가 아니라 다섯 필드를 각각 검사한다. 스프레드면
더 새 클라이언트가 쓴 `kanbanSize: "enormous"`가 그대로 통과해서 **그런 항목이 없는
`<select>`**에 닿는다.

기본값 다섯은 전부 **이 앱이 지금 하는 것**이다. 메뉴를 한 번도 열지 않은 계정이
지금과 똑같이 동작한다 — `matrixQuadrantRules`가 손대지 않은 칸에 대해 하는 약속과
같다.

### 7.3 Q5 — 버려진 리스트는 설정을 잃지 않는다

쓸기는 **받은 id 목록**만 본다. 버려진 리스트는 여전히 스토어의 레코드이므로 그 목록에
들어 있고, 따라서 키가 남는다. 키를 없애는 것은 **레코드가 없어지는 것**이고, 그 일을
하는 것은 `permanentlyDeleteList`다 — 다음 로드가 알아차린다.

되살렸는데 보기 설정이 비어 있는 것은 **아무도 요청하지 않았고 아무도 예상할 수 없는**
손실이다. `restoreDeletedTask`가 삭제가 가져간 것을 전부 되돌려 놓기로 한 것과 같은
이유다.

### 7.4 쓸기가 `normalizeAppSettings` 밖에 있다

그 함수는 설정만 본다. `list:l1`이 아직 무언가를 가리키는가는 **리스트에 대한
질문**이고, 답은 `normalizeData`가 조립하고 있는 스토어에 있다. 그래서 쓸기는 조립이
끝난 자리에서 한 번 돈다.

고아가 없으면 **같은 객체를 그대로** 돌려준다 — 지저분한 계정이 아닌 사람이 로드할
때마다 저장소가 더러워지지 않게.

### 7.5 검증

- 유닛 **2,247개 통과**(신규 19: 도메인 13 · 로드 경로 6) · `tsc` 통과
- 로드 경로를 따로 시험하는 이유는 §7.4 때문이다. 순수 함수 시험은 배선이 틀려도
  통과하고, 이 배선은 틀릴 이유가 있는 종류다 — 두 함수 중 하나만 스토어를 본다

### 7.6 남은 것

2단계 — 스코프 `⋯`. 뷰 선택기를 머리에서 메뉴로 옮기고, 그릇을 만든다.

---

## 8. 2단계 구현 기록 (2026-09-02)

### 8.1 머리에서 셋이 나가고 하나가 들어왔다

`목록 · 보드 · 타임라인` 글자 버튼 셋이 사라지고 `⋯` 하나가 섰다. 실측(`/inbox`):
머리의 텍스트가 `Inbox⋯`이고 `.tm-views`는 없다.

Gate 3이 선택기와 함께 이사했다 — **고를 것이 없으면 메뉴가 아예 안 열린다.** 여덟
스코프 중 여섯은 뷰가 하나뿐이므로(§2.1), 그 여섯에서는 항목이 0개가 되고 `⋯`도 안
그려진다. 머리가 `length > 1`로 걸던 조건 그대로가 한 층 위로 올라간 것이다.

완료·안 함·휴지통에서도 안 그려진다(§3.1).

### 8.2 아이콘 줄이 아니라 행 셋이다

캡처의 `View` 구획은 아이콘 셋이 한 줄에 놓인 모양이다. 우리는 **행 셋 + `✓`**으로
그렸다.

이 앱의 `MoreMenu`는 행을 그리는 컴포넌트이고, Today의 축 선택기가 같은 자리에서 같은
모양을 골랐다(§11.4). 아이콘 한 줄은 새 메뉴 프리미티브이고, 2단계의 일은 **그릇을
만드는 것**이지 새 컨트롤을 발명하는 것이 아니다. §5 Q6으로 남긴다.

### 8.3 리듬 시험이 또 잡았다 — 같은 28px

`.ff-icon-btn`은 28px이고 컨트롤 그리드는 32다. Today의 머리에서 `⋯`이 카드 밖으로
나왔을 때와 **글자 그대로 같은 실패**이고, 원인도 같다 — 28은 §4.87의 섹션 액션 크기이지
머리에 서는 컨트롤의 크기가 아니다.

두 번 같은 것을 밟았다는 것은 `.ff-icon-btn`의 기본이 28인 것이 함정이라는 뜻이다.
지금은 두 자리에서 각각 32로 올렸다.

### 8.4 시험 하나가 사라진 버튼을 누르고 있었다

`contextMenu.spec.ts`의 보드 카드 시험이 `page.getByRole("button", { name: "Board" })`로
뷰를 바꿨다. 그 버튼이 메뉴 안으로 들어갔으므로 **메뉴를 열고 항목을 고르도록** 고쳤다.

그리고 새 시험 넷을 붙이면서 하나 배웠다: 이 하네스의 `onNavigate`는 아무 데도 가지
않으므로, 뷰가 바뀌었는지를 **렌더로 확인할 수 없다.** 뷰는 URL의 읽기이고(§15.9),
확인할 것은 **무엇을 요청했는가**다.

### 8.5 검증

- 유닛 **2,251개 통과**(신규 4: 메뉴가 뷰를 담는다 · 고르면 이동한다 · 고를 게 없으면
  안 뜬다 · 끝난 스코프에는 안 뜬다) · `tsc` 통과
- 전체 E2E **140 통과, 실패 0** — 다른 것을 아무것도 돌리지 않고 7.4분
- 실제 앱(dev, en, `/inbox?view=board`): 머리 `Inbox ⋯`, 메뉴 `List · ✓ Board ·
  Timeline`, `List`를 고르면 `/inbox`로 가고 보드가 사라진다

> 이번 전체 E2E는 **혼자 돌렸다.** 앞선 아홉 번의 flake가 전부 내가 그 위에 유닛
> 테스트와 브라우저 조작을 얹은 실행에서 나왔고, 이번에는 실패 둘이 **진짜였다**.
> 부하를 걷어내니 신호가 남았다.

### 8.6 남은 것

3단계 — `View Options` 대화상자와 `Show Input Box`.

---

## 9. 3단계 구현 기록 (2026-09-02)

### 9.1 대화상자는 한 줄로 시작한다

`Show Date by`와 `Kanban Size`는 4·5단계다. 지금 넣으면 **아무것도 하지 않는 컨트롤
둘**이 서 있게 되고, 그건 이 세션에서 두 번 피한 모양이다(Today의 `↕`, 설정의 죽은
토글). 한 줄짜리 대화상자는 어색하지만, 어색한 것과 거짓말하는 것은 다르다.

### 9.2 `ConfirmModal`이 잘못된 그릇이었다 [실측]

처음에 `ConfirmModal`로 만들었더니 실제 앱에서 이렇게 나왔다:

```
View Options / Show Input Box / Cancel / Close
```

**같은 일을 하는 버튼이 둘.** 그 컴포넌트는 답이 둘인 질문을 위한 것이고, 이 대화상자에는
답이 없다 — 각 줄이 만져지는 즉시 커밋되므로 유일한 컨트롤은 나가는 문이다.

`ListManager`의 껍질로 바꿨다. 이 앱에서 **질문이 아닌 대화상자**의 모양이 그것이다.
`ConfirmModal`에 "취소를 숨기는 프롭"을 다는 쪽은 고르지 않았다 — 이름이 `Confirm`인
컴포넌트에 확인하지 않는 모드를 만드는 일이다.

### 9.3 그리고 진짜 버그를 하나 밟았다 [실측]

`Show Input Box`를 켰는데 **줄이 하나도 안 나오는 리스트**가 있었다. 파 보니 이 문서가
만든 것이 아니었다:

```
const [adding, setAdding] = useState("");   // "아무것도 안 열림"
```

그런데 **섹션이 없는 리스트의 유일한 열은 id가 `""`**다(no-section 버킷). 센티넬과
열 id가 같은 값이라 `adding === column.id`가 첫 페인트부터 참이었고 —

- 전에는: 그 리스트의 추가 **폼이 처음부터 열린 채로** 서 있었고, 머리의 `+`는
  `aria-expanded="true"`를 영원히 보고했다
- 3단계 뒤에는: 줄이 "아무것도 안 열렸을 때" 그려지므로 **한 번도 안 그려졌다**

같은 버그가 모양만 바꾼 것이다. 센티넬을 `null`로 바꿨다 — 열 id가 될 수 없는 값이라야
센티넬이다.

**시험이 그 버그를 지키고 있었다.** `taskBoardAdd.test.tsx`가 `aria-expanded`로 "열림"을
읽고 있었는데, 그 값이 항상 참인 열에서 통과하고 있었다. 이제 "어느 컨트롤이 화면에
있는가"로 읽는다.

### 9.4 2단계의 시험 둘이 옳게 깨졌다

메뉴에 `View Options`가 생기자 "고를 게 없으면 메뉴가 안 뜬다"가 거짓이 됐다. 그게
맞는 변화다 — §3.1은 **`⋯` 자체**를 끝난 스코프 셋에만 주지 않는다고 했고, §3.2는
**`View` 구획만** 조건부라고 했다. 뷰가 하나뿐인 스코프에서 이제 메뉴는 열리고 `View
Options` 한 줄만 들어 있다.

### 9.5 검증

- 유닛 **2,254개 통과**(신규 3: 대화상자가 열린다 · 이 스코프 키에만 쓴다 · 끄면 열의
  입구가 사라진다) · `tsc` 통과
- 전체 E2E **140 통과, 실패 0** — 단독 실행 7.7분
- 실제 앱(dev, en, `/inbox?view=board`):
  - 열 셋에 `+Add a task` 줄 셋, 옛 `+`는 **0개**, 열린 폼 0개
  - `⋯ › View Options` → `View Options × / Show Input Box` — 버튼은 `Close` 하나
  - 끄면 저장소에 `{inbox: {…, showInputBox: false}}`, 줄 셋이 사라지고 열 셋은 남는다
  - 다시 켜면 `true`로 돌아오고 줄 셋이 돌아온다

### 9.6 남은 것

4단계 — `Show Date by`. 대화상자의 둘째 줄이고, 도메인 함수 하나가 그 값을 읽는다.

---

## 10. 4단계 구현 기록 (2026-09-02)

### 10.1 도메인은 셈만 하고 낱말은 카탈로그의 것

`countdownLabel(dueDate, today)`가 **문자열이 아니라 서술**을 돌려준다:

```ts
{ key: "common.today" | "view.daysLeft" | "schedule.overdueDays", days: number }
```

여기서 `"5일 남음"`을 만들면 도메인에 한국어가 들어가고 영어는 올 데가 없어진다.
그리고 늦은 쪽은 **`schedule.overdueDays`를 다시 쓴다** — 일정 편집기가 이미 늦음을
그 낱말로 말하고 있고, 한 화면 건너에서 같은 사실을 다르게 적을 이유가 없다.

날은 자정에서 자정까지 센다. 내일은 오늘 하루 내내 `1일 남음`이고, 시각까지 세면
내일 아침 마감이 오후 네 시에 `0일 남음`이 된다.

### 10.2 자리는 하나였다

§3.5가 예측한 대로 `TaskRowContent`의 `dueLabel` 한 줄에서 갈린다. 목록·보드·매트릭스가
전부 그 컴포넌트를 그리므로 세 화면이 한 번에 따라온다.

프롭은 **선택이고 기본이 `taskTime`**이다. 이것은 스코프의 설정인데 매트릭스는 스코프가
아니다 — 설정을 가진 곳만 넘기고 나머지는 늘 그리던 날짜를 그대로 그린다.

보드의 `완료` 그룹까지 내려보냈다. 같은 열 안에서 위쪽 카드와 아래쪽 카드가 날짜를
다르게 읽으면 한 화면이 두 가지로 말하는 것이 된다.

### 10.3 시험을 둘로 갈랐다

`await user.selectOptions(...)` 뒤에 화면이 안 바뀌어서 한 번 헛짚었다. 이 하네스의
`onSetScopeViewOptions`는 아무 데도 가지 않는다 — **값이 모듈 위에 살기 때문**이고,
3단계의 뷰 시험에서 만난 것과 같은 벽이다.

그래서 쓰기와 읽기를 나눴다: 대화상자가 **무엇을 요청하는가**와, 그 값이 주어졌을 때
행이 **무엇을 그리는가**. 둘을 한 시험에 담으려면 하네스가 상태를 들어야 하는데, 그것은
이 컴포넌트가 하지 않기로 한 일을 시험이 대신 하는 셈이다.

### 10.4 검증

- 유닛 **2,261개 통과**(신규 7: 도메인 5 · 요청 1 · 그리기 1) · `tsc` 통과
- 전체 E2E **140 통과, 실패 0** — 단독 실행 5.9분
- 실제 앱(dev, en, `/inbox?view=board`), 2026-09-07 마감 작업 하나:
  - 처음 `Sep 7`
  - `⋯ › View Options › Show Date by` → 선택지 `Task Time · Countdown`
  - `Countdown` → 카드가 **`5d left`**, 저장소에 `scopeViewOptions.inbox.dateBy = "countdown"`

### 10.5 남은 것

5단계 — `Kanban Size`. 보드가 있는 스코프에만 그리는 줄이라, 3단계의 대화상자가
조건부 줄을 그릴 수 있는지가 여기서 시험된다(Q4도 거기서 답해진다).

---

## 11. 5단계 구현 기록 (2026-09-02)

### 11.1 크기는 열의 폭이다

`--tm-column-w`를 보드에 두고 열이 읽는다. 보드 하나에 크기가 하나라야 하기 때문이다 —
열마다 자기 폭을 계산하면 한 보드가 두 마음을 가질 수 있다.

```
작게 208 · 보통 260 · 크게 320
```

`260`이 `보통`인 것은 **설정이 생기기 전 모든 보드가 그리던 값**이기 때문이다. 나머지
둘의 숫자와 이름은 [추론]이다 — 캡처에 있던 것은 `Medium` 하나뿐이고, Q2는 그래서
닫히지 않는다.

### 11.2 Q4의 답이 예상보다 나빴다 — 두 줄이 아니라 한 줄이다

§3.6은 보드가 없는 스코프에서 대화상자가 **두 줄**이 될 것이라고 적었다. 실제로는
**한 줄**이다.

`Show Input Box`도 열에 작용하기 때문이다(§3.4 — 열 맨 위의 줄). 보드가 없는 스코프에는
열이 없으므로 그 스위치는 켜고 꺼도 아무 일이 없다. 3단계는 그것을 모든 스코프에
그리고 있었고, **그게 여섯 화면에서 죽은 컨트롤이었다.** 5단계가 같이 고쳤다 — §15.5의
같은 조항이 `Kanban Size`와 이것 둘 다에 걸린다.

실측(`/today`): 대화상자의 줄이 `Show Date by` 하나.

한 줄짜리 대화상자를 열 값이 있는가는 **결정이 필요하고, 이 문서가 답하지 않았다.**
Q4에 세 갈래로 적어 둔다.

### 11.3 검증

- 유닛 **2,263개 통과**(신규 2: 보드 있는 곳과 없는 곳의 줄 · 크기가 열에 닿는 것) ·
  `tsc` 통과
- 전체 E2E **140 통과, 실패 0** — 단독 실행 5.3분
- 실제 앱(dev, en):
  - `/inbox?view=board` → 대화상자 세 줄, `tm-board is-medium`, 열 폭 **260**
  - `크게` → `is-large`, 열 폭 **320**, 저장소에 `kanbanSize: "large"`
  - `/today` → 대화상자 **한 줄**(`Show Date by`)

### 11.4 남은 것

6단계 — `Hide Completed` · `Show Details`. 그리고 Q4.

---

## 12. 6단계 구현 기록 (2026-09-02)

### 12.1 `Hide Completed`도 보드에만 있다

§3.7은 "목록 뷰에서는 완료된 행을 질의에서 뺀다"고 적었는데, **이미 빠져 있다.**
`queryScopeTasks`의 `finished`는 기본이 false이고 목록은 그대로 부른다(`isActive`).
보드만 `{ finished: true }`로 한 번 더 물어서 열마다 `완료` 그룹을 만든다.

그래서 이 토글이 숨길 것이 있는 화면은 보드뿐이고, 메뉴에도 `state.view === "board"`일
때만 나온다. 5단계가 `Show Input Box`와 `Kanban Size`에 적용한 §15.5가 셋째로 걸린
자리다 — **이 설계는 같은 실수를 세 번 했다.** 캡처가 보드 화면이라, 보드에서만 뜻이
있는 것을 스코프 전체의 설정으로 읽은 것이다.

### 12.2 Q3 — 본문 없는 카드는 아무것도 그리지 않는다

빈 줄을 남겨 카드 높이를 고르게 만드는 쪽은 고르지 않았다. 그 줄이 말하는 것은 **이
작업에는 할 말이 없다**이고, 그걸 위해 한 줄을 쓰는 셈이다.

그리고 바뀌는 것이 없다는 점이 결정적이다 — 대체되는 마크(`hasBody` 아이콘)도 본문이
없으면 그려지지 않았다. 조용한 카드는 이 설정을 켜기 전과 똑같이 조용하다.

### 12.3 마크와 줄은 같은 사실이다

켜면 아이콘이 **사라지고** 줄이 온다. 둘 다 그리면 한 카드가 "본문이 있다"를 두 번
말한다. 한 줄로 자르는 것도 같은 종류의 절제다 — 본문 길이만큼 자라는 카드는 열이
카드 셋을 담다 하나를 담게 만들고, 그건 `Kanban Size`가 답할 질문이지 카드가 답할
질문이 아니다.

### 12.4 검증

- 유닛 **2,267개 통과**(신규 4: 완료 그룹이 사라진다 · 마크가 줄로 바뀐다 · 본문 없는
  카드 · 여러 줄에서 한 줄) · `tsc` 통과
- 전체 E2E **140 통과, 실패 0** — 단독 실행 6.2분
- 실제 앱(dev, en, `/inbox?view=board`):
  - 메뉴 `List · ✓ Board · Timeline · Hide Completed · Show Details · View Options`
  - `Show Details` → `✓`가 붙고 카드에 `DBc06 sdfsd sdf sdf sdf`, 마크는 0개
  - `Hide Completed` → `완료` 그룹 1개가 0개로
  - 저장소에 `{hideCompleted: true, showDetails: true}`

### 12.5 이 문서의 여섯 단계가 끝났다

남은 것은 Q1(꺼진 `Show Input Box`의 참조 화면) · Q2(`Kanban Size`의 나머지 두 값) ·
Q4(보드 없는 스코프의 한 줄짜리 대화상자) · Q6(`View`를 아이콘 줄로) · Q7(리스트의
`보관`·`삭제`가 머리에 남아 있다). Q4와 Q6·Q7은 스크린샷이 아니라 **결정**이 필요하다.
