# 시각 언어 통일 계획 (§V)

- 대상 설계서: [`TICKTICK_NAV_SHELL_REDESIGN_SPEC.md`](TICKTICK_NAV_SHELL_REDESIGN_SPEC.md) §11 Visual System
- 선행 문서: [`TICKTICK_NAV_SHELL_PHASE0_AUDIT.md`](TICKTICK_NAV_SHELL_PHASE0_AUDIT.md) (D-01~D-30, P0 완료)
- 작성일: 2026-08-19
- 기준 코드: v0.15.0 (`6d9672a`)

---

## V.0 이 문서가 무엇인가

P0가 끝난 뒤 실행 중인 앱을 §11.2의 "TickTick-like" 정의로 채점한 결과와, 어긋난 항목을 어떻게 되돌릴지의 계획이다.

**세 가지만 지킨다.**

1. **판정은 측정에서 나온다.** 아래 모든 숫자는 실행 중인 앱에서 `getComputedStyle` / `getBoundingClientRect`로 잰 것이다. 선언을 읽고 판단하지 않는다 — 이 리포에서 그 실수는 이미 두 번 났다(감사 R.5.3, D-26).
2. **각 단계는 검증 가능한 문장으로 끝난다.** "더 TickTick 같다"는 검증이 아니다. "밝기 순서가 Rail < 사이드바 < 캔버스다"는 검증이다.
3. **감사 문서의 규칙을 그대로 따른다.** 설계서와 이 저장소가 부딪히면 이 문서에서 해소하고, 설계서 본문은 고치지 않는다.

---

## V.1 채점 — 2026-08-19, v0.15.0

설계서 §11.2는 이 프로젝트에서 "TickTick-like"가 무엇인지 아홉 줄로 정의한다. 그 아홉 줄을 그대로 체크리스트로 썼다.

| # | §11.2의 정의 | 실측 | 판정 |
|---|---|---|---|
| 1 | 좁은 Global Rail | 56px | ✅ |
| 2 | muted navigation surfaces | 밝기 = 캔버스 < Rail < 사이드바 (양 테마) | ❌ **반대** |
| 3 | 작은 radius | 6·8·10·12·14·18·999·50% 공존 | ❌ |
| 4 | 일정한 36px navigation rhythm | 사이드바 30 / Rail 40 / 레거시 컨트롤 31~32 | ❌ **격자 셋** |
| 5 | 얇은 divider | 1px | ✅ |
| 6 | subtle hover | `rgba(…, .04~.06)` | ✅ |
| 7 | active state의 과도한 filled color 금지 | 채도 100% 채움 4종 잔존 | ⚠️ |
| 8 | icon + text의 낮은 대비 | 뮤티드 5.22:1, 본문 15.63:1 | ✅ |
| 9 | content-first hierarchy | #2에 종속 | ❌ |

**4승 3패 2유보.** 통과한 넷은 전부 P0에서 셸을 새로 만들며 얻은 것이고, 실패한 셋은 전부 **P0가 손대지 않은 레거시 페이지 쪽**에 뿌리가 있다.

---

## V.2 무엇이 다른가

### V.2.1 네비게이션이 콘텐츠보다 밝다 — 가장 근본

```
다크   캔버스 #101012 (16,16,18)  <  Rail #161618 (22,22,24)  <  사이드바 #1c1c1e (28,28,30)
라이트 캔버스 #f2f2f7 (242,242,247) < Rail #f7f7f8 (247,247,248) < 사이드바 #fafafb (250,250,251)
```

**사이드바가 화면에서 가장 밝은 면이다.** 두 테마 모두. TickTick은 정확히 반대로, 사이드바가 가장 가라앉고 콘텐츠 영역이 가장 밝다. 설계서 §11.6도 `Rail → Sidebar → Main` 순으로 밝아지라고 한다.

### V.2.2 떠 있는 파란 원

| 요소 | 크기 | 배경 | 채도 | 어디에 |
|---|---|---|---|---|
| `.ollama-chat-fab` | 56×56, radius 50% | `#007aff` | **100%** | **모든 페이지** |
| `.gcal-col-date` (오늘) | 32×32, radius 9999 | `#007aff` | 100% | 캘린더 |
| `.tdy-btn` (primary) | 74×32 | `#007aff` | 100% | Today |
| `.tm-quickadd-submit` | 52×34 | `#007aff` | 100% | Tasks |

뒤의 셋은 정당한 primary action이다. 문제는 **첫 줄**이다 — 화면에서 채도가 가장 높고 면적이 가장 큰 채움이 "AI 채팅 열기"이고, 그게 어느 화면에나 떠 있다. §11.67(accent 포화 자제)과 정면으로 부딪히고, TickTick에는 FAB라는 물건 자체가 없다.

### V.2.3 radius가 제각각

§11.39가 정한 범위는 **6~12px**이다.

| 요소 | radius | 초과 |
|---|---|---|
| `.foc-card` | 18px | +6 |
| `.tdy-capture`, `.tdy-brief-icon` | 14px | +2 |
| `.tdy-capture-toggle`, `.foc-options-button` | 999px | pill |
| `.gcal-col-date` | 9999px | 원 |
| `.ollama-chat-fab` | 50% | 원 |

pill과 원이 사각형 사이에 섞이면 "생산성 도구"가 아니라 "앱 런처"의 어휘가 된다.

### V.2.4 수직 리듬이 셋

| 격자 | 값 | 어디 |
|---|---|---|
| A | **30px** | Tasks 사이드바 행 |
| B | **40px** | Rail 아이템 |
| C | **31~32px** | 레거시 페이지의 버튼·입력 |
| — | 56px | 헤더 (§A.4, 의도된 별개 값) |

§11.2가 말하는 "일정한 리듬"이 아니라, 서로 배수 관계도 아닌 격자 세 개가 겹쳐 있다.

### V.2.5 컬러 그림자

실행 중 수집된 그림자 넷 중 하나가 **파란 발광**이다.

```
rgba(47, 111, 224, 0.28) 0px 6px 16px      ← --shadow-accent
rgba(23, 28, 48, 0.18)   0px 2px 8px
rgba(0, 0, 0, 0.3)       0px 1px 2px
rgba(0, 0, 0, 0.38)      0px 4px 16px
```

§11.38은 그림자를 **팝오버와 다이얼로그에만** 허용하고 Rail·사이드바·헤더·뷰 전환에는 금지한다. 색이 있는 그림자는 §11 어디에도 없다.

---

## V.3 왜 재발하는가 — 컴포넌트 언어가 다섯 벌

| 접두사 | 화면 | 자기 카드 | 자기 버튼 | 자기 radius |
|---|---|---|---|---|
| `tm-` | Tasks 모듈 | ○ | ○ | 6·8 |
| `tdy-` | Today | ○ | ○ | 12·14 |
| `gcal-` | 캘린더 | ○ | ○ | 12·9999 |
| `foc-` | Focus | ○ | ○ | 18·999 |
| `spc-` | Spaces | ○ | ○ | 12 |

**V.2의 2~5번은 전부 이 표의 증상이다.** 다섯 벌이 각자 카드·버튼·입력·radius를 정의하고, 화면 하나를 고치면 나머지 넷은 그대로 남는다.

v0.15.0의 [`20-density.css`](src/styles/20-density.css)는 이 다섯 벌 **위에 밀도만 덮은** 층이다. **크기는 맞췄지만 모양은 여전히 다섯 벌이다.** 토큰을 더 얹어도 이 구조는 안 없어진다 — V-6이 별도 단계로 잡혀 있는 이유다.

---

## V.4 정정 — D-26의 오판

P0-10(D-26)에서 §11.6의 표면 계단을 이렇게 번역했다:

> Main을 흰색으로 하지 않는다. 이 앱의 Main은 **캔버스**고 그 위의 카드가 흰색이다. 캔버스를 희게 하면 모든 카드가 평평해진다. chrome이 캔버스와 카드 사이에 앉는 것으로 §11.71(content-first)은 이미 성립한다.

**뒷문장이 틀렸다.** 카드가 있는 화면(Today, Spaces)에서는 성립한다. 그런데 **카드가 없는 화면** — 캘린더 그리드, Focus, 매트릭스 — 에서는 비교 대상이 캔버스뿐이고, 거기서 사이드바는 그냥 화면에서 제일 밝은 면이다. 앱의 절반에서만 참인 근거로 전체를 판정했다.

**남기는 규칙:** 표면 위계를 판단할 때는 **가장 불리한 화면**에서 잰다. 카드가 구제해 주는 화면이 아니라.

---

## V.5 단계

각 단계는 **검증** 줄로 끝난다. 그 문장이 참이 되면 그 단계는 끝난 것이다.

### V-1 표면 밝기 순서 뒤집기 `위험 낮음` `효과 가장 큼`

`--bg-rail` / `--bg-context-sidebar`를 캔버스보다 **가라앉게** 재지정한다. 토큰 정의는 [`01-base.css`](src/styles/01-base.css)의 §11 블록 한 곳뿐이므로 값 네 개만 바뀐다.

- 다크: Rail·사이드바를 `#101012`보다 어둡게 (예: `#0d0d0f` / `#121214`)
- 라이트: `#f2f2f7`보다 낮은 명도로 (예: `#eceef1` / `#f0f1f4`)
- 카드·팝오버 등 elevated 면은 건드리지 않는다 — 그건 캔버스 위에 뜨는 층이고 계단의 반대쪽 끝이다

> **검증:** 다크·라이트 각각에서 `/calendar`·`/focus`·`/board`(카드가 없는 화면)를 열어 세 면의 상대 휘도를 재면 순서가 **Rail < 사이드바 < 캔버스**다.

### V-2 radius 수렴 `위험 낮음`

`--density-radius`(10px)와 §11.40의 매핑으로 14·18·999·50%를 흡수한다. [`20-density.css`](src/styles/20-density.css)에 선택자를 더한다.

- `.foc-card` 18 → 10
- `.tdy-capture`, `.tdy-brief-icon` 14 → 10
- pill(`999`)은 **뱃지·카운트에만** 남긴다. 버튼·토글은 8~10으로
- `.gcal-col-date`의 원은 "오늘"이라는 캘린더 관용구이므로 **예외로 남긴다** — 근거를 이 문서에 적어두는 것이 조건

> **검증:** `.app-shell *`·`.tm-shell *` 중 폭 20px 이상인 요소의 radius를 모으면 `{6, 8, 10, 12}` ∪ (뱃지 pill) ∪ (`.gcal-col-date`)뿐이다.

### V-3 컬러 그림자 폐기 `위험 낮음`

`--shadow-accent`를 제거하고 §11.37의 두 토큰(`popover` / `dialog`)으로 대체한다. 현재 사용처는 `.brand-mark` 계열과 primary 버튼.

> **검증:** 실행 중 수집한 `boxShadow` 값 집합에 `rgb` 성분이 회색이 아닌 항목이 없다. Rail·사이드바·헤더·뷰 전환의 `boxShadow`는 `none`이다.

### V-4 FAB 처리 `판단 필요`

AI 채팅 진입점을 없애자는 것이 아니라, **채도 100% 56px 원이 모든 화면에 상시**인 것이 문제다. 선택지 셋:

| | 무엇 | 비용 | TickTick 정합 |
|---|---|---|---|
| a | Rail 유틸리티로 이동 (검색·설정 옆) | 중 | 가장 높음 |
| b | 크기·채도만 낮춤 (40px, accent-soft + accent 아이콘) | 낮음 | 중간 |
| c | 현행 유지, 근거를 문서화 | 0 | 낮음 |

**a를 권한다.** Rail은 이미 "전역 유틸리티는 하단"이라는 자리를 갖고 있고(§2.5), FAB가 사라지면 §11.67 위반 중 가장 큰 것이 없어진다. 단 Rail 항목 추가는 §1.5의 금지 목록과 대조가 필요하다 — **Q-06(Matrix)와 같은 성격의 판단**이므로 결정 번호를 받아야 한다.

> **검증:** (a 선택 시) 어느 페이지에서도 `position: fixed`이면서 채도 60% 이상인 40px 초과 요소가 없다.

### V-5 리듬 통일 `위험 중간`

격자를 **32px** 하나로 모은다. 사이드바 30 → 32, 레거시 컨트롤 31~32 → 32. Rail의 40은 §2.5가 정한 값이므로 **건드리지 않고 예외로 명시**한다(아이콘 전용 열은 텍스트 행과 다른 리듬을 가져도 된다).

- `--density-control-h`가 이미 32px이므로 레거시는 대부분 자동
- Tasks 사이드바의 30px는 §2.29가 "행 최소 높이"로 정한 값 → 32로 올리면 §2.29의 개정이 필요하다

> **검증:** 사이드바 행·레거시 컨트롤·입력의 높이 집합이 `{32}`(터치에서 `{44}`)이고, 40px는 `.rail-item`에만 나타난다.

### V-6 컴포넌트 언어 통합 `위험 큼` `별도 단계`

다섯 벌을 한 벌로 모은다. **V-1~V-5가 끝난 뒤에 착수한다** — 앞 단계들이 "무엇이 공통값인가"를 이미 토큰으로 뽑아 놓기 때문이다.

착수 순서 제안:
1. 카드 — `tm-card`(가칭) 하나로, `tdy-card`·`foc-card`·`summary-card`·`topic-card` 흡수
2. 버튼 — primary / secondary / ghost 셋
3. 입력 — 한 벌
4. 행 — 리스트 행 한 벌

> **검증:** 새 화면을 만들 때 `tdy-`·`foc-`·`gcal-` 접두사의 카드/버튼/입력 클래스를 **하나도** 쓰지 않고 완성할 수 있다.

---

## V.6 수용 기준

전 단계가 끝났을 때 §V.1의 표를 다시 채점해 **❌가 없고 ⚠️는 근거가 이 문서에 적혀 있는 것만** 남는다.

측정은 브라우저에서 하고, 다음 넷을 반드시 포함한다.

1. **양 테마** — 라이트에서만 맞는 팔레트는 절반만 맞은 것이다
2. **카드가 없는 화면** — 캘린더·Focus·매트릭스 (V.4의 교훈)
3. **양 셸** — Tasks 모듈과 레거시를 **건너가며** 잰다 (D-30의 교훈)
4. **양 포인터** — `(pointer: coarse)`에서 히트 영역이 44px 아래로 내려가지 않는다

---

## V.7 하지 않기로 한 것

- **§11.3의 단일 accent `#5B6EF5`** — `[data-accent]` 5색 사용자 설정이 죽는다. D-26에서 확정, 유지.
- **§11.5의 푸른기 다크 팔레트** — 이 리포의 다크는 순중립이다. 두 면만 다른 팔레트에서 가져오면 테마가 둘로 보인다. D-26에서 확정, 유지.
- **§11.6의 흰색 Main** — 이 앱의 Main은 캔버스다. 다만 **그 판정의 근거였던 문장은 V.4에서 정정됐다** — 결론은 같고 이유가 바뀌었다.
- **전역 `zoom` 재도입** — D-30에서 폐기. 밀도는 토큰으로만 조절한다.

---

## V.8 미결

| # | 질문 | 막는 단계 |
|---|---|---|
| V-Q1 | FAB를 Rail로 올릴 것인가 (§1.5 금지 목록과 대조 필요) | V-4 |
| V-Q2 | 사이드바 행 30 → 32는 §2.29 개정을 요구한다. 개정할 것인가 | V-5 |
| V-Q3 | `.gcal-col-date`의 원을 캘린더 관용구로 예외 인정할 것인가 | V-2 |

**V-1과 V-3은 막는 미결이 없다.** 둘 다 토큰 값 변경이고, 효과가 가장 큰 것(V-1)이 그 안에 있다.
