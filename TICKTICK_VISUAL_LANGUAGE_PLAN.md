# 시각 언어 통일 계획 (§V)

- 대상 설계서: [`TICKTICK_NAV_SHELL_REDESIGN_SPEC.md`](TICKTICK_NAV_SHELL_REDESIGN_SPEC.md) §11 Visual System
- 선행 문서: [`TICKTICK_NAV_SHELL_PHASE0_AUDIT.md`](TICKTICK_NAV_SHELL_PHASE0_AUDIT.md) (D-01~D-30, P0 완료)
- 작성일: 2026-08-19
- 기준 코드: v0.15.0 (`6d9672a`)

---

## V.0 이 문서가 무엇인가

P0가 끝난 뒤 실행 중인 앱을 §11.2의 "TickTick-like" 정의로 채점한 결과와, 어긋난 항목을 어떻게 되돌릴지의 계획이다.

**세 가지만 지킨다.**

1. **판정은 측정에서 나온다.** 아래 모든 숫자는 실행 중인 앱에서 `getComputedStyle` / `getBoundingClientRect`로 잰 것이다. 선언을 읽고 판단하지 않는다 — 이 리포에서 그 실수는 이미 두 번 났다(감사 R.5.3, D-26).
2. **각 단계는 검증 가능한 문장으로 끝난다.** "더 TickTick 같다"는 검증이 아니다. "밝기 순서가 Rail < 사이드바 < 캔버스다"는 검증이다.
3. **감사 문서의 규칙을 그대로 따른다.** 설계서와 이 저장소가 부딪히면 이 문서에서 해소하고, 설계서 본문은 고치지 않는다.

---

## V.1 채점 — 2026-08-19, v0.15.0

설계서 §11.2는 이 프로젝트에서 "TickTick-like"가 무엇인지 아홉 줄로 정의한다. 그 아홉 줄을 그대로 체크리스트로 썼다.

| # | §11.2의 정의 | 실측 | 판정 |
|---|---|---|---|
| 1 | 좁은 Global Rail | 56px | ✅ |
| 2 | muted navigation surfaces | 밝기 = 캔버스 < Rail < 사이드바 (양 테마) | ❌ **반대** |
| 3 | 작은 radius | 6·8·10·12·14·18·999·50% 공존 | ❌ |
| 4 | 일정한 36px navigation rhythm | 사이드바 30 / Rail 40 / 레거시 컨트롤 31~32 | ❌ **격자 셋** |
| 5 | 얇은 divider | 1px | ✅ |
| 6 | subtle hover | `rgba(…, .04~.06)` | ✅ |
| 7 | active state의 과도한 filled color 금지 | 채도 100% 채움 4종 잔존 | ⚠️ |
| 8 | icon + text의 낮은 대비 | 뮤티드 5.22:1, 본문 15.63:1 | ✅ |
| 9 | content-first hierarchy | #2에 종속 | ❌ |

**4승 3패 2유보.** 통과한 넷은 전부 P0에서 셸을 새로 만들며 얻은 것이고, 실패한 셋은 전부 **P0가 손대지 않은 레거시 페이지 쪽**에 뿌리가 있다.

---

## V.2 무엇이 다른가

### V.2.1 네비게이션이 콘텐츠보다 밝다 — 가장 근본

```
다크   캔버스 #101012 (16,16,18)  <  Rail #161618 (22,22,24)  <  사이드바 #1c1c1e (28,28,30)
라이트 캔버스 #f2f2f7 (242,242,247) < Rail #f7f7f8 (247,247,248) < 사이드바 #fafafb (250,250,251)
```

**사이드바가 화면에서 가장 밝은 면이다.** 두 테마 모두. TickTick은 정확히 반대로, 사이드바가 가장 가라앉고 콘텐츠 영역이 가장 밝다. 설계서 §11.6도 `Rail → Sidebar → Main` 순으로 밝아지라고 한다.

### V.2.2 떠 있는 파란 원

| 요소 | 크기 | 배경 | 채도 | 어디에 |
|---|---|---|---|---|
| `.ollama-chat-fab` | 56×56, radius 50% | `#007aff` | **100%** | **모든 페이지** |
| `.gcal-col-date` (오늘) | 32×32, radius 9999 | `#007aff` | 100% | 캘린더 |
| `.tdy-btn` (primary) | 74×32 | `#007aff` | 100% | Today |
| `.tm-quickadd-submit` | 52×34 | `#007aff` | 100% | Tasks |

뒤의 셋은 정당한 primary action이다. 문제는 **첫 줄**이다 — 화면에서 채도가 가장 높고 면적이 가장 큰 채움이 "AI 채팅 열기"이고, 그게 어느 화면에나 떠 있다. §11.67(accent 포화 자제)과 정면으로 부딪히고, TickTick에는 FAB라는 물건 자체가 없다.

### V.2.3 radius가 제각각

§11.39가 정한 범위는 **6~12px**이다.

| 요소 | radius | 초과 |
|---|---|---|
| `.foc-card` | 18px | +6 |
| `.tdy-capture`, `.tdy-brief-icon` | 14px | +2 |
| `.tdy-capture-toggle`, `.foc-options-button` | 999px | pill |
| `.gcal-col-date` | 9999px | 원 |
| `.ollama-chat-fab` | 50% | 원 |

pill과 원이 사각형 사이에 섞이면 "생산성 도구"가 아니라 "앱 런처"의 어휘가 된다.

### V.2.4 수직 리듬이 셋

| 격자 | 값 | 어디 |
|---|---|---|
| A | **30px** | Tasks 사이드바 행 |
| B | **40px** | Rail 아이템 |
| C | **31~32px** | 레거시 페이지의 버튼·입력 |
| — | 56px | 헤더 (§A.4, 의도된 별개 값) |

§11.2가 말하는 "일정한 리듬"이 아니라, 서로 배수 관계도 아닌 격자 세 개가 겹쳐 있다.

### V.2.5 컬러 그림자

실행 중 수집된 그림자 넷 중 하나가 **파란 발광**이다.

```
rgba(47, 111, 224, 0.28) 0px 6px 16px      ← --shadow-accent
rgba(23, 28, 48, 0.18)   0px 2px 8px
rgba(0, 0, 0, 0.3)       0px 1px 2px
rgba(0, 0, 0, 0.38)      0px 4px 16px
```

§11.38은 그림자를 **팝오버와 다이얼로그에만** 허용하고 Rail·사이드바·헤더·뷰 전환에는 금지한다. 색이 있는 그림자는 §11 어디에도 없다.

---

## V.3 왜 재발하는가 — 컴포넌트 언어가 다섯 벌

| 접두사 | 화면 | 자기 카드 | 자기 버튼 | 자기 radius |
|---|---|---|---|---|
| `tm-` | Tasks 모듈 | ○ | ○ | 6·8 |
| `tdy-` | Today | ○ | ○ | 12·14 |
| `gcal-` | 캘린더 | ○ | ○ | 12·9999 |
| `foc-` | Focus | ○ | ○ | 18·999 |
| `spc-` | Spaces | ○ | ○ | 12 |

**V.2의 2~5번은 전부 이 표의 증상이다.** 다섯 벌이 각자 카드·버튼·입력·radius를 정의하고, 화면 하나를 고치면 나머지 넷은 그대로 남는다.

v0.15.0의 [`20-density.css`](src/styles/20-density.css)는 이 다섯 벌 **위에 밀도만 덮은** 층이다. **크기는 맞췄지만 모양은 여전히 다섯 벌이다.** 토큰을 더 얹어도 이 구조는 안 없어진다 — V-6이 별도 단계로 잡혀 있는 이유다.

---

## V.4 정정 — D-26의 오판

P0-10(D-26)에서 §11.6의 표면 계단을 이렇게 번역했다:

> Main을 흰색으로 하지 않는다. 이 앱의 Main은 **캔버스**고 그 위의 카드가 흰색이다. 캔버스를 희게 하면 모든 카드가 평평해진다. chrome이 캔버스와 카드 사이에 앉는 것으로 §11.71(content-first)은 이미 성립한다.

**뒷문장이 틀렸다.** 카드가 있는 화면(Today, Spaces)에서는 성립한다. 그런데 **카드가 없는 화면** — 캘린더 그리드, Focus, 매트릭스 — 에서는 비교 대상이 캔버스뿐이고, 거기서 사이드바는 그냥 화면에서 제일 밝은 면이다. 앱의 절반에서만 참인 근거로 전체를 판정했다.

**남기는 규칙:** 표면 위계를 판단할 때는 **가장 불리한 화면**에서 잰다. 카드가 구제해 주는 화면이 아니라.

---

## V.5 단계

각 단계는 **검증** 줄로 끝난다. 그 문장이 참이 되면 그 단계는 끝난 것이다.

### V-1 표면 밝기 순서 뒤집기 `위험 낮음` `효과 가장 큼`

`--bg-rail` / `--bg-context-sidebar`를 캔버스보다 **가라앉게** 재지정한다. 토큰 정의는 [`01-base.css`](src/styles/01-base.css)의 §11 블록 한 곳뿐이므로 값 네 개만 바뀐다.

- 다크: Rail·사이드바를 `#101012`보다 어둡게 (예: `#0d0d0f` / `#121214`)
- 라이트: `#f2f2f7`보다 낮은 명도로 (예: `#eceef1` / `#f0f1f4`)
- 카드·팝오버 등 elevated 면은 건드리지 않는다 — 그건 캔버스 위에 뜨는 층이고 계단의 반대쪽 끝이다

> **검증:** 다크·라이트 각각에서 `/calendar`·`/focus`·`/board`(카드가 없는 화면)를 열어 세 면의 상대 휘도를 재면 순서가 **Rail < 사이드바 < 캔버스**다.

**결과 — 2026-08-19, 통과.** 실행 중인 앱에서 잰 상대 휘도(각 면의 유효 배경을 부모까지 거슬러 올라가 구한 뒤 WCAG 식으로 계산):

| 테마 | Rail | 사이드바 | 캔버스 |
|---|---|---|---|
| 라이트 | `#eceef1` 0.8533 | `#f0f1f4` 0.8797 | `#f2f2f7` 0.8910 |
| 다크 | `#0a0a0c` 0.00308 | `#0d0d0f` 0.00408 | `#101012` 0.00524 |

`/calendar`·`/focus`·`/board`(사이드바 없음, Rail < 캔버스)와 `/tasks`·`/spaces`(세 면 전부) × 양 테마 = 10개 조합 전부 순서가 맞다.

**이 문서가 예시로 적어둔 다크 값은 틀렸다.** `#0d0d0f` / `#121214`로 넣고 재보니 사이드바(18,18,20)가 캔버스(16,16,18)보다 **밝아서** `/tasks`·`/spaces`에서 FAIL이 났다. 계단이 세 칸인데 두 칸만 보고 고른 값이다. `#0a0a0c` / `#0d0d0f`로 고쳤다 — 3·3·2의 균등한 계단이고, §11.69(순검정 금지)에도 걸리지 않는다.

부수 효과 하나를 기록해 둔다. `--text-tertiary`(#8e8e93)의 대비가 라이트에서 Rail 3.05 → **2.81**, 사이드바 3.13 → **2.89**로 내려갔다. 다크는 반대로 5.54 → **6.07** / 5.22 → **5.95**로 올라갔다. 라이트의 이 토큰은 **원래 캔버스 위에서 이미 2.92**였으므로 이제 셋이 같은 수준으로 모인 것이고, 토큰 자체가 AA 아래인 것은 이 단계가 만든 문제가 아니다 — 별건으로 남긴다. (§V.1의 8번이 잰 5.22:1은 이 토큰이 아니라 카드 위의 `--text-secondary`였다. 카드는 이 단계가 건드리지 않았다.)

### V-2 radius 수렴 `위험 낮음`

`--density-radius`(10px)와 §11.40의 매핑으로 14·18·999·50%를 흡수한다. [`20-density.css`](src/styles/20-density.css)에 선택자를 더한다.

- `.foc-card` 18 → 10
- `.tdy-capture`, `.tdy-brief-icon` 14 → 10
- pill(`999`)은 **뱃지·카운트에만** 남긴다. 버튼·토글은 8~10으로
- `.gcal-col-date`의 원은 "오늘"이라는 캘린더 관용구이므로 **예외로 남긴다** — 근거를 이 문서에 적어두는 것이 조건

> **검증:** `.app-shell *`·`.tm-shell *` 중 폭 20px 이상인 요소의 radius를 모으면 `{6, 8, 10, 12}` ∪ (뱃지 pill) ∪ (`.gcal-col-date`)뿐이다.

**결과 — 2026-08-19, 통과.** 데이터가 있는 계정으로 8개 라우트 + 다이얼로그·Task Detail·커맨드 메뉴를 돌아 폭 20px 이상 요소의 radius를 모으면 `{6, 8, 10, 12}` ∪ 아래 예외뿐이다. 빈 계정에서는 카드·행·모달이 아예 렌더되지 않아 **바꾸기 전 코드로도 통과했을 것**이라, 가드는 태스크를 만들고 다이얼로그를 연 뒤에 잰다.

**방법을 하나 바꿨다.** 이 문서는 `20-density.css`에 선택자를 더하라고 했지만, 정의 자리의 값은 그대로 둔 채 덮는 층을 하나 더 얹는 셈이라 **정의를 고쳤다**(48개 규칙). §V.3이 지적한 "다섯 벌 위에 층을 얹는다"를 V-2에서 또 하지 않기 위해서고, V-6이 나중에 두 군데를 풀지 않아도 된다.

가장 큰 한 방은 선택자가 아니라 **토큰**이었다. [`01-base.css`](src/styles/01-base.css)의 옛 이름 `--rounded-xs/md/lg`가 5·11·18px이었다 — 스케일에 없는 값 셋이고, `--rounded-lg`를 쓰는 규칙은 자기가 스케일을 벗어나는지 알 방법이 없었다. 이름은 두고 값만 6·10·12로 옮기니 ~50개 규칙이 한 번에 들어왔다. `--radius-xl`(16)도 12로.

매핑은 §11.40 그대로다.

| 무엇 | 값 | 근거 |
|---|---|---|
| 다이얼로그·시트·패널 | 12 | §11.40 Search panel |
| 팝오버·메뉴 | 10 | §11.40 Popover/Menu |
| 카드·타일·행 | 10 | 밀도 층이 이미 쓰던 `--density-radius` |
| 버튼·입력·헤더 액션 | 8 | §11.40 Header action / Input |
| 툴팁·kbd | 6 | §11.40 Tooltip |

**V-Q3 결정 — 캘린더 날짜의 원은 남긴다.** 여기에 `.gcal-mini-day`(미니 월의 날짜 칸)도 같은 관용구로 포함한다. 하나만 원이고 다른 하나가 사각형이면 같은 "날짜"가 두 모양이 된다.

스케일 밖에 남은 것과 그 이유 — 이 목록이 [`e2e/radiusScale.spec.ts`](e2e/radiusScale.spec.ts)의 허용 목록이다. **적어두지 않은 예외가 다섯 값짜리 스케일을 여덟 값으로 되돌리는 경로**이므로, 예외는 코드와 이 표 양쪽에 있어야 한다.

| 무엇 | 모양 | 이유 |
|---|---|---|
| `.gcal-col-date`, `.gcal-mini-day` | 원 | 캘린더 날짜 관용구 (V-Q3) |
| `.gcal-cat-badge`, `.gcal-now-badge`, `.gcal-taskpanel-rail-badge`, `.foc-group-title small`, `.ff-board-count` | pill | §11.2가 뱃지·카운트에 남겨둔 것 |
| `.rail-avatar-initial` | 원 | §11.40 Avatar round |
| `.ff-color-swatch`, `.tm-swatch` | 원 | 색은 컨트롤이 아니라 점이다 |
| `.tm-preview-*` | 3~5px | 레이아웃 축소 썸네일 — 여기서 radius는 모양이 아니라 질감이다 |
| `.ollama-chat-fab` | 원 | **결정이 아니다.** V-4의 것이고 V-Q1에 막혀 있다 |

판단이 갈릴 수 있는 것 하나: `.foc-play`(42px 원형 재생 버튼)를 10px로 내렸다. 타이머의 원형 재생은 흔한 관용구지만 §11 어디에도 근거가 없고, 같은 화면의 `.foc-options-button`(pill)을 내리면서 이것만 원으로 두면 Focus 화면에만 원이 남는다.

### V-3 컬러 그림자 폐기 `위험 낮음`

`--shadow-accent`를 제거하고 §11.37의 두 토큰(`popover` / `dialog`)으로 대체한다. 현재 사용처는 `.brand-mark` 계열과 primary 버튼.

> **검증:** 실행 중 수집한 `boxShadow` 값 집합에 `rgb` 성분이 회색이 아닌 항목이 없다. Rail·사이드바·헤더·뷰 전환의 `boxShadow`는 `none`이다.

**결과 — 2026-08-19, 통과.** 7개 라우트 × 양 테마에서 모든 요소의 `boxShadow`를 걷었다. 색이 있는 값 **0건**, Rail·사이드바·헤더의 `boxShadow`는 `none`. 남은 값은 전부 `rgba(0,0,0,·)` 넷(라이트)과 셋(다크)이고, 그림자가 붙은 요소는 툴팁·팝오버·커맨드 메뉴 — §11.38이 허용하는 곳뿐이다.

**`--shadow-accent`만 지워서는 안 됐다.** §V.2.5의 표는 파란 발광을 그 토큰의 것으로 적었지만, 실제로 화면에 그려지던 두 개는 [`04-today.css`](src/styles/04-today.css)에 **하드코딩**되어 있었다 — `.tdy-brief-icon`의 `rgba(47,111,224,.28)`와 `.tdy-btn-navy`의 `rgba(23,28,48,.18)`. 둘 다 테마 분기가 없어 다크에서도 같은 값을 그리고 있었다. 토큰만 지우고 검증했으면 이 둘이 그대로 남은 채 통과로 보였을 것이다. 검증을 **실행 중인 앱에서 값을 걷어** 하기로 한 것이 잡아냈다.

셋 다 팝오버도 다이얼로그도 아니므로 회색 그림자로 바꾸지 않고 **없앴다**(§11.38: 버튼·아이콘에는 그림자가 없다).

### V-4 FAB 처리 `판단 필요`

AI 채팅 진입점을 없애자는 것이 아니라, **채도 100% 56px 원이 모든 화면에 상시**인 것이 문제다. 선택지 셋:

| | 무엇 | 비용 | TickTick 정합 |
|---|---|---|---|
| a | Rail 유틸리티로 이동 (검색·설정 옆) | 중 | 가장 높음 |
| b | 크기·채도만 낮춤 (40px, accent-soft + accent 아이콘) | 낮음 | 중간 |
| c | 현행 유지, 근거를 문서화 | 0 | 낮음 |

**a를 권한다.** Rail은 이미 "전역 유틸리티는 하단"이라는 자리를 갖고 있고(§2.5), FAB가 사라지면 §11.67 위반 중 가장 큰 것이 없어진다. 단 Rail 항목 추가는 §1.5의 금지 목록과 대조가 필요하다 — **Q-06(Matrix)와 같은 성격의 판단**이므로 결정 번호를 받아야 한다.

> **검증:** (a 선택 시) 어느 페이지에서도 `position: fixed`이면서 채도 60% 이상인 40px 초과 요소가 없다.

**결과 — 2026-08-19, a 선택. 통과.** 양 테마 × 8개 라우트에서 `position: fixed`이고 채도 60% 이상인 40px 초과 요소 **0건**.

**V-Q1 결정 — Rail의 유틸리티로 올린다.** §1.5는 Rail의 항목을 닫힌 목록으로 정해 두었고 AI는 거기 없다. 예외의 근거는 **D-19에 쓴 논리를 한 번 더 적용한 것**이다 — 그 금지 목록이 막는 것은 *화면*이 Rail 항목으로 승격되는 일인데, 이건 화면이 아니다. 있던 자리 위에 패널을 여는 유틸리티고, 그건 바로 위 줄의 Search가 하는 일과 같은 종류다. Settings는 §1.5가 최하단으로 못 박았으므로 그 위에 넣는다.

**§V.2.2의 "모든 페이지"는 틀렸다.** FAB는 레거시 브랜치 안에서만 렌더되고 있었다 — Tasks 모듈의 열 개 라우트에는 **애초에 없었다.** 그래서 진입점을 Rail(양 셸 위)로 올리자마자, 버튼은 모든 화면에 있는데 패널은 절반에서만 열리는 상태가 됐다. `renderAiChat()`을 커맨드 메뉴와 같은 층으로 끌어올려 해소했다. 이게 **가드가 두 셸을 다 도는 이유**다(D-30).

**옮기다가 원래 있던 버그가 드러났다.** 패널을 닫으면 `AnimatePresence`가 exit 애니메이션만 돌리고 노드를 **트리에서 빼지 않았다**. React fiber로 확인하니 `OllamaChat [open=false]`인데 섹션은 `PresenceChild` 아래 그대로 있었고, 그 요소는 412×592에 `opacity: 0`, `pointer-events: auto` — 화면 오른쪽 아래 모서리에서 **보이지 않는 채로 클릭을 먹고 있었다.** FAB도 같은 상태를 토글하고 있었으니 이 버그는 V-4보다 오래됐다. 퇴장 애니메이션 하나와 투명 클릭 방해물을 맞바꿀 이유가 없어, 패널은 `open`과 함께 마운트/언마운트한다(입장 애니메이션은 유지).

**767px 아래에는 Rail이 없다.** [`19-app-shell.css`](src/styles/19-app-shell.css)가 모바일에서 Rail을 `display: none`으로 두고 있어(§2.39의 모바일 셸은 아직 없다), 진입점을 Rail로만 옮기면 작은 화면에서 AI가 **사라진다.** 그 폭에서만 나오는 40px 런처를 뒀다 — 56px 채도 100% 원이 아니라, 테두리 있는 surface 색 버튼에 radius 10이다. 두 진입점이 한 화면에 같이 나오지 않는 것도 가드가 확인한다.

덤으로 하나 더: 900px 아래의 `.ollama-chat-panel`이 radius **16**이었다. V-2가 놓친 이유는 그 가드가 데스크톱 표현만 재기 때문이다 — 이 규칙은 900px 아래에서만 존재한다. 12로 내렸다. **미디어 쿼리 안의 값은 그 폭에서 재지 않으면 보이지 않는다.**

### V-5 리듬 통일 `위험 중간`

격자를 **32px** 하나로 모은다. 사이드바 30 → 32, 레거시 컨트롤 31~32 → 32. Rail의 40은 §2.5가 정한 값이므로 **건드리지 않고 예외로 명시**한다(아이콘 전용 열은 텍스트 행과 다른 리듬을 가져도 된다).

- `--density-control-h`가 이미 32px이므로 레거시는 대부분 자동
- Tasks 사이드바의 30px는 §2.29가 "행 최소 높이"로 정한 값 → 32로 올리면 §2.29의 개정이 필요하다

> **검증:** 사이드바 행·레거시 컨트롤·입력의 높이 집합이 `{32}`(터치에서 `{44}`)이고, 40px는 `.rail-item`에만 나타난다.

**결과 — 2026-08-19, 통과. 단 값이 32가 아니라 36이다.**

**이 단계의 전제가 틀렸다.** 위에서 사이드바 행 30px를 "§2.29가 정한 값"이라고 썼고 코드의 주석도 그렇게 달려 있었는데, §2.29는 **Pointer Behavior**다 — 클릭·더블클릭·우클릭 얘기고 높이는 한 줄도 없다. 행 높이를 정하는 절은 §4.86/§4.87이고, 거기 값은 이렇다.

| 무엇 | 설계서 값 |
|---|---|
| Nav row | 36px |
| Tree row rhythm | 36px |
| + Space(생성) row | 32px |
| Section header | 28px |
| Section action | 28px |

그리고 §V.1의 채점표 4번이 인용한 §11.2의 정의 자체가 **"일정한 36px navigation rhythm"**이다. 32로 모았으면 채점표의 그 줄은 통과가 아니라 "설계서와 다른 값으로 통일"이 됐을 것이다.

**V-Q2는 해소됐다 — 개정할 §2.29가 없다.** 고칠 것은 설계서가 아니라 잘못 단 인용 주석이었고, [`17-tasks-module.css`](src/styles/17-tasks-module.css)의 `.tm-row` 주석을 §4.86으로 바꿨다.

**격자:**

| 어디 | 값 | 근거 |
|---|---|---|
| 사이드바 nav·tree 행 | **36** | §4.86 |
| 사이드바 생성 행 | 32 | §4.86 |
| 사이드바 섹션 액션 | 28 | §4.87 |
| Main의 버튼·입력·셀렉트 | **32** | `--density-control-h`, §4.86의 생성 행과 같은 값 |
| Rail 아이템 | 40 | §2.5, 예외로 명시 |

**실측은 "31~32"가 아니었다.** 위 §V.2.4는 레거시 컨트롤을 31~32로 적었지만, 실제로 화면에 나오는 높이는 24·26·28·30·33·34·38·40·41·42였다. 설정 화면의 세그먼티드가 24, Tasks 헤더의 Archive/Delete가 26, 캘린더 카테고리 행이 28, Quick Add가 34, Matrix의 셀렉트가 33, Focus의 옵션 버튼이 42. 14개 선언과 전역 `input`/`select` 바닥값 하나로 모았다.

그 전역 바닥값에서 하나 배웠다. `input, select { min-height: 32px }`를 그대로 걸면 **체크박스와 라디오까지** 32가 되고, 그것을 담은 행이 전부 42px로 부푼다(`.gcal-cat-row`에서 바로 났다). 체크박스는 자기 상자 크기로 사는 요소라 규칙에서 뺐다.

격자 밖에 남은 것 — [`e2e/verticalRhythm.spec.ts`](e2e/verticalRhythm.spec.ts)의 허용 목록이다.

| 무엇 | 높이 | 이유 |
|---|---|---|
| `.rail-item` | 40 | §2.5. 아이콘 전용 열은 텍스트 행과 다른 리듬을 가져도 된다 |
| `.tm-task-open`, `.foc-task-main`, `.motion-task-row` | 21~65 | **태스크 행은 콘텐츠다.** 제목 한 줄이냐 제목+메타 두 줄이냐로 높이가 정해지고, 격자는 단일 행과 컨트롤의 것이다 |
| `.gcal-mini-day` | 30 | 미니 월의 날짜 칸 — 자기 그리드가 크기를 준다 |
| `.ff-color-swatch` | 28 | 색은 컨트롤이 아니라 점이다 |
| `.gcal-taskpanel-rail-btn` | 56 | 세로 탭 — 행을 따라 읽는 물건이 아니다 |
| `.ollama-chat-fab` | 56 | **결정이 아니다.** V-4의 것이고 V-Q1에 막혀 있다 |

`.foc-play`(42×42 원형 재생)는 32×32로 내렸다. V-2에서 radius를 내린 것과 같은 이유이고, 근거 절이 없는 예외를 하나 남기면 Focus만 다른 격자를 갖게 된다.

터치 바닥값(44)은 이 단계가 건드리지 않았다. §15.12의 것이고 [`e2e/navShell.spec.ts`](e2e/navShell.spec.ts)가 이미 지키고 있어, 이 가드는 데스크톱에서만 돈다.

### V-6 컴포넌트 언어 통합 `위험 큼` `별도 단계`

다섯 벌을 한 벌로 모은다. **V-1~V-5가 끝난 뒤에 착수한다** — 앞 단계들이 "무엇이 공통값인가"를 이미 토큰으로 뽑아 놓기 때문이다.

착수 순서 제안:
1. 카드 — `tm-card`(가칭) 하나로, `tdy-card`·`foc-card`·`summary-card`·`topic-card` 흡수
2. 버튼 — primary / secondary / ghost 셋
3. 입력 — 한 벌
4. 행 — 리스트 행 한 벌

> **검증:** 새 화면을 만들 때 `tdy-`·`foc-`·`gcal-` 접두사의 카드/버튼/입력 클래스를 **하나도** 쓰지 않고 완성할 수 있다.

---

## V.6 수용 기준

전 단계가 끝났을 때 §V.1의 표를 다시 채점해 **❌가 없고 ⚠️는 근거가 이 문서에 적혀 있는 것만** 남는다.

측정은 브라우저에서 하고, 다음 넷을 반드시 포함한다.

1. **양 테마** — 라이트에서만 맞는 팔레트는 절반만 맞은 것이다
2. **카드가 없는 화면** — 캘린더·Focus·매트릭스 (V.4의 교훈)
3. **양 셸** — Tasks 모듈과 레거시를 **건너가며** 잰다 (D-30의 교훈)
4. **양 포인터** — `(pointer: coarse)`에서 히트 영역이 44px 아래로 내려가지 않는다

---

## V.7 하지 않기로 한 것

- **§11.3의 단일 accent `#5B6EF5`** — `[data-accent]` 5색 사용자 설정이 죽는다. D-26에서 확정, 유지.
- **§11.5의 푸른기 다크 팔레트** — 이 리포의 다크는 순중립이다. 두 면만 다른 팔레트에서 가져오면 테마가 둘로 보인다. D-26에서 확정, 유지.
- **§11.6의 흰색 Main** — 이 앱의 Main은 캔버스다. 다만 **그 판정의 근거였던 문장은 V.4에서 정정됐다** — 결론은 같고 이유가 바뀌었다.
- **전역 `zoom` 재도입** — D-30에서 폐기. 밀도는 토큰으로만 조절한다.

---

## V.8 미결

| # | 질문 | 막는 단계 |
|---|---|---|
| ~~V-Q1~~ | ~~FAB를 Rail로 올릴 것인가~~ → **올린다(a).** §1.5의 예외, 근거는 D-19와 같다 (V-4에서 결정) | — |
| ~~V-Q2~~ | ~~사이드바 행 30 → 32는 §2.29 개정을 요구한다~~ → **질문이 성립하지 않았다.** §2.29는 Pointer Behavior고, 행 높이는 §4.86이 36으로 정해 둔 것이었다 (V-5에서 확인) | — |
| ~~V-Q3~~ | ~~`.gcal-col-date`의 원을 캘린더 관용구로 예외 인정할 것인가~~ → **인정**, `.gcal-mini-day` 포함 (V-2에서 결정) | — |

~~**V-1과 V-3은 막는 미결이 없다.**~~ V-1~V-5 완료, 미결 셋 다 해소. 남은 것은 **V-6**뿐이다.
