# 타이머는 자리를 지키고, 목록만 흐른다

> 상태: **Phase 1~4 완료 · I6 조사 중 전역 집중 바 버그 수정** · 2026-09-06
> (I1=A 페이지 지역 · I2=B clamp 유지+예외 · I3=B OverlayScrollbar · I4=최소폭 기반 ·
>  I5=B 배치 먼저 · I6=별도 · I7=타이머만 — 사용자 확정)
> 요청: 시안 B의 배치 + **"리스트가 있는 쪽만 스크롤하고 전체 화면은 안 움직이게"**
> 대상: `components/FocusPage.tsx`(1,209줄) · `styles/24-focus.css`(539줄) ·
> `styles/19-app-shell.css`의 스크롤 모델
> 선행 문서: `SWISS_MINIMAL_DESIGN.md`(자·색·모션 — §5 스케일, §15 모션) ·
> `docs/focus-v2-implementation.md`(방금 올라간 포커스 v2)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 실행 중인 앱에서 잰 스크롤 구조 · 현재 포커스 화면의 DOM과 CSS · 시안 대비 존재 여부 | [실측] |
| **관찰** | 시안 B 한 장 | [관찰] |
| **결정** | 사용자 — 이 배치, 그리고 목록만 스크롤 | [결정] |
| **추론** | 시안이 보여주지 않는 것(빈 상태 · 큐가 0개일 때 · 좁은 폭) | [추론] |

---

## 1. 먼저 스크롤부터 — 이 앱은 문서가 통째로 스크롤된다 [실측]

`/focus`를 띄우고 스크롤 컨테이너를 전수 조사했다.

```
내부 스크롤러: 0개
문서: scrollHeight 1086 > clientHeight   ← 유일한 스크롤러
.focus-page-v2: overflow visible, height 1038
```

**"전체 화면은 안 움직인다"는 지금 구조에서 성립하지 않는다.** 페이지가 길어지면 문서가
움직이고, 그 위에 얹힌 것들이 함께 밀린다. 지금은 두 장치가 그걸 부분적으로 가리고 있다:

| | 지금 |
|---|---|
| `.global-rail` | `position: sticky; top: 0` — 밀려도 제자리에 붙는다 |
| `.tm-sidebar` | `overflow-y: auto` — 자기 스크롤을 갖는다 |
| `OverlayScrollbar` | `document.scrollingElement`를 잰다 (`OverlayScrollbar.tsx:86`) |

마지막 줄이 이 결정의 무게다. **커스텀 스크롤바가 문서 스크롤러에 묶여 있고,
`e2e/overlayScrollbar.spec.ts`가 그 폭·위치·전환을 검사한다.**

### 1.0 그런데 이 리포는 이미 한 번 풀었다 [실측]

설계를 쓴 뒤에 찾았다. `03-planning.css`의 **매트릭스 페이지가 정확히 같은 구조**를 갖고 있다:

```css
@media (min-width: 901px) {
  .app-shell > main:has(.ff-matrix-page) { height: 100dvh; min-height: 0; overflow: hidden auto; }
  .app-shell > main:has(.ff-matrix-page) > .page-grid { height: 100%; min-height: 0; align-items: stretch; }
  .ff-matrix-page { height: 100%; min-height: 0; padding-bottom: 0; }
  .ff-matrix { min-height: 452px; }
}
```

그리고 그 파일이 **네 가지 교훈을 주석으로 적어놨다** — 발명하지 않고 그대로 따른다.

1. `main`은 `hidden`이 아니라 **`hidden auto`**다. 창이 최소 높이보다 짧을 수 있고, 그때
   무언가는 양보해야 한다. 양보하는 것이 그 스크롤바다.
2. **페이지 자체는 자르지 않는다.** 자르면 넘친 내용에 닿을 길이 사라진다.
3. **그리드에 바닥을 준다.** 없으면 창이 짧아질 때 상자가 머리만 남는다.
4. **`padding-bottom`을 걷는다.** 더 자랄 수 없는 페이지의 아래 여백은 빈칸이다.

경계도 그쪽이 그은 선(901)을 쓴다 — §4.3이 최소 폭으로 계산한 820에 가장 가까운 표준
브레이크포인트가 `--bp-md: 900`이고, 매트릭스가 같은 이유로 같은 자리에 그었다.

**이 절이 §1.1의 결론을 바꾸지는 않는다.** A(페이지 지역)가 맞고, 다만 A는 새 방식이 아니라
**이 리포에 이미 있는 방식**이다.

### 1.1 두 갈래

| | 무엇 | 범위 | 대가 |
|---|---|---|---|
| **A. 페이지 지역** | 포커스 페이지만 뷰포트 높이에 맞추고(`height: calc(100dvh - 헤더)`, `overflow: hidden`), 큐 목록에 자기 스크롤을 준다. 문서는 이 화면에서 스크롤할 것이 없어진다 | 이 페이지 | 다른 화면은 그대로. 화면마다 스크롤 모델이 달라진다 |
| **B. 셸 전역** | `.app-frame`을 `height: 100dvh; overflow: hidden`으로 고정하고 영역마다 스크롤러를 둔다 | 전 화면 | `OverlayScrollbar`가 문서가 아니라 각 영역을 재야 하고, 그 스펙도 바뀐다. `sticky` 레일이 불필요해진다 |

**추천: A.** 이유 셋 —

1. **요청이 이 화면의 것이다.** 전역 모델 전환은 시안 한 장이 요구하는 범위를 넘는다.
2. **B는 이미 작동하는 것을 흔든다.** 레일 `sticky`와 사이드바 `overflow-y`와 오버레이
   스크롤바는 지금 서로 맞물려 있고, 스펙이 그걸 지킨다.
3. **A가 B로 가는 길을 막지 않는다.** 페이지가 자기 높이를 책임지는 구조는 나중에 셸이
   고정될 때 그대로 살아남는다.

### 1.2 A를 고르면 지켜야 할 것

- **`100vh`가 아니라 `100dvh`.** 스킬의 Layout 규칙이 명시한다 — *"100vh는 모바일
  브라우저에서 문제가 된다."* 이 앱은 데스크톱이지만 Tauri 창도 크기가 변한다.
- **`overflow: hidden`을 함부로 걸지 않는다.** 같은 규칙이 *"숨긴 넘침이 중요한 내용을
  자른다"*고 적어놨다. 자르는 것은 **큐 목록 하나**이고, 그 안에는 스크롤러가 있다.
- **좁은 폭에서는 이 구조를 풀어야 한다.** 두 열이 한 열이 되는 순간 화면 높이 안에
  다 들어가지 않는다 → §4.3.

---

## 2. 시안이 요구하는 것 중, 이미 있는 것과 없는 것 [실측]

| 시안의 요소 | 지금 | 비고 |
|---|---|---|
| 스톱워치 / 포모도로 전환 | **있음** | `.focus-segments` · `.focus-mode` |
| 큰 시계 `00:00` | **있음** | `--focus-clock-display: clamp(66px, 7.2vw, 96px)` |
| 집중 시작 / 일시정지 / 종료 | **있음** | `.focus-main-actions` |
| 작업 메모 (0/500) | **있음** | `FocusPage.tsx`에 26회 등장 |
| 몰입 보기 · 미니 창 · 기록 | **있음** | `.focus-immersive` · 미니 창 · `.focus-records` |
| 완료한 작업 | **있음** | 기록 쪽 |
| **작업 큐** (체크박스 · 태그 · 소요시간 · 순서 손잡이) | **없음** | 지금은 작업을 **고르는** 피커(`.focus-candidates`)뿐이다 |
| **오늘의 세션 요약** (집중 시간 · 완료 · 평균) | **없음** | |
| **2열 배치** | **없음** | 지금은 `max-width: 640px` 가운데 한 열 |
| 레일 하단 "집중 중" 미니 패널 | **없음** | |

**즉 이 작업의 절반은 배치이고 절반은 새 기능이다.** 큐와 요약은 그릴 데이터가 아직 없다
(큐는 순서를 가진 목록이므로 저장이 필요하고, 요약은 세션 집계가 필요하다). → **I5**

---

## 3. 배치

```
┌ 레일 ┬ 헤더 ─────────────────────────────────────────────┐
│      ├───────────────────────────┬─────────────────────┤
│      │  현재 작업 · 타이머        │  작업 큐            │  ← 이 열만
│  집중│  (고정)                   │  ░░░ 스크롤 ░░░     │     흐른다
│  중  ├───────────────────────────┤                     │
│  패널│  작업 메모 (고정)          ├─────────────────────┤
│      │                           │  오늘의 세션 요약    │
└──────┴───────────────────────────┴─────────────────────┘
```

- **두 열, 고정 비율이 아니라 최소 폭.** 왼쪽은 시계가 주인이므로 줄어들면 안 되고,
  오른쪽은 목록이라 줄어들어도 읽힌다.
  `grid-template-columns: minmax(420px, 1.35fr) minmax(320px, 1fr)`
- **세로는 두 층.** 왼쪽은 타이머 카드 + 메모 카드, 오른쪽은 큐 + 요약.
- **큐만 `overflow-y: auto`.** 요약은 오른쪽 열 바닥에 고정이고 큐가 남는 높이를 먹는다
  (`grid-template-rows: minmax(0, 1fr) auto`).

`minmax(0, 1fr)`의 `0`이 핵심이다 — 그리드 항목의 기본 `min-height: auto`는 내용 높이를
바닥으로 삼아서, 그것을 0으로 내리지 않으면 **`overflow-y: auto`가 아무것도 하지 않는다.**

---

## 4. 스크롤 지역의 경계

### 4.1 고정과 흐름

| 영역 | |
|---|---|
| 페이지 셸 | **고정** — `height: calc(100dvh - var(--shell-header-h)); overflow: hidden` |
| 타이머 카드 · 메모 카드 | **고정** — 내용이 넘치면 메모의 textarea가 자기 스크롤을 갖는다 |
| 작업 큐 | **흐름** — `overflow-y: auto; overscroll-behavior: contain` |
| 세션 요약 | **고정** — 오른쪽 열 바닥 |

`overscroll-behavior: contain`이 없으면 큐를 끝까지 스크롤한 뒤의 휠이 문서로 넘어간다
— 다른 화면에서는 그게 스크롤 체이닝이지만 여기서는 **"전체 화면이 안 움직인다"는 약속을
깨는 것**이다.

### 4.2 오버레이 스크롤바와의 관계

`OverlayScrollbar`는 `element ?? document.scrollingElement`를 받는다 — **이미 대상 요소를
받을 수 있게 되어 있다.** 큐에 붙일지, 큐는 네이티브 스크롤바를 쓸지가 **I3**.

### 4.3 좁은 폭에서는 이 구조를 푼다 [추론]

두 열이 한 열이 되면 타이머 + 메모 + 큐 + 요약이 한 화면 높이에 들어가지 않는다.
그 지점부터는 **페이지 고정을 풀고 문서 스크롤로 되돌린다** — 지금 다른 화면들이 하는 그대로다.
경계는 `01-base.css`가 정해둔 5종 브레이크포인트 중 하나를 쓴다. → **I4**

---

## 5. 시안의 시각 언어는 우리 자와 다르다 [관찰]

시안 B는 **둥근 카드 · 부드러운 그림자 · 알약 칩 · 채도 높은 파랑**으로 그려져 있다.
그건 이 리포가 방금 떠나온 Bento Grids의 언어다(`SWISS_MINIMAL_DESIGN.md` §1.5).

**배치는 가져오고 시각 언어는 현재 자를 쓴다:**

| 시안 | 우리 자 |
|---|---|
| 카드 모서리 ~12px | `var(--radius-sm)` = 4px |
| 카드 그림자 | 없음 — 헤어라인(`--border-subtle`) |
| 알약 태그 칩 | `--radius-pill`은 남아 있으므로 그대로 (§I1의 예외) |
| 파랑 CTA | `var(--accent-fill)` (라이트 `#0064d2` · 다크 `#0071e3`) |
| 큰 시계 | §5.1 참조 |

### 5.1 시계는 자 밖에 있다

시안의 시계는 화면에서 가장 큰 글자이고, 현재 구현도 `clamp(66px, 7.2vw, 96px)`이다.
우리 표시 스케일의 꼭대기는 `--display-lg: 48px`이다.

§8.6이 `--display-*`를 만든 이유가 정확히 이것이었다 — *"6단에 얹으면 24px이 되어 이 앱의
이름값이 사라진다."* 시계는 그 논리의 극단이므로 **표시 스케일을 한 단 더 올리거나
(`--display-xl`), 시계 하나를 기록된 예외로 둔다.** → **I2**

---

## 6. 결정할 것

| # | 질문 | 선택지 | 기본 제안 |
|---|---|---|---|
| **I1** | 스크롤 모델 | A) 이 페이지만 고정 · B) 셸 전역 고정 | **A** — §1.1 |
| **I2** | 시계 크기 | A) `--display-xl` 신설(예: 72px) · B) `clamp()` 유지하고 예외로 기록 · C) `--display-lg`(48px)로 낮춤 | **B** — 시계는 뷰포트를 따라 자라야 하고, `clamp`는 그걸 하는 유일한 문법이다. 예외에 이유를 적는다 |
| **I3** | 큐의 스크롤바 | A) 네이티브 · B) `OverlayScrollbar`를 큐에 붙임 | **B** — 컴포넌트가 이미 대상 요소를 받는다. 앱 전체가 같은 스크롤바를 쓴다 |
| **I4** | 좁은 폭 경계 | 5종 브레이크포인트 중 하나 (§4.3) | 두 열이 각자의 최소 폭(420+320+간격)을 못 지키는 지점 |
| **I5** | 큐와 요약의 데이터 | A) 이번에 함께 만든다 · B) 배치만 먼저, 큐는 기존 피커를 옮겨 담고 요약은 뒤로 | **B** — 배치와 새 기능을 한 커밋에 섞으면 무엇이 깨졌는지 알 수 없다 |
| **I6** | 레일 하단 "집중 중" 패널 | A) 이번에 · B) 별도 | **B** — 레일은 전 화면 공통이라 포커스 화면의 범위가 아니다 |
| **I7** | 몰입 보기와의 관계 | 이 배치가 몰입 모드에서는 어떻게 되나 | 몰입은 지금처럼 타이머만 남긴다 — 큐는 집중을 깨는 것이므로 |

---

## 7. 단계

| Phase | 내용 | 검증 |
|---|---|---|
| **0** | I1~I7 답하기 | 코드 0줄 |
| **1** | 페이지를 뷰포트 높이에 맞추고 2열 그리드로. 기존 요소를 그 안에 재배치(새 기능 없음) | 문서 스크롤이 이 화면에서 사라지는지 [실측] |
| **2** | 큐를 기존 피커에서 옮겨 담고 `overflow-y: auto` + `overscroll-behavior: contain` | 큐 끝에서 휠이 문서로 새지 않는지 [실측] |
| **3** | 세션 요약 (집계 필요) | |
| **4** | 좁은 폭에서 고정이 풀리는지 · `scale.test.ts` 통과 · E2E | |

Phase 1과 2를 나누는 이유: **"전체가 안 움직인다"와 "목록이 움직인다"는 서로 다른 실패
모드를 갖는다.** 하나가 깨졌을 때 어느 쪽인지 알 수 있어야 한다.


---

## 8. Phase 1 결과 [실측]

`FocusPage.tsx`에 `.focus-layout` 래퍼 한 겹, `24-focus.css`에 미디어 블록 하나.
**새 기능은 없다**(I5-B).

### 8.1 요청이 실제로 성립하는지 재봤다

1280×800에서 후보 항목을 DOM으로 복제해 목록을 넘치게 만든 뒤:

```
목록:  scrollHeight 936 > clientHeight 580  →  스크롤 356px 이동
문서:  넘침 false · 목록을 스크롤할 때 같이 움직임 false
main:  넘침 false · 같이 움직임 false
```

**목록만 흐르고 전체 화면은 서 있다.**

### 8.2 배치

`grid-template-columns: minmax(420px, 1.35fr) minmax(320px, 1fr)` → 실측 508.97 / 377.03.
세션이 도는 동안에는 후보 목록이 렌더되지 않으므로(`FocusPage.tsx`의 조건)
`:not(:has(.focus-candidates))`로 한 열로 접고 타이머가 가운데를 되찾는다.

### 8.3 아직 시안이 아니다

| 시안 | 지금 |
|---|---|
| 오른쪽 = **작업 큐** (체크박스 · 순서 손잡이 · 태그 · 소요시간) | "바로 시작할 일" **3개짜리 짧은 목록** — 큐가 아니다 |
| 왼쪽 아래 작업 메모 카드 | 없음 (메모는 있지만 이 자리가 아니다) |
| 오른쪽 아래 오늘의 세션 요약 | 없음 |
| 카드 테두리 | 지금 두 열은 카드 크롬이 없다 |

**그래서 목록은 현재 절대 넘치지 않는다** — 3개는 어떤 창 높이에서도 580px를 채우지 못한다.
§8.1의 검증이 DOM 복제로 이루어진 이유이고, Phase 2가 진짜 큐를 그 자리에 넣기 전까지는
스크롤 지역이 **작동하지만 쓰이지 않는** 상태다.

### 8.4 검증

vitest 2,716 · tsc 0 · playwright **239 통과 / 0 실패** · `scale.test.ts` 통과
(새 CSS 블록이 자를 벗어나지 않는다).


---

## 9. Phase 2 — 집중 큐

오른쪽 영역이 "바로 시작할 일" 3개짜리 짧은 목록에서 **순서를 저장하는 큐**가 됐다.

### 9.1 저장 모델

`PlannerData.focusQueue: string[]` 하나. `Task.order`를 재사용하지 않는다(결정 1·2) —
목록의 정렬은 그 목록의 것이고 "다음에 무엇에 집중할까"는 다른 질문이라, 같은 필드에 두 질문을
담으면 리스트를 정렬할 때마다 집중 순서가 따라 흔들린다.

`checkItems`의 sortKey(분수 인덱싱) 방식을 따르지 않은 이유도 같은 자리에 있다: 그쪽이 푸는
문제 — 여러 클라이언트가 같은 목록에 동시에 끼워 넣는 것 — 가 여기엔 없다. 큐는 한 사람이
자기 앞에 놓는 순서다(결정 3).

**로컬 전용이다.** `buildSyncPlan`의 `collectionTables`는 행의 모음을 옮기는 목록이고 이것은
순서 하나이므로, 그 목록에 없으면 원격은 이 필드를 모른 채 그대로 돈다.

### 9.2 죽은 id는 읽을 때 거르고 쓸 때 턴다 (결정 9)

지우는 경로가 여럿이다 — `deleteTask` · `permanentlyDeleteTask` · `emptyTrash` · 동기화로
들어온 원격 삭제. 하나를 놓치면 큐가 유령을 그린다. 그래서 **경로를 고치는 대신** 큐를 읽는
곳에서 거르고(`visibleQueue`), 큐를 쓰는 모든 함수가 `compactQueue`를 통과하게 했다.
아직 없는 삭제 경로까지 자동으로 덮인다.

### 9.3 실측

| 확인 | 결과 |
|---|---|
| UI로 6개 담기 | 저장·화면 순서 일치, 완료된 Task는 후보에서 제외 |
| 결정 7 (중복 없음) | 피커가 이미 담긴 것을 아예 보여주지 않는다 |
| 결정 10 (드래그 유지) | 마지막 줄을 첫 자리로 끌고 **재실행 후에도 그 순서** |
| **완료 기준** | 큐 `sh 366 > ch 309`, 57px 스크롤 · 문서 0 · main 0 · **타이머 0** |

### 9.4 이 과정에서 낸 실수 둘

1. **이름을 바꾸고 선택자를 안 고쳤다.** 섹션을 `.focus-candidates` → `.focus-queue`로
   바꾸면서 접기 규칙 `:not(:has(.focus-candidates))`를 그대로 뒀다. 없는 클래스라 **항상
   매치**돼서 레이아웃이 늘 한 열로 접혔고, 스크롤러가 21px까지 눌렸다. 재보고서야 알았다 —
   `gridTemplateColumns`가 `"1102px"` 한 값이었다.
   같은 이름 변경으로 `.focus-candidates` 스타일 블록 10곳도 통째로 죽어 있었다.
2. **자동으로 오르던 목록에 기대던 스펙을 깨뜨렸다.** `e2e/focusDetail.spec.ts`가 Task를
   심고 `/focus`에서 그것이 보이기를 기대했는데, 결정 4로 큐는 사용자가 담는 것이 됐다.
   스토리지를 심는 대신 **스펙이 담는 단계를 밟게** 고쳤다 — 그게 실제 경로다.

### 9.5 하지 않은 것

결정대로 세션 요약(11)과 새 예상 시간 모델(12)은 없다. 큐 줄의 숫자는 기존 `actualSeconds`
그대로이고, "지금까지 집중한 시간"이라는 뜻을 `title`로 밝혔다.

### 9.6 검증

vitest **2,727 통과**(큐 도메인 11개 신규) · tsc 0 · playwright **239 통과 / 0 실패**


---

## 10. Phase 3 — 오늘의 세션 요약

### 10.1 차트가 아니다

`dataviz` 스킬의 첫 단계가 답을 준다 — *"때로는 답이 차트가 아니다(스탯 타일 또는 히어로
숫자)"*. 셋 다 하나의 숫자이고 비교할 기간도 시간축도 없다. 그릴 것이 없는데 그리면 장식이다.

같은 스킬의 규격 둘을 지켰다.

| 규격 | 여기서 |
|---|---|
| **히어로 숫자는 뷰당 하나** | 이 화면의 그것은 시계다(실측 92.16px). 타일 값은 `--type-xl`(18px)로 겨루지 않는다 |
| **큰 독립 숫자에 `tabular-nums`를 쓰지 않는다** — 칼럼 줄맞춤의 도구다 | 실측 `font-variant-numeric: normal` |
| 라벨은 문장형, 콜론 없음 | "집중 시간" · "완료한 작업" · "평균 집중 시간" |

`delta`와 `trend`(스파크라인)는 선택이고 넣지 않았다 — 비교할 기간이 없고, 그것을 만드는 것은
Phase 2의 결정 12가 거절한 "새 모델"이다.

### 10.2 집계는 새로 만들지 않았다

기록 화면이 이미 같은 셋을 계산한다 — `focusRecords`로 구간을 모으고 `ms > 0`인 것만 센다.
여기서는 **그 함수에 오늘 하루를 주는 것**이 전부다.

"완료한 작업"만 다른 곳에서 온다: 세션이 아니라 Task의 `completedAt`이고, 그것을 타임존의
하루로 접는 것은 `focusDate`가 이미 하는 일이다.

### 10.3 배치

오른쪽 열이 두 층이 됐다: `grid-template-rows: minmax(0, 1fr) auto` — 큐가 남는 전부를 먹고
요약이 바닥에 자기 높이로 선다. 실측 `519px / 87.5px`.

여기서도 `minmax(0, 1fr)`의 `0`이 없으면 큐가 자기 내용 높이로 부풀어 **요약을 화면 밖으로
밀어낸다.** 이 문서에서 세 번째로 같은 함정이다(§3 · §9 · 여기).

### 10.4 실측 — 실제 세션으로

19초짜리 집중을 시작하고 끝내니 타일이 **Focused 19s · Tasks done 1 · Average session 19s**로
갱신됐다. 큐 6줄은 세션 내내 그대로였다(기존 동작 유지 확인).

### 10.5 검증

vitest 2,727 · tsc 0 · playwright **239 통과 / 0 실패**


---

## 11. 작업 메모 — 다이얼로그에서 자리로

### 11.1 저장 모델을 만들지 않았다

메모는 예전부터 **세션의 것**이다(`FocusSession.focusNote`). 이 카드는 그 값을 다이얼로그
대신 자리에서 편집할 뿐이다. 시안의 "이번 세션" 드롭다운이 가리키는 것과 같은 것이다.

대상은 다이얼로그를 열던 세 진입점과 같은 규칙이다: **지금 도는 세션, 없으면 방금 끝난 것.**
둘 다 없으면 적을 곳이 없으므로 입력을 잠그고 그 이유를 적는다("집중을 시작하면 적을 수
있어요") — 눌리지 않는 빈 상자는 고장과 구별되지 않는다.

다이얼로그는 **남긴다.** 기록 줄에서 지난 세션의 메모를 여는 길은 이 카드가 아니다.

### 11.2 500은 입력의 상한이지 저장의 규칙이 아니다

시안의 `0 / 500`을 `maxLength`로 받되 정규화나 엔진의 검증은 건드리지 않았다. 조이면 이 카드가
생기기 전에 쓰인 긴 메모가 갈 곳을 잃는다.

세는 숫자에는 `aria-hidden`을 걸었다 — `maxLength`가 보조기술에 같은 사실을 이미 말한다.

### 11.3 실측

세션을 시작하니 잠금이 풀리고 "이번 세션"으로 바뀌었고, 21자를 치니 `21 / 500`과 함께
**활성 세션의 `focusNote`에 저장**됐다. 끝내니 "방금 끝난 세션"으로 바뀌며 값이 남았다.

왼쪽 열도 오른쪽과 같은 두 층이 됐다(`minmax(0, 1fr) auto`). 그래서 **두 열의 바닥선이
776px로 정확히 맞는다** [실측].

> 검증 중에 내 테스트가 한 번 거짓 경보를 냈다. `focusSessions.at(-1)`로 "마지막 세션"을
> 짚었는데 이 배열은 **최신순**이라 옛 세션을 읽고 있었다. 코드가 아니라 단언이 틀렸다.

---

## 12. Phase 4 — 마무리 검증 [실측]

| 확인 | 결과 |
|---|---|
| **900 아래에서 고정이 풀리는가** (§4.3 · I4) | 860px에서 한 열(`682px`), `main` 높이 자유(1534px), 큐 `overflow: visible`, 문서 스크롤 복귀 — 전부 설계대로 |
| **몰입에서 큐·요약·메모가 사라지는가** (I7) | 셋 다 사라지고 한 열(`1216px`)에 타이머만 남는다 |
| 두 열의 바닥선 | 776px로 일치 |
| `scale.test.ts` | 통과 — Phase 1~3의 새 CSS 세 블록이 자를 벗어나지 않는다 |
| vitest / tsc / playwright | **2,727 · 0 · 239 통과 0 실패** |

### 12.1 아직 시안이 아닌 것

| 시안 | 지금 |
|---|---|
| 카드마다 테두리와 배경 | 네 영역은 헤어라인으로만 나뉜다 |
| 큐 줄의 태그 칩 · 체크박스 | 리스트 이름과 시간만 |
| 레일 하단 "집중 중" 패널 | 없음 — I6에서 별도 작업으로 뺐다 |
| 헤더의 검색 · 아바타 | 없음 |

배치와 스크롤과 데이터는 시안의 것이고, **표면 장식은 아직 아니다.** 그것을 넣는 일은
`SWISS_MINIMAL_DESIGN.md` §1.5가 떠나온 언어로 돌아가는 것이므로, 넣는다면 그 문서의
결정을 다시 여는 일이 된다.


---

## 13. I6을 보러 갔다가 버그를 찾았다

### 13.1 시안의 왼쪽 열은 이 앱의 레일이 아니다 [실측]

`.global-rail`의 **실측 폭은 50px**이고 아이콘 전용이다. 시안의 왼쪽 열은 로고와 글자 붙은
메뉴와 "집중 중" 패널(작업명 · `18:42` · 버튼 셋)이 들어간 **~200px 기둥**이다. 그것은 이 앱의
`레일(50px) + 컨텍스트 사이드바`를 합친 모양이고, `/focus`에는 그 사이드바가 아예 없다.

**50px 안에 작업명과 시간과 버튼 셋은 들어가지 않는다.** I6이 "레일은 전 화면 공통"이라며
뒤로 미뤄둔 이유가 여기서 구체가 됐다 — 이건 배치가 아니라 셸의 모양을 바꾸는 일이다.

### 13.2 그런데 그 패널의 일은 이미 있는 것이 하고 있었다

`GlobalFocusBar`(`components/GlobalFocusBar.tsx`)가 화면 아래 고정 바로 세션을 보여주고
일시정지 · 종료 · 포커스 열기를 준다. 시안의 패널은 **새 기능이 아니라 그것의 다른 자리**다.

### 13.3 다만 그것이 절반의 화면에서 사라지고 있었다 [실측]

```
/focus 에서 시작 → status "running" → SPA로 Tasks 이동
→ .foc-global-bar : DOM에 없음   (탭 제목은 "04:04 · Focus"로 계속 돌았다)
```

배제한 것들: 마운트 가드 없음 · `FocusBarContent` 조기 반환 없음 · `activeFocusSession`은
paused도 포함 · CSS 숨김 규칙 아님(요소가 숨겨진 게 아니라 없었다) · 콘솔 에러 없음.

**원인은 `App.tsx`의 렌더 분기였다.** 이 파일은 라우트에 따라 여러 갈래로 `return`하는데,
`GlobalFocusBar`는 그중 하나에만 있었다. Tasks 모듈이 `/today` · `/list/*` · 검색을 가져가는
분기(주석이 *"the module claims it too"*라고 적은 그 분기)에는 없었다.

**버그는 컴포넌트에 없었다.** `GlobalFocusBar`는 세션을 받으면 처음부터 그렸다. 틀린 것은
어느 분기가 그것을 그리느냐였고, 그래서 jsdom이 아니라 E2E가 답할 수 있는 종류였다 —
`focusDetail.spec.ts`가 같은 이유로 E2E인 것과 같다.

### 13.4 고친 방법과 가드

레일이 이미 쓰는 관용구를 따랐다 — 주석이 *"레일은 분기 위에서 한 번 만들어 어느 셸도
소유하지 않는다"*고 적어둔 그것. 바도 분기 위에서 한 번 만들고 두 분기가 같은 값을 그린다.

`e2e/globalFocusBar.spec.ts`를 새로 뒀다. **수정을 되돌리면 세 뷰포트에서 모두 실패하고,
되돌리면 통과한다** — 가드가 실제로 무언가를 잡는지 확인한 것이다.

### 13.5 곁가지로 본 것

전체 리로드 중에 도는 세션은 `paused`로 돌아온다. 복구 경로가 그렇게 설계된 것인지 버그인지는
이번에 판단하지 않았다 — SPA 이동에서는 `running`이 유지되므로 위 문제와는 별개다.
