# Calendar — Apple Calendar 실측 비율/레이아웃 분석

- 작성일: 2026-08-26
- 기준: `v0.19.2` (`6026071`), `main`
- 목적: 캘린더의 **비율과 레이아웃**을 Apple Calendar에 맞추기 전, 양쪽 숫자를 먼저 확정한다.
- 이 문서는 **측정만** 한다. 코드는 아직 건드리지 않았다.
- 관련: [CALENDAR_APPLE_DESIGN.md](CALENDAR_APPLE_DESIGN.md) — 색·형태 언어를 다룸(이미 반영됨). 이 문서는 **치수**만 다룬다.

---

## 0. 결론 세 줄

1. **시간 행 높이가 2.26배 크다.** Apple 42.5px / 현재 96px. 캘린더가 애플처럼 안 보이는 원인의 대부분이 이 한 숫자다.
2. **그리드선 위계가 뒤집혀 있다.** Apple은 가로(시간)선이 세로(요일)선보다 진하다. 현재는 반대다. 게다가 Apple에는 **30분 보조선이 없다.**
3. **시간 그리드 왼쪽 크롬이 502px다.** Apple은 사이드바 하나 200px. 1440px 창에서 35% vs 14%.

---

## 1. 측정 방법과 신뢰도

### Apple 쪽
`support.apple.com/guide/calendar/…/mac`(macOS Calendar 사용 설명서)에 실린 원본 스크린샷 3장을 내려받아 PNG를 직접 디코딩하고, 행/열 휘도 프로파일에서 그리드선 위치를 검출했다.

| 이미지 | 크기 | 뷰 | 상태 |
|---|---|---|---|
| `dc7fbe0c…` | 1144×734 | Day + 인스펙터 | 스케일 1.4× |
| `6c51314e…` | 814×522 | Week + 사이드바 | 스케일 1.0× |
| `3a27119a…` | 814×522 | Week, 사이드바 접힘 | 스케일 1.0× |

**스케일 보정**: macOS 신호등 버튼은 지름 12pt·중심 간격 20pt가 표준이다. 이걸 자로 썼다.
- 814px 이미지: 지름 12–13px, 간격 19.75px → **1.0×**
- 1144px 이미지: 지름 ~16px, 간격 28px → **1.4×**

**교차 검증**: 시간 행 높이가 세 이미지에서 42.6 / 42.5 / (60÷1.4=42.9) px로 나왔다. 서로 다른 뷰·다른 스케일에서 ±0.4px 안에 들어왔으므로 **42.5px는 신뢰할 수 있는 값**이다.

> 한 가지 확인된 사실: macOS Calendar의 시간 행 높이는 **창 크기와 무관하게 고정**이고, 대신 뷰가 스크롤된다. 요일 열 폭만 유동(`1fr`)이다.

### 현재 앱 쪽
`npm run dev`(127.0.0.1:5180) → 캘린더 주간 뷰 → 뷰포트 1440×900으로 고정 후 `getBoundingClientRect()` / `getComputedStyle()` 실측. CSS를 읽은 값이 아니라 **렌더된 값**이다.

---

## 2. Apple Calendar 실측치

### 2.1 창 골격 (Week, 814px 창)

```
┌─ 툴바(타이틀바 통합) ────────────────────────────  48px
├─ 타이틀 줄  "April 2025"          ‹ Today ›  ───  47px
├─ 요일 헤더  Sun 30 · Mon 31 · …  ─────────────  28px
├─ 종일 밴드  (콘텐츠만큼, 흰 배경) ────────────  57px  ← 가변
╞═ 굵은 구분선 ═══════════════════════════════  2–3px
└─ 시간 그리드 (스크롤) ─────────────────────  나머지
```

| 항목 | 값 |
|---|---|
| 사이드바 폭 | **200px** (macOS 기본값) |
| 시간 거터 폭 | **50px** (사이드바 펼침) / **52px** (접힘) |
| 요일 열 폭 | **80.5px** (@814, 사이드바 펼침) / **108.9px** (접힘) → 유동 `1fr` |
| 그리드 상단까지 크롬 | 185px |

### 2.2 시간 그리드 — 핵심

| 항목 | 값 |
|---|---|
| **1시간 행 높이** | **42.5px** |
| 30분 | 21.25px |
| 15분 | 10.6px |
| 24시간 전체 높이 | 1020px |
| 30분 보조선 | **없음** |
| 표시 범위 | 0–24시 전체 스크롤 (잘라내지 않음) |

### 2.3 그리드선 위계 (이게 은근히 중요하다)

| 선 | 색 | 배경 대비 알파 |
|---|---|---|
| 가로 = 정시선 | `#E0E0E0` on `#F9F9F9` | **≈ 0.10** |
| 세로 = 요일 구분선 | `#E8E8E8` on `#F9F9F9` | **≈ 0.068** |

**가로선이 세로선보다 1.5배 진하다.** 캘린더는 "시간 축"을 읽는 도구지 "요일 표"가 아니라는 뜻이다. 눈이 먼저 가로줄을 타고, 요일 경계는 배경으로 물러난다.

배경: 시간 그리드 `#F9F9F9`(아주 옅은 회색), 종일 밴드 `#FFFFFF`(흰색). 표면 대비는 거의 0에 가깝다 — 카드처럼 띄우지 않는다.

### 2.4 이벤트 블록

Week 뷰의 파란 블록을 픽셀 단위로 뜯은 값:

| 항목 | 값 |
|---|---|
| 가로 인셋 | **0** — 열 폭을 꽉 채운다 |
| 세로 인셋 | 위/아래 **2px** (30분 이벤트: 21.25px 슬롯에 17px 블록) |
| 좌측 액센트 바 | **2px**, 블록 왼쪽 끝에서 **2px 안쪽** (flush 아님) |
| 채움 | 캘린더 색 **16%** on white (`#3478F6` → `#DEEBFD`) |
| 텍스트 | 캘린더 색 **+ 검정 50:50** (`#3478F6` → `#163C7B`) |
| 그림자 | 없음 |
| 모서리 | ~2–3px (거의 각짐) |
| 제목 | ~11pt / semibold |
| 시간 줄 | ~9.5pt, 시계 글리프 + `9–10AM`, 블록이 충분히 높을 때만 표시 |

### 2.5 종일 밴드 / 헤더 / 마커

| 항목 | 값 |
|---|---|
| 종일 칩 높이 | **17px** (피치 19px = 칩 17 + 간격 2) |
| 종일 밴드 배경 | 흰색 |
| 요일 헤더 | **한 줄 인라인** — `Sun 30`, 요일 약어와 날짜가 같은 baseline |
| 요일 헤더 높이 | **28px**, 폰트 ~13px |
| 오늘 마커 | **22×22 원**, `#EB4C46`(시스템 레드), 날짜 숫자에만 |
| 타이틀 | `April`(bold) + ` 2025`(regular) — **한 줄에 두 무게**, 28px, 그리드 위 별도 줄 |
| 시간 라벨 | 숫자 ~13px + `AM/PM` 작은 캡, 우측 정렬, **정시선에 세로 중앙 정렬** |
| 현재 시각 | 거터 안의 빨간 알약 배지(`9:41`) + 얇은 빨간 선 |

---

## 3. 현재 앱 실측치 (1440×900)

```
전역 레일 50 │ 리스트 사이드바 232 │ 캘린더 사이드바 220 │ 그리드 826 │ 작업패널 48
└──────────── 그리드 왼쪽 크롬 합계 502px (창의 35%) ────────────┘
```

| 항목 | 값 | 출처 |
|---|---|---|
| 툴바 | 52px + gap 12 | `09-calendar-redesign.css:8` |
| 요일 헤더 | **68px** (2줄 스택) | `.gcal-col-head` padding `10px 4px 8px` |
| 오늘 마커 | **32×32**, `--accent`(파랑) | `09-…:171` |
| 종일 밴드 | 32px(min) / 104px(max) + 리사이저 7px | `02-calendar.css:593` |
| 시간 거터 | **56px** | `02-calendar.css:655` |
| 요일 열 폭 | 109.7px (유동) | 실측 |
| **1시간 행 높이** | **96px** | `src/utils/calendarTime.ts:8` `SLOT_HEIGHT` |
| 표시 범위 | 6–24시 (18시간) = 1728px | `calendarTime.ts:6-7` |
| 30분 보조선 | 있음(점선) | `.gcal-time-slot-half` |
| 가로 정시선 | `rgba(0,0,0,0.05)` | `--grid-line` |
| 세로 요일선 | `rgba(0,0,0,0.07)` | `--border-subtle` |
| 종일 칩 | 18px | `02-calendar.css:422` |
| 이벤트 채움 | 색 14% | `10-calendar-apple.css:16` |
| 이벤트 텍스트 | 색 74% + `--text-primary` | `10-calendar-apple.css:21` |
| 이벤트 좌측 바 | 3px, flush | `10-calendar-apple.css:20` |
| 이벤트 그림자 | 없음 ✅ | `10-calendar-apple.css:71` |

---

## 4. 갭 — 영향 큰 순서

### 🔴 G1. 시간 행 높이 96px → 애플은 42.5px

이 하나가 체감의 대부분이다. **하루 중 화면에 보이는 시간**으로 환산하면:

| 창 높이 | Apple | 현재 | 배수 |
|---|---|---|---|
| 900px | **16.8시간** | 6.7시간 | 2.5× |
| 522px | **7.9시간** | 2.8시간 | 2.8× |

1시간 셀의 종횡비도 따라 달라진다 — Apple 108.9×42.5 = **2.56:1**(가로로 누움), 현재 109.7×96 = **1.14:1**(거의 정사각형). 애플 캘린더가 "하루를 한눈에 보는 도구"로 읽히고 우리 것이 "블록 편집기"로 읽히는 이유다.

`SLOT_HEIGHT`는 `calendarTime.ts` 상수 하나이고 드래그/리사이즈 계산이 전부 여기서 파생되므로, **변경 비용은 낮다.**

### 🔴 G2. 그리드선 위계 역전 + 30분 보조선

- 현재: 세로(0.07) > 가로(0.05) → 요일 표처럼 보인다
- Apple: 가로(0.10) > 세로(0.068) → 시간 축이 먼저 읽힌다
- 30분 점선은 Apple에 **없다**. 행 높이를 42.5px로 줄이면 30분 = 21px이라 보조선까지 있으면 줄무늬가 된다.

### 🟠 G3. 그리드 왼쪽 크롬 502px

Apple은 200px 사이드바 하나. 우리는 전역 레일 50 + 리스트 사이드바 232 + 캘린더 사이드바 220. 캘린더 화면에서 리스트 사이드바가 꼭 필요한지가 쟁점 — 이건 **레이아웃이 아니라 IA 결정**이라 별도 판단이 필요하다.

### 🟠 G4. 요일 헤더 68px → 28px

2줄 스택(요일 위, 32px 날짜 원 아래)을 Apple은 1줄 인라인(`Sun 30`) 28px로 처리한다. 오늘 원도 32 → 22px. 헤더에서 **40px를 회수**할 수 있고, 그만큼 그리드가 늘어난다.

### 🟡 G5. 이벤트 블록 미세 치수

| 항목 | Apple | 현재 | 조치 |
|---|---|---|---|
| 좌측 바 | 2px, 2px 안쪽 | 3px, flush | 얇게 + 인셋 |
| 세로 인셋 | 2px | 없음 | 추가 |
| 채움 | 16% | 14% | 거의 맞음 |
| 텍스트 | 색+검정 50:50 | 색 74% + ink | Apple이 더 어둡다 |

### 🟢 G6. 이미 맞는 것 — 건드리지 말 것

- 종일 칩 18px (Apple 17px)
- 이벤트 블록 가로 full-width (`width: 100%`)
- 이벤트 그림자 제거됨
- 시간 거터 56px (Apple 50–52px, 오차 범위)
- 요일 열 유동 `1fr`

---

## 5. 숫자 뒤의 원리 (이걸 놓치면 숫자만 맞고 느낌은 안 맞는다)

1. **행 높이는 고정, 열 폭은 유동.** Apple은 창을 키워도 시간 밀도를 바꾸지 않는다. 시간은 절대량이고 요일 폭은 남는 공간의 분배다.
2. **시간 축이 요일 축을 이긴다.** 선 굵기·색으로 명시적 위계를 만든다.
3. **격자는 조용하고 이벤트가 말한다.** 30분선 없음, 그림자 없음, 표면 대비 없음, 배경은 흰색과 `#F9F9F9` 사이.
4. **이벤트는 슬롯을 꽉 채운다.** 가로 인셋 0. "이 시간대는 점유됐다"를 면적으로 말한다. 세로 2px 인셋은 붙은 이벤트를 떼어놓기 위한 최소치일 뿐이다.
5. **크롬은 얇고 딱 한 겹.** 사이드바 하나, 헤더 한 줄, 타이틀 한 줄. 시간 그리드에 픽셀을 몰아준다.

---

## 6. 다음 단계 제안

작업은 **크롬 → 격자 → 블록** 순서가 안전하다. 위에서부터 회수한 픽셀이 아래 단계의 판단 근거가 되기 때문이다.

**Phase 1 — 격자 비율 (효과 대비 가장 쌈)**
- `SLOT_HEIGHT` 96 → **48px** 검토
  - Apple 정확값은 42.5px지만, 48px는 15분=12px·30분=24px로 전부 정수라 스냅/드래그 계산이 깔끔하다. Apple보다 13% 높지만 현재보다 2배 조밀하다.
  - 42.5px 그대로 갈지, 48px로 타협할지는 **결정 필요**.
- 30분 점선 제거
- `--grid-line` 0.05 → 0.10, 세로선 0.07 → 0.065 (위계 역전 해소)
- 표시 범위 6–24시 → 0–24시 전체 스크롤 검토

**Phase 2 — 헤더 크롬**
- 요일 헤더 2줄 → 1줄 인라인, 68px → 28px
- 오늘 원 32 → 22px
- 타이틀을 툴바 밖 별도 줄로 (`April 2025` bold+regular 2무게)

**Phase 3 — 블록 미세 조정**
- 좌측 바 3px → 2px + 2px 인셋
- 세로 2px 인셋
- 채움 14% → 16%, 텍스트를 더 어둡게

**Phase 4 — IA (별도 논의)**
- 캘린더 화면에서 리스트 사이드바를 접을지

---

## 7. 아직 검증 못 한 것

- **다크 모드 실측치 없음.** Apple 다크에서 그리드선/채움 알파가 어떻게 바뀌는지는 위 스크린샷(전부 라이트)으로 알 수 없다.
- **Month / Year 뷰 치수** 미측정. 이 문서는 Week/Day만 다룬다.
- **폰트 크기**는 글리프 높이 역산이라 ±1px 오차가 있다. 행 높이·열 폭 같은 기하값보다 신뢰도가 낮다.
- 스크린샷이 Apple 문서용 캡처라 **기본 설정 상태**라고 가정했다. 사용자가 바꿀 수 있는 설정이 섞여 있을 가능성은 낮지만 0은 아니다.
