# Calendar — Apple Calendar 실측 비율/레이아웃 분석

- 작성일: 2026-08-26 (1차) / 2026-08-26 (2차 재측정)
- 기준: 1차 `v0.19.2` (`6026071`) · 2차 `3416a09`, `main`
- **2차에서 정정된 것**: 시간 행 42.5 → 42.8px · 그리드 배경(주말/평일 분리) ·
  선 위계(역전이 아니라 Apple은 가로·세로가 같음) · 왼쪽 크롬 502 → 566px
- **2차 추가**: 사이드바 미니 캘린더(§2.6) · 숫자와 타입 스케일(§2.7) · Month/Year(§7) · 다크(§8)
- 목적: 캘린더의 **비율과 레이아웃**을 Apple Calendar에 맞추기 전, 양쪽 숫자를 먼저 확정한다.
- 이 문서는 **측정만** 한다. 코드는 아직 건드리지 않았다.
- 관련: [CALENDAR_APPLE_DESIGN.md](CALENDAR_APPLE_DESIGN.md) — 색·형태 언어를 다룸(이미 반영됨). 이 문서는 **치수**만 다룬다.

---

## 0. 결론 세 줄

1. **시간 행 높이가 2.24배 크다.** Apple 42.8px / 현재 96px. 캘린더가 애플처럼 안 보이는 원인의 대부분이 이 한 숫자다.
2. **가로선이 세로선보다 너무 옅다.** Apple은 가로·세로를 **같은 굵기**(α 0.075)로 긋는다.
   현재는 가로 0.05 / 세로 0.08로 세로가 1.6배 진하다. 그리고 Apple에는 **30분 보조선이 없다.**
3. **시간 그리드 왼쪽 크롬이 566px다.** Apple은 사이드바 하나 200px. 1440px 창에서 39% vs 14%.

> 2차 측정에서 1차의 "가로선이 세로선보다 1.5배 진하다"는 **사실이 아님**이 확인됐다.
> 아래 §2.3에 근거를 적었다. 고칠 방향은 "가로를 세로보다 진하게"가 아니라 **"둘을 같게"**다.

---

## 1. 측정 방법과 신뢰도

### Apple 쪽
`support.apple.com/guide/calendar/…/mac`(macOS Calendar 사용 설명서, macOS Tahoe)에 실린 원본
스크린샷을 내려받아 PNG를 직접 디코딩하고, 행/열 휘도 프로파일에서 그리드선 위치를 검출했다.

2차에서는 가이드의 **전체 이미지를 훑었다** — 문서에 등장하는 44장을 모두 받아 창 크기별로 분류했다.
시간 그리드가 보이는 것은 아래 다섯 장뿐이고, 나머지는 아이콘·팝오버·Info 창이다.

| 이미지 | 크기 | 뷰 | 스케일 | 쓸모 |
|---|---|---|---|---|
| `6c51314e…` | 814×522 | Week + 사이드바 | **1.00×** | **기준 캡처** — 색·간격 모두 여기서 읽었다 |
| `5735fcc7…` | 1146×910 | Week + Focus 필터 | 1.40× | 행 높이 교차 검증 |
| `dc7fbe0c…` | 1144×734 | Day + 인스펙터 | 1.40× | 행 높이 교차 검증 |
| `9e004a58…` | 1144×826 | Day + 미리알림 | 1.40× | 행 높이 교차 검증 |
| `3a27119a…` | 814×522 | Week + 계정 시트 | 1.00× | **기하값만** — 아래 경고 참고 |

> ⚠️ `3a27119a`는 계정 선택 **시트가 떠서 배경이 dim 처리된** 캡처다. 창 크기와 행 간격은
> 유효하지만 **색·알파를 여기서 읽으면 전부 어두운 쪽으로 치우친다.** 1차의 선 색 수치가
> 이 캡처에서 나왔을 가능성이 높고, §2.3의 정정이 그 때문이다.

**스케일 보정**: macOS 신호등 버튼은 지름 12pt·중심 간격 20pt가 표준이다. 이걸 자로 썼다.
채도(max−min RGB)로 세 버튼을 검출해 중심 간격을 재는 방식으로 자동화했다.

| 이미지 | 신호등 간격 | 지름 | 스케일 |
|---|---|---|---|
| `6c51314e` | 19.96px | 12.46px | **0.998×** |
| `5735fcc7` | 28.04px | 12.7px(÷scale) | **1.402×** |
| `dc7fbe0c` | 28.02px | 13.4px(÷scale) | **1.401×** |
| `9e004a58` | 28.02px | 13.3px(÷scale) | **1.401×** |

**교차 검증**: 시간 행 높이가 네 캡처에서 42.75 / 42.81 / 42.82 / 42.8px로 나왔다.
서로 다른 뷰·두 가지 스케일에서 ±0.05px 안에 들어왔다 → **42.8px**.
(1차의 42.5px는 선 중심 검출 대신 라벨 위치를 쓴 오차로 보인다. 0.3px 차이는
24시간 전체로 누적하면 7px이므로 실무상 결론은 바뀌지 않는다.)

**"행 높이가 창 크기와 무관하게 고정"의 근거** — 1차는 이것을 단정했지만, 근거를 적을 수 없었다.
Apple 가이드는 `Show __ hours at a time`이 **6~24시간 사이의 설정값**이라고 명시하고,
받은 캡처는 전부 논리 크기 ~814×522(1144÷1.4 = 817)로 **사실상 같은 창**이라 창 크기를
바꿔가며 비교할 수가 없다. 대신 이렇게 확인했다:

- 기준 캡처에서 그리드 콘텐츠 상단은 y=185, 첫 정시선은 y=214다. 간격이 **29px = 0.68행**이다.
- 창 높이에 N시간을 맞춰 넣는 레이아웃이라면 뷰포트 상단이 정시 경계에 붙고 스크롤이 없다.
  **행 중간에서 시작한다는 것 자체가 스크롤 상태**라는 뜻이다.

즉 42.8px는 **기본 zoom에서의 고정 행 높이**이고, 창을 키우면 더 많은 시간이 보인다.
설정을 바꾸면 이 값이 달라진다는 것은 남는 사실이다 — 42.8px는 불변량이 아니라 기본값이다.

> ⚠️ **캡처는 Display P3다.** 오늘 마커의 코어 픽셀이 `(235,75,70)`으로 읽히는데,
> P3 → sRGB로 변환하면 `#FF373C`이고 이는 Apple이 공표한 systemRed 라이트
> `#FF383C`와 1 이내로 일치한다. 즉 **이 문서의 색 값 중 채도가 있는 것**
> (§2.4의 이벤트 채움·글자, 오늘 마커)은 **변환 전 P3 수치**이므로 그대로 쓰면 안 된다.
>
> **회색과 기하값은 영향이 없다.** 중성축은 두 색공간에서 같아서, 변환해도
> 236 → 236, 249 → 249로 그대로다. §2.3의 선 알파·배경과 모든 치수는 유효하다.

### 현재 앱 쪽
`npm run dev`(127.0.0.1:5180) → 캘린더 주간 뷰 → 뷰포트 1440×900으로 고정 후 `getBoundingClientRect()` / `getComputedStyle()` 실측. CSS를 읽은 값이 아니라 **렌더된 값**이다.

---

## 2. Apple Calendar 실측치

### 2.1 창 골격 (Week, 814px 창)

```
┌─ 툴바(타이틀바 통합) ────────────────────────────  48px
├─ 타이틀 줄  "April 2025"          ‹ Today ›  ───  47px
├─ 요일 헤더  Sun 30 · Mon 31 · …  ─────────────  28px
├─ 종일 밴드  (콘텐츠만큼, 흰 배경) ────────────  57px  ← 가변
╞═ 굵은 구분선 ═══════════════════════════════  2–3px
└─ 시간 그리드 (스크롤) ─────────────────────  나머지
```

| 항목 | 값 |
|---|---|
| 사이드바 폭 | **200px** (macOS 기본값) |
| 시간 거터 폭 | **50px** (사이드바 펼침) / **52px** (접힘) |
| 요일 열 폭 | **80.5px** (@814, 사이드바 펼침) / **108.9px** (접힘) → 유동 `1fr` |
| 그리드 상단까지 크롬 | 185px |

### 2.2 시간 그리드 — 핵심

| 항목 | 값 |
|---|---|
| **1시간 행 높이** | **42.8px** (4개 캡처 · 2개 스케일, ±0.05) |
| 30분 | 21.4px |
| 15분 | 10.7px |
| 24시간 전체 높이 | 1027px |
| 30분 보조선 | **없음** |
| 표시 범위 | 0–24시 전체 스크롤 (잘라내지 않음) |

### 2.3 그리드선 — 1차 결론을 뒤집은 곳

**정정: Apple은 가로와 세로를 같은 굵기로 긋는다.** 1차의 "가로가 세로보다 1.5배 진하다"는
성립하지 않는다. dim되지 않은 기준 캡처(`6c51314e`, 1.00×)에서 이벤트가 걸리지 않은
빈 셀만 골라 전수로 쟀다.

| 선 | 측정 위치 | 픽셀값 | 알파(검정 기준) |
|---|---|---|---|
| 가로 = 정시선 | 흰 평일 열 (y=214·427·470·513) | 236 on 255 | **0.0745** |
| 가로 = 정시선 | 회색 주말 열 (y=427) | 230 on 249 | **0.0763** |
| 세로 = 요일 구분선 | 흰 열 사이 (x=411·572·652) | 236 on 255 | **0.0745** |

두 가지가 한꺼번에 나온다.

1. **같은 α 0.075다.** 가로·세로가 픽셀값까지 236으로 일치한다.
2. **고정색이 아니라 알파 합성이다.** 같은 선이 흰 열에서 236, 회색 열에서 230으로 읽힌다.
   고정 `#E0E0E0`이라면 배경과 무관하게 224였을 것이다. 1차가 적은 `#E0E0E0`/`#E8E8E8`은
   서로 다른 배경 위의 같은 선을 두 색으로 읽은 결과다.

**배경도 정정한다 — 그리드는 단색이 아니다.** 열마다 다르다.

| 열 | 배경 |
|---|---|
| 평일 (Mon–Fri) | `#FFFFFF` |
| 주말 (Sat·Sun) | `#F9F9F9` = 검정 α **0.024** |
| 시간 거터 | `#FBFBFB` |
| 사이드바 | `#F2F2F2` |

종일 밴드도 같은 규칙을 따른다 — 평일 칸은 흰색, 토요일 칸은 `#F9F9F9`다.
1차가 "시간 그리드 `#F9F9F9`"라고 적은 것은 **주말 열을 그리드 전체로 읽은 것**이다.

**30분 보조선은 확실히 없다.** 정시선 사이 42px 구간(y=440–461)이 전부 255로 균일하다.

**종일 밴드 아래 굵은 구분선**: y=182–184, 코어 217 on 255 → α ≈ 0.15, 실효 두께 2px.
정시선의 두 배 굵기다 — 여기만 위계가 있다.

### 2.4 이벤트 블록

Week 뷰의 파란 블록을 픽셀 단위로 뜯은 값:

| 항목 | 값 |
|---|---|
| 가로 인셋 | **0** — 열 폭을 꽉 채운다 |
| 세로 인셋 | 위/아래 **2px** (30분 이벤트: 21.25px 슬롯에 17px 블록) |
| 좌측 액센트 바 | **2px**, 블록 왼쪽 끝에서 **2px 안쪽** (flush 아님) |
| 채움 | 캘린더 색 **16%** on white (`#3478F6` → `#DEEBFD`) |
| 텍스트 | 캘린더 색 **+ 검정 50:50** (`#3478F6` → `#163C7B`) |
| 그림자 | 없음 |
| 모서리 | ~2–3px (거의 각짐) |
| 제목 | ~11pt / semibold |
| 시간 줄 | ~9.5pt, 시계 글리프 + `9–10AM`, 블록이 충분히 높을 때만 표시 |

### 2.5 종일 밴드 / 헤더 / 마커

| 항목 | 값 |
|---|---|
| 종일 칩 높이 | **17px** (피치 19px = 칩 17 + 간격 2) |
| 종일 밴드 배경 | 흰색 |
| 요일 헤더 | **한 줄 인라인** — `Sun 30`, 요일 약어와 날짜가 같은 baseline |
| 요일 헤더 높이 | **28px**, 폰트 ~13px |
| 오늘 마커 | **23×23 원**(면적 환산 22.3), 시스템 레드, 날짜 숫자에만 |
| 타이틀 | `April`(bold) + ` 2025`(regular) — **한 줄에 두 무게**, 28px, 그리드 위 별도 줄 |
| 시간 라벨 | 숫자 ~13px + `AM/PM` 작은 캡, 우측 정렬, **정시선에 세로 중앙 정렬** |
| 현재 시각 | 거터 안의 빨간 알약 배지(`9:41`) + 얇은 빨간 선 |

### 2.6 사이드바 미니 캘린더 (2차 추가)

기준 캡처(`6c51314e`, 1.00×)의 사이드바 하단 미니 월. 200px 사이드바 안에 들어 있다.

| 항목 | 값 |
|---|---|
| **열 피치** | **24.2px** (24.5 / 24.0 / 24.0 / 24.5 / 24.5 / 24.0) |
| **행 피치** | **20.6px** (6개 날짜 행) |
| 셀 종횡비 | **1.17 : 1** — 가로로 눕는다 |
| 그리드 폭 | 7 × 24.2 = 169px → 사이드바의 **85%**, 좌우 여백 각 ~15px |
| 그리드 높이 | 요일 행 + 6행 = **129px** (y=371..500) |
| 오늘 마커 | **16×16 원**, 시스템 레드, 숫자만 흰색 |
| 마커 / 행 피치 | **0.78** — 셀을 꽉 채우지 않는다 |
| 헤더 | `‹ April 2025 ›` 한 줄, y=346 |
| 헤더 → 요일 행 | 28px |

미니 캘린더도 Week 그리드와 같은 원리를 따른다 — **셀이 가로로 눕고**(1.17:1),
마커는 셀보다 작다. 정사각 셀에 꽉 찬 원을 그리면 다른 물건이 된다.

### 2.7 숫자 — 글리프 실측과 타입 스케일 (2차 추가)

**측정한 것은 cap height**(대문자/숫자의 실제 잉크 높이)다. 폰트 크기는 거기서 나눈 값이고,
SF Pro의 cap height ÷ em ≈ 0.70을 썼다. 즉 **cap height는 측정, 폰트 크기는 추정**이다.

| 자리 | cap height | 추정 폰트 | 비고 |
|---|---|---|---|
| 타이틀 `April 2025` | **20px** | ~28px | `April` bold + ` 2025` regular |
| Week 헤더 `Sun 30` | **10px** | ~14px | 요일·날짜가 한 줄, 같은 크기 |
| 시간 거터 숫자 `9` | **10px** | ~14px | 헤더와 같은 크기 |
| 거터 `AM` 캡 | **5px** | ~7px | 숫자의 **정확히 0.50** |
| 미니 헤더 `2025` | **9px** | ~13px | |
| 미니 요일 `S M T…` | **7px** | ~10px | |
| 미니 날짜 | **7px** | ~10px | 요일과 같은 크기 |

여기서 나오는 비율 셋:

```text
타이틀 : 본문 숫자   =  20 : 10  = 2.00
본문 숫자 : 미니 숫자 =  10 :  7  = 1.43
AM 캡 : 숫자         =   5 : 10  = 0.50
오늘 원 : 숫자 cap    = 22.3 : 10 = 2.23   (Week 헤더)
```

**단계가 세 개뿐이다** — 28 / 14 / 10. 그리고 타이틀이 본문 숫자의 정확히 두 배다.

### 2.8 공식 자료에서 가져온 것 (2차 추가)

스크린샷 말고 **Apple이 직접 공표한 값**을 쓸 수 있는 곳이 하나 있다. HIG 페이지는 JS로
그려져서 HTML에는 값이 없지만, DocC 기반이라 뒤에 JSON이 있다.

```text
https://developer.apple.com/tutorials/data/design/human-interface-guidelines/<page>.json
```

`typography` / `color` / `layout` / `sidebars` / `windows` / `toolbars`를 받아 확인한 결과:

| 찾던 것 | 공식 자료에 있나 |
|---|---|
| **macOS 텍스트 스타일 (크기 + 행간)** | **있다** — 아래 표 |
| 시스템 색 12종 라이트/다크 RGB | 있다 (스와치 이미지의 `alt`에 `R-255,G-56,B-60` 형식) |
| 사이드바 폭 · 툴바 높이 · 행 높이 | **없다** — 해당 페이지에 치수 표가 없고 개정 이력뿐이다 |
| separator / 격자선 알파 | **없다** — AppKit 의미론적 색은 이름만 있고 값이 없다 |

즉 **타이포는 공식값으로 대체할 수 있고, 레이아웃 치수는 여전히 스크린샷 실측이 유일한 근거다.**

**macOS built-in text styles** (공식):

| 스타일 | 크기 | 행간 | 행간/크기 |
|---|---|---|---|
| Large Title | 26 | 32 | 1.23 |
| Title 1 | 22 | 26 | 1.18 |
| Title 2 | 17 | 22 | 1.29 |
| Title 3 | 15 | 20 | 1.33 |
| Headline / Body | 13 | 16 | 1.23 |
| Callout | 12 | 15 | 1.25 |
| Subheadline | 11 | 14 | 1.27 |
| Footnote / Caption 1 / Caption 2 | 10 | 13 | 1.30 |

macOS 기본 본문은 **13pt**, 최소 **10pt**다(같은 문서).

§2.7의 cap height를 이 사다리에 대면 이렇게 읽힌다. cap/em ≈ 0.705를 쓰고,
안티에일리어싱 때문에 ±1px가 붙으므로 작은 쪽은 한 칸이 애매하다.

| 자리 | 측정 cap | 대응 스타일 |
|---|---|---|
| 타이틀 | 20px | Large Title 26 |
| Week 헤더 · 거터 숫자 | 10px | Title 3 15 (또는 Body 13) |
| 미니 헤더 | 9px | Body 13 |
| 미니 요일 · 날짜 | 7px | Footnote/Caption 10 |

### 2.9 행간 — 우리는 1.5 고정, macOS는 1.18–1.33

우리 캘린더의 `line-height`는 **모든 크기에서 정확히 1.5배**다.

| 요소 | 크기 / 행간 | 비율 |
|---|---|---|
| `.gcal-range-label` | 15 / 22.5 | 1.50 |
| `.gcal-col-date` | 16 / 24 | 1.50 |
| `.gcal-mini-month-head` | 13 / 19.5 | 1.50 |
| `.gcal-mini-day` | 11 / 16.5 | 1.50 |
| `.gcal-col-weekday` · `.gcal-time-label` | 10 / 15 | 1.50 |

macOS는 크기마다 다르고 전부 정수 pt다 — 큰 글자일수록 촘촘하고(1.18) 작은 글자일수록
넉넉하다(1.30). 우리는 그 곡선 없이 하나의 배수를 곱한다.

**결과적으로 우리 텍스트 줄은 macOS보다 매 단계 15~25% 높다.** 밀도가 중요한 격자에서
이건 누적된다 — 이것이 G4(헤더 68px)의 절반이다:

```text
현재 헤더 68px = 패딩 18 + 요일 줄 15 + gap 3 + 날짜 원 32
                        (10px 글자가 15px를 먹는다)
```

그리고 우리 크기 중 **사다리에 없는 값**이 셋 있다: `.gcal-col-date` 16px,
`.gcal-month-date` 11.2px, `.gcal-year-month-title` 16px. 16은 15와 17 사이,
11.2는 11과 12 사이다.

---

## 3. 현재 앱 실측치 (1440×900)

```
전역 레일 50 │ 작업 사이드바 248 │ 여백 32 │ 캘린더 사이드바 220 │ 여백 16 │ 그리드 778 │ 패널 48
└──────────────────── 그리드 왼쪽 크롬 합계 566px (창의 39%) ────────────────────┘
```

> **정정: 502px가 아니라 566px다.** 내역은 `.app-shell`의
> `grid-template-columns: 248px 1142px`에서 읽었다 — 작업 사이드바 컬럼은 **248px**이고,
> 그 뒤 캘린더 영역 안쪽 여백이 **32px**다. (2차 초안에서 이 둘을 합쳐 "사이드바 280px"이라고
> 적었는데, 합계는 같지만 귀속이 틀려서 고쳤다.)
>
> 1차의 232px는 248에서 화살표 키 한 칸(−16px) 줄인 값과 정확히 일치한다 —
> 사이드바를 손으로 좁혀둔 세션에서 나온 값으로 보인다. 2차는 `localStorage`가 빈
> 새 프로파일에서 쟀고 `focusflow-sidebar-width` 키가 없었으므로 248px가 기본값이다.
> 사이드바 폭은 저장되는 사용자 설정이므로 **이 숫자는 상수가 아니다.**

| 항목 | 값 | 출처 |
|---|---|---|
| 툴바 | 52px + gap 12 | `09-calendar-redesign.css:8` |
| 요일 헤더 | **68px** (2줄 스택) | `.gcal-col-head` padding `10px 4px 8px` |
| 오늘 마커 | **32×32**, `--accent`(파랑) | `09-…:171` |
| 종일 밴드 | 32px(min) / 104px(max) + 리사이저 7px | `02-calendar.css:593` |
| 시간 거터 | **56px** | `02-calendar.css:655` |
| 요일 열 폭 | 109.7px (유동) | 실측 |
| **1시간 행 높이** | **96px** | `src/utils/calendarTime.ts:8` `SLOT_HEIGHT` |
| 표시 범위 | 6–24시 (18시간) = 1728px | `calendarTime.ts:6-7` |
| 30분 보조선 | 있음(점선) | `.gcal-time-slot-half` |
| 가로 정시선 | `rgba(0,0,0,0.05)` | `--grid-line` |
| 세로 요일선 | `rgba(25,25,25,0.08)` | `--border-subtle` (1차의 0.07은 옛 값) |
| 30분 점선 | `rgba(0,0,0,0.04)` dashed | `--grid-line-half` |
| 주말 열 배경 | `rgba(0,0,0,0.02)` | `02-calendar.css:694` `.is-weekend` |
| 종일 칩 | 18px | `02-calendar.css:422` |
| 이벤트 채움 | 색 14% | `10-calendar-apple.css:16` |
| 이벤트 텍스트 | 색 74% + `--text-primary` | `10-calendar-apple.css:21` |
| 이벤트 좌측 바 | 3px, flush | `10-calendar-apple.css:20` |
| 이벤트 그림자 | 없음 ✅ | `10-calendar-apple.css:71` |
| 미니 캘린더 셀 | **29.7×29.7 + gap 2 → 피치 31.7px** | `.gcal-mini-day` |
| 미니 그리드 | 220×207.3 (사이드바 폭 100%) | `.gcal-mini-month-grid` |
| 미니 오늘 마커 | 29.7px 원 `#007AFF` | `.gcal-mini-day.is-today` |
| 미니 날짜 / 요일 / 헤더 폰트 | 11px / 10px / 13px | |
| Week 헤더 요일·날짜 | 10px(대문자) / 16px, 원 32px | `.gcal-col-weekday` · `.gcal-col-date` |
| 시간 거터 라벨 | 10px / 500 | `.gcal-time-label` |
| 타이틀(범위 라벨) | 15px / 600 | `.gcal-range-label` |

---

## 4. 갭 — 영향 큰 순서

### 🔴 G1. 시간 행 높이 96px → 애플은 42.8px

이 하나가 체감의 대부분이다. **하루 중 화면에 보이는 시간**으로 환산하면:

| 창 높이 | Apple (42.8) | 현재 (96) | 배수 |
|---|---|---|---|
| 900px | **16.7시간** | 6.69시간 | 2.50× |
| 700px | **12.0시간** | 4.60시간 | 2.61× |

> **2차가 한 번 잘못 고쳤다가 되돌린 자리다.** 2차 초안은 `.gcal-time-scroll`의
> 높이(750px)를 그리드 높이로 읽어 7.81시간이라고 적었다. 그런데 요일 헤더·종일
> 밴드·리사이저를 담은 `.gcal-timegrid-sticky`(108px)가 **그 스크롤러 안에** 있어서
> 그만큼을 가린다. 실제로 보이는 그리드는 750 − 108 = **642px**이고 642 ÷ 96 =
> **6.69시간** — 즉 **1차의 6.7시간이 맞았다.**
>
> 같은 착오에서 "우리 크롬이 Apple보다 얇다"가 나왔는데 이것도 틀렸다.
> 그리드 위에 쌓인 것은 우리 **258px**(창 900px 기준), Apple **185px**다.
> 우리가 73px 더 두껍다.

1시간 셀의 종횡비도 따라 달라진다 — Apple 108.9×42.8 = **2.54:1**(가로로 누움), 현재 103×96 = **1.07:1**(거의 정사각형). 애플 캘린더가 "하루를 한눈에 보는 도구"로 읽히고 우리 것이 "블록 편집기"로 읽히는 이유다.

`SLOT_HEIGHT`는 `calendarTime.ts` 상수 하나이고 드래그/리사이즈 계산이 전부 여기서 파생되므로, **변경 비용은 낮다.**

### 🔴 G2. 가로선이 너무 옅다 + 30분 보조선

1차는 이것을 "위계 역전"으로 적었으나, §2.3에서 Apple은 **위계를 두지 않는다**는 것이 확인됐다.
갭은 여전히 있지만 방향이 다르다.

| 선 | Apple | 현재 | 차이 |
|---|---|---|---|
| 가로 정시선 | α 0.075 | α 0.05 | **33% 옅다** |
| 세로 요일선 | α 0.075 | α 0.08 | 거의 맞음 |
| 30분 보조선 | 없음 | α 0.04 점선 | 없애야 함 |

지각 밝기(CIE L\*)로 환산하면 차이가 더 분명하다. 흰 배경 기준 ΔL\*:

| | Apple | 현재 |
|---|---|---|
| 가로 정시선 | **6.60** | 4.42 |
| 세로 요일선 | **6.60** | 7.09 |

즉 우리 격자는 **세로가 가로보다 1.6배 또렷하게** 보이고, Apple은 정확히 1.00배다.
"요일 표처럼 보인다"는 1차의 관찰 자체는 맞았고, 원인이 세로를 낮추는 데 있는 게 아니라
**가로를 Apple 수준으로 올리는 데** 있었다.

30분 점선은 Apple에 **없다**. 행 높이를 42.8px로 줄이면 30분 = 21px이라 보조선까지 있으면 줄무늬가 된다.

### 🟠 G3. 그리드 왼쪽 크롬 566px

Apple은 200px 사이드바 하나. 우리는 전역 레일 50 + 작업 사이드바 248 + 캘린더 사이드바 220
(+ 여백 32 + 16). 캘린더 화면에서 작업 사이드바가 꼭 필요한지가 쟁점 — 이건 **레이아웃이 아니라
IA 결정**이라 별도 판단이 필요하다. 1차보다 64px 더 나쁘다.

### 🟠 G4. 요일 헤더 68px → 28px

2줄 스택(요일 위, 32px 날짜 원 아래)을 Apple은 1줄 인라인(`Sun 30`) 28px로 처리한다. 오늘 원도 32 → 22px. 헤더에서 **40px를 회수**할 수 있고, 그만큼 그리드가 늘어난다.

내역은 §2.9에 있다 — 68px는 패딩 18 + 요일 줄 15 + gap 3 + 원 32이고, 그중 요일 줄 15px는
10px 글자에 **1.5 행간**을 곱한 값이다(macOS라면 13px). 즉 이 갭의 절반은 스택 구조이고
나머지 절반은 행간 정책이다.

### 🟡 G5. 이벤트 블록 미세 치수

| 항목 | Apple | 현재 | 조치 |
|---|---|---|---|
| 좌측 바 | 2px, 2px 안쪽 | 3px, flush | 얇게 + 인셋 |
| 세로 인셋 | 2px | 없음 | 추가 |
| 채움 | 16% | 14% | 거의 맞음 |
| 텍스트 | 색+검정 50:50 | 색 74% + ink | Apple이 더 어둡다 |

### 🟠 G7. 미니 캘린더가 두 배 크고, 타입 스케일이 뒤집혀 있다 (2차 추가)

**미니 캘린더 — 셀 면적이 2배다.**

| 항목 | Apple | 현재 | 배수 |
|---|---|---|---|
| 셀 피치 | 24.2 × 20.6 | 31.7 × 31.7 | 가로 1.31× · 세로 1.54× |
| 셀 면적 | 499px² | 1005px² | **2.01×** |
| 셀 종횡비 | 1.17 : 1 (눕는다) | 1.00 : 1 (정사각) | |
| 그리드 높이 | 129px | 207px | **1.61×** |
| 그리드 폭 / 사이드바 | 169/200 = **85%** | 220/220 = **100%** | 여백이 없다 |
| 오늘 마커 / 행 피치 | 16/20.6 = **0.78** | 29.7/31.7 = **0.94** | 셀을 꽉 채운다 |
| 날짜 글자 / 셀 높이 | 7/20.6 = **0.34** | ~7.7/31.7 = **0.24** | 셀만 크고 글자는 그대로 |

Week 그리드에서 본 것과 **같은 병**이다 — 셀이 크고, 정사각이고, 안의 것이 셀을 꽉 채운다.
마지막 줄이 특히 그렇다: 셀을 1.5배 키웠는데 숫자는 안 키워서, 숫자가 공간에 떠 있다.

**타입 스케일 — 타이틀이 날짜보다 작다.**

| 자리 | Apple | 현재 |
|---|---|---|
| 타이틀 | ~28px (cap 20) | **15px** |
| Week 헤더 날짜 | ~14px (cap 10) | **16px** |
| 타이틀 : 날짜 | **2.00** | **0.94** |

Apple은 타이틀이 날짜 숫자의 정확히 두 배인데, 우리는 **타이틀이 더 작다.**
화면에서 가장 큰 글자가 "8월 24일 – 8월 30일"이 아니라 날짜 칸의 숫자라는 뜻이고,
G4(헤더 68px)의 32px 오늘 원과 같은 뿌리다 — 헤더가 크고 타이틀이 작다.

오늘 원과 숫자의 관계도 다르다. Apple 22.3/10 = 2.23, 현재 32/11.5 = 2.78 —
우리 원이 숫자에 비해 더 크다. G4에서 원을 22px로 줄이면 이 비율도 같이 맞는다.

### 🟢 G6. 이미 맞는 것 — 건드리지 말 것

- 종일 칩 18px (Apple 17px)
- 이벤트 블록 가로 full-width (`width: 100%`)
- 이벤트 그림자 제거됨 — `09-calendar-redesign.css:216`이 그림자를 주지만
  `10-calendar-apple.css:71`이 뒤에 로드되며 `box-shadow: none`으로 덮는다. 확인함.
- 시간 거터 56px (Apple 50–52px, 오차 범위)
- 요일 열 유동 `1fr`
- **주말 열 음영** — 우리 `rgba(0,0,0,0.02)` vs Apple `#F9F9F9`(= α 0.024). 2차에서 새로 확인.
  Apple도 주말을 음영으로 구분하고, 값까지 거의 같다. 1차는 이 규칙 자체를 못 봤다.

---

## 5. 숫자 뒤의 원리 (이걸 놓치면 숫자만 맞고 느낌은 안 맞는다)

1. **행 높이는 고정, 열 폭은 유동.** Apple은 창을 키워도 시간 밀도를 바꾸지 않는다. 시간은 절대량이고 요일 폭은 남는 공간의 분배다.
2. **격자선에는 위계가 없다.** 가로도 세로도 α 0.075로 같다(§2.3). 위계는 딱 한 군데,
   종일 밴드 아래 굵은 구분선(α 0.15)뿐이다 — "여기서부터 시간 축"이라는 경계 하나만 말한다.
3. **격자는 조용하고 이벤트가 말한다.** 30분선 없음, 그림자 없음, 표면 대비 없음, 배경은 흰색과 `#F9F9F9` 사이.
4. **이벤트는 슬롯을 꽉 채운다.** 가로 인셋 0. "이 시간대는 점유됐다"를 면적으로 말한다. 세로 2px 인셋은 붙은 이벤트를 떼어놓기 위한 최소치일 뿐이다.
5. **크롬은 얇고 딱 한 겹.** 사이드바 하나, 헤더 한 줄, 타이틀 한 줄. 시간 그리드에 픽셀을 몰아준다.

---

## 6. 다음 단계 제안

작업은 **크롬 → 격자 → 블록** 순서가 안전하다. 위에서부터 회수한 픽셀이 아래 단계의 판단 근거가 되기 때문이다.

**Phase 1 — 격자 비율 (효과 대비 가장 쌈)**
- `SLOT_HEIGHT` 96 → **48px** 검토
  - Apple 정확값은 42.8px지만, 48px는 15분=12px·30분=24px로 전부 정수라 스냅/드래그 계산이 깔끔하다. Apple보다 12% 높지만 현재보다 2배 조밀하다.
  - **세 값을 실제로 렌더해 봤다**(아래 §6.1). 결정은 아직 안 났다.
- 30분 점선 제거
- `--grid-line` 0.05 → **0.075**, 세로선은 0.08 그대로 (Apple은 둘이 같다 · §2.3)
- 다크의 `--grid-line` 0.07 → **0.060** — 같은 값이 아니라 같은 **지각 무게**로 맞춘다(§8.3)
- 표시 범위 6–24시 → 0–24시 전체 스크롤 검토

**Phase 1.5 — Month가 창에 안 들어간다 (2차에서 발견)**
- 6주 × 128px = 768px > 뷰포트 752px → 1440×900에서 항상 스크롤(§7.2)
- Week와 반대로 Month는 행이 창에 맞춰 늘고 줄어야 한다는 쪽이 유력하지만,
  대볼 Apple 캡처가 없다(§7.1). 값을 정하기 전에 캡처부터 구하는 게 순서다

### 6.1 세 값을 실제로 렌더한 결과 (2차 추가)

`SLOT_HEIGHT`를 96 / 48 / 42.8로 바꿔가며 Playwright(chromium)로 1440×900을 렌더했다.
CSS로 흉내내지 않고 **상수를 실제로 바꿨다** — 드래그·리사이즈 계산이 전부 그 상수에서
나오므로, 값만 덮으면 그리드와 블록이 서로 다른 좌표계를 쓰게 된다. 렌더 후 원복했다.

| SLOT_HEIGHT | 슬롯 | 24시간 본문 | 900px 창에서 보이는 시간 |
|---|---|---|---|
| 96 (현재) | 96px | 1728px (6–24시) | 7.81시간 |
| 48 | 48px | 864px | 15.63시간 |
| 42.8 (Apple) | 42.8px | 770px | 17.52시간 |

눈으로 본 것 셋:

- **96 → 48의 차이가 크고, 48 → 42.8의 차이는 작다.** 96에서 14–20시 여섯 칸만 보이던 것이
  48에서 11–24시가 되고, 42.8은 거기서 09시가 더 들어오는 정도다.
- **42.8에서 30분 점선이 줄무늬가 된다.** 1차가 예측한 그대로다(§G2). 21px 간격으로 점선이
  깔리면 격자가 시끄러워진다. 어느 값을 고르든 30분선 제거가 먼저다.
- 10px 시간 라벨은 42.8px 행에서도 겹치지 않는다. 라벨 크기는 이 결정의 제약이 아니다.

정수 스냅(15분=12px·30분=24px)이 필요하면 48, 비율을 그대로 가져가려면 42.8이다.
**아직 고르지 않았다.**

**Phase 2 — 헤더 크롬**
- 요일 헤더 2줄 → 1줄 인라인, 68px → 28px
- 오늘 원 32 → 22px
- 타이틀을 툴바 밖 별도 줄로 (`April 2025` bold+regular 2무게)

**Phase 3 — 블록 미세 조정**
- 좌측 바 3px → 2px + 2px 인셋
- 세로 2px 인셋
- 채움 14% → 16%, 텍스트를 더 어둡게

**Phase 4 — IA (별도 논의)**
- 캘린더 화면에서 리스트 사이드바를 접을지

---

## 7. Month / Year 뷰 (2차 추가)

### 7.1 Apple 쪽 — 잴 수 있는 자료가 없다

macOS Calendar 사용 설명서에 실린 이미지 **44장을 전수로 받아 확인했다.** 창 캡처는 아홉 장이고
시간 그리드가 보이는 것은 다섯 장인데, **전부 Day 아니면 Week다.** Month·Year 뷰 캡처는 없다.
`icl1000`–`icl1050` 문서 번호를 훑어도, 뷰 전환을 설명하는 `icl1010`·`icl1002`에도 없다.

그래서 이 절의 Apple 열은 **비워 둔다.** 추정으로 채우면 이 문서의 나머지와 신뢰도가 섞인다.
닫으려면 macOS에서 Calendar를 Month/Year로 놓고 직접 캡처하는 수밖에 없다.

### 7.2 현재 앱 — Month (1440×900, 라이트)

| 항목 | 값 | 비고 |
|---|---|---|
| 그리드 | 752×769 | 7열 × 107.4px, 6행 × 128px |
| **행 높이** | **128px 고정** | `min-height: 128px`, `grid-template-rows` 6×128px |
| 뷰포트 | 752px | `.gcal-main` |
| **스크롤 높이** | **816px** | → **64px 넘침**. 1440×900에서 마지막 주가 잘린다 |
| 요일 스트립 | 15px / 폰트 13px | |
| 셀 패딩 | 6px | |
| 셀 경계 | `1px rgba(0,0,0,0.05)` 아래쪽만 | 세로 경계는 0px |
| 날짜 알약 | 24×22, radius full, 11.2px | |
| 오늘 | 셀 배경 흰색 + 알약 `#007AFF` 흰 글자 | |
| 이전/다음 달 칸 | 배경 `#F2F2F7` | `.is-outside` |

**여기서 나온 것 하나**: Month가 창에 안 들어간다. 6주 × 128px = 768px인데 뷰포트는 752px이라
**항상 스크롤**이다. 달력의 Month 뷰는 "한 달을 한 화면에서 본다"가 존재 이유인데,
1440×900이라는 평범한 창에서 그게 성립하지 않는다. Week의 `SLOT_HEIGHT`와 같은 병이다 —
행 높이가 고정이고 그 값이 크다.

Apple Week에서 확인된 원리("행 높이 고정, 열 폭 유동")는 **Week에 대한 것**이다. Month에서
같은 선택을 하면 스크롤되는 달력이 되므로, Month는 오히려 행이 창에 맞춰 늘고 줄어야 한다.
확인할 Apple 캡처가 없으므로 이건 측정이 아니라 **추론**이라고 적어 둔다.

### 7.3 현재 앱 — Year (1440×900, 라이트)

| 항목 | 값 |
|---|---|
| 그리드 | 800×646, padding 12px 16px, gap 16px(행) / 28px(열) |
| 열 수 | **4열** × 171px (3행 × 4열 = 12달) |
| 달 블록 | 171×197, 내부 gap 6px |
| 달 제목 | 16px / 600 / `#007AFF` |
| 요일 스트립 | 17px, 폰트 13px, 7×24px |
| 날짜 칸 | 24×22, 폰트 12px, radius full |
| 스크롤 | 750 ≤ 752 → **넘치지 않음** |

Year는 창에 들어간다. Month만 넘친다.

---

## 8. 다크 모드 (2차 추가)

### 8.1 Apple 쪽 — 여전히 없다

두 경로를 다 확인했다.

- **스크린샷**: 가이드 이미지 44장이 전부 라이트다. 다크 캡처가 한 장도 없다.
- **공표된 수치**: Apple이 macOS 시스템 색의 다크 RGBA를 숫자로 공개하는 문서를 찾지 못했다.
  HIG의 색 문서는 의미론적 이름(separator 등)을 주지 그 값을 주지 않는다.
  Calendar의 격자선이 어떤 시스템 색에서 오는지도 공표돼 있지 않다.

**결론: Apple 다크 값은 공개 자료로는 닫을 수 없다.** Mac에서 직접 캡처해야 한다.

### 8.2 현재 앱 — 양쪽 다 쟀다

`data-theme`를 바꿔가며 렌더된 값을 읽었다.

| 토큰 | 라이트 | 다크 |
|---|---|---|
| `--grid-line` (가로 정시선) | `rgba(0,0,0,0.05)` | `rgba(255,255,255,0.07)` |
| `--grid-line-half` (30분) | `rgba(0,0,0,0.04)` | `rgba(255,255,255,0.05)` |
| `--border-subtle` (세로 요일선) | `rgba(25,25,25,0.08)` | `rgba(255,255,255,0.08)` |
| 페이지 배경 | `#F2F2F7` | `#101012` |
| 표면 / 스티키 헤더 | `#FFFFFF` | `#1C1C1E` |
| 이벤트 채움 | 색 14% | 색 **30%** |
| 이벤트 글자 | 색 74% + ink | 색 **55%** + ink |

출처: `09-calendar-redesign.css:128–138`, `10-calendar-apple.css:14–29`.

### 8.3 다크가 라이트보다 격자가 무겁다

알파만 보면 라이트 0.05 / 다크 0.07이라 "조금 올렸네"로 읽히지만, 배경이 다르므로
지각 밝기로 환산해야 비교가 된다. ΔL\* (선 vs 자기 배경):

| 선 | 라이트 | 다크 | 다크/라이트 |
|---|---|---|---|
| 가로 정시선 | 4.42 | **7.68** | 1.74× |
| 세로 요일선 | 7.09 | **8.74** | 1.23× |
| 30분 점선 | 3.53 | 5.54 | 1.57× |
| *(참고) Apple 라이트* | *6.60* | *—* | |

**다크의 정시선은 라이트의 1.74배로 또렷하다.** 같은 격자가 테마에 따라 다른 무게로 보인다는
뜻이고, 어느 쪽도 Apple의 6.60에 맞지 않는다 — 라이트는 아래로, 다크는 위로 벗어나 있다.

두 테마를 Apple의 무게(ΔL\* 6.60)에 맞추려면:

```text
라이트  가로 0.05  → 0.075   (검정 on #FFF)
다크    가로 0.07  → 0.060   (흰색 on #1C1C1E)
```

이 두 숫자는 **측정이 아니라 위 측정값에서 푼 것**이다. 목표를 "Apple 라이트와 같은 지각 무게"로
잡았을 때의 답이고, 목표를 바꾸면 답도 바뀐다.

---

## 9. 아직 검증 못 한 것

2차에서 닫힌 것과 남은 것을 갈라 적는다.

**닫힌 것**

- 행 높이 42.8px — 4개 캡처 · 2개 스케일 교차 검증 (§1)
- "행 높이 고정" — 스크롤 오프셋으로 근거를 세움 (§1)
- 그리드선 알파와 배경 — dim되지 않은 기준 캡처에서 전수 측정 (§2.3)
- 우리 쪽 Month/Year 치수, 우리 쪽 다크 값 (§7.2·§7.3·§8.2)
- Apple 미니 캘린더 기하값과 글리프 cap height, 양쪽 타입 스케일 (§2.6·§2.7·G7)
- **macOS 텍스트 스타일과 시스템 색은 공식 자료로 확보**했다 — 추정이 아니다 (§2.8)
- 캡처가 P3라는 것과, 그것이 회색·기하값에는 영향이 없다는 것 (§1)

**남은 것**

- **Apple 다크 실측치.** 공개 스크린샷도, 공표된 숫자도 없다(§8.1). Mac에서 직접 캡처해야 한다.
  HIG가 주는 다크 값은 시스템 색 12종뿐이고, 격자선이 쓰는 separator 계열은 값이 없다(§2.8).
- **Apple의 레이아웃 치수(사이드바 폭·툴바 높이·행 높이)는 공식 자료에 없다**(§2.8).
  이 문서의 기하값이 스크린샷 실측인 것은 게을러서가 아니라 그것 말고 근거가 없기 때문이다.
- **§2.4·§2.5의 채도 있는 색 값은 P3 → sRGB 변환이 안 된 상태**다(§1의 경고).
- **Apple Month / Year 치수.** 가이드 이미지 44장에 한 장도 없다(§7.1). 같은 방법이 필요하다.
- **`Show __ hours at a time` 기본값.** 42.8px가 어느 설정값에 대응하는지 모른다.
  받은 캡처가 전부 같은 창 크기라 "창을 키우면 몇 시간이 보이는가"를 확인할 수 없었다.
  42.8px는 기본 zoom의 값이지 앱의 불변량이 아니다(§1).
- **폰트 크기**는 cap height ÷ 0.70으로 역산한 값이라 ±1px 오차가 있다(§2.7).
  cap height 자체는 측정값이므로, 비교는 폰트 크기보다 **cap height끼리** 하는 쪽이 안전하다.
- **Apple 미니 캘린더의 Month/Year 뷰 대응물**은 확인 못 했다. Day/Week 사이드바에만 있는지,
  Month 뷰에서도 같은 미니 월이 뜨는지는 캡처가 없어 모른다(§7.1과 같은 이유).
- 스크린샷이 Apple 문서용 캡처라 **기본 설정 상태**라고 가정했다. 사용자가 바꿀 수 있는 설정이 섞여 있을 가능성은 낮지만 0은 아니다.
- **`SLOT_HEIGHT` 42.8 vs 48 결정.** 두 값으로 각각 렌더해 나란히 비교하기로 했고, 아직 안 했다.
