# 2026-07-04 Dev Log

> 다른 환경(다른 계정/PC)에서 이어서 작업하는 경우 이 문서와
> [[../../DESKTOP_APP_DESIGN|DESKTOP_APP_DESIGN.md]](리포 루트)만 읽으면 맥락이 복원된다.

## 오늘 한 일 (커밋 순서대로)

### 1. Vercel 배포 실패 원인 규명 + 수정 — `912ece0 edit error`

- 증상: 로컬 변경이 배포에 반영 안 됨. 원인은 Vercel 빌드가 TS 타입 에러로 실패 → 이전 성공 빌드를 계속 서빙.
- 에러: `src/motion/variants.ts`의 `backdropVariants`/`modalVariants`에 타입 annotation이 없어
  `ease: [0.2, 0, 0, 1]` 배열이 `number[]`로 widen → framer-motion `Variants` 타입과 불일치.
- 수정: 두 객체에 `Variants` 타입 명시 (`import type { Variants } from "framer-motion"`).
- 교훈: 로컬 `npm run dev`는 타입체크를 안 함. 푸시 전 `npm run build`(= `tsc -b && vite build`) 필수.

### 2. 디자인 시스템 전면 적용 — `b783b65 change design`

레퍼런스: `C:\Users\minju\Downloads\Redesign FocusFlow Dashboard\` (Figma Make 산출물, iOS/HIG 스타일).
캘린더 한 화면이 아니라 공통 디자인 시스템을 추출해 전 화면에 적용했다. 전부 `src/styles.css`.

- 토큰(`:root`): 배경 `#F2F2F7`, 잉크 `#1C1C1E/#3C3C43/#8E8E93`, 보더 `rgba(0,0,0,0.07)`,
  라운드 8/10/12/16(기본 10), 그림자 `--shadow-{xs,sm,md,lg}` + `--shadow-accent`,
  이벤트 틴트 `--tint-{blue,purple,green,orange,red}-{bg,text,accent}`,
  본문 14px(기존 17px), Display 22/700, 라벨 10/700 uppercase. 다크모드/액센트 테마 대응.
- 공통 컴포넌트: `.ff-btn` 4종(h36·r10·13/600, primary는 accent 그림자), `.ff-segmented`,
  모달/배지/필, `.ds-label` 유틸.
- 화면별: 캘린더(툴바·주간그리드·사이드바·작업패널, 오늘 컬럼 틴트는 WeekView.tsx에 `is-today` 클래스 1줄 추가),
  글로벌 사이드바(액센트 틴트 액티브), Today(네이비 CTA→accent), 아이젠하워(틴트 사분면),
  페이지 타이틀 22/700 통일, `--spc-*` 팔레트 공용 토큰 연결, slate 하드코딩 색 제거.
- 방식: 기존 클래스/DOM 유지, styles.css 끝에 오버라이드 레이어 추가(기존 "IA refresh" 패턴 답습). 기능 변화 없음.

### 3. 아이젠하워 상세 패널 닫기 버그 수정 — `73fe4e8 fix bug`

- 증상: 목록 클릭 → X로 닫으면 패널 내용이 화면 하단에 잠깐 보임.
- 원인: X 클릭 시 `selectedTask`가 즉시 비워져 `.page-grid`가 단일 열(`no-detail`)로 붕괴하는데,
  패널은 AnimatePresence exit(0.42s) 동안 DOM에 남아 grid 두 번째 자식으로 하단에 흘러내림.
- 수정(styles.css): `.page-grid { position: relative }` +
  `.page-grid.no-detail > .detail-panel { position: absolute; top: 0; right: 0; width: 360px }`
  — exit 순간에만 매칭되어 제자리에서 슬라이드아웃. 같은 구조인 Archive 화면도 함께 해결.

### 4. 데스크톱 앱 설계 확정 — `e0a4460 add design plan`

- 리포 루트 `DESKTOP_APP_DESIGN.md`에 전체 설계 저장. 요지:
  - Tauri 2 선택 (Electron 아님 — 모바일 확장성 + 로컬 Ollama 직결 + 경량)
  - `src/platform/` 어댑터 계층으로 코어를 플랫폼 중립 유지
  - 배포: 지인 공유 규모, GitHub Releases + tauri updater, 코드 서명 생략
  - 기능 범위: 트레이 미니 타이머 + 시스템 알림 + Ollama 직결 (전역단축키·자동시작·오프라인은 후순위)
  - 로드맵 4단계: ①어댑터 도입 → ②Tauri 셸 → ③네이티브 통합 → ④CI 빌드+업데이터
- [[../Decisions/Architecture_Decisions|Architecture_Decisions]]에도 결정 기록 추가.

## 현재 상태

- branch: `codex/new_design` (Vercel이 이 브랜치를 자동 배포)
- `npm run build` 통과, 콘솔 에러 0
- 데스크톱 작업은 아직 시작 안 함 (설계만 확정)

## 다음 작업 (이어서 할 일)

1. 데스크톱 1단계: `src/platform/` 어댑터 도입 — DESKTOP_APP_DESIGN.md §2, §7 참고.
   Rust 설치 없이 가능. 완료 기준: 웹 동작/빌드 회귀 0.
2. 시작 전 확인: 사용자 PC에 Rust 툴체인 설치됐는지 (§8 준비물).
3. (미해결) 이 세션에서 preview 스크린샷 도구가 계속 타임아웃 — 기능 문제 아님, 검증은
   computed-style/a11y snapshot으로 대체했음.

## 실행 방법 (새 환경 셋업)

```
git clone https://github.com/minjunPark11/Todolist.git
cd Todolist && git checkout codex/new_design
npm install
npm run dev        # 개발 서버
npm run build      # tsc -b && vite build — 푸시 전 필수
```
