# 2026-07-02 Dev Log

## 오늘 한 일

- 현재 Git 상태 확인
- 최근 commit 10개 확인
- npm 설치/실행 상태 확인
- source tree와 주요 파일 구조 분석
- Calendar, Study, AI Assistant, Sidebar/navigation, 데이터 모델 파일 확인
- Obsidian Vault 문서 생성/업데이트

## Git 상태

명령:

- `git status`
- `git log --oneline --decorate -10`

결과:

- branch: `codex/new_design`
- remote tracking: `origin/codex/new_design`과 up to date
- untracked: `docs/`

최근 commit:

- `f8e9425 (HEAD -> codex/new_design, origin/codex/new_design) Fix production build failures from the i18n work`
- `78d78d5 Start new installs with a blank workspace instead of demo data`
- `cdd6172 Translate all UI chrome to the new i18n system`
- `be0ef15 Add Korean/English i18n infrastructure with a Settings language toggle`
- `703e26c (origin/claude/ollama-local-setup-6enkbr) Collapse calendar sidebar to a slim icon rail instead of hiding it`
- `eb34850 Reflect mini-calendar selected date in the main calendar view`
- `cae9c64 Fix calendar sidebar checkboxes stretched to full width`
- `c395e8c Fix calendar layout at laptop widths and all-day band`
- `a7a9584 Add comprehensive current-state feature/flow documentation`
- `0db6112 Fix stale docs: gateway fallback is remoteOllamaProvider, not server`

## 실행 확인

- `node_modules` 존재: `C:\Users\minju\Todolist\node_modules`
- `package-lock.json` 존재: `C:\Users\minju\Todolist\package-lock.json`
- `npm.cmd run typecheck`: 성공
- `npm.cmd run dev`: Vite ready, `http://127.0.0.1:5173/`
- `npm run build`: PowerShell script execution policy로 실패
- `npm.cmd run build`: Vite build 중 `dist/assets` mkdir EPERM으로 실패

## 최근 작업 추정

- 추정: 최근 commit 흐름상 i18n 작업이 크게 들어왔고 production build 이슈를 고친 직후 상태다.
- 추정: 첫 실행 시 demo data가 자동으로 들어가지 않고 blank workspace로 시작하도록 변경되었다.
- 추정: Calendar sidebar/mini calendar/layout 관련 UX 개선이 이어서 진행되었다.
- 추정: AI gateway fallback 문서/구현에서 remote Ollama provider 방향으로 정리 중이다.

## 구현된 기능

- 구현됨: Today, Inbox, Projects, Planning, Calendar, Study, Archive, Settings page
- 구현됨: Task detail, Task list, Quick add, global search
- 구현됨: Calendar month/week/day, layers, project filter, quick create, draft create, drag/drop schedule
- 구현됨: Study topics/notes/reviews와 spaced review queue
- 구현됨: Ollama chat, intent detection, compact app context, action preview/apply
- 구현됨: localStorage persistence, optional Supabase auth/sync, JSON import/export

## 미완성/개선 필요

- 개선 필요: production build output directory 권한/잠금 문제 확인
- 개선 필요: Study 데이터의 Supabase sync 포함 여부 결정
- 개선 필요: AI `serverProvider` 사용 여부 결정
- 개선 필요: `App.tsx` 책임 분리
- 개선 필요: Today bucket 계산 중복 후보 정리
- 개선 필요: 루트 문서와 Obsidian Vault 문서의 source of truth 정리

## 다음 리팩토링 후보

1. 빌드 실패 원인 확인: `C:\Users\minju\Todolist\dist` 권한/잠금/삭제 가능 여부
2. `App.tsx`에서 AI action executor와 page containers 분리
3. Study Supabase schema/save/load 추가 여부 결정
4. Calendar context를 실제 view state와 연결
5. `utils\planner.ts`의 Today bucket 계산을 단일 source로 사용

## 1차 App.tsx 리팩토링

- 구현됨: `C:\Users\minju\Todolist\src\App.tsx`의 page routing/renderPage 로직을 `C:\Users\minju\Todolist\src\app\AppPages.tsx`로 분리.
- 구현됨: tasks page filter/sort/group 상태를 `C:\Users\minju\Todolist\src\app\AppPages.tsx`로 이동.
- 구현됨: delete confirmation modal과 toast 렌더링을 `C:\Users\minju\Todolist\src\app\AppModals.tsx`로 분리.
- 구현됨: JSON export/import 로직을 `C:\Users\minju\Todolist\src\app\useDataPortability.ts`로 분리.
- 구현됨: AI agent action execution 로직을 `C:\Users\minju\Todolist\src\app\executeAgentActions.ts`로 분리.
- 유지됨: 기존 UI/기능 동작은 변경하지 않음.
- 검증됨: `npm.cmd run typecheck` 성공.
- 확인됨: `npm.cmd run build`는 TypeScript와 Vite transform 이후 `EPERM: operation not permitted, mkdir 'C:\Users\minju\Todolist\dist\assets'`로 실패. 리팩토링 코드 문제가 아니라 기존 build output 권한/잠금 문제로 구분.
