# 예상 시간과 블록 길이 — 한 숫자가 두 질문에 답하고 있었다

> 상태: **설계 · 미구현** · 2026-09-01
> (`TASK_DETAIL_PANEL_MERGE_DESIGN.md` §6.4가 `estimatedMinutes`와 `categoryId`를
> "여전히 어느 화면에도 없다"로 세어 두었다. 다시 재 보니 둘 다 그 문장이 맞지 않고,
> `estimatedMinutes` 쪽은 더 큰 것이 있었다.)
> 대상: `Task.estimatedMinutes` · `CalendarView.getTaskDuration` · 상세의 본문 구획

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | `CalendarView` · `usePlannerData.updateTaskSchedule` · `FocusQueue` · `CalendarRightTaskPanel` · `EventPopover` | [실측] |
| **인용** | `TICKTICK_TASK_DETAIL_SPEC.md` §2.12 · §5.17 · §5.18 · 리포의 `CALENDAR_DESIGN.md` §1.1 | [인용] |
| **추론** | 위 둘로 메운 것 | [추론] |

스크린샷은 필요 없었다. **이 문서가 묻는 것은 참조 앱이 무엇을 그리는가가 아니라 우리
한 필드가 몇 개의 질문에 답하고 있는가**이고, 그건 우리 소스만으로 답해진다.

---

## 1. §6.4의 문장은 두 항목 모두에 대해 틀렸다 [실측]

### 1.1 `categoryId`는 이미 편집된다

`EventPopover.tsx:240`의 `onChangeCategory`가 캘린더 이벤트 팝오버에서 카테고리를
바꾼다. "어느 화면에도 없다"는 이 필드에 대해서는 사실이 아니다.

### 1.2 `estimatedMinutes`도 이미 보인다

두 곳에서 **읽힌다**:

| 어디 | 무엇으로 |
|---|---|
| `FocusQueue.tsx:231` | 오늘 큐의 행에 `예상 N분` |
| `CalendarRightTaskPanel.tsx:176` | 오른쪽 패널의 작업 줄에 `· N분` |

그리고 한 곳에서 **쓰인다** — `CalendarView.tsx:568`, 블록을 리사이즈할 때.

즉 §6.4가 세었어야 할 것은 "화면이 없다"가 아니었다. 아래가 그것이다.

---

## 2. 진짜 문제 — 한 숫자, 두 질문 [실측]

`estimatedMinutes`는 지금 **서로 다른 두 질문**에 답한다.

| 질문 | 언제 필요한가 |
|---|---|
| A. **이 일은 얼마나 걸릴까** (예상 소요) | 시각이 있든 없든 |
| B. **이 블록은 몇 분짜리로 그릴까** (길이) | 그리드에 놓을 때 |

B는 시각이 있으면 **계산되는 값**이다. 명세가 그것을 두 번 못박는다 [인용]:

> §2.12 Duration Is Derived — Duration을 canonical persisted field로 중복 저장하지
> 않는다. `duration = endAt − startAt`. 따라서 기본적으로 `durationMinutes`는 저장하지
> 않는다.
>
> §5.17 Duration Task — End Time이 존재하면 Duration Task로 간주한다. …
> `durationMinutes`를 canonical field로 저장하지 않는다.

그리고 **우리 문서도 이미 같은 결론에 도달해 있었다** [인용: `CALENDAR_DESIGN.md:52`]:

> `estimatedMinutes`는 만들지 않는다. duration = endTime − startTime으로 파생.

코드는 반대로 갔다.

### 2.1 그래서 두 값이 어긋난다 [실측]

```
CalendarView.tsx:436  getTaskDuration
  1. task.estimatedMinutes > 0        ← 먼저 본다
  2. endTime − startTime
  3. 우선순위 기본값 (60 / 40 / 30)
```

| 자리 | 시각을 바꾸나 | 추정치를 바꾸나 |
|---|---|---|
| 캘린더 리사이즈 (`:568`) | O | **O** |
| 캘린더 블록 이동 (`handleMoveItem`) | O | X |
| 상세의 일정 피커 (`updateTaskSchedule`) | O | **X** |

세 번째 줄이 결함이다. 상세에서 종료 시각을 `10:30`에서 `11:30`으로 바꾸면 작업은
**한 시간짜리가 되지만** `estimatedMinutes`는 60인 채로 남고, 다음에 캘린더가 길이를
물으면 `getTaskDuration`이 **그 옛날 60을 실제 시각보다 우선한다.**

같은 질문에 답이 둘이고 하나만 최신이다. `TASK_DETAIL_PANEL_MERGE_DESIGN.md` §8이
`?task=`에서 없앤 것과 **정확히 같은 모양**이다 — 다만 그쪽은 "어느 작업이 열렸나"였고
이쪽은 "몇 분짜리인가"다.

### 2.2 그런데 지우기만 하면 잃는 것이 있다

명세대로 필드를 지우면 질문 A가 답을 잃는다. **아직 시각이 없는 작업**의 길이는 파생으로
답할 수 없고 — 뺄 것이 없다 — 남는 것은 우선순위 기본값(60/40/30)뿐이다.

`FocusQueue`가 그리는 `예상 30분`은 우선순위에서 나온 추측이 아니라 **사용자가 적은
판단**이다. 그것을 지우는 것은 §2.12가 요구하는 일이 아니다. 명세가 금지하는 것은
`durationMinutes` — **파생 가능한 것의 중복 저장**이고, "얼마나 걸릴 것 같은가"는
무엇에서도 파생되지 않는다.

---

## 3. 결정 — 의미를 갈라 둘 다 남긴다

### 3.1 두 이름, 두 규칙

| | 무엇 | 어디 산다 | 누가 쓴다 |
|---|---|---|---|
| **예상 소요** | 사용자의 판단. 시각과 무관 | `Task.estimatedMinutes` | 사용자만 |
| **블록 길이** | 시각의 결과 | **저장하지 않는다** | `endTime − startTime` |

### 3.2 `getTaskDuration`의 순서를 뒤집는다

```
1. endTime − startTime         ← 시각이 있으면 그것이 길이다 (§5.17)
2. estimatedMinutes > 0        ← 시각이 없을 때 사용자의 판단
3. 우선순위 기본값             ← 둘 다 없을 때의 추측
```

한 줄 순서를 바꾸는 것이지만, 이것이 §2.1의 결함을 없앤다. 시각이 있는 작업의 길이는
**언제나** 시각에서 나오므로, 시각을 바꾸는 자리 셋 중 어디를 쓰든 어긋날 값이 없다.

### 3.3 리사이즈는 추정치를 건드리지 않는다

`CalendarView.tsx:566`의 주석은 "블록 길이가 곧 새 예상 소요"라고 적고 있다 [실측].
그 문장을 버린다. 블록을 늘린 것은 **일정을 바꾼 것**이고, 그것이 "이 일이 더 오래
걸릴 것 같다"는 뜻인지 "이 시간대를 더 잡아두겠다"는 뜻인지 앱이 알 수 없다.

버리는 대신 **아무것도 하지 않는다** — 시각이 바뀌었고, §3.2에 따라 길이는 이미 시각을
따라간다.

### 3.4 상세에 예상 소요가 생긴다

`TICKTICK_DETAIL_ANATOMY_DESIGN.md` §2가 세운 규칙 그대로다 [인용]: **쓰이지 않는 것은
그리지 않고, `⋯`가 불러낸다.**

| | |
|---|---|
| 레지스트리 | `add` 그룹에 `addEstimate` 한 줄 (`actions.ts`의 넷 옆에 다섯째) |
| 본문 | `estimatedMinutes > 0`일 때만 구획 하나. `⋯ › 예상 시간`이 열고 캐럿을 그 안에 넣는다 |
| 비우면 | `0`이 되고 구획이 사라진다 — 태그·메모와 같은 규칙 |

새 컨트롤을 발명하지 않는다. 숫자 하나짜리 칸이고, `분` 단위다. **뽀모도로 개수로 세지
않는다** — 우리 포커스 엔진은 세션을 초로 재고(`actualSeconds`), 큐가 이미 `예상 N분`으로
그린다. 두 번째 단위를 들이면 §3.1이 막 없앤 종류의 중복이 다시 생긴다.

### 3.5 마이그레이션은 없다

지금 저장된 값 중 시각과 어긋나는 것들이 있다. **고치지 않는다.**

§3.1 이후 두 숫자는 **다른 질문의 답**이므로, 다른 것이 오류가 아니다. "10:30–11:30에
잡아 뒀지만 실제로는 30분이면 될 것 같다"는 완전히 말이 되는 상태이고, 마이그레이션은
그 상태를 사용자에게 묻지도 않고 없애는 일이 된다.

### 3.6 `categoryId`는 캘린더 전용으로 둔다 — 항목을 닫는다

§1.1대로 이미 편집된다. 상세에 넣지 않는 이유는 셋이다:

1. 참조 앱에 대응 개념이 **없다.** TickTick에는 캘린더 카테고리가 없으므로 배치를
   물어볼 근거가 없고, 근거 없이 짓지 않는다
2. 이것은 **작업의 속성이 아니라 캘린더의 표시 색**이다. 기본값이 프로젝트 카테고리 →
   기본 개인 카테고리로 떨어진다는 것이 그 증거다(`types.ts:98` 주석) — 작업 자신의
   속성이라면 상속받을 것이 없다
3. 상세에 넣으면 리스트·태그와 나란히 서서 **셋이 같은 종류로 보인다.** 앞의 둘은
   작업이 어디에 속하는가이고 이것은 어느 달력 층에 그려지는가다

§6.4의 이 항목은 **세는 것으로 끝낸다.**

---

## 4. 단계

| 단계 | 내용 | 검증 |
|---|---|---|
| **1** | `getTaskDuration`의 순서를 뒤집고, 리사이즈가 추정치를 쓰지 않게 한다 | 화면에 새로 생기는 것이 없다. 시각을 바꾸는 세 자리가 전부 같은 길이를 내는지 — 지금은 셋 중 둘이 어긋난다 |
| **2** | 레지스트리에 `addEstimate` + 상세의 예상 시간 구획 | §3.4. 1단계가 먼저인 이유는, 편집 칸을 먼저 주면 **어긋나는 값을 사용자가 직접 만들 수 있게** 되기 때문이다 |
| **3** | `CALENDAR_DESIGN.md` §1.1의 줄을 고친다 | "만들지 않는다"는 이미 만들어진 필드에 대해 틀린 문장이고, 3단계가 끝나면 **의미가 달라진 채로 남는다**는 것이 맞는 문장이 된다 |

1단계만으로도 값이 있다 — 화면은 그대로인데 두 값이 어긋나던 자리가 없어진다.

---

## 5. 열린 질문

| | 질문 | 언제 |
|---|---|---|
| Q1 | 시각이 있는 작업에서도 예상 소요를 **보여 줄까**. 보여 주면 "11:30까지 잡아 뒀지만 30분이면 된다"가 화면에 드러나고, 감추면 §3.5가 말이 되는 상태라고 한 것이 사용자에게는 안 보인다 | 2단계 |
| Q2 | `예상 N분`을 그리는 두 자리(`FocusQueue` · `CalendarRightTaskPanel`)가 같은 낱말을 쓰는가. 지금 i18n 키가 `todayv.estimate`와 `eis.minutes`로 **다르다** [실측] | 2단계 |
| Q3 | 포커스 세션이 끝났을 때 `actualSeconds`와 예상을 나란히 보여 줄 자리가 있는가. 예상을 적게 하는 이유의 절반이 그 비교인데, 지금은 비교가 어디에도 없다 | 2단계 이후 |
| Q4 | 반복 작업의 예상 소요는 발생마다 다른가. 지금은 필드가 작업에 하나이므로 전부 같다 | 열지 않음 |

---

## 6. 이 문서가 확인하지 않은 것

- MCP 투영(`server/data/projections.ts:157`)이 `estimatedMinutes`를 내보낸다 [실측].
  외부 AI가 그것을 "길이"로 읽는지 "예상"으로 읽는지는 이 문서 밖이고, §3.1이 이름의
  뜻을 좁히므로 그 설명도 한 번 봐야 한다
- `domain/view/item.ts:133`이 뷰 아이템에 같은 값을 싣지만, **읽는 곳을 찾지 못했다**
  [실측: `components/` 전체에서 간트·타임라인 쪽 참조 0]. 실어만 두고 아무도 안 보는
  값이면 §3이 끝난 뒤 지울 후보다 — 이 문서가 지우지는 않는다
