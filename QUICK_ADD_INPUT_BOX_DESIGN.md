# 퀵애드 — 한 줄이 태스크의 모든 것을 물어본다

> 상태: **설계 · 구현 완료(다섯 단계 전부)** · 2026-09-03
> (사용자가 참조 앱의 퀵애드를 여섯 장 캡처해 주었다: 우리 현재 · 휴지 상태 · `⌄` 메뉴 ·
> 노트 모드 · `Inbox` 하위 메뉴 · `Tags` 하위 메뉴.)
> 대상: `components/tasks/TaskQuickAdd.tsx` · `domain/tasks/createResolver.ts` ·
> `components/tasks/{PriorityPicker,ListPicker,TagPicker,SchedulePicker}.tsx` ·
> `styles/17-tasks-module.css`
> 앞선 문서: `TICKTICK_COMPONENT_10_QUICK_ADD.md`(같은 컴포넌트의 실측 · §10이 지금 모양을 만들었다)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 6장 | [관찰] |
| **실측** | `TaskQuickAdd.tsx` · `createResolver.ts` · 네 피커의 props · `supabase/migrations` · `01-base.css` | [실측] |
| **결정** | 사용자가 고른 것: 2·3번처럼 만들기 · 4번은 `Convert to Note`에서만 · 메뉴에서 `Add from Template`과 `Input Box Setting` 빼기 | [결정] |
| **추론** | 치수 · 칩이 무엇을 쓰는지 · 커밋 후 무엇이 남는지 | [추론] |

---

## 1. 스크린샷이 보여 준 것 [관찰]

| # | 무엇 | 읽히는 것 |
|---|---|---|
| 1 | **우리 현재** | 보더 있는 상자 + 오른쪽에 파란 `Add` 버튼 |
| 2 | **참조 · 휴지** | `⊹ Add Task` ……… `▦ Today ⌄` — 오른쪽 끝에 **날짜 칩 + 캐럿**, `Add` 버튼 **없음** |
| 3 | **캐럿의 메뉴** | `Priority` 제목 + 깃발 넷(빨강·주황·파랑·빈) │ `Inbox >` · `Tags >` · `Attachment` · `Add from Template` · `Convert to Note` · `Input Box Setting` |
| 4 | **노트 모드** | 두 줄. 위: 노트 아이콘 + `Record inspiration and time` / 아래: `▦ Today` · 태그 · 받은함 · `⋯` ……… `Add` 버튼 |
| 5 | **`Inbox >`** | 리스트 선택 하위 메뉴 — `Inbox` · `ZZ Folder >` · 리스트들 |
| 6 | **`Tags >`** | 태그 검색 입력 + 태그 목록 + `Save` / `Cancel` |

**[관찰] 이미지 5는 글자를 친 뒤에도 상자가 여전히 한 줄이고 `Add` 버튼이 없다.** 즉
`Add` 버튼은 "타이핑 중에 나타나는 것"이 아니라 **노트 모드에만 있는 것**이다.

**[결정] 메뉴에서 `Add from Template`과 `Input Box Setting`은 뺀다.**

---

## 2. 지금 우리 퀵애드 [실측]

### 2.1 한 줄이고, 묻는 것이 없다

`TaskQuickAdd.tsx`가 그리는 것: 아이콘 · 제목 입력 · (글자가 있을 때만) `Add` 버튼.
그 아래 `extras`에 폴더 스코프의 리스트 `<select>`, 템플릿 메뉴, 힌트.

**날짜·우선순위·태그·리스트를 고르는 자리는 없다.** 날짜는 **일부러 지웠다** —
`TaskQuickAdd.tsx`의 주석 그대로:

> 날짜 필드 없음. §12.6이 날짜 없이는 커밋을 거부했기 때문에 있었고, 그 거부는 사라졌다.
> 폼을 풀어 주는 것만이 일이던 컨트롤에는 남은 일이 없다.

**이 결정은 뒤집히지 않는다** — §3.1이 그 이유를 다시 쓴다. 새 칩은 폼을 **풀어 주는**
컨트롤이 아니라, 리졸버가 **이미 정한 날짜를 소리 내어 말하는** 것이다.

### 2.2 초안이 실릴 그릇은 이미 있다 [실측]

`CreateResolution`(`createResolver.ts`)은 이미 이렇게 생겼다:

| 필드 | 지금 누가 채우나 | 초안이 쓸 자리 |
|---|---|---|
| `targetListId` | 스코프 · `chosenListId` | **`Inbox >`가 고른 리스트** |
| `patch: Partial<Task>` | 스코프(예: upcoming은 `{dueDate}`) | **날짜 칩 · 우선순위 깃발** |
| `applyTagIds` | 태그 스코프의 Auto Apply | 태그(이름 문제는 §5.3) |
| `dailyPlan` | Today 스코프 | 그대로 |

즉 **새 도메인 개념이 필요하지 않다.** 스코프가 조용히 정하던 값을 사용자가 **덮어쓸 수
있게** 하는 것이 이 작업의 전부다(노트 모드는 예외 — §7).

특히 `upcoming`은 이미 `patch: { dueDate: chosenDate || today }`다. **참조가 `Next 7 Days`
에서 칩에 `Today`라고 쓰는 것과 우리 리졸버가 이미 하는 일이 같다.** 칩은 그것을 보이게
만들 뿐이다.

### 2.3 그런데 기존 피커 넷은 초안에 쓸 수 없다 [실측]

| 피커 | 첫 prop |
|---|---|
| `PriorityPicker` | `task: Task` |
| `ListPicker` | `task: Task` |
| `TagPicker` | `task: Task` (`tagsForTask(task.id, …)`) |
| `SchedulePicker` | `task: Task` |

**퀵애드에는 아직 Task가 없다.** 이것이 이 작업의 유일한 구조적 난점이고, §6이 그것만
다룬다. 가짜 Task를 지어내는 것은 답이 아니다 — `id`가 없는 Task를 `tagsForTask`에
넘기면 조용히 빈 배열이 나오고, 그런 종류의 거짓말은 나중에 진짜 버그로 갚는다.

### 2.4 없는 기능 둘 [실측]

- **첨부(Attachment)**: 파일 저장소가 **없다**. Supabase 버킷 0개, 업로드 경로 0개,
  `attachment`를 언급하는 코드 0줄.
- **노트(Note)**: 태스크와 구별되는 노트 엔티티가 **없다**. `space_notes` ·
  `concept_notes` 테이블은 Spaces 지식베이스의 것이고 리스트의 항목이 아니다.

---

## 3. 결정 A — 트레일링 슬롯: `▦ <날짜> ⌄`

### 3.1 칩 — 리졸버가 이미 정한 날짜를, 말한다

```
[⊹] 기본함에 작업 추가하기                          ▦ 오늘  ⌄
```

- **값**: `resolution.dailyPlan?.planDate ?? resolution.patch.dueDate ?? 초안의 선택`.
  스코프가 정하는 날이 없으면(Inbox · List · Folder) 칩은 **`날짜`**라는 흐린 글자로
  선다 — 자리는 지키고, 아직 값이 없다고 말한다.
- **누르면**: 일정 편집기(§6.4).
- **§2.1의 결정과 충돌하지 않는 이유**: 지운 컨트롤은 **커밋을 막던 필수 질문**이었고,
  이것은 **이미 결정된 값의 표시**다. 아무것도 막지 않는다 — 손대지 않고 Enter를 치면
  지금과 똑같이 동작한다.
- **`dailyPlan`과 `dueDate`는 다른 주장이다**(§12.5.1). 칩이 **쓰는** 값은 언제나
  `dueDate`이고, Today 스코프의 `dailyPlan`은 그대로 남는다. Today에서 칩은 계획일을
  **보여 주되**, 사용자가 날짜를 고르면 그것은 마감일로 쓰인다. [추론 · §12에서 확인]

### 3.2 `Add` 버튼은 사라진다

[관찰] 이미지 5는 글자가 있는데도 버튼이 없다. 커밋 경로는 이미 셋이다 — Enter,
바깥 클릭(`onBlur`), 그리고 **없어질** 버튼. 앞의 둘은 그대로 남는다.

노트 모드에는 `Add` 버튼이 **있다**(이미지 4) — 두 줄짜리 상자에서는 Enter가 줄바꿈일
수 있으므로 커밋에 눈에 보이는 자리가 필요하다. §7.

---

## 4. 결정 B — 캐럿의 메뉴

이미지 3에서 둘을 뺀 것 [결정]:

```
┌─────────────────────────┐
│ 우선순위                │
│  ⚑ ⚑ ⚑ ⚐                │   ← 네 칸, 누르면 초안에 실린다
├─────────────────────────┤
│ ⇥ 받은함           >    │   ← 리스트 선택 (이미지 5)
│ ⌗ 태그             >    │   ← 태그 선택 (이미지 6)
│ ⎘ 노트로 전환           │   ← §7
└─────────────────────────┘
```

### 4.1 `Attachment`는 그리지 않는다 [결정 · 2026-09-03]

한번 비활성 줄로 남기기로 정했다가, 구현 중에 사용자가 **아예 빼는 것**으로
바꾨 주었다. 이 앱에는 파일을 넣을 곳이 없고(§2.4), 누를 수 없는 회색 줄은
기능도 약속도 아니다. 첨부가 필요해지는 날, 그것은 저장소를 가진 별도의 작업이다.


## 5. 결정 C — 초안(Draft)

```ts
interface QuickAddDraft {
  title: string;
  /** "" = 스코프가 정한 값을 그대로 쓴다. */
  dueDate: string;
  priority: TaskPriority;   // 기본 "none"
  /** "" = 리졸버가 정한 targetListId. */
  listId: string;
  /** 이름으로 든다 — 새 태그는 아직 id가 없다(§13.42와 같은 이유). */
  tagNames: string[];
  kind: "task" | "note";
}
```

### 5.1 커밋 — 리졸버 위에 초안을 얹는다

```ts
const finalResolution = {
  ...resolution,
  targetListId: draft.listId || resolution.targetListId,
  patch: {
    ...resolution.patch,
    ...(draft.dueDate ? { dueDate: draft.dueDate } : {}),
    ...(draft.priority !== "none" ? { priority: draft.priority } : {}),
  },
  applyTagNames: draft.tagNames,
};
onCreate(draft.title.trim(), finalResolution);
```

**스코프가 먼저, 사용자가 나중.** 스코프의 `patch`를 지우지 않고 덮어쓰는 순서가
중요하다 — 필터 스코프가 정하는 필드들(§12.11)은 사용자가 건드리지 않은 한 살아 있어야
한다.

### 5.2 커밋 후 남는 것

제목만 비운다. 날짜·리스트·우선순위·태그·모드는 **남는다** — 지금 코드가 이미 그렇게
정해 두었고("같은 날, 같은 리스트로 여러 개를 넣는 것이 흔한 경우이고 줄마다 다시 답하게
하는 것은 세금") 이유는 항목이 늘어도 같다.

### 5.3 `applyTagNames` — `CreateResolution`에 필드 하나

`applyTagIds`는 **스코프의** 태그이고 id로 온다. 초안의 태그는 **이름**이다(새로 만든
태그에는 id가 없다). 둘은 합쳐져야 하고, 합치는 자리는 이미 `App.tsx`에 있다 — 지금
`applyTagIds`를 이름으로 바꾸어 `{ tags }`로 넘기는 그 자리. 그 병합을 **합집합**으로
바꾼다. 필드를 하나 더 두는 이유는 §12.4의 Auto Apply와 사용자의 선택이 **서로 다른
사실**이기 때문이다: 하나는 스코프가 요구하는 것이고 하나는 사람이 고른 것이다.

---

## 6. 결정 D — 피커를 초안도 쓸 수 있게 (§2.3의 답)

**규칙 하나: 고르는 UI는 값을 받고, Task를 받는 것은 그 위의 껍데기다.**

| 피커 | 지금 | 나눈 뒤 |
|---|---|---|
| Priority | `PriorityOptions`가 **이미** `{current, onChange}`만 받는다(파일 안 비공개 함수) | **재사용하지 않았다** — §6.5 |
| List | `ListOptions`가 **이미** `{currentId, lists, folders, onMove}`만 받는다 | **export만 하면 끝났다** |
| Tag | `TagPicker`가 `tagsForTask(task.id, …)`로 보유 태그를 구한다 | `TagOptions`가 `selectedNames: string[]`을 받는다. 도메인에는 `tagOptionsFor(names, tags, query)`가 생기고 기존 `tagPickerOptions`가 그것에 위임한다 |
| Schedule | `SchedulePicker`가 Task에서 스케줄을 읽는다 | 초안은 **날짜 하나**만 필요하다 → §6.4 |

이렇게 하면 **한 벌의 선택 UI를 두 곳이 쓴다.** 초안용으로 따로 그리면, 태그 목록의
정렬이나 리스트의 폴더 그룹핑이 두 자리에서 갈라진다 — 우선순위 팔레트가 다섯 자리에서
갈라졌던 것과 같은 방식으로(`TASK_PRIORITY_CHECKBOX_DESIGN.md` §2.1).

### 6.4 날짜 칩은 앱의 진짜 일정 편집기를 연다 [구현됨 · 2026-09-03]

처음에는 네 개의 바로가기만 두는 작은 팝오버로 시작했다. 사용자가 **디테일과 같은
편집기**를 요구했고, 그것이 옳다 — 날짜를 고른다는 것이 두 자리에서 다른 뜻이면 둘은
언젠가 갈라진다.

막고 있던 것은 하나였다: **리마인더는 필드가 아니라 행이다.** `Task id`가 있어야 쓸 수
있으므로, 아직 없는 태스크에 대고 리마인더 패널을 여는 것은 보관할 수 없는 답을 받는
일이었다. 해결은 그 순서를 인정하는 것이다.

| 편집기가 정하는 것 | 어디로 가나 |
|---|---|
| 마감일 · 시작일 · 시작/종료 시각 · **반복** | `scheduleToTaskPatch(schedule)` → `resolution.patch` → `createTask` |
| **리마인더** | `resolution.reminders` → 태스크가 만들어진 **뒤** `planner.addTaskReminders(id, specs)` |

`scheduleToTaskPatch`는 `updateTaskSchedule`이 쓰는 바로 그 변환이다. 그러므로 퀵애드에서
정한 일정과 디테일에서 정한 일정은 **같은 필드를 같은 값으로** 쓴다. 리마인더도 같은
`planReminderRows`를 지나므로 id와 시각을 만드는 자리는 여전히 하나다.

**`updateTaskSchedule`을 그대로 부를 수는 없었다** — 그것은 먼저 `data.tasks`에서 태스크를
찾는데, 한 줄 앞에서 만든 태스크는 그 클로저에 아직 없다. `addTaskReminders`가 조회 없이
`setData`만 하는 이유다. `dailyPlan`이 이미 "만든 뒤의 두 번째 쓰기"였고, 이것은 그 옆에
선다.

**푸터는 `취소` / `확인`이다** [결정 · 2026-09-03]. 디테일의 `일정 지우기`가 비우는 것은
**저장된 일정**이고, 여기에는 저장된 것이 없다. 그 자리에 필요한 것은 고르지 않고
빠져나가는 길이다. `ScheduleEditor`에 `onCancel`이 있으면 왼쪽 버튼이 그것으로 바뀐다 —
빨강이 아니라 조용한 회색으로, 지우기와 나가기가 같아 보이지 않도록.

**초기값은 칩이 말하던 날로 씨를 뿌린다.** 그러지 않으면 칩이 `오늘`이라고 말하는 동안
달력은 빈 일정으로 열리고, 편집기의 `dirty` 검사 때문에 **이미 보이는 그 날을 다시 눌러야
확인이 켜진다.**

### 6.5 우선순위는 글자 목록이 아니라 색 줄이다 [구현됨]

`PriorityOptions`를 가져다 쓰려고 했지만, 그것은 글자가 붙은 **세로 목록**이고
이미지 3은 **가로 네 칸**이다. 디테일에서는 그 팝오버가 화면의 전부라 목록이 맞고,
여기서는 메뉴 한 줄이라 네 줄이면 나머지 전부를 합친 높이가 된다.

나뉘어야 했던 것—**팔레트**—는 이미 `--priority-*` 토큰 하나다
(`TASK_PRIORITY_CHECKBOX_DESIGN.md` §3). 두 표현은 같은 색을 읽고 같은 `PRIORITY_LEVELS`를
돌고, 다른 모양을 그린다.

---

## 7. 결정 E — 노트 모드 (`Convert to Note`)

### 7.1 노트란 무엇인가 — 우리 데이터 모델에서

[실측] `tasks` 테이블은 `data jsonb` 한 칸이다. `Task.startDate`의 주석이 선례를 남겼다:
**"추가 전용(M0). `data` jsonb 안에 타므로 테이블 변경이 필요 없다."**

그러므로:

```ts
/** 체크박스가 없는 항목 (이미지 4). 없으면 "task" — 지금까지의 모든 레코드가 그렇다. */
kind?: "task" | "note";
```

**마이그레이션 없음.** 비용은 스키마가 아니라 **의미**에 있다:

| 질문 | 노트의 답 |
|---|---|
| 완료할 수 있나 | **아니오.** 체크박스를 그리지 않는다(`TaskCheck`를 쓰지 않는다) |
| `Completed` 스코프에 나오나 | 아니오 |
| 카운트에 세나 | **센다** — 화면에 보이는 줄 수와 다른 숫자는 그것이 구분하려던 사실보다 나쁘다(§13.2) |
| 우선순위·태그·리스트·날짜 | 전부 가진다 (이미지 4의 툴바가 넷을 다 그린다) |
| 디테일을 여나 | 연다. 완료 체크만 없는 같은 패널 |

### 7.2 상자의 모양 (이미지 4)

```
┌────────────────────────────────────────────┐
│ ▤  영감과 시간을 기록하세요                 │  ← 노트 아이콘 + 전용 플레이스홀더
│ ▦ 오늘   ⌗   ⇥   ⋯              [ 추가 ]   │  ← 툴바 + Add 버튼
└────────────────────────────────────────────┘
```

두 줄인 것이 요점이다: 노트는 **여러 줄을 쓰는 것**이므로 입력이 `<textarea>`가 되고,
그러면 Enter가 커밋일 수 없다 → `Add` 버튼이 필요하다(§3.2).

### 7.3 범위 [결정]

노트는 **Phase 5**로 마지막에 놓는다 [결정 · 2026-09-03 — 다섯 단계 전부 구현]. 순서가
중요해서다: 칩과 메뉴는 "이미 있는 값을 보이게 하는 것"이고, 노트는 **리스트에 새로운
종류의 항목을 들이는 것**이다. 앞의 넷은 노트 없이도 완성이고, 노트는 앞의 넷이 만든
초안 위에 `kind` 하나를 얹는다.

---

## 8. 레이아웃

```css
/* 상자는 §10.2가 정한 그대로 — 32px, radius 10, 전경 3% 계열 채움,
   1px transparent 보더, focus에서 accent. 바뀌는 것은 트레일링 슬롯의 내용뿐이다. */
.tm-quickadd-trailing {   /* 새로 */
  display: flex;
  flex: none;
  align-items: center;
  gap: 2px;
}
.tm-quickadd-date { … }   /* 칩: 아이콘 14 + 텍스트 12–13, accent 색 */
.tm-quickadd-more { … }   /* 캐럿: 20×20, 흐린 색, hover에서 surface-2 */
```

**높이 32는 유지한다.** §10.2가 세로 리듬(`e2e/verticalRhythm.spec.ts`)을 근거로 참조의
39를 따르지 않기로 했고, 트레일링 슬롯이 그 안에 들어간다(칩 20 + 캐럿 20).

노트 모드는 `.tm-quickadd-box.is-note`로 두 줄이 된다 — 높이 `auto`, 최소 64.

---

## 9. 접근성

| 항목 | 어떻게 |
|---|---|
| 캐럿 | `aria-haspopup="menu"`, 접근 이름은 "추가 옵션" |
| 칩 | Phase 1에서는 **버튼이 아니라 텍스트**(`<span>`) — 누를 수 없는 것을 버튼으로 그리지 않는다 |
| 우선순위 넷 | 메뉴 안의 `radiogroup`. 색만으로 구분되지 않게 각 칸에 접근 이름("높음")을 준다 |
| 하위 메뉴 | 기존 `Popover`/`FloatingMenu`의 규칙 그대로 — Escape가 한 겹씩 |
| 초안의 상태 | 무엇이 실릴지는 칩과 메뉴의 선택 표시로 보인다. 태그가 붙으면 상자 아래 한 줄(지금 `willTag` 힌트가 쓰는 그 자리) |

---

## 10. 바뀌지 않는 것

- **커밋 경로 셋 → 둘**: Enter와 바깥 클릭. `onBlur` 커밋 규칙(폼 안으로 이동하는 것은
  "떠나는 것"이 아니다)은 그대로이고, **메뉴와 하위 메뉴가 폼 밖의 포털에 그려지므로**
  그 예외 판정에 레이어를 포함시켜야 한다 — §11 Phase 1의 유일한 함정.
- **`resolveCreateContext`의 스코프별 답** — 한 줄도 바뀌지 않는다. 초안은 그 위에 얹힌다.
- **폴더 스코프의 리스트 질문** — `Inbox >`가 생겨도 §12.4의 "반드시 골라야 한다"는 남는다.
  다만 그 `<select>`는 `Inbox >`로 대체된다(같은 질문의 더 나은 자리).
- **템플릿 메뉴 · 힌트 줄** — 그대로.

---

## 11. 구현 단계 — 다섯 단계 전부 끝났다 [2026-09-03]

| # | 무엇 | 상태 |
|---|---|---|
| **1** | 초안 상태 + 트레일링 슬롯(칩 + 캐럿) + `Add` 버튼 제거 | ✓ |
| **2** | 메뉴의 우선순위 넷 (§6.5) | ✓ |
| **3** | `Inbox >` · `Tags >` 하위 메뉴 — §6의 분리 작업 포함 | ✓ |
| **4** | 칩을 고르는 칩으로 (§6.4) | ✓ |
| **5** | 노트 모드 — `kind` 필드, 행 렌더, 완료 행동의 부재 | ✓ |
| **6** | `npx tsc -b` + 전체 테스트 | ✓ |

하위 메뉴는 **중첩 Popover**로 됐고, 그것이 새 장치가 아니었다는 것이 확인됐다 —
§19.24의 스택은 `parentId`를 컨텍스트에서 읽고(`Popover.tsx:107`), 자식 안을 누르면
조상을 살려 둔다(`dismissedByPointer`가 `ancestorIds`를 걷는다). 실측: 하위 메뉴가
부모 오른쪽에 열리고, Escape 한 번에 하위 메뉴만 닫혔다.

## 12. 테스트

| 무엇 | 어디 |
|---|---|
| 빈 상태에서 칩과 캐럿이 보이고, `Add` 버튼이 없다 | `taskQuickAdd.test.tsx` |
| 글자를 쳐도 `Add` 버튼이 생기지 않는다 | 같은 곳 |
| 칩이 스코프가 정한 날짜를 말한다 (Today / Next 7 Days / Inbox 셋) | 같은 곳 |
| 우선순위를 고르고 Enter → `onCreate`의 patch에 `priority`가 실린다 | 같은 곳 |
| 리스트를 고르면 `targetListId`가 그것으로 간다 | 같은 곳 |
| 태그를 고르면 `applyTagNames`로 나가고, 스코프의 Auto Apply와 **합쳐진다** | `createResolver` + 모듈 |
| 메뉴를 클릭해도 `onBlur` 커밋이 일어나지 않는다 | 같은 곳 — §10의 함정 |
| 커밋 후 제목만 비워지고 나머지는 남는다 | 같은 곳 |

---

## 13. 결정된 것과 남은 것

**결정 [2026-09-03]**

1. **`Attachment`는 그리지 않는다**(§4.1).
2. **노트는 카운트에 세고 목록에 선다**(§7.1). 세지 않는 쪽을 권했다가 바꾸었다 —
   화면에 보이는 줄 수와 다른 숫자는, 그것이 구분하려던 사실보다 나쁜 거짓이다.
   노트는 완료할 수 없을 뿐 항목이다.
3. **우선순위 줄은 높음→없음 순서**로 그렸다(참조 앱과 같이). 디테일의 팝오버는 기존
   순서(없음→높음)를 유지한다 — 하나는 색 줄이고 하나는 글자 목록이라 읽는 방향이
   다르다(§6.5).
4. **Today에서 칩이 고른 날짜는 `dueDate`**로 쓴다(§3.1). 스코프가 붙이는 `dailyPlan`은
   그대로 남는다.
5. **템플릿 메뉴는 `extras`에 남아 있다.** 참조 메뉴의 `Add from Template`을 뺀 것과
   우리 기능을 없애는 것은 다른 일이다.

**남은 것**

6. 노트의 디테일은 여전히 태스크의 패널이다 — 완료 체크만 없다. 하위 작업·의존성 같은
   줄이 노트에도 맞는지는 써 보고 정할 일이다.
7. 노트를 모아서 보는 자리(참조 앱의 `Notes` 탭)는 이 문서 밖이다.
