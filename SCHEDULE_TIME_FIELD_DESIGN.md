# 시간과 알림은 선 자리에서 열린다

> 상태: **구현됨** · 2026-09-03 (같은 날 1차 설계를 **갈아엎었다** — §0.1;
> 구현하며 정해진 것 넷은 §12)
> (사용자가 넉 장을 캡처해 주었다: 우리 현재 시간 패널 · 참조 앱의 시간 목록 ·
> 참조 앱의 알림 목록 · 알림의 `Custom` 폼.)
> 대상: `components/schedule/{ScheduleEditor,TimePanel,ReminderPanel,ChoicePanel}.tsx` ·
> `domain/schedule/{editorState,timeOptions,reminders}.ts` · `utils/clock.ts` ·
> `styles/18-schedule-editor.css`

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 5장 (다섯째는 사용자 지정의 단위 목록) | [관찰] |
| **실측** | 브라우저에서 시간 패널을 열어 DOM을 읽었다 · `TimePanel.tsx` · `ReminderPanel.tsx` · `editorState.ts` · `reminders.ts` · `clock.ts` | [실측] |
| **결정** | 사용자가 고른 것 넷: 그 자리에서 목록이 뜰 것 · 30분 단위 · **현재 시각을 올림한 정시**가 기본값 · 알림도 그 자리에서, `Custom`은 4번 이미지처럼 | [결정] |
| **추론** | 파싱 문법 · 23시대의 기본값 | [추론] |

### 0.1 1차 설계에서 뒤집힌 것 둘

이 문서의 첫 판은 같은 날 오전에 썼고 구현 전에 두 곳이 뒤집혔다. 남겨 두는 편이
정직하다:

| # | 1차 | 지금 | 왜 |
|---|---|---|---|
| 1 | 시간은 **별도 패널**로 (지금 구조 유지) | **누른 자리에서** 목록이 내려온다 | [관찰] 2·3번 이미지. 달력을 지우고 다른 화면으로 넘어가는 것과, 누른 줄 밑이 열리는 것은 다른 경험이다 |
| 2 | 값이 없을 때 목록은 `09:00`에 선다. **시계를 읽지 않는다** | **현재 시각을 올림한 정시**가 기본값 | [결정] 사용자가 명시했다: 오후 7시 15분이면 8시, 2시 1분이면 3시 |

2번은 이 편집기가 지켜 온 규칙(`오늘 밤` 단축키를 없앤 이유 — `quickDate.ts` 서문)과
부딪힌다. 그 규칙이 막으려던 것은 **시계에 따라 값이 달라지는 저장**이었고, 여기서
시계는 **첫 제안**을 만든다 — 사용자가 그대로 확인하면 그 값이 저장되지만, 그것이 바로
"기본값"이 뜻하는 바다. §3.3이 이 차이를 조건 하나로 못 박는다.

---

## 1. 스크린샷이 보여 준 것 [관찰]

| # | 무엇 | 읽히는 것 |
|---|---|---|
| 1 | **우리 현재** | `Starts` 아래 `--:-- --` 네이티브 입력 + 프리셋 넷. 눌러도 아무것도 안 열린다 |
| 2 | **참조 · 시간** | 달력 **아래**에 `🕐 8:00 PM ✕` 필드, 그 **바로 밑**에 30분 목록. 현재 값이 맨 위, 파랑 + 체크. 목록이 카드 밑단을 넘어 떠 있다 |
| 3 | **참조 · 알림** | `Time ›` 는 닫힌 채, `Reminder ⌄` 가 열려 그 **밑으로** 목록이 내려온다. `On the day (9:00 AM)` … `7 days early (9:00 AM)` · `Custom` · `Constant Reminder` 토글 · `Save`/`Cancel` |
| 4 | **`Custom`** | 단위 셀렉트(`Day`) · `Day in advance [1]` · `Remind me at [9:00 AM]` · **문장 미리보기** `1 day early, Remind me at 9:00 AM on Sep 2, 2026.` · `Save`/`Cancel` |

**[관찰] 3번의 `›`와 `⌄`가 같은 줄의 두 상태다.** 즉 세 줄 모두 **그 자리에서 펼쳐지는
줄**이고, 화살표는 접힘/펼침의 표시다.

---

## 2. 실측 — 지금 무엇이 일어나는가

### 2.1 "아무것도 안 뜬다"의 정체

시간 패널을 실제로 열어 DOM을 읽었다. **패널은 열린다** — `‹ Time / Starts / 9:00 AM ·
1:00 PM · 6:00 PM · 9:00 PM / (설명) / Remove time`이 전부 렌더된다.

아무것도 뜨지 않는 것은 **필드**다:

```html
<input type="time" step="300" value="">    <!-- 화면에는 --:-- -- -->
```

- 눌러도 `[role="listbox"]`는 **0개**. 네이티브 `<input type="time">`은 크로미움에서
  **목록을 열지 않는다** — 구간을 화살표로 오르내리는 스피너가 전부다.
- `step="300"`(5분)은 그 스피너의 간격일 뿐, 고를 수 있는 목록이 아니다.

버그가 아니라 **컨트롤 선택의 결과**다. "브라우저가 알아서 해 주겠지"에 걸었고, 크로미움은
아무것도 해 주지 않았다.

### 2.2 지금은 화면을 갈아탄다

`EditorPanel = "calendar" | "time" | "reminder" | "repeat" | "start" | "end"`이고,
`시간`을 누르면 `SET_PANEL`이 **달력을 지우고** 시간 화면을 그린다(설계 §2.15). 되돌아가는
`‹`가 있고, Escape가 한 겹 벗기도록 `document`에 캡처 단계 리스너까지 달려 있다(§2.16).

[관찰]이 보여 주는 것은 다른 모양이다: **달력은 그대로 있고, 누른 줄 밑이 열린다.**

### 2.3 옆에 있는 문제 둘 [실측]

**(a) 프리셋 넷이 앱의 시계 설정을 무시한다.** `formatLocalTime`은 `hour12`를 넘기지
않으므로 로케일의 관습만 따른다. 앱에는 `data-time-format`(`locale`/`12h`/`24h`) 설정이
있고 캘린더는 그것을 읽는 `formatClock`을 쓴다(SETTINGS_REVIEW 4.2). **`24h`로 둔
사용자에게 이 패널만 `9:00 AM`이라고 말한다.**

**(b) 종료 시각도 같은 네이티브 입력이다.** 하나를 고치면 둘 다이고, 둘이 같은
컴포넌트여야 한다.

---

## 3. 결정 A — 시간

### 3.1 30분 48칸, 도메인에서

```ts
// domain/schedule/timeOptions.ts
export const TIME_STEP_MINUTES = 30;
/** 00:00부터 23:30까지 오름차순. 순수 함수 — 시계를 읽지 않는다. */
export function timeOptions(step?: number): LocalTime[];
```

걸러 내지 않는다. [관찰] 2번에서 `8:00 PM` 위의 시각들이 안 보이는 것은 사라져서가 아니라
**현재 값이 맨 위에 오도록 스크롤**되었기 때문이다. `24:00`은 목록에 없다 — 그것은 일정의
*끝*을 말하는 값이지 고르는 항목이 아니다.

### 3.2 타이핑

```ts
/** 사람이 쓴 시각을 `HH:MM`으로. 읽을 수 없으면 null. */
export function parseTimeInput(text: string): LocalTime | null;
```

| 입력 | 결과 |
|---|---|
| `7` | `07:00` |
| `730` · `0730` | `07:30` |
| `7:30` | `07:30` |
| `19:00` · `1900` | `19:00` |
| `7:30 PM` · `7pm` | `19:30` · `19:00` |
| `오전 7:30` · `오후 7시` | `07:30` · `19:00` |
| `25:00` · `7:70` · `abc` | **`null`** |

`null`은 거절이지 0시가 아니다 — 읽을 수 없는 입력에 시각을 지어내면 사용자가 쓴 것과
다른 시간이 조용히 저장된다. `12 AM` = `00:00`, `12 PM` = `12:00`은 표로 못 박는다:
`(h % 12) + (pm ? 12 : 0)`이 그것을 정확히 낸다.

### 3.3 기본값 — 현재 시각을 올림한 정시 [결정]

```ts
/** 07:15 → 08:00, 14:01 → 15:00, 07:00 → 07:00, 23:30 → 23:00? 아니다 — §아래 */
export function nextWholeHour(now: LocalTime): LocalTime;
```

- **분이 0이면 그대로.** `07:00` → `07:00`. 올림은 "이미 지난 시각을 제안하지 않기"
  위한 것이고, 정각은 지나지 않았다.
- **23시대는 `23:00`으로 내린다** [추론]. `24:00`은 목록에 없고(§3.1) 다음 날로 넘기는
  것은 이 필드가 할 말이 아니다 — 날짜는 위의 달력이 정한다.
- **인자로 받는다.** 도메인은 여전히 시계를 읽지 않는다: 컴포넌트가 읽어서 넘긴다.
  그래야 테스트가 `07:15`을 그냥 줄 수 있다.

**언제 쓰이나**: `시간` 줄을 펼쳤는데 **시작 시각이 비어 있을 때만.** 값이 있으면 건드리지
않는다. 이것이 §0.1의 규칙과 충돌하지 않는 이유다 — 시계는 **빈 칸을 채우는 제안**을
만들 뿐, 이미 있는 값을 바꾸지 않는다.

### 3.4 컴포넌트 — 콤보박스 하나

`components/schedule/TimeField.tsx`. **이 앱에 이미 두 개 있는 패턴**이다: 리스트 피커와
태그 피커가 `role="combobox"` + `aria-activedescendant` + `role="listbox"`로 같은 일을
한다(§13.26–§13.27). 세 번째를 발명하지 않는다.

| 키 | 하는 일 |
|---|---|
| 글자 | 필드에 들어간다. 목록은 열린 채, 파싱된 값 이상의 첫 항목으로 스크롤 |
| `↓` `↑` | 목록을 열고 활성 항목을 옮긴다. **포커스는 필드에 남는다** |
| `Enter` | 필드가 읽히면 **그것**을, 아니면 활성 항목을 고른다 |
| `Escape` | 목록만 닫는다 |
| blur | 읽히면 커밋, 아니면 **마지막 유효 값으로 되돌린다** |

`Enter`가 타이핑을 우선하는 이유: 화살표를 쓰지 않고 `7:30`이라 친 사람에게 활성 항목은
스크롤 위치일 뿐이다. `✕`는 값을 지운다([관찰] 2번) — 패널의 `시간 제거`는 시작과 종료를
**함께** 지우므로 다른 일이고, 둘 다 남는다.

---

## 4. 결정 B — 세 줄이 제자리에서 열린다

`시간` · `알림` · `반복` 세 줄은 **누르면 그 밑이 열리는 줄**이 된다. 화살표는 `›`에서
`⌄`로 돈다([관찰] 3번).

### 4.1 열리는 것은 카드가 아니라 층이다

목록은 **줄에 붙은 중첩 팝오버**로 그린다(placement `bottom-start`, 폭은 줄의 폭).
카드를 늘리지 않는 이유:

- [관찰] 2·3번에서 목록이 **카드 밑단을 넘어** 떠 있다.
- 카드를 늘리면 달력이 밀리거나 편집기 안에 두 번째 스크롤이 생긴다. 달력은 이 편집기의
  본문이고, 시각 하나를 고르려다 그것이 움직이는 것은 §2.15가 애초에 피하려던 일이다.
- 중첩 팝오버는 **이미 검증됐다** — 퀵애드의 `받은함 ›` · `태그 ›`가 그렇게 열리고,
  §19.24의 스택이 `parentId`로 부모를 살려 둔다.

### 4.1.1 덮는다, 밀지 않는다

열린 목록은 **아래 줄들 위에 뜬다.** 알림과 반복, 그리고 푸터는 밀려 내려가는 것이 아니라
**가려진다** — [관찰] 2번에서 목록이 카드 밑단을 넘어 나가는 것이 그 증거다. 카드가
늘어나는 방식이었다면 목록은 카드 안에 갇혔을 것이다.

가려지는 것이 손해가 아닌 이유: 지금 답하는 질문은 하나이고, 그 아래 줄들은 그 질문이
끝난 뒤에 답할 것들이다. 닫으면 그대로 돌아온다.

### 4.1.2 시간 줄만 다른 점 하나

세 줄 다 제자리에서 열리지만, **시간은 줄 자체가 입력칸이 된다** ([관찰] 2번과 3번을
나란히 놓으면 보인다 — 3번의 `Time ›`가 2번에서는 `🕐 [오후 8:00] ✕`이다).

| | 접힘 | 펼침 |
|---|---|---|
| 시간 | `🕐 시간 ›` | **줄 = 입력칸** `🕐 [오후 8:00] ✕`, 그 밑에 30분 목록 |
| 알림 | `🔔 알림 ›` | 줄은 그대로 `⌄`, 그 밑에 목록 |
| 반복 | `🔁 반복 ›` | 위와 같음 |

시간만 그런 이유는 그것만 **타이핑할 수 있는 값**이기 때문이다(§3.2). 알림과 반복은 고르는
것뿐이라 줄이 입력칸이 될 까닭이 없다.


### 4.2 그래서 사라지는 것

- `EditorPanel`에서 `"time"` · `"reminder"` · `"repeat"`이 빠진다. 남는 것은
  `"calendar" | "start" | "end"` — 진짜로 화면을 갈아타는 셋뿐이다.
- `‹` 뒤로가기 머리 세 개, `SET_PANEL` 왕복.
- **§2.16의 손수 만든 Escape 캡처 리스너.** 그것이 하던 일("하위 패널의 Escape는 편집기가
  아니라 달력으로 돌아간다")을 레이어 스택이 이미 한다 — 한 번에 한 겹.

### 4.3 세 줄이 같은 모양이어야 하는 이유

사용자가 말한 것은 `시간`과 `알림` 둘이다. `반복`만 화면을 갈아타게 두면 **한 카드 안에서
같은 생김새의 세 줄이 서로 다르게 행동한다.** `반복`은 `ChoicePanel` 하나라 옮기는 비용이
거의 없다.

---

## 5. 결정 C — 알림 목록

[관찰] 3번을 우리 모델로 옮기면:

```
🔔 알림                              ⌄
┌──────────────────────────────────┐
│ 당일  (오전 9:00)             ✓  │   ← offersFor(draft)가 주는 목록
│ 1일 전 (오전 9:00)               │      (시각이 있는 일정이면 다른 목록)
│ 2일 전 (오전 9:00)               │
│ 1주 전 (오전 9:00)               │
├──────────────────────────────────┤
│ 사용자 지정…                     │   ← §6
└──────────────────────────────────┘
```

- **괄호 안의 시각은 실제로 언제인지**다 — `reminderMoment(spec, draft)`가 이미 계산한다.
  [관찰] 3번이 그렇게 쓴다.
- **다중 선택 그대로.** §6.15/§6.17: 하루 전 **그리고** 한 시간 전을 함께 고를 수 있고,
  체크가 그것을 말한다.
- **`Save`/`Cancel`은 두지 않는다** [결정 · 관찰과 다름]. 우리 토글은 **초안**에 즉시
  실리고, 초안 전체는 편집기의 `확인`이 저장한다. 목록 안에 또 `Save`를 두면 한 값에
  확인이 셋이 된다(목록 → 편집기 → 그리고 퀵애드라면 태스크 생성). §6.21의 사용자 지정
  폼만은 `저장`/`취소`를 갖는다 — 그것은 여러 칸을 조립하는 **빌더**라 중간 상태가 있다.
- **`Constant Reminder`는 범위 밖.** 우리 `ReminderSpec`에 대응하는 개념이 없다(§9).

---

## 6. 결정 D — 사용자 지정 ([관찰] 4번, 5번)

5번 이미지가 단위 목록을 확정해 주었다: **분 · 시간 · 일 · 주** 넷. [관찰]

```
┌──────────────────────────────┐
│ [ 30 ]    [ 분        ⌄ ]    │   분 · 시간 · 일 · 주
│                              │
│ 30분 전                      │   ← 미리보기 (reminderMoment)
│ 9월 3일 오후 7:30에 알림     │
│                              │
│   [ 저장 ]   [ 취소 ]        │
└──────────────────────────────┘
```

### 6.1 단위 넷이 우리 모델로

새 필드가 필요 없다. 넷 다 `offsetMinutes` 하나로 떨어진다:

| 단위 | `ReminderSpec` |
|---|---|
| 분 × N | `{ type: "relative", offsetMinutes: N, allDayTime: null }` |
| 시간 × N | `{ type: "relative", offsetMinutes: N × 60, allDayTime: null }` |
| 일 × N | `{ type: "relative", offsetMinutes: N × 1440, allDayTime: T }` |
| 주 × N | `{ type: "relative", offsetMinutes: N × 10080, allDayTime: T }` |

`주`는 7일이고 그 이상은 아니다 — `reminderMoment`가 `floor(offset / 1440)`으로 날짜를
빼므로, 7의 배수라는 사실만으로 정확히 그 주의 같은 요일에 떨어진다.

### 6.2 시각 칸은 일·주에서만 나온다 [관찰]

5번 이미지에는 `Remind me at`이 **없고** 4번(`Day`)에는 **있다.** 그것이 규칙이다:

| 단위 | "몇 시에" | 왜 |
|---|---|---|
| 분 · 시간 | **묻지 않는다** | 태스크의 시각에서 거꾸로 세는 것이라 답이 이미 정해져 있다(§6.12) |
| 일 · 주 | **묻는다** | 며칠 전의 어느 시각인지는 아무도 모른다. 기본값 `09:00` |

이것은 `allDayTime`이 `null`인지 아닌지와 **정확히 같은 구분**이다 — 화면의 규칙이 아니라
데이터의 규칙을 그리는 것이므로, 둘이 어긋날 수 없다.

### 6.3 절대 시각은 이 셀렉트에 넣지 않는다 [결정]

참조의 단위 넷은 전부 상대 시각이다. 우리에겐 **절대 시각 알림**이 따로 있고(§6.13,
지금 패널의 `At a specific time`), 그것을 다섯 번째 단위로 밀어 넣으면 셀렉트 하나가
"얼마나 전에"와 "언제"라는 **다른 질문 둘**을 답하게 된다.

그래서 목록의 형제로 남긴다:

```
│ 사용자 지정…        │   ← 상대 (이 폼)
│ 특정 시각…          │   ← 절대 (지금의 datetime-local)
```

기능은 그대로 있고, 각 줄이 한 질문만 한다.

### 6.4 미리보기

문장은 `reminderMoment(spec, draft)`의 결과로 쓴다 — 목록의 괄호와 같은 함수다. 폼이 자기
계산을 하면 같은 알림을 두 자리가 다르게 말할 수 있다.

날짜가 아직 없으면 `reminderMoment`가 `null`을 낸다. 그때 미리보기는 "날짜를 먼저
고르세요"라고 쓴다 — 지금 줄이 `disabled={!dated}`로 **막는** 사실을, 막는 대신 **말한다.**

---

## 7. 표기는 앱의 시계 설정을 따른다

목록 48칸도, 필드의 값도, 프리셋 넷도, 알림 괄호 속 시각도
`formatClock(time, getTimeFormat(), locale)`로 쓴다. `24h`인 사용자는 `19:30`을 본다.
`TimePanel`의 `formatLocalTime` 호출은 그래서 사라진다(§2.3a) — 함수 자체는
`scheduleFormatting`의 다른 자리에서 계속 쓰이므로 지우지 않는다.

파싱은 반대 방향이고 설정과 무관하다: `24h`로 둔 사람이 `7pm`이라고 쳐도 읽어 준다.

---

## 8. 구현 단계

| # | 무엇 | 크기 |
|---|---|---|
| 1 | `domain/schedule/timeOptions.ts` — `timeOptions` · `parseTimeInput` · `nextWholeHour` + 테스트 | 작음 |
| 2 | `TimeField` 콤보박스 (§3.4) | 중간 |
| 3 | 세 줄을 제자리 펼침으로 (§4) — `EditorPanel` 축소, `‹` 머리와 §2.16 리스너 제거 | 중간 |
| 4 | 알림 목록을 그 자리로 (§5) | 작음 |
| 5 | 사용자 지정 폼 (§6) | 중간 |
| 6 | CSS — 목록 `max-height`·스크롤·활성/선택, 줄의 `⌄` | 작음 |
| 7 | `npx tsc -b` + 전체 테스트 | — |

1~2는 그 자체로 배포 가능하다(패널 안에서 필드만 나아진다). 3부터 모양이 바뀐다.

---

## 9. 범위 밖

- **타임존 셀렉트**([관찰] 2번 하단). `Schedule.timezone`은 필드로 있으나 Phase 1이
  **해석하지 않기로** 명시했다(`types.ts`의 NOTE ON TIMEZONE). 고를 수 있게 만들면 아무
  효과 없는 컨트롤이 된다 — 리마인더 전달(Phase 11)과 함께 다룬다.
- **`Constant Reminder`**([관찰] 3번). 대응하는 개념이 없다. 만들려면 "울린 뒤에도 계속
  울린다"가 무엇인지부터 정해야 하고, 그것은 알림 전달의 설계다.
- **5분·1분 눈금 설정.** `timeOptions(step)`이 인자를 받는 것은 그때를 위한 자리이지,
  지금 설정을 만들자는 뜻이 아니다.

---

## 10. 테스트

| 무엇 | 어디 |
|---|---|
| `timeOptions()`가 48칸, `00:00`~`23:30` | `timeOptions.test.ts` |
| `parseTimeInput`의 §3.2 표 전부 (`null` 포함, `12 AM`/`12 PM`) | 같은 곳 |
| `nextWholeHour`: `07:15`→`08:00` · `14:01`→`15:00` · `07:00`→`07:00` · `23:40`→`23:00` | 같은 곳 |
| 필드를 누르면 `listbox`가 열린다 (지금은 0개) | `timeField.test.tsx` |
| `↓`가 포커스를 필드에 두고 활성 항목만 옮긴다 | 같은 곳 |
| 타이핑 후 `Enter`가 활성 항목이 아니라 타이핑을 고른다 | 같은 곳 |
| 읽을 수 없는 입력 후 blur는 마지막 유효 값으로 | 같은 곳 |
| `24h` 설정에서 목록이 `19:30`이라 쓴다 | 같은 곳 |
| 빈 시작 시각으로 `시간`을 펼치면 올림한 정시가 들어간다. **값이 있으면 그대로** | `scheduleEditor` 테스트 |
| `시간`·`알림`·`반복`을 펼쳐도 달력이 그대로 있다 | 같은 곳 |
| Escape가 목록만 닫고 편집기는 남긴다 | 같은 곳 |
| 사용자 지정: `일 × 2 + 09:00` → `offsetMinutes 2880, allDayTime "09:00"` | `reminderCustom.test.tsx` |
| 사용자 지정의 미리보기가 목록의 괄호와 같은 시각을 쓴다 | 같은 곳 |

---

## 11. 결정된 것과 남은 것

**결정 [2026-09-03]**

1. **목록은 거르지 않고 스크롤만 한다**(§3.1). [관찰] 2번이 그렇게 읽힌다.
2. **반복도 함께 제자리 펼침으로**(§4.3). 한 카드 안의 세 줄이 서로 다르게 행동하지
   않도록.
3. **사용자 지정의 단위는 분 · 시간 · 일 · 주**(§6.1). 5번 이미지가 확정해 주었다 —
   추론이 아니라 관찰이 되었다.
4. **절대 시각은 그 셀렉트에 넣지 않고 목록의 형제로 남긴다**(§6.3).

**남은 것**

5. `Constant Reminder`와 타임존은 여전히 범위 밖(§9). 둘 다 알림 전달(Phase 11)의
   설계가 먼저다.
6. 사용자 지정의 숫자 칸 상한 — `99`쯤에서 막을지, 열어 둘지. 열어 두면 `9999주 전`이
   가능하고 `reminderMoment`는 그것을 계산해 낸다. 실물 보고 정한다.

---

## 12. 구현하며 정해진 것 (2026-09-03)

§8의 일곱 단계를 순서대로 구현했다. 설계에 없던 판단 넷을 남긴다.

### 12.1 하루 종일 일정에서는 사용자 지정도 `분`·`시간`을 내놓지 않는다

§6.1은 단위 넷을 무조건 내놓는 것처럼 읽힌다. 실물에서 확인해 보니 **하루 종일
일정에 `30분 전`을 저장하면 다음 편집에서 조용히 사라진다** — `reconcileReminders`가
§6.33대로 그것을 버린다. 폼이 고르게 하고 미리보기까지 보여 준 뒤 잃어버리는 셈이라,
`unitsFor(schedule)`을 두어 시각이 없는 일정에는 `일`·`주`만 준다. §6.11이 목록에
적용하는 규칙과 같은 것이고, 그래서 새 규칙이 아니라 이미 있던 규칙의 자리를 하나
더 찾은 것이다.

### 12.2 프리셋 라벨에서 시각을 뺐다

§5의 괄호가 `reminderMoment`를 쓰는 순간, `1일 전 오전 9시 (오전 9:00)`가 된다.
설계의 모형(`당일 (오전 9:00)`)이 답이었다: 라벨에서 시각을 빼고 괄호에 맡긴다.
요약 줄(§6.34)은 그래서 `1일 전`이라고만 쓴다.

### 12.3 빈 칸에는 "없음" 대신 그 칸의 이름

시간 줄에는 필드가 **둘**(시작·종료)이고 아이콘이 같다. 빈 종료 칸이 `없음`이라고
쓰면 시간이 없다는 것은 말하지만 어느 쪽인지는 말하지 않는다. 그래서 placeholder는
`시작`·`종료`다.

### 12.4 목록은 `PopoverContent`를 **형제로** 둔다

React는 자식의 layout effect를 부모의 ref보다 **먼저** 실행한다. 목록을 필드 박스의
자식으로 두면 열리는 그 커밋에서 앵커가 `null`로 측정되고, 측정 없는 surface는
`visibility: hidden`으로 그려진다 — 즉 **아무것도 안 뜬다.** 브라우저에서 그렇게
됐고, 형제로 옮겨 고쳤다(`TimeField.tsx`의 주석).

부수적으로 `Popover`가 버튼이 아닌 앵커를 받도록 `usePopoverAnchor`를 더했다.
콤보박스는 필드가 포커스를 쥔 채로 목록을 여는 위젯이라 `PopoverTrigger`(누르면
포커스를 가져가는 버튼)로는 만들 수 없다.

### 12.5 이름이 바뀐 파일

| 전 | 후 | 왜 |
|---|---|---|
| `TimePanel.tsx` | `TimeRow.tsx` | 더 이상 패널이 아니다 |
| `ReminderPanel.tsx` | `ReminderList.tsx` | 같은 이유 |
| `ChoicePanel.tsx` | (삭제) | 반복은 `ScheduleEditor` 안의 `RepeatChoices` 하나뿐이다 |

`schedule.timeNote`도 지웠다 — "저장은 달력 화면의 확인에서"라고 쓰여 있었고,
갈아탈 달력 화면이 없어졌다.

### 12.6 여전히 남은 것

§11.6(숫자 칸 상한)은 그대로 열려 있다. 실물에서 `9999주 전`은 계산되고 저장된다.
