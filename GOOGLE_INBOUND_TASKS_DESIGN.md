# 전용 캘린더에서 온 일정은 작업이 된다

> 상태: **구현 기준 정책 정리** · 2026-09-08. 사용자 평가로 B1·B2·B4·B6·B8 유지,
> B3·B7 보강, B5·B9 수정. 구체 정책을 정하라는 후속 요청에 따라 §12에 구현 기본값을 정했다.
> 아직 구현하지 않았다. **§12 → §11 → 최초 초안 순으로 우선한다.**
> 요청: "구글 캘린더에서 추가를 하면 FocusFlow에 추가가 안 돼." → **B안: 들어온 것을
> 작업으로 승격한다.**
> 대상: `domain/calendar/googleSync/taskInboundPlan.ts`(신규) ·
> `domain/calendar/googleSync/inboundShape.ts` · `hooks/useGoogleInboundSync.ts` ·
> `hooks/usePlannerData.ts` · `lib/googleCalendarSources.ts` ·
> `supabase/migrations/021_google_inbound_cursor.sql`(신규)
> 선행 문서: `GOOGLE_CALENDAR_SYNC_DESIGN.md`(§6 인바운드 · §7 delete wins · §5.3 LWW —
> 이 문서는 그 P3 결정을 **뒤집는다**) · `GOOGLE_CALENDAR_LIST_LABELS_AND_TAGS_DESIGN.md` §3.8

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 사용자 — 구글에서 만든 일정이 앱에 안 들어온다 · 중복 스크린샷(이 문서의 R2가 그 잔해를 다룬다) | [관찰] |
| **실측** | 인바운드는 `ExternalState`만 건드리고 `tasks`를 쓰는 줄이 없다 · 전용 캘린더는 소스 목록에서 빠졌다(`dc98cbc` 직전 커밋) · `google_calendar_sources`의 커서는 계정 것이다 · RRULE → `Task.repeatType` 파서는 **없다**(반대 방향만 있다) · 태스크는 Supabase로 계정 단위 동기화된다 | [실측] |
| **결정** | 사용자 — B안(승격) | [결정] |
| **미확인** | 이미 생긴 중복 이벤트가 새 작업으로 밀려들 위험의 실제 규모 (§9 R2) | [미확인] |

---

## 1. 무엇이 뒤집히는가 [실측]

선행 문서 P3은 이렇게 정했다: **"구글에서 온 것은 `ExternalCalendarEvent`이고 `Task`
모델을 건드리지 않는다."** Q2도 "승격은 v1에 불필요"였다.

그 결정의 근거는 **범위**였다 — 구글에서 읽는 캘린더는 개인·업무·공유 달력이고, 남의
회의를 내 할 일 목록에 밀어 넣는 것은 아무도 원하지 않는다. 그 근거는 지금도 옳다.

바뀐 것은 읽는 대상이다. **전용 FocusFlow 캘린더는 우리가 만든 것이고 우리 것만 있다.**
거기 있는 일정은 정의상 이 앱의 작업이거나, 사용자가 이 앱을 위해 적어 넣은 것이다.
그러므로 뒤집는 것은 P3 전체가 아니라 **한 캘린더에 대한 예외**다.

| 캘린더 | 들어오면 | 왜 |
|---|---|---|
| 전용 FocusFlow | **작업** | 우리 것이다. 남의 일정이 섞이지 않는다 |
| 사용자가 고른 다른 캘린더 | 외부 일정 (지금 그대로) | 남의 회의는 내 할 일이 아니다 |

---

## 2. 결정 요약

| # | 결정 | 근거 절 |
|---|---|---|
| **B1** | 승격은 **전용 캘린더에만** 적용한다. 다른 캘린더는 지금 그대로 | §1 |
| **B2** | 전용 캘린더는 **항상** 읽는다. 켜고 끄는 설정을 두지 않는다 | §3.1 |
| **B3** | 계정·연결 세대·캘린더·이벤트 ID로 매핑하고 서버에서 중복 생성을 막는다 | §11.1 |
| **B4** | 들어오는 필드는 **제목·날짜·시간·설명** 넷뿐 | §4.3 |
| **B5** | 공통 동기화 기준값으로 변경 여부를 판정한다. 양쪽 변경은 보존하고 해결 전 전송을 보류한다 | §11.2 |
| **B6** | 구글에서 지운 일정은 작업을 **휴지통으로** 보낸다. 완전 삭제가 아니다 | §6 |
| **B7** | 새 반복 원본과 회차는 승격하지 않는다. 회차 삭제로 원본 작업을 지우지 않는다 | §11.3 |
| **B8** | 새 작업은 **Inbox**로 간다 | §4.2 |
| **B9** | 기존 연결 행의 `sync_token`을 재사용한다. 작업·매핑·충돌 기록과 원자적으로 확정한다 | §11.4 |

---

## 3. 어디서 읽는가

> §3~§10은 최초 제안의 근거 기록이다. §11과 충돌하는 내용은 구현 계약으로 사용하지 않는다.

### 3.1 전용 캘린더는 목록에 없고, 항상 읽힌다 [B2]

전용 캘린더는 사용자가 고르는 소스 목록에서 뺐다 — 켜면 우리가 내보낸 이벤트가 외부
일정으로 되돌아와 격자에 같은 일이 두 번 그려지기 때문이다.

그 이유는 **외부 일정으로** 읽을 때의 이야기다. 작업으로 읽으면 되돌아온 것은 자기
작업과 만나 하나로 합쳐진다(§4.1). 그러므로 목록에서 뺀 것은 그대로 두고 — 켜고 끌
것이 아니다 — 인바운드 패스가 **따로** 읽는다.

설정을 두지 않는 이유: 이 캘린더를 안 읽겠다는 것은 "구글에서 고친 것을 안 받겠다"는
뜻인데, 그건 연결을 끊는 것과 같다. 이미 있는 스위치다.

### 3.2 커서는 연결 행에 [B9]

`google_calendar_sources`는 **사용자가 고른 읽기 소스**의 표다. 전용 캘린더는 그 표의
행이 아니므로, 커서는 연결 행에 칼럼 하나로 붙인다.

```sql
alter table public.google_calendar_connections
  add column if not exists inbound_sync_token text;
```

`google_calendar_sources`의 커서가 기기별 저장소와 어긋나 생긴 문제(`c79624f`)는 여기
없다 — **작업은 계정 단위로 동기화된다.** 한 기기가 받아 작업으로 만들면 다른 기기는
Supabase로 그것을 본다. 커서가 계정 것인 게 맞다.

---

## 4. 들어온 것을 무엇으로 만드는가

### 4.1 id가 답한다 [B3]

```
구글 이벤트 하나
  ├ id가 어느 Task의 googleEventId다  → 그 Task를 고친다 (§5)
  ├ etag가 그 Task의 googleEtag와 같다 → 우리가 방금 쓴 것. 아무것도 안 한다
  └ 모르는 id                          → 새 Task (§4.2)
```

가운데 줄이 에코 방지다. 지금 외부 일정 경로가 쓰는 것과 같은 규칙이고
(`inboundPlan` §6.3), 다른 것은 비교 대상이 `ExternalCalendarEvent`가 아니라 `Task`라는
점뿐이다.

**이 판정은 순수 함수 하나로 짠다** — `planTaskInbound(items, tasks, options)`가 세
더미(`create` · `update` · `trash`)를 낸다. 구글 계정 없이 픽스처로 증명된다.

### 4.2 새 작업의 모양 [B8]

| 필드 | 값 |
|---|---|
| `title` | `summary`, 없으면 `(제목 없음)` |
| `dueDate` | 시작 날짜 (뷰어 시간대로 읽은 것) |
| `startTime` / `endTime` | 시간이 있는 일정일 때만. 종일이면 빈 문자열 |
| `description` | 구글의 `description` 그대로 |
| `listId` | **Inbox** (`inboxListId`) |
| `googleEventId` / `googleEtag` / `googleSyncedAt` | 방금 읽은 값 |
| 그 외 | `normalizeTask`의 기본값 |

`googleSyncedAt`을 **반드시 채운다.** 비워 두면 다음 아웃바운드 패스가 이 작업을
"한 번도 안 보낸 것"으로 보고 구글에 **또 만든다.** 방금 고친 중복 버그와 같은 모양이
반대편에서 나는 것이다.

`metadataKey`도 마찬가지다 — 지금 계산되는 값과 같게 채워 두지 않으면 한 번의 불필요한
PATCH가 따라온다. 데이터는 안 깨지지만 조용한 왕복 한 번이다.

### 4.3 넷만 들어온다 [B4]

제목 · 날짜 · 시간 · 설명. 우선순위 · 리스트 · 태그 · 하위작업은 **구글에 없다.**
라벨은 구글에 있지만 우리가 소유한다(선행 문서 §3.8) — 구글에서 라벨을 바꿔도 리스트는
안 옮긴다. 그 규칙은 그대로다.

### 4.4 반복은 승격하지 않는다 [B7]

`toGoogleEventBody`가 우리 반복을 RRULE로 쓰는 코드는 있지만 **반대 방향 파서는 없다**
[실측]. `parseRRule`은 `IcsRecurrence`를 만들 뿐이고, 그것은 그리기용이지 `Task`의
`repeatType`이 아니다.

그러므로 구글에서 만든 **반복** 일정은 작업이 되지 않는다. 무시하는 것이 아니라
**건너뛰고 카드에 한 줄로 고지한다** — 조용히 사라지면 "동기화가 됐다는데 없다"가 된다.

우리가 만든 반복 작업의 이벤트는 이미 매핑이 있으므로 §4.1의 첫 줄로 흐른다. 다만
그 편집은 §5의 규칙 아래 **반복 규칙만 빼고** 적용한다. 반복은 여전히 단방향이다.

---

## 5. 충돌 — 나중이 이긴다 [B5]

```
event.updated  vs  task.updatedAt
```

| 상황 | 하는 일 |
|---|---|
| `event.updated > task.updatedAt` | 들여온다. `googleSyncedAt`과 `googleEtag`를 새 값으로 |
| `event.updated <= task.updatedAt` | 들여오지 않는다. **`googleSyncedAt`은 갱신한다** |

둘째 줄의 갱신이 없으면 같은 이벤트를 매 패스마다 다시 보고 매번 지는 판정을 반복한다 —
선행 문서 M1이 412에서 이미 밟은 함정이고, 같은 해법을 쓴다.

교차 필드 손실은 남는다: 구글에서 시간을, 앱에서 제목을 같은 창에 고치면 나중 것이
통째로 이긴다(선행 문서 R4). 필드 단위 병합은 이 문서의 범위가 아니다 — 연결 시점
고지에 한 줄이 이미 있다.

---

## 6. 삭제 [B6]

| 어디서 | 결과 |
|---|---|
| 구글에서 일정 삭제 (`status: "cancelled"`) | 작업을 **휴지통으로** |
| 앱에서 작업 휴지통 | 이벤트 삭제 (지금 그대로) |
| 앱에서 완전 삭제 | 툼스톤 → 이벤트 삭제 (지금 그대로) |

**완전 삭제가 아니라 휴지통인 이유:** 구글에서의 삭제 한 번이 앱의 데이터를 되돌릴 수
없게 지우면, 그 실수는 복구할 방법이 없다. 휴지통은 이 앱이 이미 가진 되돌릴 자리다.

**부재는 삭제가 아니다.** 목록에 없다는 것으로 지우지 않는다 — `cancelled`라는 명시적
응답만 삭제로 읽는다(선행 문서 §7.1). 410으로 커서가 날아가 전체를 다시 받는 순간이
정확히 그 함정이 열리는 때다.

---

## 7. 무엇이 바뀌는가

| 파일 | 변화 |
|---|---|
| `domain/calendar/googleSync/taskInboundPlan.ts` | **신규.** 순수. `create`·`update`·`trash` 세 더미 (§4.1) |
| `domain/calendar/googleSync/inboundShape.ts` | 이벤트 → 작업 필드 변환 하나 추가. 시간대 처리는 있는 것을 쓴다 |
| `hooks/useGoogleInboundSync.ts` | 전용 캘린더를 따로 읽고, 결과를 작업 쪽으로 보낸다 |
| `hooks/usePlannerData.ts` | `applyGoogleInboundTasks` — 만들기·고치기·휴지통을 한 번의 `setData`로 |
| `lib/googleCalendarSources.ts` | 연결 행의 커서 읽기/쓰기 |
| `supabase/migrations/021_…sql` | 칼럼 하나 (§3.2) |
| `components/calendar/GoogleCalendarCard.tsx` | 반복 일정을 건너뛴 개수 한 줄 (§4.4) |

---

## 8. 단계

| # | 항목 | 선행 |
|---|---|---|
| 1 | `planTaskInbound` — 순수, 픽스처로 세 더미 증명 (§4.1, §5, §6) | 없음 |
| 2 | 마이그레이션 021 + 커서 읽기/쓰기 (§3.2) | 없음 |
| 3 | 인바운드 훅이 전용 캘린더를 읽고 작업에 적용 (§4.2) | 1·2 |
| 4 | 반복 건너뜀 고지 (§4.4) | 3 |

1이 이 기능의 전부에 가깝다. 3은 그 위의 I/O 껍데기다 — 선행 문서 M1이 `eventShape`와
OAuth를 그 순서로 쌓은 것과 같은 이유다.

---

## 9. 위험

| # | 무엇 | 대응 |
|---|---|---|
| **R1** | 승격이 켜진 첫 패스가 전체 목록을 받는다 | 전용 캘린더의 이벤트는 거의 전부 우리 id다 → §4.1의 첫 줄로 흘러 합쳐진다. 대량 생성이 나지 않는다 |
| **R2** | **방금 고친 중복 버그의 잔해** [미확인] | 중복 이벤트는 우리가 모르는 id다 → 그대로 두면 **작업이 새로 생긴다.** §9.1의 가드 |
| R3 | 구글에서 한 삭제가 작업을 지운다 | §6 — 휴지통이지 완전 삭제가 아니다 |
| R4 | 교차 필드 편집 손실 | §5 — 알려진 성질, 연결 시점 고지 |
| R5 | 반복 일정이 조용히 사라진다 | §4.4 — 건너뛰고 개수를 말한다 |

### 9.1 R2의 가드

모르는 id인데 **이미 매핑된 어떤 작업과 제목·시작·끝이 같으면** 새 작업을 만들지 않는다.
그것은 사람이 적은 일정이 아니라 우리가 두 번 쓴 이벤트다.

이 가드는 승격이 켜지는 순간에만 값이 있고 그 뒤로는 거의 안 걸린다. 그래도 남긴다 —
비용은 비교 한 번이고, 없을 때의 비용은 사용자의 목록에 유령 작업이 쌓이는 것이다.

사용자에게는 여전히 **구글에서 중복 일정을 지우라고** 안내한다. 가드는 안전망이지
청소가 아니다.

---

## 10. 열린 질문

| # | 질문 | 기본값 제안 |
|---|---|---|
| Q1 | 승격된 작업을 목록에서 구분해 보일 것인가 | 구분하지 않는다. 어디서 왔든 작업은 작업이다 |
| Q2 | 종일 일정이 여러 날에 걸치면 | 시작일을 `dueDate`로. 우리 모델에 기간 작업이 없다 |
| Q3 | 구글에서 제목을 비우면 | `(제목 없음)`. 빈 제목은 목록에서 누를 수 없는 행이 된다 |
| Q4 | 휴지통에 있는 작업의 이벤트가 구글에서 되살아나면 | 휴지통에서 꺼내지 않는다. 되살리기는 사람의 결정이다 |

---

## 11. 사용자 평가 반영 — 구현에 적용할 수정안

### 11.1 B3 — ID 매핑과 동시 기기

로컬 `tasks`에서 ID를 못 찾았다는 것만으로 새 작업을 만들지 않는다. 다른 기기의 작업이
아직 내려오지 않았을 수 있다. 서버의 매핑을 최종 기준으로 삼는다.

- 매핑 키는 `(user_id, connection_generation, calendar_id, event_id)`이며 서버 UNIQUE
  제약을 둔다. 연결 세대는 재연결할 때 바뀌고 커서 갱신 때는 바뀌지 않는다.
- 기존 Task의 `googleEventId`도 새 매핑으로 등록해야 한다. 기존 연결의 소유 관계를
  확인하고 중복 매핑을 검사한 뒤 승격을 켠다. 모호한 매핑은 자동으로 하나를 고르지 않는다.
- 작업 생성과 매핑 등록은 같은 서버 트랜잭션이다. 두 기기가 같은 이벤트를 받으면
  하나만 생성하고 다른 기기는 확정된 매핑과 작업을 다시 읽는다.
- 갱신 계획은 읽었던 작업의 서버 revision을 포함한다. 적용 시 revision이 바뀌었으면
  재조회·재계산한다. 인바운드뿐 아니라 일반 작업 저장 경로도 같은 revision 검사를
  지켜야 한다. 기존의 무조건 upsert가 남으면 늦은 저장이 새 값을 덮을 수 있다.
- 아웃바운드 생성도 같은 매핑 계약에 참여한다. 서버 조정과 재시도 시 동일한 이벤트 ID를
  사용하는 멱등 생성이 필요하다. 브라우저 탭 잠금만으로 기기 간 생성을 막았다고 보지 않는다.
- 휴지통 작업과 완전 삭제 툼스톤도 매핑 조회에 포함해 재생성을 막는다.

§9.1의 제목·시작·끝 일치는 **중복 후보**일 뿐이다. 사람이 같은 일정 두 개를 만들 수도
있으므로 이것만으로 가져오기를 영구 생략하지 않는다. 출처가 확인되지 않는 후보는
검토 대기 기록으로 보존하고 사용자에게 알린다. Google 이벤트를 자동 삭제하지 않는다.

### 11.2 B5 — 시각 비교 대신 공통 기준값

`event.updated`는 Google의 시각이고 `task.updatedAt`은 기기 시각일 수 있다.
또 작업의 태그·우선순위 변경은 공유 필드의 변경과 다르다. 따라서 두 시각을 비교해
내용을 버리는 §5의 LWW를 사용하지 않는다. `updated`는 진단 정보로만 보관한다.

마지막으로 양쪽에서 확인된 **공유 필드의 정규화된 값**을 `base`로 저장한다.
공유 필드는 제목·일정·설명이며 일정은 날짜·시간·종일 여부를 묶어서 비교한다.
같은 시간대 변환·빈 값·날짜 규칙으로 정규화한 `local`, `remote`를 `base`와 비교한다.
v1은 공유 필드 묶음 단위로 판정하며 자동 필드별 병합은 하지 않는다.

| 조건 | 처리 |
|---|---|
| `local = base`, `remote = base` | 내용 변경 없음 |
| `local = base`, `remote != base` | 원격 공유 필드 적용. 앱 소유 필드 유지 |
| `local != base`, `remote = base` | 로컬 편집 유지, 아웃바운드 대기 유지 |
| `local = remote` | 같은 결과로 수렴. 그 값을 새 기준으로 확정 |
| 양쪽 변경이고 서로 다름 | 양쪽 값과 base를 서버에 보존하고 해당 작업 전송 보류 |
| 기존 매핑에 base 없음 | 같으면 기준 초기화. 다르면 충돌로 보존; 시각으로 추측하지 않음 |

충돌은 카드의 개수와 해결 화면으로 노출한다. 사용자가 앱 값 또는 Google 값을 선택하면
현재 revision/etag를 재검사하고 적용한다. 그 사이 값이 바뀌었으면 다시 판정한다.
아웃바운드의 기존 412 처리도 이 계약을 사용해야 한다. 인바운드만 바꾸면 다음 전송이
충돌 값을 덮을 수 있다. 성공 응답을 받을 때도 전송 중 생긴 새 로컬 편집을 보존한다.

`etag`는 동일 버전 확인에 사용하며 크기나 시각처럼 정렬하지 않는다. 같은 etag의
응답은 원격 내용 변경이 없다는 뜻이고, 로컬 변경까지 동기화됐다는 뜻은 아니다.

**기존 메타데이터의 의미를 바꾸지 않는다.** 현재 `googleSyncedAt`은 마지막으로 전송한
Task의 `updatedAt`이다 (`googleCalendarOutbound.ts`, `outboundPlan.ts`). 수신 시각이나
`event.updated`를 넣지 않는다. base와 로컬 dirty 상태를 명시적으로 저장하고 아웃바운드
변경 감지를 함께 수정한다. base/동기화 완료 표시는 실제 저장·전송 성공한 내용에만 붙인다.
`googleMetadataKey`도 Google 라벨이 실제로 일치하거나 전송 성공했을 때만 확정한다.

### 11.3 B7 — 반복 원본과 회차를 먼저 분류

Google의 `recurrence`는 원본의 반복 규칙이고, `recurringEventId`는 회차가 속한 원본을
가리킨다. 회차에는 `originalStartTime`도 사용된다. 취소된 회차는 일반 일정의 필드가
없을 수 있다. [Google Events 공식 문서](https://developers.google.com/workspace/calendar/api/v3/reference/events)

| 종류 | v1 처리 |
|---|---|
| 미매핑 단발 일정 | Inbox 승격 |
| 미매핑 반복 원본 (`recurrence`) | 승격 생략, 이유와 ID 기록 |
| 반복 회차·예외 (`recurringEventId` 또는 `originalStartTime`) | 승격·원본 갱신 생략 |
| 기존 작업에 정확히 매핑된 반복 원본 | 공유 필드만 §11.2로 판정. 반복 규칙 유지 |
| 취소된 회차 | 생략. `recurringEventId`로 원본 작업을 찾아 휴지통에 보내지 않음 |
| 취소된 단발/원본 ID가 작업과 정확히 매핑됨 | 휴지통 이동. 이미 삭제된 작업은 유지 |
| 모르는 취소 ID | 작업 생성·삭제 없이 처리 기록만 남김 |

**회차 분류가 일반 cancelled 처리보다 먼저다.** 원본의 취소 응답에 반복 규칙이 없어도
정확한 매핑으로 판정할 수 있어야 한다. 지원하지 않는 회차는 서버에 ID·이유를 기록해
재수신 시 개수를 중복 가산하지 않는다. 고지는 반복 원본과 회차를 구분한다.

### 11.4 B9 — 작업과 커서는 같은 확정 단위

실제 `017_google_calendar.sql`에는 연결 행의 **`sync_token`이 이미 있다.**
§3.2의 `inbound_sync_token` 추가 SQL은 폐기한다. 021은 커서 한 칼럼 추가가 아니라
매핑·base·revision·연결 세대·처리 결과 저장과 원자적 적용 경로를 위한 마이그레이션이다.

1. 현재 연결 세대, 캘린더 ID, 커서, 연결의 동기화 revision을 읽는다.
2. 같은 조회 조건으로 모든 페이지를 받는다. 중간 `pageToken`을 최종 커서로 저장하지 않는다.
3. 최신 서버 작업에 대해 계획을 계산하고 revision 전제조건을 붙인다.
4. 서버 트랜잭션에서 연결 전제조건과 작업 revision을 검사한다. 작업·매핑·base·충돌 및
   생략/검토 기록을 저장한 뒤 마지막 `nextSyncToken`과 동기화 revision을 함께 확정한다.
5. 경합하면 트랜잭션 전체를 취소하고 최신 상태에서 재계산한다. 클라이언트는 커밋 결과를 반영한다.

로컬 `setData` 성공은 서버 저장 성공이 아니다. §7의 한 번의 `setData`만으로 완료되지 않는다.
원자적 저장 경로가 준비되기 전에는 계정 공용 커서를 전진시키지 않는다. 적용 RPC는 인증된
계정에만 쓰고 기존 일반 upsert 경로가 revision/연결 검사를 우회하지 않도록 바꾼다.

페이지 수신 실패·잘못된 데이터·서버 저장 실패는 커서 전진을 막는다. 충돌과 의도적인 생략은
원본/이유를 나중에 처리할 수 있게 영속 저장한 경우에만 처리 완료로 센다. 커밋 후 응답 유실은
같은 패스 ID로 재시도하거나 서버 확정 상태를 조회해 해결한다. 연결 해제·재연결 이전 응답은
세대 검사로 거절한다. 커서 문자열 자체의 대소 비교로 최신 여부를 판단하지 않는다.

410이면 전체 재조회하되 기존 Task·매핑·base는 지우지 않는다. 전체 응답에 없다는 이유로
작업을 삭제하지 않는다. 새 전체 조회 결과가 확정될 때 커서를 교체한다. 페이지 처리와 최종
토큰, 410의 재동기화 조건은 [Google 동기화 문서](https://developers.google.com/workspace/calendar/api/guides/sync)를 따른다.

### 11.5 수정된 구현 순서와 완료 기준

1. **공유 필드/base·매핑·revision 계약 및 순수 계획 함수 테스트.** 계획 결과는
   `create/update/trash` 외에 `conflict/skipped/review`와 전제조건을 포함한다.
2. **서버 저장 경로와 021.** 기존 매핑 이관, 일반 작업 저장 revision 검사,
   작업·처리 기록·커서 트랜잭션, 연결 세대 검사를 구현한다.
3. **인바운드·아웃바운드 연결.** 공통 base와 충돌 정책, 멱등 생성, 전송 중 편집 보호를 적용한다.
4. **충돌 해결 및 생략 안내 화면.** 기존 연결 안내의 LWW 문구도 실제 정책으로 바꾼다.

필수 검증은 두 기기의 동일 이벤트 생성 경합, 일반 편집과 인바운드 저장 경합,
인바운드와 아웃바운드 동시 실행, 시계 오차·앱 전용 필드 변경·base 없는 기존 매핑,
반복 원본/회차 수정·회차 취소·원본 취소, 여러 페이지 중간 실패, 서버 저장 실패,
커밋 후 응답 유실, 410 전체 재조회, 재연결 후 늦은 응답이다.
각 실패에서 **사용자 편집이 사라지지 않고 작업이 중복 생성되지 않으며 미처리 변경을
커서가 건너뛰지 않는지** 검증한다. 순수 함수 테스트만으로 동시 기기 저장 계약이
완료됐다고 판단하지 않는다.

---

## 12. 구체 정책 — v1 구현 기본값

이 절은 §10의 열린 질문과 §11의 선택지를 닫는다. 기술적인 저장 방식과 사용자가 보는
동작을 함께 정한다. 아래 정책은 이번 작업에서 정한 구현 기준이며 사용자 평가표의
직접 인용은 아니다.

### 12.1 동기화 범위와 시점

- 전용 캘린더만 Task로 양방향 동기화한다. 다른 선택 캘린더의 외부 일정 동작은 유지한다.
- 연결 완료, 로그인 후 초기 데이터 수신 완료, 창 재포커스, 수동 새로고침에 실행한다.
  정기 폴링은 v1에 추가하지 않는다. 실행 중 같은 연결의 트리거는 한 번으로 합친다.
- 한 연결의 패스는 인바운드 확정 후 아웃바운드 순서다. 기기 간에는 서버의 연결 실행권을
  사용한다. 실행권은 120초 임대, 30초마다 갱신하고 상실하면 새 요청을 중단한다.
  만료된 실행자의 저장은 증가하는 실행 번호로 거절한다. 네트워크 중 이미 전송된 요청은
  취소됐다고 가정하지 않으며 멱등 ID·조건부 쓰기·다음 재조회로 처리한다.
- 자동 실행의 일시적 네트워크 실패는 5초·30초·120초 간격으로 최대 세 번 재시도한다.
  서버가 재시도 시점을 명시하면 그 시점보다 일찍 보내지 않는다. 인증 실패는 재연결 안내,
  그 외 지속 실패는 카드에 표시하고 다음 트리거를 기다린다.

### 12.2 충돌에서 사용자가 보는 것

- v1은 **제목·일정·설명 묶음 전체**로 비교한다. 한쪽만 바뀌면 자동 반영하고,
  양쪽 결과가 다르면 자동 승자를 정하지 않는다. 필드별 혼합 선택은 v1에 넣지 않는다.
- 충돌 중 앱에는 현재 앱 값을 보여준다. 다른 작업은 계속 동기화하고 해당 작업의
  공유 필드·라벨·반복 규칙 전송은 보류한다. 명시적인 삭제는 §12.4를 따른다.
- 캘린더 연결 카드에 `동기화 충돌 N개 · 확인`을 표시한다. 상세에는 `앱 내용 사용`,
  `Google 내용 사용`, `나중에`를 제공하고 두 버전의 제목·일정·설명을 비교해 보여준다.
- 선택은 로컬 revision, 원격 etag, 충돌 revision이 여전히 같은 경우에만 적용한다.
  달라졌으면 최신 두 버전을 보여주고 `내용이 변경되어 다시 확인이 필요합니다`라고 안내한다.
- 앱 값을 선택하면 원격 최신 버전에 조건부 전송한다. Google 값을 선택하면 공유 필드만
  앱에 적용한다. 어느 쪽이든 서버 확정/Google 쓰기 성공 전에는 해결 완료로 표시하지 않는다.
- 해결하지 않은 충돌과 후보 기록은 자동 만료하지 않는다. 해결된 충돌의 비교 사본은
  30일 후 삭제하고, 매핑·base 및 필요한 삭제 기록은 별도로 유지한다.

### 12.3 중복 후보와 기존 매핑

- 정확한 매핑은 자동 갱신한다. 알려지지 않은 ID만 생성 후보가 된다.
- 기존 매핑 작업과 정규화한 제목·시작·끝이 일치하면 `중복 확인 N개`에 보류한다.
  유사한 제목 검색이나 설명 비교로 임의 병합하지 않는다.
- 선택지는 `별도 작업으로 가져오기`, `이 일정 가져오지 않기`, `나중에`다.
  `가져오지 않기`는 해당 연결·캘린더·이벤트 ID를 제외 목록에 저장한다. Google 원본은
  그대로 두고 카드의 제외 목록에서 사용자가 다시 가져오기를 선택할 수 있게 한다.
- 기존 작업에 두 번째 활성 이벤트를 연결하는 병합 기능은 v1에 넣지 않는다.
- 미매핑 이벤트끼리 제목·시간이 같다는 이유로 합치지 않는다. 서로 다른 두 일정일 수 있다.
- 이관 중 하나의 이벤트 ID에 여러 Task가 연결된 경우 해당 매핑만 검토 대상으로 두고
  관련 자동 쓰기를 보류한다. 정상 매핑의 동기화는 계속한다.

### 12.4 삭제와 복구

- 정확히 매핑된 원본/단발 이벤트의 명시적 취소는 **삭제 우선**이다. 미전송 편집이 있어도
  그 편집이 포함된 현재 Task를 휴지통으로 이동한다. 내용은 보존한다.
- 모르는 취소 ID는 삭제 기록만 남긴다. 응답의 부재나 조회 404만으로 Task를 지우지 않는다.
- Google에서 되살아난 일정은 휴지통 Task를 자동 복구하지 않는다. 같은 이벤트를 새 Task로
  만들지도 않는다. `Google에서 복구된 일정` 검토 항목을 남긴다.
- 앱에서 사용자가 복구하면 살아 있는 동일 이벤트를 먼저 조회해 재연결 가능 여부를
  확인한다. 취소됐거나 없으면 새 이벤트 ID로 만든다. 양쪽 내용이 다르면 §12.2를 적용한다.
- 완전 삭제된 Task는 자동 복구하지 않는다. 옛 이벤트 매핑의 삭제 표식을 연결 수명 동안
  남겨 재생성을 막는다. 미처리 Google 삭제 요청도 성공 전에 버리지 않는다.

### 12.5 반복과 표현할 수 없는 일정

- 신규 반복 원본·회차·예외는 모두 작업으로 만들지 않는다. 기존 매핑 원본의 공유 필드는
  동기화하되 Google에서 수정한 반복 규칙은 가져오지 않는다.
- 회차 취소는 원본 작업을 지우지 않는다. 반복 규칙 차이를 발견하면 원본에 대해
  `반복 규칙은 앱에서 변경해 주세요`를 표시한다. 일반 공유 필드 PATCH에는 recurrence를
  넣지 않는다. 앱에서 반복 규칙을 명시적으로 편집했을 때만 그 규칙을 내보낸다.
- 반복을 지원하지 않아 생략한 항목은 카드에서 원본/회차별 개수와 사유를 확인할 수 있다.
  같은 ID의 재수신은 개수를 누적하지 않는다. 그 이벤트가 지원 가능한 단발로 바뀌면 재평가한다.
- **초안 Q2를 정정한다.** 현재 Task는 `startDate`~`dueDate`로 여러 날 종일 일정을
  표현하며 기존 `eventShape.ts`도 이를 내보낸다. 들어오는 여러 날 종일 일정은
  `startDate = 시작일`, `dueDate = Google 종료일의 전날`로 보존한다. 하루짜리는
  `dueDate = 시작일`, 시간은 비우고 불필요한 기간 시작일을 만들지 않는다.
- 현재 아웃바운드는 시간이 있는 일정을 같은 `dueDate` 위에 쓴다. 자정을 넘는 시간 일정,
  초 단위 정밀도 등 왕복 변환으로 원본이 손실되는 일정은 v1에서 자동 승격/갱신하지 않는다.
  원본과 `현재 지원하지 않는 일정 형식` 사유를 저장하고 사용자에게 표시한다.
  시간·기간을 조용히 잘라서 가져오지 않는다.

### 12.6 시간대·본문·작업 기본값

- 동기화 시간대는 연결 시 캘린더의 IANA 시간대를 연결 행에 고정한다. 기기의 현재 시간대를
  변환 기준으로 쓰지 않는다. 출장이 다른 기기의 Task 날짜를 바꾸어서는 안 된다.
  시간대 변경/불일치를 감지하면 자동 재해석을 보류하고 연결 카드에서 확인하게 한다.
- 종일 값은 날짜로 보존하고 시간대 변환을 하지 않는다. 시간 있는 일정은 고정한 연결
  시간대로 변환하고, 정규화 후 원래 순간으로 되돌릴 수 없는 값은 지원 불가로 분류한다.
- 새 Task는 계정의 canonical Inbox로 간다. 우선순위·태그·완료 여부·하위작업·알림은
  앱 기본값을 쓰며 Google 색·라벨에서 추정하지 않는다. 기존 Task의 앱 전용 필드는 유지한다.
- 비어 있거나 공백뿐인 제목은 `(제목 없음)`으로 표시한다. 원격 원문을 base에 함께 보존해
  이 표시용 기본값만으로 Google 제목을 덮어쓰지 않는다.
- 설명은 원문 보존이 원칙이다. 인바운드 설명의 `#`을 태그로 해석하지 않는다.
  Google HTML을 실행 가능한 DOM으로 삽입하지 않는다. 비교는 줄바꿈 형태만 통일하고
  공백·링크·본문 의미를 임의로 제거하지 않는다.
- 가져온 작업을 목록에서 별도 그룹으로 나누지 않는다. 연결/충돌 정보는 상세와 연결 카드에 둔다.

### 12.7 저장·재연결의 최종 규칙

- 페이지를 전부 받은 후 작업·매핑·base·충돌·생략·제외 상태와 최종 커서를 **한 번의 서버
  트랜잭션**으로 확정한다. 로컬 전용 저장이나 부분 성공이면 커서를 전진시키지 않는다.
- 처리할 수 없는 이벤트도 ID와 원본 및 사유를 내구성 있게 저장했으면 커서를 전진시킬 수
  있다. ID가 없어 재처리를 식별할 수 없거나 저장 자체가 실패했으면 패스 전체를 실패시킨다.
- 같은 패스의 재시도는 같은 pass ID를 쓴다. 연결 세대, 실행 번호, 읽었던 sync revision을
  확인해 늦은 패스가 더 새 커서를 덮지 못하게 한다. Google 호출은 DB 트랜잭션 밖에서 수행한다.
- 410은 전체 재조회이며 Task 삭제/매핑 초기화가 아니다. 새 전체 결과가 커밋되기 전에는
  이전 저장 상태를 보존한다.
- 연결 해제는 동기화와 대기 요청을 중단할 뿐 Task나 Google 일정을 삭제하지 않는다.
  재연결은 새 세대로 시작한다. **동일한 검증된 Google 계정 식별자와 캘린더 ID**일 때만
  기존 매핑·제외 기록을 새 세대로 이관하고 커서는 초기화한다. 이메일 문자열만으로 판단하지 않는다.
- 다른 계정/캘린더라면 기존 Google 연결 작업은 앱에 보존하되 새 캘린더에 자동 대량 전송하지
  않는다. 이관할 작업을 사용자가 선택하는 별도 흐름으로 둔다. 옛 삭제 표식도 새 계정에 적용하지 않는다.

### 12.8 구현 시작 조건

첫 구현 단위는 §12 정책을 표현하는 타입·순수 판정 함수·fixture 테스트다.
이어서 revision을 우회하는 기존 저장 경로와 구버전 클라이언트 쓰기를 조사하고,
서버에서 이를 차단/검증할 수 있는 호환성 전환을 먼저 구현한다. 지원하지 않는 구버전에는
업데이트 필요를 표시한다. 그 준비 없이 새 인바운드를 활성화하지 않는다.
실제 활성화는 서버 마이그레이션·매핑 이관·동시 기기 통합 테스트까지 통과한 후에 한다.

## 13. 구현 현황

- 1차: `taskInboundShape.ts`와 `taskInboundPlan.ts`의 순수 변환·판정 구현.
  결과에는 서버 scope/revision 전제조건과 원격 원문을 포함한다. 생성 ID 발급이나 저장은 하지 않는다.
- 공유 필드 base 비교, 충돌 양쪽 보존, 중복 후보, 명시적 삭제, 반복 회차 우선 분류,
  날짜 범위·고정 시간대 변환, 서머타임 중복 시각 및 손실 변환 거절을 fixture로 검증한다.
- 2차: 마이그레이션 021 작성. 서버 매핑/처리 기록, Task revision, 계정 활성화 게이트,
  실행권·fence, 일반 Task revision RPC, 원자적 작업/커서 확정 및 멱등 receipt를 구현했다.
  PGlite PostgreSQL에서 DB 계약 테스트 21개 통과. 운영 서버에는 적용하지 않았다.
- 3차(2026-09-09): 활성 계정의 `usePlannerData` Task 읽기·쓰기를 revision RPC로 전환했다.
  계정별 로컬 미전송 기록, 재시작 복구, 진행 중 요청 receipt, 충돌 양쪽 보존 및 재시도 보류,
  계정 전환과 같은 브라우저의 여러 탭에 대한 작성 권한 보호를 구현했다.
- Google 인바운드·아웃바운드 실행기 통합과 충돌 해결 화면은 아직 미구현이다.
  계정 활성화 기본값은 비활성이고 운영 마이그레이션은 적용하지 않았다.
- 4차: token/connect 전후 버전 확인과 022 토큰 만료 대기 게이트를 구현했다.
  현재 Google 실행기는 프로토콜 1을 유지하며 운영 배포와 활성화는 하지 않았다.
- 5차: 기존 ID 이관 판정과 서버 전용 023 등록 RPC를 구현했다. 검증된 소유 증거가 있는
  단일 매핑만 등록하고, 중복/소유 불명/조회 실패는 검토 기록으로 보존한다.
  base를 추측하지 않으며 Task revision·중복 재검사와 배치 롤백·멱등 재시도를 검증했다.
- 6차: OAuth code 교환 후 Google UserInfo에서 sub를 검증하고 024 RPC로 grant를 저장한다.
  같은 sub만 기존 grant 교체를 허용한다. 다른 계정·기존 식별 불명은 기존 연결을 보존하며
  명시적 연결 해제를 안내한다. 이메일이나 새 sub를 옛 매핑의 소유 증거로 소급하지 않는다.
- 7차: 연결 저장을 서버 검증 API로 전환했다. sub 재검증과 CalendarList dataOwner·권한·
  비기본 캘린더·시간대를 검사하고 025에서 grant 버전/연결 세대를 재검사한다.
  같은 검증 scope는 기존 세대·커서를 유지하고 미검증 과거 연결은 새 세대로 격리한다.
- 8차: 026에서 검증된 연결 이력을 보존하고 동일 sub·캘린더·시간대 재연결 시 새 세대로
  매핑·base·검토/제외·삭제 표식을 복사한다. 커서는 초기화하며 다른 계정은 격리한다.
  연결 해제의 DB 삭제는 버전 검사와 이력 저장을 포함하는 원자적 RPC로 전환했다.
- 9차: 027 작업권으로 서비스의 code 교환·철회 요청을 직렬화한다. 응답 유실은 자동
  외부 재시도/잠금 만료 대신 복구 대기로 보존한다. 철회 후 기존 grant 거절을 확인하며,
  관리자만 외부 종료 증거를 기록하고 잠금을 복구할 수 있다. 운영 적용은 아직 하지 않았다.
- 10차: 인바운드 실행기를 실제 lease/snapshot/commit RPC에 연결했다. 페이지/410 처리,
  고정 계정 JWT, 전송 전 journal, 응답 유실 receipt 재확인, 이관 review/excluded 보존을
  구현했고 028의 서버 검증과 실제 PostgreSQL 통합 테스트를 추가했다.
- 11차: `taskOutboundPlan.ts`에 매핑된 단일 이벤트의 base 기반 쓰기 판정, 손실 없는 공유
  필드 PATCH 변환, 충돌 선택의 scope/Task/검토 revision·etag·표시 내용 재검사와 응답 내용
  검증을 구현했다. 순수 계획 단계이며 Google 쓰기나 검토 기록 해제는 아직 수행하지 않는다.
- 12차: 029에 서버 쓰기 예약·단일 전송권·결과 확정·전송 전 취소 RPC를 구현했다.
  Google 충돌 선택은 Task/base/검토 해제를 원자적으로 적용하고, 앱 선택은 영속 예약한다.
  서버 실행기를 `google/task-write` API에 연결했다. 전송 중 편집 보존, If-Match,
  응답 유실의 GET 재확인을 실제 PostgreSQL과 모의 Google HTTP로 통합 검증했다.
- 13차: 030–032와 검토 화면, Task 저장·인바운드·아웃바운드를 잇는 coordinator 및 자동 실행을
  연결했다. 충돌 선택, 중복 별도 가져오기/제외, 복원, 일반 일정 생성/삭제, 응답 유실 조회 복구와
  DB 소유자 복구 감사를 구현했다. 활성 계정만 새 프로토콜 2 경로를 사용한다.
- 14차: 033–034에 앱 반복 일정 신규 전송, 명시적 반복 규칙 적용, 다른 연결 작업의 선택 복사와
  해결된 비교 사본의 30일 정리를 추가했다. 요청 fingerprint/작업 ID를 남겨 정리 후에도
  중복 실행을 막는다. 신규 Google 반복 원본/회차 승격은 기존 정책대로 지원하지 않는다.
  비접속 계정의 일일 정리는 운영 환경의 Cron 등록을 확인해야 한다.
- 운영 배포/마이그레이션/활성화와 실제 Google·동시 기기 검증은 수행하지 않았다.
  조사 및 구현 근거는 `docs/google-task-inbound-storage.md`, 전환 조건은
  `docs/google-task-rollout.md`에 기록했다. 위 단계별 ‘미구현’ 설명은 당시 상태다.
