# Spaces 재설계 — 화면과 조작

- 작성일: 2026-08-14
- 기준: `v0.5.3` (`a927892`), 브랜치 `fix/store-write-amplification`
- 선행 문서: `SPACES_CLICKUP_REDESIGN.md` (데이터 모델 · D1~D9 · 마이그레이션). **이 문서는 그 P4를 설계한다**
- 대상: `src/components/Sidebar.tsx`, `src/components/SpacesPage.tsx`, `src/components/spaces/**`, `src/App.tsx`의 탐색 상태

---

## 0. 범위

모델 문서가 "무엇을 저장하는가"를 정했다. 이 문서는 **"사용자가 무엇을 보고 무엇을 누르는가"**를 정한다.

확신도는 모델 문서 §0을 승계한다 — **ClickUp을 실제로 열어보지 않았다.** 계층(Space→Folder→List)과 사이드바가 트리라는 것은 [확인], 화면 세부·간격·메뉴 구성은 [불명]이다. 그래서 이 문서는 **ClickUp 화면 복제가 아니라 계층을 이 앱의 기존 언어로 표현하는 것**이다.

---

## 1. 진단 — 현행 탐색이 3단계를 표현할 수 없다

```
App.tsx:131   const [selectedProjectId, setSelectedProjectId] = useState("");
App.tsx:132   const [isProjectDetailOpen, setIsProjectDetailOpen] = useState(false);
```

**평평한 상태 두 개**다. "어느 Space의 어느 Folder의 어느 List"를 담을 수 없다. 탭만 URL(`?tab=`)에 있고 나머지는 메모리에만 있어서, 새로고침하면 어느 Space를 보고 있었는지 잃는다.

사이드바도 이미 포화다 — 1차 2개 + 프로젝트 바로가기 + 2차 6개. 여기에 3단 트리를 얹으면 터진다.

---

## 2. 확정 결정

### U1. 사이드바가 곧 Space 탐색기다 — "공간" 메뉴 항목을 없앤다

ClickUp의 사이드바는 목록이 아니라 **트리**다. 현행처럼 "공간"이라는 목적지를 누르고 들어가 카드 그리드를 보는 구조와 양립하지 않는다.

```
   FocusFlow                    [<<]
   ┌──────────────────────────────┐
   │ 🔍 검색                      │
   ├──────────────────────────────┤
   │ 오늘                      12 │
   │ 캘린더                       │
   ├─ 뷰 ─────────────────────────┤
   │   지평                       │
   │   계획                     3 │
   │   보관함                     │
   │   + 뷰                       │
   ├─ 스페이스 ───────────────────┤
   │ ▾ 🎯 커리어                  │
   │     ▾ 2026 상반기            │   ← Folder (선택)
   │         이직 준비          5 │   ← List
   │         포트폴리오         2 │
   │     읽을 것                8 │   ← Folderless List (D4)
   │     + 리스트                 │
   │ ▸ 📚 학습                    │
   │ ▸ 🏃 건강                    │
   │ + 스페이스                   │
   ├──────────────────────────────┤
   │ 집중 · 설정                  │
   └──────────────────────────────┘
```

- 2차 네비의 **지평·계획·보관함은 "뷰" 섹션으로** 이사 (`CLICKUP_IMPORT_DESIGN.md` §4.2)
- **"공간" 항목은 사라진다.** 트리가 그 자리다
- 기존 "프로젝트 바로가기" 섹션도 사라진다 — 트리가 그것의 상위 호환이다
- **집중·설정만 하단에 남는다**

### U2. 리스트가 하나뿐인 Space는 리스트 계층을 **숨긴다** — ClickUp과 의도적으로 다른 곳

`TIMESTRIPE_REFERENCE.md` §3의 태도. 다르게 가는 곳은 이유를 적는다.

**ClickUp은 List를 항상 보여준다 — [검증됨].** 사이드바 문서가 *"all of your Spaces, Folders, Subfolders, and Lists are displayed"*라고 명시한다([What is the Spaces Sidebar?]). 팀에서는 List가 담당·권한·워크플로의 경계라 숨기면 안 되기 때문이다. **여기는 사용자가 한 명이고, 그 경계가 존재하지 않는다.**

> 즉 U2는 ClickUp에서 가져온 것이 아니라 **우리가 더한 것**이다. 이식이 아니라 이탈이므로, 판단이 틀렸다면 되돌릴 곳은 여기다.

[What is the Spaces Sidebar?]: https://help.clickup.com/hc/en-us/articles/32490148963479-What-is-the-Spaces-Sidebar

```
리스트 1개              리스트 2개 이상
▾ 🎯 커리어      12     ▾ 🎯 커리어
   (리스트 안 보임)         이직 준비        5
                            포트폴리오       2
                            + 리스트
```

- Space를 열면 기본 List의 내용이 **바로** 나온다 (D5의 기본 List가 투명해진다)
- `+ 리스트`로 두 번째를 만드는 **순간** 트리에 리스트가 나타난다
- **한 번 드러난 Space는 계속 드러난 채로 둔다.** 리스트를 다시 1개로 줄여도 숨기지 않는다

숨김은 **되돌릴 수 없는 한 방향**이다. 리스트 개수로 매번 다시 판정하면, 리스트를 하나 지웠을 뿐인데 트리에서 계층이 통째로 사라져 "리스트가 없어졌다"로 읽힌다. 조건이 바뀔 때마다 UI가 나타났다 사라지는 것은 완화가 아니라 불안정이다.

→ `Space.listsRevealed?: boolean`. 두 번째 List가 생기는 순간 `true`가 되고 **되돌아가지 않는다.** 계층을 한 번 배운 사용자에게서 그것을 도로 빼앗지 않는다.

> **이것이 모델 문서 §0에서 약속한 완화의 실물이다.** 계층은 필요해질 때만 보인다 — 그리고 한 번 필요해졌으면 계속 보인다. Folder도 같은 규칙을 따른다: 만들기 전에는 존재하지 않고, 만든 뒤에는 비어도 남는다.

### U3. 선택 상태를 경로로 바꾸고 URL에 싣는다

§1의 평평한 두 상태를 대체한다.

```ts
type Selection =
  | { kind: "space";  spaceId: string }
  | { kind: "folder"; spaceId: string; folderId: string }
  | { kind: "list";   spaceId: string; folderId?: string; listId: string };
```

| 위치 | URL |
|---|---|
| Space | `/s/:spaceId` |
| Folder | `/s/:spaceId/f/:folderId` |
| List | `/s/:spaceId/l/:listId` |
| 뷰 지정 | `?view=:viewId` |

`folderId`를 List 선택에도 담는 것은 **트리에서 어느 가지를 펼쳐야 하는지** 알기 위해서다. 모델에서 `List.spaceId`를 중복 저장한 것과 같은 이유(모델 문서 §4).

새로고침·뒤로가기·딥링크가 전부 동작하고, 현행 `?tab=`은 `?view=`로 흡수된다.

### U4. 모든 수준이 **같은 뷰 시스템**을 쓴다. 범위만 다르다

ClickUp 계층의 핵심이자, 이 앱이 얻는 가장 큰 것.

| 선택 | 범위 |
|---|---|
| Space | 그 Space의 모든 Item (하위 Folder·List 전부 롤업) |
| Folder | 그 Folder 아래 List들의 Item |
| List | 그 List의 Item |

**화면 구성은 세 수준이 동일하다:**

```
┌────────────────────────────────────────────────┐
│ 🎯 커리어 › 이직 준비            [⋯]  [+ 추가] │
├────────────────────────────────────────────────┤
│ [리스트] [보드] [지평] [테이블] [캘린더]  [+] │   ← 뷰 탭
├────────────────────────────────────────────────┤
│ 필터: 열림 ▾   그룹: 상태 ▾   정렬: 마감 ▾    │
├────────────────────────────────────────────────┤
│                                                │
│   (선택된 뷰의 내용)                           │
│                                                │
└────────────────────────────────────────────────┘
```

- **뷰 탭은 그 수준에 저장된다.** Space에 붙인 뷰는 Space에서, List에 붙인 뷰는 그 List에서

> **검증 (2026-08-14).** ClickUp과 일치한다 — *"Views Bar at the top of any Space, Folder, Subfolder, or List"*, 그리고 *"Each Space, Folder, Subfolder, and List you create has a List and Board view by default"*([Intro to views]). **뷰는 상속되지 않고 소속된다** — 대신 수준 간 **이동**이 가능하다([Move views]). §7-3의 미결이 이것으로 풀린다.

[Intro to views]: https://help.clickup.com/hc/en-us/articles/6329880717719-Intro-to-views
[Move views]: https://help.clickup.com/hc/en-us/articles/6310319472279-Move-views
- 현행 `SpaceDetailView`의 `overview` / `goals` / `tasks` / `notes` 탭이 여기로 대체된다:

| 현행 탭 | 대체 |
|---|---|
| `tasks` | 뷰 (`filter:{sourceTypes:["task"]}`) — 기본 리스트 뷰 |
| `goals` | 뷰 (`filter:{sourceTypes:["goal"]}`) 또는 지평 뷰 |
| `notes` | **탭으로 남긴다** (§U6) |
| `overview` | **탭으로 남긴다** (§U7) |

### U5. 목표와 태스크는 한 List에 섞이되, 배지로 구분한다

모델 D3이 List에 Item(Task | Goal)을 담기로 했다. 화면에서는:

- `HORIZONS_DESIGN.md` D7의 **균일 카드 원칙을 유지한다** — 목표와 태스크가 같은 모양
- 좌측에 **타입 배지**(◇ 목표 / ○ 태스크)만 다르다
- 섞이는 게 싫으면 **뷰 필터로 나눈다** (`sourceTypes`). 구조가 아니라 뷰의 문제로 내린다

### U6. 노트는 Space 수준의 탭으로 남긴다 — 뷰가 아니다

노트는 Item이 아니다(모델 §4의 별도 `notes` 컬렉션). 필터·그룹·정렬의 대상이 아니라 **읽고 쓰는 곳**이다.

- Space 수준에만 둔다. List마다 노트 탭이 있으면 어디에 썼는지 잃는다
- `Note.listId`는 선택적으로 남겨 특정 List에 연결할 수 있게 한다(모델 §4). 하지만 **읽는 자리는 Space 하나**

### U7. 개요(Overview)는 남기되 입력을 다시 정의한다

모델 문서 §8-4의 숙제. 현행 AI 브리핑은 파생 `Space.status` / `aiPriority`를 먹는데, 그것들을 버린다(모델 D1).

→ 브리핑 입력을 **저장된 사실로만** 다시 만든다:

```
미완료 Item 수 · 마감 임박(7일) · blocked 상태 · 최근 활동(Activity)
· 이번 주 집중 시간(FocusSession) · 상태 그룹별 분포
```

전부 이미 있는 레코드에서 파생된다. 진단 문자열(`mainSignal`·`aiPriority`)을 저장하지 않고 **볼 때 계산한다** (`CLICKUP_IMPORT_DESIGN.md` P3).

### U8. 접힘·모바일 — 트리가 감당 못 하는 두 경우

**레일(접힘) 모드.** 현행은 아이콘만 남긴다(`Sidebar.tsx:165` `railed`). 트리는 표현 불가.
→ Space 아이콘만 세로로 쌓고, 클릭하면 **플라이아웃**으로 그 Space의 하위를 띄운다.

**모바일(≤860px).** 현행 오버레이 메뉴에 3단 트리는 답답하다.
→ **드릴다운**: 스페이스 목록 → (탭) → 그 Space의 리스트 → (탭) → 내용. 상단에 뒤로가기. 트리를 펼치지 않는다.

### U9. 이동은 드래그와 메뉴 **둘 다** 제공한다

드래그만 있으면 모바일과 접근성에서 막힌다.

| 대상 | 드래그 | 메뉴 |
|---|---|---|
| Item → 다른 List | ✔ | "이동…" |
| List → 다른 Folder/Space | ✔ | "이동…" |
| Folder 순서 | ✔ | — |
| Item → 상태 변경 | ✔ (보드 뷰) | 행의 상태 드롭다운 |
| **뷰 → 다른 수준** | 탭 드래그 | "뷰 이동…" — 뷰가 상속되지 않으므로 필요하다 (U4 검증) |

`MotionDropZone` · `MotionTaskRow`가 이미 있으므로 드래그 표현은 기존 것을 쓴다.

---

## 3. 상태(Status) 관리 화면 — 모델 D7

```
Space 설정 › 상태
┌──────────────────────────────────────────┐
│  시작 전                                 │
│    ⣿ Inbox              ●회색      [⋯]  │
│  진행                                    │
│    ⣿ To Do              ●파랑      [⋯]  │
│    ⣿ Waiting            ●노랑      [⋯]  │
│    + 상태 추가                           │
│  완료                                    │
│    ⣿ Done               ●초록      [⋯]  │
│  닫힘                                    │
│    ⣿ Archived           ●회색      [⋯]  │
└──────────────────────────────────────────┘
```

- **그룹은 4개로 고정**(`notStarted` / `active` / `done` / `closed`). 사용자가 만들지 않는다 — 앱이 "완료로 치는가"를 판단해야 하기 때문(모델 D7)
- 그룹 안에서 상태를 추가·개명·재정렬·색 지정
- `done` 그룹에는 최소 1개가 있어야 한다. 마지막 하나는 삭제 불가
- 상태를 지우면 **그 상태의 Item을 어디로 옮길지 먼저 묻는다.** 조용히 지우지 않는다
- List 오버라이드는 List 설정에서. 안 건드리면 Space 상속

---

## 4. 생성 흐름

### Space 만들기 (모델 D8 — 종류가 아니라 템플릿)

```
1. 이름 · 색 · 아이콘
2. 템플릿 선택:  [빈 Space]  [프로젝트]  [학습]  [영역]
3. 만들기
```

템플릿은 초기 List·Status·뷰를 깔고 **끝난다.** Space 레코드에 흔적을 남기지 않는다.

| 템플릿 | 초기 List | 초기 뷰 |
|---|---|---|
| 빈 Space | `Tasks` | 리스트 |
| 프로젝트 | `Tasks` | 리스트 · 보드 |
| 학습 | `Tasks` | 지평 · 리스트 |
| 영역 | `Tasks` | 리스트 |

현행 `spaceTypeConfig.ts`의 프리셋 문구(빈 상태 예시 등)가 여기로 이사한다.

### List·Folder 만들기

- 트리의 `+ 리스트` — 인라인 입력. 모달 없음
- Folder는 `+` 메뉴에서 명시적으로. **먼저 권하지 않는다** (U2)

---

## 5. 버리는 화면

| 현행 | 처분 |
|---|---|
| `SpacesPage.tsx` 카드 그리드 (1,082줄) | **삭제.** 트리가 대체 |
| `Space.status` · `mainSignal` · `aiPriority` 진단 카드 | **삭제** (모델 D1) |
| `SpaceDetailView` 탭 4개 (678줄) | 2개는 뷰로, 2개는 탭 유지 (U4) |
| `SpaceHorizons.tsx` (179줄) | **삭제.** `groupBy:"horizon"` 뷰 (모델 D2) |
| `SpaceWorkTabs.tsx` | 뷰로 대체 |
| `spaceTypeConfig.ts` | 템플릿 정의로 이사 (§4) |
| 사이드바 "프로젝트 바로가기" 섹션 | 트리로 대체 (U1) |
| 사이드바 "공간" 항목 | **삭제** (U1) |

---

## 6. 순서

모델 문서 P4를 쪼갠다. 앞에서 뒤로 갈수록 되돌리기 어렵다.

```
U-1  탐색 상태·라우팅       U3의 Selection + URL. 화면은 그대로 두고 배선만
U-2  사이드바 트리          U1 · U2 · U8. "공간" 항목 제거
U-3  뷰 셸                  U4의 헤더·탭·필터 바. 내용은 기존 컴포넌트 재사용
U-4  탭 이관                tasks/goals → 뷰. notes/overview 유지 (U6·U7)
U-5  상태 UI                §3
U-6  생성 흐름              §4
U-7  정리                   §5의 삭제
```

**U-1이 먼저인 이유**: 라우팅 없이 트리를 만들면 트리 상태가 또 메모리에만 남아 §1을 반복한다.

---

## 7. [불명] · 숙제

1. **ClickUp 실제 사이드바 동작** — 다중 펼침 여부, 드래그 세부, 컨텍스트 메뉴 구성 [불명]
2. ~~**U2의 되돌림**~~ — **해결 (2026-08-14).** 되돌리지 않는다. `Space.listsRevealed` 플래그로 한 방향 고정. 근거는 U2 본문
3. ~~**뷰 탭의 소속 수준**~~ — **해결 (2026-08-14, 확인 후).** ClickUp은 **상속하지 않는다.** 뷰는 만든 수준에 소속되고, 수준 간 이동만 가능하다. 초판이 "상속한다고 기억한다"고 적은 것은 오류. U4를 그에 맞춰 갱신했다 — 상속 없음, 대신 "이동" 메뉴가 필요하다(U9의 이동 표에 뷰 행 추가 대상)
4. **Item 다중 List 소속** — 모델 §8-2와 연동. UI에서는 "여러 리스트에 표시" 뱃지가 필요
5. **기존 `?tab=` 딥링크** — `?view=`로 옮길 때 legacy 값 매핑 필요. `SpaceDetailView.tsx:96` `readTabFromUrl`이 선례
6. **트리 성능** — Space·List가 수십 개가 되면 사이드바 렌더 비용. 현행 프로젝트 목록은 필터링 없이 전부 그린다(`Sidebar.tsx:302`)
