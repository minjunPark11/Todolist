# Component 14 — Light Theme

측정일 2026-08-21 · TickTick Web (`#p/inbox/kanban`) · Chrome

---

## 0. 측정 조건 · 세션에 남긴 것

Component 01~13은 **전부 다크에서만** 측정됐다. 그 결과 우리 앱의 라이트 값 여러 개가 "다크 알파를 뒤집은 외삽"으로 들어가 있었고, 이 문서는 그것을 실측으로 대체하기 위한 것이다.

**계정 설정은 바꾸지 않았다.** 테마 전환은 `body[data-theme]` 속성을 DOM에서 잠시 바꿔 CSS를 다른 분기로 태우는 방식으로 했고, 측정이 끝나는 즉시 원래 값으로 되돌렸다. 복구 확인: `data-theme=night-dark-theme` · `body.dark` 존재 · `background rgb(28,28,28)` · `localStorage.appTheme = "grey"` 무변경 · URL 무변경. 데이터 생성·수정·삭제 없음.

### 0.1 테마 스위치의 실체

| 항목 | 값 |
|---|---|
| 실제 스위치 | **`body[data-theme]`** |
| 다크 값 | `night-dark-theme` |
| **라이트 값** | **`white-theme`** |
| 함께 붙는 클래스 | `body.dark` (다크일 때) |
| 사용자 설정 | `localStorage.appTheme = "grey"` (OS 다크 선호를 따라 다크로 해석됨) |

**[Observed] `body.dark` 클래스만 제거하는 것으로는 라이트가 되지 않는다.** 행 텍스트가 흰색으로 남고 선택 레이어도 흰 알파 그대로다. `data-theme`이 실제 팔레트를 고른다.

**[Observed] `data-theme`에 유효하지 않은 값을 넣으면 라이트가 아니라 "테마 없음"으로 떨어진다** — 텍스트가 순수 검정(`rgb(0,0,0)`), 배경 투명. `grey` · `light` · `day-theme` · `default` 등 10개 후보 중 **`white-theme` 하나만** 실제 팔레트를 냈다. 나머지는 전부 미매칭이다.

---

## 1. 가장 중요한 결론 — 알파는 두 테마가 같고, 전경색만 바뀐다

| 대상 | dark | light | 알파 |
|---|---|---|---|
| 전경 기준색 | `rgb(255,255,255)` | **`rgb(25,25,25)`** | — |
| 사이드바 행 텍스트 | `rgb(255,255,255)` | `rgb(25,25,25)` | — |
| **선택 배경** | `rgba(255,255,255,0.08)` | `rgba(25,25,25,0.08)` | **.08 동일** |
| **내비 행 hover** | `rgba(255,255,255,0.03)` | `rgba(25,25,25,0.03)` | **.03 동일** |
| 섹션 헤더 제목 | `rgba(255,255,255,0.3)` | `rgba(25,25,25,0.3)` | **.3 동일** |
| Rail 우측 보더 | `rgba(255,255,255,0.05)` | `rgba(25,25,25,0.05)` | **.05 동일** |
| 카드 보더 | — | `rgba(25,25,25,0.08)` | .08 |

**[Observed] 5/5 항목에서 알파가 정확히 일치한다.** 바뀌는 것은 알파가 곱해지는 색뿐이다.

**[Observed] 그 색은 순수 검정이 아니라 `rgb(25,25,25)`다.** 흰 바탕에서 `rgba(0,0,0,.08)`은 234.6, `rgba(25,25,25,.08)`은 236.6으로 약 2단계 밝다.

**[Inference]** 흰 배경에 순수 검정 알파를 얹으면 회색이 차갑게 죽는다. 25는 그것을 피하면서도 텍스트 대비는 유지하는 값으로 보인다. 다만 이 추론은 검증하지 않았다.

---

## 2. 그림자 — 색은 그대로, 기하가 바뀐다

떠 있는 패널(`.global-quick-add`, radius 16 — C12가 잰 모달과 같은 radius) 실측:

| | dark | light |
|---|---|---|
| box-shadow | `rgba(0,0,0,0.2) 0 16px 40px` | **`rgba(0,0,0,0.2) 0 5px 18px`** |

**[Observed] 알파는 0.2로 동일하고, offset과 blur가 16/40 → 5/18로 줄어든다.**

**[Observed] 그림자만은 라이트에서도 순수 검정 기준이다.** 채움·보더·텍스트가 전부 `rgb(25,25,25)` 기준인 것과 다르다.

**[Observed] 모든 그림자가 테마에 따라 바뀌지는 않는다.** 툴팁(`.pop-tip`)은 `rgba(0,0,0,0.12) 0 2px 11px`로 양쪽 동일했다.

**[Observed] 칸반 카드는 라이트에서만 그림자를 갖는다** — light `rgba(25,25,25,0.03) 0 2px 3px`, dark `none`.

**[Inference]** 어두운 바탕에서는 그림자 대신 표면색 차이로 층을 만들고(카드 36 vs 배경 28), 밝은 바탕에서는 표면색이 둘 다 흰색에 가까워 그림자가 그 역할을 대신하는 것으로 보인다.

---

## 3. Rail 밝기 방향은 테마에 따라 뒤집힌다

| | Rail | 캔버스 | 방향 |
|---|---|---|---|
| dark | `rgb(36,36,36)` | `rgb(28,28,28)` | Rail이 **밝다** |
| light | `rgba(245,245,245,0.8)` | 흰색 | Rail이 **어둡다** |

**[Observed] 규칙은 "Rail이 밝다"가 아니라 "Rail이 극단값에서 물러나 있다"이다.**

이것은 종합 문서 §9.3-18의 판단에 직접 영향을 준다. 거기서는 "TickTick은 Rail이 더 밝고 우리는 더 어두우니 방향이 반대"라고 적었는데, **라이트에서는 TickTick도 우리와 같은 방향이다.** 우리 V-1 원칙("내비가 콘텐츠보다 어둡게")은 TickTick의 라이트와 일치하고 다크와만 어긋난다.

---

## 4. 우리 앱 적용 — 외삽이었던 값의 정정

| 토큰 | 외삽값(전) | 실측 기준(후) | 판정 |
|---|---|---|---|
| `--bg-hover-nav` | `rgba(0,0,0,.03)` | `rgba(25,25,25,.03)` | 알파 ✓ · 기준색 정정 |
| `--bg-hover-content` | `rgba(0,0,0,.05)` | `rgba(25,25,25,.05)` | 알파 미확인 · 기준색 정정 |
| `--surface-2` | `rgba(0,0,0,.06)` | `rgba(25,25,25,.06)` | 알파 미확인 · 기준색 정정 |
| `--bg-selected` | `rgba(0,0,0,.08)` | `rgba(25,25,25,.08)` | **알파 ✓** · 기준색 정정 |
| `--bg-selected-hover` | `rgba(0,0,0,.10)` | `rgba(25,25,25,.10)` | 알파 미확인 · 기준색 정정 |
| `--bg-pressed` | `rgba(0,0,0,.12)` | `rgba(25,25,25,.12)` | 우리 값 · 기준색만 정정 |
| `--scrollbar-thumb` | `rgba(0,0,0,.3)` | `rgba(25,25,25,.3)` | 알파 ✓(섹션 헤더와 동일 단) |
| `--border-subtle` | `rgba(0,0,0,.07)` | `rgba(25,25,25,.08)` | **.08로** — 다크와 대칭이 됨 |
| **`--shadow-panel`** | `0 16px 40px rgba(0,0,0,.13)` | **`0 5px 18px rgba(0,0,0,.2)`** | **양쪽 다 틀렸다** |

`--shadow-panel`이 이 문서의 가장 큰 수확이다. 기존 주석은 "라이트는 측정된 적이 없으므로 이 스케일 자체의 0.13을 유지한다"고 적혀 있었는데, 추측의 방향이 정확히 반대였다 — **알파를 낮추는 게 아니라 유지하고, 기하를 줄인다.**

---

## 5. 이 측정이 확인하지 않은 것

- **본문 행 hover(5%)를 라이트에서 직접 재지 못했다.** 내비 행 3%는 실제 마우스로 확인했으나 본문 행은 확인하지 못했다. 우클릭 직후 칸반 카드가 `rgba(25,25,25,0.04)`로 관측됐는데, 이것이 hover인지 우클릭 활성 상태인지 구분하지 못했다 — **4%일 가능성이 열려 있다.**
- **메뉴·모달의 라이트 그림자.** 표본은 퀵애드 패널 하나다. 컨텍스트 메뉴를 열려고 우클릭했으나 커스텀 메뉴가 뜨지 않았고(네이티브 메뉴로 추정) 더 시도하지 않았다.
- **`data-theme` 오버라이드는 앱이 실제로 전환한 상태가 아니다.** CSS 분기는 정확히 타지만, JS가 런타임에 인라인으로 칠하는 값이 있다면 그것은 따라오지 않는다. 그런 값이 있는지는 확인하지 않았다.
- **원본 CSS 규칙.** 스타일시트가 CDN에 있어 CSSOM 접근도 `fetch`도 CORS로 막혔다. 값은 전부 computed style로만 얻었다.
- **다른 라이트 변형.** `white-theme` 하나만 확인했다. TickTick이 제공하는 다른 밝은 테마가 같은 알파를 쓰는지는 모른다.
