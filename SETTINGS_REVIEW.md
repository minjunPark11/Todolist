# 설정 화면 — 실측 리뷰와 단계

- 작성일: 2026-08-26
- 기준: `v0.19.4` (`3121202`), `main`
- 방법: Playwright로 여섯 탭을 1440×900에 렌더해 재고, 주장은 전부 코드에서 확인했다.
  이 문서는 **측정과 판단**이고, 구현 결과는 각 단계 아래에 기록한다.

---

## 0. 결론 세 줄

1. **계정 카드가 깨져 있다.** 나머지 설정이 `ff-` 계열로 옮겨갈 때 계정·앱 정보만 남았고,
   로그인 화면의 부제가 빈 상태 텍스트로 새어 나온다.
2. **콘텐츠가 1326px로 늘어난다.** 라벨과 컨트롤이 1100px 떨어져 한 행이 한 눈에 안 들어온다.
3. **알림 설정이 없다.** Reminder를 엔티티로 만들고 권한을 네 상태로 설계해 놓고,
   사용자가 그것을 볼 곳도 되돌릴 곳도 만들지 않았다.

---

## 1. 전수 — 여섯 탭

| 탭 | 내용 | 콘텐츠 높이 | 카드 |
|---|---|---|---|
| Appearance | 테마 · 강조색 · 글자 크기 · 언어 | 574px | 1 |
| Behavior | 시작 화면 + 토글 4 | 656px | 1 |
| Calendar | 카테고리 · 공유 · 외부 캘린더 · 동기화 | 982px | 4 |
| Knowledge base | 볼트 · 임베딩 · 색인 | 982px | 4 |
| Local AI | 기기 스캔 · 카탈로그 · 실행 · 저장소 · 턴 로그 | **1428px** | 4 |
| Data | 내보내기 · 가져오기 · 초기화 + 계정 + 앱 정보 | 784px | 1 + 2 |

**여섯 중 둘(Knowledge, Local AI)이 웹에서는 통째로 비활성이다.** 설치본에서만 의미가 있는
탭이 목록의 3분의 1을 차지한다.

---

## 2. 레이아웃 실측

```text
main        x=50   w=1390
ff-page     x=82   w=1326
카드         x=82   w=1326      ← 셸의 92%
카드 패딩     8px 20px
```

**행이 전폭이라 라벨과 값이 1100px 떨어진다.** `Theme`는 x=104, 그 컨트롤은 x=1210이다.
macOS 시스템 설정은 콘텐츠 컬럼이 ~700px이고 화면을 키워도 늘어나지 않는다 —
설정은 읽는 화면이지 채우는 화면이 아니기 때문이다.

**디자인 언어가 셋 공존한다.**

| | 어디 | 모양 |
|---|---|---|
| A | Calendar 탭 | 아이콘 + 제목 + 힌트 + 액션 카드. ~~**이것이 기준이 되어야 한다**~~ |
| B | Appearance · Behavior · Data | 라벨/힌트 좌, 컨트롤 우, 전폭 행 |
| C | 계정 · 앱 정보 | 마이그레이션되지 않은 레거시 |

> Phase 9 정정: A와 B는 **다른 언어가 아니다.** 타입 사다리가 같고 차이는 아이콘 열
> 하나이며, A는 Calendar 탭 안의 카드 넷일 뿐이다. 고칠 것은 "A로 통일"이 아니라
> **한 열에 왼쪽 가장자리가 둘이라는 것**이었다.

**탭을 바꾸면 높이가 574 → 1428px로 요동한다.** Appearance에서는 화면 아래 절반이 비고,
세그먼트 탭 자체도 전폭으로 늘어나 탭 여섯 개가 왼쪽에 뭉친다.

---

## 3. 결함

### 3.1 계정·앱 정보 카드 (🔴)

`App.tsx`의 `AccountSection`과 `SettingsPage`의 앱 정보 절이 **구 클래스**를 쓴다 —
`settings-card`, `section-title`, `account-stack`, `settings-actions`, `empty-state`.
나머지 설정은 전부 `ff-settings-card` + `ff-settings-row`다.

결과적으로 한 행에 다섯 조각이 정렬 없이 흩어진다:

```text
Account │ localStorage │ Supabase env vars are not configured… │ Your day starts here │ LocalStorage mode
```

`Your day starts here`는 **로그인 화면의 부제**(`auth.signInSubtitle`)다. 빈 상태 문구로
재사용됐는데 설정 안에서는 아무 뜻이 없다.

### 3.2 계정이 Data 탭 맨 아래에 있다 (🔴)

로그인·로그아웃·클라우드 동기화가 "내보내기 / 가져오기 / 전체 초기화" **아래**에 있다.
로그인하려면 Data 탭을 열고 스크롤해야 한다. 계정은 보통 첫 항목이다.

### 3.3 Local AI 탭은 설정이 아니라 관리 화면이다 (🟠)

모델 카탈로그, 실행 모드, 포트, 바이너리 경로, 외부 URL, 저장소, 설치된 모델, 턴 로그.
설정 항목이라기보다 서버 관리 콘솔이고, 탭 하나가 나머지 다섯을 합친 것보다 길다.

### 3.4 값을 되묻고 싶은 토글 (🟡)

`showSidebarCounts`, `confirmBeforeDelete`는 기본값이 옳다면 없어도 되는 종류다.
취향 토글은 늘어날수록 조합이 늘고 유지비가 붙는다.

> Phase 10 정정: 둘을 묶은 것이 틀렸다. `showSidebarCounts`는 **읽는 곳이 없어서**
> 취향 토글조차 아니었고, `confirmBeforeDelete`는 동작한다.

> `reduceMotion`은 다르다. `01-base.css:430`이 OS의 `prefers-reduced-motion`을 이미
> 존중하므로, 앱 토글은 **강제** 수단으로 정당하다.

---

## 4. 없는 것 — 코드로 확인한 구멍

### 4.1 알림 설정 (🔴)

Reminder는 엔티티이고 `notificationAccess`는 네 상태(unasked / granted / denied /
unsupported)로 설계돼 있다. **설정에는 아무것도 없다.** 권한이 거부되면 앱 안에서
되돌릴 경로가 없다.

```text
알림
  권한 상태          묻지 않음 / 허용됨 / 거부됨 / 지원 안 함   [권한 요청]
  종일 일정 기본 시각   09:00      ← §6.12의 allDayTime, 지금은 코드 상수
  기본 미리 알림       없음 / 10분 전 / 1시간 전
```

### 4.2 시간 표기 12/24 (🔴)

`WeekView.tsx:65`

```ts
const timeLabelFormatter = new Intl.DateTimeFormat("en", { hour: "2-digit", minute: "2-digit", hour12: false });
```

**로케일과 무관하게 영어·24시간 고정이다.** 언어를 한국어로 바꿔도 캘린더 시간은
`13:00`이고, 참조로 삼은 macOS 화면은 `오후 1시`다. `lib/externalCalendars.ts:97`도 같다.

### 4.3 주 시작 요일 (🟠)

`date.ts:66`의 `getWeekStart`가 `getDay()`를 그대로 빼므로 **일요일 시작 하드코딩**이다.
한국·유럽에서 월요일 시작이 흔하고 macOS도 설정 항목으로 둔다.

### 4.4 캘린더 "한 번에 표시할 시간" (🟠)

`HOURS_AT_A_TIME = 12`가 상수다. Apple은 이것을 6~24 설정으로 낸다
(CALENDAR_GEOMETRY_DESIGN §7.2). 이미 함수 인자 하나라 비용이 거의 없다.

### 4.5 포커스 세션 기본 길이 (🟡)

`startFocusSession(taskId, source, durationMinutes)`로 호출부마다 넘기는데
사용자 기본값이 없다.

> Phase 8에서 확인한 정정: **호출부가 넘긴 적이 없다.** 세 번째 인자는 존재만 하고
> 모든 세션 길이가 훅 안의 추론이었다. 그리고 이 항목을 막고 있던 것은 길이가 아니라
> **설정 시스템이 둘이라는 것**이었다.

### 4.6 자동 백업 (🟡)

로컬 우선 앱인데 수동 `Export JSON`뿐이다. localStorage가 비면 복구 수단이 없다.

---

## 5. 단계

```text
Phase 1  계정·앱 정보를 현재 디자인으로 옮기고 첫 탭으로            ← 3.1 · 3.2
Phase 2  콘텐츠 폭 제한                                        ← 2
Phase 3  알림 설정 신설                                        ← 4.1
Phase 4  12/24 표기와 주 시작 요일                              ← 4.2 · 4.3
Phase 5  캘린더 표시 시간 수를 설정으로                           ← 4.4
Phase 6  Local AI를 설정 밖으로 (별도 논의)                      ← 3.3
Phase 7  좁은 폭에서 읽히게                                     ← 6-1
Phase 8  Focus 설정을 설정 화면으로 + 기본 세션 길이               ← 4.5
Phase 9  한 열, 한 왼쪽 가장자리                                 ← 2
Phase 10 아무 일도 하지 않던 토글                                ← 3.4
Phase 11 자동 백업                                             ← 4.6
```

Phase 1과 2는 서로 독립이고 둘 다 리스크가 낮다. 3부터는 저장할 값이 늘어나므로
`appSettings`의 모양이 바뀐다.

### Phase 1 결과 (완료)

계정과 앱 정보가 `ff-settings-card` + `SettingsRow`로 옮겨졌고, **Account가 첫 탭**이 됐다.
`Row`는 `SettingsRow`로 내보내 계정 슬롯이 같은 것을 쓴다 — 슬롯이 App.tsx에 있는 이유는
auth props 때문이므로 컴포넌트를 옮기는 대신 프리미티브를 공유했다.

렌더 확인: 레거시 클래스(`settings-card` · `account-stack` · `section-title`) **0개**,
`Your day starts here` **사라짐**, 첫 탭 = Account.

앱 정보는 두 행이 됐다 — 버전, 그리고 업데이트 확인(상태가 힌트로 들어간다).
"업데이트 확인 / 설치"가 한 행에 둘일 수 있어 `.ff-settings-actions`를 만들었다.

### Phase 2 결과 (완료)

```text
카드 폭   1326px  →  760px
카드 x    82      →  82      (왼쪽 정렬 유지)
```

`.ff-page`가 `margin: 0 auto`라 폭만 줄이면 가운데로 밀려난다. Today·Spaces·Calendar가
전부 x=82에서 시작하므로 `margin-left: 0`으로 되돌렸다 — 넓은 창에서 설정만 화면
가운데로 떠 있는 것은 이 앱의 다른 화면과 다르다.

### Phase 3 결과 (완료)

**알림 탭을 만들었다.** Behavior 안의 카드가 아니라 탭인 이유는, 이 화면의 주된 일이
"알림이 안 온다"는 사람이 **이름으로 찾아오는 것**이기 때문이다. `Behavior`는 그 이름이
아니다.

| 행 | 내용 |
|---|---|
| 알림 권한 | 네 상태를 각각 다른 문장으로. `unasked`일 때만 [알림 허용] |
| 테스트 알림 보내기 | `platform.notify()`를 실제로 호출하고 결과를 그대로 말한다 |

**버튼을 `unasked`에서만 내는 것이 이 화면의 판단이다.** 거절 뒤의 재요청은 모든
브라우저가 조용히 버린다(`useNotificationAccess`가 그 호출을 떨어뜨리는 곳에 적혀 있다).
`denied`에 버튼을 두면 아무 일도 하지 않는 컨트롤이 되고, 사용자는 권한이 딴 데 있다는
것이 아니라 앱이 고장났다고 배운다. 그래서 `denied`의 문장이 브라우저·OS 설정을 가리킨다.

**테스트 발송이 따로 있는 이유**: 권한은 경로의 일부일 뿐이다. 권한이 허용됐는데 OS에서
음소거된 상태는 여기서 구분이 안 되고, `notify()`가 true를 돌려주는데 아무것도 안 뜨는
것이 그 둘을 가르는 유일한 신호다.

네 상태 매핑은 `utils/notificationCopy.ts`로 뺐다. **헤드리스 브라우저는 넷 중 셋을
재현할 수 없다** — Chromium은 `denied`로 시작해 거기 머문다. 브라우저에서 확인한 것은
`denied` 문장과 Send 버튼이 비활성이라는 것뿐이고, 나머지 세 갈래는 테스트가 본다(5개).

#### 이번 단계에서 제외한 것과 이유

§4.1이 적은 나머지 두 줄은 값 하나를 설정으로 빼는 일이 아니다.

- **종일 일정 기본 시각**: `ALL_DAY_REMINDER_TIME`은 새 알림의 기본값이면서 동시에
  저장된 `1d-9am` 프리셋의 **역사적 의미**다(`domain/schedule/reminder.ts`의 마이그레이션
  표). 이것을 설정으로 바꾸면 이미 저장된 알림이 울리는 시각이 소급해서 달라진다.
  게다가 offer의 id가 문자 그대로 `1d-9am`이라, 08:00으로 바꾸는 순간 이름이 거짓이 된다.
  기본값과 저장된 뜻을 먼저 분리해야 하고, 그건 설정 작업이 아니라 도메인 작업이다.
- **기본 미리 알림**: 지금 알림은 사용자가 고를 때만 생긴다. "기본값"은 일정에 시간을
  넣는 순간 알림이 하나 **자동으로 생기는** 동작이고, 없던 동작을 만드는 일이다.

### Phase 4 결과 (완료)

두 값이 `AppSettings`에 들어갔다. 컴포넌트까지는 **루트 `dataset`**으로 내려간다 —
테마·강조색·글자 크기가 이미 쓰는 통로이고(`motion/reducedMotion.ts`가 같은 관용구),
표시 취향 하나 때문에 CalendarView → WeekView → 블록의 시간 줄까지 모든 층이 쓰지도
않는 prop을 나르게 할 이유가 없다.

```text
timeFormat: "locale" | "12h" | "24h"    기본 "locale"
weekStart:  "sunday" | "monday"          기본 "sunday"
```

**`timeFormat`의 기본값이 `locale`인 것은 의도된 동작 변경이다.** 지금까지는
`Intl.DateTimeFormat("en", { hour12: false })`로 고정이었고, 그것은 선택이 아니라
누락이었다. 언어를 따르는 것이 그 코드가 하는 척하던 일이다.

**`weekStart`의 기본값이 `sunday`인 것은 반대 이유다.** 기존 계정이 전부 그것을 보고
있었으므로, 기본을 로케일로 바꾸면 아무 설정도 건드리지 않은 사람의 주가 조용히 움직인다.

브라우저 실측 (세 조합):

| 설정 | 첫 열 | 시간 라벨 | 블록 |
|---|---|---|---|
| en / locale / sunday | `Sun 23` | `5 AM` | `1:00 PM – 2:30 PM` |
| ko / 12h / monday | `24일 (월)` | `오전 5시` | `오후 1:00 – 오후 2:30` |
| en / 24h / monday | `Mon 24` | `05:00` | `13:00 – 14:30` |

시각을 그리던 자리 여덟 곳이 `utils/clock.ts` 하나를 지나간다 — 거터 라벨, 현재 시각
배지, 블록의 시간 줄, 드래그 미리보기, 월간 칩, 이벤트 팝오버, 빠른 생성.

**테스트 하나는 이 실행 환경이 답할 수 없다.** Node의 ICU에는 한국어 월 이름은 있는데
오전/오후가 없어서 `PM 1:00`을 돌려준다. 그래서 단위 테스트는 "오후"라는 낱말이 아니라
**로케일을 Intl에 넘긴다는 것과 12시간이 24시간과 다르다는 것**을 확인하고, 낱말은 위의
브라우저 실측이 본다.

설정 행은 Appearance의 Language 옆에 뒀다 — 둘 다 언어를 따르는 값이고, macOS도
Language & Region에 함께 넣는다.

### Phase 5 결과 (완료)

`HOURS_AT_A_TIME = 12`가 `hoursAtATime` 설정이 됐다. §4.4가 예상한 대로 **함수 인자
하나**가 전부였다 — R1이 이미 행 높이를 `max(floor, gridHeight / N)`로 만들어 뒀고,
바뀐 것은 N이 상수냐 읽는 사람 것이냐다.

```ts
slotHeightFor(viewportHeight: number, hours: number = HOURS_AT_A_TIME): number
```

**설정 행은 Appearance가 아니라 Calendar 탭에 뒀다.** 표기·주 시작이 Language 옆에
있는 이유는 그 둘이 언어를 따르기 때문이고, 이 값은 아무것도 따르지 않는다. macOS도
같게 나눈다 — Language & Region이 아니라 캘린더 설정이다.

**범위는 6–24, 컨트롤은 `<select>`다.** 19개는 세그먼트로 낼 수 없고, Apple의 스테퍼는
값을 보려면 눌러 봐야 한다. 목록은 고르기 전에 범위를 보여준다.

**이 설정만 숫자여서 `oneOf`가 아니라 클램프를 지난다.** 판단이 둘로 갈린다 —

| 들어온 값 | 나가는 값 | 왜 |
|---|---|---|
| 범위 밖의 수 (`2`, `48`, `9.4`) | `6`, `24`, `9` | 지킬 의도가 있다. 미래 클라이언트가 더 넓은 범위를 쓴다면 그릴 수 있는 가장 가까운 시간으로 |
| 값이 아닌 것 (`0`, `""`, `"twelve"`, `undefined`) | `12` | 없던 값이다. 여섯 시간을 요청한 적이 없으므로 바닥이 아니라 기본값 |

**24를 골라도 24가 다 보이지는 않는다.** 738px 격자에서 24시간이면 행이 30.75px가 되고
바닥 44px가 이긴다 — 16.8시간이 보이고 나머지는 스크롤된다. R1의 규칙 그대로이고
Calendar.app도 같다. N이 실제로 지켜지는 조건은 `gridHeight >= 44 * N`이다.

브라우저 실측 (1440x1000, 격자 738px):

| 설정 | 행 높이 | 실제로 보이는 시간 |
|---|---|---|
| 6 | 124px | 5.95 |
| 8 | 92px | 8.02 |
| 12 (기본) | 60px | 12.3 |
| 24 | 44px (바닥) | 16.77 + 스크롤 |

#### 함께 고친 것 — Phase 4가 반쪽만 동작했다

루트 dataset을 쓰는 `App.tsx`의 effect가 **의존성 배열에 `timeFormat`·`weekStart`를
넣지 않았다.** dataset에 쓰는 곳이 그 effect뿐이라, 다른 설정이 함께 움직이지 않으면
effect가 다시 돌지 않고 읽는 쪽은 옛 값을 계속 본다.

브라우저에서 재현했다 — 언어를 건드리지 않고 표기만 24시간 → 12시간으로 바꾸면
저장값과 세그먼트는 `12h`인데 `dataset.timeFormat`은 `24h`에 남아 캘린더가 안 바뀐다.
§Phase 4의 실측 세 조합이 매번 언어를 같이 바꿔서 이것을 가렸다.

배열이 이제 위에서 쓰는 값 전부를 담는다. 같은 통로를 쓰는 `hoursAtATime`도 그래서
처음부터 거기 들어간다. 고친 뒤 같은 조작에서 라벨이 `12 AM` → `00:00`으로 바뀌고,
`locale`로 되돌리면 다시 `12 AM`이 된다 — 언어는 `en` 그대로다.

#### 이번 단계에서 제외한 것

**"표시할 시간"을 Month에 적용하지 않았다.** 이 값은 하루의 세로 분량이고 Month에는
그런 축이 없다. D8이 정한 72px 하한이 Month의 규칙이고, 둘은 다른 결정이다.

### Phase 7 결과 (완료)

§6이 "1440×900만 쟀다"고 남긴 것을 쟀고, 둘이 깨져 있었다.

**탭 여덟 개가 614px을 차지하는데 셸의 900px 규칙이 탭 바에 `overflow-x: auto`를 준다.**
칼럼이 그보다 좁아지는 순간 넘치는 탭은 스크롤 뒤로 들어가고, **거기 있다는 표시가 없다** —
375px에서는 311px 칼럼 안에 614px이 들어간다. 세그먼트 컨트롤에는 스크롤바도 가장자리
페이드도 없어서, 화면만 보면 탭이 넷뿐인 것처럼 보인다.

**`.ff-settings-row`는 어느 폭에서도 행을 유지한다.** 라벨과 컨트롤이 남은 폭을 나눠 쓰다
컨트롤이 자기 안에서 줄바꿈하고, 행이 82px에서 최대 121px까지 부푼다.

#### 680px은 고른 값이 아니라 잰 값이다

칼럼은 셸의 900px 아래에서 `min(760px, 뷰포트 - 64px)`이고 탭 바는 그중 614px을 쓴다.
따라서 탭은 **뷰포트 678px 아래에서** 넘친다. 680px은 모든 탭이 들어가고 **여덟 탭 중
어느 행도 82px을 넘지 않는 마지막 폭**이다.

| 뷰포트 | 칼럼 | 탭 | 가장 높은 행 |
|---|---|---|---|
| 720 | 656 | 다 보임 | 82 |
| 700 | 636 | 다 보임 | 82 |
| **680** | **616** | **다 보임 (614 필요)** | **82** |
| 660 | 596 | 넘침 | 102 (Account · Data) |
| 640 | 576 | 넘침 | 102 |
| 375 | 311 | 넘침 | 121 |

**컨테이너 쿼리가 더 맞는 방아쇠인데 쓸 수 없다.** 깨지는 것은 창이 아니라 칼럼이고
캘린더는 이미 `@container`를 쓴다(`02-calendar.css`). 그런데 `ConfirmModal`이 포털 없이
`.ff-settings-page` 안에서 렌더되므로, `container-type`을 주면 이 요소가 모달의
`position: fixed` 배경의 컨테이닝 블록이 된다. 전체 화면 오버레이가 760px 칼럼 안에
갇힌다. 미디어 쿼리는 그 대가를 치르지 않는다.

#### 고친 것

| | 전 | 후 |
|---|---|---|
| 탭 바 | 넘쳐서 스크롤, 표시 없음 | `flex-wrap: wrap` — 375px에서 3줄 106px, **여덟 개 다 보임** |
| 행 | 라벨/컨트롤 가로 분할, 안에서 줄바꿈 | 세로로 쌓임, 둘 다 같은 왼쪽 가장자리 |
| 오른쪽 정렬 컨트롤 | `flex-end` (마주 볼 라벨이 없는데) | `flex-start` |

**쌓으면 행이 82 → 130px로 커진다.** 그것이 이 수정의 대가이고, 방향은 맞다 — 가로를
유지한 채 안에서 줄바꿈하던 행도 102~121px이었고, 그쪽은 높이를 내주면서 읽히지도 않았다.
680px 위는 아무것도 바뀌지 않는다(1440px에서 칼럼 760 · 탭 바 38 · 행 82, 전과 동일).

375px에서 여덟 탭 전부 확인: **가로 넘침 없음**, 칼럼 밖으로 나가는 요소 없음.
Calendar 탭의 `ff-cal-card-head`(디자인 언어 A)는 이 규칙이 건드리지 않는데도 들어맞는다.

### Phase 8 결과 (완료)

§4.5는 "포커스 세션 기본 길이가 없다"고만 적었는데, 파고 보니 **막고 있던 것이 따로
있었다 — 설정 시스템이 둘이다.**

`FocusUserSettings`(탭 타이머 · 완료 알림 · 미니 타이머 버튼) 셋은 `AppSettings`가 아니라
`focusflow.focusSettings.v1`이라는 **별도 localStorage 키**에 살고, 동기화되지 않고,
설정 화면이 아니라 **Focus 페이지 헤더의 톱니 팝오버** 안에서 고쳐졌다.

#### 저장소는 그대로 두고, 찾는 곳만 하나로

**셋을 `AppSettings`로 옮기지 않았다.** 브라우저 탭 제목, OS 알림, 데스크톱 전용 창 —
셋 다 *이 기기*에 관한 값이다. 동기화하면 데스크톱에서 켠 미니 타이머 버튼이 웹에서
의미 없이 켜져 있게 된다. 옮긴 것은 **어디서 찾느냐**이지 어디에 두느냐가 아니다.

그래서 새 `Focus` 탭은 동기화되는 행 하나와 로컬 행 셋이 나란히 앉는다. 그 이음매는
사용자의 것이 아니므로 화면에 드러내지 않는다.

| | 전 | 후 |
|---|---|---|
| 토글 셋 | Focus 페이지 톱니 팝오버 | 설정 > Focus (저장소는 그대로) |
| 기본 세션 길이 | 없음 (코드 안 상수) | 설정 > Focus, `AppSettings` |

팝오버와 그 CSS(`.foc-options-*`, `.foc-option-row`), `focus.optionsTitle` 문자열은
지웠다. 같은 컨트롤이 두 곳에 있는 것이 한 곳에 있는 것보다 나쁘다.

#### 길이를 정하는 규칙이 파일 하나가 됐다

기존 규칙은 2000줄짜리 훅 안의 인라인 식이었고 **테스트가 없었다**. `startFocusSession`의
`durationMinutes` 인자는 **어떤 호출부도 넘긴 적이 없어서**, 모든 세션 길이가 그 추론이었다.

```text
1. 호출자가 요청한 값       — 아직 아무도 안 넘긴다. 문은 열어 둔다
2. 작업 자신의 시작·종료 폭  — 사용자가 이미 "언제"를 말했다
3. 사용자의 기본값          — 이번에 생긴 것
```

`domain/focus/sessionLength.ts`로 나왔고 테스트 11개가 붙었다.

**`"auto"`가 기본값인 것은 Phase 4의 `weekStart`와 같은 이유다.** 지금까지 우선순위가
high인 작업은 50분, 나머지는 30분이었다 — 인터페이스 어디에도 안 적힌 숫자다. 기본을
어떤 수로 정하면 설정을 한 번도 안 연 사람의 high 작업이 조용히 짧아진다. `"auto"`는
그 50/30을 그대로 유지하고, 수를 고르는 순간 그 휴리스틱은 은퇴한다.

> 목록에 50과 30을 일부러 넣었다. `auto`가 내는 값이므로, 그걸 원하는 사람이 우선순위에
> 기대지 않고 직접 말할 수 있어야 한다.

**한 가지는 고쳤다.** 종료가 시작보다 이른 작업은 인라인 판이 차이를 1로 클램프해서
**1분짜리 세션**을 시작했다. 그건 의도가 아니라 깨진 데이터라서, 이제 기본값으로 넘어간다.

#### 브라우저 실측

빈 프로필에 확인용 작업 둘(시각 없음, high / none)을 심어 세션을 시작하고 되돌렸다.

| 설정 | 작업 | `durationMinutes` |
|---|---|---|
| `25분` | high | **25** (예전 규칙이면 50) |
| `auto` | high | **50** |
| `auto` | none | **30** |

저장 위치도 확인했다 — `focusDefaultMinutes: 25`는 `focusflow.appData.v1`에, 미니 타이머
토글은 `focusflow.focusSettings.v1`에 들어가고 **서로 새지 않는다**. 탭이 아홉이 됐지만
Phase 7의 wrap이 받아낸다(375px에서 아홉 개 전부 보임, 탭 바 106px, 가로 넘침 없음).

#### 남는 것

§4.6 자동 백업은 여전히 열려 있고, 이 단계와 무관하다.

### Phase 9 결과 (완료)

**§2의 "디자인 언어가 셋 공존한다"는 읽기가 틀렸다.** C(레거시)는 Phase 1이 없앴고,
남은 A와 B를 CSS로 나란히 놓으면 **타입 사다리가 같다** — 제목 15px, 힌트 13px
`--text-tertiary`, 제목·힌트 왼쪽 / 컨트롤 오른쪽. 차이는 **38px 아이콘 열 하나**다.

전수해 보면 A는 앱 전체의 경쟁 언어가 아니라 **Calendar 탭 안의 카드 넷**이다. 나머지
여덟 탭은 전부 `SettingsRow`를 쓴다. 그래서 "A로 통일"은 할 일이 아니었다.

#### 진짜 결함: 한 열에 왼쪽 가장자리가 둘

Calendar 탭에서 제목이 두 x좌표에서 시작했다. Phase 5가 넣은 설정 카드에는 머리가
없고 나머지 넷에는 있어서다.

```text
전                                후
x=20   Hours at a time            x=72   ⏱ General          ← 머리를 달았다
x=72   🗂 Category management      x=72   🗂 Category management
x=72   🔗 Share my calendar        x=72   🔗 Share my calendar
x=72   🌐 External calendars       x=72   🌐 External calendars
x=72   🔄 Sync status              x=72   🔄 Sync status
```

**머리를 지우는 대신 다는 쪽을 골랐다.** 아이콘을 버리면 A는 `SettingsRow`로 붕괴해
한 패턴이 되지만, 넷은 각각 자기 목록·상태·액션을 가진 하위 시스템이고 아이콘이 그것을
구분해 준다. `General`도 지어낸 이름이 아니다 — macOS 캘린더 설정이 "한 번에 표시할
시간"을 두는 절의 이름이 그것이다.

**규칙:** 카드는 절이다. **머리를 단 카드가 하나라도 있는 열에서는 모든 카드가 머리를
단다.** 카드가 하나뿐인 탭은 탭 이름이 머리이므로 필요 없다(Appearance·Behavior·
Focus·Data·Notifications). Knowledge는 카드 둘이 **모두** 머리가 없어 그 자체로 일관된다.

#### 같이 고친 것 둘

**탭 바가 전폭으로 늘어났다.** `.ff-segmented`는 `inline-flex`인데 `.ff-page`의 플렉스
아이템이라 **블록화**되어 칼럼을 꽉 채운다. 리뷰가 본 것이 이것이다("탭 여섯 개가 왼쪽에
뭉친다"). Phase 2의 폭 제한이 1326 → 760으로 줄여 놨을 뿐 원인은 남아 있었다.
`align-self: flex-start`로 마지막 탭 오른쪽 여백 **45px → 3px**.

**Calendar 스택이 칼럼을 13px 넘고 있었다.** `.ff-cal-settings-stack`이 암시 트랙
`auto`를 쓰는데 그리드 아이템의 `min-width`가 `auto`라, 트랙이 가장 넓은 아이템의
min-content로 벌어진다 — 375px에서 **311px 칼럼 안에 324px 트랙**, 다섯 카드 전부가
페이지 밖으로 13px. `minmax(0, 1fr)`로 고쳤다.

> Phase 7이 이걸 놓친 이유를 적어 둔다: 그때 Calendar 탭에서 잰 것은 `ff-cal-card-head`
> 요소였고 그건 335px로 칼럼 안에 있었다. **카드 상자 자체는 재지 않았다.** 이번엔 페이지
> 아래 모든 요소의 오른쪽 끝을 훑어서 여덟 탭 전부 넘침 0을 확인했다.

그 스윕이 하나를 더 잡았다 — 카드 머리의 액션 버튼이 `flex-shrink: 0`이라 375px에서
카드 밖으로 12px 밀려났다. 680px 아래에서 머리가 줄바꿈하고 액션이 아랫줄 왼쪽으로
간다. Phase 7이 행에 내린 결정과 같은 모양을 한 치수 위에서 되풀이한 것이다.

#### 남는 것 — 이 단계가 못 고치는 §2

**탭 높이 요동(574 → 1428px).** 그 1428px은 Local AI 탭이고, 그건 Phase 6이다.

### Phase 10 결과 (완료)

§3.4는 둘을 같은 종류로 묶었다 — "기본값이 옳다면 없어도 되는 취향 토글". 코드를 보면
**둘은 같은 것이 아니다.**

| | 읽는 곳 | 판정 |
|---|---|---|
| `showSidebarCounts` | **없음** | 제거 |
| `confirmBeforeDelete` | `App.tsx`의 `requestDeleteTask` | 남긴다 |

**`showSidebarCounts`는 아무것도 하지 않았다.** 저장되고, 동기화되고, 설정 화면에 토글이
있는데 **읽는 렌더 코드가 한 줄도 없다.** 힌트는 "사이드바 항목 옆에 할 일 개수를
표시합니다"라고 적혀 있고, 켜도 꺼도 개수는 나오지 않는다. 취향의 문제가 아니라 **거짓말하는
컨트롤**이고, 그래서 판단이 필요 없다.

> 저장된 값은 지우지 않는다. `normalizeAppSettings`의 M0 스프레드가 모르는 필드를
> 그대로 통과시키므로, 계정에 남아 있는 값은 손대지 않은 채 남는다 — 언젠가 사이드바
> 개수를 실제로 만들면 그 값이 거기 있다. 지운 것은 타입·기본값·정규화 줄·토글·문구다.

**`confirmBeforeDelete`는 남긴다.** 이건 실제로 동작한다. 그리고 §3.4가 몰랐던 사실이
하나 더 있다 — **삭제는 이미 되돌릴 수 있다.** `deleteTaskWithUndo`가 항상 undo 토스트를
띄우므로, 확인 창을 끈 사람도 안전망을 잃지 않는다. 그것이 "없어도 되지 않나"의 근거가
되긴 하지만, **동작하는 안전장치를 사용자 대신 치우는 것은 이 문서의 판단 범위가 아니다.**
지울 이유가 "쓰는 사람이 적을 것 같다"뿐이면 남긴다.

### Phase 11 결과 (완료)

§4.6이 옳았다 — 로컬 우선 앱인데 복구 수단이 수동 `Export JSON` 하나였다. 손대 보니
**앱에 파일 쓰기 경로 자체가 없었다.** `PlatformFiles`는 주석부터가 "Never exposes a
write path"이고(볼트는 읽기 전용 소스다), Tauri capabilities도 `fs:allow-read-*`만 있다.

#### 어디에 쓰는가

**설치본의 앱 데이터 폴더에 진짜 파일로.** 사이트 데이터를 지워도 살아남는 것은 그것뿐이다.
**웹에는 정직한 구현이 없다** — 오리진 저장소 안에 둔 사본은 그것이 지켜야 할 데이터와
함께 사라진다. 그래서 웹은 흉내 내지 않고 행을 비활성으로 두고 **왜인지 말한다.**

#### 쓰기 경로를 Rust에 둔 이유

`fs:allow-write-text-file` 권한을 프론트에 주는 대신 명령 넷을 만들었다.

```text
get_backups_dir / list_backups / read_backup / write_backup
```

**프론트엔드는 디렉터리를 이름 대지 않는다.** 내용과 보관 개수를 넘길 뿐이고, 유일하게
파일명을 받는 명령은 **이 코드가 만든 이름만** 받는다 — 접두사·확장자가 맞고 구분자나
`..`이 없어야 하며, 아니면 정제하지 않고 거절한다. 쓸 수 있는 곳을 넓히려면 설정이 아니라
코드를 고쳐야 한다. `models_dir`·`ensure_knowledge_db_dir`와 같은 신뢰 모델이다.

**가지치기는 쓰기 다음에 한다.** 실패한 쓰기가 이미 가장 오래된 사본을 없앤 뒤이면 안 된다.

#### 복원기를 새로 만들지 않았다

백업 파일은 **`Export JSON`이 내놓는 것과 같은 바이트**다. 그래서 복원 경로는 이미 있는
가져오기이고, 형식 하나에 읽는 쪽도 하나다. 새 형식을 만들었다면 계속 맞춰야 할 리더가
둘이 됐을 것이다. `마지막 백업` 행의 문구가 그 경로를 가리킨다.

#### 판단 셋

- **기본값은 `끔`.** 남의 기계에 예정된 파일 쓰기를 묻지도 않고 시작하지 않는다.
- **"한 번도 안 함"은 밀린 것으로 친다.** 설정을 켜면 한 주기 뒤가 아니라 지금 사본이
  생긴다 — 메우려는 구멍은 이미 열려 있다.
- **시계가 뒤로 갔으면 밀린 것으로 친다.** 표준시 변경이나 NTP 보정으로 `lastAt`이 미래에
  박히면, 백업이 가장 필요한 그때 멈춰 있게 된다.

**마지막 백업 시각은 `AppSettings`가 아니라 로컬 키에 있다.** 동기화하면 한 기계의 백업이
다른 기계의 일정을 채워 버리는데, 정작 그 사본은 다른 기계가 닿을 수 없는 디스크에 있다.

#### 검증 — 무엇을 봤고 무엇을 못 봤나

| | 방법 | 결과 |
|---|---|---|
| Rust 명령 넷 | `cargo check` | **컴파일됨** |
| 예약 규칙 | 단위 테스트 10개 | 통과 |
| 웹 분기 | 브라우저 | 행 비활성 + 이유 문구 |
| React 상태 기계 | 어댑터를 메모리 구현으로 잠시 바꿔 실측 | 아래 |

프로브에서 확인한 것: `매일`로 바꾸자 보관 개수 행이 나타나고, **자동 검사가 스스로
파일을 만들었다**(수동 클릭분과 4초 차이로 둘). 페이로드는 유효한 JSON이고 키가
`tasks · projects · subtasks · focusSessions …` — 내보내기와 같다. `keep`이 7로 전달되고
마지막 시각이 로컬 키에 저장되며, 실패를 강제하면 행이 **"The last attempt failed: disk is
full (probe)"**로 바뀐다. 조용히 넘어가지 않는다.

> **못 본 것을 분명히 적는다.** 이 환경은 웹 dev 서버라 **Rust가 실제로 도는 것은 보지
> 못했다.** 파일이 진짜 디스크에 생기는지, 가지치기가 오래된 것부터 지우는지, `폴더 열기`가
> 탐색기를 여는지는 **설치본을 빌드해서 확인해야 한다.** 컴파일과 프론트 계약까지가
> 여기서 낼 수 있는 증거다.

---

## 6. 확인 못 한 것

- ~~**태블릿·모바일 폭의 설정 화면.**~~ Phase 7에서 375–1440px을 쟀다.
- **Knowledge base / Local AI 탭의 실제 동작.** 웹에서는 잠긴 상태만 보인다.
- **설정 값이 저장되는 경로.** 이 리뷰는 화면과 구조만 봤다.
- **자동 백업의 데스크톱 런타임.** Phase 11은 `cargo check`까지다 — 설치본에서 파일이
  실제로 생기고 가지치기가 도는지는 빌드해서 봐야 한다.
