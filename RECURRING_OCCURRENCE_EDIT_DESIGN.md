# 반복의 회차를 편집한다

> 상태: **설계 · 착수 전** · 2026-09-09
> 요청: "이번 회차만 옮기고, 이번 회차만 건너뛰고, 이번 회차만 고치고, 이 회차 이후 전부 바꾼다.
> 구글에서 온 반복 일정과 앱에서 만든 반복 작업 **둘 다**. 앱 반복도 앞으로의 회차가 다 보였으면."
> 선행 문서: `GOOGLE_CALENDAR_SYNC_DESIGN.md` §8 (반복 단방향) ·
> `GOOGLE_INBOUND_TASKS_DESIGN.md` (반복 원본/회차는 Task로 승격하지 않는다)
> 이 문서는 그 둘의 "반복은 손대지 않는다"를 **처음으로 뒤집는다.**

---

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 코드에서 잰 것 — 반복 모델이 둘이다 · Task 는 rolling 이고 회차 개념이 없다 · 완료된 과거 회차는 **이미 별도 Task 행으로 실체화되어 있다** · 외부 일정은 펼쳐지고 회차는 `readOnly` 다 | [실측] |
| **결정** | 사용자 — 둘 다 대상 · 앞으로의 회차도 보인다 · 네 동작 전부 | [결정] |
| **규격** | Google Calendar API — `events.instances` · `recurringEventId` · `originalStartTime` · `EXDATE` | [규격] |
| **열림** | 밀린 반복을 어떻게 보일 것인가 (§5.3) — 이 문서가 정하지 않고 묻는다 | [열림] |

---

## 1. 지금 무엇이 있는가 — 반복 모델이 **둘**이다 [실측]

같은 앱 안에 서로 다른 반복 모델이 둘 돌아간다. 이것이 이 설계의 출발점이다.

### 1.1 Task — rolling. 회차가 없다

`Task` 가 가진 반복 필드는 넷뿐이다 (`types.ts:222`):

```ts
repeatType: RepeatType;     // none | daily | weekly | monthly | yearly
repeatInterval: number;
repeatDays: number[];       // JS 요일, 0 = 일요일
repeatEndDate: string;
```

**예외를 저장할 자리가 없다.** `exdates` 도 `recurrenceId` 도 없다.

그리고 펼치지 않는다. 반복 작업은 레코드 **하나**이고 `dueDate` **하나**를 갖는다. 완료하면
`getNextDueDate` 가 그 `dueDate` 를 앞으로 굴린다 (`utils/planner.ts:131`).

즉 **캘린더에 한 번만 그려진다.** 다음 회차는 아직 존재하지 않는다.

### 1.2 그런데 과거는 이미 실체화되어 있다 [실측]

`planRecurringCompletion` (`utils/planner.ts:182`) 이 하는 일:

```
완료 →  ① 완료된 스냅샷 Task 를 새 id 로 만든다 (repeat 필드는 비운다)
        ② 원본의 dueDate 를 다음 회차로 굴린다
```

주석이 이유를 말한다 — `completedAt` 을 원본에 쓰면 "오늘 완료됨" 으로 읽혀 열린 목록에서
사라지기 때문이다. 그래서 **끝난 회차는 진짜 Task 행으로 남는다.**

> **이 사실이 설계를 쉽게 만든다.** 앱은 이미 "과거 회차 = 실체 · 미래 회차 = 없음" 이라는
> 반쪽 시리즈 모델이다. 이 문서가 하는 일은 **미래 쪽을 채우는 것**이지 모델을 갈아엎는 것이
> 아니다.

### 1.3 외부 일정 — series. 회차가 있는데 읽기 전용이다

`ExternalCalendarEvent` 는 반대로 시리즈 어휘를 전부 갖췄다 (`types.ts:1005~1023`):

```ts
recurrence?: IcsRecurrence;  // 규칙. 원본에만 있다
exdates?: string[];          // 주최자가 취소한 회차
recurrenceId?: string;       // 이 레코드가 대체하는 회차
occurrenceOf?: string;       // 펼쳐진 회차가 나온 시리즈
```

`expandIcsOccurrences` (`lib/ics/recurrence.ts:204`) 가 범위를 받아 펼치고, 편집된 회차를
끼워 넣고, 취소된 회차를 뺀다. **읽기는 이미 제대로 한다.**

없는 것은 쓰기다. 펼친 회차는 `readOnly: true` 로 나오고, 코드가 이유까지 적어뒀다:

> *"회차는 이 목록 안에만 있는 합성 id 라, 그 id 로 쓰면 아무것도 못 찾고 사라진다. 한 회차를
> 편집하려면 `events.instances` 로 패치해야 하는데 아직 아무도 그러지 않는다. 그때까지는
> 아무 일도 안 하는 드래그를 내놓느니 못 한다고 말한다."*

v0.22.13 이 이 `readOnly` 를 넣었다. **이 문서는 그 주석이 지목한 일을 하는 것이다.**

### 1.4 그리고 이미 비대칭이 있다 [실측]

`toRrule` (`eventShape.ts:111`) 이 Task 의 반복 필드를 RRULE 로 만들어 구글에 보낸다.

```
FocusFlow:  반복 작업 1개  →  캘린더에 1번 그려짐
Google:     RRULE 시리즈   →  격자에 N번 그려짐
```

**같은 작업이 구글에서는 여러 번, 앱에서는 한 번 보인다.** 사용자가 "앞으로의 회차도 다
보였으면" 이라고 한 것은 이 비대칭을 지적한 것이다.

---

## 2. 결정 요약

| # | 결정 | 근거 절 |
|---|---|---|
| **R1** | 구글에서 온 반복 일정과 앱 반복 작업 **둘 다** 회차 편집 대상 [결정] | §6, §7 |
| **R2** | 앱 반복 작업도 **앞으로의 회차를 캘린더에 그린다** [결정] | §3 |
| **R3** | 네 동작 전부 — 이동 · 건너뛰기 · 내용 수정 · 이 회차 이후 전부 [결정] | §6 |
| **R4** | 미래 회차는 **가상으로 펼치고, 손댄 회차만 실체화한다** | §3 |
| **R5** | 과거 완료 회차는 지금 방식 그대로 둔다. 이미 맞다 | §1.2 |
| **R6** | Task 의 회차 어휘를 `ExternalCalendarEvent` 의 것과 **같은 이름으로** 맞춘다 | §4 |
| **R7** | 회차 편집은 **전용 캘린더 밖의 구글 일정에도** 연다 — `events.instances` 로 | §7 |
| **열림** | 밀린 반복을 어떻게 보일 것인가 | §5.3 |

---

## 3. 핵심 결정 — 가상으로 펼치고, 손대면 실체화한다 (R4)

세 가지 길이 있고, 앞의 둘은 못 쓴다.

| 안 | 무엇 | 왜 안 되는가 |
|---|---|---|
| 전부 실체화 | 회차마다 Task 행을 미리 만든다 | 끝없는 반복은 행이 무한하다. 동기화·저장이 폭증하고, 규칙을 바꾸면 전부 다시 써야 한다 |
| 전부 가상 | 회차를 항상 규칙에서 계산한다 | 회차별 override 를 **저장할 곳이 없다.** 편집 자체가 불가능하다 |
| **가상 + 손대면 실체화** | 규칙으로 펼치되, 편집된 회차만 진짜 행이 된다 | — |

세 번째가 iCalendar 가 `RRULE` + `RECURRENCE-ID` 로 하는 것이고, 구글이
`recurringEventId` + `originalStartTime` 으로 하는 것이며, **이 앱이 외부 일정에 대해 이미
하고 있는 것**이다 (§1.3). 새 발명이 아니라 이미 있는 어휘를 Task 로 넓히는 것이다.

```
원본 Task (시리즈)          가상 펼치기              실체화된 회차
repeatType: weekly    →     9/09 ┐
dueDate: 2026-09-09         9/16 ├ 규칙에서 계산      9/23 → Task 행
                            9/23 ┘  (저장 안 함)      (목요일로 옮겨짐)
                            …                        seriesId + recurrenceId
```

---

## 4. Task 모델 변경 (R6)

`ExternalCalendarEvent` 가 쓰는 이름을 그대로 가져온다. 두 모델이 같은 어휘를 쓰면 캘린더
렌더링과 팝오버가 갈래를 하나 덜 갖는다.

```ts
export interface Task {
  // …
  /** 이 시리즈에서 건너뛴 회차의 원래 날짜들. 원본에만 있다. */
  exdates?: string[];
  /**
   * 이 레코드가 대체하는 회차의 **원래 날짜**. 옮긴 뒤의 날짜가 아니다.
   * 있으면 이 Task 는 시리즈의 한 회차이지 독립 작업이 아니다.
   */
  recurrenceId?: string;
  /** 이 회차가 속한 시리즈 원본의 Task id. */
  occurrenceOf?: string;
}
```

셋 다 **선택 필드**다. `normalizeTask` 가 미지 필드를 먼저 spread 하므로
(`normalize.ts`) **테이블 변경도 마이그레이션도 없다** — M0. 이 필드를 모르는 구버전
클라이언트는 지우지 않고 실어 나른다.

`repeatType` 등 기존 넷은 그대로 둔다. 이 설계는 규칙 표현을 바꾸지 않는다.

### 4.1 완료 스냅샷과의 통합

§1.2 의 완료 스냅샷은 **사실상 이미 override 다** — 원본을 대체하는, 그 회차만의 레코드.
지금은 그 사실을 기록하지 않아서 "언제의 회차였는지" 를 잃는다.

`planRecurringCompletion` 이 만드는 스냅샷에 `recurrenceId` + `occurrenceOf` 를 채우면:

- 완료 회차와 편집 회차가 **같은 종류의 레코드**가 된다
- 펼치기가 "이 날짜는 이미 실체가 있다" 를 한 규칙으로 판정한다
- 기존 데이터는 두 필드가 비어 있을 뿐이고, 그건 지금과 똑같이 동작한다

---

## 5. 펼치기 — `expandTaskOccurrences`

`expandIcsOccurrences` 와 같은 모양의 함수를 Task 에 대해 만든다. 범위를 받아 그 범위에
닿는 회차를 돌려준다.

```
1. 원본의 규칙으로 [from, to] 안의 날짜를 만든다
2. exdates 에 있는 날짜는 뺀다
3. recurrenceId 가 그 날짜인 실체 Task 가 있으면 그것으로 갈아끼운다
4. 나머지는 가상 회차로 만든다 — 원본에서 파생, 자기 id 는 합성
```

3번이 완료 스냅샷과 편집 회차를 **동시에** 처리한다. 둘 다 `recurrenceId` 를 갖기 때문이다.

### 5.1 가상 회차의 id

`expandIcsOccurrences` 가 이미 답을 냈다 — 회차마다 자기 id 를 준다. 원본 id 를 공유하면
id 로 키를 잡는 목록이 하나만 보여주고 나머지를 조용히 버린다.

Task 도 같게 한다: `${seriesId}::${originalDate}`. 실체화되면 진짜 Task id 를 받는다.

### 5.2 범위가 필요하다

끝없는 반복은 범위 없이 펼칠 수 없다. 캘린더는 이미 보이는 범위를 안다
(`buildCalendarItems`). 리스트·보드·타임라인은 범위가 다르므로 §9 에서 따로 본다.

### 5.3 [열림] 밀린 반복을 어떻게 보일 것인가

**이것이 이 설계에서 유일하게 안 정한 것이고, 정해야 한다.**

지금 `getNextDueDate` 는 밀린 반복을 **오늘로 끌어온다** (`planner.ts:139`):

```ts
const baseDate = task.dueDate && task.dueDate > today ? task.dueDate : today;
```

주석의 이유: *"순전히 dueDate 로만 하니 일주일 방치한 반복이 또 과거 날짜를 만들어서,
사용자가 밀린 만큼 완료를 반복해야 따라잡혔다."*

그런데 **시리즈 펼치기는 드리프트하지 않는다.** 규칙대로 계산하면 매주 반복을 3주 방치했을 때
밀린 회차 3개가 전부 나타난다.

| 안 | 3주 밀린 매주 반복이 | 성격 |
|---|---|---|
| **가** | 지난 회차 3개가 다 보인다 | 일정 관점. 정직하지만 밀린 사람에게 잔해를 쌓는다 |
| **나** | 오늘 하나로 합쳐 보인다 (현행) | 할 일 관점. 따라잡기 쉽지만 "9/16 것" 이 사라진다 |
| **다** | 지난 회차는 N개까지만 보인다 | 절충. 경계가 자의적이다 |

**추천은 「나」의 유지다** — 단, 미래 쪽만 펼친다. 근거: 이 앱은 할 일 앱이고, 현행 동작에
이미 그 판단이 담겨 있으며 주석이 그 이유를 적어뒀다. 사용자가 원한 것은 *"앞으로의 회차"*
였지 밀린 회차가 아니다.

구현으로는 **펼치기 범위의 시작을 `max(from, today)` 로 자른다.** 과거 쪽은 실체화된
회차(완료 스냅샷·편집 회차)만 보인다 — 그건 지금과 정확히 같다.

> 이 절은 결정을 기다린다. 「가」를 고르면 §6 의 완료 동작이 통째로 달라진다.

---

## 6. 네 동작 (R3)

`recurrenceId` 를 쓰는 회차 하나를 **X**, 시리즈 원본을 **S** 라 한다.

### 6.1 이번 회차만 옮긴다

```
S 는 그대로 둔다.
X 를 실체 Task 로 만든다:
  recurrenceId = 원래 날짜   ← 옮기기 전
  occurrenceOf = S.id
  dueDate/startTime/endTime = 새 값
  repeat* = 비운다           ← 회차는 스스로 반복하지 않는다
```

`repeat*` 를 비우는 것은 완료 스냅샷이 이미 하는 일이다 (§1.2). 같은 이유다.

### 6.2 이번 회차만 건너뛴다

```
S.exdates 에 원래 날짜를 더한다. 끝.
```

넷 중 가장 싸다. 실체 레코드가 생기지 않는다.

이미 실체화된 회차를 건너뛰면 그 Task 를 휴지통으로 보내고 `exdates` 에도 더한다 — 안 그러면
다음 펼치기가 가상 회차로 되살린다.

### 6.3 이번 회차만 내용을 고친다

6.1 과 같은 실체화이고, 바뀌는 필드가 제목/본문일 뿐이다. **한 경로로 구현한다** — "회차를
실체화한 뒤 필드를 쓴다".

### 6.4 이 회차 이후 전부 바꾼다

넷 중 유일하게 시리즈를 건드린다. iCalendar 가 하는 것과 같이 **쪼갠다**:

```
S  : repeatEndDate = X 의 전날로 잘라낸다
S' : 새 Task. 새 규칙 + dueDate = X 의 날짜
     S 의 exdates 중 X 이후의 것을 가져간다
     S 의 실체 회차 중 X 이후의 것은 occurrenceOf 를 S' 로 옮긴다
```

**과거는 건드리지 않는다.** 이미 끝난 회차의 기록을 고쳐 쓰지 않는다는 것은 이 앱이
`isSyncEligible` 에서 이미 정한 태도다 — *"끝난 일도 그날 있었던 일"*.

주의: `repeatEndDate` 는 이미 있는 필드다. 쪼개기는 **새 개념을 만들지 않는다.**

---

## 7. 구글 쪽

### 7.1 외부 일정의 회차 — `events.instances` [규격]

`lib/ics/recurrence.ts` 주석이 지목한 그 길이다.

지금 인바운드는 `singleEvents=false` 로 **원본만** 받는다 (`googleCalendarInbound.ts:127`).
그래서 회차의 구글 id 를 갖고 있지 않고, 그래서 쓸 곳이 없다.

바꾸지 않는다 — 전체 목록을 회차로 펼쳐 받으면 수백 개가 오고, 앱이 직접 펼치는 이유가
사라진다. 대신 **편집하는 순간에만** 그 회차의 실제 id 를 가져온다:

```
회차 편집 클릭
  → events.instances(원본 id, originalStart 부근)  ← 그 회차 하나의 실제 id
  → 그 id 로 PATCH (이동·내용) 또는 DELETE (건너뛰기)
  → 다음 인바운드 패스가 override 를 원본과 함께 받아온다
```

읽기 모델은 이미 override 를 처리한다 (`recurrenceId` 로 갈아끼운다). **인바운드는 바꿀 게
없다.**

`readOnly: true` 는 그대로 두되 조건이 붙는다 — 캘린더의 `accessRole` 이 owner/writer 이고
회차 편집 경로가 켜져 있을 때만 푼다.

### 7.2 앱 반복 작업의 회차가 구글로 나갈 때

Task 시리즈는 이미 RRULE 로 나간다 (§1.4). 회차를 실체화하면 구글에도 override 가 되어야
한다. 같은 규격이다:

| 앱 | 구글 |
|---|---|
| `recurrenceId` | `originalStartTime` |
| `occurrenceOf` | `recurringEventId` |
| `exdates` | 그 회차 인스턴스를 `cancelled` 로 |

**단 이것은 `docs/google-task-rollout.md` 의 반복 정책과 충돌한다** — 지금 정책은 *"일반 공유
필드 PATCH 에는 반복 규칙을 넣지 않는다"* 이고 *"Google 의 신규 반복 원본/회차는 Task 로
승격하지 않는다"* 이다. 이 설계는 그 둘 중 **앞의 것만** 넓힌다. 뒤의 것(승격 금지)은 그대로
둔다 — 남의 반복 회의가 내 할 일이 되는 것은 여전히 원하지 않는다.

### 7.3 순서

**7.1 이 먼저다.** 외부 일정 쪽은 읽기 모델이 이미 완성되어 있어 쓰기만 붙이면 된다.
7.2 는 021–034 가 운영에 올라가고 작업 동기화가 켜진 뒤에야 검증할 수 있다.

---

## 8. UI

구글·애플이 쓰는 3지 선택을 따른다. 회차를 편집하고 저장할 때 묻는다:

```
┌─────────────────────────┐
│ 반복 작업을 수정합니다   │
│  ○ 이번 회차만           │  → §6.1 / §6.3
│  ○ 이번 회차 및 이후      │  → §6.4
│  ○ 전체 시리즈           │  → 지금의 동작 (원본을 고친다)
└─────────────────────────┘
```

삭제도 같은 셋이고, "이번 회차만" 이 §6.2 다.

**묻지 않는 경우가 있다**: 반복이 아닌 작업, 그리고 이미 실체화된 회차(자기 자신만 고치는
것이 자명하다).

---

## 9. 파급 — 캘린더 밖

미래 회차가 생기면 캘린더 말고도 영향을 받는다. 각각 **범위를 갖는가**가 갈림길이다.

| 곳 | 범위 | 판단 |
|---|---|---|
| 캘린더 격자 | 보이는 주/월 | 펼친다. 이 설계의 목적 |
| 타임라인/간트 | 보이는 구간 | 펼친다 |
| 리스트 (오늘/예정) | 날짜 필터 | 펼친다. 단 §5.3 의 답에 달렸다 |
| 보드 | **없음** | 펼치지 않는다. 칸반에 같은 카드가 52장 뜨면 못 쓴다 |
| 검색 | 없음 | 펼치지 않는다. 원본과 실체 회차만 |
| 리마인더 큐 | 다음 발생 | 지금 로직 유지. 가상 회차에는 알림을 걸지 않는다 |
| 포커스 세션 | 현재 | 실체 회차에만 붙는다 |
| Supabase 동기화 | — | 가상 회차는 **저장하지 않는다.** 규칙에서 다시 나온다 |

마지막 줄이 R4 의 실질적 이유다.

---

## 10. 단계

| 단계 | 내용 | 크기 |
|---|---|---|
| **M1** | Task 에 `exdates`/`recurrenceId`/`occurrenceOf` 추가. 완료 스냅샷이 채우게 한다 | 작음. M0, 마이그레이션 없음 |
| **M2** | `expandTaskOccurrences` + 캘린더가 미래 회차를 그린다 | 중간. §5.3 의 답이 필요 |
| **M3** | §6.1~6.3 — 이동 · 건너뛰기 · 내용 수정. §8 의 3지 선택 | 중간 |
| **M4** | §6.4 — 이 회차 이후 전부. 시리즈 쪼개기 | 중간 |
| **M5** | §7.1 — 외부 일정 회차를 `events.instances` 로 편집. `readOnly` 를 조건부로 | 중간 |
| **M6** | §7.2 — Task 회차 override 가 구글로. **021–034 운영 적용 이후** | 큼 |

M1 은 지금 당장 할 수 있고 아무것도 바꾸지 않는다 — 기록만 시작한다. M5 는 M2~M4 와 독립이라
병렬로 갈 수 있다.

---

## 11. 열린 질문

1. **§5.3 — 밀린 반복.** 「가」(다 보인다) / 「나」(오늘 하나, 추천) / 「다」(N개까지).
   이 답이 M2 의 모양을 정한다.
2. 시리즈 원본의 `dueDate` 는 무엇을 뜻하는가 — 지금은 "다음 회차" 이고 시리즈로 보면
   "첫 회차" 다. §5.3 에서 「나」를 고르면 둘이 같아지므로 질문이 사라진다.
3. 완료 스냅샷에 `recurrenceId` 를 채우는 것을 **기존 데이터에 소급**할 것인가. 안 해도
   동작하지만(§4.1), 과거 회차의 날짜를 복원할 수 없게 된다.
