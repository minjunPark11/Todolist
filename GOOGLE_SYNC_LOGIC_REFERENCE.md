# 구글 캘린더 동기화 로직 — 지금 코드가 실제로 하는 일

> 상태: **구현 기준 정리** · 2026-09-08 · v0.22.12 + `feat(calendar): 구글 일정을 격자에서 끌고, 늘이고, 이름을 고친다`
> 성격: `GOOGLE_CALENDAR_SYNC_DESIGN.md`가 **무엇을 하기로 했는지**를 적는다면,
> 이 문서는 **지금 코드가 무엇을 하는지**를 적는다. 둘이 어긋나는 자리는 §7에 모아 두었다.
> 대상: `domain/calendar/googleSync/*` · `lib/googleCalendar*` · `hooks/useGoogle*Sync.ts` · `components/CalendarView.tsx`

---

## 0. 먼저 — 레코드가 둘이다

화면의 블록은 비슷하게 생겼지만 뒤에 있는 것은 둘이다. 편집이 어느 경로를 타는지는 전적으로
이 구분이 정한다 — 결정 P3(`Task` 모델을 건드리지 않는다)이 만든 갈림길이다.

| 레코드 | 누가 소유 | 어디에 산다 |
|---|---|---|
| `Task` | FocusFlow | 로컬 + 계정 동기화. 날짜가 있으면 전용 캘린더로 **나간다** |
| `ExternalCalendarEvent` | 구글 | 구글이 보내준 것의 미러. 앱은 고쳐서 **되돌려쓴다** |

그래서 경로가 셋이다.

```
FocusFlow                                            Google
─────────────────────────────────────────────────────────────────────
Task ─────── A · insert/patch/delete ──────────▶ "FocusFlow" 캘린더
             (편집 후 1.8초 · 창 포커스)
     ◀╌╌╌╌╌╌ ✗ 돌아오는 길 없음 ╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌

ExternalCalendarEvent ◀── B · events.list(syncToken) ── 설정에서 고른 캘린더
                          (앱 열 때 · 창 포커스 · 연결 변경)
                      ─── C · events.patch/delete ───▶
                          (격자에서 고치는 즉시)
```

**셋 중 둘(B·C)만이 한 레코드를 사이에 두고 마주 보는 진짜 양방향이다.** 태스크는 나가기만
하고, 그 사실이 아래 규칙의 절반을 설명한다.

| 내가 한 일 | 레코드 | 경로 | 도착지 |
|---|---|---|---|
| 태스크의 제목 · 날짜 · 시각 · 반복을 고침 | `Task` | **A** | 전용 "FocusFlow" 캘린더 |
| 태스크를 휴지통에 넣거나 날짜를 지움 | `Task` | **A** | 전용 캘린더에서 `events.delete` |
| 구글 앱에서 일정을 만들거나 고침 | `ExternalCalendarEvent` | **B** | 앱의 외부 이벤트 목록 |
| 격자에서 구글 일정을 끌기 · 늘이기 · 이름 고치기 | `ExternalCalendarEvent` | **C** | 그 일정이 사는 구글 캘린더 |
| 격자에서 구글 일정을 삭제 | `ExternalCalendarEvent` | **C** | 구글에서 `events.delete` |

---

## 1. 경로 A — FocusFlow에서 태스크를 고쳤을 때

`hooks/useGoogleOutboundSync.ts` · `domain/calendar/googleSync/outboundPlan.ts` ·
`lib/googleCalendarOutbound.ts`

### 1.1 언제 도는가

| 방아쇠 | 무엇 |
|---|---|
| 디바운스 | 태스크나 툼스톤이 바뀌고 **1.8초** 뒤 — ICS 재발행과 같은 간격 |
| 포커스 | 창이 포커스를 얻을 때 — 백그라운드에서 벌어진 것을 줍는다 |
| 연결 변경 | `GOOGLE_CONNECTION_CHANGED` |
| 공짜 패스 | `isEmptyPlan`을 **토큰 요청 전에** 본다. 할 일 없는 패스는 요청 0건 |

자주 돌아도 되는 이유가 마지막 줄이다. 쓰기 결과를 태스크에 적으면 태스크가 바뀌고
디바운스가 다시 걸리는데, 그 패스의 계획이 비어 요청 없이 멈춘다. 스스로를 부르는 루프가
여기서 끊긴다. `running` ref가 패스의 중복 실행도 막는다.

### 1.2 나갈 자격

`isSyncEligible` = **날짜가 있고**(`dueDate`가 `YYYY-MM-DD`) **살아 있을 것**(`isTaskAlive` —
휴지통 · 포기 제외). 그뿐이다.

- **완료된 태스크도 계속 나간다.** 끝난 일도 그날 일어난 일이라, 체크하는 순간 캘린더에서
  지우면 그날의 기록이 다시 쓰이는 셈이다.
- **"구글에 이벤트가 있는가"는 자격에 들어가지 않는다.** 그 질문의 답은 `googleEventId`이고,
  둘을 한 조건으로 섞으면 API 실패가 "사용자가 저쪽에서 지웠다"로 읽힌다(§5.1이 거부한 그 길).
- 날짜 없는 태스크 · 인박스 · 우선순위 · 리스트 · 태그 · 체크리스트는 구글 이벤트에 담길
  자리가 없어 나가지 않는다.

### 1.3 한 패스가 하는 일 — 네 더미, 이 순서로

| # | 더미 | 무엇 |
|---|---|---|
| 1 | `create` | 자격 있고 `googleEventId`가 없는 것. **실패한 쓰기의 재시도 경로이기도 하다** — 실패는 아무것도 적지 않으므로 다음 패스가 같은 계획을 다시 세운다 |
| 2 | `update` | 자격 있고 id도 있고 `updatedAt > googleSyncedAt`인 것 |
| 3 | `delete` | id는 있는데 자격을 잃은 것 — 휴지통에 갔거나 날짜가 지워졌다 |
| 4 | `orphans` | 태스크가 완전 삭제돼 물어볼 곳이 없는 이벤트(§4.3 툼스톤) |

`needsUpdate`(2번)가 없으면 날짜 있는 태스크 300개가 창 포커스마다 300번 다시 쓰인다.

순서는 우연이 아니다. create가 먼저라야 실패한 것이 다음 패스로 넘어가고, delete가 create보다
먼저면 없는 것을 지운다. 401이 한 번 나오면 나머지도 전부 같은 이유로 실패하므로 패스가 그
자리에서 멈춘다.

### 1.4 태스크가 이벤트로 번역되는 규칙

`toGoogleEventBody` — 타임존은 `appSettings.timezone`.

| 태스크 | 구글 이벤트 | 주의 |
|---|---|---|
| `title` | `summary` | — |
| `description` | `description` | 있을 때만 |
| `startTime` 있음 | `start/end.dateTime` + `timeZone` | 끝이 없거나 시작보다 이르면 **+1시간**(23시 이후 23:59 클램프) — 발행 중인 ICS와 같은 답 |
| 시각 없음 | `start/end.date` | 종일. `end`는 배타적이라 **다음 날** |
| `startDate < dueDate` | 여러 날 종일 이벤트 | 범위가 그대로 이벤트 길이가 된다 |
| `repeatType` | `recurrence: [RRULE]` | FREQ · INTERVAL · BYDAY · UNTIL. **아웃바운드 전용**(§8) |

태스크는 시각을 기기 로컬 벽시계 문자열로만 든다(`"2026-09-04"` + `"14:00"`). RFC3339 + IANA
타임존으로의 변환은 **이 직렬화 함수 한 곳에서만** 일어난다 — 도메인 계층은 계속 벽시계를 쓴다(§9.2).

### 1.5 충돌 — If-Match, 그리고 412

1. PATCH에 `If-Match: googleEtag`를 건다. 우리가 본 적 없는 버전은 조용히 덮이지 않는다.
2. 412가 오면 GET으로 상대를 읽는다. 412는 "구글이 우리가 마지막으로 합의한 것보다 새 것을
   들고 있다"는 뜻이다.
3. 상대의 `updated`가 우리 `updatedAt`보다 나중이면 **덮지 않는다.** 대신 상대의 etag를
   저장한다 — 내용에 동의해서가 아니라, 다음 패스가 같은 싸움을 다시 하지 않게 하려고.
4. 아니면 `If-Match` 없이 재시도한다. 마지막 수정이 이긴다(P4, 레코드 단위).

**진 쪽도 `googleSyncedAt`은 갱신한다.** 안 그러면 같은 412를 매 패스마다 영원히 다시 만난다.
대신 그 편집은 구글에 반영되지 않은 채 로컬에만 남는다 — 다음번에 그 태스크를 다시 고쳐
`updatedAt`이 앞설 때까지.

### 1.6 답이 이상하게 왔을 때

| 응답 | 결론 | 다음 패스 |
|---|---|---|
| 200 / 201 | `googleEventId` · `googleEtag` · `googleSyncedAt`을 태스크에 기록 | 안 건드림(`needsUpdate`가 false) |
| 404 / 410 | 그 이벤트는 없다는 **구글의 명시적 답** → 매핑만 해제. 태스크는 안 건드린다 | **다시 만든다** |
| 412 | §1.5의 LWW 판정 | 이긴 쪽에 따라 |
| 401 | 권한이 죽었다 → 패스 전체 중단 | 재연결 전까지 아무것도 |
| 그 외 · 네트워크 | **아무것도 기록하지 않음** | 같은 계획을 그대로 다시 |

404를 "매핑 해제"로 읽는 것과, §7.1이 금지하는 "목록에서의 부재를 삭제로 읽기"는 다르다.
전자는 지목한 이벤트에 대한 구글의 답이고, 후자는 우리 쪽 침묵을 근거로 삼는 일이다.

### 1.7 삭제 — 두 갈래

- **휴지통 · 날짜 삭제** — 태스크는 아직 `googleEventId`를 들고 있으므로 delete 더미가 지운다.
  휴지통에서 복구하면 자격을 되찾아 **새 id로 다시 만들어진다**.
- **완전 삭제** — 레코드가 사라지면 물어볼 곳이 없어진다. 그래서 지우기 직전에
  `tombstonesAfterRemoval`이 `appSettings.googleDeletedEventIds`에 id를 남기고, 다음 패스가
  그것을 지운다. **성공한 삭제만** 목록에서 빠지므로(`withoutTombstone`) 네트워크 실패는 일을
  잃지 않는다. 툼스톤은 delete 더미가 이미 맡은 이벤트(`spokenFor`)를 제외하고 중복도 제거한다.

---

## 2. 경로 B — 구글 캘린더에서 고쳤을 때 (인바운드)

`hooks/useGoogleInboundSync.ts` · `domain/calendar/googleSync/inboundPlan.ts` ·
`lib/googleCalendarInbound.ts`

디바운스가 없다. 아웃바운드는 사용자의 타자가 멎기를 기다려야 하지만, 인바운드의 방아쇠는
**창이 돌아오는 것**이고 그건 이미 정의상 안정된 신호다.

### 2.1 어느 캘린더를 읽는가

- `google_calendar_sources`에서 **`selected = true`인 것만**. 새로 발견된 캘린더는 기본이
  **해제**라, 회사 일정 · 생일 · 공휴일 · 남이 보낸 초대가 저절로 넘어오지 않는다.
- 편집 가능 여부는 추측하지 않고 **`accessRole`을 읽는다** — `owner`/`writer`만 쓰기 가능.
  구독한 공휴일 캘린더는 읽히기만 한다.
- 캘린더를 끄면 **그 이벤트도 함께 사라진다**(`reconcileGoogleCalendars`). 단, 이번 라운드의
  목록에 아예 없는 캘린더(읽기 실패 · 아직 안 적힌 행)는 **남긴다** — 타임아웃 하나로 남의
  일정을 지울 수는 없다.

### 2.2 한 캘린더, 한 패스

1. **`events.list`를 부른다.** `maxResults=250`, `showDeleted=true`. 저장된 `syncToken`이 있으면
   증분, 없으면 전체(`singleEvents=false` — 반복 전개는 앱이 한다). 페이지는 최대 40장.
2. **410이면 커서를 버리고 전체를 다시 나열한다.** 410은 에러가 아니라 "커서가 너무
   오래됐다"는 말이고, 유일하게 옳은 복구가 전체 재나열이다.
3. **응답을 세 갈래로 읽는다.** `status: "cancelled"`는 삭제, 우리가 가진 etag와 같은 것은
   **에코**(우리 자신의 쓰기)라 건너뛰고, 나머지가 upsert다.
4. **적용한 다음에야 `syncToken`을 저장한다.** 이벤트보다 먼저 저장된 커서는 그 이벤트들을
   영원히 건너뛴다.

### 2.3 이 설계에서 가장 위험한 한 줄 (§7.1)

**삭제는 `cancelled`로만 판정하고, 목록에서의 부재로는 절대 판정하지 않는다.**

전체 재나열은 커서가 만료될 때마다 일어나고 첫 패스도 같은 모양이다. 부재가 삭제를 뜻한다면
커서 하나 만료에 사용자의 모든 일정이 휴지통으로 간다 — delete wins에는 "정말입니까"가 없다.

그래서 `planInbound`는 **우리가 가진 이벤트 집합을 인자로 받지 않는다.** "우리 것 중 뭐가 안
왔나"를 물을 방법 자체가 없고, 묻고 싶은 사람은 먼저 시그니처를 바꿔야 한다 — 그 지점이
누군가 이 주석을 읽는 자리다. `applyInboundPlan`도 `current`를 **필터링만** 한다.

### 2.4 에코 방지 (§6.3)

우리가 방금 쓴 이벤트는 다음 폴링에 **그 쓰기가 돌려준 etag를 달고** 돌아온다. 그걸 적용하면
레코드가 바뀌고, 바뀌면 또 쓰기가 걸리고, 조용히 도는 루프가 된다. 그래서 `etag`가 우리가 가진
것과 같으면 건너뛴다 — 경로 C가 쓰기 성공 시 새 etag를 반드시 저장하는 이유가 이것이다.

### 2.5 구글 이벤트가 앱 레코드로 번역되는 규칙

`toExternalEvent`.

| 구글 | 앱 | 주의 |
|---|---|---|
| `start.date` | 맨 날짜 문자열 | 종일의 표시. `T`가 없다는 것이 곧 종일이라는 뜻 |
| `start.dateTime` | UTC instant + `timezone` | 오프셋 붙은 문자열을 잘라 쓰면 14:00이 전 세계에 14:00으로 보인다 |
| `accessRole` | `readOnly` | `owner`/`writer`가 아니면 읽기 전용 |
| `recurrence` RRULE | `recurrence` + `exdates` | 그리기용으로만 쓴다 — 편집은 열지 않는다 |
| `recurringEventId` | `recurrenceId` | 시리즈의 한 회차를 대체하는 레코드 |
| `updated` | `updatedAt` | 우리 시각이 아니라 **구글의** 것 — LWW 비교의 기준 |

---

## 3. 경로 C — 앱에서 구글 일정을 고쳤을 때

`App.updateExternalEvent` · `domain/calendar/googleSync/externalEventShape.ts` ·
`lib/googleCalendarEventWrite.ts` · `components/CalendarView.tsx`

패스를 타지 않는다. 고치는 **즉시** 그 이벤트가 사는 캘린더로 `events.patch`가 나간다.
화면에는 먼저 그리고, 쓰기가 안 되면 되돌린다 — 왕복을 기다리면 느린 연결에서 모든 편집이
고장난 것처럼 느껴지고, 되돌리지 않으면 일어나지 않은 편집을 다음 폴링이 조용히 무를 때까지
격자가 보여준다.

### 3.1 제스처가 편집으로 번역되는 규칙

| 제스처 | 편집 | 구글로 나가는 것 |
|---|---|---|
| 월 격자에서 다른 날로 끌기 | `{ date }` | 시계도 종류도 그대로 — 종일은 종일로, 14:00은 14:00으로 |
| 주 격자에서 옮기기 | `{ date, startTime, endTime, allDay: false }` | `start/end.dateTime` + `timeZone` |
| 종일 칩을 시간대로 끌기 | 위와 같음 | 시각이 정해졌다는 것이 곧 시간 이벤트라는 뜻 |
| 종일 띠에 떨구기 | `{ date, allDay: true }` | `start.date` · `end.date`(배타적) |
| 위아래 끝 잡고 늘이기 | `{ date, startTime, endTime, allDay: false }` | **움직인 쪽 끝만** |
| 팝오버에서 제목 · 메모 고치기 | `{ title, description }` | `summary` · `description` |

- **안 움직인 것은 안 보낸다.** 제스처는 언제나 위치 전체를 보고한다 — 아래 끝만 늘여도 시작
  시각을 함께 말한다. 그대로인 필드를 실어 보내면 etag만 올라가고, 그게 다음 폴링에 남의
  편집으로 돌아온다.
- **이동은 걸친 날 수를 유지한다.** 자정을 넘는 일정도, 사흘짜리 종일 일정도 옮긴 뒤 길이가 같다.
- **보내는 필드는 다섯뿐이다** — 제목 · 설명 · 시작 · 끝 · 종일 여부. 우리가 들고 있는 모든
  필드를 되돌려 보내면 참석자 목록이나 회의 링크가 낡은 사본으로 덮인다.

### 3.2 결과

| 결과 | 뜻 | 격자는 |
|---|---|---|
| `written` | 들어갔다 | 낙관적으로 그린 것을 유지하고 **새 etag와 `updated`를 기록** |
| `superseded` | 412 → 구글이 더 나중이다 | 원래대로 되돌리고 상대 etag만 저장 |
| `gone` | 404 / 410 | 그 블록을 지운다 |
| `unchanged` | 편집이 아무것도 안 바꿨다 | 요청조차 안 나간다 |
| `expired` · `failed` | 권한이 죽었거나 실패 | 원래대로 되돌린다 |

삭제는 `If-Match`를 걸지 않는다 — 삭제는 병합이 아니고, 누가 먼저 이름을 고쳤다는 이유로
거절되는 건 이상한 실패 방식이다. 404와 410도 성공이다(없어졌다는 것이 원하던 결과니까).
그 외의 답은 "계정에 아직 있다"는 뜻이므로, 이미 잊은 격자에 **되돌려 놓는다**.

### 3.3 격자가 손대지 않는 것

| 무엇 | 왜 |
|---|---|
| ICS 구독 이벤트 | 파일이다. 편집을 보낼 곳이 없다 |
| `accessRole`이 reader인 캘린더 | 구글이 거절할 편집을 내주는 것이 안 내주는 것보다 나쁘다 |
| 반복 일정(시리즈 · 회차 모두) | 회차의 id는 합성된 것이고 뒤의 레코드는 시리즈이며, 구글은 "이번 것만인가 전부인가"를 묻는다. 격자에 그 질문을 할 제스처가 없다(§8) |
| 여러 날 종일 이벤트 | 덮는 날마다 칩이 하나씩 나오는데, 그 하나를 끄는 것이 "전체를 옮겨라"인지 "이 끝을 바꿔라"인지 답할 수 없다 — 태스크 범위를 못 끄는 것과 같은 이유 |
| 카테고리(어느 캘린더에 사는가) | 구글 캘린더 사이로 옮기는 것은 다른 연산이다 |

외부 블록은 읽기 전용이어도 **태스크 스케줄 기록기에 닿지 않는다**(`CalendarView.editExternal`).
이벤트 id를 태스크로 착각해 아무것도 안 쓰는 경로를 남겨둘 이유가 없다.

---

## 4. 충돌과 삭제, 두 경로 나란히

| 질문 | A · 태스크 | C · 구글 이벤트 |
|---|---|---|
| 충돌 감지 | `If-Match`에 마지막으로 본 etag — **양쪽이 같은 규칙** | 〃 |
| 412가 왔을 때 | GET으로 상대를 읽고 `updated` vs `updatedAt`, 나중 것이 이긴다 | 〃 |
| 지면 | 안 덮고 etag와 `googleSyncedAt`만 갱신 → 값이 갈린 채 남는다 | 화면을 되돌리고 etag만 갱신 → **다음 폴링이 구글의 값을 가져온다** |
| 404 / 410 | 매핑 해제 → 다음 패스가 다시 만든다 | 격자에서 제거 → 다음 폴링이 정리한다 |
| 앱에서 삭제 | `events.delete`. 완전 삭제는 툼스톤 경유 | `events.delete`, `If-Match` 없이 |
| 구글에서 삭제 | **알지 못한다** — 다음 패치가 404를 만나야 안다 | `cancelled` 행을 보고 격자에서 지운다 |
| 실패했을 때 | 아무것도 안 적음 → 다음 패스가 같은 계획 | 화면 롤백 → 사용자가 다시 하거나 폴링이 정리 |

한 캘린더 위에서 두 기록기가 다른 충돌 규칙을 쓰면, 나중에 아무도 설명할 수 없는 방식으로
편집이 사라진다. 그래서 C는 A의 규칙을 **의도적으로 그대로** 쓴다.

---

## 5. 연결과 토큰

- 연결하면 `calendars.insert`로 **"FocusFlow" 캘린더 하나**를 만들고 그 `calendarId`를 저장한다.
  두 번째로 눌러도 저장된 것이 계정에 아직 있으면 재사용한다 — 빈 캘린더가 줄줄이 생기지 않는다.
  사용자가 구글에서 그 캘린더를 지웠으면 404로 돌아오고, 새로 만든다.
- 계정 주소는 `primary` 캘린더의 id에서 읽는다(그 id가 곧 이메일이다). userinfo 스코프를 더
  받지 않아도 된다.
- 액세스 토큰은 매 패스마다 `/api/google/token`에서 새로 받는다. `connected: false`는 에러가
  아니라 "연결한 적 없다"는 평범한 답이라 `null`로 돌아온다.
- 401은 **두 가지**다 — 앱 세션이 없는 것과 구글 권한이 죽은 것. 둘을 갈라 읽는다.

---

## 6. 파일 지도

| 파일 | 무엇 |
|---|---|
| `domain/calendar/googleSync/eventShape.ts` | 자격 판정 · 태스크 → 이벤트 바디 · RRULE |
| `domain/calendar/googleSync/outboundPlan.ts` | 네 더미로 나누는 순수 계획 · `needsUpdate` |
| `domain/calendar/googleSync/tombstones.ts` | 완전 삭제된 태스크의 이벤트 id 보관 · `outboundAction` |
| `domain/calendar/googleSync/inboundPlan.ts` | **§7.1 가드** · 에코 판정 · 응답 적용 |
| `domain/calendar/googleSync/inboundShape.ts` | 구글 이벤트 → 앱 레코드 |
| `domain/calendar/googleSync/externalEventShape.ts` | 제스처 → patch, 그리고 같은 편집을 우리 레코드에 |
| `domain/calendar/googleSync/connectFlow.ts` | OAuth 왕복 · 딥링크 · 상태 검증 |
| `lib/googleCalendarOutbound.ts` | A의 실행기 — 순서 · 412 · 404 |
| `lib/googleCalendarInbound.ts` | B의 실행기 — 페이지네이션 · 410 재동기화 |
| `lib/googleCalendarEventWrite.ts` | C의 실행기 — 한 이벤트의 patch/delete |
| `lib/googleCalendar.ts` | 토큰 · 연결 · 전용 캘린더 생성 |
| `lib/googleCalendarSources.ts` | 어느 캘린더를 읽을지 · `syncToken` 보관 |
| `hooks/useGoogleOutboundSync.ts` · `useGoogleInboundSync.ts` | 언제 도는가 |
| `utils/calendarItems.ts` | 어떤 블록이 끌 수 있는가(`draggable` · `readOnly`) |
| `components/CalendarView.tsx` | 제스처가 어느 기록기로 가는가(`editExternal`) |

---

## 7. 설계와 어긋나는 자리, 그리고 비어 있는 곳

> 아래 §7.1~§7.3과 별개로, **지금 코드가 잘못 결론짓는 여섯 가지**(생성 중복 · 410 후
> 좀비 이벤트 · 회차 취소 누락 · 연속 편집 경쟁 · 404 오독 · 편집 불가 이벤트 종류)는
> `GOOGLE_SYNC_HARDENING_DESIGN.md`가 따로 다룬다.

### 7.1 태스크에는 인바운드가 없다

인바운드는 `ExternalCalendarEvent`만 쓴다 — **태스크를 건드리는 코드 경로가 없다**(P3의 결과).
그래서 설계 문서 §7 표의 "구글에서 삭제 → 태스크를 휴지통으로"는 **아직 구현되지 않은 줄**이다.

- 구글에서 FocusFlow 일정을 고쳐도 그 값이 태스크로 돌아오지 않는다. 다만 §1.5의 412 판정이
  있어 **우리가 그 편집을 덮어 파괴하지는 않는다.** 두 값이 갈린 채 남고, 다음번 로컬 편집이
  다시 이긴다.
- 구글에서 태스크 이벤트를 지우면 앱은 모른다. 그 태스크를 다시 고쳐 패치가 나갈 때 404를
  만나야 매핑이 풀리고, 그 다음 패스가 새로 만든다.

### 7.2 전용 캘린더를 읽기 소스로 켜면 겹친다

같은 일정이 태스크로 한 번, 외부 이벤트로 한 번 그려진다. 새 캘린더는 기본이 해제라 일부러
켜야 그렇게 되지만, 소스 목록에서 걸러지지는 않는다.

### 7.3 그 밖

| 무엇 | 지금 |
|---|---|
| 주기 타이머 | 없다. 창 포커스와 수동뿐(Q4의 기본값). 웹훅 · 크론 · 도메인 인증은 P8이 명시적으로 뺐다 |
| 반복 | FocusFlow → 구글 단방향. 구글의 반복은 읽어서 그리기만 한다(§8) |
| 한 캘린더의 상한 | 전체 나열이 40페이지(250 × 40)를 넘으면 **실패로 처리**하고 `syncToken`을 저장하지 않는다 — 짧게 읽은 목록으로 커서를 만들면 그 간격이 영구화된다 |
| 인바운드 이벤트 | 태스크가 아니므로 완료 체크 · 포커스 타이머 · 리스트 배정이 없다. "태스크로 승격"은 v1 범위 밖(Q2) |
