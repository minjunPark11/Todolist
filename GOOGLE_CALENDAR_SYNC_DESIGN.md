# 구글 캘린더 양방향 동기화 — 일정만, 대등하게, 마지막이 이긴다

> 상태: **설계** · 2026-09-04 (P1~P9 전부 사용자 결정 — §3)
> 요청: "구글 캘린더 쪽에서도 일정을 추가하면 그게 FocusFlow로 넘어오게. 양방향으로 편집도 되게. 저장소만 구글 캘린더에."
> 대상: `hooks/usePlannerData.ts` · `lib/externalCalendars.ts` · `types.ts` ·
> `domain/calendar/googleSync/*`(신규) · `platform/*` · `components/SettingsPage.tsx`
> 선행 문서: `CALENDAR_COLOR_SOURCE_AND_VIEW_OPTIONS_DESIGN.md` ·
> `FOCUSFLOW_EXTERNAL_AI_ACCESS_ARCHITECTURE.md`(B3 타임존 — §9가 그 부채를 갚는다) ·
> `RAIL_SYNC_AND_NOTIFICATIONS_DESIGN.md`(레일 동기화 버튼 — §6.4가 재사용한다)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 코드에서 잰 것 — 아웃바운드 ICS가 이미 자동이다 · `ExternalCalendarEvent`가 이미 구글 이벤트 모양이다 · `Task.updatedAt`이 모든 편집에서 갱신된다 · 삭제는 `deletedAt` 소프트다 | [실측] |
| **결정** | 사용자 — 전용 캘린더 · 날짜 있으면 나감 · LWW · delete wins · 반복 단방향 · 매핑 3필드 | [결정] |
| **추론** | 교차 필드 편집 손실의 실제 빈도 · 인바운드 이벤트를 태스크로 승격할 필요가 생길지 | [추론] |
| **규격** | Google Calendar API — `events.list(syncToken)` · `status: "cancelled"` · RRULE · `updated` | [규격] |

---

## 1. 먼저 사실 — 연동은 이미 절반 있고, 그 절반이 못 하는 일이 정확히 이 설계의 범위다 [실측]

| 방향 | 지금 | 한계 |
|---|---|---|
| FocusFlow → 구글 | ICS 피드 발행. 태스크가 바뀌면 **1.8초 디바운스 후 자동 재발행** (`App.tsx:735`, `api/calendar/[token].js`) | 구글이 구독 URL을 8~24시간에 한 번만 읽는다. 우리 `Cache-Control: max-age=300`은 무시된다 |
| 구글 → FocusFlow | ICS 구독 읽기, 30분 주기 (`lib/externalCalendars.ts`) | **읽기 전용**이다. `ExternalCalendarEvent.readOnly`가 `true` 리터럴로 못박혀 있다 (`types.ts:853`) |

즉 발행도 구독도 이미 돈다. **없는 것은 쓰기다.** 이 설계가 만드는 것은 새 캘린더 기능이
아니라, 이미 있는 두 통로 위에 **쓰기와 충돌 판정**을 얹는 일이다.

그리고 애초에 이 논의를 시작하게 한 "데이터가 많아지면 서버가 감당하나"는 **이 문서의
문제가 아니다.** 완전 삭제는 `diffRemovedIds`가 잡아 서버에서도 지운다
(`domain/sync/diffRecords.ts:23`). 실제 무한 증가원은 휴지통이 없는 `focusSessions`
(`types.ts:533`) 하나뿐이고, 그건 별도 아카이빙 과제다. 저장소 교체(구글 드라이브 포함)는
근거가 없어 **기각한다** — §11.3.

---

## 2. 범위 — 구글 캘린더가 담을 수 있는 것만

`Task`는 필드가 서른 개가 넘는다 (`types.ts:74`). `priority` · `projectId` · `tags` ·
`sortKey` · `estimatedMinutes` · `isSomeday` · `contentMode`… 구글 캘린더 이벤트가 가진
것은 제목 · 설명 · 장소 · 시작 · 끝 · 반복뿐이다.

그러므로 **"저장소를 구글 캘린더로"는 일정 슬라이스에 한해서만 성립한다.**

| 구글에 산다 | 구글에 못 산다 |
|---|---|
| 날짜 잡힌 태스크의 제목 · 시작 · 끝 · 종일 여부 · 반복 | 날짜 없는 태스크 · 인박스 · `isSomeday` · 우선순위 · 리스트/프로젝트 구조 · 태그 · 체크리스트 · 포커스 세션 |

`extendedProperties.private`에 나머지를 우겨넣는 안은 **채택하지 않는다.** 그렇게 하면
구글은 저장소가 아니라 불투명한 JSON 창고가 되고, 구글 UI에서 손댈 수도 없다.

### 2.1 로컬 사본은 남는다 [실측]

앱은 로컬 우선이고 (`focusflow.appData.v1` — `domain/migrations/persistPlannerData.ts:5`),
리스트 · 보드 · 타임라인이 전부 `dueDate`/`startTime`을 읽어 그린다. 오프라인에서도 떠야
한다. 따라서 실제로 구현되는 것은:

> **구글은 대등한 편집 지점이다.** 로컬에 미러가 있고, 충돌하면 §5.3의 규칙이 정한다.

초기 논의의 "구글이 권한자(authority)"라는 표현은 **폐기한다.** 충돌 처리를 넣는 순간
권한자는 없어지고 양쪽이 대등해진다.

---

## 3. 결정 요약 [결정]

| # | 결정 | 근거 절 |
|---|---|---|
| **P1** | 전용 **"FocusFlow" 구글 캘린더** 하나만 양방향 대상 | §4.1 |
| **P2** | 나갈 자격 = **날짜가 있음**(`dueDate`). 시간 있으면 시간 이벤트, 없으면 종일 | §5.1 |
| **P3** | 구글에서 온 것은 `ExternalCalendarEvent`. **`Task` 모델을 건드리지 않는다** | §6.2 |
| **P4** | 충돌은 **LWW(마지막 수정 우선), 레코드 단위** | §5.3 |
| **P5** | 삭제는 **delete wins**. 구글에서 지우면 휴지통(`deletedAt`) | §7 |
| **P6** | 반복은 **FocusFlow → 구글 단방향** | §8 |
| **P7** | 매핑은 `googleEventId` · `etag` · `updatedAt` **+ 삭제 툼스톤 배열** | §4.2 |
| **P8** | `events.list(syncToken)` **폴링**. 웹훅 · 크론 · 도메인 인증 없음 | §6.1 |
| **P9** | **아웃바운드 먼저**, 인바운드 나중 | §12 |

---

## 4. 데이터 모델

### 4.1 전용 캘린더 [결정]

구글 계정에는 캘린더가 여럿이다. 인바운드를 계정 전체로 열면 회사 일정 · 생일 · 공휴일 ·
남이 보낸 초대까지 전부 넘어온다.

연결할 때 `calendars.insert`로 **"FocusFlow" 캘린더를 하나 만들고**, 그 `calendarId`를
설정에 저장한다. 인바운드도 아웃바운드도 그 캘린더 하나만 본다.

부수 효과가 두 개 있고 둘 다 이득이다:

- 경계가 사용자 눈에 보인다. 구글에서 그 캘린더에 만들면 넘어오고, 개인 캘린더에 만들면 안 넘어온다.
- **P2가 만드는 소음을 사용자가 스스로 끌 수 있다.** 마감일만 있는 잡일까지 종일 이벤트로 뜨는데, 구글에서 FocusFlow 캘린더 체크만 해제하면 안 보인다. 데이터는 그대로다.

### 4.2 매핑 필드 [결정]

`Task`에 `googleEventId`와 `googleEtag` 두 필드를 더한다. `updatedAt`은 이미 있다.
이 셋이 각각 **다른 쪽**을 가리켜야 LWW가 계산된다.

| 필드 | 가리키는 쪽 | 쓰임 |
|---|---|---|
| `googleEventId: string` | 연결고리 | 빈 값 = 아직 안 만들어짐 |
| `googleEtag: string` | **저쪽** | 원격이 바뀌었나. 쓸 때 `If-Match`로도 쓴다 |
| `updatedAt: string` | **우리 쪽** | 이미 있다 (`types.ts:147`) — 편집 27곳에서 전부 갱신된다 [실측] |

`data` jsonb 안에 타므로 **테이블 변경이 없다.** `normalizeTask`가 미지 필드를 먼저
spread하므로 이 필드를 모르는 구버전 클라이언트도 지우지 않고 실어 나른다.

**상태 머신은 두지 않는다.** `pending`/`failed`를 따로 들 필요가 없다 — `googleEventId`의
유무가 곧 "생성에 성공했는가"다. 날짜가 있는데 id가 없으면 다음 동기화 패스가 만든다.
재시도가 암묵적으로 처리된다.

### 4.3 삭제 툼스톤

`emptyTrash` · `permanentlyDeleteTask`는 레코드를 컬렉션에서 완전히 뺀다
(`App.tsx:1401`). 그 순간 `googleEventId`도 함께 사라진다. 그때 오프라인이거나 API가
실패하면 **구글 이벤트가 고아로 남고 지울 근거가 영영 없어진다.**

`googleEventId` 문자열 배열 하나를 `AppSettings` 옆에 둔다. 완전 삭제 시 id를 밀어 넣고,
`events.delete` 성공하면 뺀다. 이것 없이는 새는 것을 막을 방법이 없다.

### 4.4 인증 — 딥링크 하나가 나머지를 전부 정한다 [결정] [규격]

데스크톱 리다이렉트를 **딥링크로** 하기로 했다. 그 결정에서 네 가지가 연쇄로 따라 나오며,
**뒤의 셋은 선택이 아니다.** 나중에 "개인용 앱인데 왜 중간에 서버가 있나"를 묻게 되므로
여기 적어 둔다.

| # | 사실 | 따라 나오는 것 |
|---|---|---|
| 1 | 구글은 **데스크톱 클라이언트에 커스텀 URI 스킴을 주지 않는다.** 데스크톱용 리다이렉트는 loopback(`127.0.0.1:port`)뿐이다 | `focusflow://`를 구글에 직접 등록할 수 없다 |
| 2 | 그러므로 딥링크는 **https 경유로만** 성립한다 | 구글 → Vercel 콜백(https) → `focusflow://` |
| 3 | https 리다이렉트는 **Web application** 클라이언트 타입이고, 그 타입은 토큰 교환에 `client_secret`을 요구한다 | 교환은 **서버에서만** 할 수 있다 |
| 4 | 서버가 secret을 쥐면 refresh token도 거기 두는 것이 자연스럽다 | **클라이언트는 refresh token을 영영 보지 않는다** |

4는 딸려온 이득이다. loopback을 골랐다면 Desktop 클라이언트 타입이 되고, 구글 스스로
"installed app의 secret은 기밀이 아니다"라고 말하는 값이 바이너리에 실렸을 것이다.
딥링크 쪽이 자격증명을 더 잘 지킨다.

대가는 **웹 페이지 한 장과 엔드포인트 두 개**다. 오프라인에서 최초 연결을 할 수 없다는
뜻이기도 한데, OAuth는 어느 방식이든 그렇다.

#### 4.4.1 토큰은 두 테이블로 나눈다

`google_calendar_connections` — 연결 상태(캘린더 id, 계정 이메일, 연결 시각). 사용자가
읽는다. 화면이 "연결됨"을 그리려면 필요하다.

`google_calendar_tokens` — refresh token. **RLS를 켜고 정책을 하나도 만들지 않는다.**
Supabase에서 정책 없는 RLS 테이블은 service role 외 누구도 읽지 못한다. 정책 목록이 비어
있는 것이 실수가 아니라 **그 자체로 보안 속성**이라는 뜻이며, 마이그레이션에 그렇게 적어
둔다 — 비어 있는 정책 목록은 빠뜨린 것처럼 보이기 때문이다.

한 테이블에 두고 컬럼 권한을 `revoke` 하는 안보다 이쪽을 고른 이유는 하나다. 나중에
이 파일을 여는 사람이 **테이블 정의만 보고** 무엇이 새지 않는지 알 수 있어야 한다.

---

## 5. 아웃바운드 (FocusFlow → 구글)

### 5.1 나갈 자격 [결정]

> **날짜가 있으면 동기화 대상이다.**

`dueDate`가 있으면 나간다. `startTime`이 있으면 시간 이벤트, 없으면 종일 이벤트.
`deletedAt`이 있으면 자격을 잃는다(→ §7).

초안의 **"날짜 있음 ⟺ 구글 이벤트 있음"이라는 쌍조건은 폐기한다.** 그것은 코드가 유지할 수
있는 불변식이 아니라 바라는 상태였고, 그 차이가 데이터 소실 경로를 하나 열었다:

> 쌍조건 아래서 "구글에 이벤트가 없다"는 **두 가지 뜻**이 된다 —
> ① 사용자가 구글에서 지웠다 ② 우리가 만드는 데 실패했다.
> 구분하지 못하면 API 한 번 실패한 태스크를 "지워졌네" 하고 휴지통에 넣는다.

`googleEventId`가 그 구분을 준다. **id가 있는 레코드에 한해서만 삭제 판정을 적용한다.**

### 5.2 자격이 대칭이라 소유권 플래그가 필요 없다

구글에서 만든 종일 이벤트는 날짜가 있으므로 P2의 자격을 그대로 만족한다. 왕복해도 자격을
잃지 않는다. 초안이 필요로 했던 "이건 구글에서 온 거니 필터를 우회한다"는 플래그가
통째로 사라진다.

### 5.3 충돌 — LWW, 레코드 단위 [결정]

`Task.updatedAt`(우리)과 구글 이벤트의 `updated`(저쪽)를 비교해 **나중 것이 이긴다.**

**감수하는 손실을 명시한다.** 레코드 단위이므로, FocusFlow에서 제목을 고치고 나중에
구글에서 시간을 옮기면 **제목 수정이 통째로 사라진다.** 필드 단위로 쪼갤 수는 없다 —
구글은 이벤트당 `updated` 하나만 주고 필드별 시각을 주지 않는다. 필드 단위 병합은
마지막 동기화 기준값을 저장하는 3-way를 요구하는데, 개인 사용 기준에서 그 복잡도는
값어치를 못 넘는다 [결정].

빈도는 낮다 [추론]. 동기화가 §6.4의 시점에 돌므로 두 편집 사이 창이 좁다. 실제로 물릴
시나리오는 **데스크톱에 앱을 켜둔 채 폰에서 고치는 경우** 하나다.

기기 시계 skew도 같은 이유로 감수한다 — 시계가 뒤처진 기기는 그 편집이 계속 진다.

---

## 6. 인바운드 (구글 → FocusFlow)

### 6.1 증분 폴링 [규격]

`events.list(calendarId, syncToken)`으로 바뀐 것만 받는다. 웹훅(`events.watch`)은
도메인 소유권 인증과 만료 채널 갱신 크론을 요구하는데, 폴링 대비 체감 차이가 개인
사용에서 거의 없다 → **채택하지 않는다.**

### 6.2 구글에서 온 것은 `Task`가 아니다 [결정] [실측]

`ExternalCalendarEvent`가 이미 구글 이벤트와 같은 모양이다 (`types.ts:841`):

```
title · start · end · allDay · timezone? · description? · location?
· recurrence? · exdates? · readOnly: true
```

캘린더 뷰에 이미 그려진다. `readOnly: true` 리터럴만 소스별로 갈라 ICS 구독은 읽기 전용,
구글 API 캘린더는 쓰기 가능으로 둔다.

이 선택이 공짜로 주는 것이 둘이다:

1. **`Task` 모델을 건드리지 않는다.** 남의 일정이 인박스 · 리스트 · 보드를 오염시키지 않는다.
2. **인바운드는 타임존 작업이 필요 없다.** `ExternalCalendarEvent`에 `timezone?`이 이미 있다 (`types.ts:851`). §9의 부채는 **아웃바운드에만** 걸린다.

구글 일정에 우선순위를 붙이거나 포커스 타이머를 돌리고 싶어지면 "태스크로 만들기" 액션을
나중에 얹는다. **v1 범위 밖** [추론].

### 6.3 에코 루프 방지

우리가 쓴 것이 다음 폴링에 되돌아와 다시 쓰이는 무한 루프를 막는다. 쓰기 응답의 `etag`를
저장하고, 인바운드로 온 이벤트의 `etag`가 저장된 것과 같으면 **우리가 방금 쓴 그 버전이므로
무시한다.** 기계적이지만 빠뜨리면 조용히 도는 루프가 된다.

### 6.4 언제 도는가

앱을 열 때 · 창이 포커스를 얻을 때 · 레일의 동기화 버튼을 누를 때. 셋째는 이미 있다
(`RAIL_SYNC_AND_NOTIFICATIONS_DESIGN.md`).

---

## 7. 삭제 — delete wins [결정]

| 어디서 | 무슨 일 |
|---|---|
| 구글에서 삭제 | 태스크를 **휴지통으로** (`deletedAt` 설정 — `domain/tasks/taskState.ts:41`) |
| FocusFlow에서 휴지통 | 구글에서 `events.delete` |
| 휴지통에서 복구 | §5.1 자격을 되찾으므로 구글에 다시 만들어진다 (새 `googleEventId`) |
| FocusFlow에서 완전 삭제 | §4.3 툼스톤 경유로 구글에서 삭제 |

휴지통을 거치므로 구글 쪽 오조작으로 우선순위 · 태그 · 체크리스트가 즉시 증발하지 않는다.
30일 안에 되돌릴 수 있다.

### 7.1 이 설계 전체의 최상위 방어선 [규격]

> **삭제는 `events.list`가 돌려주는 `status: "cancelled"`로만 판정한다.**
> **목록에 없다는 이유로는 절대 판정하지 않는다.**

delete wins는 사용자 확인 단계가 없다. 오판을 잡아 줄 것이 아무것도 없다.

`syncToken`은 만료된다(410 GONE). 그러면 전체 재조회로 떨어지는데, **그때 부재를 삭제로
읽으면 한 번의 잘못된 판정으로 모든 일정이 휴지통으로 간다.** 물어보지도 않고.

구현 시 이 가드에 단위 테스트를 붙인다 — 전체 재조회 응답에서 아무것도 삭제되지 않음을
증명하는 테스트.

---

## 8. 반복 — 아웃바운드 단방향 [결정]

FocusFlow의 반복 모델은 `repeatType`(5종) · `repeatInterval` · `repeatDays[]` ·
`repeatEndDate`다 (`types.ts:222`). RRULE의 `FREQ` / `INTERVAL` / `BYDAY` / `UNTIL`로
**좁고 완전하게** 매핑된다. 나가는 쪽은 쉽다.

들어오는 쪽은 반대다. 구글 RRULE 전체를 5종으로 줄이면 손실이 크다. 그러므로 **반복은
나가기만 한다.**

**대가 두 개를 명시한다:**

1. 우리가 쓴 반복 일정을 **구글에서 고치면 다음 쓰기에 조용히 덮인다.** 구글에는 "이 이벤트 수정 금지" 같은 잠금이 없어 구조적으로 막을 수 없다. 최소한 연결 시점에 사용자에게 한 번 알린다.
2. 구글에서 만든 반복 일정은 들어오지 않는다. 기존 ICS 경로의 `expandIcsOccurrences` (`lib/externalCalendars.ts:15`)를 재사용해 **읽기 전용으로 보여만 주는** 선택지는 있다 → §13 Q1.

---

## 9. 타임존 — 아웃바운드의 선행 조건 [실측]

`Task`는 시각을 **기기 로컬 벽시계 문자열**로만 들고 있다 — `dueDate: "2026-09-04"`,
`startTime: "14:00"` (`types.ts:100`, `types.ts:115`). 타임존이 없다. 지금의 ICS 발행이
`DTSTART:20260904T140000`처럼 floating time을 쓰는 것도 그래서다
(`api/calendar/[token].js`의 `toFloatingDateTime`).

구글 API는 RFC3339 + IANA 타임존을 요구한다. 이것이
`FOCUSFLOW_EXTERNAL_AI_ACCESS_ARCHITECTURE.md`가 **B3**으로 적어둔 부채이고, 여기서는
우회가 불가능하다.

### 9.1 필드는 이미 있다 — M1 착수 시 확인한 것 [실측]

이 문서 초안은 `AppSettings.timezone`을 새로 더하라고 썼다. **틀렸다. 이미 있고, 완전하다:**

| 무엇 | 어디 |
|---|---|
| 필드 | `types.ts:678` |
| 감지 | `detectTimezone()` — `normalize.ts:95` |
| 기본값 | `normalize.ts:123` |
| 정규화 (다른 기기가 쓴 값이 이김) | `normalize.ts:427` |
| 자기 교정 effect | `usePlannerData.ts:540` — 감지값과 다를 때만 쓴다 |
| 테스트 | `hooks/appSettingsTimezone.test.ts` |

MCP 작업(`FOCUSFLOW_EXTERNAL_AI_ACCESS_ARCHITECTURE.md` M1)이 B3 부채의 **절반을 이미
갚아 두었다.** 따라서 §9에 남은 일은 필드가 아니라 **변환 경계 하나**뿐이다.

### 9.2 남은 일 — 변환은 구글 경계 한 곳에서만

도메인 계층은 벽시계 문자열을 계속 쓴다. `(dueDate, startTime, timezone)` → RFC3339
변환은 아웃바운드 직렬화 함수 안에서만 일어난다. §6.2 덕분에 인바운드는 이 변환이 필요
없다.

### 9.3 벽시계와 절대시각이 어긋나는 지점 [실측]

`timezone`은 사용자 선택이 아니라 **기기 감지값이고, 기기가 움직이면 따라 바뀐다**
(`usePlannerData.ts:540`). 여기서 의미 충돌이 하나 생긴다:

> 서울에서 14:00 태스크를 만들면 구글에 `14:00 KST`로 간다. 베를린에 도착하면
> `timezone`이 `Europe/Berlin`으로 바뀐다. 앱에서는 여전히 14:00로 읽히지만
> (벽시계), 구글은 `14:00 KST = 07:00 CEST`를 들고 있다. **둘이 갈라진다.**

FocusFlow의 시각은 벽시계 의미이고 구글 이벤트는 절대시각 의미라, 구조적으로 한쪽을
골라야 한다. **쓰는 시점의 존으로 변환하고, 이동에 따른 어긋남은 감수한다**(R8) —
존이 바뀔 때마다 전 일정을 다시 밀어 넣는 대안은 비용이 훨씬 크고, 그것대로 "베를린에
와서 보니 서울 회의가 07:00로 옮겨져 있다"는 다른 종류의 놀라움을 만든다.

---

## 10. 위험 목록

| # | 위험 | 방어 |
|---|---|---|
| R1 | 전체 재조회에서 부재를 삭제로 오판 → 전 일정 휴지통 | §7.1 가드 + 단위 테스트 **(최우선)** |
| R2 | API 실패를 "구글에서 지워짐"으로 오판 | `googleEventId` 유무로 구분 (§5.1) |
| R3 | 에코 루프 | `etag` 대조 (§6.3) |
| R4 | 교차 필드 편집 손실 | **감수** (§5.3). 문서화하고 알린다 |
| R5 | 구글에서 반복 일정 수정 → 소실 | **감수** (§8). 연결 시 고지 |
| R6 | 완전 삭제 시 구글 이벤트 고아화 | 툼스톤 배열 (§4.3) |
| R7 | 기기 시계 skew로 편집이 계속 짐 | **감수** (§5.3) |
| R8 | 다른 타임존으로 이동 시 벽시계와 구글의 절대시각이 갈라짐 | **감수** (§9.3) |

R1이 유일하게 "한 번에 전부"를 잃는 항목이다. 나머지는 전부 레코드 하나 단위다.

---

## 11. 인증과 비용

### 11.1 비용 [규격]

**0원.** Google Calendar API는 과금이 없다(프로젝트당 일 100만 요청 수준의 쿼터, 개인
사용은 근처도 못 간다). Google Cloud 프로젝트는 결제 계정 없이 만들어진다. 토큰 교환용
serverless는 이미 쓰는 Vercel 안이다.

### 11.2 OAuth

스코프는 `calendar.events` + 캘린더 생성용 `calendar`. sensitive로 분류되지만 restricted가
아니므로 CASA 보안심사는 없다.

- **Testing** 상태: 심사 없이 즉시. 단 refresh token이 7일마다 만료 → 주 1회 재로그인
- **Production 미검증**: 첫 로그인에 경고 화면 한 번, 이후 토큰 만료 없음. 사용자 100명 한도

개인 사용은 후자를 권한다. *(구글이 정책을 종종 바꾸므로 착수 시점에 현행 문서로 재확인할 것.)*

### 11.3 기각한 대안 — 구글 드라이브를 전체 저장소로

**기각한다.** 근거 셋:

1. **풀려던 문제가 없다.** §1 — 저장 용량은 병목이 아니다.
2. **MCP 외부 AI 접근이 무너진다.** `server/data/repository.ts:251`이 Supabase REST를 직접 읽고, 인증은 Supabase Auth의 OAuth 2.1 서버에, 권한은 24개 테이블 RLS에 얹혀 있다. `api/calendar/[token].js`도 service role로 Supabase를 읽는다. 드라이브에는 행 단위 권한 개념이 없다.
3. **오히려 악화된다.** 드라이브는 파일을 통째로만 가져오므로 `updated_at > 커서` 증분 로딩이 원리적으로 불가능하다. Supabase로는 가능하다 — 지금 안 하고 있을 뿐이다 (`hooks/usePlannerData.ts:585`가 `limit` 없이 전체를 받는다).

드라이브는 **자동 백업 대상**으로서만 가치가 있다. `useAutoBackup`(`app/useAutoBackup.ts`)이
이미 Export JSON을 만들고, `platform.backups`는 데스크톱 전용이라 웹에 빈칸이 있다
(`platform/types.ts`가 그 이유를 스스로 밝힌다). 단방향이라 정합성 위험이 0이다.
**별도 과제로 분리한다.**

---

## 12. 단계 [결정]

### M1 — 토대 + 아웃바운드

| # | 항목 | 상태 |
|---|---|---|
| 1 | `AppSettings.timezone` (§9) | **이미 있음** — §9.1 |
| 2 | 순수 직렬화 — 자격 판정 (§5.1) · RFC3339 변환 (§9.2) · RRULE 매핑 (§8) | **완료** — `domain/calendar/googleSync/eventShape.ts` |
| 3 | 매핑 필드 + 툼스톤 배열 (§4.2, §4.3) | **완료** — `types.ts` · `plannerData/normalize.ts` · `googleSync/tombstones.ts` |
| 4a | 딥링크 수신 경로 (§4.4) | **완료** — `src-tauri/src/main.rs` · `platform/{types,tauri,web}.ts` |
| 4b | 토큰 테이블 (§4.4.1) | **완료** — `supabase/migrations/017_google_calendar.sql` |
| 4c | OAuth 엔드포인트 5개 | **완료** — `src/integrations/google/*` · `api/google/*` |
| 4d | 연결 · 해제 UI, 전용 캘린더 생성 (§4.1) | 4c 배포 확인 후 |
| 5 | `events.insert/patch/delete` 실행기 | |
| 6 | 실패 시 재시도 — `googleEventId` 없는 레코드를 다음 패스가 집는다 | 3의 `outboundAction`이 이미 답한다 |

순서를 초안에서 바꿨다. **2·3이 순수하고 테스트 가능하므로 먼저 간다** — 자격 판정과
RRULE 매핑은 구글 계정 없이 픽스처로 증명된다. OAuth(4)는 그 위에 얹는 I/O 껍데기다.
`server/data/*`가 이미 쓰는 방식과 같다.

여기까지로 "FocusFlow에 적으면 구글 캘린더에 몇 초 안에 저장된다"가 완성된다.

### 12.1 셋업 절차

**코드가 아니라 자격증명이 남았다.** OAuth 클라이언트는 계정 주인만 만들 수 있다.

#### 어느 구글 계정으로 만드는가

**아무 계정이나 된다.** 프로젝트는 OAuth 클라이언트를 소유할 뿐이고, 그 클라이언트를
통해 연결하는 사람의 계정과 같을 필요가 없다 — 서비스를 운영하는 계정과 그 서비스를
쓰는 계정은 원래 별개다. 일정을 동기화할 계정과 달라도 상관없다.

#### 1. Google Cloud

1. [console.cloud.google.com](https://console.cloud.google.com) → 프로젝트 만들기. **결제 계정 불필요.**
2. **API 및 서비스 → 라이브러리 → Google Calendar API → 사용**
3. **OAuth 동의 화면** — User Type `External`. 앱 이름과 지원 이메일은 사용자에게 보인다.
4. 게시 상태 (§11.2에서 고른 것):
   - **Testing**: 테스트 사용자로 직접 추가한 사람만, refresh token이 7일마다 만료
   - **Production (미검증)**: 아무나 쓸 수 있고 토큰도 안 만료. 대신 첫 로그인에 경고 화면 한 번, 누적 100명 한도 — **개인/소수 사용은 이쪽**
5. **사용자 인증 정보 → OAuth 클라이언트 ID → 웹 애플리케이션**
6. **승인된 리디렉션 URI 두 개:**

```
https://todolist-three-gray-92.vercel.app/api/google/callback
https://<supabase-project-ref>.supabase.co/auth/v1/callback
```

첫째는 캘린더 연동(§4.4), 둘째는 구글 로그인. **클라이언트 하나로 둘 다 쓴다.**

`focusflow://`는 **넣지 않는다.** 넣을 수도 없다(§4.4 표 1행) — 그 스킴은 콜백 페이지가
앱으로 돌아올 때만 쓰이고, OS 등록은 `tauri.conf.json`의
`plugins.deep-link.desktop.schemes`가 한다.

#### 2. Vercel 환경 변수

환경 변수는 **전부 함수 환경에만** 들어간다. 빌드에는 하나도 없다.

| 이름 | 값 |
|---|---|
| `GOOGLE_CLIENT_ID` | 발급받은 클라이언트 ID |
| `GOOGLE_CLIENT_SECRET` | **비밀 — 빌드에 절대 넣지 않는다** |
| `GOOGLE_REDIRECT_URI` | `https://todolist-three-gray-92.vercel.app/api/google/callback` |
| `SUPABASE_SERVICE_ROLE_KEY` | 이미 있다 (`api/calendar/[token].js`가 쓴다) |

`VITE_GOOGLE_CLIENT_ID`는 **필요 없다.** 동의 URL을 `/api/google/start`가 서버에서
만들기 때문에 앱은 그 주소로 이동만 하면 된다. `.env`·`release.yml`·GitHub Secrets 세
곳에 같은 값을 넣어야 하는 변수가 하나 줄고, v0.1.4 사고의 모양이 하나 사라진다.

#### 3. Supabase

1. `supabase/migrations/017_google_calendar.sql` 적용 (토큰 테이블 두 개)
2. Authentication → Providers → **Google** 켜고 같은 클라이언트 ID/secret 입력

#### 4. 확인

`/api/google/start`가 스스로 답한다 — 설정이 끝났는지 짐작하지 않아도 된다:

```
curl -si "https://todolist-three-gray-92.vercel.app/api/google/start?state=a1b2c3d4e5f60718.web"
```

| 응답 | 뜻 |
|---|---|
| `404` | 코드가 아직 배포되지 않았다 |
| `500` + `missing env: ...` | 배포됐고, 빠진 변수 이름이 본문에 있다 |
| `302` → `accounts.google.com/...` | **끝났다** |

### M2 — 인바운드

6. `events.list(syncToken)` 폴링 (§6.1)
7. `ExternalCalendarEvent` 쓰기 가능화 (§6.2)
8. 에코 방지 (§6.3), LWW 판정 (§5.3)
9. **`cancelled` 가드 + 그 단위 테스트** (§7.1)
10. delete wins 양방향 (§7)

**M1을 붙이고 한두 주 써 본 뒤 M2로 간다** [결정]. 매핑 · 타임존 · 전용 캘린더가 자리를
잡은 위에 인바운드를 얹으면 버리는 작업이 없다. 처음부터 양방향으로 짜면 타임존 버그와
에코 루프와 삭제 사고가 한꺼번에 온다.

---

## 13. 열린 질문

| # | 질문 | 기본값 제안 |
|---|---|---|
| Q1 | 구글에서 만든 **반복** 일정을 읽기 전용으로 보여줄 것인가, 아예 무시할 것인가 (§8) | 무시. 필요해지면 `expandIcsOccurrences` 재사용 |
| Q2 | 인바운드 이벤트를 태스크로 **승격**하는 액션이 v1에 필요한가 (§6.2) | 불필요 |
| Q3 | R4(교차 필드 편집 손실)를 사용자에게 **언제** 알릴 것인가 | 연결 시점 1회 고지 |
| Q4 | 폴링 주기 — 창 포커스 외에 **주기 타이머**도 둘 것인가 (§6.4) | 두지 않음. 포커스 + 수동으로 충분 |
| Q5 | 완전 삭제된 태스크의 **툼스톤 보존 기간** (§4.3) | 무기한. 성공 시 즉시 제거되므로 쌓이지 않는다 |
| Q6 | 스코프를 **앱이 만든 캘린더로만** 좁힐 수 있는가 (§4.1의 전용 캘린더와 정확히 맞는다) | 지금은 전체 `calendar`. 구글 문서로 확인 후 좁힐 것 |
