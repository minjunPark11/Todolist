# 상세 패널 둘 — 무엇이 다르고, 어느 쪽이 남는가

> 상태: **1·2단계 구현됨 · 3단계 미착수** · 2026-09-01
> (§3~§4가 1단계, §5가 2단계다. `TASK_CONTENT_MODE_DESIGN.md` §11.6과 §12.4가
> "패널 하나로 합칠 것인가"를 두 번 남겼고, 그 문서 밖이라고 적었다. 여기가 그 밖이다.
> 3단계 착수 전에 §5.5의 두 질문이 먼저다.)
> 대상: `components/TaskDetail.tsx`(레거시 패널)와 `components/tasks/TaskDrawer.tsx`(드로어)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 두 컴포넌트의 소스, 호출자, 그리고 `Task`의 필드 | [실측] |
| **인용** | `TICKTICK_TASK_DETAIL_SPEC.md`(드로어의 명세) · `TASK_CONTENT_MODE_DESIGN.md` | [인용] |
| **추론** | 위 둘로 메운 것 | [추론] |

스크린샷은 없다. **이 문서가 묻는 것은 참조 앱이 무엇을 그리는가가 아니라 우리 앱이
같은 것을 두 번 그리고 있는가**이고, 그건 우리 소스만으로 답해진다.

---

## 1. 두 패널은 각각 어디서 열리는가 [실측]

| | 레거시 패널 | 드로어 |
|---|---|---|
| 파일 | `components/TaskDetail.tsx` (359줄) | `components/tasks/TaskDrawer.tsx` (593줄) |
| 그리는 곳 | `App.tsx`의 `renderTaskDetail()` | `TasksModule.tsx` |
| 열리는 화면 | 오늘 · 매트릭스 · 캘린더 · 프로젝트 | 작업 모듈 전부(리스트·보드·간트) |
| 열림 상태 | `planner.selectedTask` (메모리) | `?task=` (URL) |
| 표현 | 오늘은 오버레이, 나머지는 격자 한 칸 | 넷(`taskDetailPresentationFor`) |

**같은 계정의 같은 작업을 두 화면이 서로 다른 패널로 연다.** 오늘 페이지에서 연 작업과
작업 모듈에서 연 같은 작업이 다른 필드를 보여 준다는 뜻이다.

---

## 2. 무엇이 어느 쪽에만 있었나 [실측]

1단계 **이전**의 표다.

| | 레거시 | 드로어 | 비고 |
|---|---|---|---|
| 제목 | ✓ | ✓ | 둘 다 `DeferredInput` |
| 본문(설명/체크리스트) | ✓ | ✓ | `TASK_CONTENT_MODE_DESIGN.md` §12가 맞춘 것 |
| 일정 | ✓ | ✓ | 둘 다 `SchedulePicker` |
| 리스트 이동 | `<select>` | `ListPicker` | 드로어 쪽만 하위 트리를 함께 옮긴다(§13.14) |
| 우선순위 | `<select>` | `PriorityPicker` | 드로어 쪽만 되돌릴 수 있다(§8.36) |
| 하위 작업 | ✓ | ✓ | 드로어 쪽만 자식을 열 수 있다 |
| **`notes`** | ✓ | **없음** | ← |
| **`blockedByTaskId`** | ✓ | **없음** | ← |
| 태그 | 없음 | ✓ | |
| 완료 체크박스 | 없음 | ✓ | |
| 고정(pin) 표시 | 없음 | ✓ | |
| ⋯ 액션 메뉴 | 버튼 4개 | ✓ | |
| 활동 기록 | 없음 | ✓ | |
| 상위 경로 | 없음 | ✓ | |
| 폭 조절 | 없음 | ✓ | |

**드로어가 레거시의 상위집합이 아니었다.** 화살표 둘이 그 이유이고, 그것이 "합칠 것인가"에
지금 답할 수 없었던 진짜 이유다 — 드로어로 갈아끼우는 순간 두 필드가 화면에서 사라진다.

### 2.1 그래서 순서는 이렇게 된다 [추론]

| 단계 | 내용 | 상태 |
|---|---|---|
| **1** | 드로어가 `notes`와 `blockedByTaskId`를 갖는다 — 상위집합이 되는 일 | **완료**(§3·§4) |
| **2** | 드로어의 호스트 배선을 `TasksModule`에서 꺼내 `App.tsx`도 부를 수 있게 | **완료**(§5) |
| **3** | 네 화면을 갈아끼우고 `TaskDetail.tsx`를 지운다 | 미착수 — §5.5가 먼저 |

1단계를 먼저 하는 이유는 되돌리기가 쉬워서가 아니라 **그것만으로도 값이 있어서**다.
드로어는 작업 모듈의 상세이고, 작업 모듈은 이 앱에서 작업을 읽는 주 화면이다. 그 화면에서
`notes`를 **볼 수도 쓸 수도 없었다.**

---

## 3. 1단계 (1) — "메모"가 두 필드를 가리키고 있었다

`Task`에는 본문이 될 수 있는 문자열이 **둘** 있다 [실측]:

| 필드 | 누가 쓰나 | 누가 읽나 |
|---|---|---|
| `description` | 레거시 패널("설명 추가") · 드로어의 본문 · 체크리스트 전환 | 행의 본문 배지 · 검색 · MCP 투영 |
| `notes` | 레거시 패널("메모") · **캘린더 이벤트 팝오버의 메모 칸** | 행의 본문 배지 · 검색 · MCP 투영 |

그런데 드로어의 본문 머리말은 `tasks.notes` — **"메모"** 였다. 레거시 패널의 `notes`
머리말도 **"메모"** 다. 같은 단어가 두 필드 위에 서 있었고, 두 패널은 같은 사람이 같은
날 여는 화면이다.

이 낱말이 어디서 왔는지는 명세에 있다 [인용]: 참조 앱의 본문 placeholder가
`Add notes...`이고(`TICKTICK_TASK_DETAIL_SPEC.md` 10605행), 같은 문서가 그 컴포넌트를
**Description Editor**라고 부른다(10413행). **참조 앱의 Task에는 헷갈릴 두 번째 필드가
없다.** 우리 것에는 있다.

그래서 드로어의 본문 머리말은 `tasks.description` — **"설명"** 이 됐고, placeholder도
레거시가 이미 쓰던 `taskDetail.addDescription`("설명 추가")을 쓴다. 드로어의 본문 칸에는
placeholder가 아예 없었다.

**`notes` 쪽 낱말은 건드리지 않았다.** 레거시 패널은 이미 "설명 추가" / "메모"로 갈라
부르고 있었으므로 애매했던 것은 드로어 한쪽이다.

---

## 4. 1단계 (2) — 없던 두 줄

### 4.1 `notes`

하위 작업 아래에 한 칸. 본문 옆이 아니라 **아래**인 이유는 이것이 본문의 대안이 아니기
때문이다 — 한 작업이 설명과 메모를 **동시에** 가질 수 있고, 그 둘을 한 머리말 아래
포개어 놓은 것이 §3의 혼동을 만든 배치다.

없어서 무엇이 깨져 있었는지 [실측]:

- 오늘 페이지에서 적은 메모가 작업 모듈에서 **보이지 않았다**
- 캘린더의 이벤트 팝오버는 그 칸을 `notes`에 쓰고 있다(`CalendarView.tsx:659`) — 즉
  **드로어에 없는 필드를 다른 화면이 계속 쓰고 있었다**
- 검색은 `notes`를 매칭한다(`server/data/queries/tasks.ts:87`). 검색 결과로 열린 상세에
  검색어가 없는 상태가 만들어진다
- 행의 본문 배지는 `notes || description`으로 켜진다(`TaskRowContent.tsx:79`). 배지가
  켜진 행을 열었는데 본문이 비어 있는 경우가 있었다

### 4.2 `blockedByTaskId`

리스트·태그 아래 속성 줄 하나(`선행 작업`)와, 그 반대 방향(`이 작업을 기다리는 항목`).

이것도 레거시 전용이었다. 그런데 이 필드는 **화면 밖에서 계속 일하고 있다** [실측]:
간트의 의존 화살표(`domain/view/connectors.ts`), 오늘 큐의 강등(`isTaskBlocked`),
MCP 투영. 작업 모듈에서는 **그 이유를 말하는 컨트롤 없이 결과만 보였다.**

**`<select>`로 뒀다.** 위의 리스트와 태그는 팝오버 피커인데, 그 둘에는 명세가 있고
(§13.9 · §13.36) 의존 관계에는 없다. 이웃과 모양을 맞추려고 세 번째 팝오버를 발명하는
것은 근거 없는 설계다 — 레거시가 그리던 컨트롤을 **그대로** 옮기는 것이 이 단계를
재설계가 아니라 이동으로 남긴다.

i18n 키는 `tasks.*`로 새로 만들지 않고 **`taskDetail.*`를 그대로 쓴다.** 같은 문자열을
두 벌 적으면 §30.2가 센 고아 키가 늘어난다. 없어지는 것은 패널이고 낱말은 남는다.

### 4.3 `blockerChoices` — 두 패널이 같은 규칙을 두 번 적고 있었다

레거시 패널은 "설정된 선행 작업이 완료돼 후보 목록에서 빠져도 옵션으로 남긴다"를
**JSX 안에서** 처리했다. `<select>`의 값이 옵션에 없으면 빈칸으로 그려지므로, 이건
표시 취향이 아니라 **컨트롤이 거짓말을 하지 않게 하는 규칙**이다.

드로어가 같은 줄을 그리면 그 규칙도 두 벌이 된다. 그래서
`domain/tasks/dependencies.ts`의 `blockerChoices(tasks, task)`로 나갔고, 계정에 없는
id는 **id 자체를 이름으로** 돌려준다 — 다른 기기에서 온 값도 필드가 쥐고 있는 값이고,
날 id를 보여 주는 편이 "없음"이라고 말하는 것보다 정직하다.

### 4.4 검증

- 유닛 2,156개 통과(신규 12개: 도메인 4 · 드로어 8) · `tsc` 통과
- 신규 시험이 고정하는 것은 낱말이 아니라 **낱말과 필드의 짝**이다. 머리말만 확인하는
  시험은 "메모" 칸이 `description`을 쓰는 드로어도 통과시킨다

### 4.5 남은 것

- **2단계가 진짜 일이다.** 드로어의 프롭 중 `presentation` · `resize` · `actions` ·
  `activity` · `ancestors` · `canAddSubtask`와 `onComplete` · `onSetPriority` ·
  `onRunAction`은 전부 `TasksModule` **안에서** 만들어진다. `App.tsx`는 `drawer` 프롭
  묶음(§16.28)을 이미 넘기고 있으므로 데이터는 있고, 없는 것은 **호스트**다
- 오늘 페이지의 오버레이(`tdy-detail-overlay`)와 드로어의 네 표현이 겹친다. 3단계는
  둘 중 하나를 고르는 일이지 둘을 합치는 일이 아니다
- 레거시 패널의 `estimatedMinutes`·`categoryId`는 **어느 패널에도 없다.** 이 문서가
  세지 않은 것이고, 3단계가 패널을 지우기 전에 한 번 더 세야 한다

---

## 5. 2단계 — 호스트를 꺼냈다 (2026-09-01)

§4.5가 "2단계가 진짜 일이다"라고 적었다. 데이터는 `App.tsx`에 이미 있고 없는 것은
**호스트**라는 것도. 그 호스트를 꺼냈다.

### 5.1 없던 것은 컬렉션이 아니라 파생이었다 [실측]

드로어는 일부러 **컬렉션이 아니라 답**을 받는다(§15.63). 그 답을 만드는 자리가
`TasksModule` 안이었고, 그래서 나머지 네 화면은 재료를 다 갖고도 패널을 열 수 없었다.

| 드로어가 받는 것 | 누가 만들었나 | 무엇이 필요한가 |
|---|---|---|
| `ancestors` · `canAddSubtask` | `TasksModule` 지역 | 전체 `tasks` |
| `blockerOptions` · `blocking` | 〃 (§4에서 추가) | 전체 `tasks` |
| `actions` | 〃 | `taskActions` + `focusBusy` |
| `onRunAction` | 〃 `runTaskAction` | 레지스트리 switch 12갈래 + 되돌리기 |
| `onComplete` · `onSetPriority` | 〃 `mutate` | 명령 경로와 undo |
| `activity` · `onCloseActivity` | 〃 `activityTaskId` 상태 | Task별 상태 |
| `presentation` · `resize` | 〃 훅 두 개 | 이동 가능 |

`App.tsx`가 이 표를 손으로 다시 쓰는 것은 **분기 하나짜리 복제가 아니라 switch 하나짜리
복제**다 — 두 화면이 "안 함으로 표시"가 무엇을 쓰는지에 대해 갈라지는 방법.

### 5.2 그래서 셋으로 나눴다

| 새 파일 | 무엇 | 줄 |
|---|---|---|
| `hooks/useTaskCommands.ts` | 알림 · `mutate` · `toggleDone` · 레지스트리 switch · 클립보드 · 기록 상태 | 216 |
| `components/tasks/TaskDetailPane.tsx` | "여기 Task 하나"와 드로어의 프롭 서른 개 사이의 파생 전부 | 158 |
| `components/tasks/TaskUndoStrip.tsx` | §9.36의 줄 | 46 |
| `components/tasks/taskDetailBundle.ts` | `TaskDetailBundle` — 인라인이던 프롭 묶음 | 48 |

`TasksModule.tsx`: **1,553줄 → 1,294줄.**

**나눈 자리의 근거:**

`useTaskCommands`는 **훅이지 컴포넌트가 아니다.** 드로어만 쓰는 게 아니기 때문이다 —
행의 체크박스, 행의 `⋯`, 보드의 카드 메뉴가 전부 같은 `mutate`와 같은 알림을 쓴다.
컴포넌트로 만들었다면 모듈이 자기 행을 위해 두 번째 사본을 들고 있어야 했다.

`TaskUndoStrip`은 **컴포넌트다.** 훅이 알림을 들고 있는데 그걸 그리는 곳이 없으면
되돌릴 수 없는 변경이 된다. 두 화면이 각자 그리되 같은 것을 그려야 한다.

`TaskDetailPane`은 **상태를 갖지 않는다.** `presentation`과 `resize`는 프롭으로 들어온다 —
어디에 그리는가는 표면의 질문이고(§15.17), Task가 무엇인가는 아니다.

그리고 꺼내는 김에 하나가 합쳐졌다: 드로어의 완료 체크박스는 이제 행의 체크박스와 **같은
`toggleDone`** 을 부른다. 두 자리가 같은 삼항식을 따로 적고 있었고, §12.12가 막으려는
것이 정확히 그 모양이다 — 행과 패널이 `done`이 무엇을 쓰는지에 대해 갈라지는 것.

**링크는 표면이 만든다.** `useTaskCommands`는 `linkFor`를 받는다. 작업 모듈의 Task는
스코프 아래 `?task=`에 살고, 열린 Task를 메모리에 두는 페이지는 다른 주소를 내놓는다 —
훅이 알아낼 수 있는 종류가 아니다(§15.19).

### 5.3 "App도 부를 수 있다"를 시험으로 적었다

리팩터링의 값은 전부 다음 단계에 있고, 그래서 **끝났다는 말이 검증되지 않는다**.
`taskDetailPane.test.tsx`가 그 자리를 메운다: `TasksModule`을 **한 줄도 쓰지 않고**
번들·`useTaskCommands`·표현·너비를 손으로 조립해 패널을 연다. 대략 스무 줄이고, 그게
3단계가 화면당 치를 값이다.

그 파일이 고정하는 것은 마크업이 아니라 **이음매**다:

- 파생이 파생된 채로 도착한다 — 상위 경로가 열리고, 반대 방향 의존이 열리고,
  `blockerChoices`가 고리를 닫을 후보를 거부한다
- 패널에서 한 변경이 같은 명령 경로로 돌아온다 — 완료 → `onMutate` → 취소 줄 →
  누르면 복원
- 패널 **밖에서** 실행한 액션이 패널이 읽는 같은 상태에 앉는다 (`작업 기록`)

### 5.4 검증

- 유닛 2,161개 통과(신규 4개) · `tsc` 통과 · E2E 118개 통과
- 사용하지 않게 된 import 29개를 걷어냈다. 그중 일곱(`useEffect` · `Space` ·
  `inboxBoardColumns` · `COMPLETED_PAGE` · `createListPayload` · `CreateListDraft` ·
  `platform`)은 **이번 변경 이전부터** 죽어 있었다
- 실제 앱(dev, ko, `/inbox`): 드로어 열기 · 완료 → `완료로 표시했습니다` + 실행 취소로
  복원 · `⋯` 여덟 항목 · `작업 기록` 열림 · `링크 복사`가 `/inbox?task=…`를 만든다
  (클립보드가 거부되면 §15.22대로 그 URL을 줄에 내놓는다) · 행 체크박스와 행 `⋯`도 같은 줄

### 5.5 남은 것 — 3단계가 먼저 답해야 하는 것 둘

- **표현이 겹친다.** 오늘 페이지는 자기 오버레이(`tdy-detail-overlay`)를 갖고 있고
  드로어는 표현이 넷이다(§15.17). 3단계는 둘을 합치는 일이 아니라 **하나를 고르는 일**이다
- **`estimatedMinutes`와 `categoryId`는 어느 패널에도 없다.** §4.5가 세어 뒀고 아직
  아무도 답하지 않았다. 레거시 패널을 지우면 그 둘은 "어디에도 없던 채로" 남는다 —
  지우기 전에 둘 곳을 정하거나, 정하지 않기로 정해야 한다
