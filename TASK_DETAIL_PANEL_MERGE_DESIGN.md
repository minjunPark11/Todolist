# 상세 패널 둘 — 무엇이 다르고, 어느 쪽이 남는가

> 상태: **1~3단계 구현됨 · 패널은 하나고 주소를 갖는다** · 2026-09-01
> (§3~§4가 1단계, §5가 2단계, §6이 3단계, §7·§8이 §6.9의 첫 두 줄이다.
> `TASK_CONTENT_MODE_DESIGN.md` §11.6과 §12.4가 "패널 하나로 합칠 것인가"를 두 번
> 남겼고, 그 문서 밖이라고 적었다. 여기가 그 밖이었고, 답은 **드로어**다. 남은 것은
> §6.9의 둘 — 전부 이 문서가 만든 것이 아니라 지나가며 세어 둔 것이다.)
> 대상: `components/TaskDetail.tsx`(레거시 패널, **지웠다**)와
> `components/tasks/TaskDrawer.tsx`(드로어, 남았다)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 두 컴포넌트의 소스, 호출자, 그리고 `Task`의 필드 | [실측] |
| **인용** | `TICKTICK_TASK_DETAIL_SPEC.md`(드로어의 명세) · `TASK_CONTENT_MODE_DESIGN.md` | [인용] |
| **추론** | 위 둘로 메운 것 | [추론] |

스크린샷은 없다. **이 문서가 묻는 것은 참조 앱이 무엇을 그리는가가 아니라 우리 앱이
같은 것을 두 번 그리고 있는가**이고, 그건 우리 소스만으로 답해진다.

---

## 1. 두 패널은 각각 어디서 열리는가 [실측]

| | 레거시 패널 | 드로어 |
|---|---|---|
| 파일 | `components/TaskDetail.tsx` (359줄) | `components/tasks/TaskDrawer.tsx` (593줄) |
| 그리는 곳 | `App.tsx`의 `renderTaskDetail()` | `TasksModule.tsx` |
| 열리는 화면 | 오늘 · 매트릭스 (§6.1이 잰 것. 이 줄은 `TaskDetail.tsx`의 주석을 믿고 "캘린더 · 프로젝트"까지 적고 있었다 — 캘린더는 이 패널을 그리지 않았고 프로젝트 페이지는 존재하지 않는다) | 작업 모듈 전부(리스트·보드·간트) |
| 열림 상태 | `planner.selectedTask` (메모리) | `?task=` (URL) |
| 표현 | 오늘은 오버레이, 나머지는 격자 한 칸 | 넷(`taskDetailPresentationFor`) |

**같은 계정의 같은 작업을 두 화면이 서로 다른 패널로 연다.** 오늘 페이지에서 연 작업과
작업 모듈에서 연 같은 작업이 다른 필드를 보여 준다는 뜻이다.

---

## 2. 무엇이 어느 쪽에만 있었나 [실측]

1단계 **이전**의 표다.

| | 레거시 | 드로어 | 비고 |
|---|---|---|---|
| 제목 | ✓ | ✓ | 둘 다 `DeferredInput` |
| 본문(설명/체크리스트) | ✓ | ✓ | `TASK_CONTENT_MODE_DESIGN.md` §12가 맞춘 것 |
| 일정 | ✓ | ✓ | 둘 다 `SchedulePicker` |
| 리스트 이동 | `<select>` | `ListPicker` | 드로어 쪽만 하위 트리를 함께 옮긴다(§13.14) |
| 우선순위 | `<select>` | `PriorityPicker` | 드로어 쪽만 되돌릴 수 있다(§8.36) |
| 하위 작업 | ✓ | ✓ | 드로어 쪽만 자식을 열 수 있다 |
| **`notes`** | ✓ | **없음** | ← |
| **`blockedByTaskId`** | ✓ | **없음** | ← |
| 태그 | 없음 | ✓ | |
| 완료 체크박스 | 없음 | ✓ | |
| 고정(pin) 표시 | 없음 | ✓ | |
| ⋯ 액션 메뉴 | 버튼 4개 | ✓ | |
| 활동 기록 | 없음 | ✓ | |
| 상위 경로 | 없음 | ✓ | |
| 폭 조절 | 없음 | ✓ | |

**드로어가 레거시의 상위집합이 아니었다.** 화살표 둘이 그 이유이고, 그것이 "합칠 것인가"에
지금 답할 수 없었던 진짜 이유다 — 드로어로 갈아끼우는 순간 두 필드가 화면에서 사라진다.

### 2.1 그래서 순서는 이렇게 된다 [추론]

| 단계 | 내용 | 상태 |
|---|---|---|
| **1** | 드로어가 `notes`와 `blockedByTaskId`를 갖는다 — 상위집합이 되는 일 | **완료**(§3·§4) |
| **2** | 드로어의 호스트 배선을 `TasksModule`에서 꺼내 `App.tsx`도 부를 수 있게 | **완료**(§5) |
| **3** | 화면을 갈아끼우고 `TaskDetail.tsx`를 지운다 | **완료**(§6) — 네 화면이 아니라 **둘**이었다(§6.1) |

1단계를 먼저 하는 이유는 되돌리기가 쉬워서가 아니라 **그것만으로도 값이 있어서**다.
드로어는 작업 모듈의 상세이고, 작업 모듈은 이 앱에서 작업을 읽는 주 화면이다. 그 화면에서
`notes`를 **볼 수도 쓸 수도 없었다.**

---

## 3. 1단계 (1) — "메모"가 두 필드를 가리키고 있었다

`Task`에는 본문이 될 수 있는 문자열이 **둘** 있다 [실측]:

| 필드 | 누가 쓰나 | 누가 읽나 |
|---|---|---|
| `description` | 레거시 패널("설명 추가") · 드로어의 본문 · 체크리스트 전환 | 행의 본문 배지 · 검색 · MCP 투영 |
| `notes` | 레거시 패널("메모") · **캘린더 이벤트 팝오버의 메모 칸** | 행의 본문 배지 · 검색 · MCP 투영 |

그런데 드로어의 본문 머리말은 `tasks.notes` — **"메모"** 였다. 레거시 패널의 `notes`
머리말도 **"메모"** 다. 같은 단어가 두 필드 위에 서 있었고, 두 패널은 같은 사람이 같은
날 여는 화면이다.

이 낱말이 어디서 왔는지는 명세에 있다 [인용]: 참조 앱의 본문 placeholder가
`Add notes...`이고(`TICKTICK_TASK_DETAIL_SPEC.md` 10605행), 같은 문서가 그 컴포넌트를
**Description Editor**라고 부른다(10413행). **참조 앱의 Task에는 헷갈릴 두 번째 필드가
없다.** 우리 것에는 있다.

그래서 드로어의 본문 머리말은 `tasks.description` — **"설명"** 이 됐고, placeholder도
레거시가 이미 쓰던 `taskDetail.addDescription`("설명 추가")을 쓴다. 드로어의 본문 칸에는
placeholder가 아예 없었다.

**`notes` 쪽 낱말은 건드리지 않았다.** 레거시 패널은 이미 "설명 추가" / "메모"로 갈라
부르고 있었으므로 애매했던 것은 드로어 한쪽이다.

---

## 4. 1단계 (2) — 없던 두 줄

### 4.1 `notes`

하위 작업 아래에 한 칸. 본문 옆이 아니라 **아래**인 이유는 이것이 본문의 대안이 아니기
때문이다 — 한 작업이 설명과 메모를 **동시에** 가질 수 있고, 그 둘을 한 머리말 아래
포개어 놓은 것이 §3의 혼동을 만든 배치다.

없어서 무엇이 깨져 있었는지 [실측]:

- 오늘 페이지에서 적은 메모가 작업 모듈에서 **보이지 않았다**
- 캘린더의 이벤트 팝오버는 그 칸을 `notes`에 쓰고 있다(`CalendarView.tsx:659`) — 즉
  **드로어에 없는 필드를 다른 화면이 계속 쓰고 있었다**
- 검색은 `notes`를 매칭한다(`server/data/queries/tasks.ts:87`). 검색 결과로 열린 상세에
  검색어가 없는 상태가 만들어진다
- 행의 본문 배지는 `notes || description`으로 켜진다(`TaskRowContent.tsx:79`). 배지가
  켜진 행을 열었는데 본문이 비어 있는 경우가 있었다

### 4.2 `blockedByTaskId`

리스트·태그 아래 속성 줄 하나(`선행 작업`)와, 그 반대 방향(`이 작업을 기다리는 항목`).

이것도 레거시 전용이었다. 그런데 이 필드는 **화면 밖에서 계속 일하고 있다** [실측]:
간트의 의존 화살표(`domain/view/connectors.ts`), 오늘 큐의 강등(`isTaskBlocked`),
MCP 투영. 작업 모듈에서는 **그 이유를 말하는 컨트롤 없이 결과만 보였다.**

**`<select>`로 뒀다.** 위의 리스트와 태그는 팝오버 피커인데, 그 둘에는 명세가 있고
(§13.9 · §13.36) 의존 관계에는 없다. 이웃과 모양을 맞추려고 세 번째 팝오버를 발명하는
것은 근거 없는 설계다 — 레거시가 그리던 컨트롤을 **그대로** 옮기는 것이 이 단계를
재설계가 아니라 이동으로 남긴다.

i18n 키는 `tasks.*`로 새로 만들지 않고 **`taskDetail.*`를 그대로 쓴다.** 같은 문자열을
두 벌 적으면 §30.2가 센 고아 키가 늘어난다. 없어지는 것은 패널이고 낱말은 남는다.

### 4.3 `blockerChoices` — 두 패널이 같은 규칙을 두 번 적고 있었다

레거시 패널은 "설정된 선행 작업이 완료돼 후보 목록에서 빠져도 옵션으로 남긴다"를
**JSX 안에서** 처리했다. `<select>`의 값이 옵션에 없으면 빈칸으로 그려지므로, 이건
표시 취향이 아니라 **컨트롤이 거짓말을 하지 않게 하는 규칙**이다.

드로어가 같은 줄을 그리면 그 규칙도 두 벌이 된다. 그래서
`domain/tasks/dependencies.ts`의 `blockerChoices(tasks, task)`로 나갔고, 계정에 없는
id는 **id 자체를 이름으로** 돌려준다 — 다른 기기에서 온 값도 필드가 쥐고 있는 값이고,
날 id를 보여 주는 편이 "없음"이라고 말하는 것보다 정직하다.

### 4.4 검증

- 유닛 2,156개 통과(신규 12개: 도메인 4 · 드로어 8) · `tsc` 통과
- 신규 시험이 고정하는 것은 낱말이 아니라 **낱말과 필드의 짝**이다. 머리말만 확인하는
  시험은 "메모" 칸이 `description`을 쓰는 드로어도 통과시킨다

### 4.5 남은 것

- **2단계가 진짜 일이다.** 드로어의 프롭 중 `presentation` · `resize` · `actions` ·
  `activity` · `ancestors` · `canAddSubtask`와 `onComplete` · `onSetPriority` ·
  `onRunAction`은 전부 `TasksModule` **안에서** 만들어진다. `App.tsx`는 `drawer` 프롭
  묶음(§16.28)을 이미 넘기고 있으므로 데이터는 있고, 없는 것은 **호스트**다
- 오늘 페이지의 오버레이(`tdy-detail-overlay`)와 드로어의 네 표현이 겹친다. 3단계는
  둘 중 하나를 고르는 일이지 둘을 합치는 일이 아니다
- 레거시 패널의 `estimatedMinutes`·`categoryId`는 **어느 패널에도 없다.** 이 문서가
  세지 않은 것이고, 3단계가 패널을 지우기 전에 한 번 더 세야 한다

---

## 5. 2단계 — 호스트를 꺼냈다 (2026-09-01)

§4.5가 "2단계가 진짜 일이다"라고 적었다. 데이터는 `App.tsx`에 이미 있고 없는 것은
**호스트**라는 것도. 그 호스트를 꺼냈다.

### 5.1 없던 것은 컬렉션이 아니라 파생이었다 [실측]

드로어는 일부러 **컬렉션이 아니라 답**을 받는다(§15.63). 그 답을 만드는 자리가
`TasksModule` 안이었고, 그래서 나머지 네 화면은 재료를 다 갖고도 패널을 열 수 없었다.

| 드로어가 받는 것 | 누가 만들었나 | 무엇이 필요한가 |
|---|---|---|
| `ancestors` · `canAddSubtask` | `TasksModule` 지역 | 전체 `tasks` |
| `blockerOptions` · `blocking` | 〃 (§4에서 추가) | 전체 `tasks` |
| `actions` | 〃 | `taskActions` + `focusBusy` |
| `onRunAction` | 〃 `runTaskAction` | 레지스트리 switch 12갈래 + 되돌리기 |
| `onComplete` · `onSetPriority` | 〃 `mutate` | 명령 경로와 undo |
| `activity` · `onCloseActivity` | 〃 `activityTaskId` 상태 | Task별 상태 |
| `presentation` · `resize` | 〃 훅 두 개 | 이동 가능 |

`App.tsx`가 이 표를 손으로 다시 쓰는 것은 **분기 하나짜리 복제가 아니라 switch 하나짜리
복제**다 — 두 화면이 "안 함으로 표시"가 무엇을 쓰는지에 대해 갈라지는 방법.

### 5.2 그래서 셋으로 나눴다

| 새 파일 | 무엇 | 줄 |
|---|---|---|
| `hooks/useTaskCommands.ts` | 알림 · `mutate` · `toggleDone` · 레지스트리 switch · 클립보드 · 기록 상태 | 216 |
| `components/tasks/TaskDetailPane.tsx` | "여기 Task 하나"와 드로어의 프롭 서른 개 사이의 파생 전부 | 158 |
| `components/tasks/TaskUndoStrip.tsx` | §9.36의 줄 | 46 |
| `components/tasks/taskDetailBundle.ts` | `TaskDetailBundle` — 인라인이던 프롭 묶음 | 48 |

`TasksModule.tsx`: **1,553줄 → 1,294줄.**

**나눈 자리의 근거:**

`useTaskCommands`는 **훅이지 컴포넌트가 아니다.** 드로어만 쓰는 게 아니기 때문이다 —
행의 체크박스, 행의 `⋯`, 보드의 카드 메뉴가 전부 같은 `mutate`와 같은 알림을 쓴다.
컴포넌트로 만들었다면 모듈이 자기 행을 위해 두 번째 사본을 들고 있어야 했다.

`TaskUndoStrip`은 **컴포넌트다.** 훅이 알림을 들고 있는데 그걸 그리는 곳이 없으면
되돌릴 수 없는 변경이 된다. 두 화면이 각자 그리되 같은 것을 그려야 한다.

`TaskDetailPane`은 **상태를 갖지 않는다.** `presentation`과 `resize`는 프롭으로 들어온다 —
어디에 그리는가는 표면의 질문이고(§15.17), Task가 무엇인가는 아니다.

그리고 꺼내는 김에 하나가 합쳐졌다: 드로어의 완료 체크박스는 이제 행의 체크박스와 **같은
`toggleDone`** 을 부른다. 두 자리가 같은 삼항식을 따로 적고 있었고, §12.12가 막으려는
것이 정확히 그 모양이다 — 행과 패널이 `done`이 무엇을 쓰는지에 대해 갈라지는 것.

**링크는 표면이 만든다.** `useTaskCommands`는 `linkFor`를 받는다. 작업 모듈의 Task는
스코프 아래 `?task=`에 살고, 열린 Task를 메모리에 두는 페이지는 다른 주소를 내놓는다 —
훅이 알아낼 수 있는 종류가 아니다(§15.19).

### 5.3 "App도 부를 수 있다"를 시험으로 적었다

리팩터링의 값은 전부 다음 단계에 있고, 그래서 **끝났다는 말이 검증되지 않는다**.
`taskDetailPane.test.tsx`가 그 자리를 메운다: `TasksModule`을 **한 줄도 쓰지 않고**
번들·`useTaskCommands`·표현·너비를 손으로 조립해 패널을 연다. 대략 스무 줄이고, 그게
3단계가 화면당 치를 값이다.

그 파일이 고정하는 것은 마크업이 아니라 **이음매**다:

- 파생이 파생된 채로 도착한다 — 상위 경로가 열리고, 반대 방향 의존이 열리고,
  `blockerChoices`가 고리를 닫을 후보를 거부한다
- 패널에서 한 변경이 같은 명령 경로로 돌아온다 — 완료 → `onMutate` → 취소 줄 →
  누르면 복원
- 패널 **밖에서** 실행한 액션이 패널이 읽는 같은 상태에 앉는다 (`작업 기록`)

### 5.4 검증

- 유닛 2,161개 통과(신규 4개) · `tsc` 통과 · E2E 118개 통과
- 사용하지 않게 된 import 29개를 걷어냈다. 그중 일곱(`useEffect` · `Space` ·
  `inboxBoardColumns` · `COMPLETED_PAGE` · `createListPayload` · `CreateListDraft` ·
  `platform`)은 **이번 변경 이전부터** 죽어 있었다
- 실제 앱(dev, ko, `/inbox`): 드로어 열기 · 완료 → `완료로 표시했습니다` + 실행 취소로
  복원 · `⋯` 여덟 항목 · `작업 기록` 열림 · `링크 복사`가 `/inbox?task=…`를 만든다
  (클립보드가 거부되면 §15.22대로 그 URL을 줄에 내놓는다) · 행 체크박스와 행 `⋯`도 같은 줄

### 5.5 남은 것 — 3단계가 먼저 답해야 하는 것 둘

- **표현이 겹친다.** 오늘 페이지는 자기 오버레이(`tdy-detail-overlay`)를 갖고 있고
  드로어는 표현이 넷이다(§15.17). 3단계는 둘을 합치는 일이 아니라 **하나를 고르는 일**이다
- **`estimatedMinutes`와 `categoryId`는 어느 패널에도 없다.** §4.5가 세어 뒀고 아직
  아무도 답하지 않았다. 레거시 패널을 지우면 그 둘은 "어디에도 없던 채로" 남는다 —
  지우기 전에 둘 곳을 정하거나, 정하지 않기로 정해야 한다

---

## 6. 3단계 — 패널 하나 (2026-09-01)

§5.5가 3단계 전에 답할 것 둘을 남겼다. 첫째는 **드로어로 통일**로 답이 왔고, 둘째는
§6.4가 답한다. 그래서 레거시 패널을 지웠다.

### 6.1 먼저 — 네 화면이 아니라 두 화면이었다 [실측]

`TaskDetail.tsx`의 주석은 이 패널이 "오늘·캘린더·프로젝트 페이지가 여는 것"이라고
적어 뒀고, §1의 표가 그 문장을 그대로 옮겼다. **틀렸다.** `renderTaskDetail()`의 호출처는
둘뿐이다:

| 화면 | 어떻게 |
|---|---|
| 오늘(`/app`) | `.tdy-detail-overlay` 안의 오버레이 |
| 매트릭스(`/board`) | 격자의 둘째 칸 |

캘린더는 `onClearTaskSelection`만 받고 패널을 그리지 않는다. 프로젝트 페이지는 **없다**
(`activePage`는 today · board · calendar · focus · settings 다섯이다). 집중 페이지는
`onOpenTask={planner.selectTask}`를 넘기지만 그 선택을 그리는 것이 없다 — **클릭이 아무
일도 하지 않는 자리**이고, 이 변경 이전부터 그랬다. 3단계는 그 죽은 배선을 고치지
않는다. 다만 이제는 **패널을 붙이면 되는 일**이 됐다(§6.5).

### 6.2 오늘 페이지의 다섯 번째 표현이 사라졌다

드로어는 표현이 넷이다(§15.17): 넓은 데스크톱은 옆칸, 좁은 데스크톱은 오버레이,
태블릿은 시트, 모바일은 전체 화면. 오늘 페이지는 여기에 **다섯 번째**를 갖고 있었다 —
`.tdy-detail-overlay`, 자기만의 스크림과 400px 서랍.

그게 있던 이유는 주석에 적혀 있었다: "오늘 격자에는 나란히 놓을 상세 칸이 없다." 이제
있다. 격자는 §15.17이 **칸이라고 말할 때만** 칸이 된다:

```tsx
const detailIsColumn = Boolean(planner.selectedTask) && detailPresentation === "inline-drawer";
```

이전 규칙은 "작업이 열려 있으니 둘째 칸이 있다"였다. 패널이 이 절반의 앱 소유였을 때는
맞는 말이었고, 표현이 넷인 컴포넌트에는 아니다 — 셋은 `position: fixed`라 칸을 차지하지
않는다.

**격자의 둘째 트랙도 바뀌었다.** `360px` → `auto`. 360은 레거시 패널에 박혀 있던 폭이고,
드로어의 폭은 읽는 사람 것이다(§1.12, 360~600, 자기 모서리의 손잡이로 끈다). `AppPages`가
`--tm-detail-w`를 격자에 얹어서 칸과 패널이 같은 숫자를 읽는다.

### 6.3 문서로 스크롤하는 페이지 위의 옆칸

여기 하나 걸렸다. `.tm-shell`은 자기 높이를 묶는다(`100dvh` · `overflow: hidden`), 그리고
그게 `.tm-drawer-scroll`이 **패널 안에서** 스크롤할 수 있는 이유다. 오늘·매트릭스는 레거시
셸이고 **문서로 스크롤한다** — 높이를 묶는 것이 없으면 패널이 내용 전체 높이로 자라고
고정 헤더(§1.7)를 데리고 간다.

한 규칙으로 묶었다:

```css
.page-grid > .tm-drawer.is-inline-drawer {
  position: sticky; top: 32px; max-height: calc(100vh - 64px); align-self: start;
}
```

**레거시 패널이 이 자리에서 쓰던 것과 같은 두 줄이다.** 하는 일도 같다: flex 열에게 나눌
높이를 준다. 1360×820에서 실측: `sticky` · `max-height: 756px` · 패널 480 · 헤더가
제자리 · 스크롤 영역이 나머지 · 가로 넘침 0.

### 6.4 §5.5의 둘째 질문 — `estimatedMinutes`와 `categoryId`

**이 변경이 잃게 하는 것이 없다.** 두 필드는 §4.5가 세었을 때 이미 **어느 패널에도
없었다.** 레거시를 지운다고 도달 가능하던 것이 사라지는 게 아니라, 도달 불가능하던 것이
그대로 도달 불가능하다. 그래서 3단계를 막지 않는다 — 다만 **답해지지도 않았다.**

같은 실측을 다시 하면서 **정말로 줄어드는 것 둘**을 찾았다. 이쪽이 §5.5가 물었어야 할
질문이다:

| 레거시 버튼 | 무엇이 됐나 |
|---|---|
| `오늘로 이동` | 일정 팝오버의 빠른 날짜가 같은 일을 한다 |
| `복제` | ⋯ 메뉴의 `복제` |
| **`보관`** | ⋯ 메뉴의 **`안 함으로 표시`**. 같은 필드다 — `planner.archiveTask`는 `wontDoAt`을 쓰고, 로드 시 정규화가 옛 `archived`를 그리로 옮긴다(`normalize.ts`). 라벨이 처음으로 사실을 말한다 |
| **`삭제`** | ⋯ 메뉴의 **`휴지통으로`**. **같지 않다** ← |

`삭제`는 저장소에서 영구히 지웠고(`deleteTaskWithUndo`), `휴지통으로`는 `deletedAt`을
쓰는 되돌릴 수 있는 삭제다. 후자가 더 새로운 모델이고 휴지통 스코프가 그것을 보여 주며
`복원`이 있다. 그래도 **영구 삭제는 이 두 화면에서 사라진다** — 남은 입구는 캘린더의
`onDeleteTask` 하나다(확인 모달과 토스트 되돌리기는 그대로 산다).

일부러 그렇게 뒀다. 상세 패널에서 작업을 영구히 지우는 것은 휴지통 이전의 모델이고,
레지스트리의 danger 그룹에는 그 자리가 없다(§15.29). **휴지통을 비우는 방법이 없다**는
것은 이 문서의 일이 아니고, 다음 사람이 조사부터 하지 않도록 여기 적어 둔다.

> **정정 (2026-09-02).** 위의 "어느 화면에도 없다"는 **두 항목 모두에 대해 틀렸다.**
> 다시 재 보니:
>
> - `categoryId`는 캘린더 이벤트 팝오버에서 편집된다(`EventPopover.tsx:240`의
>   `onChangeCategory`).
> - `estimatedMinutes`는 두 곳에서 읽힌다 — 오늘 큐의 `예상 N분`
>   (`FocusQueue.tsx:231`)과 캘린더 오른쪽 패널의 `· N분`
>   (`CalendarRightTaskPanel.tsx:176`) — 그리고 캘린더 리사이즈가 값을 쓴다
>   (`CalendarView.tsx:568`).
>
> 없는 것은 화면이 아니라 **상세의 편집 칸**이고, 그보다 큰 것이 하나 더 있다:
> `getTaskDuration`(`CalendarView.tsx:436`)이 `estimatedMinutes`를
> `endTime − startTime`보다 **먼저** 보는데, 시각을 바꾸는 세 자리 중 추정치를 같이
> 갱신하는 것은 캘린더 리사이즈 하나뿐이다. 상세의 일정 피커로 종료 시각을 바꾸면
> 추정치가 옛 값으로 남고 캘린더가 **그것을 실제 시각보다 우선한다.**
>
> 세어만 둔다. 이 문서의 일이 아니고, 별도 설계도 두지 않기로 했다(사용자 확정).

### 6.5 지운 것

| 무엇 | 줄 |
|---|---|
| `components/TaskDetail.tsx` | 354 |
| `components/taskDetailChecklist.test.tsx` | 147 |
| `01-base.css`의 `.detail-*` · `.filters` · `.muted-panel` | 287 |
| `02-calendar.css`의 반응형 패널 · 퇴장 애니메이션 · `.task-action-row` 무리 | 71 |
| `03-planning.css` · `04-today.css`(`.tdy-detail-overlay`) · `21-components.css` | 40 |
| i18n 키 13개 × 2언어 | 26 |

**시험이 그 키들을 먼저 말했다.** §32.4가 붙인 고아 0 불변식이 패널을 지운 직후 실패했고,
`taskDetail.title` · `selectTaskHint` · `taskTitleAria` · `taskDescriptionAria` ·
`planning` · `priority` · `list` · `noList` · `actions`와 `spaceHub.section.subtasks` ·
`spaceHub.action.addSubtask` · `common.duplicate` · `today.moveToToday`를 이름으로 댔다.
§32.4가 "화면만 지우고 키를 두고 가면 시험이 그걸 말해 준다"고 적은 그대로다.

`taskDetail.*` 여덟은 살아남았다 — 드로어가 그것들을 말한다(§4.2가 두 벌 적지 않기로 한
결과다).

### 6.6 링크 하나가 정직하지 않다

`⋯ › 링크 복사`는 이 두 화면에 **없던 기능**이라 새로 생겼는데, 이 페이지들은 열린 작업을
주소가 아니라 메모리에 둔다. 자기 주소를 복사하면 `오늘`로 가는 링크가 되고 받은 사람은
찾아 헤맨다.

그래서 `linkFor`가 **작업 모듈의 주소**를 만든다 — `/today?task=<id>`. 모듈의 `openedTask`는
스코프의 행이 아니라 **전체 작업**에서 찾으므로 어느 스코프로 열려도 맞는 작업이 열린다.
실측: 복사된 링크를 열면 `발표 준비`가 드로어에 뜬다.

정직하지만 최선은 아니다. **이 페이지들의 주소에 `?task=`를 넣는 것**이 더 나은 답이고,
그러면 새로고침과 뒤로 가기도 함께 고쳐진다(§5.15가 드로어에 대해 이미 그렇게 한다).
이 문서의 다음 일이다.

### 6.7 검증

- 유닛 2,155개 통과(레거시 패널의 체크리스트 시험 6개가 대상과 함께 나갔다 — 같은 동작을
  `taskDrawerBody` · `ChecklistEditor` · `contentModeToggle`이 드로어 쪽에서 덮는다) ·
  `tsc` 통과 · E2E 118개 통과 · 고아 키 0
- 실제 앱(dev, ko):
  - `/app` 1440폭 — 격자 `574px 480px`, 옆칸 드로어, `--tm-detail-w` 480
  - `/board` 1360폭 — `742px 480px`, `sticky` · `max-height 756px` · 넘침 0 · 폭 손잡이 있음
  - 456폭 — `is-full-screen`, 격자는 `no-detail`
  - 완료 → `완료로 표시했습니다` + 실행 취소로 복원 · ⋯ 여덟 항목 · 우선순위 팝오버 ·
    리스트 피커 · 작업 기록 · Escape로 닫힘
  - `링크 복사` → `/today?task=…`, 그 주소를 열면 그 작업이 열린다

### 6.8 그리고 전체 화면이 화면을 갖지 못하고 있었다

456폭에서 재보다가 찾았다: `.mobile-menu-button`이 z-index 220으로 전체 화면 드로어
(z-index 50) **위에** 12,12에 앉아 상세의 헤더 컨트롤을 덮고 있었다.

작업 모듈에는 이미 그 규칙이 있다 — `.tm-shell.detail-full .tm-menu-open { display: none }`,
§15.13("전체 화면 상세는 화면을 갖는다. 뒤의 것은 흐려지는 게 아니라 비켜난다").
레거시 셸에는 없었는데, **거기서 전체 화면 상세가 열린 적이 없었기 때문**이다. 이제
열린다. 선택자 하나를 같은 규칙에 붙였다.

### 6.9 남은 것

- **집중 페이지의 죽은 클릭**(§6.1). 이제 패널을 붙이면 끝나는 일이다 — **§7에서 붙였다**
- **`?task=`를 이 페이지들의 주소로**(§6.6). 링크·새로고침·뒤로 가기가 한 번에 풀린다
  — **§8에서 넣었다**
- **휴지통을 비우는 방법**(§6.4). 영구 삭제가 캘린더 한 곳에만 남았다 —
  **`TRASH_PERMANENT_DELETE_DESIGN.md`가 받아 갔고 다섯 단계로 끝났다**
- **`estimatedMinutes` · `categoryId`**(§6.4). 여전히 어느 화면에도 없다 —
  **이 줄은 틀렸다**, §6.4의 정정을 볼 것

---

## 7. §6.9의 첫 줄 — 집중 페이지가 클릭을 그린다 (2026-09-01)

### 7.1 고칠 것은 컴포넌트가 아니라 어느 페이지가 패널을 그리느냐였다 [실측]

`FocusPage`는 4년 내내 맞게 동작하고 있었다. 큐의 모든 행이
`onClick={() => onOpenTask(task.id)}`이고([FocusPage.tsx:207]), `AppPages`가 거기에
`planner.selectTask`를 넘긴다. 선택은 **정말로 일어났다.** 다만 `renderTaskDetail()`의
호출처가 오늘과 매트릭스 둘뿐이어서, 그 선택을 그리는 것이 없었다.

그래서 이 변경에는 컴포넌트가 없다. 집중 페이지를 오늘·매트릭스와 **같은 그리드**에
넣고 그 안에서 `renderTaskDetail()`을 부르는 것이 전부다.

```
- <FocusPage … />
+ <section className={pageGridClass("foc-grid")} style={gridStyle}>
+   <FocusPage … />
+   {renderTaskDetail()}
+ </section>
```

다섯 번째 표현을 만들지 않았다는 것이 요점이다. §6.2가 오늘 페이지에서 지운 것이 바로
그것이고, 집중에 스크림 하나를 새로 그렸다면 같은 자리로 돌아갔을 것이다. 네 표현
(옆칸·오버레이·시트·전체 화면)이 폭에 따라 그대로 나온다.

### 7.2 `no-detail`의 980px 상한만 사양이 다르다

`.page-grid.no-detail`은 아무것도 열리지 않았을 때 페이지를 980px로 좁혀 가운데
세운다. 집중 페이지의 좌우 여백은 캘린더·오늘과 맞추기로 되어 있으므로(§05-spaces의
`.foc-page` 주석) 이 상한은 원하지 않는 것이다. 오늘이 `.tdy-grid`로 빠져나가는 것과
같은 방법으로 `.foc-grid`를 만들었다 — 한 줄짜리 오버라이드다.

실측(1280폭): 닫힌 상태 `page-grid no-detail foc-grid`, 한 트랙 1166px, 왼쪽 82px —
변경 전과 같은 수. 열린 상태 `page-grid foc-grid`, `662px 480px`, 드로어는
`is-inline-drawer`에 `position: sticky`.

`.foc-page`에 `min-width: 0`도 함께 붙였다. 그리드 아이템의 하한은 콘텐츠이므로,
없으면 페이지가 옆칸을 내주는 대신 넘쳐 나간다.

### 7.3 지나가다 찾은 것 — 1240px 아래에서 큐는 눌러지지 않았다 [실측]

E2E를 세 뷰포트에 돌리자 태블릿(800)과 모바일(393)에서 클릭이 **가로채였다**:
`.foc-summary`가 큐의 행 위에 앉아 있었다. 스태시하고 다시 재 봤다 — **변경 전에도
똑같았다.** 이 문서가 만든 것이 아니다.

원인은 `@media (max-width: 1240px)`의 두 줄이다:

```css
.foc-layout {
  grid-template-columns: minmax(0, 1fr);
  grid-template-areas: none;   /* ← 영역만 지웠다 */
}
```

`grid-template-areas: none`은 영역을 지우지만, 그 영역을 쓰던 아이템들의
`grid-area: queue`는 그대로 남는다. 남은 `grid-area: queue`는 이제 **`queue`라는 이름의
선**으로 읽히고, 그런 선도 없으므로 브라우저가 암시적 트랙을 만들어 낸다. 800폭에서
계산된 트랙이 `314px 0px 340px`였고, 큐 카드와 요약 카드가 **둘 다 x=428에서**
시작했다. 나중에 그려지는 요약이 큐로 가는 클릭을 전부 받았다.

넷을 `grid-area: auto`로 돌려보내는 것이 실제로 쌓는 일이다. 실측(800폭): 큐 130 ·
타이머 402 · 요약 738 · 인사이트 1005, 넷 다 x=82 w=686, 클릭은 `.foc-task-main`에
닿는다.

이걸 이 단계에서 고친 이유는 범위가 아니라 **주장** 때문이다. 이 단계의 주장은 "집중
페이지에서 작업을 누르면 상세가 열린다"인데, 1240px 아래에서는 애초에 누를 수가
없었다. 고치지 않으면 세 뷰포트 중 하나에서만 참인 주장이 된다.

### 7.4 시험은 E2E다

`e2e/focusDetail.spec.ts` — 버그가 컴포넌트에 있던 적이 없기 때문이다. `FocusPage`를
jsdom에서 마운트하면 프롭을 부르는 것까지는 언제나 통과했고, 통과하는 동안 화면에는
아무 일도 일어나지 않았다. **어느 페이지가 패널을 그리는가**는 조립된 앱만 답한다.

두 시험이고 하나는 세 뷰포트 전부에서 돈다:

- `clicking a task opens it` — 세 뷰포트. 폭마다 표현이 다르므로 주장은 "옆칸이
  생긴다"가 아니라 **"무언가 열린다"**다. 누르기 전 `.tm-drawer`가 0개라는 것을 먼저
  본다 — 매트릭스에서 열어 둔 상세가 남아 있으면 집중이 아무 일도 안 하고도 통과한다
- `closing it gives the page its width back` — 1280 이상만(§15.17). 열었을 때 페이지가
  실제로 좁아지는지까지 보는 이유는, 안 좁아지면 두 폭이 같아져서 마지막 줄이 **틀린
  이유로** 통과하기 때문이다. 닫은 뒤에는 "더 넓어졌다"가 아니라 **같은 수**를 요구한다
  — `.foc-grid` 오버라이드가 빠지면 980과 1166 사이 어딘가에 앉고, 그것도 그럴듯해
  보인다

제목을 `toHaveValue`로 읽는다. 드로어의 제목은 아직 `<input>`이고
(`TICKTICK_DETAIL_ANATOMY_DESIGN.md` §3), `toHaveText`는 빈 문자열을 받는다.

### 7.5 검증

- 유닛 2,161개 통과 · `tsc` 통과 · E2E 신규 6개(2개는 폭 조건으로 skip) 포함 전체 통과
- 실제 앱(dev, en): 1280 → 옆칸 · 1100 → 오버레이(fixed) · 900 → 우측 시트 · 420 →
  전체 화면(`app-shell detail-full`). 네 폭 모두 가로 스크롤 없음
- 콘솔 오류는 업데이터의 CORS 하나뿐 — 브라우저 dev에서만 나는 기존 잡음이다

### 7.6 남은 것

§6.9의 나머지 셋. 다음은 둘째 줄 — **`?task=`를 이 페이지들의 주소로**(§6.6). 집중이
이제 같은 그리드에 들어왔으므로 세 페이지가 한 번에 같이 고쳐진다. §8이 그 일이다.

---

## 8. §6.9의 둘째 줄 — 열린 작업이 주소가 된다 (2026-09-01)

### 8.1 진짜 문제는 링크가 아니라 출처가 둘이었다는 것 [실측]

§6.6은 이것을 "링크 하나가 정직하지 않다"로 적었지만, 부정직한 링크는 증상이었다.
원인은 **어느 작업이 열려 있는가**의 답을 이 세 페이지가 `usePlannerData`의
`selectedTaskId`에서 읽고 있었다는 것이다. 모듈은 §5.15부터 URL에서 읽는다. 같은
질문에 두 개의 답이 있었고, 그중 하나만 새로고침·뒤로 가기·링크가 볼 수 있었다.

그래서 이 단계는 링크를 고치는 일이 아니라 **답을 하나로 줄이는 일**이다. 셋이
한꺼번에 풀리는 것은 그 결과다.

### 8.2 주소가 답한다

`pageRoute.ts`는 "페이지마다 경로 하나, 정확히 일치, 매개변수 없음"이라고 적어
두었다. 매개변수 하나가 생겼고, 그 하나가 링크·새로고침·뒤로 가기가 나를 이유 전부다.

```ts
const PAGES_WITH_DETAIL = new Set<PageId>(["today", "board", "focus"]);
export function pageUrlFor(page: PageId, taskId = ""): string
export function taskIdForPageUrl(url: string): string
```

캘린더와 설정은 목록에 없다. 둘 다 상세를 그리지 않으므로 `?task=`는 페이지가 지킬 수
없는 약속이 된다 — **쓰지도 읽지도 않는다.**

`taskIdForPageUrl`이 `pageForPath` 대신 `ROUTE_TO_PAGE`를 직접 보는 이유가 하나 더
있다. `pageForPath`는 모르는 주소에 `today`라고 답하고, 모듈의 주소는 **자기
`?task=`를 갖고 있다**. 여기서까지 답하면 주소 하나에 주인이 둘이 되고, 그것이 이
변경이 없애려는 바로 그 모양이다.

`App`은 이제 이렇게 읽는다:

```ts
const openedTaskId = taskIdForPageUrl(currentUrl);
const openedTask = openedTaskId ? planner.tasks.find((t) => t.id === openedTaskId) ?? null : null;
```

전체 작업에서 찾는다 — 모듈이 §5.30에서 대는 것과 같은 이유다. 링크는 그것이 지목한
작업을 열어야 하고, 도착한 페이지가 그 작업을 목록에 넣지 않았더라도 그렇다.

### 8.3 지워진 것이 남은 것보다 많다

`selectedTaskId`가 죽으면서 그 주위의 배선이 같이 나갔다. `usePlannerData`에서 14줄:
상태 하나, 파생 하나, 그리고 **`setSelectedTaskId` 호출 여섯**이다. 여섯을 하나씩
대신할 필요가 없었던 것이 이 모델의 요점이다.

| 있던 것 | 대신하는 것 |
|---|---|
| `deleteTask` · `giveUpTask` · `deleteTasks`의 선택 해제 | 없음. 없는 id는 아무것도 열지 않는다(§5.30) |
| `importData` · `resetData`의 선택 해제 | 리셋만 `replace`로 주소를 씻는다 — 뒤로 갈 자리가 아니다 |
| `duplicateTask`의 `setSelectedTaskId(plan.rootId)` | `App`의 `onDuplicate`가 사본을 연다. 상세가 있는 자리에서만 |

캘린더의 `onClearTaskSelection`도 나갔다. 프롭 하나와 호출 다섯인데, **읽는 쪽이
없었다.** `viewTaskInCalendar`가 작업을 고르고 캘린더로 보내면 캘린더는 그것을
그리지 않고 자기 핸들러 다섯 곳에서 지우기만 했다. 보이지 않는 것을 지우고 있었던
것이다. 지금은 고르지 않고 보내고, 지울 것도 없다.

### 8.4 여닫는 것은 양쪽 다 push다

모듈의 규칙 그대로다(§16.28 Gate 5): 한 번의 클릭이 연 것을 한 번의 Back이 닫는다.

```ts
function openTaskOnPage(taskId: string) { navigateUrl(pageUrlFor(activePage, taskId)); }
function closeTaskOnPage() {
  if (!openedTaskId) return;
  navigateUrl(pageUrlFor(activePage));
}
```

`closeTaskOnPage`의 가드는 Escape 때문에 있다. Escape는 아무것도 열려 있지 않을 때도
눌리고, 그때마다 같은 주소를 한 항목씩 쌓으면 Back이 자기 자리에서 제자리걸음을 한다.
E2E가 그 방향으로 시험한다 — 두 번째 Escape 뒤의 Back이 **작업이 열린 주소로
돌아오는지**(가드가 있음) 아니면 중복된 `/board`에 써버리는지(가드가 없음).

키다운 이펙트의 의존성에 `activePage`와 `openedTaskId`를 더했다. Escape가 이제
navigate이므로 **키를 누른 순간에 열려 있던 주소**를 써야 하는데, `planner`는 URL만
바뀔 때 바뀌지 않는다.

### 8.5 링크는 이제 읽는 사람이 보고 있는 페이지다

```ts
linkFor: (taskId) => `${origin}${pageUrlFor(activePage, taskId)}`
```

매트릭스에서 복사하면 매트릭스가, 그 작업이 열린 채로 돌아온다. §6.6이 `/today?task=`로
우회한 것은 이 주소가 존재하지 않았기 때문이고, 이제 존재한다. `taskLink.ts`의
`taskLinkFor`는 모듈만 쓰므로 `App`의 import만 지웠다.

### 8.6 시험

유닛 6개(`pageRoute.test.ts`) — 왕복, 상세를 그리지 않는 페이지의 거부, 모듈 주소에
답하지 않기, 아무것도 열지 않았을 때, 이스케이프가 필요한 id.

E2E 6개(`e2e/pageTaskUrl.spec.ts`) — 유닛이 덮을 수 없는 것이 이 변경의 이유 전부이기
때문이다. 셋 다 브라우저의 것이다: 새로고침이 열려 있던 것을 다시 열고, Back이 닫고,
복사한 링크가 같은 페이지에 같은 작업을 연다.

- `opening writes it, closing takes it away`
- `Escape closes it, and only when something is open`
- `a reload reopens it` — 이 변경 전에는 빈 매트릭스에 내려앉던 자리다
- `one Back closes what one click opened`
- `the copied link is this page, not the Module's`
- `leaving the page leaves the Task behind` — 캘린더가 `?task=`를 물려받지 않는다.
  모바일은 제외했다. 전체 화면 상세가 화면을 가지므로(§15.13) 누를 레일이 없고,
  먼저 닫으면 시험할 것이 남지 않는다

### 8.7 검증

- 유닛 2,167개 통과(신규 6) · `tsc` 통과 · E2E 신규 6개 × 세 뷰포트에서 17 통과 1 skip
- 실제 앱(dev): 집중에서 작업 클릭 → `/focus?task=task-0f8be…`, 새로고침 → 같은 작업이
  다시 열림, Back → `/focus`에 드로어 없음
- 폭을 바꿔도 주소는 그대로다(§15.9) — 800에서 `is-right-sheet`, 1280에서
  `is-inline-drawer`, `?task=`는 양쪽 다 같은 값

> 실측 메모: 브라우저 창(Browser pane)으로는 Escape를 시험할 수 없었다. 진짜 키가
> 페이지에 **도달하지 않는다** — window의 capture 리스너까지 걸어 두고 눌러도 아무것도
> 보이지 않았고, `dispatchEvent`로 만든 것만 핸들러에 닿았다. 창의 문제이지 앱의
> 문제가 아니며, Playwright는 진짜 키를 보내므로 §8.6의 시험이 이 자리를 덮는다.

### 8.8 남은 것

§6.9의 나머지 둘 — **휴지통을 비우는 방법**(§6.4)과 **`estimatedMinutes` ·
`categoryId`**(§6.4). 둘 다 이 문서가 만든 것이 아니라 지나가며 세어 둔 것이다.

첫째는 `TRASH_PERMANENT_DELETE_DESIGN.md`가 받아 갔고 끝났다. 둘째는 §6.4의 정정이
말하는 대로 처음 세어 둔 문장 자체가 틀렸고, **설계를 두지 않기로 했다** — 세어 둔
채로 남는다.
