# 휴지통 — 버린 것을 되찾는 곳과, 정말로 지우는 곳

> 상태: **설계 · 미구현** · 2026-09-01
> (`TASK_DETAIL_PANEL_MERGE_DESIGN.md` §6.4가 "휴지통을 비우는 방법이 없다"를 세어
> 두고 그 문서의 일이 아니라고 적었다. 여기가 그 일이다.)
> 대상: `/trash` 스코프 화면 · 휴지통에 있는 작업의 상세 · 액션 레지스트리의 danger 그룹

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 사용자가 준 참조 앱 스크린샷 1장 — 휴지통 스코프가 열리고 그 안의 작업 하나가 상세에 열린 상태 | [관찰] |
| **실측** | `domain/tasks/`의 상태 술어·뮤테이션·액션 레지스트리, `TasksModule`의 헤더, `ListManager` | [실측] |
| **인용** | `TICKTICK_STYLE_…_v16_E2E_IMPLEMENTATION_PLAN.md` §1.12 · `TICKTICK_NAV_SHELL_PHASE0_AUDIT.md` | [인용] |
| **추론** | 위 셋으로 메운 것 | [추론] |

스크린샷은 **한 장**이다. 그래서 아래에서 [관찰]로 적는 것은 그 한 장에 실제로 보이는
것뿐이고, 마우스를 올려야 나오는 것·우클릭으로 나오는 것은 **보이지 않았다**고 적는다.
그 구분이 이 문서에서 제일 중요한 줄이다 — §3.1이 결정을 그 위에 세운다.

---

## 1. 참조 앱의 휴지통 [관찰]

### 1.1 사이드바

맨 아래 시스템 구획이 **`Completed` · `Trash` 둘**이다. `Trash`가 현재 위치라 행 전체가
밝은 배경으로 칠해져 있다. **둘 다 개수가 없다** — 같은 사이드바에서 `Today 3` ·
`Inbox 3` · `Fidelity Audit 1` · `학교 15`는 개수를 달고 있으므로, 없는 것은 그리지
않기로 한 결과지 자리가 없어서가 아니다.

`Won't Do` 행은 **없다.** 우리 `TICKTICK_NAV_SHELL_PHASE0_AUDIT.md` §P0-4b-3은 하단
구획을 "완료 · 안 함 · 휴지통 셋"으로 적었는데 [인용], 이 스크린샷은 둘이다. 우리
사이드바도 지금은 둘이고(`안 함`은 `완료`에 합쳐졌다 — `scopeQuery.ts:198`), **이미
스크린샷과 같다.** 여기서 바꿀 것이 없다.

### 1.2 목록 머리

`☰ Trash` 제목 하나, 그리고 **오른쪽 끝에 아이콘 버튼 하나**. 개수도, 뷰 선택기도,
`+` 추가 줄도 없다.

그 아이콘 버튼이 무엇인지는 라벨이 없어 [추론]이다 — 휴지통 모양이고, 이 화면에서
제목 옆에 놓을 만한 전역 행동은 **비우기** 말고 없다.

### 1.3 행

`체크박스 · 제목 · (오른쪽) 리스트 이름 · 아이콘 · 날짜`. 우리 행과 같은 뼈대다.

버려질 때의 상태를 **그대로 들고 있다**는 것이 이 화면의 성격을 말한다:

| 보이는 것 | 무엇 |
|---|---|
| ☐ 빈 체크박스 · 밝은 제목 | 아직 할 일이던 것을 버렸다 |
| ☑ 체크된 체크박스 · **흐린** 제목 | 끝낸 것을 버렸다 |
| ⊠ ✕ 표시된 상자 · 흐린 제목 | 안 하기로 한 것을 버렸다 |

즉 **휴지통은 네 번째 상태가 아니라 다른 축이다.** 완료·안 함과 나란히 놓인 칸이 아니라,
그 셋 중 무엇이었든 그 위에 덮이는 표시다. 우리 `taskState.ts`가 이미 그렇게 적어 두었다
[실측]: `isTaskAlive = !isTrashed && !isWontDo`이고 `isTrashed`는 `deletedAt` 하나만 본다.

나머지 [관찰]:

- 왼쪽 가장자리에 **색 막대**가 있는 행과 없는 행이 있다
- 날짜는 기한이 지난 것이 빨강, 아닌 것이 회색 — **휴지통 안에서도 그대로다**
- URL이 제목인 행은 **링크로 그려진다**(파랑 · 밑줄)
- 하위 작업이 있는 행 하나에 `>` 펼침 삼각형이 있다
- 반복 아이콘 · 노트 아이콘이 리스트 이름 옆에 붙는다

**행 위에 `⋯`는 보이지 않는다.** 호버·우클릭이 무엇을 여는지는 이 스크린샷으로 알 수
없다.

### 1.4 상세 — 여기가 답이다

휴지통의 작업 하나가 오른쪽 상세에 열려 있고, **푸터가 다른 화면과 다르다**:

```
🗑 Restore                                              🗑
```

왼쪽에 라벨 있는 `Restore`, 오른쪽 끝에 아이콘 하나. 평소 푸터에 있던 것 —
`⇥ 리스트 · A · 💬 · ⋯` (`TICKTICK_DETAIL_ANATOMY_DESIGN.md` §1.3이 잰 것) [인용] —
이 **전부 없다.**

그러니까 참조 앱의 답은 이렇다: **휴지통에 있는 작업의 상세는 두 가지 행동만 하는
화면이 된다 — 되살리거나, 정말로 지우거나.** 나머지는 지금 물을 일이 아니라서 치웠다.

상세 위쪽은 평소대로다: 체크박스 · `📅 Aug 27, 5d overdue`(빨강) · 깃발 · 제목 ·
본문. 읽기 전용인지는 이 한 장으로 알 수 없다.

---

## 2. 우리 것은 어디까지 있나 [실측]

### 2.1 모델은 전부 있다

| 있는 것 | 어디 |
|---|---|
| soft delete | `mutations.ts:29` `trashTask` — `deletedAt`을 쓴다 |
| 되살리기 | `mutations.ts:100` `restoreTask` — `deletedAt`을 비운다 |
| 되돌리기 | 둘 다 `TaskMutation`이라 토스트의 실행 취소가 그냥 붙는다 |
| 스코프 | `scopeRegistry.ts:205` — `/trash`, 목록 뷰만, 생성 불가, `countMode: "trash"` |
| 질의 | `scopeQuery.ts:196` — `Boolean(task.deletedAt)` 한 줄 |
| 격리 | 스마트 리스트·캘린더·검색에서 빠진다(Gate 8-2) [인용] |
| 상태 술어 | `taskState.ts:57` `isTrashed` |

### 2.2 없는 것은 정확히 하나

**작업의 hard delete.** 액션 레지스트리에 `restore`(status 그룹)와 `trash`(danger
그룹)는 있고 `deleteForever`가 **없다**(`actions.ts:151`).

계획서 §1.12는 이미 적어 두었다 [인용]:

> Trash ↓ restore / permanent delete

즉 설계가 빠진 것이 아니라 **구현이 빠졌다.** 이 문서가 새로 정하는 것은 "무엇을"이
아니라 "어디에 · 몇 번 묻고"다.

### 2.3 선례가 리포 안에 있다

`ListManager.tsx:10`이 이렇게 적어 두었다 [실측]:

> 영구 삭제는 이 앱에서 유일하게 hard-delete하는 자리이므로 **유일하게 두 번 묻는다.**
> 확인 문장은 함께 사라질 작업 개수를 말하는데, 그것이 사용자에게 필요한 사실이고
> "삭제"라는 낱말이 가리는 사실이기 때문이다.

이 문장은 이제 **틀리게 된다** — 두 번째 hard-delete 자리가 생긴다. 그 문장을 고치는
것도 이 작업의 일부다.

그리고 `ListManager`의 확인 방식은 모달이 아니라 **제자리 확인**이다: `영구 삭제`를
누르면 그 행이 확인 줄로 바뀐다(`confirming === list.id`). 새 다이얼로그를 발명하지
않아도 되는 모양이 이미 있다.

### 2.4 헤더에 자리가 있다

`TasksModule.tsx:928`의 `.tm-scope-actions`가 리스트 스코프의 `보관`·`휴지통으로`를
제목 오른쪽에 그린다 [실측]. §1.2의 아이콘 버튼이 앉을 자리가 **이미 있고**, 지금은
`scope.kind === "list"`일 때만 그려진다.

---

## 3. 결정

### 3.1 스크린샷이 고른 답 — 행 메뉴가 아니라 상세 푸터다

사전에 "행 `⋯` + 헤더 비우기"로 정했는데, §1.4가 다른 것을 보여 준다. 참조 앱에서
**하나를 영구히 지우는 자리는 상세의 푸터**이고, 행 위의 `⋯`는 이 스크린샷에 **없다**.

그래서 이렇게 정한다. 셋 다 만들되 **하나의 레지스트리 항목에서** 나오게 한다:

| 자리 | 무엇 | 근거 |
|---|---|---|
| **상세 푸터** | 휴지통의 작업일 때만, 푸터가 `되살리기` + `영구 삭제`로 **바뀐다** | [관찰] §1.4 |
| **행 · 상세의 `⋯`** | danger 그룹에 `영구 삭제` 한 줄이 는다 | [추론] — 우리 앱은 행과 상세가 같은 레지스트리를 쓴다(`actions.ts`). 하나를 넣으면 둘 다 생기고, 넣지 **않는** 쪽이 오히려 특례가 된다 |
| **헤더** | 휴지통 스코프에 `휴지통 비우기` | [관찰] §1.2 + [추론] 라벨 |

행 메뉴를 참조 앱에서 못 봤다고 빼지 않는 이유는 §0의 구분 때문이다 — **없는 것을 본
게 아니라 보지 못했다.** 우리 구조에서는 넣는 쪽이 기본이고 빼는 쪽이 결정이다.

### 3.2 상세 푸터가 바뀐다는 것의 뜻

이것이 이 문서에서 제일 큰 변경이고, §1.4가 요구하는 것이다. 휴지통의 작업을 열면:

- 평소 푸터(`리스트 피커 · A · 💬 · ⋯`)가 **사라지고**
- `되살리기`(라벨) + `영구 삭제`(아이콘)만 남는다

왜 리스트 피커가 사라지는가에 대한 답은 [추론]이지만 단단하다: **버린 작업을 다른
리스트로 옮기는 것은 대답할 질문이 아니다.** 되살린 다음에 할 일이고, 되살리기 전에
그것을 물으면 사용자가 버린 것을 편집하게 된다.

`⋯`도 사라진다. 레지스트리는 이미 스코프별로 가용성을 계산하므로(`availabilityOf`),
휴지통에서 쓸 수 없는 항목은 이미 걸러진다 — 남는 것이 `되살리기`와 `영구 삭제` 둘뿐이면
그 둘을 메뉴 뒤에 숨길 이유가 없다. **메뉴가 두 줄짜리가 되는 대신 푸터가 두 버튼이
된다.**

### 3.3 몇 번 묻는가

`ListManager`의 규칙을 그대로 가져온다 — hard-delete는 두 번 묻는다.

| 행동 | 묻는 방식 | 문장이 말해야 하는 것 |
|---|---|---|
| **하나 영구 삭제** | 제자리 확인(§2.3의 모양) | 되돌릴 수 없다는 것 |
| **휴지통 비우기** | 제자리 확인 또는 모달 | **몇 개가 사라지는지** |

`비우기`가 개수를 말해야 하는 이유는 `ListManager`가 개수를 말하는 이유와 같다 —
"비우기"라는 낱말이 가리는 사실이 개수다. 휴지통에 30개가 있는지 3개가 있는지 모르고
누르는 버튼이면 안 된다.

**되돌리기(Undo)는 주지 않는다.** 토스트의 실행 취소는 `TaskMutation`의 undo patch로
되는데, hard-delete에는 되돌릴 patch가 없다 — 레코드가 없어진다. §9.35가 "Undo는
상태를 복원한다"고 적은 것과 정확히 맞지 않는 유일한 행동이고, 그래서 **먼저** 묻는다.

### 3.4 `confirmBeforeDelete` 설정은 여기에 적용되지 않는다

앱에는 `appSettings.confirmBeforeDelete`가 있고 `requestDeleteTask`가 그것을 본다
[실측: `App.tsx:1053`]. 영구 삭제는 **그 설정을 보지 않는다.**

이유는 그 설정이 답하는 질문이 다르기 때문이다. `confirmBeforeDelete`는 "휴지통으로
보내기 전에 물을까"이고, 그건 되돌릴 수 있는 행동이라 끄는 것이 합리적인 선택이다.
영구 삭제는 되돌릴 수 없고, **되돌릴 수 없는 것을 묻지 않게 하는 설정은 두지 않는다.**
`ListManager`도 이 설정을 보지 않는다.

### 3.5 자동 만료는 두지 않는다

참조 앱에는 며칠 뒤 자동 삭제가 있을 수 있지만 **이 스크린샷에는 그 근거가 없다** —
날짜별 구획도, "N일 후 삭제됩니다" 문구도 보이지 않는다.

근거 없이 짓지 않는다. 게다가 자동 만료는 앱이 사용자 데이터를 **묻지 않고** 지우는
유일한 자리가 되므로, 그것을 시작하려면 이 문서 한 장보다 더 큰 결정이 필요하다.

### 3.6 개수는 사이드바에 그리지 않는다

§1.1의 [관찰]대로다. 우리 레지스트리에는 `countMode: "trash"`가 있으므로 개수를 **셀 수
있지만**, 참조 앱은 그리지 않고 우리도 지금 그리지 않는다. 그대로 둔다.

다만 `비우기`의 확인 문장은 그 개수를 쓴다(§3.3) — 세는 것과 상시로 보여 주는 것은
다른 결정이다.

---

## 4. 단계

| 단계 | 내용 | 왜 이 순서인가 |
|---|---|---|
| **1** | 도메인 — `permanentlyDeleteTask` / `emptyTrash`. 순수 함수, UI 없음 | 되돌릴 수 없는 유일한 행동이므로, 무엇이 함께 사라지는지(하위 작업·체크 항목·태그 링크·리마인더)를 **화면 없이** 먼저 정하고 시험한다. `lifecycle.permanentlyDeleteList`가 같은 일을 리스트에 대해 이미 한다 |
| **2** | 레지스트리에 `deleteForever` — danger 그룹, 휴지통에서만 가용 | 행 메뉴와 상세 `⋯`가 한 번에 생긴다. 아직 푸터는 안 건드린다 |
| **3** | 상세 푸터 교체 — 휴지통일 때 `되살리기` + `영구 삭제` | §1.4. 2단계가 있어야 이 둘이 부를 것이 생긴다 |
| **4** | 헤더의 `휴지통 비우기` + 개수를 말하는 확인 | 하나씩 지우는 길이 먼저 있고 나서 전부 지우는 길이다 — 매트릭스에서 6b를 6c보다 앞에 둔 것과 같은 이유 |
| **5** | `ListManager.tsx:10`의 "유일하게"를 고친다 | 그 문장은 4단계가 끝나는 순간 틀린다 |

1단계와 2단계는 화면을 안 건드리므로 되돌리기 쉽다. 3단계부터 화면이 바뀐다.

---

## 5. 열린 질문

| | 질문 | 언제 |
|---|---|---|
| Q1 | 휴지통의 상세는 **읽기 전용**인가. 스크린샷으로는 판별 불가(§1.4). 편집을 허용하면 "버린 것을 고치는" 상태가 생기고, 막으면 제목의 오타 하나 고치려고 되살렸다 다시 버려야 한다 | 3단계 |
| Q2 | 하위 작업이 있는 작업을 영구 삭제하면 자식도 함께 가는가. `permanentlyDeleteList`는 자식 작업을 함께 지운다 [실측] — 같은 규칙이면 확인 문장이 개수를 말해야 한다 | 1단계 |
| Q3 | 부모는 휴지통에 없고 자식만 휴지통에 있을 수 있는가. `scopeQuery`가 `task.parentTaskId`가 있는 행을 스코프에서 빼므로(`:191`) **휴지통 목록에 자식은 안 보인다** [실측]. 그러면 자식만 버려진 경우 되살릴 방법이 없다 | 1단계 |
| Q4 | `비우기`의 확인은 제자리인가 모달인가. `ListManager`는 제자리, 리셋은 모달 | 4단계 |
| Q5 | 행의 `⋯`를 참조 앱이 정말로 안 주는가, 아니면 호버에만 나오는가 | 스크린샷 한 장 더 |

---

## 6. 이 문서가 확인하지 않은 것

- 호버·우클릭이 여는 것. §1.3의 마지막 줄이 그 한계다
- 휴지통 목록의 정렬. 스크린샷의 날짜가 `Aug 27 · Apr 22 · Apr 14 · Mar 21 · Mar 25 …`
  순이라 **버린 순서**로 보이지만, `deletedAt`이 화면에 없어서 확인할 수 없다.
  우리 것은 지금 스코프의 기본 정렬을 쓴다
- 다중 선택. 계획서가 P1로 미룬 것이고 [인용], 휴지통에서 여러 개를 골라 지우는 것은
  그것이 온 다음의 이야기다
