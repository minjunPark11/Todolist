# 휴지통 — 버린 것을 되찾는 곳과, 정말로 지우는 곳

> 상태: **설계 · 전 단계 구현됨** · 2026-09-02
> (`TASK_DETAIL_PANEL_MERGE_DESIGN.md` §6.4가 "휴지통을 비우는 방법이 없다"를 세어
> 두고 그 문서의 일이 아니라고 적었다. 여기가 그 일이다.)
> 대상: `/trash` 스코프 화면 · 휴지통에 있는 작업의 상세 · 액션 레지스트리의 danger 그룹

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 사용자가 준 참조 앱 스크린샷 1장 — 휴지통 스코프가 열리고 그 안의 작업 하나가 상세에 열린 상태 | [관찰] |
| **실측** | `domain/tasks/`의 상태 술어·뮤테이션·액션 레지스트리, `TasksModule`의 헤더, `ListManager` | [실측] |
| **인용** | `TICKTICK_STYLE_…_v16_E2E_IMPLEMENTATION_PLAN.md` §1.12 · `TICKTICK_NAV_SHELL_PHASE0_AUDIT.md` | [인용] |
| **추론** | 위 셋으로 메운 것 | [추론] |

스크린샷은 **한 장**이다. 그래서 아래에서 [관찰]로 적는 것은 그 한 장에 실제로 보이는
것뿐이고, 마우스를 올려야 나오는 것·우클릭으로 나오는 것은 **보이지 않았다**고 적는다.
그 구분이 이 문서에서 제일 중요한 줄이다 — §3.1이 결정을 그 위에 세운다.

---

## 1. 참조 앱의 휴지통 [관찰]

### 1.1 사이드바

맨 아래 시스템 구획이 **`Completed` · `Trash` 둘**이다. `Trash`가 현재 위치라 행 전체가
밝은 배경으로 칠해져 있다. **둘 다 개수가 없다** — 같은 사이드바에서 `Today 3` ·
`Inbox 3` · `Fidelity Audit 1` · `학교 15`는 개수를 달고 있으므로, 없는 것은 그리지
않기로 한 결과지 자리가 없어서가 아니다.

`Won't Do` 행은 **없다.** 우리 `TICKTICK_NAV_SHELL_PHASE0_AUDIT.md` §P0-4b-3은 하단
구획을 "완료 · 안 함 · 휴지통 셋"으로 적었는데 [인용], 이 스크린샷은 둘이다. 우리
사이드바도 지금은 둘이고(`안 함`은 `완료`에 합쳐졌다 — `scopeQuery.ts:198`), **이미
스크린샷과 같다.** 여기서 바꿀 것이 없다.

### 1.2 목록 머리

`☰ Trash` 제목 하나, 그리고 **오른쪽 끝에 아이콘 버튼 하나**. 개수도, 뷰 선택기도,
`+` 추가 줄도 없다.

그 아이콘 버튼이 무엇인지는 라벨이 없어 [추론]이다 — 휴지통 모양이고, 이 화면에서
제목 옆에 놓을 만한 전역 행동은 **비우기** 말고 없다.

### 1.3 행

`체크박스 · 제목 · (오른쪽) 리스트 이름 · 아이콘 · 날짜`. 우리 행과 같은 뼈대다.

버려질 때의 상태를 **그대로 들고 있다**는 것이 이 화면의 성격을 말한다:

| 보이는 것 | 무엇 |
|---|---|
| ☐ 빈 체크박스 · 밝은 제목 | 아직 할 일이던 것을 버렸다 |
| ☑ 체크된 체크박스 · **흐린** 제목 | 끝낸 것을 버렸다 |
| ⊠ ✕ 표시된 상자 · 흐린 제목 | 안 하기로 한 것을 버렸다 |

즉 **휴지통은 네 번째 상태가 아니라 다른 축이다.** 완료·안 함과 나란히 놓인 칸이 아니라,
그 셋 중 무엇이었든 그 위에 덮이는 표시다. 우리 `taskState.ts`가 이미 그렇게 적어 두었다
[실측]: `isTaskAlive = !isTrashed && !isWontDo`이고 `isTrashed`는 `deletedAt` 하나만 본다.

나머지 [관찰]:

- 왼쪽 가장자리에 **색 막대**가 있는 행과 없는 행이 있다
- 날짜는 기한이 지난 것이 빨강, 아닌 것이 회색 — **휴지통 안에서도 그대로다**
- URL이 제목인 행은 **링크로 그려진다**(파랑 · 밑줄)
- 하위 작업이 있는 행 하나에 `>` 펼침 삼각형이 있다
- 반복 아이콘 · 노트 아이콘이 리스트 이름 옆에 붙는다

**행 위에 `⋯`는 보이지 않는다.** 호버·우클릭이 무엇을 여는지는 이 스크린샷으로 알 수
없다.

### 1.4 상세 — 여기가 답이다

휴지통의 작업 하나가 오른쪽 상세에 열려 있고, **푸터가 다른 화면과 다르다**:

```
🗑 Restore                                              🗑
```

왼쪽에 라벨 있는 `Restore`, 오른쪽 끝에 아이콘 하나. 평소 푸터에 있던 것 —
`⇥ 리스트 · A · 💬 · ⋯` (`TICKTICK_DETAIL_ANATOMY_DESIGN.md` §1.3이 잰 것) [인용] —
이 **전부 없다.**

그러니까 참조 앱의 답은 이렇다: **휴지통에 있는 작업의 상세는 두 가지 행동만 하는
화면이 된다 — 되살리거나, 정말로 지우거나.** 나머지는 지금 물을 일이 아니라서 치웠다.

상세 위쪽은 평소대로다: 체크박스 · `📅 Aug 27, 5d overdue`(빨강) · 깃발 · 제목 ·
본문. 읽기 전용인지는 이 한 장으로 알 수 없다.

---

## 2. 우리 것은 어디까지 있나 [실측]

### 2.1 모델은 전부 있다

| 있는 것 | 어디 |
|---|---|
| soft delete | `mutations.ts:29` `trashTask` — `deletedAt`을 쓴다 |
| 되살리기 | `mutations.ts:100` `restoreTask` — `deletedAt`을 비운다 |
| 되돌리기 | 둘 다 `TaskMutation`이라 토스트의 실행 취소가 그냥 붙는다 |
| 스코프 | `scopeRegistry.ts:205` — `/trash`, 목록 뷰만, 생성 불가, `countMode: "trash"` |
| 질의 | `scopeQuery.ts:196` — `Boolean(task.deletedAt)` 한 줄 |
| 격리 | 스마트 리스트·캘린더·검색에서 빠진다(Gate 8-2) [인용] |
| 상태 술어 | `taskState.ts:57` `isTrashed` |

### 2.2 없는 것은 정확히 하나

**휴지통에서 나가는 문.** 액션 레지스트리에 `restore`(status 그룹)와 `trash`(danger
그룹)는 있고 `deleteForever`가 **없다**(`actions.ts:151`).

> **1단계에서 정정** — hard delete 자체는 없지 않았다. `usePlannerData.deleteTask`가
> 이미 그 일을 하고 있고, 캘린더의 `onDeleteTask` 하나가 그것을 부른다(§6.4가 적은
> 그대로다). 없던 것은 **가드와 표면**이지 삭제가 아니었다. §2.3이 그 삭제가 무엇을
> 흘리고 있었는지 잰다.

계획서 §1.12는 이미 적어 두었다 [인용]:

> Trash ↓ restore / permanent delete

즉 설계가 빠진 것이 아니라 **구현이 빠졌다.** 이 문서가 새로 정하는 것은 "무엇을"이
아니라 "어디에 · 몇 번 묻고"다.

### 2.3 [1단계 실측] 지우는 자리가 둘이었고, 둘 다 흘리고 있었다

같은 질문 — **작업 하나가 사라질 때 무엇이 함께 사라지는가** — 에 코드가 두 곳에서
따로 답하고 있었고, 두 답이 다르고 둘 다 불완전했다.

| 자리 | 가져간 것 | 남긴 것 |
|---|---|---|
| `deleteTask` (캘린더의 삭제) | 하위 작업 승격 · `subtasks` · `checkItems` · `reminders` | **`taskTags` 링크** |
| `lifecycle.permanentlyDeleteList` | 리스트의 작업들 · (호출부가 손으로) `checkItems` | **`subtasks` · `taskTags` · `reminders`**, 그리고 다른 리스트에 있는 자식이 **없어진 부모를 계속 가리켰다** |

그리고 되돌리기에 구멍이 하나 더 있었다. `restoreDeletedTask`는 작업 · 자식의 부모
관계 · `subtasks`만 되돌려 놓았다 — **`deleteTask`가 가져간 `checkItems`와
`reminders`는 되돌리지 않았다.** 즉 캘린더에서 작업을 지우고 실행 취소를 누르면
작업은 돌아오고 **체크리스트는 사라진 채로 돌아왔다.** 토스트는 성공한 것처럼 보였다.

이것이 1단계가 순수 함수 하나로 시작해야 했던 이유다. 화면을 먼저 만들었다면 세 번째
답이 생겼을 것이다.

### 2.4 선례가 리포 안에 있다

`ListManager.tsx:10`이 이렇게 적어 두었다 [실측]:

> 영구 삭제는 이 앱에서 유일하게 hard-delete하는 자리이므로 **유일하게 두 번 묻는다.**
> 확인 문장은 함께 사라질 작업 개수를 말하는데, 그것이 사용자에게 필요한 사실이고
> "삭제"라는 낱말이 가리는 사실이기 때문이다.

이 문장은 이제 **틀리게 된다** — 두 번째 hard-delete 자리가 생긴다. 그 문장을 고치는
것도 이 작업의 일부다.

그리고 `ListManager`의 확인 방식은 모달이 아니라 **제자리 확인**이다: `영구 삭제`를
누르면 그 행이 확인 줄로 바뀐다(`confirming === list.id`). 새 다이얼로그를 발명하지
않아도 되는 모양이 이미 있다.

### 2.5 헤더에 자리가 있다

`TasksModule.tsx:928`의 `.tm-scope-actions`가 리스트 스코프의 `보관`·`휴지통으로`를
제목 오른쪽에 그린다 [실측]. §1.2의 아이콘 버튼이 앉을 자리가 **이미 있고**, 지금은
`scope.kind === "list"`일 때만 그려진다.

---

## 3. 결정

### 3.1 스크린샷이 고른 답 — 행 메뉴가 아니라 상세 푸터다

사전에 "행 `⋯` + 헤더 비우기"로 정했는데, §1.4가 다른 것을 보여 준다. 참조 앱에서
**하나를 영구히 지우는 자리는 상세의 푸터**이고, 행 위의 `⋯`는 이 스크린샷에 **없다**.

그래서 이렇게 정한다. 셋 다 만들되 **하나의 레지스트리 항목에서** 나오게 한다:

| 자리 | 무엇 | 근거 |
|---|---|---|
| **상세 푸터** | 휴지통의 작업일 때만, 푸터가 `되살리기` + `영구 삭제`로 **바뀐다** | [관찰] §1.4 |
| **행 · 상세의 `⋯`** | danger 그룹에 `영구 삭제` 한 줄이 는다 | [추론] — 우리 앱은 행과 상세가 같은 레지스트리를 쓴다(`actions.ts`). 하나를 넣으면 둘 다 생기고, 넣지 **않는** 쪽이 오히려 특례가 된다 |
| **헤더** | 휴지통 스코프에 `휴지통 비우기` | [관찰] §1.2 + [추론] 라벨 |

행 메뉴를 참조 앱에서 못 봤다고 빼지 않는 이유는 §0의 구분 때문이다 — **없는 것을 본
게 아니라 보지 못했다.** 우리 구조에서는 넣는 쪽이 기본이고 빼는 쪽이 결정이다.

### 3.2 상세 푸터가 바뀐다는 것의 뜻

이것이 이 문서에서 제일 큰 변경이고, §1.4가 요구하는 것이다. 휴지통의 작업을 열면:

- 평소 푸터(`리스트 피커 · A · 💬 · ⋯`)가 **사라지고**
- `되살리기`(라벨) + `영구 삭제`(아이콘)만 남는다

왜 리스트 피커가 사라지는가에 대한 답은 [추론]이지만 단단하다: **버린 작업을 다른
리스트로 옮기는 것은 대답할 질문이 아니다.** 되살린 다음에 할 일이고, 되살리기 전에
그것을 물으면 사용자가 버린 것을 편집하게 된다.

`⋯`도 사라진다. 레지스트리는 이미 스코프별로 가용성을 계산하므로(`availabilityOf`),
휴지통에서 쓸 수 없는 항목은 이미 걸러진다 — 남는 것이 `되살리기`와 `영구 삭제` 둘뿐이면
그 둘을 메뉴 뒤에 숨길 이유가 없다. **메뉴가 두 줄짜리가 되는 대신 푸터가 두 버튼이
된다.**

### 3.3 몇 번 묻는가

`ListManager`의 규칙을 그대로 가져온다 — hard-delete는 두 번 묻는다.

| 행동 | 묻는 방식 | 문장이 말해야 하는 것 |
|---|---|---|
| **하나 영구 삭제** | 제자리 확인(§2.4의 모양) | 되돌릴 수 없다는 것 |
| **휴지통 비우기** | 제자리 확인 또는 모달 | **몇 개가 사라지는지** |

`비우기`가 개수를 말해야 하는 이유는 `ListManager`가 개수를 말하는 이유와 같다 —
"비우기"라는 낱말이 가리는 사실이 개수다. 휴지통에 30개가 있는지 3개가 있는지 모르고
누르는 버튼이면 안 된다.

**되돌리기(Undo)는 주지 않는다.** 토스트의 실행 취소는 `TaskMutation`의 undo patch로
되는데, hard-delete에는 되돌릴 patch가 없다 — 레코드가 없어진다. §9.35가 "Undo는
상태를 복원한다"고 적은 것과 정확히 맞지 않는 유일한 행동이고, 그래서 **먼저** 묻는다.

### 3.4 `confirmBeforeDelete` 설정은 여기에 적용되지 않는다

앱에는 `appSettings.confirmBeforeDelete`가 있고 `requestDeleteTask`가 그것을 본다
[실측: `App.tsx`의 `requestDeleteTask`]. 영구 삭제는 **그 설정을 보지 않는다.**

이유는 그 설정이 답하는 질문이 다르기 때문이다. `confirmBeforeDelete`는 "휴지통으로
보내기 전에 물을까"이고, 그건 되돌릴 수 있는 행동이라 끄는 것이 합리적인 선택이다.
영구 삭제는 되돌릴 수 없고, **되돌릴 수 없는 것을 묻지 않게 하는 설정은 두지 않는다.**
`ListManager`도 이 설정을 보지 않는다.

### 3.5 자동 만료는 두지 않는다

참조 앱에는 며칠 뒤 자동 삭제가 있을 수 있지만 **이 스크린샷에는 그 근거가 없다** —
날짜별 구획도, "N일 후 삭제됩니다" 문구도 보이지 않는다.

근거 없이 짓지 않는다. 게다가 자동 만료는 앱이 사용자 데이터를 **묻지 않고** 지우는
유일한 자리가 되므로, 그것을 시작하려면 이 문서 한 장보다 더 큰 결정이 필요하다.

### 3.6 개수는 사이드바에 그리지 않는다

§1.1의 [관찰]대로다. 우리 레지스트리에는 `countMode: "trash"`가 있으므로 개수를 **셀 수
있지만**, 참조 앱은 그리지 않고 우리도 지금 그리지 않는다. 그대로 둔다.

다만 `비우기`의 확인 문장은 그 개수를 쓴다(§3.3) — 세는 것과 상시로 보여 주는 것은
다른 결정이다.

---

## 4. 단계

| 단계 | 내용 | 왜 이 순서인가 |
|---|---|---|
| **1** | 도메인 — `permanentlyDeleteTask` / `emptyTrash`. 순수 함수, UI 없음 | **완료**(§7). 되돌릴 수 없는 유일한 행동이므로, 무엇이 함께 사라지는지를 **화면 없이** 먼저 정하고 시험한다 |
| **2** | 레지스트리에 `deleteForever` — danger 그룹, 휴지통에서만 가용 | **완료**(§8). 행 메뉴와 상세 `⋯`가 한 번에 생긴다. 아직 푸터는 안 건드린다 |
| **3** | 상세 푸터 교체 — 휴지통일 때 `되살리기` + `영구 삭제` | **완료**(§9). §1.4. 2단계가 있어야 이 둘이 부를 것이 생긴다 |
| **4** | 헤더의 `휴지통 비우기` + 개수를 말하는 확인 | **완료**(§10) |
| **5** | `ListManager.tsx:10`의 "유일하게"를 고친다 | **완료**(§10.4) |

1단계와 2단계는 화면을 안 건드리므로 되돌리기 쉽다. 3단계부터 화면이 바뀐다.

---

## 5. 열린 질문

| | 질문 | 언제 |
|---|---|---|
| ~~Q1~~ | ~~휴지통의 상세는 읽기 전용인가~~ — **닫혔다**(§14, 사용자 확정). **상태만 얼린다.** 완료·일정·우선순위·체크리스트 전환은 잠기고 제목과 본문은 그대로 |  |
| ~~Q2~~ | ~~하위 작업이 있는 작업을 영구 삭제하면 자식도 함께 가는가~~ — **닫혔다**(§7.2). 자식은 **가지 않는다.** 최상위로 승격된다 |  |
| ~~Q3~~ | ~~부모는 휴지통에 없고 자식만 휴지통에 있을 수 있는가~~ — **닫혔다**(§13). 있을 수 있었고, 그 행은 **어디에도 안 보이는데 `비우기`가 지웠다**. 휴지통만 예외로 자식을 보여준다 |  |
| ~~Q4~~ | ~~`비우기`의 확인은 제자리인가 모달인가~~ — **닫혔다**(§10.2). 모달. 제자리로 바꿀 행이 없다 |  |
| ~~Q5~~ | ~~행의 `⋯`를 참조가 정말 안 주는가~~ — **닫혔다**(§15). 캡처가 필요 없어졌다: 우리 §14가 이미 답을 갖고 있었고, 그 메뉴는 §14를 우회하는 **뒷문**이었다 |  |

---

## 6. 이 문서가 확인하지 않은 것

- 호버·우클릭이 여는 것. §1.3의 마지막 줄이 그 한계다
- 휴지통 목록의 정렬. 스크린샷의 날짜가 `Aug 27 · Apr 22 · Apr 14 · Mar 21 · Mar 25 …`
  순이라 **버린 순서**로 보이지만, `deletedAt`이 화면에 없어서 확인할 수 없다.
  우리 것은 지금 스코프의 기본 정렬을 쓴다
- 다중 선택. 계획서가 P1로 미룬 것이고 [인용], 휴지통에서 여러 개를 골라 지우는 것은
  그것이 온 다음의 이야기다

---

## 7. 1단계 구현 기록 (2026-09-01)

### 7.1 쓴 것

`domain/tasks/trash.ts` — 순수 함수 넷.

| | 무엇 |
|---|---|
| `removeTasksForever(rows, ids)` | 규칙 그 자체. 나머지 셋이 이것을 부른다 |
| `permanentlyDeleteTask(rows, id)` | `deletedAt` 가드 + 하나 제거. `done`을 돌려준다 |
| `emptyTrash(rows)` | 버려진 것 전부 + `removed` 개수 |
| `trashedTaskIds(tasks)` | §12.13의 한 필드를 읽는 자리 |

`TaskRows`는 스토어가 아니라 **스토어의 조각**이다(`tasks · subtasks · checkItems ·
taskTags · reminders`). 이 파일이 리스트나 설정을 건드릴 수 없다는 것이 타입에 적혀
있는 셈이고, "되돌릴 수 없는 행동"에는 그 정도 값을 한다.

### 7.2 Q2의 답 — 자식은 가지 않는다

`deleteTask`의 주석이 이미 답을 적어 두고 있었다: **자식은 최상위로 승격된다. 그 일은
여전히 진짜 일이기 때문이다.** 체크 항목이나 리마인더는 자기 작업 없이는 뜻이 없지만
자식 작업은 뜻이 있다.

그래서 이것은 재귀 수집이 아니라 **생존자에 대한 두 번째 패스**로 썼다. 코드 모양이
규칙 자체다 — 제거된 작업 **아래의 것은 아무것도 함께 제거되지 않는다.**

결과로 §3.3의 확인 문장은 자식 개수를 말할 필요가 없다. `비우기`만 개수를 말한다.

### 7.3 흘리던 것 셋을 막았다

§2.3의 표대로 고쳤다:

- `deleteTask` → `removeTasksForever`. `taskTags` 링크가 이제 함께 간다
- `permanentlyDeleteList` → 같은 함수. `subtasks · taskTags · reminders`가 함께 가고,
  다른 리스트에 있던 자식이 **승격된다**(전에는 없어진 부모를 계속 가리켰다).
  손으로 부르던 `pruneOrphanCheckItems`는 호출부가 없어져 import에서 빠졌다 — 함수와
  그 시험은 남겼다
- `restoreDeletedTask` → `checkItems · taskTags · reminders`를 인자로 받는다.
  **삭제가 가져가는 모든 것이 시그니처에 있으므로**, 둘이 다시 어긋나려면 시그니처가
  먼저 그렇다고 말해야 한다

각 목록을 "다른 작업의 것 + 이 작업이 갖고 있던 것"으로 다시 만든다. 그냥 이어 붙이면
같은 실행 취소를 두 번 눌렀을 때 행이 두 배가 되고, 토스트는 두 번 눌릴 수 있다.

### 7.4 검증

- 유닛 **2,185개 통과**(신규 18: 도메인 11 · 스토어 7) · `tsc` 통과
- 스토어 쪽 시험이 따로 있는 이유는 §2.3의 되돌리기 구멍 때문이다. 순수 함수로는 볼 수
  없는 주장이고, **"삭제가 가져간 것을 실행 취소가 전부 돌려놓는다"** 가 이 파일의 이유다
- 시험을 쓰다가 하나 배웠다: `addSubtask`는 레거시 `Subtask` 행이 아니라 **자식 작업**을
  쓴다. 옛 컬렉션에는 쓰는 곳이 남아 있지 않다

### 7.5 화면은 아직 없다

`permanentlyDeleteTask`와 `emptyTrash`는 플래너가 내보내지만 **부르는 화면이 없다.**
2단계가 레지스트리에 `deleteForever`를 넣을 때 처음 눌린다.

---

## 8. 2단계 구현 기록 (2026-09-01)

### 8.1 레지스트리 한 줄이 표면 셋을 만든다

§3.1이 예측한 대로였다. `actions.ts`에 넣은 것은 넷이다:

- `TaskActionId`에 `"deleteForever"`
- `DEFINITIONS`에 `danger` 그룹, `danger: true`, **`trash` 바로 아래**
- `TRASHED_ACTIONS`에 추가 — 휴지통의 작업이 할 수 있는 일이 셋에서 넷이 됐다
- `availabilityOf`에서 `restore`와 **같은 줄**에 놓았다: 휴지통이 아니면 둘 다 `hidden`

`trash`와 `deleteForever`가 **동시에 나오는 일은 없다.** 같은 동사의 두 깊이이고,
빨간 줄 둘 중 어느 쪽이 되돌릴 수 없는지 읽는 사람이 구분하게 만드는 것이 이 메뉴가
피해야 할 모양이다. 시험이 그 배타를 양방향으로 고정한다.

### 8.2 이 줄만 행동하지 않는다

`runTaskAction`의 다른 모든 갈래는 `mutate`를 거치고, 그래서 전부 실행 취소가 붙는다.
`deleteForever`는 붙일 patch가 없다 — 사라진 행을 되돌리는 patch란 없다. §3.3이 "먼저
묻는다"로 정한 이유가 그것이고, 코드에서는 **이 갈래만 상태를 세운다**:

```ts
case "deleteForever":
  return setPendingDeleteForever(task);
```

묻는 상태를 각 화면이 아니라 **훅**이 든다. "hard delete는 두 번 묻는다"는 명령에
대한 규칙이지 화면에 대한 규칙이 아니고, 두 화면이 따로 들면 그중 하나가 안 묻게 되는
날이 온다.

그리고 확인 버튼을 누르는 순간 `canRunTaskAction`을 **한 번 더** 묻는다. §15.67의
재확인이 여기서 제일 필요하다 — 다이얼로그가 떠 있는 동안 동기화가 그 작업을 다른
기기에서 복원할 수 있고, 그러면 이 클릭은 **휴지통에 있지도 않은 것을 지우게 된다.**

### 8.3 문구가 말하는 사실

`ListManager`의 문장을 본떴다 — 동사가 가리는 사실을 말한다. 거기서는 함께 사라질
작업 개수였고, 여기서는 **하위 작업이 함께 가지 않는다**는 것이다:

> 휴지통에서도 계정에서도 사라집니다. **하위 작업은 최상위로 남습니다.** 되돌릴 수
> 없습니다.

§7.2가 정한 규칙을 화면이 말해 주지 않으면, 자식을 정리하려고 부모를 지운 사람이
자식이 그대로 남아 있는 것을 나중에 발견한다.

`TaskDeleteForeverGate`는 `TaskUndoStrip`과 같은 이유로 컴포넌트다: 훅을 두 셸이
부르고, **아무도 그리지 않는 질문은 한 번에 지워지는 hard delete**다.

### 8.4 검증

- 유닛 **2,189개 통과**(신규 4: 레지스트리 2 · 메뉴 2) · `tsc` 통과
- 실제 앱(dev, en, `/trash`): 행의 `⋯` → `Copy link · Task activities · … ·
  Restore · Delete forever`. 누르면 **아무것도 지워지지 않고** 다이얼로그가 뜬다.
  확인 → 행이 사라지고 저장소에도 없다(다른 작업 하나는 그대로)
- 콘솔 오류는 업데이터의 CORS 하나뿐 — 기존 잡음이다

### 8.5 지나가다 찾은 것 — 행 메뉴가 휴지통에서도 우선순위를 준다 [실측]

실제 앱에서 휴지통 행의 `⋯`를 열어 보니 이렇게 나온다:

```
Copy link · Task activities · High · Medium · Low · No priority ·
Due today · Due tomorrow · Clear the date · Restore · Delete forever
```

가운데 일곱 줄은 **레지스트리에서 오지 않는다.** 행 메뉴가 자기 우선순위 구획과 날짜
구획을 따로 그린다(`TICKTICK_FIDELITY_AUDIT.md`가 적은 그대로다). 그래서 §15.5가
"휴지통은 그 동사들이 적용되지 않는 곳"이라고 정한 것이 **레지스트리 항목에만**
적용되고 있다 — 버린 작업의 마감일을 오늘로 바꿀 수 있다.

이 문서가 만든 것이 아니고, 2단계가 고치지도 않았다. 세어 둔다.

### 8.6 남은 것

3단계 — 휴지통의 상세 푸터를 `되살리기` + `영구 삭제`로 바꾸는 일(§3.2). 지금은 그 둘이
`⋯` 안에 있고, §1.4의 스크린샷은 밖에 있다.

---

## 9. 3단계 구현 기록 (2026-09-01)

### 9.1 푸터가 갈린다

`TaskDrawer`의 푸터가 `isTrashed(task)`로 갈린다. 살아 있는 작업은 전과 같이 리스트
피커와 `⋯`이고, 버려진 작업은 `되살리기`(라벨) + `영구 삭제`(아이콘)다 — §1.4의 배치
그대로.

둘 다 **`onRunAction`을 부른다.** 스토어 함수를 직접 부르지 않는 것이 요점이다: 그러면
2단계가 만든 두 번째 물음이 이 버튼들에는 없는 채로 생긴다. 지금은 같은 명령이므로
같은 다이얼로그가 뜬다.

아이콘은 그려 넣었다. `components/schedule/icons.tsx`가 길게 적어 둔 이유 그대로 —
이모지는 플랫폼의 그림을 폰트의 색으로 타입 스케일의 크기로 그리는 것이고, 그 셋 중
무엇도 이 앱이 고른 것이 아니다. 24 viewBox · stroke 1.9, 앱의 나머지 아이콘과 같은 손이다.

### 9.2 `⋯`가 사라지는 것이 손해가 아닌 이유

레지스트리는 버려진 작업에 넷을 남긴다(`copyLink · activities · restore ·
deleteForever`). 그중 **둘이 이제 푸터에 나와 있으므로**, 메뉴 뒤에 남는 것은 링크 복사와
활동 기록 둘이다. 두 줄짜리 메뉴는 길을 막는 문일 뿐이라 함께 치웠다.

행에서는 그대로다 — 행에는 상세가 없고, 넷 전부 행의 `⋯`에 남는다.

### 9.3 시험이 옮겨 앉았다

2단계에서 쓴 세 시험이 상세의 `⋯`로 휴지통 작업의 메뉴를 열고 있었는데, 3단계가 그
`⋯`를 없앴다. **행의 메뉴로 옮겼고**, 옮기다가 하나 배웠다:

> 버려진 작업은 리스트 스코프에 **행이 없다.** `queryScopeTasks`가 걸러 내기 때문이다.
> 그런데 상세는 열린다 — `?task=`는 전체 작업에서 찾으니까. 그래서 `url`을
> `/list/l1?task=t1`로 두면 **상세만 있고 행이 없는 화면**이 되고, 행 메뉴를 여는
> 시험은 `/trash`에서 렌더해야 한다.

그리고 §8.5가 세어 둔 것 때문에 목록 전체를 고정하지 않았다. 행 메뉴는 자기 우선순위·
날짜 구획을 버려진 작업에도 주고 있고, 정확한 배열을 시험에 박으면 **그것을 의도로
못박는 셈**이 된다. 대신 레지스트리가 기여하는 것만 확인한다 — `되살리기`와 `영구
삭제`가 있고 `휴지통으로`·`고정`·`완료`가 없다.

### 9.4 Q1은 열어 둔다

푸터에서 리스트 피커와 `⋯`가 빠졌으므로 버려진 작업의 상세에서 **구조를 바꾸는 길은
없다.** 남은 것은 제목과 본문이고, 그건 여전히 편집된다.

읽기 전용으로 만들지 않았다. §1.4의 한 장으로는 판별이 안 되고(§0), 근거 없이 잠그면
제목의 오타 하나 때문에 되살렸다 다시 버려야 한다. 스크린샷이 한 장 더 생기면 닫힌다.

### 9.5 검증

- 유닛 **2,191개 통과**(신규 2: 버려진 작업의 푸터 · 살아 있는 작업의 푸터가 그대로인지)
  · `tsc` 통과
- 디자인 불변 E2E 9개(수직 리듬 · 반경 스케일 · 표면 위계) 통과 — 새 컨트롤 둘이 32px
  그리드와 반경 스케일 위에 있다
- 실제 앱(dev, en, `/trash?task=…`): 푸터가 `Restore` + `Delete forever` 둘뿐,
  `.tm-list-trigger` **없음**. 아이콘을 누르면 아무것도 지워지지 않고 다이얼로그가 뜬다

> 2단계의 전체 E2E 실행에서 `verticalRhythm`이 하나 실패했는데 `page.goto` 타임아웃이었다.
> 위 9개를 따로 돌려 전부 통과하는 것을 확인했다 — 이 세션에서 세 번째 만나는 같은
> 종류의 부하 flake다.

### 9.6 남은 것

4단계 — 헤더의 `휴지통 비우기`와 개수를 말하는 확인(§3.3). 하나씩 지우는 길이 이제
둘(행 메뉴 · 상세 푸터) 다 있으므로 전부 지우는 길이 그 다음이다.

---

## 10. 4~5단계 구현 기록 (2026-09-02)

### 10.1 개수를 어디서 세는가가 이 단계의 전부다

확인 문장이 말해야 하는 것은 개수다(§3.3). 그 개수를 **화면의 행 수로 세지 않았다.**

`scopeQuery`는 `parentTaskId`가 있는 행을 스코프에서 뺀다(`:191`). 그래서 부모와 자식이
둘 다 버려져 있으면 휴지통 목록에는 **하나만 보이고** `emptyTrash`는 **둘을 지운다.**
행을 세면 "3개"라고 말하고 5개를 없애는 화면이 된다.

`trashedTaskIds(tasks).length`를 쓴다 — §7.1이 세는 일을 도메인에 넣어 둔 이유가
이것이고, 화면이 **지워질 것과 다르게 세는 것**을 막는 유일한 방법이다.

이것이 Q3(자식만 버려진 경우)의 절반을 건드린다. 나머지 절반 — 목록에 안 보이는 자식을
**되살릴** 방법 — 은 여전히 열려 있다.

### 10.2 Q4 — 모달이다

`ListManager`는 제자리 확인을 쓰는데, 그건 **행이 있기 때문**이다. 지울 리스트마다 한
줄이 있고 그 줄이 확인 줄로 바뀐다.

`비우기`에는 그런 행이 없다. 머리의 아이콘 버튼 하나이고, 32px 칸을 확인 문장으로 바꿀
수는 없다. 모달이고, 리셋이 쓰는 것과 같은 `ConfirmModal`이다.

`TrashEmptyGate`가 `TaskDeleteForeverGate` 옆에 파일 하나로 선다 — **hard delete가
무엇을 묻는지는 행동마다 한 곳**이라는 §8.2의 규칙 그대로다.

### 10.3 비어 있으면 버튼이 없다

지우는 것이 일의 전부인 버튼은 지울 것이 없을 때 할 말이 없다. `trashedCount > 0`일
때만 그린다. 실측: 비운 직후 버튼이 사라진다.

### 10.4 5단계 — 틀리게 된 문장

`ListManager.tsx`의 머리말이 이렇게 적고 있었다:

> 영구 삭제는 이 앱에서 **유일하게** hard-delete하는 자리이므로 유일하게 두 번 묻는다.

이제 셋이다 — 리스트, 휴지통의 작업 하나, 휴지통 전체. 문장을 고치면서 **그 문장이
정말로 말하던 규칙은 남겼다**: hard delete는 두 번 묻고, 확인은 함께 사라지는 개수를
말한다. 셋 다 그렇게 한다.

### 10.5 검증

- 유닛 **2,222개 통과**(신규 4: 휴지통 헤더에만 있음 · 비었을 때 없음 · 개수를 말하고
  답하기 전엔 아무것도 안 지움 · 아니오면 안 지움) · `tsc` 통과 · 디자인 불변 E2E 9개 통과
- 실제 앱(dev, en, `/trash`), 세 개를 버려 놓고:
  - 머리 오른쪽 끝에 32×32 아이콘, 라벨 `Empty trash`
  - 누르면 `Empty the trash? / 3 tasks leave the trash and the account. …` —
    이 시점에 **저장소는 그대로 5개**
  - 확인 → 셋이 사라지고 둘이 남고, 행 0개, **버튼도 사라진다**
  - 콘솔 오류는 업데이터의 CORS 하나뿐

### 10.6 남은 것

이 문서의 단계는 끝났다. §5의 Q1(휴지통 상세가 읽기 전용인가) · Q3의 나머지 절반
(목록에 안 보이는 자식을 되살리는 길) · Q5(행의 `⋯`가 호버에만 나오는가)는 열려 있고,
§8.5가 세어 둔 것 — 행 메뉴가 버려진 작업에도 우선순위·날짜를 준다 — 도 그대로다.


---

## 13. Q3 — 지울 수는 있는데 볼 수는 없던 행 (2026-09-02)

### 13.1 무엇이 문제였나 [실측]

승격된 하위 작업(`parentTaskId`가 있는 진짜 `Task`)을 지우면 소프트 삭제된다.

```
usePlannerData.ts:1793   task.id === subtaskId ? { ...task, deletedAt: now } : task
             주석:  "A promoted child is a Task; deleting it is the Task path,
                     soft-delete and all, so it can be undone like any other."
```

**되돌릴 수 없었다.** 세 파일이 각자 옳은 일을 하다가 겹쳤다.

| 파일 | 하는 일 | 결과 |
|---|---|---|
| `scopeQuery.ts:191` | `parentTaskId`가 있으면 **모든** 스코프에서 뺀다 | 휴지통 목록에 안 뜬다 |
| `children.ts:36` | `deletedAt`인 자식을 부모의 상세에서 뺀다 | 부모를 열어도 안 보인다 |
| `trash.ts:37` | `deletedAt`만 보고 id를 모은다 | **개수에 세어지고 `비우기`가 지운다** |

즉 그 행은 **어느 화면에도 없는데** 휴지통의 뱃지를 부풀리고, 그 목록을 설명하는
확인창을 누르면 함께 영구 삭제됐다. Undo 토스트가 유일한 출구였고 그것이 사라지면 끝이다.

**볼 수는 없는데 지울 수는 있는 레코드**는 어느 쪽으로 봐도 옳지 않다.

### 13.2 결정 — 규칙의 예외는 휴지통 하나

`scopeQuery`의 그 한 줄은 **일이 어디에 보이는가**에 대한 규칙이고, 일이 질문인 곳에서는
전부 옳다. 자식은 목록이 아니라 부모의 상세에 속한다.

**휴지통은 일의 목록이 아니다.** 지워진 레코드가 결정을 기다리는 목록이고, 그래서
`active` 전제가 닿지 않는 두 스코프 중 하나다(`scopeQuery`의 기존 주석이 이미 그렇게
적어놨다). 그 자리에 일의 규칙을 적용한 것이 사고였다.

```ts
if (task.parentTaskId && scope.kind !== "trash") return false;
```

**휴지통의 버튼이 지울 수 있는 것은 휴지통이 보여줘야 한다.** 이것이 규칙이다.

예외는 휴지통뿐이다. `completed`는 그대로 자식을 뺀다 — 완료된 자식은 부모 안에서
보이고 되돌릴 수 있으므로 갇히지 않는다.

### 13.3 되살리기가 되살리기가 되려면

행이 보이는 것만으로는 부족했다. 휴지통에서 자식 행은 최상위 행과 **구별되지 않는다**.
되살리면 부모의 상세로 돌아가는데, 독자가 보고 있던 곳이 아니라서 **사라진 것처럼**
읽힌다.

그래서 행이 누구의 자식인지 말한다 — `↳ 부모 제목`, 리스트 이름과 같은 무리에, 같은
조용함으로. `TaskRowContent`의 `listName`이 있는 이유와 정확히 같다: **행이 자기가 앉은
자리에서는 얻을 수 없는 사실.** 화살표는 장식이라 `aria-hidden`이고, 문장은
`title`이 든다(`{{title}}의 하위 작업`).

부모마저 사라진 경우엔 아무것도 그리지 않는다. `removeTasksForever`가 자식을 최상위로
승격시키므로(§7.2), 없는 레코드를 가리키는 이름은 나올 일이 없다.

### 13.4 또 시험이 결함을 고정하고 있었다

```
it("keeps a deleted child out of Trash too, where it would also be a row")
```

이번 세션에서 **세 번째**다(§13.6의 `Show Input Box`, `taskBoardAdd`의 `adding` 센티널).
셋 다 "지금 이렇게 동작한다"를 "이렇게 동작해야 한다"로 적어둔 시험이었다.

### 13.5 검증

- 유닛 **2,290개 통과** · `tsc` 통과 · 전체 E2E **140 통과, 실패 0**(단독 8.2분)
- 도메인 시험 셋: 휴지통은 자식을 보여준다 · 예외는 `completed`로 새지 않는다 ·
  단일 술어뿐 아니라 `queryScopeTasks`의 **모음 경로**에서도 행이 살아남는다
- 행 시험 둘: 부모 이름을 말한다는 것, 자식이 아니면 아무것도 안 그린다는 것
- [실측] 실행 중인 앱에서 갇힌 행을 하나 만들어 끝까지 따라갔다:
  `/trash`에 `Stranded subtask ↳ LAC Lab 6`으로 뜨고 → `Restore` →
  `deletedAt: ""`, `parentTaskId` 보존 → 부모 상세의 `Subtasks 0/1`에 돌아왔다

---

## 14. Q1 — 상태는 얼리고, 낱말은 놔둔다 (2026-09-02)

### 14.1 §9.4가 적어놓은 것이 틀렸다

3단계는 이렇게 적었다.

> 푸터에서 리스트 피커와 `⋯`가 빠졌으므로 버려진 작업의 상세에서 **구조를 바꾸는 길은
> 없다.** 남은 것은 제목과 본문이고, 그건 여전히 편집된다.

[실측] 실행 중인 앱, `/trash?task=…`에서 실제로 살아 있던 컨트롤은 아홉이다.

```
☑ 체크박스              ← 버린 작업을 완료시킬 수 있었다
📅 Aug 20 13d overdue   ← 버린 작업의 일정을 바꿀 수 있었다
⚐ 우선순위
제목 · 체크리스트 전환 · 본문
[Restore] [Delete forever]
```

"제목과 본문"이 아니었다. **완료·일정·우선순위·체크리스트 전환**이 전부 살아 있었다.

### 14.2 체크박스가 실제로 깨진 하나

체크하면 `completedAt`이 `deletedAt` 옆에 찍힌다. `matchesScope`의 `completed`는
`!task.deletedAt`을 요구하므로 그 작업은 **완료 목록에도 안 뜬다.** 아무 데도 안 가고
있다가, 되살리면 **완료된 작업**이 돌아온다 — 독자가 버린 그 작업이 아니다.

일정과 우선순위는 그만큼 나쁘지는 않다. 되살릴 때 나타나는 편집이라는 점에서 제목·본문과
같은 갈래다. 그럼에도 함께 얼린 이유는 다음 절이다.

### 14.3 결정 — 경계는 「이다」와 「말한다」 사이

참조 앱이 이 화면에서 치운 것은 **리스트 피커**(어디에 있는지)와 **`⋯`**(무엇을 할지)고,
남긴 것은 제목·본문 영역이다(§1.4). 그 선이 그대로 규칙이 된다.

> **휴지통은 작업이 무엇'인'지를 얼리지, 무엇을 '말하'는지는 얼리지 않는다.**

- **얼린다**: 완료, 일정, 우선순위, 체크리스트 전환. 앞의 셋은 생애주기의 사실이고,
  넷째는 본문의 **모양**을 다시 쓴다 — 오타 고치기와 다른 갈래다
- **놔둔다**: 제목, 본문. 되살리기 전에 메모를 고쳐 두는 것은 아무것도 해치지 않고,
  근거 없이 잠그면 오타 하나 때문에 되살렸다 다시 버려야 한다

### 14.4 `disabled`인가 없애는 것인가 — 둘 다다

§15.5는 "여기서 할 일이 없는 행동은 회색이 아니라 **없다**"고 했다. 그 규칙은
**행동**의 것이고, 여기 있는 것은 **바꿀 수 없는 사실**이다. 그래서 갈렸다.

| | 처리 | 왜 |
|---|---|---|
| 체크박스 | `disabled` | 참조도 이 화면에 체크박스를 그린다(§1.4). 사실은 남고 문만 닫힌다 |
| 일정 · 우선순위 | `<span>`으로 정적 렌더 | 값이 없으면 **아무것도 안 그린다** — 누를 수 없는 빈 칩은 값이 아니다 |
| 체크리스트 전환 | 없앤다 | 이건 사실이 아니라 순수한 행동이다. §15.5 그대로 |

대비는 낮추지 않았다. 독자는 되살릴지 말지를 **정하는 중**이고, 못 읽는 날짜는
눈감고 내리는 결정이다. 커서와 호버만 걷었다.

### 14.5 `readOnly`는 프롭이지 쌍둥이 컴포넌트가 아니다

`SchedulePicker`와 `PriorityPicker`가 각각 `readOnly`를 받는다. 라벨·지각 일수·빨간색,
그리고 접근성 문장이 전부 그 파일들 안에서 계산되므로, 밖에 정적 쌍둥이를 만들면
그 규칙들의 **두 번째 사본**이 생긴다.

[실측] 걸림돌 하나: `.sched-trigger:hover`는 클래스 선택자라 `<button>`이든 `<span>`이든
똑같이 걸린다. 두 호버 규칙을 `:not(.is-readonly)`로 좁혔다.

### 14.6 검증

- 유닛 **2,296개 통과** · `tsc` 통과 · 전체 E2E **139 통과 · 1 실패**였으나 그 하나
  (`taskRow.spec.ts:40`)는 제가 실행 중에 브라우저를 몰고 파일을 쓴 탓이고,
  **단독 재실행에서 5개 전부 통과**했다. 건드린 것과 무관한 스펙이다
- 시험 넷: 넷을 얼린다는 것 · 낱말은 놔둔다는 것 · 버려지지 않은 작업에는
  **아무것도 하지 않는다**는 것 · 값이 없으면 칩을 안 그린다는 것
- [실측] 실행 중인 앱: 버린 작업의 상세가 `checkbox disabled` ·
  `span.sched-trigger.is-readonly.is-late "Schedule, Aug 20, 13d overdue"` ·
  `span.tm-priority-trigger.is-high.is-readonly "Priority, High"` · 전환 없음 ·
  제목·본문·`Restore`·`Delete forever` 살아 있음. 일반 작업은 `.is-readonly` 0개

---

## 15. Q5 — 캡처를 기다릴 필요가 없었다 (2026-09-02)

### 15.1 질문이 바뀌었다

원래 질문은 "참조 앱이 휴지통 행에 `⋯`를 안 주는가, 호버에만 주는가"였고 캡처 한 장을
기다리고 있었다. §14를 만들고 나니 그 질문이 **우리 것이 아니게** 됐다.

[실측] 우리 휴지통 행의 `⋯`가 열던 것:

```
Copy link · Task activities
High · Medium · Low · No priority          ← 우선순위
Due today · Due tomorrow · Clear the date  ← 일정
Restore · Delete forever
```

§14가 상세에서 **정적 `<span>`으로 얼려놓은 바로 그 둘**이 여기서는 눌리고 있었다.
패널이 지키던 규칙을 그 옆의 메뉴 일곱 줄이 되돌리고 있었다. 참조가 무엇을 하든,
우리 규칙이 이미 답을 갖고 있었다.

### 15.2 레지스트리는 옳았고, 손으로 쓴 두 구획이 몰랐다

`actions.ts`의 `TRASHED_ACTIONS`는 이미 넷뿐이고 나머지를 `hidden`으로 보낸다.
그런데 `TasksModule.taskMenuAt`이 우선순위와 일정 구획을 **레지스트리 밖에서 손으로**
끼워 넣는다.

그 함수 바로 위 주석이 같은 사고를 이미 기록해 두고 있었다.

> Complete, Won't Do and Trash used to be written out here by hand — which is
> how the row menu came to offer a trashed Task "Move to trash".

**한 겹 위에서 같은 실수가 남아 있었다.** 레지스트리는 배웠고, 손으로 쓴 두 구획은
배우지 못했다. `isTrashed(task)`로 둘을 막았다.

### 15.3 시험이 이것을 정직하게 비워 뒀었다

기존 시험은 정확한 목록을 박지 않고 이렇게 적어놨다.

> What the REGISTRY contributes, rather than the whole list: this row menu draws
> priority and date sets of its own, which it still offers a trashed Task (§8.5
> counts that and does not fix it). **Pinning the exact list here would nail
> that down as intended.**

결함을 고정하지 않으려고 일부러 비워 둔 것이다 — 이 세션에서 마주친 다른 셋과 달리
이 시험은 옳았다. 이제 박을 수 있게 됐으므로 박았다(문맥 메뉴 자신의 `Close` 포함).

### 15.4 검증

- 유닛 **2,298개 통과** · `tsc` 통과 · 전체 E2E **140 통과, 실패 0**(단독 7.9분)
- 시험 셋: 휴지통 행 메뉴의 **정확한 목록** · 일곱 줄이 하나도 없다는 것 ·
  버려지지 않은 작업에는 그대로 있다는 것
- [실측] 실행 중인 앱: 휴지통 행의 `⋯`와 **우클릭** 둘 다
  `Copy link · Task activities · Restore · Delete forever · Close`.
  일반 작업은 17줄 그대로

---

## 16. 리스트도 휴지통으로 (2026-09-03)

요구 셋:

1. **삭제하면 휴지통으로 간다.**
2. 휴지통 머리의 **전체 비우기**를 누르면 전부 없어진다.
3. 항목을 누르면 **복원 / 영구 삭제**를 고를 수 있다.

### 16.1 [실측] 셋 다 이미 있다 — 작업(Task)에 대해서는

실행 중인 앱(`/trash`)에서 확인한 것:

| 요구 | 작업 | 근거 |
|---|---|---|
| 삭제 → 휴지통 | ✅ | `deletedAt`을 찍고 `/trash`가 그 행들을 그린다 |
| 전체 비우기 | ✅ | 머리의 `휴지통 비우기` 버튼 — **개수를 말하는** 확인 대화상자(`TrashEmptyGate`)를 연다 |
| 항목 → 복원/영구삭제 | ✅ | 행을 누르면 Detail이 열리고 그 바닥이 `복원` + 영구삭제 아이콘(`TrashedFooter`) |

즉 **참조 앱 캡처 두 장이 요구하는 화면은 작업에 대해 이미 그대로 있다.** 캡처보다
나은 점 하나도 그대로 둔다: 참조 앱의 빗자루는 바로 비우지만, 우리 것은 **몇 개가
사라지는지 말한 뒤에** 비운다(§3.3). "비우기"는 읽는 데 아무 값도 안 드는 낱말이고
되돌리는 데는 전부가 든다.

### 16.2 그래서 이번에 만드는 것은 **리스트의 몫**이다

리스트를 지우면 `deletedAt`이 찍히고 작업들은 `listId`를 그대로 들고 있다
(`usePlannerData.trashList` — "soft, every one of them"). 여기까지는 작업과 같다.

**그런데 그 리스트는 어느 화면에도 안 나온다.**

- `/trash`의 질의는 `case "trash": return Boolean(task.deletedAt)` — **작업만** 본다.
- 삭제된 리스트의 작업들은 `deletedAt`이 안 찍힌다. `isTaskActive`가 "소유 리스트가
  죽었으면 작업도 활성이 아니다"로 조용히 빼기 때문이다(§6.56은 그게 의도라고 적어 뒀다 —
  리스트를 되살리면 작업이 전부 같이 돌아오는 이유).
- 그래서 지금 삭제된 리스트를 볼 수 있는 곳은 **사이드바 `관리` 뒤의 다이얼로그
  하나**뿐이다.

한 문장으로: **버린 리스트는 버린 작업과 다른 곳에 있고, 그 곳은 숨어 있다.**

### 16.3 결정 — 휴지통은 "버린 것"의 화면이지 "버린 작업"의 화면이 아니다

`/trash`가 **두 구획**을 그린다.

```
휴지통                                    🗑 휴지통 비우기
┌────────────────────────────────────────────────────┐
│ 리스트 2                                            │
│   Work        작업 12개    [복원]  [🗑]              │
│   Old notes   작업 0개     [복원]  [🗑]              │
├────────────────────────────────────────────────────┤
│ 작업 12                                             │
│   … 지금 그대로의 행들 …                             │
└────────────────────────────────────────────────────┘
```

- **리스트 구획이 위**에 온다. 리스트를 되살리면 그 안의 작업이 통째로 돌아오므로,
  아래에서 작업을 한 줄씩 되살리기 전에 먼저 보아야 하는 것이 위에 있는 편이 맞다.
- 리스트가 하나도 없으면 **구획 자체가 없다**. 지금의 휴지통과 픽셀 하나 다르지 않다.
- 개수는 `작업 N개`. `ListManager`가 이미 그 문장을 말한다(`taskCountInList`) — 문장을
  옮기는 것이지 새로 쓰는 것이 아니다.

### 16.4 리스트 행을 누르면 — Detail을 만들지 않는다

작업은 누르면 Detail이 열리고 그 바닥에 두 버튼이 있다. 리스트에는 **Detail이라는 화면이
없다** — 리스트를 여는 것은 "그 리스트의 작업 목록으로 가는 것"이고, 버려진 리스트에는
갈 곳이 없다.

그래서 **두 컨트롤을 행 안에 둔다**: `복원`과 영구삭제 아이콘. 두 답만 필요한 것을 위해
화면을 하나 새로 만드는 것은, §3.2가 작업의 Detail에서 ⋯를 걷어낸 것과 같은 이유로
거부한다 — 두 줄짜리 메뉴는 길을 막는 메뉴다.

배치는 작업 Detail의 바닥과 **같은 순서**다: 복원은 낱말을 달고 왼쪽, 영구삭제는 아이콘만
달고 오른쪽. §3.2가 그렇게 정한 이유(파괴적인 쪽은 조준해야 하고, 누르면 곧장 실행되지
않고 대화상자를 연다)가 여기서도 그대로 성립한다.

영구삭제가 여는 대화상자는 **이미 있는 것**이다 — `ListManager`가 쓰던, 몇 개의 작업이
함께 사라지는지 말하는 그 문장(§6.56의 유일한 허용 캐스케이드).

### 16.5 전체 비우기는 리스트도 가져간다

`emptyTrash`는 지금 `trashedTaskIds`만 지운다. **리스트도 지운다.** 이유는 하나다 —
"휴지통을 비웠는데 휴지통이 안 비어 있는" 화면을 만들 수 없다.

확인 문장이 둘 다 말한다:

> 작업 12개와 리스트 2개를 영구히 삭제합니다. 리스트와 함께 그 안의 작업 5개도 사라집니다.

세 번째 문장이 필요한 이유: 삭제된 리스트의 작업은 **휴지통의 작업 12개에 안 들어 있다**
(§16.2). 그 숫자를 말하지 않으면 "12개"라고 읽고 17개를 지우게 된다.

### 16.6 보관(`archivedAt`)은 없앤다

지난 대화의 결정이다. 되돌리는 문이 삭제와 똑같이 그 숨은 다이얼로그 하나뿐인 두 번째
소프트 상태는 **삭제의 약한 복제본**이다. 리스트 `⋯`에서 `보관` 행을 빼고 `삭제`만
남긴다.

**이미 보관해 둔 리스트가 있는 계정**은 그 리스트가 갈 데가 없어지므로, 휴지통의 리스트
구획이 그것도 함께 그린다 — 행에 `보관됨` 한 마디를 달고, 같은 두 버튼을 준다.
저장된 `archivedAt`은 **건드리지 않는다**(마이그레이션 없음). 복원하면 그 필드가 지워지고,
그 계정은 다시는 보관 상태를 만들 수 없다. 상태가 스스로 말라 없어지는 방식이고,
데이터를 다시 쓰면서 지우는 것보다 안전하다.

기각한 대안: 로드 시 `archivedAt`을 `deletedAt`으로 옮겨 쓰기. 한 줄이면 되지만 **읽기가
쓰기가 된다** — 이 저장소가 `matrixQuadrantRules`·`inboxColumns`에서 반복해서 거부한
것이고, 구버전 클라이언트와 같이 쓰는 계정에서 되돌릴 수 없다.

### 16.7 그리고 사이드바의 `관리`가 사라진다

문이 휴지통으로 옮겨졌으므로 `ListManager` 다이얼로그와 그것을 여는 `관리` 버튼이 통째로
나간다. 사이드바 `리스트` 구획의 머리에는 `+` 하나만 남는다 — 사용자가 처음에 지적한
그 화면이 그렇게 정리된다.

나가는 것: `ListManager.tsx`, `tasks.manageLists`/`manageListsAria`,
`TasksSidebar`의 `onManageLists`, `TasksSidebarSlot`의 `managing` 상태.
`tasks.manager*` 문자열 일곱 중 **복원·영구삭제·개수 셋은 살아남아** 휴지통의 리스트
구획으로 옮겨 간다 — 나머지(제목, `보관됨`/`삭제됨` 머리말, 빈 상태 두 줄)는 지운다.
§32.4의 고아 키 0 시험이 빠뜨린 것을 잡는다.

### 16.8 단계

1. **도메인** — `emptyTrash`가 리스트까지 지우고 세 숫자(작업·리스트·딸려가는 작업)를
   돌려준다. 순수 함수 + 유닛.
2. **휴지통 화면** — 리스트 구획, 행의 두 버튼, 기존 두 대화상자 재사용.
3. **비우기 문장** — 세 숫자를 말하는 새 문구.
4. **제거** — `보관` 행, `관리` 버튼, `ListManager`, 죽은 문자열.

1과 2 사이에서 앱은 항상 멀쩡하다. 4는 2가 끝난 뒤에만 안전하다 — 순서가 곧 안전이다.

### 16.9 시험

- `emptyTrash`가 작업과 리스트를 함께 지우고, 리스트에 딸린 작업까지 데려간다
- 리스트가 없으면 휴지통 화면이 지금과 같다(구획이 아예 없다)
- 리스트 행의 `복원`이 `restoreList`를 부르고, 그 리스트의 작업들이 **자기 스코프로
  전부 돌아온다**(§6.56이 약속한 것을 처음으로 시험이 지킨다)
- 영구삭제 대화상자가 **딸려가는 작업 개수**를 말한다
- 보관돼 있던 리스트가 그 구획에 보이고 복원된다
- 사이드바에 `관리` 버튼이 없다

### 16.10 안 하는 것

- **폴더**의 휴지통. 폴더는 삭제 시 리스트를 데려가지 않고 그룹만 푼다 — 다른 이야기다.
- **자동 비우기**(30일 뒤 영구 삭제). 참조 앱에 있지만 시간이 지나면 스스로 지우는 기능은
  별도 결정이다.
- **작업 쪽 휴지통 UI 변경.** §16.1이 실측한 대로 이미 요구를 만족한다.

### 16.11 구현 결과 (2026-09-03)

네 단계를 순서대로 냈다. 설계에서 **벗어난 것**과 **설계가 예상하지 못한 것**만 적는다.

**1단계 (도메인).** `emptyTrash(rows, binnedListIds)`가 세 숫자를 돌려준다. 리스트는
**id로만** 받는다 — `trash.ts`가 스스로 선언한 "이 파일은 리스트 레코드에 손댈 수 없다,
폭발 반경이 타입에 있다"는 경계를 지키기 위해서고, 리스트 행을 지우는 것은 훅의 몫이다.

설계에 없던 것 하나: **`permanentlyDeleteList`의 가드를 넓혀야 했다.** `deletedAt`만 보고
있어서, 보관된 리스트는 휴지통에 **보이는데 지울 수 없는** 기록이 될 뻔했다. `isLive`가
아닌 것 전부로 넓혔다 — 두 상태 모두 클릭 하나에서 두 걸음 떨어져 있고, 그게 이 가드의
목적이다.

**2단계 (화면).** `TrashLists` + `ListDeleteForeverGate`. 행은 작업 행(`.tm-task`)과 작업
Detail 바닥의 두 컨트롤(`.tm-drawer-restore`, `.tm-drawer-delete-forever`)을 그대로 입는다 —
같은 제스처에 다른 옷을 입히면 없는 차이를 있다고 말하게 된다.

**3단계 (문장).** `TrashEmptyGate`가 `count: number`에서 `summary: TrashSummary`로 바뀌고,
리스트가 있을 때만 두 번째 문장을 붙인다. 머리의 버튼 조건도 넓혔다 — **휴지통에 리스트만
있어도** 비울 수 있어야 한다.

**4단계 (제거).** 나간 것: `ListManager.tsx`(파일), 사이드바의 `관리` 버튼,
`TasksSidebarSlot`의 `managing` 상태와 두 prop, 리스트 `⋯`의 `보관` 행,
`usePlannerData.archiveList`, 문자열 아홉.

`domain/spaces/lifecycle.archiveList`는 **남겼다.** 예전 빌드에서 보관해 둔 계정의 데이터를
읽는 어휘이고, 시험이 그 상태를 만들어 내는 방법이기도 하다. 사용자가 도달할 수 있는 문은
전부 닫혔다.

**설계가 예상하지 못한 것 (실제 앱에서 발견).** 리스트만 든 휴지통에서 아래에
**"휴지통이 비어 있습니다"** 가 같이 떴다. 위의 구획이 그 문장을 반박한다. 빈 상태는
아무것도 없는 화면의 것이므로, 리스트가 있으면 그 줄을 그리지 않는다.

**남은 이름 하나:** 문자열 넷이 `manager*`였다. 그 이름의 컴포넌트가 없어졌으므로 셋을
옮겼고(`trashListArchived` · `trashListTaskCount` · `listDeleteForeverBody`), `managerRestore`는
지웠다 — `tasks.menu.restore`가 같은 낱말을 이미 말하고, 같은 제스처다.

### 16.12 검증

- 유닛 **2,390개 통과**(신규 18: 도메인 8 · 화면 11 중 신규) · `npx tsc -b` 통과
- 실제 앱(dev, ko), 처음부터 끝까지:
  - 리스트 `⋯` → `리스트 삭제`(보관 행 없음) → 사이드바에서 사라짐
  - `/trash` → `리스트 1 · Throwaway · 작업 1개 · 복원 · 🗑`, 빈 상태 문장 없음
  - 🗑 → **"'Work'을(를) 영구히 삭제할까요? / 작업 3개도 함께 영구 삭제됩니다"** → 리스트와
    작업 3개가 함께 사라짐(보관 상태였던 리스트로 확인 — 넓힌 가드가 동작한다)
  - `복원` → 리스트가 사이드바로 돌아오고 **그 안의 작업도 함께** 돌아옴
  - `휴지통 비우기` → **"작업 1개가 … 리스트 1개와 그 안의 작업 1개도 함께 사라집니다"** →
    둘 다 사라지고 머리의 버튼도 사라짐
  - 사이드바 `리스트` 머리 = `리스트 | +` (관리 없음)
