# Spaces 재설계 II — 업무 영역 계층과 View 레지스트리 (폐기)

- 작성일: 2026-08-16
- **폐기일: 2026-08-17. `SPACES_REDESIGN_II.md`가 대체한다.**

---

## 왜 폐기하는가

이 문서의 핵심 결정(H2)은 **기존 `Project` 레코드를 Folder로 강등하고 기존 `Folder`를 Subfolder로 재해석**하는 것이었다. 근거는 마이그레이션 비용이었다 — 계층을 위에 얹으면 Project N행, 아래에서 밀어 올리면 Task N천 행.

**그 근거가 틀렸다.** 두 안 모두 `Project.spaceId` 한 필드 추가로 끝난다. 강등안이 아끼는 재작성은 없었고, 대가만 남았다:

```
task.projectId   → 실제로는 Folder를 의미하게 된다
list.spaceId     → 실제로는 Folder를 의미하게 된다
```

`SPACES_REDESIGN_II.md` §8이 이 점을 정확히 지적한다. §74의 우선순위 — *"코드를 덜 바꾸는 것보다 도메인 의미를 계속 틀리게 유지하지 않는 것"* — 로 판단하면 강등안은 채택할 이유가 없다.

## 살아남은 것

이 문서에서 유효했던 내용은 대체 문서로 옮겼다.

| 이 문서 | 옮겨간 곳 |
|---|---|
| H10 부채 목록 (타입 어휘 3벌, `space:<id>` 태그, id 네임스페이스) | §0.3.7 · §0.3.8 · §61 · STEP 4.5 |
| `G-RENDER-01`의 전신 — 뷰 엔진이 layout을 선언만 한다는 관찰 | §0.3.2 |
| Space 수준 Board의 status 소유 문제 | §0.3.3 |
| U2 `listsRevealed` 단방향 규칙 보존 | §61 체크리스트 |

## 채택되지 않은 것

- Subfolder 도입 (대체 문서 §2 — 필요성이 증명되지 않았다)
- `Project` → `Folder` 개명
- 배치 마법사 우선 (대체 문서 §39 — 기본 Space backfill 후 사용자가 옮기는 쪽이 안전하다)
