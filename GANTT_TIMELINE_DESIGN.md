# 간트(타임라인) 뷰 설계

- 작성일: 2026-08-15
- 기준: `v0.6.0` (`66c032d`) + 이 브랜치의 뷰 엔진 작업 (보관함·보드·공간 작업 탭이 이미 엔진으로 이동)
- 선행 문서: `CLICKUP_IMPORT_DESIGN.md` (§4.1 Item, §4.2 ViewSpec), `HORIZONS_DESIGN.md` (지평 = 시간 축), `SPACES_BOARD_DESIGN.md` (영역 × 시간 두 축)
- 대상: `src/domain/view/` + 새 `TimelinePage` / `TimelineView` + `src/styles/`

---

## 0. 이 문서의 위치

간트는 이 브랜치에서 진행한 통합의 **마지막 조각**이다. 앞선 네 단계가 끝났다.

| 단계 | 상태 |
|---|---|
| 보관함 → 뷰 엔진 | 완료 |
| 보드 구현 + 계획(아이젠하워) 흡수 | 완료 — `EisenhowerPage` 555줄 삭제 |
| SpaceNote 제거 | 완료 |
| 공간 작업 탭 → 보드 | 완료 — `SpaceWorkTabs` 213줄 삭제 |
| **간트** | **이 문서** |

**이미 있는 재료.** 간트를 위해 새로 발명할 개념은 하나도 없다.

| 필요한 것 | 이미 있는 것 |
|---|---|
| 막대의 기간 | `domain/view/span.ts` — `spanForItem` / `spanDays` / `spanIntersects`, 테스트 11개 |
| 시작일 필드 | `Task.startDate` (jsonb에 실려 마이그레이션 불필요) |
| 행 그룹 | `viewSpec.groupBy: "space" | "list" | "status" | …` |
| 줌 단계 | `utils/horizons.ts` — `life | year | month | week | day` |
| 기간 이동 | `domain/horizons/horizonAnchors.ts` — `shiftHorizonAnchor` |
| 의존선 | `Task.blockedByTaskId` + `domain/tasks/dependencies.ts` (`dependentsOf`, `wouldCycle`) |
| 드롭 → 패치 | `domain/view/board.ts`의 `patchForColumn` 패턴 |

---

## 1. 확신도

| 항목 | 확신도 |
|---|---|
| `span.ts` · `startDate` · 의존성 API의 현재 동작 | **[확인]** — 이 브랜치에서 작성·테스트함 |
| 렌더 비용 추정(§3 D3) | **[확인]** — 같은 저장소의 `CalendarView` 933줄 / `WeekView` 962줄이 근거 |
| 사용자가 다중일 기간을 실제로 입력할지 | **[불명]** — `startDate`는 방금 생겼고 아직 아무 데이터에도 없다. §7의 위험 |
| 의존선을 몇 개나 그리게 될지 | **[불명]** — `blockedByTaskId`는 1:1이라 상한이 낮지만 실사용 밀도는 모름 |
| 간트가 지평 페이지를 대체할 수 있는지 | **[불명]** — §6 P4의 착수 조건이 이것의 검증이다 |

**검증 한계.** 헤드리스 브라우저 창이 화면에 표시되지 않아 `requestAnimationFrame`이 실행되지 않는다. 애니메이션·드래그 체감은 이 환경에서 검증 불가이며, 사용자가 직접 봐야 한다.

---

## 2. 트레이드오프 — 한 번만 적는다

**가로 스크롤을 버리고 페이지 이동을 택한다.** 이것이 이 설계의 유일한 큰 트레이드오프이며, 나머지 결정 대부분이 여기서 파생된다.

- **잃는 것**: 6개월을 한 번에 훑는 조망. 스크롤로 시간을 "느끼는" 감각.
- **얻는 것**: 가상 스크롤 없음, 헤더·본문 스크롤 동기화 없음, 스크롤 위치 상태 없음. CSS Grid가 배치를 전부 처리한다.

같은 저장소에서 시간 축 UI를 두 번 만들었고 두 번 다 900줄을 넘었다(`CalendarView` 933, `WeekView` 962). 세 번째를 그 비용으로 만들면 **삭제한 것보다 많이 쓰게 된다** — 이번 통합의 목적이 개념을 7개에서 2개로 줄이는 것이었는데, 줄 수로 본전도 못 찾는다.

그리고 페이지 이동은 이 저장소에 **이미 있는 패턴**이다. `7cecab4 feat: add navigable horizon periods`가 만든 `shiftHorizonAnchor`가 정확히 그 일을 한다. 새 상호작용을 가르치지 않아도 된다.

---

## 3. 확정 결정

### D1. 간트는 `layout: "timeline"`이다 — 새 엔진이 아니다

`ViewSpec.layout` 유니온에 `"timeline"`이 **이미 있다**. 간트는 보드와 같은 `Item[]`, 같은 `applyView`, 같은 `filter`/`groupBy`를 쓰고 배치만 다르다.

```ts
{ filter: {spaceId?}, groupBy: "space" | "list" | "none", sort: {key:"scheduledDate"}, layout: "timeline" }
```

`groupBy`가 행 그룹을 만든다. 보드에서 컬럼을 만들던 그 축이 여기서는 행 묶음이 된다 — 같은 함수, 같은 결과, 다른 그림.

**따라서 `applyView`는 손대지 않는다.** 간트가 필요로 하는 유일한 신규 도메인 로직은 §4의 창(window) 계산과 §5의 드래그 패치뿐이다.

### D2. 사이드바에 `타임라인`을 추가한다 — 보드의 옵션이 아니라

내부적으로는 layout 하나 차이지만, 사용자가 말한 최종 형태는 "**간트 차트랑 보드**" 두 기둥이다. 보드 안의 드롭다운에 숨기면 두 기둥이 아니라 한 기둥에 딸린 옵션이 된다.

보드와 **스코프 셀렉터·묶는 기준을 공유**한다(같은 컴포넌트 재사용). 페이지만 둘이고 컨트롤은 하나의 어휘다.

### D3. 창은 고정 폭이고, 좌우 화살표 + "오늘"로 이동한다

가로 스크롤 없음. 창은 줌 단계가 정하는 **N개 컬럼**이고, 화면 폭에 맞춰 CSS Grid가 나눈다.

| 줌 | 컬럼 단위 | 컬럼 수 | 창 길이 |
|---|---|---|---|
| `day` | 1일 | 14 | 2주 |
| `week` | 1주 | 12 | 약 3개월 |
| `month` | 1개월 | 12 | 1년 |
| `year` | 1년 | 5 | 5년 |

**컬럼 수는 viewport에 반응하지 않는다.** 창 길이가 화면마다 달라지면 좌우 이동 한 번의 의미가 기기마다 달라진다. 좁은 화면에서는 **날짜 라벨만 축약한다**(`08-17` → `17`).

`life`는 **줌 단계가 아니다.** 첫날도 마지막날도 없는 기간이라 컬럼으로 나눌 수 없다. `life` 지평의 목표는 타임라인 하단 **"기간 없음" 트레이**에 목록으로 둔다(§4.3).

이동은 `shiftHorizonAnchor(unit, anchor, ±1)`. 새 함수를 만들지 않는다.

**"오늘" 버튼을 둔다.** 스크롤을 버린 대가로 사용자가 창을 여러 번 넘긴 뒤 현재 위치를 잃을 수 있다. 스크롤 타임라인이라면 스크롤바가 위치를 알려주지만 페이지 이동에는 그런 단서가 없다. 복귀 경로가 한 번의 클릭이어야 한다.

### D4. 창 밖의 아이템은 렌더하지 않는다 — 단, 걸친 막대는 자른다

`spanIntersects(span, from, to)`가 이미 있고 양끝 포함으로 판정한다. 창을 관통하는 막대는 잘린 쪽에 **연속 표시(◀ / ▶)**를 단다. 잘렸다는 사실을 숨기면 "3일짜리 작업"으로 오독된다.

**렌더 상한이 여기서 나온다.** 최악의 경우가 `창에 걸친 아이템 수 × 컬럼 수`이고, 컬럼은 최대 14다. 가상 스크롤이 필요 없는 이유가 이것이다.

### D5. 막대는 세 날짜에서 해석하고, 추론을 저장하지 않는다

`spanForItem`이 이미 이 규칙이다. 다시 적지 않고 결과만 쓴다.

- 날짜가 **하나라도** 있으면 막대가 있다 (마감일만 있으면 하루짜리)
- 날짜가 **없으면** 타임라인이 아니라 "기간 없음" 트레이로
- `inferredStart`가 참이면 **점선 테두리**로 그린다 — 시작이 사용자의 선언이 아니라 폴백이라는 표시

마지막 항목이 중요하다. 실선 막대는 "월요일에 시작한다고 네가 말했다", 점선 막대는 "네가 준 것으로 미루어 여기쯤"이다. 둘을 같게 그리면 플래너가 추측을 사실로 보고하게 된다.

### D6. 드래그는 저장된 것만 옮긴다

간트의 핵심 상호작용이고, 가장 틀리기 쉬운 곳이다.

| 동작 | 쓰는 것 |
|---|---|
| 왼쪽 끝 리사이즈 | `startDate` — **없었으면 여기서 생긴다** (사용자의 선언이므로 정당) |
| 오른쪽 끝 리사이즈 | `dueDate` |
| 막대 전체 이동 | **저장된 날짜만** 같은 일수만큼 이동 |

세 번째가 결정이다. `startDate`가 없는 막대(`inferredStart`)를 통째로 끌면, `startDate`를 새로 써넣고 싶은 유혹이 있다. **쓰지 않는다.** 그 시작은 사용자가 고른 적 없는 날짜이고, 저장하는 순간 추론이 사실로 굳는다. 저장된 `dueDate`·`scheduledDate`만 이동하고, 시작은 계속 파생된다.

`patchForColumn`과 같은 자리에 `patchForSpanDrag(task, drag)`로 두고 순수 함수로 테스트한다. 보드가 세운 규칙 — *"드롭은 컬럼을 세팅할 수 없고, 컬럼을 읽어낸 필드를 되써야 한다"* — 의 시간 축 버전이다.

**이동 단위는 일수가 아니라 컬럼이다.** 월 줌에서 컬럼 하나는 한 달이므로, 3월 막대를 오른쪽으로 한 칸 끌면 4월이어야 한다. 일수 델타로 계산하면 달마다 길이가 달라 엉뚱한 날짜에 떨어진다. 따라서 `move`는 `{zoom, steps}`로 표현하고 도메인이 `addDays`/`addMonths`를 골라 적용한다.

**P2 구현 노트 (2026-08-15).** 드롭 대상은 픽셀이 아니라 **컬럼 셀**이다. 컬럼이 창을 그리는 단위 그 자체라 드롭이 모호할 수 없고, 모든 줌에서 같은 상호작용이 된다. 대가는 드래그 중 라이브 프리뷰가 없다는 것이고 P2는 이를 받아들인다. 오른쪽 끝을 끌면 그 컬럼의 **마지막 날**이 `dueDate`가 된다(월 컬럼에 떨구면 "그 달 말까지"). 잘린 쪽 끝에는 핸들을 그리지 않는다 — 창 밖이라 잡을 것이 없다.

### D7. 의존선은 1:1 유한선이다

`blockedByTaskId`는 작업당 하나다. 따라서 화살표는 **최대 아이템 수만큼**이고, N² 문제가 없다.

- 종류: finish-to-start 하나만 (`blockedByTaskId`가 표현할 수 있는 전부)
- 렌더: 막대 레이어 위 SVG 오버레이 하나
- **양끝이 모두 창 안에 있을 때만 그린다.** 한쪽이 창 밖이면 그 막대에 배지로 표시한다 — 화면 밖으로 나가는 선은 어디로 가는지 알려주지 못하면서 잉크만 쓴다
- 드래그로 의존성 만들기는 P3 이후. `wouldCycle`이 이미 순환을 막는다

**P3 구현 노트 (2026-08-15).** 끝점은 **계산이 아니라 측정**한다. 막대는 높이가 정해지지 않은 그룹 헤딩 사이에 CSS Grid로 놓이므로, 브라우저 레이아웃을 복제하지 않고는 y를 산술로 얻을 수 없다 — 그리고 레이아웃 모델을 두 벌 두면 동기화할 것이 하나 더 생긴다. `ResizeObserver`로 창 크기 변화도 따라간다.

**설계에 없던 것을 하나 추가했다 — 충돌 표시.** 후행 작업이 선행 작업이 끝나기 전에 시작하도록 잡혀 있으면 계획이 자기모순이다. 데이터가 이미 답을 갖고 있고(두 span 비교), **다른 화면은 이걸 보여줄 수 없다.** 일반 선과 똑같이 그리면 이 뷰만이 할 수 있는 일을 스스로 감춘다. 충돌선은 빨간색이며, 앞으로 갈 수 없으므로 아래로 우회한다.

### D8. 목표는 막대, 마일스톤은 마름모

`Item.source`가 이미 셋을 구분한다.

- **작업(task)**: 막대
- **목표(goal)**: 막대 — `schedule.startDate` → `deadlineDate`. `unscheduled`/`life`는 시작이 없어 폴백
- **마일스톤(milestone)**: `deadlineDate` 하나뿐 → **마름모 마커**. 간트의 표준 관례이고, 우연히도 데이터 모양과 정확히 맞는다

---

## 4. 계산 — 새로 필요한 도메인 로직

전부 `src/domain/view/timeline.ts`에 순수 함수로. UI는 결과만 그린다.

### 4.1 창

```ts
export interface TimelineWindow {
  unit: "day" | "week" | "month" | "year";
  /** 창의 첫 컬럼이 시작하는 날 (YYYY-MM-DD). */
  anchor: string;
  /** 컬럼 경계들. 길이 = 컬럼 수 + 1. */
  columns: string[];
  from: string;  // columns[0]
  to: string;    // 마지막 컬럼의 끝 (포함)
}
export function timelineWindow(unit, anchor, columnCount): TimelineWindow
```

### 4.2 막대 배치

```ts
export interface BarPlacement {
  /** 1-기반 CSS grid-column 시작. */
  columnStart: number;
  /** 배타적 끝 — `grid-column: start / end`. */
  columnEnd: number;
  clippedStart: boolean;
  clippedEnd: boolean;
}
export function placeBar(span: Span, window: TimelineWindow): BarPlacement | null
```

`null`은 "창과 겹치지 않음". 이 함수가 D4의 자르기와 D3의 그리드 좌표를 동시에 답한다.

**테스트할 경계**: 창 첫날에 끝나는 막대 / 마지막날에 시작하는 막대 / 창을 완전히 덮는 막대 / 컬럼 경계에 정확히 걸친 막대 / 월 단위에서 월말·윤일.

### 4.3 기간 없음 트레이

`spanForItem`이 `null`을 준 아이템 + `life` 지평 목표. 타임라인 하단 접이식 목록. **여기서 막대로 끌어다 놓으면** 그 날짜가 `startDate`로 쓰인다 — 사용자가 고른 것이므로 D6에 어긋나지 않는다.

---

## 5. 데이터 모델 — 변경 없음

**새 필드도, 새 테이블도, 마이그레이션도 없다.**

`startDate`는 이미 추가했고 `data jsonb`에 실려 서버 작업이 0이었다. 간트가 쓰는 나머지는 전부 기존 필드다.

| 간트가 읽는 것 | 출처 |
|---|---|
| 막대 기간 | `startDate` / `scheduledDate` / `dueDate` |
| 행 | `spaceId` / `listId` (`membership.ts`가 해석) |
| 색 | `Item.color` (스페이스 색) |
| 의존선 | `blockedByTaskId` |
| 완료 표시 | `Item.done` |
| 차단 표시 | `Item.blocked` |

---

## 6. Phase

| | 내용 | 착수 조건 |
|---|---|---|
| **P1** | 읽기 전용 타임라인. `timeline.ts` + `TimelineView` + `TimelinePage`. 줌 4단계, 좌우 이동, 자르기 표시, 기간 없음 트레이 | 없음 — 지금 시작 가능 |
| **P2** | 드래그: 양끝 리사이즈 + 전체 이동. `patchForSpanDrag` — **완료 (2026-08-15)** | P1이 실제 데이터로 읽힌 뒤 |
| **P3** | 의존선 SVG 오버레이 + 창 밖 배지 — **완료 (2026-08-15)** | P2 |
| **P4** | 지평 페이지 흡수 검토 | **P1~P3를 실사용하고 `life`/`year`가 트레이·최대 줌으로 충분한지 확인한 뒤** |

P4를 조건부로 둔 이유: 지평의 `life`는 간트가 표현할 수 없는 유일한 값이고, `HORIZONS_DESIGN` D8이 *"관점 자체가 제품"*이라고 못 박았다. 트레이가 그 자리를 대신하는지는 **써봐야 안다.** 지금 결정하면 근거 없이 결정하는 것이다.

---

## 7. 하지 않을 것

| 안 하는 것 | 이유 |
|---|---|
| 가로 스크롤 | §2 |
| 임계 경로(critical path) | `blockedByTaskId`가 1:1이라 경로가 사실상 사슬 하나. 계산은 가능하지만 보여줄 게 없다 |
| 진척률 막대 | `estimatedMinutes`/`actualSeconds`는 **투입 시간**이지 진척이 아니다. 둘을 나눠 %로 쓰면 "예상보다 오래 걸림"을 "많이 진행됨"으로 표시하게 된다 |
| 베이스라인 비교 | 계획 이력을 저장하지 않는다. 저장하려면 새 컬렉션이 필요하고, 그건 간트가 아니라 별도 기능 |
| 다중 선행 작업 | `blockedByTaskId` 하나를 배열로 바꾸는 건 데이터 변경. 필요해지면 그때 |
| 리소스/담당자 뷰 | 담당자 개념 자체가 없다 (단일 사용자 앱) |
| 작업 시간(startTime/endTime) 반영 | 간트의 최소 단위는 하루다. 시간 단위는 캘린더가 이미 한다 |

---

## 8. 위험

**`startDate`에 데이터가 없다.** 방금 만든 필드라 기존 사용자의 모든 작업이 `startDate: ""`다. 즉 **첫 실행에서 모든 막대가 점선 하루짜리**로 보인다. 이것은 버그가 아니라 정직한 표현이지만, 첫인상이 "간트가 고장났다"가 될 수 있다.

완화: P1에서 기간 없음 트레이와 하루짜리 막대를 **의도된 상태로 읽히게** 만든다 — 빈 상태 문구가 "시작일을 넣으면 기간이 생깁니다"를 말해야 한다. 자동으로 `scheduledDate`를 `startDate`에 복사하는 마이그레이션은 **하지 않는다**(D5·D6과 정면 충돌).

---

## 9. 결정됨 (2026-08-15)

§9의 네 항목은 모두 답이 나왔다. 확정 결정이므로 D9~D12로 승격한다.

### D9. 하위 작업은 P1에서 계층이 아니라 들여쓴 라벨이다

모든 Item은 **독립된 flat row**로 렌더한다. 계층형 행도, 접기·펼치기도 P1에 없다.

`parentId`가 있고 **부모가 현재 뷰에 존재할 때만** 행 라벨을 한 단계 들여써 관계를 표시한다. 부모가 필터·창 밖에 있으면 들여쓰지 않는다 — 보이지 않는 것에 들여쓰기는 무의미한 여백이다.

**부모 기간 자동 집계 없음. 부모-자식 동시 드래그 없음.** 둘 다 "부모 막대가 자식의 합"이라는 계약을 만드는데, 그 계약은 만들기는 쉽고 지키기는 어렵다(자식 하나가 창 밖이면 합이 거짓말이 된다).

보드가 중첩을 버린 것과 일관된다. 두 뷰가 계층에 대해 같은 말을 한다 — "관계는 보여주되 계산하지 않는다".

### D10. 그룹 순서는 사용자의 `order`가 먼저, 없으면 이름순

`compareGroupIds`가 스페이스·리스트 축에서 이름순으로 떨어지던 것을 고친다. `Project.order` / `List.order`가 있으면 그것을 쓰고, 없을 때만 이름순으로 폴백한다.

**보드와 타임라인이 같은 순서를 쓴다.** 순위 계산을 `domain/view`의 한 함수에 두고 두 뷰가 같이 호출한다 — 같은 데이터가 화면에 따라 다른 순서로 나오면 그건 두 개의 진실이다.

### D11. 줌 컬럼 수는 고정, 좁은 화면에서는 라벨만 축약

D3의 표가 확정값이다. viewport에 따라 창 길이를 바꾸지 않는다.

### D12. 완료 작업은 기본 표시하되 죽여서 그린다

기본 **표시**. 완료 막대는 opacity를 낮추고 완료 표시를 단다. 상단에서 **"완료 표시" 토글로 즉시 끌 수 있다**(`filter.done`).

숨김이 기본이면 "내가 이번 달에 뭘 했지"에 답할 수 없다. 표시가 기본이면 지나간 막대가 쌓이는데, 그건 한 번의 클릭으로 치울 수 있다. 되돌리기 쉬운 쪽을 기본으로 둔다.
