# 작업 스코프 일곱 곳 전부에 세 뷰를, 그리고 선택기는 `⋯` 안으로

작업 모듈(Tasks Module)의 요구 둘, 2026-09-03.

1. **목록 · 보드 · 타임라인을 작업 스코프 전부에서** 고를 수 있다. 지금은 기본함과 진짜
   리스트, 둘뿐이다.
2. **선택기는 머리에서 사라지고 `⋯` 안으로 들어간다.** 참조 앱의 `View` 구획 —
   머리말 한 줄과 아이콘 셋 — 과 같은 모양으로.

**범위**: 오늘 · 예정 · 기본함 · 리스트 · 폴더 · 태그 · 필터 **일곱**. 완료와 휴지통은
**하지 않는다**(사용자 결정). 포기(`wontDo`)도 같이 뺀다 — 끝난 일이고 사이드바가 완료와
같이 묶는 스코프라, 완료를 빼면서 그 쌍둥이만 넣을 이유가 없다. §2.4가 그 결정이 옳은
기술적 이유도 하나 들고 있다.

---

## 1. 지금 무엇이 막고 있나

### 1.1 [실측] 규칙은 한 곳에 있다

`src/domain/tasks/scopeRegistry.ts`. 아홉 스코프 중 **일곱이 `LIST_ONLY`**, 기본함과
리스트만 `ONE_LIST_VIEWS = ["list","board","gantt"]`다. 근거는 §5.45로 적혀 있다 —
"이 스코프가 **하나의 List의 화면인가**", 그리고 Gantt가 같은 조건으로 합류한 이유는
"여러 List를 모으는 스코프 위의 타임라인은 **행마다 다른 곳에 속한 막대**를 그린다".

레지스트리가 한 곳이라는 사실이 이 작업의 절반을 이미 해 놓았다. `resolveView`,
`isViewAllowed`, `parseTaskUrl`, `resolveListView`가 전부 이 표를 읽으므로 **표를 넓히면
주소와 저장된 기본 뷰가 같이 넓어진다.** 화면마다 손댈 곳이 없다.

### 1.2 타임라인 쪽 근거는 이미 스스로 무너져 있다

`src/domain/view/spaceViews.ts:58` — gantt의 `groupBy`는 **`"list"`** 다. 그 위의 주석이
직접 말한다: 행은 라벨과 막대뿐이라 List를 적을 칸이 없으므로 **그룹 머리말**이 그 자리다.

즉 §5.45가 걱정한 "어디 것인지 모를 막대"는 타임라인이 이미 리스트로 묶어 답하고 있다.
**타임라인은 일곱 스코프 어디서든 오늘 당장 옳게 그려진다** — `TaskGanttView`는 이미
스코프를 모르고(`queryScopeTasks`가 좁혀서 넘긴다), 렌더 분기도 하나다
(`TasksModule.tsx:1210`, 주석 §50C.29).

### 1.3 진짜 질문은 보드 하나다: **열이 무엇인가**

`TasksModule.tsx:703`:

```ts
const columns = scope.kind === "inbox"
  ? inboxColumns.map(...)                          // 사용자가 배열한 규칙 열
  : listBoardColumns(boardListId, listSections);   // 그 리스트의 섹션들
```

`boardListId`는 `scope.kind === "list"`일 때만 채워진다. 그래서 오늘·예정·폴더·태그·
필터에서 그냥 켜면 `listBoardColumns("", …)` → **기본 열 하나짜리 빈 칸반**이 뜬다.
켜는 것과 되는 것은 다르다.

---

## 2. 결정 — 보드의 열은 축이 셋이고, 스코프가 축을 고른다

### 2.1 세 축

| 축 | 어느 스코프 | 열 | 카드를 끌면 |
|---|---|---|---|
| `sections` | 리스트 | 그 리스트의 섹션(+기본 열) | `moveTaskToSection` (지금 그대로) |
| `inboxRules` | 기본함 | 사용자가 배열한 규칙 열 | `inboxDropMutation` (지금 그대로) |
| **`lists`** | **오늘 · 예정 · 폴더 · 태그 · 필터** | **그 스코프가 모은 리스트들** | **`moveTaskToList`** |

`lists` 축을 고른 이유는 셋이다.

- **드롭에 뜻이 생긴다.** "이 카드를 저 열로" = "이 작업을 저 리스트로". 이미 있는
  명령이고(`domain/tasks/mutations.ts:177`) **되돌리기까지 딸려 있다**
  (`labelKey: "tasks.undoMoved"`). 새 명령을 만들지 않는다. 그 함수는 리스트가 바뀌면
  `sectionId`를 지우는 것까지 이미 안다 — 섹션은 한 리스트의 것이므로(§6.28).
- **타임라인과 같은 축을 쓴다.** §1.2의 gantt가 리스트로 묶는다. 같은 스코프에서 보드와
  타임라인이 **같은 문장**을 말하게 된다 — 열 이름과 그룹 머리말이 같다.
- **폴더에서는 참조 앱과 같은 모양**이 된다. 폴더 보드의 열은 그 폴더의 리스트들이다.

기각한 축: **날짜**(오늘 스코프에선 열 하나에 다 몰린다), **우선순위**(아이젠하워
매트릭스가 이미 그 화면이다), **스코프마다 고르게 하는 UI**(§8).

### 2.2 `lists` 축의 열 집합은 스코프마다 한 줄 다르다

- **폴더**: 그 폴더의 **살아 있는 리스트 전부**. 비어 있어도 열을 그린다 — 폴더가 곧 그
  집합이고, 빈 열은 "여기로 옮길 수 있다"는 뜻이다.
- **오늘 · 예정 · 태그 · 필터**: **행이 하나라도 있는 리스트만**. 오늘 화면에 리스트
  스무 개가 빈 열로 서 있으면 읽을 수 없는 보드가 된다.
- 어느 쪽이든, 살아 있는 리스트로 풀리지 않는 행이 있으면 **맨 앞에 `리스트 없음` 열
  하나**(id `""`). 카드가 어느 열에도 없는 상태는 만들지 않는다 — 이 저장소가 매트릭스
  §23.3에서 "할 일 앱이 낼 수 있는 최악의 버그"라고 부른 것이다.
- 열의 차례는 **사이드바의 차례**다. 이미 `groupRank(…, { lists, folders })`가 타임라인에
  같은 답을 주고 있다(D10) — 두 뷰가 리스트를 다른 순서로 세우면 안 된다.

### 2.3 드롭이 아무것도 아닌 자리

`lists` 축에서 **자기 열로의 드롭은 순서 바꾸기뿐**이고, 그건 지금 코드가 이미
`from === columnId`로 처리한다. 그리고 이 다섯 스코프는 `canManualReorder: false`이므로
(레지스트리) 순서도 안 바뀐다 — `TaskBoard`가 그때 카드를 열 끝에 놓는 것도 이미
그렇게 쓰여 있다(`TaskBoard.tsx:454`).

즉 **드롭이 쓰는 것은 오직 `listId` 하나**다. 이 다섯 스코프에서 보드는 "무엇을 어디로
옮길까"만 답한다.

### 2.4 완료·휴지통을 뺀 이유는 취향이 아니다 [실측]

`TasksModule.tsx:936`의 `finishedRows`는 `queryScopeTasks(scope, ctx, { finished: true })`
다. 그런데 `scopeQuery.ts:214–232`를 보면 **완료·포기·휴지통 스코프는 `opts.finished`를
아예 읽지 않는다** — 그 스코프의 정의가 이미 끝난 일이기 때문이다. 그래서 거기 보드를
켜면 같은 작업이 **카드로 한 번, 열의 "완료" 묶음에서 또 한 번** 그려진다.

고칠 수는 있다(그 셋에서 `finishedRows`를 비우면 된다). 그런데 그 뒤에도 남는 것이 있다:
끝난 일과 버린 일을 **끌어서 리스트를 바꾸는** 화면이고, 그 셋은 `canCreate: false`라
열의 `+`도 꺼야 하며, 지금은 `⋯` 메뉴 자체가 없어서(`scopeViewOptions.ts:56`) 메뉴를
새로 띄워야 한다. **얻는 것보다 다는 것이 많다.** 그래서 셋은 목록 그대로 둔다.

---

## 3. 선택기는 `⋯` 안으로 — §13.4를 되돌린다

### 3.1 §13.4가 무엇을 근거로 머리에 뒀고, 무엇이 달라졌나

`SCOPE_VIEW_OPTIONS_DESIGN.md §13.4`는 선택기를 메뉴에서 **머리로 되돌렸다**. 근거 둘:

> 뷰 전환은 하루에 몇 번씩 있고, 그것을 두 번 누르게 만든 것이 2단계의 값이 아니었다.
> 그리고 머리를 아끼자던 §3.2의 근거가 아이콘에는 해당하지 않는다.

**첫 번째 근거는 그대로 유효하고, 이번 변경은 그 비용을 인정하고 치른다** — 뷰 전환이
한 번에서 두 번이 된다. 바뀐 것은 두 번째다:

- 지금까지 아이콘 셋은 **아홉 중 둘**에서만 떴다. 이제 **일곱**에 뜬다. 머리에서 32px
  사각형 셋이 상시 자리를 잡는다는 뜻이고, "아끼자던 근거가 해당하지 않는다"는 문장이
  성립하던 조건이 사라진다.
- **참조 앱 자신이 이것을 메뉴에 둔다.** 캡처의 `View` 구획이 그 답이고, 이 문서 계열이
  참조하는 기준은 그쪽이다. §3.2의 원래 Q6가 바랐던 모양이기도 하다.

### 3.2 메뉴 안의 모양

```
⋯
┌──────────────────────────┐
│  보기                     │   ← 머리말 (11px, 조용한 색)
│  ▤    ▥    ▤▬             │   ← 아이콘 셋 한 줄, 현재 것만 accent
│ ──────────────────────── │
│  ✓ 완료 숨기기             │
│  ✓ 상세 보기               │
│  보기 옵션                 │
└──────────────────────────┘
```

- 아이콘은 **머리에 있던 그 아이콘 그대로**(`ViewIcon`) — §13.4.1이 고른 24 viewBox ·
  stroke 1.9. 옮기는 것은 자리지 그림이 아니다.
- 접근성: 셋은 `role="menuitemradio"` + `aria-checked`이고, `role="group"`으로 묶어
  `aria-label="보기"`. 메뉴 안의 닫힌 선택 하나를 뜻하는 정확한 역할이며, 화살표 키가
  이미 도는 표면 안에 그대로 얹힌다. 라벨은 `aria-label`/`title`로 계속 들고 있다 —
  §13.4.1의 이유 그대로, 모양이 보이지 않는 사람에게 사각형 셋은 아무 말도 안 한다.
- 고르면 **메뉴가 닫힌다**. 뷰를 바꾸는 것은 화면을 바꾸는 일이고, 바뀐 화면 위에 메뉴가
  남아 있을 이유가 없다.
- 뷰가 하나뿐인 스코프(완료·포기·휴지통)에는 이 구획이 **없다**. 지금 머리에서
  `allowedViews.length > 1`이 하던 판단을 자리만 옮겨 그대로 한다.

### 3.3 `MoreMenu`가 배워야 할 것 둘 (그리고 배우지 말아야 할 것)

`kit.tsx`의 `MoreMenuItem`은 지금 `label` · `onClick` · `danger` · `separator` 넷이다.
여기에 **선택적** 둘을 더한다.

```ts
export interface MoreMenuItem {
  label?: string; onClick?: () => void; danger?: boolean; separator?: boolean;
  /** 이 아래 묶음이 무엇인지 말하는 한 줄. 참조 앱의 `View`. */
  heading?: string;
  /** 낱말 행이 아니라 아이콘 한 줄. 고르면 메뉴가 닫힌다. */
  choices?: { id: string; label: string; icon: ReactNode; selected: boolean; onClick: () => void }[];
}
```

둘 다 **선택적**이라 이 컴포넌트를 쓰는 다른 호출자(`kit.tsx:566`의 카드 머리, 작업 행의
`⋯`)는 한 글자도 바뀌지 않는다.

배우지 말아야 할 것: **뷰가 무엇인지**. `MoreMenu`는 아이콘과 콜백을 받을 뿐이고, 어떤
뷰가 있는지·무엇이 켜져 있는지는 지금처럼 `TasksModule`이 레지스트리에서 읽는다.
"어떤 컴포넌트도 제품 규칙을 다시 유도하지 않는다"는 레지스트리 첫 문단 그대로다.

### 3.4 `⋯`가 뜨는 조건은 한 글자 넓어진다

지금은 `scopeHasViewOptions(scope.kind) && scopeMenuItems.length > 0`
(`TasksModule.tsx:1165`). 완료·휴지통·포기를 뷰에서도 빼기로 했으므로 **이 조건은 그대로
쓴다** — 그 셋에는 메뉴가 계속 없고, 나머지 일곱에는 어차피 옵션이 있다.

즉 이 요구는 메뉴가 **뜨는 규칙**을 건드리지 않는다. 내용만 는다.

---

## 4. 주소 · 저장 — 새로 만들 것이 없다

- `?view=board`가 `/today`에서 **자동으로 유효해진다**. `parseTaskUrl` → `resolveView`가
  레지스트리를 읽기 때문이고, 반대로 지금 이 순간 `/today?view=board`는 조용히 목록으로
  떨어진다(§5.46).
- 리스트의 저장된 기본 뷰(`List.defaultViewKey`)도 같은 표를 통과한다
  (`domain/tasks/listView.ts:52`). 넓히면 같이 넓어진다.
- **일곱 스코프의 `defaultView`는 전부 `list` 그대로.** 이번 변경은 무엇을 고를 수 있는지를
  넓히는 것이지, 아무의 첫 화면도 바꾸지 않는다.
- 새 설정 없음. 뷰는 지금처럼 **주소**에 있고(`?view=`), 스코프별 기억은 이미 있는
  `defaultViewKey` · `scopeViewOptions`의 몫이다.

---

## 5. 단계

1. **`boardAxis`** — `domain/tasks/boardAxis.ts`: `axisFor(scope)`, `lists` 축의 열 만들기,
   `columnOf`, 드롭 뮤테이션. 순수 함수 + 유닛. 화면은 아직 안 건드린다.
2. **레지스트리 확장** — 일곱 스코프의 `allowedViews`를 `TASK_VIEW_KINDS`로.
   `canManualReorder` · `canCreate`는 손대지 않는다.
3. **`TasksModule` 배선** — `columns` · `columnOf` · `dropOnBoard` · `createInColumn`의 세
   갈래를 1단계의 축 하나로 좁힌다.
4. **선택기 이사** — `kit.tsx`의 `heading`/`choices`, 머리의 `.tm-views` 제거, 메뉴 조립.
5. **시험과 문자열** — §6.

2단계만 나가면 빈 칸반이 뜨므로 **2와 3은 한 커밋**이다.

---

## 6. 깨질 시험, 새로 붙일 시험

깨지는 것(알고 하는 것):

- `src/components/tasks/taskActionsMenu.test.tsx:521–547` — `.tm-header .tm-views`를 직접
  찾고, 뷰 버튼의 `aria-pressed`를 읽고, 뷰가 하나인 스코프에서 `.tm-views`가 **없음**을
  주장한다. 셋 다 메뉴 안으로 옮긴다.
- `e2e/contextMenu.spec.ts:161` — `getByRole("button", { name: "Board" })`. `⋯`를 먼저 연다.
- `e2e/addList.spec.ts`의 `.tm-modal .tm-view`들은 **그대로 산다** — 리스트 만들기 모달의
  낱말 선택기는 다른 화면이고, §13.4.2가 `.is-icon` modifier로 갈라 둔 덕이다.

새로 붙일 것:

1. 일곱 스코프가 세 뷰를 허용하고 **완료·포기·휴지통은 계속 목록뿐**이다 (레지스트리를
   값으로 훑는다 — `TASK_SCOPE_KINDS` × `TASK_VIEW_KINDS`)
2. `lists` 축: 폴더는 빈 리스트도 열로 세우고, 태그는 행이 있는 리스트만 세운다
3. 살아 있는 리스트가 없는 행은 `리스트 없음` 열에 들어간다 (어느 열에도 없는 카드 0)
4. `lists` 축의 드롭이 `moveTaskToList`를 부르고 되돌리기가 붙는다
5. 자기 열로의 드롭은 아무것도 쓰지 않는다
6. `⋯` 안의 `보기` 셋이 `menuitemradio` + `aria-checked`이고, 고르면 주소의 `?view=`가
   바뀌고 메뉴가 닫힌다
7. 완료·휴지통의 `⋯`는 여전히 없다 (이 변경이 그 셋을 건드리지 않았다는 잠금)

## 7. 검증 계획

- 유닛 전체 + `npx tsc -b` (릴리스 빌드가 test 파일까지 타입체크한다)
- E2E 전체 (뷰 전환 경로가 두 곳에서 바뀐다)
- 실제 앱(dev, ko): 일곱 스코프를 하나씩 열어 `⋯` → 보드 → 타임라인
  - 폴더: 열이 그 폴더의 리스트들인지, 빈 리스트도 서 있는지
  - 태그: 행이 있는 리스트만 서 있는지, 카드를 옮기면 리스트가 바뀌고 되돌리기가 뜨는지
  - 오늘: 열 차례가 사이드바와 같은지
  - 완료·휴지통: 머리에도 메뉴에도 뷰 선택이 없는지
- 주소: `/today?view=board`를 직접 치고 새로고침해도 보드인지

## 8. 이번에 하지 않는 것

- **칸반 기준 고르기 UI**("리스트/날짜/우선순위"). 축은 스코프가 정한다. 필요해지면
  `scopeViewOptions`에 값 하나로 붙일 자리가 이미 있다.
- **태그·우선순위 축.**
- **`lists` 축 열의 이름 바꾸기·추가.** 그 열은 리스트라는 기록이고, 이름은 사이드바가
  가진다.
- **완료 · 포기 · 휴지통** (§2.4).
- **캘린더 뷰.** `SpaceViewId`에는 있지만 이 모듈의 `TaskViewKind`에는 없다.
---

## 9. 구현 결과 (2026-09-03)

다섯 단계를 순서대로 냈다. 설계에서 **벗어난 것**과 **설계가 예상하지 못한 것**만 적는다.

### 9.1 1단계 — `domain/tasks/boardAxis.ts`

설계 그대로. 순수 함수 다섯(`boardAxisFor` · `listAxisColumns` · `listAxisColumnOf` ·
`moveToListColumn` · `createInListColumn`)과 유닛 14개.

한 가지 정확해진 것: `리스트 없음` 열에 실제로 도달하는 경우는 **`listId`가 아무 리스트도
가리키지 않는 행**이다. 리스트가 보관·삭제된 작업은 `isTaskActive`가 활성 스코프에서 아예
빼기 때문이다(`return !owner || …`). 함수 자체는 그 사정을 모르고 **손에 쥔 행이 필요로
하는 것**만 답한다 — 주석을 그렇게 고쳤다.

### 9.2 2단계 — 레지스트리

일곱 스코프가 `ALL_VIEWS`. 옛 규칙을 고정하던 시험 **여덟 개**가 옳게 깨졌고, 전부 새
규칙 + **"끝난 일은 목록뿐"** 을 함께 주장하도록 옮겼다(`scopeRegistry.test.ts` ·
`listView.test.ts` · `commands.test.ts` · `taskScopeUrl.test.ts`). 절반이 무효가 된 시험을
지우는 대신 **살아남은 절반이 무엇인지 적는** 편을 택했다 — 다음에 누가 종결 스코프까지
넓히면 그 시험이 막는다.

### 9.3 3단계 — 배선, 그리고 **설계에 없던 정리 하나**

네 갈래(`columns` · `columnOf` · `dropOnBoard` · `createInColumn`)가 `axis` 하나를 읽는다.

설계가 예상하지 못한 것: **§13.x Q4의 `Task Time` 두 줄이 죽은 코드가 됐다.** 그 두 줄은
"보드가 없는 스코프에서는 대화상자가 한 줄짜리가 되니 메뉴로 꺼낸다"였는데, 이제 **`⋯`가
있는 스코프는 전부 보드가 있다.** 그래서 조건 분기를 지우고 대화상자 한 줄만 남겼다.
따라온 것 둘:

- `ScopeViewOptionsDialog`의 `hasBoard` prop도 같이 나갔다 — 그 대화상자를 열 수 있는
  스코프는 전부 보드가 있으므로 **항상 참인 분기**였다.
- `tasks.dateByMenu.*` 키 둘이 고아가 되어 두 언어에서 지웠다(§32.4의 고아 0 시험이 바로
  잡는다).

`Task Time`의 문이 둘에서 하나로 줄었다.

### 9.4 4단계 — 선택기 이사, 그리고 **키보드 한 줄**

`MoreMenuItem`에 `heading`/`choices` 둘을 더했고 다른 호출자는 그대로다.

설계가 놓쳤던 것: **`moveMenuFocus`가 `[role='menuitem']`만 걸었다.** 뷰 셋을
`menuitemradio`로 만들면 화살표 키가 **그 줄을 그냥 건너뛴다** — 키보드로 온 사람이 바꾸러
온 바로 그 줄을. 공용 헬퍼의 선택자를 세 역할 전부로 넓혔다(`menuitem` ·
`menuitemradio` · `menuitemcheckbox`). ARIA에서 셋 다 메뉴 항목이므로 이건 확장이 아니라
빠져 있던 것이다.

문자열: `tasks.viewLabel`의 값을 `Switch view` → **`View`/`보기`** 로 바꿔 **보이는 머리말과
그룹의 접근성 이름 하나로 겸했다.** 키를 새로 만들고 옛 키를 고아로 남기는 것보다 낫다.

CSS: `.tm-view.is-icon`(머리의 아이콘 선택기 전용)은 지웠고, 리스트 만들기 모달이 쓰는
`.tm-view`는 그대로다 — §13.4.2가 modifier로 갈라 둔 덕에 지울 것이 한 규칙뿐이었다.

### 9.5 5단계 — 시험

- 새 파일 둘: `boardAxis.test.ts`(14) · `taskBoardListAxis.test.tsx`(5, 배선)
- 옮긴 것: 헤더 선택기 시험 넷 → 메뉴, E2E 둘(`contextMenu.spec.ts`의 `Board` 버튼,
  `addList.spec.ts`의 "오늘은 타임라인을 금지한다" → **휴지통**으로)
- 유닛 **2,371개 통과**(신규 19) · `npx tsc -b` 통과
- E2E **140 통과 · 실패 0**(4.1분, mobile 프로젝트 160 skip — 기존 기준선과 같다)

### 9.6 실제 앱에서 확인한 것 (dev, ko, 1280×800)

| 화면 | 결과 |
|---|---|
| `/today?view=board` | 열이 `기본함 · Work · Home` — **행이 있는 리스트만**, 사이드바 차례대로 |
| 위에서 카드를 Work 열로 드롭 | `listId`가 `list-work`로 바뀌고 **되돌리기 토스트**가 떴다. Home 열은 비면서 사라졌다 |
| `/folder/fld-1?view=board` | `Work · Empty` — **빈 리스트도 열로 선다** |
| `/tag/tag-1?view=gantt` | 막대가 `Work` · `Home` **그룹 머리말 아래** — §1.2가 예측한 그대로 |
| `/upcoming?view=board` | 리스트 열 |
| `/trash` | 머리에 버튼이 **하나도 없다** — `⋯`도 뷰 선택기도 없음 |
| `⋯` 메뉴 | `보기` 머리말 + 아이콘 셋(현재 것만 accent) + `완료 숨기기 · 상세 보기 · 보기 옵션` |
