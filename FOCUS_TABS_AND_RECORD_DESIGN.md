# 타이머 · 포모도로 · 기록 — 셋을 같은 높이에 둔다

> 상태: **분석·설계. 구현 전** · 2026-09-10
> 요청: 첨부 목업(`focusflow_timer_pomodoro_record.html`)대로 만든다.
> 대상: `components/FocusPage.tsx`(1,408줄) · `styles/24-focus.css`(898줄)
> 선행 문서: `SPATIAL_FOCUS_FIELD_DESIGN.md`(직전 설계 — **이 문서가 대체한다, §1.1**) ·
> `FOCUS_LAYOUT_DESIGN.md` · `SWISS_MINIMAL_DESIGN.md`(자)
> 가드: `e2e/radiusScale.spec.ts` · `e2e/surfaceHierarchy.spec.ts` · `src/styles/scale.test.ts` ·
> `e2e/focusTimerV2.spec.ts` · `e2e/focusDetail.spec.ts` · `e2e/globalFocusBar.spec.ts`

---

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **실측** | 목업을 브라우저에서 띄워 잰 값 · 저장소의 데이터 모델·스펙·토큰 | [실측] |
| **관찰** | 목업 세 장이 보여주는 의도 | [관찰] |
| **결정** | 사용자 — 이 IA와 이 기록 화면 | [결정] |
| **추론** | 목업이 그리지 않은 것(빠진 기능들 · 상태 전이 · 좁은 폭) | [추론] |

---

## 1. 목업이 바꾸는 것은 배치가 아니라 **정보 구조**다 [관찰]

앞의 두 목업은 한 화면의 배치를 바꿨다. 이번 것은 **화면의 개수와 층위**를 바꾼다.

```
타이머 · 포모도로 · 기록          ← 셋이 같은 높이의 탭
```

지금은 이렇게 되어 있다: 측정 방식(스톱워치/포모도로)은 타이머 화면 **안의 토글**이고,
기록은 머리에 있는 **버튼**이 화면을 통째로 갈아끼운다. 즉 세 개가 서로 다른 층위에
흩어져 있다. 목업은 셋을 한 줄에 세운다.

**이건 개선이다.** 직전 평가에서 "`Records`는 실제로는 페이지를 통째로 바꾸는 탭인데
툴바 버튼처럼 생겼다(h1까지 바뀐다)"고 적었는데, 탭 줄이 그 문제를 정면으로 없앤다.

### 1.1 직전 설계와의 관계

`SPATIAL_FOCUS_FIELD_DESIGN.md`는 **원 · 3열 · 바닥 리본**을 설계했다. 이번 목업은
그 셋을 다 쓰지 않는다.

| | 직전 설계 | 이번 목업 |
|---|---|---|
| 타이머 화면 | 원 + 앵커 + 큐 + 리본 | 숫자 하나. 그 외 아무것도 없음 |
| 요약 | 바닥 전폭 리본 | 기록 탭의 KPI 4개 |
| 기록 | "정렬만 고친다" | **대시보드로 확장** |

**이 문서가 직전 설계를 대체한다.** 다만 직전 설계에서 살아남는 것이 셋 있고, 그것이
이 설계의 뼈대가 된다 — ① 앵커(원 위에 있던 그 요소), ② 몰입을 별도 오버레이가 아니라
감쇄로 처리한다는 결론, ③ 직전 평가의 Phase 0 수정들. 원과 리본은 버린다.

> `SPATIAL_FOCUS_FIELD_DESIGN.md`의 상태 줄에 "이 문서가 대체됨"을 적는다.

---

## 2. 목업이 **그리지 않은 것**이 이 설계의 본체다 [추론]

목업은 화면 셋을 그린 것이지 기능 목록이 아니다. 지금 앱에 있는 것 중 목업에 자리가
없는 것이 다섯이고, 각각 "없앤다"가 아니라 "어디에 두나"의 문제다.

| 빠진 것 | 지금 어디에 | 제안하는 자리 |
|---|---|---|
| **작업 선택** | 피커 버튼 | 목업의 `current-task`가 **그 자리다**. 읽기 전용 텍스트를 누를 수 있게 만든다 — 앵커(§4.1) |
| **작업 큐** | 오른쪽 열 | 오른쪽 **드로어**. 목업이 세션 상세용으로 이미 그 기구를 갖고 있다(§4.4) |
| **휴식** | 포모도로 플로우 | 포모도로 탭의 **한 상태**. `집중 세션 · 25분` 라벨이 `짧은 휴식 · 5분`이 되는 자리(§4.3) |
| **몰입 보기** | 머리 버튼 | `•••` 메뉴로. 탭 줄이 이미 조용해서 몰입의 값이 줄었지만, 전체화면은 남긴다 |
| **결과 화면** | 종료 후 전용 화면 | 타이머/포모도로 탭의 **한 상태**(§4.2) |

목업의 `세션 종료`는 시계를 `00:00`으로 되돌리고 끝난다. 실제로는 그 순간이 **기록이
생기는 순간**이라 화면이 무언가 말해야 한다.

---

## 3. 탭이 만드는 새 문제 — 세션이 도는 중의 탭 전환 [추론]

지금은 이 문제가 없다. 측정 토글이 **세션이 없을 때만 렌더**되기 때문이다
(`FocusPage.tsx`: `{!activeSession && !isBreak && !result && …}`). 탭 줄은 항상 보이므로
"스톱워치가 도는 중에 포모도로 탭을 누르면?"에 답해야 한다.

| | 무엇 | 대가 |
|---|---|---|
| **A. 상대 측정 탭을 비활성** | 도는 동안 다른 측정 탭은 누를 수 없고, 이유를 말한다 | 탭이 가끔 안 눌린다. 대신 데이터가 뒤섞이지 않는다 |
| B. 확인 후 전환 | "현재 세션을 끝내고 바꿀까요?" | 집중 중에 대화상자가 뜬다 |
| C. 뷰만 전환 | 포모도로 탭에서 스톱워치 세션이 보인다 | 탭 이름이 거짓말이 된다 |

**추천: A.** 비활성 자체보다 **이유를 말하는 것**이 핵심이다 —
`집중 중에는 측정 방식을 바꿀 수 없어요. 종료한 뒤 바꿔 주세요.` 지금 앱이 토글을
아예 숨겨서 하던 말을, 탭에서는 문장으로 한다.

**기록 탭은 언제나 눌린다.** 도는 세션을 보면서 기록을 볼 이유가 있고, 그때 전역 집중
바(`.foc-global-bar`)가 세션을 계속 들고 있다 — 이미 그 일을 하고 있는 기구다.

---

## 4. 화면별 설계

### 4.1 타이머 탭

목업의 세로 스택을 그대로 쓰되, 두 줄을 조작 가능하게 만든다.

```
지금, 한 가지에 집중하세요.        ← 유휴에만. 세션 중에는 사라진다
[ ● 연구 제안서 — 방법론 수정 ⌄ ]   ← 앵커. 누르면 피커. 세션 중에는 읽기 전용
27:14                              ← 시계
● 집중 중                          ← 상태
[ 일시정지 ]                       ← 주 버튼 하나
세션 종료                          ← 보조. 중립색 (danger 아님)
```

| 상태 | eyebrow | 앵커 | 상태 줄 | 주 버튼 | 보조 |
|---|---|---|---|---|---|
| 유휴 | 안내 문구 | 작업 선택 ⌄ | — | **집중 시작** | 큐 열기 |
| 실행 중 | 숨김 | 작업명(읽기 전용) | ● 집중 중 | **일시정지** | 세션 종료 · 메모 |
| 일시정지 | 숨김 | 〃 | ● 일시정지됨 | **재개** | 세션 종료 · 메모 |
| 복구됨 | 숨김 | 〃 | ● 복구됨 | **재개** | 공백 확인 · 세션 종료 |
| 결과 | 숨김 | 작업 연결 ⌄ | ● 기록되었어요 | **다시 집중** | 작업 완료 · 마치기 |

**작업명은 앵커에만 있다.** 직전 설계에서 앵커와 원 안에 작업명을 둘 다 적었다가
같은 문자열이 두 번 나오는 것을 렌더에서 보고 고쳤다. 그 교훈은 이 배치에서도 같다.

### 4.2 결과는 화면이 아니라 상태다

지금은 결과가 `focus-result`라는 별도 블록이고, 액션이 넷이다. 여기서는 **같은 자리에
값만 바뀐다** — 시계 자리에 누적 시간, 상태 줄에 `기록되었어요`, 주 버튼에 `다시 집중`.
자리가 유지되면 종료가 화면 전환으로 느껴지지 않는다.

### 4.3 포모도로 탭 — 휴식이 같은 화면의 한 상태다

목업의 포모도로는 카운트다운 + 진행 막대 + `시작`/`초기화`뿐이다. 실제 플로우는
`focus → break_ready → break_running/break_paused → focus`이고 긴 휴식 주기가 있다.
**라벨 한 줄과 막대 색만 바뀌면 같은 화면이 전부 담는다.**

| 상태 | 라벨 | 막대 | 주 버튼 | 보조 |
|---|---|---|---|---|
| 준비 | 집중 세션 · 25분 | 0% | **시작** | 설정 |
| 집중 중 | 집중 세션 · 25분 · 2/4 | 진행 | **일시정지** | 세션 종료 |
| 휴식 준비 | 짧은 휴식 · 5분 | 0% | **휴식 시작** | 건너뛰기 |
| 휴식 중 | 짧은 휴식 · 5분 | 진행 | **일시정지** | 휴식 종료 |
| 긴 휴식 | 긴 휴식 · 15분 | 진행 | 〃 | 〃 |

목업의 `초기화`는 뺀다 — 지금 앱에 없는 개념이고, 있으면 "기록을 지우는가"라는 질문이
생긴다. 진행 막대는 **휴식일 때 색을 바꾸지 않는다**(§5.3: 색은 상태가 아니라 정체성을
나른다). 라벨이 이미 무엇인지 말한다.

### 4.4 큐는 드로어로 간다

목업은 세션 상세용 오른쪽 드로어(`.drawer`, 390px)를 이미 갖고 있다. 큐를 같은 기구에
넣으면 새 관용구를 만들지 않고, 타이머 화면의 빈 캔버스를 지키면서, 큐의 기능(추가 ·
드래그 정렬 · 제거 · 그 작업으로 시작)을 전부 보존한다.

- 유휴 상태의 보조 버튼 `큐 열기 (3)` — 개수를 달아 존재를 알린다
- 세션 중에도 `•••`에서 열린다
- 드로어 안의 행 구조는 지금 것을 그대로 쓴다(손잡이 · 시작 · 제목 · 리스트 칩 · × )

> 대안: 타이머 탭 하단의 접이식. 캔버스를 침범하고 접힘 상태를 저장해야 해서 드로어보다
> 비싸다. 큐를 아예 없애는 선택지는 §8의 D2로 남긴다.

---

## 5. 기록 탭 — 여기서부터는 차트다

지금 기록 뷰는 필터 셋 + 통계 넷 + 목록이다. 목업은 그것을 대시보드로 만든다.
`FOCUS_LAYOUT_DESIGN.md` §10.1이 "차트가 아니다"라고 결정한 것은 **요약 타일 셋**에
대한 판단이었고, 여기서는 시간축과 비교 대상이 실제로 생기므로 그 결정과 충돌하지 않는다.

### 5.1 데이터는 이미 있다 [실측]

`FocusSession`(`types.ts:632`)이 `segments` · `startAt`/`endAt` · `taskId` ·
`accumulatedSeconds` · `measurementMode` · `endReason`을 들고 있다. 목업의 다섯 조각이
전부 이 위에서 계산된다.

| 조각 | 필요한 것 | 있는가 |
|---|---|---|
| KPI 4 | 합계 · 개수 · 평균 · 최댓값 | ✅ 지금 화면이 이미 셋을 계산한다 |
| 하루 타임라인 | 세그먼트의 시작·끝 | ✅ `segments` |
| 작업별 막대 | `taskId`별 합계 | ✅ |
| 4주 히트맵 | 날짜별 합계 | ✅ |
| 7일 추이 | 〃 | ✅ |
| **"최근 평균 대비 +12%"** | **비교 기준의 정의** | ❌ §5.5 |

### 5.2 형태 — 조각마다 하는 일이 다르다

| 조각 | 형태 | 하는 일 |
|---|---|---|
| KPI 4 | 스탯 타일 | 헤드라인 숫자. 차트가 아니다 |
| 하루 타임라인 | 시간축 위의 구간 막대 | **언제** 집중했나 |
| 작업별 | 가로 막대, 값 내림차순 | **어디에** 썼나 |
| 4주 히트맵 | 순차 램프 격자 | **꾸준한가** |
| 7일 추이 | 선 | **늘고 있나** |

세 번째 시안이 **KPI 넷 중 첫째를 크게** 만들었다. 채택한다 — dataviz의 "뷰당 히어로
숫자 하나"이고, 넷이 같은 크기면 무엇을 먼저 볼지 화면이 말하지 않는다. `집중 시간`이
그 하나다(`--type-2xl`, 나머지 셋은 `--type-xl`).

시안이 각 지표에 아이콘을 달았는데 **뺀다.** `☀`가 "오늘의 집중 시간"을 뜻하는 연결이
없고, 라벨이 이미 그 말을 한다 — 라벨과 중복인 아이콘은 장식이다.

새로 생긴 컨트롤 둘은 정의가 필요하다.

| 컨트롤 | 무엇을 바꾸나 | 판단 |
|---|---|---|
| 세션 목록의 `최신순 ⌄` | 정렬: 최신순 · 오래된순 · 긴 순 | **채택** |
| 집중 패턴의 `주간 보기 ⌄` | 시안이 말하지 않는다 | **뺀다.** 히트맵이 이미 주 단위이고, 기간은 위의 기간 칩이 정한다 |

### 5.3 색 — 마크는 단색이고, 색은 태그 칩만 진다

두 번째 시안은 세션에 `blue · violet · green`을 **행 순서로** 줬다
(`data-session="0"→blue, 1→violet, 2→green`). 세 번째 시안은 파랑의 농담(연·중·진)을
쓰는데, 구간 길이가 34m·38m·30m인 것과 농담이 대응하지 않는다 — 둘 다 **색이 아무것도
인코딩하지 않는** 경우다. 그리고 순위로 색을 주면 날짜를 하루 넘길 때 같은 작업이
다른 색이 된다.

그래서 "그럼 태그 색을 쓰자"가 자연스러운 다음 수인데, **그것도 안 된다. 잰 결과다.**
태그의 색은 사용자가 프리셋에서 고른 것이고, 그 셋을 카테고리 팔레트로 검증하면:

```
$ node scripts/validate_palette.js "#0064d2,#af52de,#34c759" --mode light
  [FAIL] CVD separation   worst adjacent #af52de↔#0064d2 ΔE 2.6 (protan)
  [WARN] Contrast vs surface   below 3:1: [["#34c759", 2.16]]
```

적록색약에서 파랑과 보라의 ΔE가 **2.6**이다. 6–8 구간이면 보조 인코딩을 붙여 쓸 수라도
있는데 2.6은 그 아래다. 게다가 이건 이 세 색만의 문제가 아니다 — **사용자가 파란 계열
태그를 셋 만들면 어떤 팔레트도 통과할 수 없다.** 사용자가 정하는 색은 애초에 검증
대상이 될 수 없다.

> **결론.**
> · **타임라인 구간 · 작업별 막대 = 단색**(액센트 하나). 구간은 **위치**로, 막대는
>   **이름**으로 이미 구별된다 — 색이 할 일이 없다.
> · **히트맵 = 한 색 순차 램프** + 스케일 범례.
> · **색은 태그 칩만 진다.** 칩은 이름을 달고 있어서 색만으로 구별하지 않으므로
>   CVD 검사의 대상이 아니다. 기존 `.tm-tag-chip`과 `tagChipColors()`를 그대로 쓴다 —
>   두 테마를 이미 답하고, 새 관용구를 만들지 않는다.

그밖에:

- **막대의 그라디언트를 뺀다** — 그라디언트가 인코딩하는 것이 없다.
- **히트맵은 한 색 light→dark로 유지한다** — 목업이 이미 맞다. 다만 **스케일 범례**를
  단다(`적음 ▢▢▢▢ 많음`).
- **텍스트는 텍스트 토큰을 입는다.** 값·라벨·범례는 잉크색이고, 색은 마크만 진다.
- 팔레트를 실제로 쓰기로 하면 눈으로 판단하지 않고
  `scripts/validate_palette.js`로 검증한다.

### 5.4 호버가 기본이다

목업의 타임라인 구간은 클릭만 되고 툴팁이 없다. 선 그래프는 y축 값이 없어서 요일별
값을 읽을 방법이 없다. 셋 다 호버 레이어로 해결한다.

- 타임라인 구간 · 히트맵 칸 · 막대: 마크별 툴팁(작업 · 시각 · 길이)
- 7일 추이: 크로스헤어 + 툴팁. 값 라벨은 끝점 하나만
- 히트 타깃은 마크보다 크게 — 구간이 9px이면 타깃은 최소 24px

### 5.5 "최근 평균 대비 +12%"는 정의가 필요하다

무엇 대비인지 화면이 말하지 않으면 숫자가 아니라 장식이다. 셋 중 하나를 고른다:
직전 7일 평균 · 직전 4주 같은 요일 평균 · 직전 동일 기간. **추천: 직전 동일 기간**
(오늘↔어제, 이번 주↔지난주). 기간 탭과 뜻이 맞고, 라벨에 그대로 쓸 수 있다 —
`어제보다 +12%`. 데이터가 부족하면 숫자 대신 `비교할 기록이 아직 없어요`.

### 5.6 빈 상태

목업에 없다. 기록이 0인 계정에서 타임라인·히트맵·추이는 **그릴 것이 없는 격자**가 된다.
KPI는 `—`, 타임라인·차트는 자리를 유지한 채 한 줄 안내. 격자만 남기고 비우는 것이
카드를 통째로 숨기는 것보다 낫다 — 다음에 무엇이 생길지 형태가 말해준다.

---

## 6. 시각 언어 — 세 번째 목업도 같은 충돌이다 [실측]

| 목업이 쓰는 것 | 값 | 우리 자 | 판정 |
|---|---|---|---|
| 모서리 | 28·22·18·16·14·13·12·11·10·5px | I1-B `[4]` | **반전** |
| 글자 크기 | 8·9·10·19·21·22·31px | §5.1의 7단 | **반전** |
| 그림자 | 상태 점의 파란 링, 카드 그림자 | §11.38 색 없음 · 떠 있는 것만 | **반전**(드로어는 예외 — 떠 있다) |
| `backdrop-filter`, 방사 그라디언트 배경 | 탭바·타이머 캔버스 | 없음 | **반전** |
| 막대 그라디언트 | `linear-gradient(90deg,…)` | — | **반전**(§5.3) |
| **글자 굵기** | 420·430·520·560·650·680·720·740·760 | 400·600·700 | **사실상 무해** ↓ |
| 시계를 얇게(420) | — | `--weight-normal: 400` | **반전 아님** |
| 3탭 IA · 대시보드 배치 | — | 자에 규정 없음 | **반전 아님** |

### 6.1 굵기 아홉은 세 개로 접힌다 [실측]

이 앱은 Pretendard **세 페이스만** 로드한다(`01-base.css:19–38`, 400/600/700).
CSS 폰트 매칭이 420·430 → **400**, 520·560 → **600**, 650~760 → **700**으로 접으므로
목업이 브라우저에서 실제로 그린 굵기도 셋뿐이다. 즉 이 행은 "반전"이 아니라 **적는
방식의 문제**다 — 400/600/700로 적으면 렌더 결과가 같다.

그리고 그 사실이 하나를 드러낸다: **목업의 시계는 얇다**(420 → 400). 지금 앱은 600이다.
큰 숫자를 얇게 쓰는 것은 자 안에서 가능하고, 목업 인상의 큰 부분이다. **채택한다.**

### 6.2 시계 크기

목업은 `clamp(104px, 12vw, 170px)`, 지금은 `--focus-clock-display: clamp(66px, 7.2vw, 96px)`.
시계는 이미 타입 스케일 밖의 디스플레이 토큰이므로(§14 주석) 새 값을 발명하는 것이
아니라 **그 토큰을 올린다**: `clamp(88px, 10vw, 140px)`. 170은 1024×720에서 다른 요소를
밀어내고, 88 하한은 좁은 폭에서 `00:00`이 한 줄에 들어가는 값이다.

### 6.3 레일은 그대로 둔다

목업에는 왼쪽 레일이 없다. 앱의 `.global-rail`은 앱 셸의 것이고
`surfaceHierarchy.spec.ts`가 **레일 < 캔버스** 명도 순서를 두 테마에서 검사한다.
탭 줄은 창이 아니라 **페이지 영역의 가운데**에 선다.

---

## 7. 격자와 치수

### 7.1 탭 줄

```
.focus-tabs   높이 56(≥1024) / 48(<1024). 아래 헤어라인 하나.
              트랙 --surface-2, 선택 --bg-surface + 헤어라인, 모서리 4px.
              페이지 영역 기준 가운데. 오른쪽 끝에 ••• (절대 위치가 아니라 3열 격자).
```

목업은 `•••`를 `position:absolute; right:26px`로 둔다. 탭 줄이 가운데 정렬이므로 폭이
좁아지면 탭과 겹친다. **`1fr · auto · 1fr` 격자**로 두면 가운데가 유지되면서 겹치지 않는다.
직전 설계에서 같은 이유로 같은 격자를 썼다.

### 7.2 기록 탭

세 번째 시안이 `집중 패턴`을 오른쪽 열에서 꺼내 **바닥 전폭**으로 내렸다. 히트맵(7열)과
추이 선이 340px 열에서는 둘 다 좁았으므로 옳은 이동이다. 그래서 여섯 층이 된다.

```
.focus-record        grid-template-rows: auto auto auto auto auto auto; gap 16
 ├ 제목 줄            h1 기록               | ‹ 📅 2026년 9월 10일 (목) ›
 ├ 기간 칩            오늘 · 이번 주 · 이번 달 · 전체
 ├ KPI 카드           4열. 첫째가 히어로, 나머지 셋은 같은 크기
 ├ 타임라인           카드. 2시간 눈금 06:00–24:00
 ├ 2열                minmax(0,1.4fr) | minmax(380px,1fr)
 │   ├ 세션 목록       정렬 ⌄
 │   └ 작업별 막대     값 + 비율
 └ 집중 패턴 (전폭)    4주 히트맵 + 범례 | 최근 7일 추이(y축·마커·끝점 값)
```

**여기서 스크롤 모델이 바뀐다.** 카드가 여섯 층이면 1440×900에 들어가지 않는다.
`§7.1`의 타이머·포모도로 탭은 뷰포트에 갇힌 채로 두고, **기록 탭에서는 페이지가 흐른다** —
`main`의 `hidden auto`가 그 일을 한다(`FOCUS_LAYOUT_DESIGN.md` §3의 교훈 1·2가 정확히
이 경우를 위해 적힌 것이다). 세션 목록의 안쪽 스크롤러는 **없앤다**: 스크롤러 둘이
겹치면 휠이 어디로 갈지 사용자가 예측할 수 없다.

### 7.2.1 제목 줄은 기록 탭에만 있고, 부제는 없다

시안이 `기록` h1과 부제("집중이 만들어가는 변화를…")를 되살렸는데 **부제는 뺀다.**
아래 카드 넷이 각자 자기 부제를 이미 달고 있어서(`하루 중 집중이 있었던 시간을…`,
`각 세션에서 어떤 작업에…`) 페이지 부제는 그 위에 한 겹 더 얹히는 설명이고, 화면이
무엇인지는 탭 이름과 h1이 이미 두 번 말한다.

타이머·포모도로 탭에 제목 줄을 두면 §4.1이 비워둔 캔버스가 무너지므로, **제목 줄은
기록 탭의 것**이다. 탭 이름과 h1이 둘 다 "기록"인 중복은 감수한다 — 탭은 앱 층위의
이동이고 h1은 그 페이지가 무엇인지다. h1까지 빼면 날짜 내비가 기댈 줄이 없어진다.

### 7.2.2 탭 줄은 가운데 알약 그대로다

세 번째 시안의 기록 화면은 탭 줄도 왼쪽 정렬 밑줄 방식으로 그리는데, **그것은
채택하지 않는다.** 탭 줄은 기록 탭의 것이 아니라 셋 위에 있는 것이고, 기록 화면
하나 때문에 세 탭 공통의 크롬을 바꾸는 것은 범위를 넘는다. §7.1의 가운데 알약을
`1fr·auto·1fr` 격자와 함께 유지한다.

기록 탭에 제목 줄이 생기면서 가운데 알약과 h1이 각각 중심을 갖는 것은 사실이다.
다만 그 둘은 층위가 달라서(탭은 앱 층위의 이동, h1은 이 페이지가 무엇인지) 경쟁으로
읽히지 않는다 — 타이머·포모도로 탭에는 h1이 아예 없으므로 알약이 유일한 중심이고,
그 일관성이 기록 탭 하나의 균형보다 크다.

**검색·알림 아이콘은 넣지 않는다.** 둘 다 레일에 이미 있고, 알림은
`RAIL_SYNC_AND_NOTIFICATIONS_DESIGN.md` §8이 설계하고 `e2e/railNotifications.spec.ts`가
검사한다. 같은 것이 두 곳에 있으면 어느 쪽이 진짜인지 알 수 없다. 탭 줄 오른쪽은
`•••` 하나다.

### 7.3 좁은 폭 (<1024) [추론 — 목업은 포기한다]

목업은 `@media(max-width:1050px)`에서 `body{overflow:auto}`로 문서 스크롤에 넘긴다.
같은 방향으로 간다(카드 격자를 유지할 높이가 없다). 다만:

- 탭 줄은 전폭 3분할
- KPI 4 → 2열
- 아래 2열 → 1열
- 타임라인은 가로 스크롤이 아니라 **6시간 단위로 접는다** — 좁은 폭에서 24시간을
  펼치면 구간이 3px가 된다

---

## 8. 결정할 것

| # | 질문 | 선택지 | 추천 |
|---|---|---|---|
| **D1** | 이 문서가 직전 설계를 **대체**하나 | 대체 · 병합(원을 타이머 탭에) | **대체** (§1.1) |
| **D2** | 큐 | 드로어 · 타이머 탭 접이식 · **삭제** | **드로어** (§4.4) |
| **D3** | 세션 중 탭 전환 | 비활성+안내 · 확인 · 뷰만 | **비활성+안내** (§3) |
| **D4** | 기록의 색 | 단색 · 작업 정체성 기반 다색+범례 | **단색** (§5.3) |
| **D5** | 비교 기준 | 직전 동일 기간 · 최근 7일 평균 · 없앤다 | **직전 동일 기간** (§5.5) |
| **D6** | 시계 굵기·크기 | 400 / `clamp(88,10vw,140)` · 지금 유지 | **바꾼다** (§6.1–6.2) |
| **D7** | 포모도로 `초기화` | 뺀다 · 넣는다 | **뺀다** (§4.3) |
| **D8** | 몰입 보기 | `•••`로 · 탭 줄에 · 없앤다 | **`•••`로** (§2) |

---

## 9. 단계

| Phase | 무엇 | 왜 이 순서 |
|---|---|---|
| **0** | 직전 평가의 ①②④ 선수정(몰입 대비, 다크 주 버튼 대비, 기록 정렬) | 지금 사용자가 겪는 회귀. 뒤 단계까지 방치하지 않는다 |
| **1** | 탭 줄 도입. 기존 두 뷰를 세 탭으로 가른다. 세션 중 전환 규칙(§3) | IA가 먼저다. 나머지가 이 위에 놓인다 |
| **2** | 타이머 탭 — 앵커 · 상태 다섯 · 결과를 상태로 · 시계 400/큰 크기 | |
| **3** | 포모도로 탭 — 휴식을 같은 화면의 상태로 · 진행 막대 | |
| **4** | 큐 드로어 | 2·3이 캔버스를 비워둔 뒤라야 드로어가 말이 된다 |
| **5** | 기록 — KPI · 타임라인 · 세션 목록(빈 상태 포함) | 데이터 조각 중 이미 계산하는 것부터 |
| **6** | 기록 — 작업별 막대 · 히트맵 · 7일 추이 · 호버 · 세션 상세 드로어 | 새 집계가 필요한 것들 |

---

## 10. 검증

- `npm test` · `npm run typecheck` · `npm run build`(api 번들 무변경)
- `e2e/focusTimerV2.spec.ts` · `focusDetail.spec.ts` · `globalFocusBar.spec.ts` —
  역할과 이름으로 찾으므로 **탭이 되어도 통과해야 한다**. `Records` 버튼이 탭이 되면
  접근 가능한 이름이 바뀌므로 그 스펙은 함께 고친다(마크업이 먼저, 스펙이 나중).
- `e2e/radiusScale.spec.ts` — 새 예외 0. 모서리는 4px, 알약만 형태로 남는다.
- `e2e/surfaceHierarchy.spec.ts` — 색 있는 그림자 0(상태 점의 파란 링을 빼는 이유),
  레일 < 캔버스 유지. 드로어의 그림자는 §11.38이 허용하는 "떠 있는 것"이다.
- `src/styles/scale.test.ts` — 위반 0 유지.
- `e2e/overlayScrollbar.spec.ts` — 기록 탭에서 세션 목록이 유일한 내부 스크롤러.
- 대비 실측: 라이트/다크 × 세 탭. 시계 · 주 버튼 · 탭 선택 상태 · KPI 값 · 차트 마크와
  그 배경. 주 버튼은 두 테마 모두 **4.5 이상**(지금 다크 3.65).
- 차트는 렌더해서 눈으로 본다 — 검증기는 색을 보지 라벨 충돌과 넘침을 보지 않는다.
