# 끌어서 만든 자리, 그 옆에 열리는 팝업

> 상태: **구현됨** · 2026-09-04 (D1 = A · D2 = A · §4는 열에도 적용, 사용자 승인 — §11이 구현하며 바뀐 것)
> (사용자가 여섯 장을 주었다: ① 캘린더에서 끌어 만든 새 할 일 폼 — 네모가 흐리고
> 폼이 잘려 있다 ② 우리 일정 편집기 ③ 태스크 디테일 패널 ④ 우리가 이번 주에 만든
> 가운데 팝업 ⑤ 참조 앱의 **작은 팝업이 카드 옆에** 떠 있는 화면 ⑥ 그 팝업의 확대.)
> 대상: `styles/02-calendar.css` · `components/calendar/{NewTaskForm,EventPopover}.tsx` ·
> `components/CalendarView.tsx` · `domain/tasks/responsive.ts` ·
> `components/tasks/{TaskDrawer,TaskDetailPane,TasksModule}.tsx` · `App.tsx` ·
> `styles/17-tasks-module.css`
> 선행 문서: `BOARD_TASK_POPUP_DESIGN.md`(§5.3의 720×640 — **이 문서가 뒤집는다**) ·
> `SCHEDULE_TIME_FIELD_DESIGN.md`(일정 편집기) · `CALENDAR_V3_DESIGN.md`

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 6장 (위) | [관찰] |
| **실측** | 실행 중인 앱에서 잰 것 — 캘린더의 태스크 클릭이 여는 것(274×107 팝오버) · `.gcal-popover`의 `max-height: 340px` · `.gcal-draft-block`의 색 · `.tm-drawer.is-center-modal`의 720×640 · `EventPopover.tsx:30`의 배치 알고리즘 | [실측] |
| **결정** | 사용자가 고른 것 다섯: 고스트 진하게 · 폼은 한눈에 · 마감일은 우리 달력으로 · 팝업은 **작게, 태스크 옆에, 전역적으로** · 팝업 안은 한 면으로 | [결정] |
| **추론** | 참조 팝업의 실제 치수 · 앵커를 어디서 얻는가 · 스크림과 포커스 트랩의 운명 | [추론] |

---

## 1. 끌 때 생기는 네모가 너무 흐리다 [실측]

```css
/* 02-calendar.css:716 */
.gcal-draft-block {
  border: 1.5px dashed var(--color-primary);
  background: var(--color-primary-tint);   /* = rgba(0, 102, 204, 0.1) */
}
```

**두 가지가 겹쳐 흐리다.**

1. **10% 틴트.** 캘린더의 격자선(`--border-subtle`)과 셀 배경 위에서 알파 0.1은
   거의 흰색이다 — [관찰] ①의 그 네모다.
2. **`--color-primary-tint`는 액센트를 따라가지 않는다** [실측]. `01-base.css:330`이
   `--color-primary: var(--accent)`로 별칭을 맞춰 두었지만, 틴트는 `:34`의
   `rgba(0, 102, 204, 0.1)` 리터럴 그대로다. 액센트를 주황으로 둔 사용자에게 이 네모는
   **주황 테두리에 파란 안**이 된다. (같은 종류의 어긋남을 `TASK_PRIORITY_CHECKBOX_DESIGN.md`
   §2.2가 우선순위 색에서 이미 한 번 잡았다.)

### 1.1 고칠 것

```css
.gcal-draft-block {
  /* 점선에서 실선으로. 점선은 "아직 확정 아님"을 말하지만, 이 네모는 이미 시각을
     정하고 폼까지 띄운 상태다 — 확정 전인 것은 제목뿐이다. */
  border: 2px solid var(--color-primary);
  /* 22%: 격자 위에서 형태가 읽히는 최저선이고, 안의 글자(같은 파랑)가 아직 대비를
     잃지 않는 최대선이다. 액센트에서 직접 섞으므로 테두리와 하나의 색이다. */
  background: color-mix(in srgb, var(--color-primary) 22%, transparent);
}
```

`color-mix`는 이 리포에 선례가 있다(`09-calendar-redesign.css:263`, `10-calendar-apple.css:238`).

**`--color-primary-tint` 자체는 이 문서에서 손대지 않는다** — 아홉 군데가 읽고 있고
(`02-calendar.css` 여덟, `08-calendar-categories.css` 하나), 전부 확인하는 것은 별건이다.
§7에 남긴다.

---

## 2. 새 할 일 폼 — 한눈에, 그리고 마감일은 우리 달력으로

### 2.1 잘리는 이유 [실측]

폼은 `CalendarPopover` 안에 들어간다(`CalendarView.tsx:933`). 그 껍데기는 **이벤트 요약
팝오버를 위한 크기**다:

```css
.gcal-popover { width: 280px(JS 상수); max-height: 340px; overflow-y: auto; }
```

`NewTaskForm`은 제목·일정·카테고리·마감일·버튼 두 개 — 340px에 들어가지 않는다.
[관찰] ①에서 `확인` 버튼이 아래로 잘려 있는 것이 그 결과다.

### 2.2 고칠 것 — 껍데기에 두 번째 크기를 준다

한 껍데기를 두 내용이 쓰고 있고, 둘의 크기가 다르다. 요약 팝오버는 280×340이 맞다 —
줄 세 개짜리 카드다. 폼에는 자기 크기를 준다:

```css
/* 초안 폼은 요약이 아니라 폼이다. 스크롤은 화면이 정말 낮을 때만. */
.gcal-popover.is-draft {
  width: 320px;
  max-height: min(560px, calc(100vh - 32px));
}
```

`POPOVER_WIDTH`(JS 상수 280)가 배치 계산에 쓰이므로, 폼일 때의 폭도 **같은 상수 자리**에서
와야 한다 — `CalendarPopover`에 `size?: "summary" | "form"`을 주고 상수를 두 개로 만든다.
CSS만 고치면 배치는 280 기준으로 계산되어 오른쪽 가장자리에서 40px 어긋난다.

### 2.3 마감일 = 우리 달력 [결정]

지금은 `<input type="date">`다(`NewTaskForm.tsx:100`) — OS가 그리는 `mm/dd/yyyy`이고,
[관찰] ①에서 이 앱의 어느 화면과도 닮지 않은 유일한 컨트롤이다. 우리에게는 이미
[관찰] ②의 편집기가 있다(`ScheduleEditor`, `SCHEDULE_TIME_FIELD_DESIGN.md`).

빠른 추가가 같은 일을 이미 한다 — `QUICK_ADD_INPUT_BOX_DESIGN.md` §6.4가 아직 존재하지
않는 태스크를 위해 편집기를 열고, `onCancel`로 되돌아온다. **같은 경로를 재사용한다:**

```tsx
// 마감일 줄은 값을 말하는 버튼이 되고, 누르면 편집기가 그 자리에서 열린다.
<Popover placement="bottom-start">
  <PopoverTrigger className="gcal-newtask-date">{dueLabel || t("calendar.optional")}</PopoverTrigger>
  <PopoverContent label={t("common.dueDate")}>
    <ScheduleEditor … onCommit={(_, next) => { setSchedule(next); return []; }} onCancel={…} />
  </PopoverContent>
</Popover>
```

#### D1 — 편집기가 돌려주는 것을 어디까지 쓰는가 [결정 필요]

편집기는 **날짜 + 시간 + 알림 + 반복**을 정한다. 지금 폼이 갖는 것은 `dueDate` 문자열
하나다.

| | **A. 날짜만 받는다** (권장) | B. 스케줄 전체를 받는다 |
|---|---|---|
| 사용자가 알림을 걸면 | 조용히 버려진다 ❌ | 저장된다 |
| 폼이 커지는가 | 아니오 | 결과 타입과 `handleCreateFromDraft`가 스케줄을 다룬다 |
| 이 화면의 시간은 | **끌어서 정한 그 시각** — 편집기의 시간과 충돌한다 | 충돌을 규칙으로 풀어야 한다 |

세 번째 줄이 결정적이다. 이 폼의 태스크는 **이미 시각을 갖고 있다**(끌어서 만든
`1:45–3:45`). 편집기가 시간을 또 정하면 둘 중 무엇이 이기는지를 사용자가 알 수 없다.

**A를 권한다 — 다만 편집기의 시간·알림·반복 줄을 그 자리에서 끈다**(`dated`가 이미
같은 방식으로 세 줄을 비활성화한다). 편집기는 여기서 **날짜를 고르는 달력**으로만 쓰인다.
그것이 [관찰] ②에서 사용자가 가리킨 것이기도 하다 — 달력 그림.

---

## 3. 태스크 팝업 — 작게, 태스크 옆에 [결정 · 되돌리기]

### 3.1 이번 주에 만든 것을 사용자가 물렸다

`BOARD_TASK_POPUP_DESIGN.md` §5.3이 정한 값은 **720 × 640, 화면 가운데, 스크림 0.32**였고
[관찰] ④가 그것이다. 사용자의 문장: *"네번째 같이 너무 전역적으로 크게 만들지 말고,
task 옆에 뜨는 식으로 전역적으로."*

[관찰] ⑤가 답이다 — 참조 앱은 **카드 바로 옆에** 작은 팝업을 띄우고, **뒤를 어둡게 하지
않는다.** 보드가 그대로 보이고 다른 카드도 그대로 눌린다.

### 3.2 그러면 이것은 모달이 아니라 팝오버다

이 한 문장이 §5.1·§5.2가 정한 것 대부분을 바꾼다:

| | 지금 (`center-modal`) | 뒤 (`anchored-popover`) |
|---|---|---|
| 자리 | 화면 가운데 | **앵커 옆** (오른쪽 → 왼쪽 → 아래, 화면 안으로 클램프) |
| 크기 | 720 × 640 | **440 × 360** (§3.3) |
| 뒤 배경 | 스크림 0.32 | **없다** |
| 뒤의 클릭 | 막힌다(스크림이 먹는다) | **닿는다** — 다른 태스크를 바로 열 수 있다 |
| 포커스 트랩 | 있다 | **없다** (모달이 아니다) |
| `role` | `dialog` + `aria-modal` | `dialog`, `aria-modal` 없음 |
| 닫기 | 스크림 클릭 · Escape | **바깥 클릭 · Escape**, 그리고 `×`가 **돌아온다** |
| z-index | 스크림 44 / 팬 45 | 팝업 45 (스크림 없음) |

`×`가 돌아오는 것이 §5의 논리에 어긋나지 않는다: 그 절이 `×`를 뺀 조건은 *"포인터로
나가는 다른 길이 있는가 — 팝업에는 스크림이 있고 아무 데나 누르면 닫힌다"*였다.
스크림이 없어지면 바깥 클릭은 남지만, 뒤의 컨트롤을 **실수로 누르는** 대가가 생긴다.
작은 `×`가 그 대가 없는 길이다.

### 3.3 치수 [추론]

[관찰] ⑤에서 잰다. 그 화면의 왼쪽 사이드바는 385px(이미지 픽셀)이고 참조 앱의 실제
사이드바는 약 310 CSS px이므로 배율은 **약 1.24**. 팝업은 이미지에서 560 × 413 →
**약 450 × 333 CSS px**.

**440 × 360으로 시작한다.** 세로를 조금 더 주는 이유는 우리 팝업이 저 팝업보다 한 줄을
더 그리기 때문이다 — 참조는 제목·태그·푸터인데 우리는 설명 필드가 있다(§4).
`.tm-drawer-scroll`이 이미 넘침을 받는다(§1.17).

### 3.4 앵커는 어디서 오는가 [결정 필요]

팝업을 그리는 것은 `TaskDetailPane`이고, 그것이 뜨는 이유는 **주소의 `?task=`**다.
주소에는 "어느 행을 눌렀는가"가 없다.

| | **A. 연 쪽이 앵커를 넘긴다** (권장) | B. 팝업이 DOM에서 찾는다 |
|---|---|---|
| 방법 | `openTask(id, anchor?)` — 행·카드·캘린더 블록이 자기 `getBoundingClientRect()`를 넘긴다 | `document.querySelector('.tm-task.is-open')` |
| 새로고침·딥링크 | 앵커 없음 → **화면 가운데로 폴백** | 행이 있으면 찾고, 없으면 폴백 |
| 결합 | 호출자 넷이 한 줄씩 | 팝업이 세 화면의 선택자를 안다 |
| 스크롤 | 앵커가 낡는다 → 닫거나 다시 잰다 | 매번 다시 찾는다 |

**A를 권한다.** 앵커는 `dragTaskId`와 같은 종류의 **뷰 상태**이고, 그것을 아는 것은
누른 쪽이다. 캘린더는 이미 `anchorFromRect`로 같은 값을 만들고 있다
(`EventPopover.tsx:24`) — 재사용한다.

배치도 새로 쓰지 않는다. `CalendarPopover`의 `place()`가 **오른쪽 → 왼쪽 → 아래,
화면 안으로 클램프**를 이미 한다(`EventPopover.tsx:47-70`). 그 함수를 `components/floating`
아래로 끌어올려 두 팝업이 같은 규칙으로 앉게 한다.

### 3.5 어디에 적용되는가 — "전역적으로"

| 표면 | 지금 | 뒤 |
|---|---|---|
| 보드(칸반) | `center-modal` | **앵커드 팝업** |
| 매트릭스 | `center-modal` | **앵커드 팝업** |
| 캘린더 | (§5) | **앵커드 팝업** |
| 리스트 (wideDesktop) | `inline-drawer` 열 | **그대로** |
| 오버레이·시트·전체화면 (좁은 창) | 그대로 | **그대로** |

리스트를 그대로 두는 이유: 그것은 팝업이 아니라 **열**이고, 사용자가 물린 것은
"팝업이 크다"이지 "열이 있다"가 아니다. 좁은 창에서도 그대로다 — 375px에서 440px 팝업은
화면보다 넓다(§4.2의 모바일 예외와 같은 논리).

`TaskDetailPresentation`의 이름은 `center-modal` → **`anchored-popover`**로 바꾼다.
값 이름이 자리를 말하고 있고, 자리가 바뀌었다.

---

## 4. 팝업 안은 하나의 면이다 [결정]

[관찰] ⑥의 참조 팝업은 위에서 아래로 **머리줄 · 본문 · 발줄** 셋이고, 그 사이를 가르는
것은 얇은 선 하나씩이다. 그 안에 상자는 없다 — 제목은 그냥 굵은 글자이고, 태그는 칩,
설명 자리는 빈 공간이다.

우리 것([관찰] ③④)은 같은 자리에 **상자가 넷** 있다:

| 무엇 | 지금 | 뒤 |
|---|---|---|
| 제목 | 테두리 있는 입력 상자(`.tm-drawer-title`, `padding 6px 8px` + 배경) | **테두리 없는 글자**, 눌러야 캐럿이 서는 필드 |
| 설명 | 테두리 있는 textarea + 크기 조절 손잡이 | **테두리 없음**, 손잡이 없음 |
| `완료` | 상자 안의 체크박스 + 글자 | 체크박스만 (`완료`라는 말은 체크박스의 접근 이름으로) |
| 일정 칩 | 테두리 있는 칩 | **글자 + 아이콘** (색은 유지 — 지남은 빨강) |

**포커스는 상자 없이도 보여야 한다.** 테두리를 없앤다는 것은 쉴 때의 이야기이고,
`:focus-visible`에서는 §11.42의 링(`--ff-focus-ring`, 2px)이 그대로 나온다. 호버에서는
아주 옅은 면(`--bg-hover-content`)이 "여기 누를 수 있다"를 말한다.

**이것은 팝업만의 규칙이 아니다.** 같은 `TaskDrawer`가 열에서도 그려지므로, 상자가
사라지는 것은 리스트의 디테일 열에서도 마찬가지다 — 한 컴포넌트를 두 벌로 가르지
않는다(§1의 축). 사용자가 팝업에서 가리킨 문제는 열에서도 같은 문제다.

---

## 5. 캘린더에서 태스크를 누르면 무엇이 열리는가 [실측 · 결정 필요]

**실측한 사실 하나를 먼저 적는다.** 지금 캘린더에서 태스크 블록을 누르면 뜨는 것은
[관찰] ③이 아니다 — **274 × 107짜리 `EventPopover`**이고, 안에는 제목·날짜·"메모 또는
URL 추가"뿐이다(주·월 뷰 모두 확인). 즉 사용자가 ③으로 기억하는 화면은 캘린더가 아니라
다른 곳에서 본 디테일이다.

그러므로 이 절의 질문은 "③을 줄이자"가 아니라 **"캘린더의 태스크 클릭은 무엇을 열어야
하는가"**다.

| | **A. 태스크면 앱의 태스크 팝업** (권장) | B. 지금의 `EventPopover` 유지 |
|---|---|---|
| 보이는 것 | 다른 화면과 **같은** 디테일 — 완료·일정·우선순위·설명·리스트 | 제목·시각·메모 |
| 할 수 있는 것 | 태스크에 할 수 있는 전부 | 시각 두 개와 메모 |
| 외부 일정·집중 기록 | `EventPopover` 그대로(태스크가 아니다) | 같음 |
| 근거 | `TASK_DETAIL_PANEL_MERGE_DESIGN.md` §2 — 화면마다 다른 필드를 보이던 것을 없앤 문서 | — |

**A를 권한다.** 그 병합 문서가 지운 것이 정확히 이 상황이다: 같은 태스크가 어느 화면에서
열렸느냐에 따라 다른 필드를 보이는 것. 캘린더만 자기 폼을 남겨 둘 이유는 "이미 있어서"
말고는 없다.

A를 택하면 `EventPopover`의 빠른 편집 폼(시작·종료·메모)은 **태스크에 대해서는** 죽는다.
외부 캘린더 이벤트와 집중 기록은 태스크가 아니므로 그대로 그 팝오버를 쓴다.

---

## 6. 구현 단계

| 단계 | 무엇 | 위험 |
|---|---|---|
| **1** | 드래그 고스트의 색 (§1.1) | 없음 — 규칙 두 줄 |
| **2** | 초안 폼의 크기 (§2.2) | 낮음 — 상수 하나가 둘이 된다 |
| **3** | 마감일 → 일정 편집기 (§2.3, D1-A) | 중간 — 편집기를 폼 안에서 여는 첫 자리 |
| **4** | 배치 함수를 `floating`으로 올린다 (§3.4) | 낮음 — 순수 이동, 캘린더가 회귀 테스트 |
| **5** | `center-modal` → `anchored-popover`: 이름 · 치수 · 스크림 제거 · 트랩 제거 · `×` 복귀 (§3.2) | **높음** |
| **6** | 앵커 배선: `openTask(id, anchor?)` (§3.4) | 중간 — 호출자 넷 |
| **7** | 캘린더의 태스크 클릭 (§5, D2-A) | 중간 |
| **8** | 팝업 안의 상자들 (§4) | 중간 — 열에도 영향 |

5를 6보다 먼저 한다: 앵커가 없으면 가운데로 폴백하므로, 5만으로도 **작은 가운데 팝업**이
되어 화면이 성립한다. 6은 그것을 옆으로 옮기는 일이다.

---

## 7. 테스트

| 무엇 | 어디 |
|---|---|
| 초안 폼이 잘리지 않는다 — `확인` 버튼이 화면 안에 있다 | e2e 신규(캘린더 드래그) |
| 마감일 버튼이 우리 편집기를 연다, `input[type=date]`가 없다 | 신규 |
| 팝업이 앵커의 오른쪽에 서고, 오른쪽 끝 카드에서는 왼쪽으로 뒤집는다 | 배치 함수의 단위 테스트(순수 함수로 올리는 이유) |
| 팝업에 스크림이 없다 — 뒤의 레일이 눌린다 | e2e. **`pageTaskUrl.spec.ts`의 "leaving the page"를 매트릭스로 되돌릴 수 있다**(§8) |
| `×`가 있다 | 단위 |
| 캘린더의 태스크 클릭이 `?task=`를 쓴다 | e2e |
| 제목·설명에 테두리가 없고, 포커스 링은 있다 | 단위(계산된 스타일) |

---

## 8. 이 문서가 되돌리는 것 (명시)

`BOARD_TASK_POPUP_DESIGN.md`에서:

- **§5.3의 720×640** → 440×360 (§3.3)
- **§5.1의 스크림과 `aria-modal`** → 없음 (§3.2)
- **§5.2 "스크림 클릭 = 닫기"** → 바깥 클릭 = 닫기, `×` 복귀
- **§5의 "팝업에는 `×`가 없다"** → 있다
- **§4.2의 규칙표** → 표현 방식 이름이 `anchored-popover`로 바뀐다. 모바일 예외는 그대로

그리고 그때 함께 바꿨던 것 하나가 **되돌아온다**: 스크림이 레일을 덮어서
`e2e/pageTaskUrl.spec.ts`의 "페이지를 떠나도 태스크는 따라가지 않는다"를 포커스 페이지로
옮겼는데(§12.3), 스크림이 없어지면 매트릭스에서 다시 가능해진다. 옮긴 자리에 두어도
규칙은 검증되므로 **되돌리는 것은 선택**이다 — 하지만 이유가 사라졌다는 사실은 적어 둔다.

---

## 9. 바뀌지 않는 것

- **`?task=` 주소** — 팝업이든 열이든 태스크가 열린 사실은 주소에 남는다.
- **`TaskDrawer`의 내용** — 헤더·본문·푸터의 순서와 필드. §4는 **테두리**를 지우지 필드를
  지우지 않는다.
- **리스트의 인라인 열**과 좁은 창의 세 표현 방식.
- **캘린더의 드래그·리사이즈·이동** — §1은 색만, §2는 폼만 바꾼다.
- **외부 캘린더 이벤트·집중 기록의 팝오버** — 태스크가 아니다(§5).
- **일정 편집기 자체** — §2.3은 그것을 **부르는 자리**를 하나 늘릴 뿐이다.

---

## 10. 열린 질문

1. **`--color-primary-tint`가 액센트를 따라가지 않는다** [실측] — 이 문서는 초안 블록
   하나만 고친다. 나머지 여덟 자리는 별건.
2. **앵커가 스크롤로 낡는 문제** (§3.4) — 닫을 것인가, 따라갈 것인가. 참조 앱은 따라간다.
   1차는 **그대로 두기**(스크롤해도 팝업은 제자리)를 제안하고, 실물에서 어색하면 §3.4의
   `place()`를 스크롤에 물린다.
3. **440×360이 우리 필드 수에 맞는가** [추론] — 구현 후 실물에서 조정.


---

## 11. 구현하며 바뀐 것 [실측 · 2026-09-04]

### 11.1 배치 함수는 옮기지 않았다 — 이미 더 나은 것이 있었다 (§3.4 · 단계 4)

캘린더 팝오버의 `place()`를 `floating`으로 끌어올릴 계획이었는데, 그 폴더에 이미
**`useFloatingPosition` + `computePlacement`**가 있었다 — 뒤집기·클램프·`maxWidth`/
`maxHeight`까지, 단위 테스트가 붙은 순수 함수로. 게다가 §19.9가 이미 이렇게 적어
두었다: *"앵커는 요소가 아니라 RECT"* — 클릭이 넘겨주는 것이 정확히 그것이다.

그래서 팝업은 그 훅을 쓴다. 캘린더의 `place()`는 자기 팝오버의 것으로 남는다(옮길
이유가 사라졌다). **덫이 하나 있었다** [실측]: 훅은 `anchorRect`/`surface` 콜백의
identity가 바뀌면 다시 재고, 재면 state를 쓴다 — 렌더마다 새 클로저를 넘기면 무한
루프다("Maximum update depth exceeded", 화면이 백지가 됐다). `useCallback` 둘로 고정했다.

### 11.2 높이는 CSS가 정한다

훅이 돌려주는 `maxHeight`는 **뷰포트에 남은 공간**이고, 그것을 인라인으로 걸었더니
§3.3이 정한 360을 덮어써서 팝업이 418px이 됐다 [실측]. 인라인으로는 `left`/`top`만
쓰고 높이는 스타일시트가 갖는다 — 배치 계산은 이미 그 상한을 입은 요소를 재므로
서로 어긋나지 않는다.

### 11.3 z-index를 하나 내렸다 (§2.3의 부작용)

`.gcal-popover`는 **1300**이었다. 앱의 플로팅 레이어 루트는 **100**이다. 그래서 새 할 일
폼이 연 일정 편집기가 **폼 뒤에** 그려졌다 [관찰 · 실측]. 날짜와 메모만 들어 있던
시절에는 드러날 수 없던 문제다.

`.gcal-popover`를 **50**으로 내렸다 — 캘린더가 그리는 것 중 가장 높은 것이 40(종일
리사이저)이고, 앱의 덮는 표면들과 같은 띠(디테일 팝업 45)이며, 플로팅 레이어(100)
아래다.

### 11.4 §4가 고친 것은 규칙이 아니라 **특이도 싸움**이었다 [실측]

디테일의 제목을 테두리 없이 그리는 규칙은 `21-components.css:190`에 **이미 있었다** —
주석까지 달려서: *"네 클래스, 세 개로는 지기 때문에"*. 그런데 지고 있었다. 그 뒤의
공용 필드 규칙에 `:not(.tm-quickadd-title)`이 나중에 추가되면서 (0,3,1) → **(0,4,1)**이
되었고, 네 클래스(0,4,0)는 그보다 낮다. 측정하니 제목은 `1px solid` 흰 상자였다.

고친 방법은 그 규칙이 **이미 쓰고 있던 방법**이다 — 이름으로 면제. `:not(.tm-drawer-title)`을
더하고, 네 단계 선택자를 `.tm-drawer-title:hover/:focus` 둘로 줄였다. 함께 지운 상자:
`메모` textarea의 테두리와 채움, 일정 칩의 테두리와 틴트, 그리고 두 textarea의
**크기 조절 손잡이** — 테두리가 사라진 카드에서 유일하게 남는 장식이었다.

### 11.5 되돌린 것 (§8의 예고대로)

스크림이 없어졌으므로 `e2e/pageTaskUrl.spec.ts`의 두 곳이 원래대로 돌아갔다:
닫기 헬퍼는 다시 `×` 하나가 되었고, "페이지를 떠나도 태스크는 따라가지 않는다"는
포커스 페이지에서 **매트릭스로 복귀**했다. 후자가 스크림이 사라졌다는 가장 날카로운
증거다 — 태스크가 열린 채로 레일이 눌린다.

### 11.6 실측

| | 값 |
|---|---|
| 팝업 | 440 × 360, 카드 오른쪽 +8px (`x=643`, 카드 우변 635) |
| 화면 아래쪽 블록에서 | 왼쪽으로 뒤집고 위로 밀림 (`x=361, y=492` — 블록은 `809, 818`) |
| 스크림 · `aria-modal` · 포커스 트랩 | 없음 |
| `×` | 있음 |
| 드래그 고스트 | `2px solid` 액센트 · 액센트 22% |
| 새 할 일 폼 | 314 × 383, 최대 560 — **스크롤 없음** |
| 마감일 | `9월 17일`로 확정, 편집기의 세 줄은 비활성 |
| 캘린더 블록 클릭 | `/calendar?task=t1` · 앱의 팝업 · `gcal-popover` 없음 |

### 11.7 테스트

- `e2e/calendarCreate.spec.ts`(신규) — 고스트의 색·폼이 잘리지 않음·마감일이 우리
  달력을 열고 세 줄이 잠김·블록 클릭이 앱의 팝업을 연다.
- `e2e/taskRow.spec.ts`(추가 둘) — 팝업이 카드 옆 440×360으로 서고 스크림이 없다 ·
  **뒤의 다른 카드를 바로 누를 수 있다**(모달이면 스크림이 먹던 클릭).
- `responsive.test.ts`에 `calendar` 표면 4칸, `pageRoute.test.ts`의 `?task=` 목록에 캘린더.
- `boardDetailPopup.test.tsx`는 절반이 뒤집혔다 — `aria-modal` 없음, 스크림 없음,
  `×` 있음, 그리고 **레이어 루트 위의 클릭은 닫지 않는다**(스크림이 부모였을 때는
  타깃 비교 한 줄로 끝나던 것).
