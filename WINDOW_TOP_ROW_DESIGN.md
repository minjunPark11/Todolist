# 창의 맨 위 32px — 색이 이어지고, 앱이 거기 산다

> 상태: **구현됨** · 2026-09-04 (D1 = **B**, 사용자 결정 — §7이 구현하며 바뀐 것)
> (사용자가 두 장을 주었다: 우리 설치본의 위쪽 띠 — 흰 띠 아래로 회색 페이지가
> 시작해 **선이 보인다** · 참조 앱의 위쪽 — 사이드바 항목·리스트 제목·디테일 헤더가
> **창 버튼과 같은 줄에** 있다.)
> 대상: `styles/19-app-shell.css` · `components/shell/WindowTitleBar.tsx` ·
> `components/tasks/TasksModule.tsx` · `components/MatrixPage.tsx` ·
> `components/SettingsPage.tsx` · `components/calendar/…`
> 선행 문서: `WindowTitleBar.tsx` 서문(캡션은 **바가 아니어야 한다**)

## 0. 근거 수준

| 등급 | 무엇 | 표기 |
|---|---|---|
| **관찰** | 스크린샷 2장 (위) | [관찰] |
| **실측** | 실행 중인 앱에서 `data-window-chrome="custom"`을 켜고 잰 값 — 아래 §1 표 전부 | [실측] |
| **결정** | 사용자가 고른 것 둘: 배경이 자연스럽게 이어질 것 · 전체를 위에 붙일 것 | [결정] |
| **추론** | 어느 줄을 캡션으로 올릴지 · 드래그 영역 · 오른쪽 138px의 처리 | [추론] |

**이 문서는 두 개의 독립된 변경이다.** §2는 규칙 한 줄이고 오늘 배포할 수 있다.
§3은 셸의 위쪽 가장자리를 다시 그리는 일이다. §2를 §3의 일부로 묶지 않는 이유는,
§3을 나중으로 미루더라도 §2의 선은 사라져야 하기 때문이다.

---

## 1. 지금 창의 위쪽은 이렇게 생겼다 [실측]

1280×820, `/today`, `data-window-chrome="custom"`:

| 무엇 | 값 |
|---|---|
| 캡션 높이 `--titlebar-h` | **32px** (`[data-window-chrome="custom"]`) |
| 창 버튼 | 46 × 32, 셋 → 오른쪽 **138px** |
| 레일 | x 0–50, 배경 `#eceef1`, 첫 아이콘 **y=48** |
| 사이드바 | x 50–298 (248px), 배경 `#f0f1f4`, 첫 항목 **y=48** |
| 페이지(`.tm-main`) | x 298–, 배경 **`#f2f2f7`**, 상단 y=32 |
| 제목(`.tm-header`) | **y=48**, 높이 56 |
| `--titlebar-surface`가 실제로 푸는 값 | **`#ffffff`** ← §2 |

즉 **창의 첫 48px에는 아무것도 없다.** 32는 캡션이 비워 둔 자리, 16은 페이지의
`padding-top`이다. [관찰] 2번의 참조 앱은 같은 자리에 사이드바 항목 하나, 리스트
제목 하나, 디테일 헤더 하나를 놓는다.

---

## 2. 선이 보이는 이유 — 한 줄 [실측 · 결정]

캡션은 자기 색을 칠하지 않고 **아래 열들의 색을 이어 그린다**(`window-titlebar-fill`,
`19-app-shell.css:587`). 그 그라디언트의 마지막 구간이 `--titlebar-surface`이고,
지금 그 값은 이렇게 정해진다:

```css
:root                  { --titlebar-surface: var(--bg-app); }  /* #f2f2f7 */
:root:has(.tm-shell)   { --titlebar-surface: var(--bg); }      /* #ffffff ← */
```

**두 번째 줄이 틀렸다.** 규칙의 주석은 *"Tasks 모듈의 페이지 표면은 `var(--bg)`"* 라고
적고 있지만, 실측한 `.tm-main`의 배경은 **`#f2f2f7`** — `--bg-app`이다. 레거시 셸에서는
`--titlebar-surface`(#f2f2f7)와 `main`의 배경(#f2f2f7)이 **일치한다** [실측]. 즉 선은
Tasks 모듈에서만 생기고, 사용자가 캡처한 화면이 정확히 그 경우다.

### 2.1 고칠 것

```css
/* 두 셸의 페이지 표면은 같은 토큰이다 — 실측(#f2f2f7 · #f2f2f7). 여기서 갈라 놓은
   `:root:has(.tm-shell)` 특례가 캡션에 흰색을 칠했고, 그것이 사용자가 본 선이다. */
:root {
  --titlebar-surface: var(--bg-app);
}
/* `:root:has(.tm-shell)` 규칙 삭제 */
```

`:has()` 특례가 사라지므로 규칙 하나와 주석 여섯 줄이 같이 없어진다.

### 2.2 남는 두 이음매도 같이 확인한다 [실측]

| 구간 | 캡션이 칠하는 색 | 아래 열의 색 | 일치? |
|---|---|---|---|
| 0–50 (레일) | 투명 (레일이 y=0부터 스스로 칠한다) | `#eceef1` | ✅ |
| 50–298 (사이드바) | `--bg-context-sidebar` = `#f0f1f4` | `#f0f1f4` | ✅ |
| 298–299 | `--ff-border` | 사이드바의 구분선 | ✅ |
| 299– (페이지) | `#ffffff` | `#f2f2f7` | ❌ → §2.1 |

**한 군데뿐이다.** 나머지는 이미 이어져 있고, 그래서 이 절이 한 줄로 끝난다.

---

## 3. 앱을 캡션 줄로 올린다 [결정 · 추론]

### 3.1 지금 구조가 하는 약속

`WindowTitleBar.tsx` 서문의 문장은 **"캡션은 바가 아니다"** 이고, 지금 코드는 그것을
**색으로만** 지킨다 — 색은 이어지지만 그 32px에는 아무것도 살지 않는다. 참조 앱은
같은 문장을 **내용으로** 지킨다: 그 줄에 앱의 첫 줄이 들어가 있고, 창 버튼은 그 줄의
오른쪽 끝을 나눠 쓴다. 사용자가 가리킨 차이가 이것이다.

### 3.2 D1 — 어떻게 올릴 것인가 [결정 필요]

| | A. 예약을 없애고 버튼만 띄운다 | **B. 캡션 줄을 앱의 첫 줄로 만든다** (권장) | C. 지금대로 두고 캡션만 24px로 |
|---|---|---|---|
| 위쪽 빈 공간 | 0 | 0 | 40px |
| 창 버튼과 앱 컨트롤 | 겹칠 수 있다 | 오른쪽 138px을 **예약**해서 안 겹친다 | 안 겹친다 |
| 드래그로 창 옮기기 | **잃는다**(그 자리에 앱 내용이 있다) | 앱의 첫 줄이 드래그 영역을 겸한다 | 그대로 |
| 손대는 표면 | 0 | **4곳** (§3.4) | 0 |
| [관찰] 2번과 | 비슷 | 같다 | 다르다 |

**B를 권한다.** A는 B에서 오른쪽 예약과 드래그만 뺀 것이고, 그 둘이 없으면 창을 옮길
수 없거나 버튼이 제목 위에 앉는다 — 즉 A는 완성되지 않은 B다.

### 3.3 B의 규칙 셋

```css
/* 1. 캡션은 더 이상 자리를 예약하지 않는다. 색을 잇는 fill은 남는다 —
      앱의 첫 줄이 그 자리에 오지만, 줄 사이의 빈 곳은 여전히 캡션의 색이다. */
[data-window-chrome="custom"] .app-frame-body { padding-top: 0; }
[data-window-chrome="custom"] .app-frame .global-rail { padding-top: 16px; }
[data-window-chrome="custom"] .app-frame .tm-shell { height: 100dvh; }

/* 2. 창 버튼이 앉는 자리는 앱이 비워 둔다. 창의 오른쪽 끝에 닿는 줄에만. */
:root { --titlebar-buttons-w: 0px; }
:root[data-window-chrome="custom"] { --titlebar-buttons-w: 138px; }  /* 46 × 3 */

[data-window-chrome="custom"] .tm-header,
[data-window-chrome="custom"] .ff-page-head,
[data-window-chrome="custom"] .gcal-toolbar,
[data-window-chrome="custom"] .tm-drawer.is-inline-drawer .tm-drawer-head {
  padding-right: calc(16px + var(--titlebar-buttons-w));
}
```

```tsx
/* 3. 드래그는 앱의 첫 줄이 맡는다. Tauri는 `event.target`이 이 속성을 가진
      요소일 때만 창을 끄므로, 그 안의 버튼·입력은 평소대로 동작한다. */
<header className="tm-header" data-tauri-drag-region>
```

그리고 **`.window-titlebar-drag`는 삭제된다.** 지금은 `position: fixed`로 창 폭을 가로질러
z-index 70에 떠 있는 32px 띠다(`19-app-shell.css:651`). §3.3-1이 그 아래로 앱의 첫 줄을
올리는 순간, 이 띠는 **사이드바의 첫 항목과 제목과 디테일 헤더를 덮어 클릭을 먹는다.**
없애는 것이 이 절에서 가장 위험한 한 줄이고, 대신 3-3의 속성이 그 일을 한다.

### 3.4 그 "첫 줄"이 화면마다 무엇인지 [실측]

캡션 예약을 없앴을 때 y=0..32에 올라오는 요소를 각 화면에서 재 보았다:

| 화면 | 첫 줄 | 오른쪽 끝에 무엇이 있나 |
|---|---|---|
| Tasks 모듈 (`/today`, 리스트) | `.tm-header` (제목 + 개수 + `⋯`) | `⋯` — 예약 필요 |
| 〃 (디테일 열린 상태) | `.tm-drawer .tm-drawer-head` | `×`·`⚑` — 예약 필요 |
| 매트릭스 `/board` · 설정 `/settings` | `header.ff-page-head` | 페이지 `⋯` — 예약 필요 |
| 캘린더 `/calendar` | `.gcal-toolbar` | 뷰 전환·`오늘` — 예약 필요 |
| 집중 `/focus` | 카드의 제목(전용 헤더가 없다) | 없음 — **§3.6의 열린 질문** |

**넷 + 하나.** 이 표가 D1-B의 실제 비용이다.

### 3.5 사이드바와 레일은 어떻게 되는가 [추론]

- **레일**은 이미 `height: 100dvh`로 y=0부터 자기 색을 칠한다. 지금은 캡션만큼
  `padding-top`을 더 받고 있고(`:549`), 그 항이 빠지면 첫 아이콘이 16px에서 시작한다 —
  참조 앱의 아바타가 앉은 자리와 같다 [관찰].
- **사이드바**는 첫 섹션이 y=0..으로 올라온다. 그 위에 창 버튼은 없다(버튼은 오른쪽
  138px). 그러므로 **예약도 드래그 속성도 필요 없다.**

### 3.6 열린 질문

1. **집중 페이지에 첫 줄이 없다** [실측]. 제목 행을 하나 두든지, 그 페이지에서만
   캡션 예약을 남기든지 — 후자는 화면마다 위쪽이 달라지므로 권하지 않는다.
2. **모달·팝오버가 열렸을 때의 드래그.** 버튼은 z 2000이라 항상 위에 있고, 스크림은
   z 60이므로 그 아래의 `data-tauri-drag-region`은 덮인다 — 즉 팝업이 열린 동안에는
   창을 옮길 수 없다. 지금도 드래그 띠가 z 70이라 스크림 위였으므로 **행동이 바뀐다.**
   받아들일지, 스크림에 드래그 속성을 줄지.
3. **`--titlebar-buttons-w`의 좌우.** Windows는 오른쪽, macOS는 왼쪽이다. 지금 이
   빌드는 Windows 전용(`decorations: false` + 46×32 메트릭)이므로 오른쪽으로 고정하되,
   변수 이름은 방향을 말하지 않게 둔다.

---

## 4. 구현 단계

| 단계 | 무엇 | 위험 |
|---|---|---|
| **1** | §2.1 — 캡션의 페이지 색 (규칙 한 줄) | 없음. 설치본에서 눈으로 확인 |
| **2** | §3.3-2 — `--titlebar-buttons-w` 예약을 넷에 먼저 넣는다 (아직 예약만) | 없음. 캡션이 아직 자리를 비우고 있으므로 화면은 그대로 |
| **3** | §3.3-1 + 3 — 예약 해제 · 드래그 띠 삭제 · 첫 줄에 속성 | **높음.** 여기서 클릭이 먹히거나 창이 안 움직이면 이 단계의 문제다 |
| **4** | 집중 페이지 (§3.6-1) | — |

2를 3보다 먼저 하는 것이 요점이다: 예약이 먼저 들어가 있어야 3의 순간에 버튼이 무언가를
덮는 장면이 없다.

---

## 5. 테스트

**이 변경은 e2e가 볼 수 없다** — `data-window-chrome="custom"`은 Tauri에서만 켜지고,
Playwright는 브라우저 빌드를 연다. 그래서 두 가지로 나눈다:

| 무엇 | 어떻게 |
|---|---|
| 브라우저 빌드가 **한 픽셀도** 바뀌지 않는다 | 기존 `verticalRhythm`·`surfaceHierarchy`·`navShell` 스펙 그대로 통과 |
| 캡션이 켜졌을 때의 기하 | 신규 스펙 — 루트에 `data-window-chrome="custom"`을 **직접 심고** 재는 방식(이 설계의 §1 표가 그렇게 측정되었다): 레일 첫 아이콘 y=16 · 사이드바 첫 항목 y=0 · `.tm-header`의 `padding-right` ≥ 138 · 캡션 색 = 페이지 색 |
| 실제 창 | 설치본에서 사람이 확인: 드래그로 창이 움직이는가 · 최대화/닫기가 눌리는가 · 사이드바 첫 항목이 눌리는가 |

세 번째 줄은 자동화하지 않는다. Tauri 창의 드래그는 OS가 처리하고, 그것을 검증하는
비용은 이 변경 전체보다 크다.

---

## 6. 바뀌지 않는 것

- **브라우저 빌드 · e2e** — 모든 규칙이 `[data-window-chrome="custom"]` 아래에 있다.
- **창 버튼 자체** — 46×32, z 2000, 오른쪽 끝. OS의 약속이므로 손대지 않는다.
- **미니 타이머 창** — 자기 HTML이고 이 셸을 쓰지 않는다.
- **`decorations: false`** — 시스템 캡션을 되살리는 것은 이 문서의 답이 아니다.
- **레일·사이드바·페이지의 색 토큰** — §2는 캡션이 **어느 토큰을 읽는지**만 고친다.


---

## 7. 구현하며 바뀐 것 [실측 · 2026-09-04]

### 7.1 §2가 §3에 흡수됐다 — fill을 고치는 대신 지웠다

§2는 `--titlebar-surface`를 `--bg-app`으로 바꾸는 한 줄이었다. **그 한 줄은 들어가지
않았다.** §3이 캡션 줄을 앱의 첫 줄로 만드는 순간, `.window-titlebar-fill`은 고쳐야 할
것이 아니라 **없어야 할 것**이 되기 때문이다:

- fill은 `position: fixed; z-index: 1`이다. 예약이 사라져 사이드바의 첫 항목과 제목이
  y=0으로 올라오면, fill은 그것들을 **덮는다**.
- fill이 하던 일(열들의 색을 이어 그리기)은 이제 열들이 스스로 한다 — 레일은 원래부터
  `100dvh`였고, 사이드바와 페이지가 창 위 가장자리까지 닿는다.

**색을 흉내 내는 것과 색이 거기 있는 것은 다르다.** 사용자가 본 흰 띠는 흉내가 어긋난
자리였고, 이제 흉내낼 대상이 없다. `--titlebar-surface`와 미디어 쿼리 셋도 같이 사라졌다.

`.window-titlebar-drag`(창 폭 × 32px, z-index 70)도 같은 이유로 삭제됐다 — 그 자리에
이제 진짜 컨트롤이 있다. 드래그는 §3.3-3대로 각 줄의 `data-tauri-drag-region`이 맡는다.

### 7.2 예약 대신 변수 하나

`--titlebar-h`(32px)는 **버튼이 앉는 띠의 높이**로 남았고, 레이아웃이 그것을 빼던 규칙
다섯은 지워졌다. 새로 생긴 것은 `--titlebar-buttons-w`(0px / 138px) 하나다.
`03-planning.css`의 매트릭스 높이도 `calc(100dvh - var(--titlebar-h))`에서 `100dvh`로 —
페이지가 더 이상 32px 아래에서 시작하지 않으므로, 빼면 아래에 32px이 남는다.

### 7.3 집중 페이지는 헤더가 있었다 (§3.6-1 닫힘)

`.foc-header`가 있고, 다른 셋과 같은 자리에 있다. 예약과 드래그 속성을 같이 받았다.
캡션 예약을 남기는 예외는 만들지 않았다.

### 7.4 실측

1280×860에서 `data-window-chrome="custom"`을 켜고:

| | 전 | 후 |
|---|---|---|
| 레일 첫 아이콘 · 사이드바 첫 항목 · 제목의 y | 48 | **16** (페이지 자신의 패딩) |
| `.app-frame-body`의 `padding-top` | 32px | **0px** |
| 디테일 열 헤더의 `padding-right` | 16px | **154px** (16 + 138) |
| 매트릭스 · 캘린더 · 집중 · 설정의 첫 줄 | — | 같은 154px, 드래그 영역 있음 |
| `.tm-header`의 `padding-right` (1280) | — | **0** — 그 폭에서는 디테일 열이 예약되어 있어 버튼 아래가 아니다 |
| 〃 (1100) | — | **138px**, `⋯`는 오른쪽 끝에서 162px |

### 7.5 테스트

`e2e/windowChrome.spec.ts`(신규) — 브라우저 빌드에 루트 속성을 **직접 심고** 재는
§5의 방식 그대로다. 다섯 가지: 띠를 예약하지 않는다 · fill과 드래그 띠가 없다 ·
네 화면이 버튼 폭을 비운다 · 디테일 열도 그렇다 · **속성이 없으면 아무것도 바뀌지
않는다**(마지막이 브라우저 빌드의 회귀 테스트다).

창을 실제로 끌어 옮기는 것과 버튼이 창을 최소화하는 것은 여전히 사람이 설치본에서
확인할 일이다(§5).
