# Calendar — 비율·레이아웃 설계

- 작성일: 2026-08-26
- 기준: `main` + `73d22a5`, `v0.19.2`
- 입력: [CALENDAR_APPLE_METRICS.md](CALENDAR_APPLE_METRICS.md) — 이 문서는 **측정하지 않는다.**
  그 문서가 확정한 숫자를 코드로 옮기는 **판단**만 한다.
- 관련: [CALENDAR_APPLE_DESIGN.md](CALENDAR_APPLE_DESIGN.md) — 색·형태 언어(이미 반영됨)

---

## 0. 무엇을 고치는 설계인가

측정이 끝나고 보니 갭 셋이 **같은 병**이었다.

```text
시간 행    96px       Apple 42.8px      2.24배
텍스트 줄  1.5배 행간  macOS 1.18–1.33   1.15~1.25배
미니 셀    31.7 정사각 Apple 24.2×20.6   2.01배(면적)
```

전부 "고정 크기를 크게 잡았고, 그 안의 것이 셀을 꽉 채운다"이다. Apple 쪽 원리는
반대다 — 셀은 가로로 눕고, 마커는 셀보다 작고(0.78), 격자선은 조용하다.

이 설계는 그 셋을 옮긴다. **색은 범위 밖이다** — 캡처가 Display P3이고 sRGB 변환이
아직 안 됐으므로(METRICS §1의 경고), 채도 있는 값을 지금 옮기면 틀린 값을 심는다.
회색·알파·기하값은 변환의 영향이 없으므로 여기서 다룬다.

---

## 1. 내리는 판단

### D1 · `SLOT_HEIGHT`는 48px로 간다

Apple 실측은 42.8px다. 그런데도 48을 고른다.

**첫째, 정수로 떨어진다.** `TIME_SNAP_MINUTES = 15`가 이미 상수로 있고
(`calendarTime.ts:9`), 48이면 15분 = 12px, 30분 = 24px다. 42.8이면 10.7px와 21.4px가
되고, 드래그 좌표 → 분 → 다시 픽셀로 왕복할 때마다 반올림이 낀다. 지금 코드는 그
왕복을 실제로 한다(`WeekView.tsx:296`의 `offsetY / (SLOT_HEIGHT * scale)`).

**둘째, 렌더로 확인한 차이가 작다.** 세 값을 실제로 띄워 보니(METRICS §6.1)
96 → 48이 큰 걸음이고 48 → 42.8은 작은 걸음이었다. 900px 창에서 15.63시간 대
17.52시간이다. 얻는 1.9시간보다 잃는 좌표 정합성이 크다.

~~**셋째, 우리 크롬이 Apple보다 얇다.**~~ **이 근거는 틀렸다** — Phase 2에서
확인했다. 스크롤러 안의 스티키 헤더를 그리드 높이에서 빼지 않고 센 값이었다.
그리드 위 크롬은 우리가 **더 두껍다**(Phase 2 이후 218px, 이전 258px / Apple 185px).

실제 수치로 다시 적으면, 900px 창에서 48px는 **14.21시간**이고 Apple의 42.8px는
16.71시간이다. 42.8px로 갔다면 15.93시간으로, 그래도 Apple에 못 미친다 — 차이는
행 높이가 아니라 크롬이기 때문이다. **D1의 결론은 유지된다**(첫째·둘째 근거는
그대로다). 다만 "Apple을 넘어선다"는 말은 성립하지 않는다.

> 뒤집는 조건: 소수 픽셀을 감당하는 좌표 계층이 생기거나, "Apple과 픽셀 단위로 같다"가
> 목표가 될 때. 그때는 상수 하나만 바꾸면 되고, 이 설계의 나머지는 그대로 성립한다.

### D2 · 표시 범위를 0–24시로 연다

`DAY_START`를 6에서 0으로 내린다. 근거는 Apple이 자르지 않는다는 것만이 아니다.

`WeekView.tsx:231`에 이런 코드가 있다 — 화면의 가장 이른 항목까지 그리드를 아래로
늘리는 `gridStartHour`. 주석이 이유를 적어 두었다: 06:00 이전에 끝나는 항목은
"렌더될 곳이 없어서 흔적 없이 사라졌다". **이 보정은 자르기 때문에 필요한 보정이다.**

0–24로 열면 `gridStartHour`는 항상 0이 되고 그 `useMemo`는 죽은 코드가 된다.
남겨두지 말고 **지운다.** 그리드의 첫 시각이 내용에 따라 달라지던 변수 하나가
사라지고, `topFor`·now 라인·드래그 좌표가 전부 고정 원점을 쓰게 된다.

첫 화면은 바뀌지 않는다. 마운트 시 현재 시각 −1시간(없으면 08:00)으로 스크롤하는
`useEffect`가 이미 있다(`WeekView.tsx:261`). 24시간을 열어도 사용자가 보는 첫 장면은
지금과 같다.

부수 효과: 48 × 24 = **1152px** 본문에 뷰포트 750px → 스크롤 여유 402px.
(42.8 + 6–24시 조합이었다면 770px 대 750px로 여유가 20px뿐이라
`overlayScrollbar.spec.ts`가 위태로웠다. D1과 D2는 함께 가야 한다.)

### D3 · 격자선에는 위계를 두지 않는다

측정이 뒤집은 것이 여기다. Apple은 가로·세로를 **같은 α 0.075**로 긋는다.

```text
가로 정시선   0.05  →  0.075
세로 요일선   0.08  →  그대로
30분 점선     제거
```

**30분선 제거가 먼저다.** 48px에서 30분은 24px이고, 그 간격으로 점선이 깔리면
격자가 줄무늬가 된다 — 42.8px 렌더에서 실제로 그렇게 보였다. 행 높이를 낮추는
어떤 값을 고르든 이건 선행이다.

### D4 · 다크는 같은 값이 아니라 같은 무게로 맞춘다

다크의 `--grid-line`을 0.07에서 **0.060**으로 내린다. 알파를 맞추는 게 아니라
지각 밝기를 맞춘다 — 라이트 0.075(흰 배경)와 다크 0.060(`#1C1C1E` 배경)이 둘 다
ΔL\* 6.60이다. 지금은 다크가 라이트의 1.74배로 또렷하다.

> 이 값은 **측정이 아니라 파생**이다. Apple 다크 실측이 생기면 재검토한다.
> 목표를 "Apple 라이트와 같은 지각 무게"로 잡았을 때의 답이다.

### D5 · 행간은 캘린더 안에서만 사다리로 내린다

우리 `line-height`는 전 사이즈 1.5배 고정이고, macOS는 1.18–1.33의 곡선이다.
**전역 1.5를 바꾸는 것은 이 설계의 범위 밖이다** — 모든 화면이 같이 움직인다.

캘린더 격자의 텍스트에만 macOS 값을 준다.

| 요소 | 지금 | 바꿀 값 | 근거 |
|---|---|---|---|
| `.gcal-col-weekday` | 10 / 15 | 10 / **13** | Caption |
| `.gcal-time-label` | 10 / 15 | 10 / **13** | Caption |
| `.gcal-mini-day` | 11 / 16.5 | 11 / **14** | Subheadline |
| `.gcal-mini-month-head` | 13 / 19.5 | 13 / **16** | Body |
| `.gcal-range-label` | 15 / 22.5 | 15 / **20** | Title 3 |

그리고 **사다리에 없는 크기 셋을 사다리 위로 옮긴다**: `.gcal-col-date` 16 → 15,
`.gcal-month-date` 11.2 → 11, `.gcal-year-month-title` 16 → 15.

타이틀이 날짜보다 작은 문제(15 vs 16)는 이것으로 동률이 될 뿐 Apple의 2.00배는
되지 않는다. 타이틀을 26px로 올리는 것은 캘린더만의 결정이 아니므로 **범위 밖**이다.

### D6 · 헤더는 구조가 아니라 방향을 바꾼다

68px → 28px. 앞서 "마크업 변경"이라고 봤지만, 실제로는 아니다.
`.gcal-col-head`의 두 span은 이미 형제이고 순서도 `요일 → 날짜`로 Apple의
`Sun 30`과 같다(`WeekView.tsx:607–610`).

```text
flex-direction: column → row      (2줄 스택 → 1줄 인라인)
날짜 원 32×32 → 22×22
패딩 10/8 → 6/6, gap 3 → 6
요일 줄 15px → 13px               (D5)
```

68px의 내역은 `패딩 18 + 요일 줄 15 + gap 3 + 원 32`였다. 절반은 스택 구조이고
절반은 행간 정책이라, D5와 D6은 같은 커밋에 묶는 편이 낫다.

`radiusScale.spec.ts:32`가 `.gcal-col-date`를 "원"으로 어서션하므로 가로·세로는
같게 유지한다(22×22).

### D7 · 미니 캘린더는 셀을 눕히고 마커를 줄인다

Apple은 200px 사이드바 안에서 셀 24.2 × 20.6(1.17:1), 그리드가 사이드바의 85%,
오늘 마커가 행 피치의 0.78이다. 우리는 220px 안에서 31.7 정사각, 100%, 0.94다.

**절대값이 아니라 비율을 옮긴다.** Apple의 85%와 1.17:1을 우리 220px에 적용한다.

```text
그리드 폭   220 × 0.85 = 187px  →  열 피치 26.7px  (좌우 여백 각 16px)
행 피치     26.7 ÷ 1.17 = 22.8px
오늘 마커   22.8 × 0.78 = 17.8px  →  18px
그리드 높이 요일 17 + 6 × 22.8 = 154px  (지금 207px)
```

절대값(24×21)을 그대로 쓰는 대안도 있다. 그쪽을 택하지 않는 이유는 Apple의 85%가
**비율로 표현된 판단**이기 때문이다 — 사이드바 폭이 다른데 셀 크기만 복사하면
여백이 76%로 벌어져 Apple이 의도한 밀도와 달라진다.

`verticalRhythm.spec.ts:28`이 `.gcal-mini-day`를 "자기가 앉은 그리드가 크기를 정하는
셀"로 참조하므로, 셀을 비정사각으로 만들 때 이 스펙을 함께 확인한다.

### D8 · Month는 "창에 들어간다"까지만 정한다

Month는 6주 × 128px = 768px인데 뷰포트가 752px이라 **항상 스크롤**이다.
한 달을 한 화면에서 보는 것이 존재 이유인 뷰에서 이건 결함에 가깝다.

그런데 **얼마로 줄일지의 근거가 없다.** Apple Month 캡처가 한 장도 없다(METRICS §7.1).
근거 없이 확정할 수 있는 것은 하나뿐이므로 그것만 정한다.

```text
min-height: 128px 고정  →  grid-template-rows: repeat(6, minmax(72px, 1fr))
```

즉 **행이 창에 맞춰 늘고 줄되, 72px 아래로는 내려가지 않는다.** 72px은 Apple 값이
아니라 우리 칩(18px) 세 줄 + 날짜 + 패딩이 들어가는 하한이다. 근거의 출처가 다르므로
문서에 그렇게 적는다.

> `02-calendar.css:339`의 주석이 "Spec §5.3: 128px min cell height"를 인용하지만,
> 어느 설계 문서에도 128px는 남아 있지 않다. 살아 있는 스펙과 충돌하지 않는다.

---

## 2. 단계

앞 단계가 없으면 다시 써야 하는 순서다.

### Phase 1 · 격자 (D1 · D2 · D3 · D4)

```text
SLOT_HEIGHT 96 → 48                 src/utils/calendarTime.ts
DAY_START 6 → 0                     src/utils/calendarTime.ts
gridStartHour useMemo 삭제           src/components/calendar/WeekView.tsx
30분 슬롯 요소 삭제                   WeekView.tsx:766
30분 규칙 삭제                       09-calendar-redesign.css:213, 02-calendar.css:688
--grid-line 0.05 → 0.075            09-calendar-redesign.css:129
다크 --grid-line 0.07 → 0.060       09-calendar-redesign.css:136
```

화면 구조를 건드리지 않는다. 회귀 위험은 드래그·리사이즈 좌표 하나이고,
그것이 D1에서 48을 고른 이유다.

#### Phase 1 결과 (완료)

렌더 실측: 슬롯 48px / 본문 1152px / 스크롤러 750px / 스크롤 여유 402px.
시간 행 168개(24시간 × 7열), 첫 라벨 `00:00`, DOM의 30분선 0개.
라이트·다크 모두 확인했다.

> **보이는 시간 수는 처음에 15.63으로 적었다가 Phase 2에서 13.38로 고쳤다.**
> `.gcal-timegrid-sticky`(요일 헤더 + 종일 밴드 + 리사이저, 당시 108px)가
> 스크롤러 안에 있어서 그만큼 그리드를 가린다. 보이는 그리드는 750 − 108 =
> 642px이고 642 ÷ 48 = **13.38시간**이다(이전 96px에서는 6.69시간).

**설계에서 벗어난 곳 하나 — 세로 요일선도 `--grid-line`을 쓴다.**

D3은 "세로 0.08 유지"라고 적었다. 라이트에서는 맞다(0.08 검정 on 흰색 = ΔL\* 6.39,
가로 0.075의 6.65와 비율 0.96). 그런데 세로선이 읽던 `--border-subtle`은
**전역 표면 토큰**이고 다크에서 흰색 0.08이다 — ΔL\* 8.74가 되어 테마를 바꾸는 순간
비율이 1.32로 돌아갔다. 두 선이 같은 무게라는 것이 D3의 요지이므로, **우연히 값이
비슷한 두 토큰이 아니라 같은 토큰을 읽게** 했다(`09-calendar-redesign.css`의
`.gcal-time-col`).

| | 가로 ΔL\* | 세로 ΔL\* | 비율 |
|---|---|---|---|
| 라이트 | 6.65 | 6.65 | **1.00** |
| 다크 | 6.61 | 6.61 | **1.00** |
| *(Apple 라이트)* | *6.60* | *6.60* | *1.00* |

**e2e 하나를 고쳤다 — `overlayScrollbar.spec.ts`.** 캘린더 스크롤바 테스트 둘이
`mouse.wheel(0, 240)`으로 **아래로만** 굴리고 그것이 실제 스크롤을 일으킨다고
가정하고 있었다. 하루가 6–24시 × 96px = 1728px일 때는 어디에 도착하든 아래로
여유가 있었지만, 1152px에 여유 402px가 되면서 **오후에는 마운트 시점에 이미
바닥**이라 아래로 굴려도 스크롤이 없고 바가 뜨지 않는다. 벽시계 의존은 원래
있었고 드러나지 않았을 뿐이라, 방향을 스크롤러에게 묻도록 바꿔 의존을 없앴다.

**마운트 스크롤은 유지된다.** 창 900/700/522px 모두에서 현재 시각 표시가 화면 안에
있다. 다만 오후에는 목표(현재−1시간)가 최대 스크롤을 넘어 바닥에 붙는다 — 현재
시각은 보이되 화면 아래쪽에 온다. 하루 전체를 402px 여유로 여는 이상 피할 수 없고,
지금 시각이 보이는 한 문제로 보지 않는다.

> 무관한 관찰: `overlayScrollbar.spec.ts`의 "is not drawn at all when the page fits"는
> **변경 전 코드에서도 실패했다가 재실행에서 통과**했다. 이 설계와 무관한 flaky다.

### Phase 2 · 헤더와 행간 (D5 · D6)

```text
.gcal-col-head  column → row, 패딩·gap 조정      09-calendar-redesign.css:149
.gcal-col-date  32 → 22 (3곳: 기본·is-today·is-selected)
line-height 5개 요소                             D5 표
사다리 밖 크기 3개                                col-date 15 / month-date 11 / year-title 15
```

Phase 1의 픽셀 회수(그리드 +48px)와 합쳐져야 "몇 시간 보이는가"가 최종값이 된다.

#### Phase 2 결과 (완료)

렌더 실측: 헤더 **28px**(목표 28), 날짜 알약 **22 × 22** `border-radius: 9999px`,
오늘 알약도 22 × 22. 보이는 시간 13.38 → **14.21시간**(스티키 108 → 68px).

타입 사다리는 8개 전부 macOS 값 위에 올라갔다.

| 요소 | 크기 / 행간 |
|---|---|
| `.gcal-range-label` · `.gcal-col-date` · `.gcal-year-month-title` | 15 / 20 |
| `.gcal-mini-month-head` | 13 / 16 |
| `.gcal-mini-day` · `.gcal-month-date` | 11 / 14 |
| `.gcal-col-weekday` · `.gcal-time-label` | 10 / 13 |

**대가 하나를 적어 둔다 — `data-font` 설정과 캘린더.** 앱에는 글자 크기 설정이 있고
(`[data-font]`가 루트를 13/14/15px로 바꾼다) rem 값은 그걸 따라 움직인다. 그런데
09 레이어는 이미 캘린더 격자 텍스트를 **px로 고정**하고 있었다 — 이 단계는 남아 있던
`.gcal-month-date`(0.8rem)와 `.gcal-year-month-title`을 같은 쪽으로 옮겼다.

즉 **캘린더 격자는 글자 크기 설정을 따르지 않는다.** 이 단계가 만든 문제는 아니지만
두 요소를 거기에 합류시켰다. macOS 사다리는 고정 pt 값이라 rem으로 옮기면 사다리가
깨지고, rem으로 사다리를 다시 짜는 것은 캘린더 혼자 정할 일이 아니다 —
§6의 "전역 타이포"와 같은 자리에 둔다.

### Phase 3 · 미니 캘린더 (D7)

```text
.gcal-mini-day          29.7 정사각 → 26.7 × 22.8
.gcal-mini-day.is-today 마커 29.7 → 18
.gcal-mini-month-grid   사이드바 100% → 85% + 좌우 여백
```

### Phase 4 · Month (D8)

```text
.gcal-month-cell  min-height 128 삭제
.gcal-month-grid  grid-template-rows: repeat(6, minmax(72px, 1fr))
```

### Phase 5 · 블록 미세 (색 변환 이후)

좌측 바 3px flush → 2px + 2px 인셋, 세로 2px 인셋까지는 기하값이라 지금 가능하다.
**채움 14% → 16%와 텍스트 색은 P3 변환이 끝난 뒤**로 미룬다.

---

## 3. 건드리지 않는 것

측정이 "이미 맞다"고 답한 것들이다. 이 설계의 어느 단계도 여기를 지나가지 않는다.

- 주말 열 음영 `rgba(0,0,0,0.02)` — Apple 0.024
- 이벤트 그림자 없음 — `10-calendar-apple.css:71`이 09의 그림자를 덮는다
- 이벤트 블록 가로 full-width
- 시간 거터 56px — Apple 50–52px
- 세로 요일선 0.08 — Apple 0.075
- 요일 열 유동 `1fr`
- Year 뷰 — 창에 들어가고(750 ≤ 752) 갭이 보고되지 않았다

---

## 4. 테스트

**움직일 수 있는 기존 스펙**

| 스펙 | 왜 | 판단 |
|---|---|---|
| `overlayScrollbar.spec.ts` | `.gcal-time-scroll`을 실제로 스크롤한다 | 안전 — 여유 402px |
| `radiusScale.spec.ts:32` | `.gcal-col-date`가 원이어야 한다 | 22×22 유지로 통과 |
| `verticalRhythm.spec.ts:28` | `.gcal-mini-day` 참조 | Phase 3에서 확인 |

**없어서 새로 필요한 것**

`SLOT_HEIGHT`에서 파생되는 좌표 왕복(픽셀 → 분 → 픽셀)에 단위 테스트가 하나도 없다.
어떤 테스트 파일도 `calendarTime`을 import하지 않는다. D1이 이 상수를 바꾸는
설계이므로, **Phase 1은 그 테스트를 먼저 쓰고 시작한다** — 15분 스냅이 12px 격자에
정확히 떨어지는지, 왕복이 무손실인지.

---

## 5. 완료 판정

- [x] 1440×900에서 Week 그리드에 **14시간 이상**이 보인다 — 14.21 (Phase 2 이후)
      *원안은 "15시간 이상 — 15.63"이었다. 스티키 헤더를 빼지 않은 수치였다.*
- [x] 24시간 본문이 1152px이고 스크롤 여유가 400px 이상이다 — 402px
- [x] 마운트 시 첫 화면이 현재 시각 근처에서 열린다 — 900/700/522px 모두 화면 안
- [x] `gridStartHour` 분기가 코드에서 사라졌다
- [x] 30분 보조선이 어느 배율에서도 그려지지 않는다 — DOM 0개, 토큰 제거
- [x] 가로 정시선과 세로 요일선의 ΔL\*가 라이트에서 1.0배 ± 0.1이다 — 1.00
- [x] 다크 정시선의 ΔL\*가 라이트와 같다 — 6.61 vs 6.65
- [ ] Week 헤더가 28px, 오늘 원이 22×22 원이다
- [ ] 캘린더의 모든 `line-height`가 macOS 사다리 위에 있다
- [ ] 캘린더의 모든 `font-size`가 사다리 위에 있다 (16px·11.2px가 없다)
- [ ] 미니 캘린더 셀이 정사각이 아니고, 오늘 마커가 행 피치의 0.8 이하다
- [ ] Month가 1440×900에서 스크롤 없이 6행을 보여준다
- [x] `npx tsc -b` 통과, `npx vitest run` 1806 통과 (Phase 1 시점)

검증은 측정과 같은 방법으로 한다 — Playwright로 창 높이를 바꿔가며
`.gcal-time-scroll`과 `.gcal-time-slot`의 렌더된 높이를 읽는다(METRICS §6.1).

---

## 7. 재설계 — 실제 macOS 화면이 기준이 된다 (2026-08-26)

### 7.1 새 증거

사용자의 실제 Mac에서 찍은 Week 뷰(전체화면 ~1920×1080, 한국어, 실제 일정)가 들어왔다.
Apple 가이드 캡처가 답하지 못하던 것 — **창을 키우면 행이 어떻게 되는가** — 을 답한다.

배율부터 확정한다. 이미지 안에 자가 둘 있다.

| 단서 | 이 캡처 | 가이드(1배 확정) | 판정 |
|---|---|---|---|
| 미니 캘린더 열 피치 | ~23px | 24.2px | 같다 → **1배** |
| 사이드바 폭 | ~180px | 200px | 같은 자릿수 → **1배** |

2배였다면 미니 캘린더가 48px이어야 한다. **1배다.** 그러면 관측값이 그대로 논리 픽셀이다.

```text
시간 행 간격     ~77px      (가이드의 42.8이 아니다)
그리드에 든 시간  정오 ~ 오후 11시 = 정확히 12시간
```

**§1의 "행 높이는 창 크기와 무관하게 고정"은 틀렸다.** 그 결론은 가이드 캡처 넉 장이
전부 같은 창 크기였기 때문에 나온 것이고, §1은 그 한계를 적어두긴 했지만 결론은
단정했다. 창이 두 배가 되자 행도 커졌다.

### 7.2 두 관측을 한 식으로

```text
slot = max(floor, 그리드 높이 ÷ N)      N = "한 번에 표시할 시간"(Apple 기본 12)
```

| 캡처 | 그리드 | ÷12 | floor | 결과 | 관측 |
|---|---|---|---|---|---|
| 가이드 (창 522) | 334px | 27.8 | **42.8** | 42.8 · 스크롤 | 42.8 ✓ |
| 사용자 (전체화면) | ~890px | 74.2 | — | 74.2 | ~77 ✓ |

floor가 있다는 증거는 이미 §1에 있었다 — 가이드 캡처의 그리드가 **행 중간(0.68행)에서
시작**했다. 12시간을 창에 맞춰 넣는 레이아웃이라면 스크롤이 없어야 하는데 스크롤 상태였다.
"맞춰 넣되 이보다 작아지지는 않는다"가 그 둘을 동시에 만족시킨다.

Apple 문서가 이 설정을 **6~24시간 범위**라고 적은 것과도 맞는다. N은 상수가 아니라
사용자 설정이고, 42.8px는 N의 함수가 아니라 **바닥값**이다.

> 아직 확인 못 한 것: N이 정말 12인지. 라벨 12개(정오~11시)가 근거인데, 정오 위에
> 얇은 행 조각이 보여 약간 스크롤된 상태일 수 있다. 그러면 N이 11이나 13일 수도 있다.
> **설계의 모양은 N이 몇이든 같고 상수 하나만 달라진다.**

### R1 · `SLOT_HEIGHT`는 상수에서 바닥값이 된다 (D1을 대체)

D1은 48px 상수를 골랐다. 그 판단의 전제가 "Apple도 상수"였고, 전제가 틀렸다.

```text
행 높이 = max(MIN_SLOT, 4의 배수로 맞춘(그리드 높이 ÷ HOURS_AT_A_TIME))
MIN_SLOT = 44          Apple floor 42.8에 가장 가까운 4의 배수
HOURS_AT_A_TIME = 12   Apple 기본값 (나중에 설정으로 열 수 있다)
```

**4의 배수로 맞추는 이유는 D1의 첫째 근거를 버리지 않기 위해서다.** 15분 = 행÷4이므로,
행이 4의 배수면 스냅이 정수 픽셀에 떨어진다. 계산값이라고 해서 소수를 받아들일 이유는 없다.

우리 앱에 적용하면:

| 창 | 그리드 | ÷12 | 행 높이 | 보이는 시간 |
|---|---|---|---|---|
| 1080px | 862px | 71.8 | **72** | 11.97 |
| 900px | 682px | 56.8 | **56** | 12.18 |
| 700px | 482px | 40.2 | **44**(바닥) | 10.95 |
| 522px | 306px | 25.5 | **44**(바닥) | 6.95 |

지금(48px 고정)은 900px에서 14.21시간, 1080px에서 17.96시간이다. 즉 **큰 창일수록
우리가 더 촘촘해지는데, macOS는 반대로 행이 커진다.** 사용자가 보낸 화면과 우리 화면이
가장 크게 갈린 지점이 이것이다.

**구현은 오히려 단순해진다.** 지금 코드는 이미 포인터 좌표를 **측정된 rect에서**
환산한다(`timeGridScaleFromRect`). 행 높이를 CSS가 정하게 하면
(`grid-template-rows: repeat(24, minmax(44px, 1fr))` 꼴) `SLOT_HEIGHT`는 레이아웃의
근거이기를 그만두고 **바닥값 하나**로 남는다. 좌표의 진짜 출처는 측정된 본문 높이다.

> Phase 1이 넣은 `calendarTime.test.ts`는 상수 기준으로 쓰여 있다. R1은 그 불변식을
> **"주어진 본문 높이에 대해"**로 다시 써야 한다 — 스냅이 정수 픽셀인지, 왕복이
> 무손실인지는 그대로 묻되 상수가 아니라 측정값을 넣는다.

### R2 · 타이틀 줄을 되찾는다

사용자 화면에서 가장 큰 글자는 **`2026년 8월`**이고 그리드 위 자기 줄에 있다.
우리는 `Aug 23 – Aug 29, 2026`이 툴바 안 15px다. §2.7이 이미 측정한 것 —
Apple은 타이틀이 날짜 숫자의 **2.00배** — 이 화면으로 확인됐다.

```text
타이틀   15px 툴바 안  →  26px(Large Title) 그리드 위 자기 줄
세그먼트 우측 정렬     →  가운데 (사용자 화면의 일/주/월/년)
```

D5는 "타이틀 26px은 앱 전역 결정이라 범위 밖"이라고 미뤘다. 그 판단은 유지하되,
**캘린더의 타이틀 줄은 캘린더의 것**이므로 여기서 올린다.

### R3 · 요일 헤더는 로케일 포맷을 따른다

사용자 화면은 `24일 (월)` — 숫자가 먼저, 요일이 괄호 안이다. 우리는 `MON 24`로
요일이 먼저이고 대문자다. 한국어 macOS의 표기이므로 **로케일 포맷 문제**이지 기하가
아니다. `Intl.DateTimeFormat`에 맡기면 언어에 따라 알아서 갈린다.

오늘 표시는 양쪽 다 숫자에만 원을 씌운다 — 이미 맞다(Phase 2에서 22px).

### R4 · 크롬 — 이건 제품 결정이다

사용자 화면의 왼쪽은 **사이드바 하나(~180px)뿐**이다. 전역 레일도, 오른쪽 작업 패널도
없다. 우리는 1920px에서 왼쪽 320px + 오른쪽 48px을 쓴다.

여기부터는 측정이 답할 수 없다. 레일과 작업 패널은 캘린더의 장식이 아니라 앱의 다른
기능으로 가는 길이다. 고를 수 있는 모양은 셋이다.

```text
A. 그대로 둔다        캘린더는 앱의 한 화면이지 Calendar.app이 아니다
B. 캘린더에서만 접는다  들어오면 레일·패널이 레일 폭으로 접히고, 호버/단축키로 편다
C. 없앤다             캘린더를 전체화면 모드로 (다른 화면과 IA가 갈린다)
```

**A로 정해졌다 (2026-08-26).** 레일은 남기고, 왼쪽은 그 안에서 가능한 만큼
Calendar.app의 모양으로 간다. **오른쪽 작업 패널은 그대로 둔다** — 그건 우리 것이고,
참조 화면에 없다는 것이 없앨 이유가 되지 않는다.

#### R4 결과 (완료)

```text
전  레일 50 │ 여백 32 │ 사이드바 220 │ gap 16 │ 그리드 …  318px
후  레일 50 │ 사이드바 200 │ 그리드 ……………………………  250px
```

| 항목 | 전 | 후 |
|---|---|---|
| 캘린더 사이드바 | 220px | **200px** (macOS 기본값) |
| 레일↔사이드바 여백 | 32px | **0** — 사이드바가 페이지의 가장자리가 된다 |
| 사이드바↔그리드 gap | 16px | **0** (사이드바 안쪽 12px 패딩으로 숨을 준다) |
| 미니 캘린더 | 마지막 섹션 아래 떠 있음 | **사이드바 바닥에 고정** |
| 요일 열 폭 (1920) | 207px | 217px |
| 우측 작업 패널 | 48px + gap 16 | **그대로** |

페이지 패딩은 `20-density.css`의 `.app-shell > main`이 소유하고 09보다 뒤에 로드된다.
순서로는 이길 수 없어 **특이도로** 이긴다 — `.app-shell > main:has(.gcal-page-shell)`.
캘린더만 왼쪽 변을 비우는 예외이므로 그 한 줄에만 적용된다.

남은 차이는 레일 50px다. Apple은 200, 우리는 250 — 그 50은 **일부러 남긴 차이**이고,
Calendar.app은 그 자체가 앱이지만 이건 더 큰 앱 안의 화면이기 때문이다.

### R5 · 이벤트 블록은 행이 커진 뒤에 다시 본다

사용자 화면의 블록은 제목 + 아이콘 달린 둘째 줄(장소/시각)을 갖고 있다. 지금 우리
블록은 제목 11px / 시각 10px이고, **48px 행에서는 두 줄이 안 들어간다.** 행이 56~72px가
되면 들어간다. 즉 블록 타이포는 R1 다음에 판단하는 것이 순서다.

### 7.3 단계 재편

```text
Phase 1  격자 비율          [완료] — R1이 SLOT_HEIGHT의 의미를 바꾸므로 일부 재작업
Phase 2  헤더와 행간         [완료] — 그대로 유효
Phase 3  R1 · 행 높이를 함수로 + 테스트 재작성      ← 다음
Phase 4  R2 · 타이틀 줄 + 세그먼트 가운데
Phase 5  R3 · 헤더 로케일 포맷
Phase 6  R4 · 크롬 (결정 후)
Phase 7  R5 · 블록 타이포 + 미니 캘린더(D7) + Month(D8)
```

Phase 1을 되돌리지는 않는다. 0–24시를 연 것(D2), 30분선 제거(D3), 선 알파(D3·D4)는
전부 이 재설계와 무관하게 성립한다. 바뀌는 것은 **행 높이 하나**다.

### 7.4 확인이 필요한 것

1. **`한 번에 표시할 시간` 설정값.** macOS 캘린더 → 설정 → 일반에 있다. 12인지 확인되면
   `HOURS_AT_A_TIME`이 확정된다.
2. **그 스크린샷의 PNG 파일.** 지금 수치는 화면에서 읽은 근사치다. 파일이 있으면
   가이드 캡처와 같은 방법으로 행 간격·열 폭·블록 인셋을 정확히 잰다.
3. **R4의 선택** (A / B / C).

---

## 6. 범위 밖 / 남은 결정

| 항목 | 막고 있는 것 |
|---|---|
| 이벤트 채움·글자 색 | 캡처가 P3다. sRGB 변환이 먼저 |
| 타이틀 15 → 26px | 앱 전역 타입 스케일 결정 |
| 전역 `line-height` 1.5 | 같은 이유 — 모든 화면이 움직인다 |
| 캘린더가 `data-font`를 따르지 않음 | 사다리를 rem으로 다시 짜는 일. 전역 타이포와 같은 결정 |
| 그리드 위 크롬 218px (Apple 185) | D6까지 해도 33px 두껍다. 툴바·타이틀 줄의 IA 문제 |
| 왼쪽 크롬 566px | IA 결정이지 치수가 아니다 |
| Month 행 높이의 "정답" | Apple Month 캡처. D8은 하한만 정했다 |
| Apple 다크 격자선 | Mac에서 직접 캡처. D4는 그때까지의 파생값 |
